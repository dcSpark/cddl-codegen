[campaign]
schema_version = 1
matrix_authority = "authoritative"
testing_authority = "authoritative"

[[selection]]
item_id = "testing.check-merged-d-ts-name-collisions-two-halves"
target_kind = "active_id"
selected_state = "selected"
priority_class = "regression"
selection_reason_md = """The split-half merged TypeScript name-collision regression remains a selected compile-failure boundary."""
cycle = "burndown-four"
remaining_scope_md = """Add the exact regression vector and preserve a loud collision verdict across the merged generated surface."""

[[selection]]
item_id = "testing.corpus-c-style-enum-data-type-choice-arm"
target_kind = "active_id"
selected_state = "selected"
priority_class = "coverage"
selection_reason_md = """The C-style enum data-type choice-arm coordinate is a selected corpus coverage cell."""
cycle = "burndown-four"
remaining_scope_md = """Add the coordinate to the authoritative corpus and pin its generated and execution outcomes."""

[[selection]]
item_id = "testing.corpus-preserve-pair-map-key-value-self-encoding"
target_kind = "active_id"
selected_state = "selected"
priority_class = "coverage"
selection_reason_md = """Preserve pair-map self-encoding needs a selected corpus coordinate to guard emitted bytes."""
cycle = "burndown-four"
remaining_scope_md = """Add the key/value self-encoding coordinate and verify the preserve carrier through its applicable faces."""

[[selection]]
item_id = "testing.execution-binary-wrappers-true-last-documented-flag-value"
target_kind = "active_id"
selected_state = "selected"
priority_class = "coverage"
selection_reason_md = """The final documented binary-wrappers flag value needs selected execution coverage against silent byte drift."""
cycle = "burndown-four"
remaining_scope_md = """Exercise the exact documented value and pin the resulting generated and runtime behavior."""

[[selection]]
item_id = "testing.lockstep-drift-gate-rust-ts-kebab-ident-mirror"
target_kind = "active_id"
selected_state = "selected"
priority_class = "regression"
selection_reason_md = """The Rust/TypeScript kebab-identifier mirror needs a selected lockstep regression guard."""
cycle = "burndown-four"
remaining_scope_md = """Add a drift mutation that proves either side changing alone is rejected by the lockstep gate."""

[[selection]]
item_id = "testing.pin-toolchain-verify-ts-s-nested-cargo-scratch"
target_kind = "active_id"
selected_state = "selected"
priority_class = "correctness"
selection_reason_md = """Nested verify scratch cargo must select the repository toolchain explicitly to eliminate false environmental verdicts."""
cycle = "burndown-four"
remaining_scope_md = """Pin every shell- or Bun-launched nested cargo coordinate and add a regression that distinguishes the repository pin from the ambient default."""

[[selection]]
item_id = "testing.verify-ts-refuse-unknown-flag-silently-degrading-plain"
target_kind = "active_id"
selected_state = "selected"
priority_class = "correctness"
selection_reason_md = """Unknown verify flags must fail closed instead of silently degrading to a plain run."""
cycle = "burndown-four"
remaining_scope_md = """Reject every unknown flag before execution and pin the diagnostic and non-execution behavior."""
