; A tag-wrapped fixed literal owns a one-variant nominal TypeChoice. Its direct codecs own both the
; mandatory tag and fixed value, including preserve-encoding metadata, rather than routing through
; a transparent alias or C-style enum. This fixture covers the literal route; tagged prelude
; constants have their own sibling fixture.
tagged = #6.5(5)
