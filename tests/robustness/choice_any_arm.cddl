; `any` as a TYPE-CHOICE ARM (`any / tstr`) panics generation at an `Option::unwrap()` on `None` in
; intermediate.rs — the choice's variant naming lookup has no entry for the `any` extern. DISTINCT
; from both the top-level alias `x = any` (generates; compile-class gap pinned by
; tests/matrix_reject/prelude.any.cddl) and the member-position `[any]` generic_instances assert
; (tests/robustness/any_member.cddl) — three positions, three failure sites. Shares its normalized
; panic class with choice_group_array_arm.cddl (different trigger shape, same message). Surfaced by
; the recombination sweep (src/tests/recombination_tests.rs).
a = any / tstr
