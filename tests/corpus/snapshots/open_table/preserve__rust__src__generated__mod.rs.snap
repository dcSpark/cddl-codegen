---
source: src/tests/snapshot_tests.rs
expression: content
---
// This file was code-generated using an experimental CDDL to rust tool:
// https://github.com/dcSpark/cddl-codegen

#![allow(
    clippy::too_many_arguments,
    clippy::large_enum_variant,
    clippy::result_large_err
)]

extern crate alloc;
use alloc::string::String;
use alloc::vec::Vec;
pub mod error;
pub mod non_empty_map;
pub mod ordered_hash_map;
pub mod pair_map;
extern crate derivative;
pub mod cbor_encodings;
mod extern_interface_check;
pub mod serialization;

use crate::generated::non_empty_map::NonEmptyMap;
use crate::generated::ordered_hash_map::OrderedHashMap;
use crate::generated::pair_map::PairMap;
use crate::generated::serialization::StringEncoding;
use cbor_encodings::{OpenTableDupEncoding, OpenTableEncoding, OpenTableNonEmptyEncoding};

#[derive(Clone, Debug, derivative::Derivative)]
#[derivative(
    Eq,
    PartialEq,
    Ord = "feature_allow_slow_enum",
    PartialOrd = "feature_allow_slow_enum",
    Hash
)]
pub enum Md {
    U64 {
        uint: u64,
        #[derivative(
            PartialEq = "ignore",
            Ord = "ignore",
            PartialOrd = "ignore",
            Hash = "ignore"
        )]
        uint_encoding: Option<cbor_event::Sz>,
    },
    Text {
        text: String,
        #[derivative(
            PartialEq = "ignore",
            Ord = "ignore",
            PartialOrd = "ignore",
            Hash = "ignore"
        )]
        text_encoding: StringEncoding,
    },
}

impl Md {
    pub fn new_uint(uint: u64) -> Self {
        Self::U64 {
            uint,
            uint_encoding: None,
        }
    }

    pub fn new_text(text: String) -> Self {
        Self::Text {
            text,
            text_encoding: StringEncoding::default(),
        }
    }
}

#[derive(Clone, Debug)]
pub struct OpenTable {
    /// The open table's TYPED entries (CDDL `* k1 => v1`, the first row): every map entry whose key is of this row's declared CBOR major type. Its map surface is FLATTENED onto the wasm class (`insert`/`get`/`len`/`keys`), so there is no whole-map getter. Defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container, which rejects a duplicate key.
    pub entries: OrderedHashMap<Vec<u8>, u64>,
    /// The open table's CAPTURED entries (CDDL `* k2 => v2`, the trailing catch-all row): every map entry the typed row did not claim. Defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container.
    pub rest: OrderedHashMap<Md, Md>,
    pub encodings: Option<OpenTableEncoding>,
}

impl OpenTable {
    pub fn new() -> Self {
        Self {
            entries: OrderedHashMap::new(),
            rest: OrderedHashMap::new(),
            encodings: None,
        }
    }
}

impl Default for OpenTable {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(Clone, Debug)]
pub struct OpenTableDup {
    /// The open table's TYPED entries (CDDL `* k1 => v1`, the first row): every map entry whose key is of this row's declared CBOR major type. Its map surface is FLATTENED onto the wasm class (`insert`/`get`/`len`/`keys`), so there is no whole-map getter. Defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container, which rejects a duplicate key.
    pub entries: PairMap<Vec<u8>, u64>,
    /// The open table's CAPTURED entries (CDDL `* k2 => v2`, the trailing catch-all row): every map entry the typed row did not claim. Defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container.
    pub rest: PairMap<Md, Md>,
    pub encodings: Option<OpenTableDupEncoding>,
}

impl OpenTableDup {
    pub fn new() -> Self {
        Self {
            entries: PairMap::new(),
            rest: PairMap::new(),
            encodings: None,
        }
    }
}

impl Default for OpenTableDup {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(Clone, Debug)]
pub struct OpenTableNonEmpty {
    /// The open table's required TYPED entries (CDDL `+ k1 => v1`, the first row): every map entry whose key is of this row's declared CBOR major type. This `NonEmptyMap` (or `NonEmptyPairMap` under `@duplicates preserve`) always holds at least one typed entry. Its map surface is FLATTENED onto the wasm class (`insert`/`get`/`len`/`keys`), so there is no whole-map getter.
    pub entries: NonEmptyMap<Vec<u8>, u64>,
    /// The open table's CAPTURED entries (CDDL `* k2 => v2`, the trailing catch-all row): every map entry the typed row did not claim. Defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container.
    pub rest: OrderedHashMap<Md, Md>,
    pub encodings: Option<OpenTableNonEmptyEncoding>,
}

impl OpenTableNonEmpty {
    /// * `first_key` - the key of the first typed entry (CDDL `+ k1 => v1`: an open table spelled with `+` holds at least one typed entry)
    /// * `first_value` - its value
    pub fn new(first_key: Vec<u8>, first_value: u64) -> Self {
        Self {
            entries: NonEmptyMap::new(first_key, first_value),
            rest: OrderedHashMap::new(),
            encodings: None,
        }
    }
}
