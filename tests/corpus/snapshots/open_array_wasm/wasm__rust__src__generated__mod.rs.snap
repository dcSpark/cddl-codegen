---
source: src/tests/snapshot_tests.rs
expression: content
---
// This file was code-generated using an experimental CDDL to rust tool:
// https://github.com/dcSpark/cddl-codegen

#![allow(
    clippy::too_many_arguments,
    clippy::large_enum_variant,
    clippy::result_large_err
)]

extern crate alloc;
use alloc::string::String;
use alloc::vec::Vec;
pub mod any_cbor;
pub mod error;
mod extern_interface_check;
pub mod serialization;

/// Open array with an ignored rest tail: tolerates unknown trailing array elements on deserialize and DROPS them, and re-serializes only the declared members. Byte round-trips do NOT hold for wire data that carried extra trailing elements.
#[derive(Clone, Debug)]
pub struct AllMandatoryIgnore {
    pub index_0: u64,
    pub index_1: String,
}

impl AllMandatoryIgnore {
    pub fn new(index_0: u64, index_1: String) -> Self {
        Self { index_0, index_1 }
    }
}

#[derive(Clone, Debug)]
pub struct Anytail {
    pub index_0: u64,
    /// Captured trailing array elements beyond the declared members (CDDL `* t` rest tail). Serialized after the declared members; defaults empty.
    pub rest: Vec<crate::generated::any_cbor::AnyCbor>,
}

impl Anytail {
    pub fn new(index_0: u64) -> Self {
        Self {
            index_0,
            rest: Vec::new(),
        }
    }
}

/// Open array with an ignored rest tail: tolerates unknown trailing array elements on deserialize and DROPS them, and re-serializes only the declared members. Byte round-trips do NOT hold for wire data that carried extra trailing elements.
#[derive(Clone, Debug)]
pub struct Ignored {
    pub index_0: u64,
}

impl Ignored {
    pub fn new(index_0: u64) -> Self {
        Self { index_0 }
    }
}

#[derive(Clone, Debug)]
pub struct Named {
    pub index_0: u64,
    pub index_1: String,
    /// Captured trailing array elements beyond the declared members (CDDL `* t` rest tail). Serialized after the declared members; defaults empty.
    pub extras: Vec<Vec<u8>>,
}

impl Named {
    pub fn new(index_0: u64, index_1: String) -> Self {
        Self {
            index_0,
            index_1,
            extras: Vec::new(),
        }
    }
}

#[derive(Clone, Debug)]
pub struct OptFixedMember {
    pub index_0: u64,
    /// Whether the optional fixed value `5` (CDDL `? index_1: 5`) is present; `false` means absent.
    pub index_1: bool,
    /// Captured trailing array elements beyond the declared members (CDDL `* t` rest tail). Serialized after the declared members; defaults empty.
    pub rest: Vec<String>,
}

impl OptFixedMember {
    pub fn new(index_0: u64) -> Self {
        Self {
            index_0,
            index_1: false,
            rest: Vec::new(),
        }
    }
}

#[derive(Clone, Debug)]
pub struct SingleFixed {
    pub index_0: u64,
    /// Captured trailing array elements beyond the declared members (CDDL `* t` rest tail). Serialized after the declared members; defaults empty.
    pub rest: Vec<u64>,
}

impl SingleFixed {
    pub fn new(index_0: u64) -> Self {
        Self {
            index_0,
            rest: Vec::new(),
        }
    }
}

#[derive(Clone, Debug)]
pub struct Typed {
    pub index_0: u64,
    pub index_1: String,
    /// Captured trailing array elements beyond the declared members (CDDL `* t` rest tail). Serialized after the declared members; defaults empty.
    pub rest: Vec<u64>,
}

impl Typed {
    pub fn new(index_0: u64, index_1: String) -> Self {
        Self {
            index_0,
            index_1,
            rest: Vec::new(),
        }
    }
}
