---
source: src/tests/snapshot_tests.rs
expression: content
---
// This file was code-generated using an experimental CDDL to rust tool:
// https://github.com/dcSpark/cddl-codegen

#![allow(
    clippy::too_many_arguments,
    clippy::large_enum_variant,
    clippy::result_large_err
)]

pub mod error;
pub mod ordered_hash_map;
pub mod ordered_set;
extern crate derivative;
pub mod cbor_encodings;
mod extern_interface_check;
pub mod serialization;

use crate::generated::ordered_set::OrderedSet;
use cbor_encodings::{HeterogeneousEncoding, HolderEncoding, HomogeneousEncoding};

/// `@duplicates reject`: a repeated element is refused (a `DuplicateKey` error) on both the wire and the API; accepted (duplicate-free) input re-emits byte-exactly in wire order (the set is order-preserving, never sorted).
pub type ElemTagged = OrderedSet<u64>;

#[derive(Clone, Debug)]
pub struct Heterogeneous {
    pub(crate) inner: OrderedSet<u64>,
    pub encodings: Option<HeterogeneousEncoding>,
}

impl Heterogeneous {
    pub fn get(&self) -> &OrderedSet<u64> {
        &self.inner
    }

    pub fn new(inner: OrderedSet<u64>) -> Self {
        Self {
            inner,
            encodings: None,
        }
    }
}

impl From<OrderedSet<u64>> for Heterogeneous {
    fn from(inner: OrderedSet<u64>) -> Self {
        Heterogeneous::new(inner)
    }
}

impl From<Heterogeneous> for OrderedSet<u64> {
    fn from(wrapper: Heterogeneous) -> Self {
        wrapper.inner
    }
}

/// The tag-258 set idiom: the tag is an encoding detail — both the `#6.258(...)` and the bare-array wire forms are accepted (serialization defaults to tagged), so either round-trips byte-exactly.
/// `@duplicates reject`: a repeated element is refused (a `DuplicateKey` error) on both the wire and the API; accepted (duplicate-free) input re-emits byte-exactly in wire order (the set is order-preserving, never sorted).
pub type HeterogeneousSet = OrderedSet<u64>;

#[derive(Clone, Debug)]
pub struct Holder {
    pub h: Homogeneous,
    pub s: Heterogeneous,
    pub a: OuterOverArray,
    pub encodings: Option<HolderEncoding>,
}

impl Holder {
    pub fn new(h: Homogeneous, s: Heterogeneous, a: OuterOverArray) -> Self {
        Self {
            h,
            s,
            a,
            encodings: None,
        }
    }
}

#[derive(Clone, Debug)]
pub struct Homogeneous {
    pub(crate) inner: OrderedSet<u64>,
    pub encodings: Option<HomogeneousEncoding>,
}

impl Homogeneous {
    pub fn get(&self) -> &OrderedSet<u64> {
        &self.inner
    }

    pub fn new(inner: OrderedSet<u64>) -> Self {
        Self {
            inner,
            encodings: None,
        }
    }
}

impl From<OrderedSet<u64>> for Homogeneous {
    fn from(inner: OrderedSet<u64>) -> Self {
        Homogeneous::new(inner)
    }
}

impl From<Homogeneous> for OrderedSet<u64> {
    fn from(wrapper: Homogeneous) -> Self {
        wrapper.inner
    }
}

/// `@duplicates reject`: a repeated element is refused (a `DuplicateKey` error) on both the wire and the API; accepted (duplicate-free) input re-emits byte-exactly in wire order (the set is order-preserving, never sorted).
pub type HomogeneousInner = OrderedSet<u64>;

pub type OuterOverArray = Vec<ElemTagged>;
