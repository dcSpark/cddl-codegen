---
source: src/tests/snapshot_tests.rs
expression: content
---
// This file was code-generated using an experimental CDDL to rust tool:
// https://github.com/dcSpark/cddl-codegen

#![allow(
    clippy::too_many_arguments,
    clippy::large_enum_variant,
    clippy::result_large_err
)]

extern crate alloc;
use alloc::string::String;
use alloc::vec::Vec;
pub mod bounded;
pub mod error;
mod extern_interface_check;
pub mod json_schema_gen;
pub mod json_value_ser;
pub mod non_empty;
pub mod open_struct_rest_json;
pub mod serialization;
pub mod static_array;

use crate::generated::bounded::BoundedVec;
use crate::generated::non_empty::NonEmptyVec;

/// `[2*5 uint]`: inclusive length window enforced at the `BoundedVec` `TryFrom<Vec<_>>` door (the CBOR decoder routes through the same door, so wire-side and API-side rejection are identical).
pub type Bounded = BoundedVec<u64, 2, 5>;

/// `[2*2 uint]`: exact static `[T; N]` carrier; the list wrapper's `TryFrom<Vec<_>>` door performs the checked Vec-to-array handover (and the CBOR decoder crosses that same door).
pub type Exact = [u64; 2];

#[derive(Clone, Debug, serde::Deserialize, serde::Serialize, schemars::JsonSchema)]
pub struct ExactMiddleOccurrence {
    pub prefix: u64,
    pub suffix: u64,
    /// Captured exact-count occurrence-segment elements before the mandatory fixed suffix, whose inclusive CDDL occurrence window is enforced by this checked carrier. Serialized at its authored source position; supplied complete to `new()`.
    pub rest: [u64; 2],
}

impl ExactMiddleOccurrence {
    /// * `rest` - the complete checked exact-count occurrence-segment carrier before its mandatory fixed suffix (its CDDL occurrence window is enforced by this carrier)
    pub fn new(prefix: u64, suffix: u64, rest: [u64; 2]) -> Self {
        Self {
            prefix,
            suffix,
            rest,
        }
    }
}

/// `[2* uint]`: inclusive length window enforced at the `BoundedVec` `TryFrom<Vec<_>>` door (the CBOR decoder routes through the same door, so wire-side and API-side rejection are identical).
pub type Lower = BoundedVec<u64, 2, { u64::MAX }>;

/// `[? uint]`: inclusive length window enforced at the `BoundedVec` `TryFrom<Vec<_>>` door (the CBOR decoder routes through the same door, so wire-side and API-side rejection are identical).
pub type Maybe = BoundedVec<u64, 0, 1>;

#[derive(Clone, Debug, serde::Deserialize, serde::Serialize, schemars::JsonSchema)]
pub struct MiddleOccurrence {
    pub prefix: u64,
    pub suffix: String,
    /// Captured major-disjoint occurrence-segment elements before the mandatory fixed suffix (CDDL `* t`). Serialized at its authored source position; defaults empty.
    #[serde(skip_serializing_if = "Vec::is_empty", default)]
    pub rest: Vec<Vec<u8>>,
}

impl MiddleOccurrence {
    pub fn new(prefix: u64, suffix: String) -> Self {
        Self {
            prefix,
            suffix,
            rest: Vec::new(),
        }
    }
}

/// `[2*3 [*2 uint]]`: inclusive length window enforced at the `BoundedVec` `TryFrom<Vec<_>>` door (the CBOR decoder routes through the same door, so wire-side and API-side rejection are identical).
pub type NestedBounded = BoundedVec<BoundedVec<u64, 0, 2>, 2, 3>;

pub type NestedUpper = Vec<BoundedVec<u64, 0, 2>>;

#[derive(Clone, Debug, serde::Deserialize, serde::Serialize, schemars::JsonSchema)]
pub struct OccurrenceHolder {
    pub o: OneOrMore,
    pub b: Bounded,
    pub upper: Upper,
    pub lower: Lower,
    pub maybe: Maybe,
    #[serde(with = "crate::generated::static_array::static_array")]
    #[schemars(schema_with = "crate::generated::static_array::static_array_schema::<u64, 2>")]
    pub exact: Exact,
    pub nested: NestedUpper,
    pub nested_bounded: NestedBounded,
    pub inline_bounded: BoundedVec<String, 1, 3>,
    pub inline_upper: BoundedVec<u64, 0, 5>,
    pub inline_lower: BoundedVec<u64, 2, { u64::MAX }>,
    pub inline_maybe: BoundedVec<u64, 0, 1>,
    #[serde(with = "crate::generated::static_array::static_array")]
    #[schemars(schema_with = "crate::generated::static_array::static_array_schema::<u64, 2>")]
    pub inline_exact: [u64; 2],
}

impl OccurrenceHolder {
    pub fn new(
        o: OneOrMore,
        b: Bounded,
        upper: Upper,
        lower: Lower,
        maybe: Maybe,
        exact: Exact,
        nested: NestedUpper,
        nested_bounded: NestedBounded,
        inline_bounded: BoundedVec<String, 1, 3>,
        inline_upper: BoundedVec<u64, 0, 5>,
        inline_lower: BoundedVec<u64, 2, { u64::MAX }>,
        inline_maybe: BoundedVec<u64, 0, 1>,
        inline_exact: [u64; 2],
    ) -> Self {
        Self {
            o,
            b,
            upper,
            lower,
            maybe,
            exact,
            nested,
            nested_bounded,
            inline_bounded,
            inline_upper,
            inline_lower,
            inline_maybe,
            inline_exact,
        }
    }
}

/// `[+ u64]`: at least one element, enforced at the `NonEmptyVec` `TryFrom<Vec<_>>` door (the CBOR decoder routes through the same door, so wire-side and API-side rejection are identical).
pub type OneOrMore = NonEmptyVec<u64>;

/// `[*5 uint]`: inclusive length window enforced at the `BoundedVec` `TryFrom<Vec<_>>` door (the CBOR decoder routes through the same door, so wire-side and API-side rejection are identical).
pub type Upper = BoundedVec<u64, 0, 5>;
