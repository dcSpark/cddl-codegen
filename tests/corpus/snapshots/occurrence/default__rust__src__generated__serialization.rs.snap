---
source: src/tests/snapshot_tests.rs
expression: content
---
// This file was code-generated using an experimental CDDL to rust tool:
// https://github.com/dcSpark/cddl-codegen

extern crate alloc;
use super::*;
use crate::generated::error::*;
use alloc::vec::Vec;
use cbor_event::de::Deserializer;
use cbor_event::se::{Serialize, Serializer};

impl cbor_event::se::Serialize for OccurrenceHolder {
    fn serialize<'se>(
        &self,
        serializer: &'se mut Serializer,
    ) -> cbor_event::Result<&'se mut Serializer> {
        serializer.write_array(cbor_event::Len::Len(3))?;
        serializer.write_array(cbor_event::Len::Len(self.o.len() as u64))?;
        for element in self.o.iter() {
            serializer.write_unsigned_integer(*element)?;
        }
        serializer.write_array(cbor_event::Len::Len(self.b.len() as u64))?;
        for element in self.b.iter() {
            serializer.write_unsigned_integer(*element)?;
        }
        serializer.write_array(cbor_event::Len::Len(self.inline_bounded.len() as u64))?;
        for element in self.inline_bounded.iter() {
            serializer.write_text(element)?;
        }
        Ok(serializer)
    }
}

impl Deserialize for OccurrenceHolder {
    fn deserialize(raw: &mut Deserializer) -> Result<Self, DeserializeError> {
        (|| -> Result<_, DeserializeError> {
            let len = raw.array()?;
            let mut read_len = CBORReadLen::from(len);
            read_len.read_elems(3)?;
            read_len.finish()?;
            let o = (|| -> Result<_, DeserializeError> {
                let mut o_arr = Vec::new();
                let len = raw.array()?;
                while match len {
                    cbor_event::Len::Len(n) => (o_arr.len() as u64) < n,
                    cbor_event::Len::Indefinite => true,
                } {
                    if matches!(len, cbor_event::Len::Indefinite)
                        && raw.cbor_type()? == cbor_event::Type::Special
                        && raw.special_break()?
                    {
                        break;
                    }
                    o_arr.push(raw.unsigned_integer()?);
                }
                let o_arr = NonEmptyVec::try_from(o_arr)?;
                Ok(o_arr)
            })()
            .map_err(|e| e.annotate("o"))?;
            let b = (|| -> Result<_, DeserializeError> {
                let mut b_arr = Vec::new();
                let len = raw.array()?;
                while match len {
                    cbor_event::Len::Len(n) => (b_arr.len() as u64) < n,
                    cbor_event::Len::Indefinite => true,
                } {
                    if matches!(len, cbor_event::Len::Indefinite)
                        && raw.cbor_type()? == cbor_event::Type::Special
                        && raw.special_break()?
                    {
                        break;
                    }
                    b_arr.push(raw.unsigned_integer()?);
                }
                if b_arr.len() < 2 || b_arr.len() > 5 {
                    return Err(DeserializeFailure::RangeCheck {
                        found: b_arr.len() as i128,
                        min: Some(2),
                        max: Some(5),
                    }
                    .into());
                }
                Ok(b_arr)
            })()
            .map_err(|e| e.annotate("b"))?;
            let inline_bounded = (|| -> Result<_, DeserializeError> {
                let mut inline_bounded_arr = Vec::new();
                let len = raw.array()?;
                while match len {
                    cbor_event::Len::Len(n) => (inline_bounded_arr.len() as u64) < n,
                    cbor_event::Len::Indefinite => true,
                } {
                    if matches!(len, cbor_event::Len::Indefinite)
                        && raw.cbor_type()? == cbor_event::Type::Special
                        && raw.special_break()?
                    {
                        break;
                    }
                    inline_bounded_arr.push(raw.text()?);
                }
                if inline_bounded_arr.len() < 1 || inline_bounded_arr.len() > 3 {
                    return Err(DeserializeFailure::RangeCheck {
                        found: inline_bounded_arr.len() as i128,
                        min: Some(1),
                        max: Some(3),
                    }
                    .into());
                }
                Ok(inline_bounded_arr)
            })()
            .map_err(|e| e.annotate("inline_bounded"))?;
            match len {
                cbor_event::Len::Len(_) => (),
                cbor_event::Len::Indefinite => match raw.special()? {
                    cbor_event::Special::Break => (),
                    _ => return Err(DeserializeFailure::EndingBreakMissing.into()),
                },
            }
            Ok(OccurrenceHolder {
                o,
                b,
                inline_bounded,
            })
        })()
        .map_err(|e| e.annotate("OccurrenceHolder"))
    }
}
