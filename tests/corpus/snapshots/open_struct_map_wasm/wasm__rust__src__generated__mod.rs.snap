---
source: src/tests/snapshot_tests.rs
expression: content
---
// This file was code-generated using an experimental CDDL to rust tool:
// https://github.com/dcSpark/cddl-codegen

#![allow(
    clippy::too_many_arguments,
    clippy::large_enum_variant,
    clippy::result_large_err
)]

pub mod any_cbor;
pub mod error;
mod extern_interface_check;
pub mod pair_map;
pub mod serialization;

use crate::generated::pair_map::PairMap;
use std::collections::BTreeMap;

#[derive(Clone, Debug)]
pub struct AnyRest {
    pub key_1: u64,
    /// Captured open-map entries whose keys are not declared fields (CDDL `* k => v` rest row). Serialized after the declared fields; defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container.
    pub rest: BTreeMap<crate::generated::any_cbor::AnyCbor, crate::generated::any_cbor::AnyCbor>,
}

impl AnyRest {
    pub fn new(key_1: u64) -> Self {
        Self {
            key_1,
            rest: BTreeMap::new(),
        }
    }
}

#[derive(Clone, Debug)]
pub struct DupRest {
    pub key_7: u64,
    /// Captured open-map entries whose keys are not declared fields (CDDL `* k => v` rest row). Serialized after the declared fields; defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container.
    pub rest: PairMap<u64, String>,
}

impl DupRest {
    pub fn new(key_7: u64) -> Self {
        Self {
            key_7,
            rest: PairMap::new(),
        }
    }
}

#[derive(Clone, Debug)]
pub struct OptRest {
    pub key_3: Option<String>,
    /// Captured open-map entries whose keys are not declared fields (CDDL `* k => v` rest row). Serialized after the declared fields; defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container.
    pub rest: BTreeMap<u64, crate::generated::any_cbor::AnyCbor>,
}

impl OptRest {
    pub fn new() -> Self {
        Self {
            key_3: None,
            rest: BTreeMap::new(),
        }
    }
}

impl Default for OptRest {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(Clone, Debug)]
pub struct RenamedRest {
    pub key_1: u64,
    /// Captured open-map entries whose keys are not declared fields (CDDL `* k => v` rest row). Serialized after the declared fields; defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container.
    pub extra: BTreeMap<u64, crate::generated::any_cbor::AnyCbor>,
}

impl RenamedRest {
    pub fn new(key_1: u64) -> Self {
        Self {
            key_1,
            extra: BTreeMap::new(),
        }
    }
}

#[derive(Clone, Debug)]
pub struct TypedRest {
    pub key_1: u64,
    /// Captured open-map entries whose keys are not declared fields (CDDL `* k => v` rest row). Serialized after the declared fields; defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container.
    pub rest: BTreeMap<u64, u64>,
}

impl TypedRest {
    pub fn new(key_1: u64) -> Self {
        Self {
            key_1,
            rest: BTreeMap::new(),
        }
    }
}

#[derive(Clone, Debug)]
pub struct UintRest {
    pub key_1: u64,
    pub key_2: String,
    /// Captured open-map entries whose keys are not declared fields (CDDL `* k => v` rest row). Serialized after the declared fields; defaults empty. `@duplicates preserve` makes this a `PairMap` (duplicate keys kept, in wire order); otherwise the loose table container.
    pub rest: BTreeMap<u64, crate::generated::any_cbor::AnyCbor>,
}

impl UintRest {
    pub fn new(key_1: u64, key_2: String) -> Self {
        Self {
            key_1,
            key_2,
            rest: BTreeMap::new(),
        }
    }
}
