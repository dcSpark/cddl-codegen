; A non-final optional ARRAY-record member whose wire major overlaps its mandatory successor.
; RFC 8610's greedy occurrence matching makes `[uint]` invalid rather than an absent optional;
; cddl-codegen therefore retains this as a serialize-only refusal fixture with no generated decoder.
optional_overlap_array = [? a: uint, b: uint]
