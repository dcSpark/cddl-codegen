[document]
schema_version = 2
authority = "authoritative"
roadmap = "testing"
source_path = "tests/testing-roadmap.toml"
projection_path = "tests/TESTING_ROADMAP.md"
frozen_source_sha256 = "b9115ada896726060e02bc5722bc8568650b0d64f73522f5affbb30b4120d70e"
frozen_source_byte_length = 323398
frozen_source_line_count = 3597
frozen_source_eof = "lf"
projection_layout = "anchors_v1"

[[section]]
section_id = "declined-boundaries"
title = "Declined (decided, with a reopening signal unless explicitly permanent)"
render_authority = "semantic"
body_md = """## Declined (decided, with a reopening signal unless explicitly permanent)

"""

[[section.source_replacement]]
span_id = "span-section-declined-boundaries"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact section bytes are semantic document prose.
"""

[[section]]
section_id = "deferred-features"
title = "Deferred features (build when a real consumer needs them)"
render_authority = "semantic"
body_md = """## Deferred features (build when a real consumer needs them)

"""

[[section.source_replacement]]
span_id = "span-section-deferred-features"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact section bytes are semantic document prose.
"""

[[section]]
section_id = "next-priority"
title = "Next work items, in priority order"
render_authority = "semantic"
body_md = """## Next work items, in priority order

"""

[[section.source_replacement]]
span_id = "span-section-next-priority"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact section bytes are semantic document prose.
"""

[[section]]
section_id = "north-star"
title = "North star — automated feature coverage"
render_authority = "semantic"
body_md = """## North star — automated feature coverage

The direction all of this points toward: **an automatic flow that ensures every CDDL construct worth
supporting works properly through the generated library — verified by execution, not by hand.**
Concretely: enumerate the feature space (`cddl-matrix/` already does this), then for each construct
*generate → compile → execute* (round-trip real values, reject invalid ones), and treat every
failure as one of two things — a construct we deliberately don't support (documented, not a gap) or a
bug to fix — closing the loop until coverage is complete and self-checking. \"Worth supporting\" is
load-bearing: some constructs (`#`/`cbor-any`, `float16`, socket plugs, …) are design decisions to
*exclude*, not holes to grind toward 100% — and the exclude list is revisable: `any` (the prelude
name) moved OFF it 2026-07-23 (maintainer-ruled feature), and the loose-CBOR family that followed
it is shipped CURRENT STATE, not roadmap — first-class `any` on every surface, open struct-maps
(trailing `* K => V` rest rows, capture and `@ignore` tolerate-and-drop flavors), and open arrays
(final-position loose `* t` and non-empty `+ t` / `1* t` rest tails; `@ignore` applies only to the
loose flavor). Per-layer test maps: `tests/README.md` § \"Open
struct-maps (rest rows)\", § \"Open struct-maps — the `@ignore` (tolerate-and-drop) flavor\", and
§ \"Open arrays (rest tails)\"; user-facing contracts: `docs/docs/current_capacities.mdx` /
`output_format.mdx` / `comment_dsl.mdx` / `wasm_differences.mdx`. The family's v1 boundaries
(non-final/multiple/plain-group rest rows, bounded occurrences, and the two key domains a row
cannot key on — float-containing, which has no total order, and `null`-admitting, which the row's
key dispatch cannot tell from an indefinite map's break) are graceful rejections whose
candidate-feature entries live in `cddl-matrix/ROADMAP.md` § findings, not here. The permanent exclusions around it stay: `#` (`Type2::Any`), `cbor-any`, `@newtype` and
control operators on `any`, and non-last bare `any` choice arms (forced-backtracking catch-all unions
put the catch-all last, so an earlier one is unreachable dead code) — see the `any` type-choice entry
in `cddl-matrix/ROADMAP.md`. One deferred coverage item remains: the wasm emit-tests minter has no
`any` ctor path (`src/emit_tests_wasm.rs`, `Any => None`), so a minted `any` value gets no wasm-side
round-trip differential (the rust leg's mint covers it); lifting it follows demand, since the wasm
`AnyCbor` wrapper is byte-oriented and exposes no value-destructuring constructor.

The value is in removing the human from that loop without losing trust: the gates must be strict
enough that \"the matrix says this feature is supported\" *means* it generates, compiles (rust **and**
wasm), and round-trips. The historical verdict meant only \"rust generates and rust-`cargo check`s,\"
which is why real bugs (inverted `nint` bound, wasm `get`/`add` type mismatch, JSON-schema turbofish)
shipped green. That oracle now exists as a set of live systems — execution-gated support verdicts at
matrix/corpus/suite breadth, per-non-default-profile probes and foreign spec-derived decode vectors,
identity-carrying decode evidence (reason-asserted rejections, spec-equal re-encoding replays,
header mutants asserting an error location that names the decoding type), and an armed
over-acceptance promotion flow whose both branches have fired. All of that is *current state*,
documented in `tests/README.md` (suite-side; § \"Decode-direction conformance\" for the evidence
contract and its validation layers) and `cddl-matrix/README.md` (probe-side; § \"Directional support
evidence\"). The json/wasm decode surfaces carry the same obligation now (the json/wasm surface legs
on both replay gates — `tests/README.md` § \"json/wasm surface legs\"). What remains on this axis lives
in the sections below (the fuzzer escalations, the recur-first residuals), not here.

"""

[[section.source_replacement]]
span_id = "span-section-north-star"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact section bytes are semantic document prose.
"""

[[section]]
section_id = "operational-watches"
title = "Operational watches"
render_authority = "semantic"
body_md = """## Operational watches

"""

[[section.source_replacement]]
span_id = "span-section-operational-watches"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact section bytes are semantic document prose.
"""

[[section]]
section_id = "pending-maintainer"
title = "Pending maintainer action"
render_authority = "semantic"
body_md = """## Pending maintainer action

"""

[[section.source_replacement]]
span_id = "span-section-pending-maintainer"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact section bytes are semantic document prose.
"""

[[section]]
section_id = "preamble"
title = "Testing roadmap preamble"
render_authority = "semantic"
body_md = """# Testing roadmap — what to do next

A curated, opinionated plan for the *next* testing push, so a future effort has a solid starting
point. This is the short list (what we'd actually do, in order), distilled from an independent
multi-agent review (`draft/testing-recommendations/RECOMMENDATIONS.md` — the exhaustive menu) plus
findings from building the current suite. See `tests/README.md` for how the current setup works and
what's already covered.

**CI policy — fast tier only.** CI runs exactly `bun run check.ts fast` (the absolute-minimum
commit gate; cost policy — see `AGENTS.md`), enforced by check.ts's `self_checks`. No item below
goes into the fast tier: new verification systems land in check.ts's `local` or `full` tier and run
locally. Promoting anything into `fast` is a maintainer decision.

How to read this file: work is grouped by what unblocks it, not by a single priority number.
\"Pending maintainer action\" needs a human decision or a long unattended run; \"Next work items\" are
buildable now; \"Standing-system residuals\" are recur-first ledger entries (each names its trigger
and the mechanical layer to build when the trigger fires — building early is deliberate
over-engineering); \"Deferred features\" wait on a real consumer need; \"Operational watches\" are
known noise sources with a capture discipline; \"Declined\" records what we decided NOT to build and
the signal that would reopen the decision.

"""

[[section.source_replacement]]
span_id = "span-section-preamble"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact section bytes are semantic document prose.
"""

[[section]]
section_id = "sources"
title = "Sources"
render_authority = "semantic"
body_md = """## Sources
"""

[[section.source_replacement]]
span_id = "span-section-sources"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact section bytes are semantic document prose.
"""

[[section]]
section_id = "standing-system"
title = "Standing-system residuals"
legacy_aliases = ["Standing-system residuals"]
render_authority = "semantic"
body_md = """Each entry here is a ledger record for a proven-once failure class: what happened, which standing
system (or working rule) owns it meanwhile, and the trigger — usually a SECOND instance — that
justifies building the named mechanical layer. Building before the trigger fires is deliberate
over-engineering; deleting an entry without either building the layer or recording why the class is
dead loses the lesson. When a trigger DOES fire, land the DETECTOR half of the named layer before
the FIX half, and let the detector's arming-run measurement re-test the entry's premises before
implementing its sketched remedy: residue predictions and escalation sketches are recorded against
premises nobody has tested — three falsified-premise instances are on record: the extreme-value
entry's \"generation already uses `_sz` where the `i64` limit bites\" premise (falsified by the
`FixedValue::to_bytes` bug its own entry had named as a mere range cap); the unused-imports entry's
\"near zero\" residue prediction (104 warning cells at the arming run, from a generated-only-view
class its sketched glob-EDGE escalation could not have seen); and the optional-fixed-FLOAT findings
entry's \"data-lossy but non-crashing\" premise (the silent drop was real only for a float ALONGSIDE
other dynamic-length optionals — an ISOLATED one FAILED generation loudly, so the entry's
certify-or-fix fork mis-modeled exactly the shape an enumeration naturally picks; delivered by
`tests/corpus/optional_fixed_float.cddl` + the float presence-field arms, with the preserve residue
since delivered too — a float's head width is an encoding variable, pinned by
`preserve_encodings_supports_floats`). A fourth is on record and is the reason a cited GATE counts as
a premise too: two entries named `all_supported_constructs_generate_all_profiles` as the disk-writing
gate their remedy would extend, and it writes nothing — it drives `api::generated_strings` in-process
over `tests/matrix_supported/`. Probe the mechanism of any gate a remedy is built on, not its name.

**Entries whose trigger has FIRED are work items, not deferrals, and they are listed here so the
section's recur-first premise stays honest.** Cited count-free and by exact title, because a
hand-maintained tally of this list is itself the rot class `cddl-matrix/ROADMAP.md` § Maintenance
records — this one went stale twice in a single day of deliveries before the number came out. The
full record stays in place below: \"A \"no gate demands this\" premise probed against ONE gate
is not evidence about a gate in another TIER\" (the mechanical half is a maintainer call — it edits
`check.ts` itself).

"""

[[section.source_replacement]]
span_id = "span-section-standing-system"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact section bytes are semantic document prose.
"""

[[fragment]]
fragment_id = "sources-exhaustive-menu"
projection_group = "sources"
title = "Full exhaustive menu (24 ranked items + blind spots): `draft/testing-recommendations/RECOMMENDATIONS.md`"
render_authority = "semantic"
lifecycle_disposition = "document_prose"
body_md = """- Full exhaustive menu (24 ranked items + blind spots): `draft/testing-recommendations/RECOMMENDATIONS.md`
"""

[[fragment.source_replacement]]
span_id = "span-fragment-sources-exhaustive-menu"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact fragment bytes are semantic document prose.
"""

[[fragment]]
fragment_id = "sources-expert-writeups"
projection_group = "sources"
title = "Per-dimension expert write-ups: `draft/testing-recommendations/*.md`"
render_authority = "semantic"
lifecycle_disposition = "document_prose"
body_md = """- Per-dimension expert write-ups: `draft/testing-recommendations/*.md`
"""

[[fragment.source_replacement]]
span_id = "span-fragment-sources-expert-writeups"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact fragment bytes are semantic document prose.
"""

[[record]]
id = "testing.add-profile-wrapper-participation-grid-participation-differs-across"
title = "Add a PROFILE axis to the wrapper-participation grid only when participation differs across profiles."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """Add a PROFILE axis to the wrapper-participation grid only when participation differs across profiles.
"""
detail_md = """- **Add a PROFILE axis to the wrapper-participation grid only when participation differs across
  profiles.** The current default-profile table and its focused preserve controls are documented in
  `tests/README.md` § “The wrapper-participation grid”. A full preserve/JSON multiplication is
  deferred because it multiplies every row to test container/derive changes rather than placement,
  and no reported row changes its `Defer` / `Borrow` / `Host` / `Local` outcome by profile.
  Reopening signal: one spec whose `collections.rs` indexes show a different participation outcome
  under two profiles. At that point add `--preserve-encodings` and `--json-serde-derives` profiles
  to the data table and its compile floors. The cross-crate duplicate-symbol flavor remains owned
  here: the duplicate-ident backstop scans one crate's own files, while
  `synthesized_name_interaction_sweep` has no dependency-index cells.
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.add-profile-wrapper-participation-grid-participation-differs-across.
"""
authority_reference_id = "ref-testing-add-profile-wrapper-participation-grid-participation-differs-across-authority"
permanence = "reopenable"
transition_ids = ["testing.trigger.add-profile-wrapper-participation-grid-participation-differs-across"]

[[record.source_replacement]]
span_id = "span-record-add-profile-wrapper-participation-grid-participation-differs-across"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.admission-path.build-registration-class-reference-position-sweep-second-escape"
title = "Admission for testing.build-registration-class-reference-position-sweep-second-escape."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_system_admission"
summary_md = """Admission for testing.build-registration-class-reference-position-sweep-second-escape.
"""
admission_kind = "independent_recurrence"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_ids = ["testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one-proof", "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two-proof"]
incident_ids = ["testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one", "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two"]

[[record]]
id = "testing.admission-path.directive-effect-round-trip-coherence-sweep-accepted-custom"
title = "Admission for testing.directive-effect-round-trip-coherence-sweep-accepted-custom."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_system_admission"
summary_md = """Admission for testing.directive-effect-round-trip-coherence-sweep-accepted-custom.
"""
admission_kind = "silent_corruption"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_ids = ["testing.proof.directive-effect-round-trip-coherence-sweep-accepted-custom"]

[[record]]
id = "testing.admission-path.gate-demands-premise-probed-against-gate-evidence-about"
title = "Admission for testing.gate-demands-premise-probed-against-gate-evidence-about."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_system_admission"
summary_md = """Admission for testing.gate-demands-premise-probed-against-gate-evidence-about.
"""
admission_kind = "independent_recurrence"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_ids = ["testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one-proof", "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two-proof"]
incident_ids = ["testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one", "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two"]

[[record]]
id = "testing.admission-path.grammar-fuzzer-embed-site-alias-roots"
title = "Admission for testing.grammar-fuzzer.embed-site-alias-roots."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_system_admission"
summary_md = """Admission for testing.grammar-fuzzer.embed-site-alias-roots.
"""
admission_kind = "independent_recurrence"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_ids = ["testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one-proof", "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two-proof"]
incident_ids = ["testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one", "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two"]

[[record]]
id = "testing.admission-path.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"
title = "Admission for testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_system_admission"
summary_md = """Admission for testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.
"""
admission_kind = "independent_recurrence"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_ids = ["testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one-proof", "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two-proof"]
incident_ids = ["testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one", "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two"]

[[record]]
id = "testing.admission-path.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"
title = "Admission for testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_system_admission"
summary_md = """Admission for testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives.
"""
admission_kind = "silent_corruption"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_ids = ["testing.proof.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"]

[[record]]
id = "testing.admission-path.rejection-message-s-remedy-string-executable-claim-pin"
title = "Admission for testing.rejection-message-s-remedy-string-executable-claim-pin."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_system_admission"
summary_md = """Admission for testing.rejection-message-s-remedy-string-executable-claim-pin.
"""
admission_kind = "independent_recurrence"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_ids = ["testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one-proof", "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two-proof"]
incident_ids = ["testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one", "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two"]

[[record]]
id = "testing.admission-path.same-named-generated-crates-silently-share-cargo-fingerprint"
title = "Admission for testing.same-named-generated-crates-silently-share-cargo-fingerprint."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_system_admission"
summary_md = """Admission for testing.same-named-generated-crates-silently-share-cargo-fingerprint.
"""
admission_kind = "silent_corruption"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_ids = ["testing.proof.same-named-generated-crates-silently-share-cargo-fingerprint"]

[[record]]
id = "testing.alloc-imports-nested-inline-modules-hand-carried-design"
title = "The alloc imports of nested inline modules are hand-carried by design, and two of the eight `natural_any_cbor_*` adapters were missed — silent under `std`, six E0425s without it."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """The alloc imports of nested inline modules are hand-carried by design, and two of the eight `natural_any_cbor_*` adapters were missed — silent under `std`, six E0425s without it.
"""
detail_md = """- **The alloc imports of nested inline modules are hand-carried by design, and two of the eight
  `natural_any_cbor_*` adapters were missed — silent under `std`, six E0425s without it.** The
  alloc-import injector deliberately does not scan nested inline `mod X { … }` bodies: a file-top
  import added for a nested usage would be unused at file scope and still would not resolve inside
  the module, so each such module carries its own `use super::alloc::…;`. The four map adapters
  carried theirs; `natural_any_cbor_seq` and `natural_any_cbor_opt_seq` named `Vec` without one.
  Fixed minimally — the two hand imports, pointing at the canonical rationale comment on
  `natural_any_cbor_btreemap` — rather than by teaching the injector to inject into nested bodies,
  because the nested-module universe in tool output is CLOSED and enumerated (those eight adapters
  plus `--emit-tests`' `cddl_generated_tests`, which opens `use super::*;` and needs nothing), while
  a nested-aware injector buys concrete new hazards for it: per-scope dedup against a consumer's
  `cddl-codegen:insert` block already binding the same name inside the module (E0252 if injected
  blindly, which is what CML's tree would have met), ownership matching extended past the column-0
  exact-match strip rule to indented lines, per-level `super` counting, and insertion-point rules
  inside nested bodies interacting with the reserved-marker hard stops. What converts \"correctness
  depends on someone noticing every new nested adapter\" into a red tier is the `json_schema`
  profile's `shim_thumb` cell above, which now compiles every adapter shape. *Reopening signal* for
  the nested-aware injector: a SECOND miss of the hand pattern — a red `json_schema`-profile cell,
  or a consumer filing, on an alloc name inside a nested inline module. Measurable by the party who
  already has the problem (the tier runner or the consumer reading the E0425), on the dimension the
  cost grows (recurrence of the class the hand pattern must cover). A new nested adapter added WITH
  its imports correct does not fire it — the signal is the pattern being missed, not the set growing.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.alloc-imports-nested-inline-modules-hand-carried-design"]
transition_ids = ["testing.trigger.alloc-imports-nested-inline-modules-hand-carried-design"]

[[record.source_replacement]]
span_id = "span-record-alloc-imports-nested-inline-modules-hand-carried-design"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.arm-position-s-classifier-compile-time-forcing-function"
title = "The arm-position axis's classifier is a compile-time forcing function, and losing it is the regression to watch for."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """The arm-position axis's classifier is a compile-time forcing function, and losing it is the regression to watch for.
"""
detail_md = """- **The arm-position axis's classifier is a compile-time forcing function, and losing it is the
  regression to watch for.** The axis itself is now enumerated: `no_silent_directive` sweeps a
  directive on a NON-LAST arm as two shapes of its own — `multi_choice_non_last_arm` and
  `option_collapse_non_last_arm` — so every directive in `KNOWN_RULE_METADATA_TAGS` is measured
  there instead of one standing in for thirteen. The rejections stay pinned where they were:
  `dsl_position_tests`' `type-choice-non-last-arm` cell with `type-choice-non-last-arm-allowed`
  pinning the `@name`/`@doc` exclusion, `no_silent_directive`'s
  `type_choice_non_last_arm_used_as_key` and `option_collapse_non_last_arm_no_alias` hand cells with
  their rule-slot placement controls, and
  `robustness_tests::option_collapse_reads_rule_position_directives` for the vocabulary at the rule
  slot. What no sweep can assert is the EXCLUSION set — which directives a variant position
  legitimately consumes — because that is a statement about future fields, not about present ones:
  `comment_ast::RuleMetadata::non_variant_directives`'s exhaustive destructuring makes a new
  `RuleMetadata` field fail to compile until its author classifies it as rule-level or variant-legal,
  and its sibling `all_directives` (the never-spliced-group refusal's list) inherits that same
  forcing function by building on it. Replacing either destructuring with a hand list silently
  removes the property.
  Reopening signal for adding a runtime assertion of the exclusion set: a `RuleMetadata` field lands
  bound to `_` in that destructuring without a variant-position cell proving it is variant-legal.

"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-arm-position-s-classifier-compile-time-forcing-function-gate-he00a841db53ba7a7564c502924f7598afeaf955962ec6bc5d5081a220a841f21"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.arm-position-s-classifier-compile-time-forcing-function's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record.source_replacement]]
span_id = "span-record-arm-position-s-classifier-compile-time-forcing-function"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.armed-idle-harness-arms-empty-head-ledgers-zero"
title = "Armed-but-idle harness arms (empty-at-HEAD ledgers, zero-count vector classes, per-row-kind gate branches) have untested first-use paths — seven review-caught instances recorded, no machinery yet."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Armed-but-idle harness arms (empty-at-HEAD ledgers, zero-count vector classes, per-row-kind gate branches) have untested first-use paths — seven review-caught instances recorded, no machinery yet.
"""
detail_md = """- **Armed-but-idle harness arms (empty-at-HEAD ledgers, zero-count vector classes, per-row-kind
  gate branches) have untested first-use paths — seven review-caught instances recorded, no
  machinery yet.** The decode-conformance family deliberately keeps machinery armed for residents
  that don't exist at HEAD (the over-acceptance flow, the exempt ledgers, the stale guards), so a
  scoping or preservation bug in such an arm is invisible to every gate until the first real
  resident arrives — and then fires as a false red or a silent data loss. The first three instances,
  all in the corpus-decode-leg delivery, were caught by in-session diff review and none by a gate, all
  since fixed: (1) the matrix arm-floor stale-exempt guard iterated a SHARED exempt ledger
  against only the MATRIX uncovered set, so the first corpus-keyed entry — which the corpus
  error messages direct users to add — would have falsely failed the matrix gate (fixed:
  per-catalog ledgers, `CORPUS_DECODE_FLOOR_ARM_EXEMPT`, each stale-guarded against its own
  set); (2) both mints' zero-candidates pin guard ignored over-acceptance pins (zero committed
  at HEAD), so the first ruby-generation gap on a row carrying one would have silently discarded
  a pin that must survive re-mints VERBATIM (fixed in both mints, with the
  don't-lean-on-`source=\"hand\"` comment); (3) the corpus drift half skipped the
  example-reconstruction staleness check for PINNED rows, leaving a fixture edit that invalidates
  a pin's justification green until the next re-mint (fixed: pinned rows check the example half).
  The shipped exemplar of the fix pattern is the drift gate's § 8 synthetic all-fields sample —
  but it covers only the writer/reader schema, not ledgers, mint buckets, or per-row-kind gate
  branches. The general mechanical layer when the build trigger below fires:
  extend the drift gate's self-check section with synthetic residents and synthetic
  perturbations — a fake entry per empty-at-HEAD ledger asserting exactly its own guard (and no
  sibling's) reacts; a pure preservation check of the mint bucket logic over a synthetic
  prior row carrying every vector class; and a perturbation sweep asserting each committed field's
  drift flips the gate red for each row KIND (active and pinned). Meanwhile the working rule:
  an armed-but-idle arm lands with its first-use self-check in the same change, or the review
  explicitly walks the empty arm. A FOURTH instance is on record from the json/wasm
  decode-surface-leg delivery, in a distinct CLAIMED-GUARD flavor and a new HOME:
  `JSON_SURFACE_SKIP` (a replay-gate rust-side skip ledger, not a drift-gate arm) shipped with its
  code comment and doc claiming the reproduction guard (\"a listed row that now round-trips fails
  the gate\") while the leg driver consumed entries WITHOUT running the leg — the guard was
  asserted, never wired; the `WASM_SURFACE_SKIP` sibling in the same change had the real guard.
  Caught by in-session diff review (the review walked the arm — the working rule holding), fixed
  in the same series. The first `policy-rejected` residents exposed two further review catches:
  (5) the matrix report counted policy vectors but the corpus report printed only its total vector
  count, obscuring its policy evidence; fixed with separate corpus policy/reason counts and the
  matrix+corpus total; (6) `PolicyFailureKind::DoubledLocation` advertised
  `DOUBLED_LOCATION_SKIP` but unconditionally failed instead of consuming the stale-guarded ledger,
  in both matrix and corpus replay loops; fixed by sharing the constraint branch's lookup and
  still-failing insertion. Two consequences for the mechanical layer: the flavor to detect is \"ledger
  entry consumed with zero reproduction attempt\", and the layer as sketched above is
  DRIFT-gate-scoped — the replay gates' rust-side skip ledgers need a sibling arm (a synthetic
  always-passing resident driven through the leg driver, asserting exactly the stale-pin failure
  fires and no other). A SEVENTH first-use preservation gap surfaced in the B3-024C corpus mint:
  the known rust-cddl `undefined`/`f7` oracle defect made the mint drop the spec-valid
  `fixed_singletons.undefined_value/8200f7` accept vector while still returning PASS; review of the
  dropped-vector report restored it manually. A future scoped re-mint can repeat that loss because
  there is no accept-side oracle-gap ledger. Add a per-catalog `(row, hex, expected failing
  oracle(s), reason)` exemption that retains only a vector the non-exempt oracle still accepts,
  fails stale when the cited oracle starts accepting or changes its failure signature, and is
  covered by the synthetic mint-bucket resident above; then migrate `8200f7` onto it. Do not weaken
  the ordinary two-oracle accept rule or create a row-wide exemption. Count: seven review-caught
  instances, zero escapes; the build trigger stays
  the first instance that survives review, with the layer owing BOTH homes when built. Related,
  smaller catch from the same review with its own
  trivial layer: a machine-written catalog `pinned_reason` cited a run artifact (\"see mint
  log\") — catalog prose is outside `lint_doc_citations`' scan surface, so non-durable citations
  there rot silently; fixed by wording review (the mint now emits self-contained tallies + a
  durable ledger pointer), and extending that lint's scan to catalog reason strings is the
  mechanical layer if it recurs.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.armed-idle-harness-arms-empty-head-ledgers-zero"]
transition_ids = ["testing.trigger.armed-idle-harness-arms-empty-head-ledgers-zero"]

[[record.source_replacement]]
span_id = "span-record-armed-idle-harness-arms-empty-head-ledgers-zero"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.assert-hand-authored-json-schema-against-type-s"
title = "Assert a hand-authored JSON schema against the type's actual serialization."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Assert a hand-authored JSON schema against the type's actual serialization.
"""
detail_md = """- **Assert a hand-authored JSON schema against the type's actual serialization.** Supplying the
  schema body needs neither new spec syntax nor a hand-written impl: `@custom_json` suppresses the
  `serde`/`schemars` derives while the type still gets a registration row, and `custom_schema_impl!`
  writes the `JsonSchema` impl around a JSON file, so an authored body lands in the document as an
  ordinary local `$defs` entry through the normal route (`docs/docs/comment_dsl.mdx` § Writing the
  `JsonSchema` impl the directive promises). The missing half is the one with the value: a
  hand-written schema drifts from the serialization it claims to describe and nothing notices — the
  reported consumer failure is a hand-maintained schema that had been wrong in two places for years.
  The assertion SHAPE already exists, hand-authored per fixture: `tests/json/tests.rs`'s
  `schemas_validate_serialization` validates a concrete value of every exported type against
  `schema_for!(T)`, with `schemas_reject_wrong_shapes` as the over-permissive counterpart (a
  degenerate always-true schema passes every positive check). What it needs in order to generalize
  into a consumer's crate is per-type sample values MINTED rather than hand-listed — the
  `--emit-tests` IR-derived value minting is the candidate. A spec-level `@json_schema = \"<file>\"`
  spelling is the cosmetic half and should not ship first: without the assertion it relocates the
  hazard instead of removing it. Reopening signal: the sample-value minting gets reused for this, or
  a consumer reports a shipped schema that contradicted its own serialization.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.assert-hand-authored-json-schema-against-type-s"]

[[record.source_replacement]]
span_id = "span-record-assert-hand-authored-json-schema-against-type-s"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.backgrounded-check-ts-full-launched-sub-agent-s"
title = "A backgrounded `check.ts full` launched from a SUB-AGENT's turn dies before it can finish — ATTRIBUTED to the launching topology, not a wall-clock ceiling. Do not watch for it; the standing rule is in `AGENTS.md`."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_incident"
summary_md = """A backgrounded `check.ts full` launched from a SUB-AGENT's turn dies before it can finish — ATTRIBUTED to the launching topology, not a wall-clock ceiling. Do not watch for it; the standing rule is in `AGENTS.md`.
"""
detail_md = """- **A backgrounded `check.ts full` launched from a SUB-AGENT's turn dies before it can finish —
  ATTRIBUTED to the launching topology, not a wall-clock ceiling. Do not watch for it; the standing
  rule is in `AGENTS.md`.** Four 2026-07-24/25 sightings, all from orchestrating sub-agents, all the
  same signature (task \"killed\", entire process tree gone, self-log stops mid-gate with no
  tier-level `RESULT` line and no error): ~53 min and ~60–65 min inside `gate_cache_closure_audit`
  (two runs, 2026-07-24), then 60 min and ~68 min inside `verify` (two runs, 2026-07-25). What
  settles it: the SAME tier on the same machine, launched from the MAIN session, ran 74 min to a
  clean `RESULT: PASS — all in-tier gates green`, and a shorter (~35 min) sub-agent background
  run completed fine — so the constraint is the sub-agent turn's lifetime, not a resource or
  wall-clock limit on the run itself. Consequence: a ~75-min full tier can NEVER complete from a
  sub-agent's turn, and delegating one is a guaranteed loss of a multi-minute run. Signature to
  distinguish from a hang: process tree GONE + log mtime stale (a hang keeps live processes).
  Distinguish from cross-session pkill (the entry above): no kill activity in any concurrent
  session, and exit is a task-kill, not `-15` in the run's own log. Recovery when one has already
  died: enumerate completed gates from the self-log (`grep '^--- '`), run the remainder isolated
  (the audit gate alone ≈16 min: `bun run audit_gate_cache_closure.ts` in `cddl-matrix/`;
  `corpus_detect.ts` and the fuzz `cargo check` are seconds), and claim the tier green only by
  COMPLETE gate enumeration — never from the partial log.

"""
incident_posture = "historical"
signature_md = """Four sub-agent-launched full tiers lost their complete process tree at roughly 53–68 minutes with no tier verdict, while the same tier from the main session completed in 74 minutes.
"""
evidence_ids = ["testing.source-observation.backgrounded-check-ts-full-launched-sub-agent-s"]
attribution_md = """Sub-agent turn lifetime topology, not a gate defect or general wall-clock ceiling.
"""
operating_rule_reference_id = "ref-testing-backgrounded-check-ts-full-launched-sub-agent-s-rule"
retirement_reference_id = "ref-testing-backgrounded-check-ts-full-launched-sub-agent-s-rule"

[[record.source_replacement]]
span_id = "span-record-backgrounded-check-ts-full-launched-sub-agent-s"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.build-registration-class-reference-position-sweep-second-escape"
title = "Build the registration-class × reference-position sweep — the second escape makes it due."
projection_group = "next-priority"
legacy_aliases = ["Next work 12"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Build the registration-class × reference-position sweep — the second escape makes it due.
"""
detail_md = """12. **Build the registration-class × reference-position sweep — the second escape makes it due.**
    The shipped incident pins and their exact coverage live in `tests/README.md` § “Recursive-type
    boundary — test map”; they are controls, not a substitute for this enumeration. Build a sibling
    to `directive_referencing_context_sweep` whose bases distinguish at least own-ident structs,
    parse-time transparent aliases, forward aliases, named generic-instance bindings whose
    canonical owns the struct, externs, and recursive collection alias hops in both naming orders.
    Cross those bases with member, element, re-alias hop, map value, tag head, and the existing
    reference-context rows. Every legal cell must generate and its emitted crate must compile under
    rust-only and wasm-bearing profiles; refusal cells must pin their diagnostic. The system is due
    because two directive-less classes escaped the hand coverage: a twice-aliased set-idiom binding
    and an alias-first recursive collection. Neither the directive sweep (wrong base axis) nor the
    recombination roles (no rule-level alias hop) could enumerate them.

"""
work_state = "ready"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.build-registration-class-reference-position-sweep-second-escape's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""
admission_ids = ["testing.admission-path.build-registration-class-reference-position-sweep-second-escape"]

[[record.source_replacement]]
span_id = "span-record-build-registration-class-reference-position-sweep-second-escape"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.cargo-feature-generated-crate-s-dependency-change-what"
title = "A cargo FEATURE on a generated crate's dependency can change what emitted code MEANS, and no axis varies dependency features — compounded by every generated `Serialize` only ever being exercised through `serde_json`, the one serializer that cannot observe dishonesty in the serde data model."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A cargo FEATURE on a generated crate's dependency can change what emitted code MEANS, and no axis varies dependency features — compounded by every generated `Serialize` only ever being exercised through `serde_json`, the one serializer that cannot observe dishonesty in the serde data model.
"""
detail_md = """- **A cargo FEATURE on a generated crate's dependency can change what emitted code MEANS, and no
  axis varies dependency features — compounded by every generated `Serialize` only ever being
  exercised through `serde_json`, the one serializer that cannot observe dishonesty in the serde
  data model.** Proven instance (consumer-reported, not any gate): under
  `serde_json/arbitrary_precision`, `serde_json::Number`'s `Serialize` emits a private
  `$serde_json::private::Number` token struct that only serde_json's own serializer collapses, so
  every `any`-carrying member's `to_json_value()` shipped a token object to JS at every magnitude —
  while `to_json()` looked perfect, because `to_json()` IS serde_json. Two independent blind spots
  produced it. Cargo unifies features across a build graph, so the feature arrives from a crate the
  generated manifest never names; the manifest merge even asserts it survives regeneration (a
  correct behaviour — the feature is legitimate), so the tool knowingly preserves a configuration
  nothing tested. And the assertion \"the emitted `Serialize` is honest in the serde data model\" is
  unobservable from serde_json: a second serializer is the only oracle that can see it, which is why
  the fixture takes `ciborium` (pure Rust, no C deps, no special case for the token) rather than
  adding another serde_json assertion. Owned meanwhile — one fixture, one feature, one alternate
  serializer, with liveness assertions in BOTH the rust and wasm crates so the feature-unification
  indirection cannot silently stop working and leave every other assertion in it vacuous — pinned by
  `json_arbitrary_precision`. Mechanical layer, when the trigger fires: a dependency-feature axis
  over the existing fixture corpus — for each generated-crate dependency, the set of its features
  that can change emitted-code semantics, generated once per (fixture × feature) cell and
  round-tripped through a non-serde_json serializer, which needs a per-dependency feature datum
  beside the manifest changeset that already knows every dependency the emitter references
  (`cargo_manifest.rs`'s `ops_for_*`). Building it now would be over-engineering: exactly one such
  feature is known to change semantics. Trigger: a consumer reporting a SECOND generated-crate
  behaviour that changes with a cargo feature they did not put in their own manifest — the party who
  already has the problem measures it as \"my spec and my manifest are unchanged and my generated code
  behaves differently\", which is what makes it reportable at all, and it lies on the axis the cost
  grows along, since the deferred layer's value scales with how many such features exist rather than
  with how many consumers hit the one we know about. A second semantics-changing feature is now
  known but is TOOL-OWNED and deliberate rather than unification-borne, so it does not fire this
  trigger: D1 ships `derivative` with `use_core`, which changes every emitted `#[derivative(…)]`
  expansion from `::std::` to `::core::` paths — landed only after probing the COMPLETE emitted
  attribute-form set (7 bodies × 3 derive lines) for equivalence on host and a no-std target.
  Recorded so the count of known semantics-changing dependency features reads two, not one, when
  this axis is next weighed.

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
control_ids = ["testing.guard.cargo-feature-generated-crate-s-dependency-change-what"]
transition_ids = ["testing.trigger.cargo-feature-generated-crate-s-dependency-change-what"]

[[record.source_replacement]]
span_id = "span-record-cargo-feature-generated-crate-s-dependency-change-what"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.cddl-module-directives-draft-ietf-cbor-cddl-modules"
title = "CDDL module directives (draft-ietf-cbor-cddl-modules) with the draft's real inlining semantics, and `as`-namespacing."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """CDDL module directives (draft-ietf-cbor-cddl-modules) with the draft's real inlining semantics, and `as`-namespacing.
"""
detail_md = """- **CDDL module directives (draft-ietf-cbor-cddl-modules) with the draft's real inlining
  semantics, and `as`-namespacing.** Both forms are currently recognized and refused loudly
  (`module_directive_import_aborts` / `module_directive_include_aborts`;
  `dotted_rule_name_rejects_gracefully` for cddlc `as`-expansion output) — deliberate: the extern
  channel shipped on the dep-side export instead (rationale: the dep's extern surface is
  `f(spec, DSL, flags, tool version)`, which spec-text resolution cannot see; and the draft's
  `;# import` means inline-and-generate, so extern semantics would contradict it — keep the `;#`
  namespace clean for a real implementation). Reopening signals: a consumer needs third-party
  module consumption (RFC extracts) or cross-module name reuse; the latter is the deep one
  (scope-qualified `RustIdent` through the IR, dep_graph ordering, emitted-test glob imports,
  JSON schema naming).
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.cddl-module-directives-draft-ietf-cbor-cddl-modules"]

[[record.source_replacement]]
span_id = "span-record-cddl-module-directives-draft-ietf-cbor-cddl-modules"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.check-merged-d-ts-name-collisions-two-halves"
title = "Check the merged `.d.ts` for name collisions BETWEEN its two halves."
projection_group = "next-priority"
legacy_aliases = ["Next work 18"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Check the merged `.d.ts` for name collisions BETWEEN its two halves.
"""
detail_md = """18. **Check the merged `.d.ts` for name collisions BETWEEN its two halves.** `json-ts-types.js`
    appends the `<key>JSON` declarations to the wasm-pack bindings under the claim that the suffix
    cannot collide with a wasm class name — an assumption, not a check: `@name` can mint a wasm
    class literally called `FooJSON` (and a contrived rule spelling that camelizes to one can too),
    and TypeScript merges a class with a same-named `interface` silently and LEGALLY, so the merged
    file's `tsc --noEmit` leg structurally cannot say so and the shipped type is the merge of two
    unrelated shapes with nothing said. `run-json2ts.js`'s duplicate-declaration guard covers only
    its own half, for the same reason it exists at all (`tsc` is not the oracle for declaration
    merging). This is the missing TS sibling of the wasm wrapper-name collision-detector family:
    per that family's ruling, a per-kind check with its own message rather than a generalization of
    an existing one. The cheap mechanical layer sits where both halves are in hand —
    `json-ts-types.js` already walks every `export class` line to place the splice, so collecting
    those names and refusing an appended declaration equal to one is a set intersection plus a
    diagnostic naming both the class and the `$defs` key, same fatal-and-file-untouched shape as
    its existing failures; the vector is an `@name FooJSON` fixture through `js_d_ts_merge`'s
    hand-laid harness.

"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "compile_failure"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.check-merged-d-ts-name-collisions-two-halves's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""

[[record.source_replacement]]
span_id = "span-record-check-merged-d-ts-name-collisions-two-halves"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.checking-runtime-flavor-s-safety-condition-rather-stating"
title = "Checking `[runtime] flavor-from`'s safety condition rather than stating it."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """Checking `[runtime] flavor-from`'s safety condition rather than stating it.
"""
detail_md = """- **Checking `[runtime] flavor-from`'s safety condition rather than stating it.** The `[runtime]`
  carrier derivation is a maintainer-CLOSED area (`AGENTS.md`), so this records the one build that
  would be defensible IF that area reopens — not an intent to build it. Shape: error only on a
  VIOLATED semantic condition in a flavor-mismatched crate, never on the mismatch itself, which the
  once-per-run carrier note already describes. The remaining known condition is a
  `--deserialize-depth-limit` divergence when the consumer uses `any`: it compiles, and silently
  guards one crate's values at another crate's limit. The former compile-time conditions are current
  coverage, not remaining work: a preserve + canonical carrier now compile-serves an isolated
  reduced `{+ K => V}` and an isolated reduced `any`, pinned by
  `a_runtime_table_exports_a_runtime_the_other_flavor_compiles_against`. Do not rebuild a
  flag→poisoned-construct map from those retired gaps; compatibility outside the pinned direction
  remains spec-dependent and explicit through `flavor-from`. The same depth-limit divergence is
  reachable WITHOUT config mode — hand invocations pairing
  `--export-static-crate` with `--common-import-override` consumers — and there it has less signage
  still: no carrier note exists, only the MATCH contract on the flag's own doc. A check on that path
  is a structurally different build, not a scope extension of the config-mode one: no single hand
  invocation sees both flag sets (the consumer knows only the runtime's path, used verbatim), so it
  would need the exported runtime to carry a machine-readable flavor record that consumer generation
  reads as an explicit cross-crate input (the `--extern-import` class — another crate's committed
  export, not this run's prior output). That half is not behind the maintainer-CLOSED gate because it
  never touches the carrier derivation. Reopening signal: a consumer report of the silent
  depth-limit mis-guarding in a shared-runtime workspace, reached via `flavor-from` or via hand-flag
  `--export-static-crate`.

"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.checking-runtime-flavor-s-safety-condition-rather-stating.
"""
authority_reference_id = "ref-testing-checking-runtime-flavor-s-safety-condition-rather-stating-authority"
permanence = "permanent"

[[record.source_replacement]]
span_id = "span-record-checking-runtime-flavor-s-safety-condition-rather-stating"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.citation-resolves-naming-wrong-thing-invisible-citation-lint"
title = "A citation that RESOLVES while naming the wrong thing is invisible to every citation lint, and the claim it makes is the half a human reads."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A citation that RESOLVES while naming the wrong thing is invisible to every citation lint, and the claim it makes is the half a human reads.
"""
detail_md = """- **A citation that RESOLVES while naming the wrong thing is invisible to every citation lint, and
  the claim it makes is the half a human reads.** The `KNOWN_PANIC_CLASSES` form of this is closed:
  each entry pairs a panic-message substring with prose naming the fixture that owns the class, and
  `known_panic_classes_cite_fixtures_that_produce_them` (always-on, `local` tier) now generates each
  `tests/**/*.cddl` fixture an entry names and requires at least one to produce the entry's substring, so a mis-citation is a red test rather than a mislead during triage — the
  instance on record cited a fixture whose actual class was the other anonymous-composite BRACKET,
  which would have sent a reader to the wrong parse site (current state in `tests/README.md` §
  \"Shape-recombination fuzzer\"). What remains is the
  SECOND FORM of the same class, in a population that detector cannot reach — the cited thing
  is a GATE and the claim is that the gate is EVIDENCE for a property. Instance (read-caught in
  orchestrator review, never committed): a delivery report cited `check.ts fast` as proof that an
  `--emit-tests` map-key spelling was byte-identical, but the snapshot corpus holds zero `__i as …`
  key expressions, so no run of that tier could ever have witnessed the property either way. The
  class property is identical — the citation RESOLVES (the gate exists, it was green), so nothing
  that checks resolution can fire, and only the claim is false — but the population is prose in
  reports and handoff docs, which no gate scans, so the ledger detector above does not extend to
  it and this is NOT its second-instance trigger firing. What DOES transfer is the cheap
  instrument, which is the same shape as the detector: before citing a gate as evidence for
  emitted TEXT, grep that gate's corpus for the text. Which corpus covers what is a `tests/README.md`
  fact, and the emit-tests key-rendering answer now lives there (§ \"Authoring standard for a
  bounded-domain emit-tests fixture\"); the general rule is that a tier verdict evidences a property
  only where that tier's corpus contains an instance of it.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.citation-resolves-naming-wrong-thing-invisible-citation-lint"]
transition_ids = ["testing.trigger.citation-resolves-naming-wrong-thing-invisible-citation-lint"]

[[record.source_replacement]]
span_id = "span-record-citation-resolves-naming-wrong-thing-invisible-citation-lint"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.comment-residue-false-matches-text-scans-over-emitted"
title = "Comment-residue false matches in text scans over emitted/prior `.rs` output — one proven instance, no machinery yet."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Comment-residue false matches in text scans over emitted/prior `.rs` output — one proven instance, no machinery yet.
"""
detail_md = """- **Comment-residue false matches in text scans over emitted/prior `.rs` output — one proven
  instance, no machinery yet.** Any diagnostic or decision that SCANS generated (or user-edited
  prior) Rust text for a code pattern shares one trap: the comment-preservation overlay and the
  users of its block grammar leave the scanned pattern behind as COMMENT bytes — a
  `cddl-codegen:replaces` recorded original is the exact deleted line `//`-commented, and
  unpreserved-comment blocks / doc comments can quote code verbatim — so a substring `contains`
  scan reads deleted code as live. Proven instance (development-red-caught, not by any shipped
  gate): the extern-glue re-export survival scan's first cut used a substring match and read a
  replace block's recorded `// pub use crate::<Name>;` original as surviving glue — corrected to
  whole-trimmed-line equality before landing, with the residue case pinned by
  `extern_reexport_diagnostic_skips_replace_deleted_glue`'s chain assertions (overlay really
  deleted the live line AND the warning subtracted the name). Working rule meanwhile: a scan over
  emitted/prior `.rs` text matches live tokens (whole-trimmed-line or token-aware), never
  substrings, and its test includes a comment-residue adversarial fixture (the pattern present
  ONLY as a recorded original / comment). One untested premise worth probing when next in the
  area: `missing_reexports`' whole-ident scan over the USER's seed-once `lib.rs` — whether a
  commented-out `// pub use generated::<Name>;` line counts as \"provided\" (same residue class,
  opposite direction: a false NEGATIVE of the warning). Mechanical layer on a SECOND instance: an
  adversarial-fixture family enumerating every output-text-scanning decision point (the
  bounded-exception diagnostics list in AGENTS.md is the seed inventory) with one residue fixture
  each, rather than trusting each new scan to remember the rule.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.comment-residue-false-matches-text-scans-over-emitted"]
transition_ids = ["testing.trigger.comment-residue-false-matches-text-scans-over-emitted"]

[[record.source_replacement]]
span_id = "span-record-comment-residue-false-matches-text-scans-over-emitted"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.complete-cargo-mutants-sweep-will-never-run"
title = "The complete `cargo-mutants` sweep will never be run."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """The complete `cargo-mutants` sweep will never be run.
"""
detail_md = """- **The complete `cargo-mutants` sweep will never be run.** The configured ~3% experiment
  (33/1040 mutants) was enough to establish its value profile: all six survivors were
  behaviorally-equivalent style-only mutations, while the work needed to finish and triage the
  sweep is roughly 30 hours of machine time plus review. The full sweep is permanently declined,
  not deferred: do not schedule it, add it to a later burndown, or invent a reopening signal. The
  existing config and partial results may remain as historical/probing machinery, but they create
  no completion obligation.

"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.complete-cargo-mutants-sweep-will-never-run.
"""
authority_reference_id = "ref-testing-complete-cargo-mutants-sweep-will-never-run-authority"
permanence = "permanent"

[[record.source_replacement]]
span_id = "span-record-complete-cargo-mutants-sweep-will-never-run"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.component-js-probe.reopening-signal"
title = "Reopening signal:"
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "signal"
summary_md = """Reopening signal:
"""
detail_md = """  - **Reopening signal:** a consumer reporting a JS-side failure on one of those axes — the party
    running the browser build, the preserve posture or a newer jco is the only one who can see it
    first, and the report names which axis to probe. The gate's exact pins are what make such a
    report actionable rather than ambiguous: a failure at a version the gate does not pin is a
    version finding, while a failure at 1.26.1 on node 22 is a regression the gate should already
    have caught.

"""
transition_kind = "reopening_signal"
observer = "consumer running the browser build, preserve posture, or a newer jco"
dimension = "the named JS-side component probe axes"
observable = "a consumer requires the JSON doors, browser build, preserve canonical-bytes posture, or a jco/node version other than jco 1.26.1 with node 22, and names that axis"
predicate_kind = "event"
current_evidence_ids = ["testing.source-observation.component-js-probe-reopening-signal"]
action_on_fire_md = """Promote only the named axis; classify a failure on the pinned version and already-probed axis as a defect finding rather than breadth demand.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "event"
event_md = """a consumer requires the JSON doors, browser build, preserve canonical-bytes posture, or a jco/node version other than jco 1.26.1 with node 22, and names that axis
"""
evidence_ids = ["testing.source-observation.component-js-probe-reopening-signal"]

[[record.source_replacement]]
span_id = "span-part-part-reopening-signal-probe"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.composing-registration-rows-across-generation-passes-share-output"
title = "Composing registration rows across generation passes that share one output crate."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Composing registration rows across generation passes that share one output crate.
"""
detail_md = """- **Composing registration rows across generation passes that share one output crate.** Two
  cddl-codegen passes targeting one output directory each own the json-gen crate's row set
  outright, so there is no supported way to have one json-gen crate publish both passes' types.
  Lowest value of the JSON-schema asks by the asking consumer's own ranking: a committed-`.d.ts`
  CI diff covers the requirement they actually have, and the emitted `add_schemas` is already `pub`,
  so a hand-written `export_schemas()` in the crate root can call both passes' registrars today.
  (Scope: this records the ask as understood from the delivery's response notes; the requester's own
  statement of it is not in-tree.) Reopening signal: a consumer whose layout genuinely forces two
  passes into one json-gen crate and for whom the hand-written composition is not enough.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.composing-registration-rows-across-generation-passes-share-output"]

[[record.source_replacement]]
span_id = "span-record-composing-registration-rows-across-generation-passes-share-output"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.config-run-s-convergence-warning-name-crate-already"
title = "The config run's convergence warning can name a crate that already converged."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """The config run's convergence warning can name a crate that already converged.
"""
detail_md = """- **The config run's convergence warning can name a crate that already converged.** `Convergence`
  snapshots each consumed sidecar's bytes at the START of the run and compares them after, so any
  sidecar that CHANGED during the run marks its reader stale. That is right for the edge the check
  exists for (a dependency reads its consumer's committed sidecar, and the consumer generates
  later), and wrong for a hand-written `[crates.<n>.wrapper-requests]` / `.key-requests` entry
  pointing at a crate that sorts EARLIER: the owner rewrote the sidecar before the reader read it,
  so the reader saw fresh bytes and is not a run behind, but the before/after comparison cannot
  tell the two apart. Reasoned from the code, not reproduced; strictly diagnostic (stderr text,
  exit status and generated bytes unaffected), and self-clearing — the redundant re-run it asks for
  converges and the second run is silent. The fix, if it is ever worth one, is to snapshot each
  sidecar when its READER reaches it rather than at run start, which needs the capture to
  interleave with the generation loop instead of bracketing it. Reopening signal: the warning names
  the same crate on two consecutive runs of an unchanged config — a false positive that does not
  clear is a different defect from this one, and the person seeing it can measure it without
  reading any of this. The blast radius stays confined to the warning: the committed-state verdict
  that shares the run's exit path (`Config::committed_verdict`) reads the tree rather than
  bracketing the run, so it has no before/after comparison for this to reach, and a spurious warning
  cannot become a spurious build failure.

"""
work_state = "deferred"
work_intent = "repair"
work_kind = "defect"
risk = "misleading_docs"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.config-run-s-convergence-warning-name-crate-already"]
regression_gap_ids = ["testing.proof-gap.config-run-s-convergence-warning-name-crate-already"]

[[record.source_replacement]]
span_id = "span-record-config-run-s-convergence-warning-name-crate-already"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.container-combinations.reopening-signal"
title = "Reopening signal:"
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "signal"
summary_md = """Reopening signal:
"""
detail_md = """    - **Reopening signal:** a THIRD construct joins the flat-walked set — countable in-tree as the
      explicit container mints in `generation/mod.rs` that exist because the conceptual visitor
      cannot see the composite (two today, both listed above). At three, per-construct hand cells
      stop being cheaper than the grid row, and the cross-axis coverage the grid gives for free
      (placement × reference mode × profile) is coverage three constructs are each doing without.

"""
transition_kind = "reopening_signal"
observer = "maintainer enumerating explicit container mints in generation/mod.rs"
dimension = "flat-walked container constructs invisible as composites to the conceptual visitor"
observable = "the count reaches at least three"
predicate_kind = "quantitative"
current_evidence_ids = ["testing.source-observation.container-combinations-reopening-signal"]
action_on_fire_md = """Promote the parent and build the placement by reference-mode by profile grid row.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "quantitative"
comparator = "ge"
threshold = 3
unit = "constructs"
scope = "explicit container mints in generation/mod.rs required because the conceptual visitor cannot see the composite"
measurement = 2
as_of = "2026-08-12"

[[record.source_replacement]]
span_id = "span-part-part-reopening-signal"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
title = "A container construct the conceptual type visitor walks FLAT has no combinatorial wasm-compile coverage — its placement behaviour rests on one hand cell per construct."
projection_group = "next-priority"
legacy_aliases = ["Next work 7"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A container construct the conceptual type visitor walks FLAT has no combinatorial wasm-compile coverage — its placement behaviour rests on one hand cell per construct.
"""
detail_md = """7. **A container construct the conceptual type visitor walks FLAT has no combinatorial wasm-compile
    coverage — its placement behaviour rests on one hand cell per construct.** Most of the IR's
    containers are `Map`/`Array` nodes a walk meets as composites; a few are assembled from inner
    types stored separately, so every walk that reasons about containers has to be told about them
    one site at a time. Today there are two — the open struct-map rest row (`* K => V`) and the open
    array rest tail (`* T`), both re-assembled through `RestRow::container_type` and minted
    explicitly in `generation/mod.rs` because the conceptual visitor never sees them as composites.
    Each is pinned cross-module by exactly one hand fixture cell (`tests/multifile`'s `open_flat` /
    `open_nested` / `open_tail` in `qux.cddl`, whose key/value/element live in `a`, `a/c/foo` and
    `b/bar`, compiled wasm-side under both fixture profiles) rather than by a grid.
"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
transition_ids = ["testing.container-combinations.reopening-signal"]

[[record.source_replacement]]
span_id = "span-record-container-construct-conceptual-type-visitor-walks-flat-combinatorial"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.containment-note-s-stated-reason-go-false-any"
title = "A containment note's stated REASON can go false without any gate noticing — the note carries a behavioural claim, and only its `spec` and (via the annotation) its support verdict are checked."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A containment note's stated REASON can go false without any gate noticing — the note carries a behavioural claim, and only its `spec` and (via the annotation) its support verdict are checked.
"""
detail_md = """- **A containment note's stated REASON can go false without any gate noticing — the note carries a
  behavioural claim, and only its `spec` and (via the annotation) its support verdict are checked.**
  Proven instance, since fixed: six containment notes justified their two-field examples with \"a
  single-field struct hits an orthogonal cddl-codegen panic\", a panic that was gone — `m = { k: 5 }`
  and every other shape those notes were attached to generate exit 0, and
  `cddl-matrix/annotations/corpus/cddl_codegen.toml` separately recorded single-field struct maps as
  supported, so the note and the annotation had been contradicting each other in the same repo. The
  notes now state the file's two-field example CONVENTION instead of a causal claim about a defect,
  and the absence of the panic is pinned by `tests/corpus/single_field_map.cddl` — the shape a note
  must take when its reason would otherwise be an unowned behavioural claim. Nothing connected them
  and nothing does now: `project_corpus`'s note↔support check (E) reads the corpus nuance overlay's
  `[[note]]` entries, not containment `note` prose, and `verify.ts` corroborates only the `example`
  against `spec`. The class is bounded — a containment note is prose, and most of it (grammar
  anchors, sibling relationships) is not machine-checkable — so the meanwhile rule is narrower than
  a checker: a note states the SHAPE and its sibling relationships, and any behavioural claim it
  makes names the fixture or annotation that owns the claim, so the claim rots where a gate already
  looks. Trigger for the mechanical layer (a lint that flags behavioural vocabulary — \"panics\",
  \"unsupported\", \"rejects\" — in a containment note whose id carries no annotation asserting it): a
  SECOND note found stating a behaviour the annotations contradict, which the next author to reuse
  a note's reasoning measures at the moment they inherit it.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.containment-note-s-stated-reason-go-false-any"]
transition_ids = ["testing.trigger.containment-note-s-stated-reason-go-false-any"]

[[record.source_replacement]]
span_id = "span-record-containment-note-s-stated-reason-go-false-any"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.control-bytes-committed-source-files-proven-instance-machinery"
title = "Control bytes in committed source files — one proven instance, no machinery yet."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Control bytes in committed source files — one proven instance, no machinery yet.
"""
detail_md = """- **Control bytes in committed source files — one proven instance, no machinery yet.** A committed
  TS script embedded a literal 0x00 byte in a template-string separator; grep/diff/review tooling
  then classifies the whole file as binary — silently exempting it from every text-shaped check
  and from readable review — while `tsc` parses it happily, so `matrix_typecheck` cannot see the
  class (fixed by spelling the byte as a backslash-u0000 escape). Working rule meanwhile: escape
  sequences, never literal control bytes, in source text. Mechanical layer if the class recurs: a
  tracked-source text-cleanliness lint — every tracked `*.ts`/`*.rs`/`*.toml`/`*.md` must be valid
  UTF-8 with no control bytes outside tab/LF/CR (a one-command scan; natural sibling of
  `lint_doc_citations` in the local tier).
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
control_ids = ["testing.guard.control-bytes-committed-source-files-proven-instance-machinery"]
transition_ids = ["testing.trigger.control-bytes-committed-source-files-proven-instance-machinery"]

[[record.source_replacement]]
span_id = "span-record-control-bytes-committed-source-files-proven-instance-machinery"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.corpus-c-style-enum-data-type-choice-arm"
title = "A corpus cell for a c-style enum as a DATA type-choice arm — the one inline-dispatch leg no corpus cell exercises."
projection_group = "next-priority"
legacy_aliases = ["Next work 21"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A corpus cell for a c-style enum as a DATA type-choice arm — the one inline-dispatch leg no corpus cell exercises.
"""
detail_md = """21. **A corpus cell for a c-style enum as a DATA type-choice arm — the one inline-dispatch leg no
    corpus cell exercises.** The c-style-enum deserialize arm has two emission forms, and the form
    it picks is a frame decision: the IIFE (self-supplied frame) whenever the caller splices or —
    since the annotate=false delivery — whenever the caller wants a plain value with no annotation
    closure to catch the early `return`s; the bare statement form only where a `Result`-returning
    closure is guaranteed. That guarantee was verified by HAND-ENUMERATING every
    `DeserializeBeforeAfter::new(\"\", \"\", true)` call site (all are annotate-gated or sit inside the
    enums.rs variant-probe closure), because no gate exercises the kept leg: the corpus spells a
    c-style enum standalone (`c_style_enum`), as a map key (`c_style_enum_map_key`), behind
    `@newtype` (`c_style_enum_newtype`) and behind encoding-carrying wrappers
    (`cbor_enum_payload`), but never as an arm of a DATA type choice (`t = fixed_enum / bytes`) —
    the one spelling that routes through the probe-closure statement form. A hand enumeration is
    evidence for the tree it was done on and nothing after it; one corpus input carrying that
    spelling (plus a group-choice-arm sibling if it routes differently) turns the guarantee into a
    standing cell: a compile-floor row under BOTH annotate=false flavors, a snapshot pin, and the
    usual registration (snapshot bless, `CORPUS_PARITY_INPUTS` row). Starting premise, probed on
    the delivering tree so the pickup starts green rather than shape-hunting: both spellings
    (`choice_holder = fixed_enum / bytes`; `gc_holder = [ a: fixed_enum // b: tstr ]`) generate at
    exit 0 and `cargo check` green under BOTH annotate=false flavors, with the statement-form leg
    visibly emitted (`Ok(()) => return Ok(FixedEnum::I0)` inside the variant-probe closures) —
    probed `--wasm=false` only; the floor's own cells will run `--wasm=true`. Same input-poverty class
    as this list's \"A corpus cell for the preserve pair-map whose key AND value are self-encoding\"
    item: the emission branch exists, the fix cycle proved it load-bearing, and no committed input
    reaches it.

"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "coverage_cell"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.corpus-c-style-enum-data-type-choice-arm's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""

[[record.source_replacement]]
span_id = "span-record-corpus-c-style-enum-data-type-choice-arm"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.corpus-preserve-pair-map-key-value-self-encoding"
title = "A corpus cell for the preserve pair-map whose key AND value are self-encoding — the one `@duplicates preserve` emission arm no committed snapshot pins."
projection_group = "next-priority"
legacy_aliases = ["Next work 19"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A corpus cell for the preserve pair-map whose key AND value are self-encoding — the one `@duplicates preserve` emission arm no committed snapshot pins.
"""
detail_md = """19. **A corpus cell for the preserve pair-map whose key AND value are self-encoding — the one
    `@duplicates preserve` emission arm no committed snapshot pins.** The positional
    (non-canonical) pair-map serialize loop binds its `.enumerate()` index `i` when a per-entry
    key/value sidecar lookup reads it and `_i` when neither does, and the committed corpus
    exercises only the first: `table_preserve`'s five positional loops all read a
    `*_key_encodings.get(i)`, while the `_i` leg — reached exactly when key and value are BOTH
    nominal self-encoding types (`m = { * ref => ref } ; @duplicates preserve` with `ref` a
    struct/collection rule, each entry carrying its own sidecar internally) — is pinned by no
    snapshot and compiled by no corpus gate. Its only current coverage is the
    `tests/recursive-collection-ref` integration fixture, which is where the `unused variable: i`
    emission defect this leg shipped with was finally seen — by the `run_test` generator-owned
    warning scan on that scan's first sweep, not by any corpus layer (fixed in the same
    delivery). The stakes are compile-shaped in both directions: a wrong emptiness condition
    either emits `_i` under a live `.get(i)` lookup (E0425 in every consumer build of the
    affected shape) or `i` under none (the warning class the scan now holds at zero). One corpus
    input with the both-nominal shape buys the leg a snapshot pin plus a
    `feature_corpus_compiles` cell — standard corpus registration (snapshot bless,
    `CORPUS_PARITY_INPUTS` row).

"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "coverage_cell"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.corpus-preserve-pair-map-key-value-self-encoding's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""

[[record.source_replacement]]
span_id = "span-record-corpus-preserve-pair-map-key-value-self-encoding"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.corpus-wide-regen-over-prior-output-sweep-reaches"
title = "The corpus-wide regen-over-prior-output sweep reaches two emission profiles of four, and its user-EDIT leg reaches one."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """The corpus-wide regen-over-prior-output sweep reaches two emission profiles of four, and its user-EDIT leg reaches one.
"""
detail_md = """- **The corpus-wide regen-over-prior-output sweep reaches two emission profiles of four, and its
  user-EDIT leg reaches one.** `regen_over_prior_output_corpus` sweeps the static floor and the
  rule-DELETION variant under the default and `--preserve-encodings` profiles (the latter for
  `cbor_encodings.rs`, a per-rule surface the default never emits); the `json` profile's `json-gen`
  tree and derive attributes, and the `component` face's WIT-side glue, are unswept, as is the
  user-EDIT leg outside the default profile. Each added profile costs one more generation per fixture
  for legs 1-2 (measured ~15 s on the delivering machine) and THREE for the edit leg, plus nested
  cargo for its compile gate — paid on every `local` tier run. **Owner meanwhile:** the emitter
  invariant itself (no generated comment on a row a spec change can delete), which is
  profile-independent in its reasoning even where the sweep is not in its reach, plus the two swept
  profiles' floors. **Trigger:** a `cddl-codegen:unpreserved-comment` sentinel reported against a
  regenerated tree — by a consumer or by any gate — in a file neither swept profile emits; the
  profile that produced it is then the one to add, not all of them.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.corpus-wide-regen-over-prior-output-sweep-reaches"]
transition_ids = ["testing.trigger.corpus-wide-regen-over-prior-output-sweep-reaches"]

[[record.source_replacement]]
span_id = "span-record-corpus-wide-regen-over-prior-output-sweep-reaches"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.crate-generates-json-schema-document-aggregate-package"
title = "A crate entry that generates only a JSON-schema document (the aggregate package)."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A crate entry that generates only a JSON-schema document (the aggregate package).
"""
detail_md = """- **A crate entry that generates only a JSON-schema document (the aggregate package).** An npm
  package composing several generated crates needs one schema document over their union, but is
  often not itself a generated crate — so it carries a hand-written json-gen crate transcribing what
  this tool already emits for a `--json-schema-dep`-only run, registration-order reasoning and all.
  Measured, the emission side is already there: an input-less run (an empty `--input` directory)
  emits a `wasm/json-gen/` whose `add_schemas` holds the threaded calls in flag order and nothing
  else, and a fixture over two generated crates builds it and gets a document carrying a root from
  each. What blocks it is the SURPLUS such a run also writes — a vestigial rust crate that
  `static/manifest_changes/json_gen.toml` then makes the json-gen crate depend on
  (`dependencies.<lib> = { path = \"../../rust\" }`, an unconditional entry in an append-only log).
  Cargo auto-adopts that path dependency as a workspace member, so its `package.name` collides with
  the hand-written umbrella crate the aggregate exists to serve; renaming escapes the collision only
  by changing the document's title and filename, both published surface.
  What to build, in order: lift the `--json-schema-dep` / `--json-gen-dep` / `--json-schema-root`
  validations out of `with_types`, which an input-less run never enters; make the `../../rust`
  manifest op conditional. Only then land the aggregate path on top. The step this list used to
  open with is already done and is what makes the rest safe: `GenerationScope::export`'s write tail
  is one parameterised implementation (`generation::write_tail`), so the no-prior-output contract
  and its diagnostic-only reads have exactly one home and direct coverage
  (`src/tests/write_tail_tests.rs`). A second write loop beside it is still the shape to avoid — the
  snapshot corpus reaches `generated_strings`, not `export` — but an aggregate path now has a
  `WriteTailPlan` to fill in rather than a write loop to clone. Reopening signal (magnitude, consumer-side): the count
  of hand-written umbrella exporter files a consumer maintains rising above one, which is what a
  project publishing a second npm package over generated crates produces.

"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.crate-generates-json-schema-document-aggregate-package"]

[[record.source_replacement]]
span_id = "span-record-crate-generates-json-schema-document-aggregate-package"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite"
title = "Cross-version preserve vectors beyond the std→alloc rewrite."
projection_group = "next-priority"
legacy_aliases = ["Next work 6"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Cross-version preserve vectors beyond the std→alloc rewrite.
"""
detail_md = """6. **Cross-version preserve vectors beyond the std→alloc rewrite.** The preserve corpus is a
   SAME-VERSION suite by construction: every case's `old.rs` and `new.rs` agree on generated code
   bytes except where the fixture deliberately drifts one item, which is the shape a re-run of one
   tool version produces. A tool UPGRADE is the other shape — it adds tokens and rewrites others
   across a whole file at once, so every anchor shifts and each item the rewrite touched drifts —
   and no vector expressed it until the no_std false positive arrived from a consumer with a
   `compile_error!` sitting three lines above the comment it claimed was lost
   (`cross_version_std_to_alloc_rewrite_self_cancels` /
   `cross_version_rewrite_traps_only_the_reworded_line`). Those two cover the rewrite class the
   upgrade actually performed; what has no witness is the same whole-file-shift shape crossing the
   overlay's other units — an insert block, a replace block's needle, and a `keep` run whose
   anchoring statement the upgrade rewrote — where the recorded original is matched by TOKENS and
   no text-presence escape hatch exists. Authored as ordinary preserve fixtures, so the corpus and
   its rustfmt-cycle sweep absorb them with zero harness change. Reopening signal, measurable by a
   consumer that already has the problem with two runs and a diff: an upgrade regen emits
   `cddl-codegen:unpreserved-comment` or a trapped block that a second, otherwise-unchanged regen
   does NOT reproduce — a block that self-clears is a false positive by construction, which is
   exactly how the no_std one was identified.

"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.cross-version-preserve-vectors-beyond-std-alloc-rewrite"]

[[record.source_replacement]]
span_id = "span-record-cross-version-preserve-vectors-beyond-std-alloc-rewrite"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.dependency-swap-pass-compile-parity-existing-gate-silently"
title = "A dependency swap can pass compile-parity and every existing gate while silently changing behavior our public API re-exposes — one proven near-miss, pins landed for the instance."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A dependency swap can pass compile-parity and every existing gate while silently changing behavior our public API re-exposes — one proven near-miss, pins landed for the instance.
"""
detail_md = """- **A dependency swap can pass compile-parity and every existing gate while silently changing
  behavior our public API re-exposes — one proven near-miss, pins landed for the instance.**
  Proven instance (caught by orchestrator differential probing, not by any test): the
  linked-hash-map→hashlink swap plan carried an `insert`→`replace` \"compatibility\" forwarding
  built on a twice-source-read premise that linked-hash-map's `insert` replaces in place. It
  moves the key to the back — identically to hashlink — so the forwarding would have CHANGED
  shipped wire-order behavior while presenting as preservation, and the swap's real divergence
  was elsewhere (`Entry::or_insert` on an occupied entry: hashlink refreshes to back,
  linked-hash-map doesn't). Every existing layer is structurally blind to the class: round-trip
  and golden-hex oracles never MUTATE through the dep surface (decode→mutate→re-serialize is
  where order semantics bite); snapshot pins hold emitted TEXT, which a backing swap legitimately
  churns; and mutation scoring runs against the CURRENT dep, so it measures coverage of today's
  baseline, never a baseline change. We therefore pinned the incumbent's semantics at the wire
  level BEFORE swapping — `ordered_hash_map_{insert_overwrite_moves_to_back,
  or_insert_keeps_position,from_iter_duplicate_keys,entry_match_shapes}` in
  `tests/preserve-encodings/tests.rs`, committed green on the old backing in their own commit, so
  the swap had to reproduce bytes rather than bless new ones — instead of trusting source reads
  (two independent ones agreed and were both wrong; only an executable differential probe —
  identical op sequences on both crates, iteration-order traces asserted equal — broke the
  premise). Residual: the pinned set covers the CONSUMER-PROBED mutation surface (insert,
  or_insert, from_iter, entry match shapes); other Deref-reachable mutation ops
  (`remove`/`extend`/`pop_*`) are differential-probed equal but unpinned, and other re-exposed
  dep surfaces (the `cbor_event` types in every deserialize signature) rely on the round-trip
  oracles alone, which do cover their read/write behavior but not order-adjacent mutation — no
  such surface exists there today. Working rule meanwhile: a change to the VERSION FLOOR or
  IDENTITY of a dependency whose types our public API re-exposes (a Deref target, a re-exported
  type, a type embedded in public signatures) first lands wire-level pins of the incumbent's
  consumer-reachable behavior in a separate commit, then swaps under them — and parity premises
  between two crates are established by executable differential probes, never by source reads.
  Mechanical layer on a SECOND swap/bump instance of this class (candidates: the cbor_event
  fork's crates.io upstreaming flip, a hashlink 0.13 floor raise): a curated dep-semantics pin
  suite per re-exposed surface, enumerated from the public-API type graph (which dep types appear
  in `pub` signatures/Deref targets), so the pin obligation is discovered by a gate rather than
  remembered by a rule.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
control_ids = ["testing.guard.dependency-swap-pass-compile-parity-existing-gate-silently"]
transition_ids = ["testing.trigger.dependency-swap-pass-compile-parity-existing-gate-silently"]

[[record.source_replacement]]
span_id = "span-record-dependency-swap-pass-compile-parity-existing-gate-silently"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.dependency-version-range-resolution-untested-generated-crates-float"
title = "Dependency version-RANGE resolution is untested: generated crates float on semver `cbor_event = \"3.3.0\"`, and nothing gates what that range actually resolves to over time."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Dependency version-RANGE resolution is untested: generated crates float on semver `cbor_event = \"3.3.0\"`, and nothing gates what that range actually resolves to over time.
"""
detail_md = """- **Dependency version-RANGE resolution is untested: generated crates float on semver
  `cbor_event = \"3.3.0\"`, and nothing gates what that range actually resolves to over time.** In
  check.ts runs the nested-cargo cells now resolve OFFLINE from the cargo cache (the
  registry-transient watch below), so the float enters one step removed — through the warm-up
  fetch, which pulls the newest semver-compatible releases into the cache — but the exposure is
  the same: a new upstream release changes what every nested-cargo cell — and every downstream
  consumer — builds against, with no gate noticing until something breaks. The
  3.2.0 upgrade itself arrived as exactly such a version event (the 2.4.0-era prediction this
  entry re-captures), absorbed deliberately with flip vectors; an UNPLANNED 3.x release would be
  absorbed silently. The mechanical layer when a release actually bites: a pinned-latest or
  `--minimal-versions`-style resolve check over one generated crate, red when the resolved
  `cbor_event` version drifts from the one the vectors were blessed against.
"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.dependency-version-range-resolution-untested-generated-crates-float"]

[[record.source_replacement]]
span_id = "span-record-dependency-version-range-resolution-untested-generated-crates-float"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.directive-dependent-emitter-edge-case-triggering-shape-hand"
title = "A directive-dependent emitter edge case whose triggering SHAPE is in no hand fixture is invisible to every combinatorial layer — the recombination fuzzer skips ALL `dsl.*` features wholesale (\"DSL comment in root RHS; not a clean reusable expression\"), so no fuzz composition ever carries a directive."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A directive-dependent emitter edge case whose triggering SHAPE is in no hand fixture is invisible to every combinatorial layer — the recombination fuzzer skips ALL `dsl.*` features wholesale (\"DSL comment in root RHS; not a clean reusable expression\"), so no fuzz composition ever carries a directive.
"""
detail_md = """- **A directive-dependent emitter edge case whose triggering SHAPE is in no hand fixture is
  invisible to every combinatorial layer — the recombination fuzzer skips ALL `dsl.*` features
  wholesale (\"DSL comment in root RHS; not a clean reusable expression\"), so no fuzz composition
  ever carries a directive.** Proven instance (probe-caught in review, then independently
  confirmed by reading — never a gate failure): the `definite_info` additive-length combine
  emitted a malformed `\"{n} + \"` expression when the conditional expression is EMPTY, a branch
  reachable only by an all-mandatory-fields open struct-map whose rest row is `@ignore`
  (tolerate-and-drop: no optionals, no captured-rest term) — no pre-existing spec could reach it,
  and only the delivery's own hand-enumerated fixtures (`details_map` in the
  `open_struct_map_ignore` snapshot, `baz` in the compiled e2e) happened to cover the degenerate
  combination. The standing discipline that catches the SHIPPED form is exactly that
  hand-enumeration: a new directive's spec must fixture the degenerate shape combos
  (all-mandatory, zero-field, empty-collection) alongside the happy paths — reviewers should
  demand the degenerate rows in the fixture list. Mechanical layer on the SECOND
  fuzzer-invisible directive-emitter instance: a directive AXIS for the recombination fuzzer —
  instead of skipping `dsl.*` ingredients, mint directive-BEARING variants of the applicable
  shape templates (rest-row `@ignore`/`@duplicates`/`@name` on map-record templates, rule-level
  `@duplicates` on collection templates), letting the existing generation sweep + layer-2
  compile gates see directive × shape products no hand fixture enumerates.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.directive-dependent-emitter-edge-case-triggering-shape-hand"]
transition_ids = ["testing.trigger.directive-dependent-emitter-edge-case-triggering-shape-hand"]

[[record.source_replacement]]
span_id = "span-record-directive-dependent-emitter-edge-case-triggering-shape-hand"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.directive-effect-round-trip-coherence-sweep-accepted-custom"
title = "A directive-effect ROUND-TRIP COHERENCE sweep: every accepted custom-codec placement executes write-then-read as an identity, with inverse stub codecs."
projection_group = "next-priority"
legacy_aliases = ["Next work 16"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A directive-effect ROUND-TRIP COHERENCE sweep: every accepted custom-codec placement executes write-then-read as an identity, with inverse stub codecs.
"""
detail_md = """16. **A directive-effect ROUND-TRIP COHERENCE sweep: every accepted custom-codec placement
    executes write-then-read as an identity, with inverse stub codecs.** The class this catches is
    invisible to both existing systems: `no_silent_directive` measures whether a directive has an
    EFFECT (a routed single half passes it), and the executed custom-pair e2e fixtures assert
    round-trips only for the complete-pair placements someone hand-wrote. What slipped between
    them, twice on 2026-08-06 (both by ad-hoc probing rather than any gate): a single
    codec half on a transparent alias was accepted and ROUTED — every embed site wrote the custom
    wire and read the default one, a silent asymmetric round-trip; and the FIELD-level single half
    shipped exactly that asymmetry at exit 0. Both are refused now, and their GRID rejection cells
    are the control — but what those refusals close is the two spellings someone happened to probe,
    not the class: every ACCEPTED placement remains unmeasured for round-trip identity.
    Mechanical shape: for each accepted `@custom_serialize`/`@custom_deserialize` placement (field,
    alias, record rule, complete-item table owner, both table positions, open-map rest row) × profile (default, preserve,
    canonical), generate with stub codecs that are true inverses of each other but NOT of the
    default wire (the hex-text pair from the e2e fixtures is the template), then execute
    `from_cbor_bytes(to_cbor_bytes(v)) == v` through a holder. Any placement whose two directions
    disagree fails identity immediately. Include the rule-CLASS flavor axis the transparent
    custom-codec-alias delivery had
    to enumerate by hand (loose/non-empty/bounded/`@duplicates` collection flavors): a coherence
    sweep that misses a flavor leaves that flavor exactly the silent carrier the pair rejections
    just closed.
    **Pending implementation phase — add the modifier matrix.** Cross EVERY accepted complete-pair
    placement (at minimum the named-record and complete whole-table rule-level owners) with BOTH
    `@custom_encodings` and `@custom_wire_major`. The test's expected-outcome table must make each
    cell prove one of two results: the modifier changes an emitted signature/storage slot at a real
    consumption channel, or generation rejects. Exit-0 generation is a FAILURE for every cell that
    names no such channel. The rule-level-owner minimum is:

    | owner | `@custom_encodings` | `@custom_wire_major` |
    | --- | --- | --- |
    | named record | reject: self-carrying struct, no encoding tuple crosses the codec call | reject: only a transparent alias keying an open table can consume a major |
    | complete whole-table wrapper | reject: self-carrying wrapper, no encoding tuple crosses the codec call | reject: only a transparent alias keying an open table can consume a major |

    The table owner’s `@custom_encodings` row is the inspection escape that prompted this phase:
    acceptance at exit 0 is specifically a silent-directive failure, not a pass. Add equivalent
    expected-outcome rows for the remaining accepted alias/field/table-position/open-row channels;
    those that are honored must assert the actual emitted signature and storage change, not merely a
    round trip.

"""
work_state = "ready"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.directive-effect-round-trip-coherence-sweep-accepted-custom's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""
admission_ids = ["testing.admission-path.directive-effect-round-trip-coherence-sweep-accepted-custom"]

[[record.source_replacement]]
span_id = "span-record-directive-effect-round-trip-coherence-sweep-accepted-custom"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.directive-s-documented-sentence-stay-true-consumer-visible"
title = "A directive's documented sentence can stay true while its consumer-visible MEANING changes underneath it, because the artifact it controls changed shape."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A directive's documented sentence can stay true while its consumer-visible MEANING changes underneath it, because the artifact it controls changed shape.
"""
detail_md = """- **A directive's documented sentence can stay true while its consumer-visible MEANING changes
  underneath it, because the artifact it controls changed shape.** `@no_json_schema_export` shipped
  meaning \"no registration row for this type\", pinned at the row level by
  `snapshot_tests::json_gen_extern_schema_rows` and the accepted/rejected table in
  `robustness_tests::no_json_schema_export_misuse_rejects_gracefully`. Both pins still pass verbatim
  after the JSON surface became one document per crate — and yet the directive now means something
  materially different to a consumer: it drops the ROOT DECLARATION, not the definition, so a
  suppressed type that a published type references is now DECLARED in the shipped `.d.ts` where it
  was previously a dangling `TS2304`. Nothing could have caught that, because every pin was on the
  emitted intermediate and none was on the surface the directive's own documentation names (\"not
  part of the published JSON-schema surface\"). The gap generalizes past this directive and past
  JSON: a pin on generated source answers \"did the emitter branch\", never \"does the shipped artifact
  say what the directive promises\". Note this is a DIFFERENT class from the
  directive×rule-shape reachability product entry, whose verdict is {effect visible, loudly
  rejected} on a byte-compare of generated source — that sweep reads \"effect visible\" for this
  directive both before and after the change. Owned meanwhile by the end-to-end pin in
  `integration_tests::package_json_pipeline` (a `@no_json_schema_export` rule that a row'd type
  embeds, asserted `$defs`-present and `.d.ts`-declared), which exists precisely so the directive's
  promise is checked where a consumer reads it. Mechanical layer, on the second instance: for each
  directive that names a SHIPPED artifact in its documentation, require one pin on that artifact
  rather than on the emitted source — the directive inventory already exists
  (`KNOWN_RULE_METADATA_TAGS`, mirrored by `corpus_detect.ts`), so the addition is a per-directive
  datum naming its shipped surface, alongside the witness-profile datum the reachability sweep
  needs. Trigger: a second directive whose meaning shifts under an artifact-shape change, or a
  consumer reporting that a directive did not do what its documentation says while every gate stayed
  green.
  **Scope note, from a near-miss in a different flavor:** this entry and its mechanical layer are
  DIRECTIVE-scoped, and the same \"documented promise nobody pins\" class reaches CLI flags and the
  emitted runtime too, where no equivalent inventory exists to hang a per-item datum on. The
  near-miss (review-caught before shipping, so no instance is on record): the `--json-schema-export`
  docs stated that a document failing the closure check \"never reaches `schemas/`\", which is true of
  the failing document and false about the directory — `export_schemas()` creates `schemas/` before
  the check and, on failure, writes nothing, so an EARLIER export's document stays on disk and a
  pipeline ignoring the non-zero exit compiles it. Fixed in the prose rather than by a pin, because
  a filesystem-effect claim has no artifact inventory to enumerate. If a flag-level instance actually
  ships, the layer to build is the same shape one rung out: a per-flag datum naming the artifact its
  documentation makes claims about.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.directive-s-documented-sentence-stay-true-consumer-visible"]
transition_ids = ["testing.trigger.directive-s-documented-sentence-stay-true-consumer-visible"]

[[record.source_replacement]]
span_id = "span-record-directive-s-documented-sentence-stay-true-consumer-visible"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
title = "A docs-vs-behavior conformance harness for `comment_dsl.mdx` / `output_format.mdx`"
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """A docs-vs-behavior conformance harness for `comment_dsl.mdx` / `output_format.mdx`
"""
detail_md = """- **A docs-vs-behavior conformance harness for `comment_dsl.mdx` / `output_format.mdx`** (snippet
  extraction + output spot-checks): emitted output is already pinned by the snapshot corpus and
  DSL-name drift by the `cddl-matrix/verify.ts` forward lint; a doc-snippet system is heavy
  machinery for prose drift that review catches at its actual rate. The decline has narrowed since
  it was made: identifier-existence drift (a cited pin that no longer exists) is now mechanically
  covered by the `lint_doc_citations` gate, so what stays declined is prose SEMANTICS only — a
  sentence whose cited pin exists but whose claim about it is wrong. A THIRD prose home joins
  `docs/` and the generated `COVERAGE.md` span: a FIXTURE HEADER commenting on a SIBLING fixture.
  `tests/robustness/choice_cbor_ref_arm.cddl` described the rule-body spelling as \"the remaining
  unsupported spelling … unaffected by rule ordering\" — a claim its sibling
  `cbor_ref_rule_body.cddl` contradicted in its own header from the commit that fixed both, and
  which the catalog snapshot (`ok`) had already recorded as false. Fixture prose is outside
  `lint_doc_citations`' scan surface for the same reason catalog prose is, and the claim named no
  artifact that could dangle, so this stays in the declined class. It is recorded as evidence the
  decline is calibrated rather than as a gap: review caught it one cycle after it shipped, which is
  the \"actual rate\" the decline is priced at, and the reopening signal for this class is a false
  claim SHIPPING PAST review — not one being caught by it. Authoring rule that would have prevented
  it at zero cost: a fixture header states what THIS fixture pins, and refers to a sibling only by
  name and role, never by asserting the sibling's support status — the catalog snapshot is where
  that status lives, and it is already gate-enforced. NB this decision covers PROSE
  drift only — the separate class of a directive silently no-oping in an unenumerated attachment
  position (`@name` was dropped on arrow keys once and bareword keys once, both found by hand) is
  a real class with its own standing system: the directive × attachment-position sweep
  `src/tests/dsl_position_tests.rs`, hard-asserted against `comment_dsl.mdx`'s claims. (The `example/`-half of this
  decision — gating the getting-started command — was accepted and shipped as
  `integration_tests::getting_started_example`.) A SECOND carve-out (2026-07-25): SYNTACTICALLY
  INVALID directive illustrations are not prose-semantics drift either, and review's \"actual
  rate\" proved one delivery late for them — the open-arrays doc pass shipped an inline example in
  the one-line directive-before-closing-bracket spelling (the CDDL comment swallows the `]`) two
  bullets BELOW the doc's own warning against exactly that spelling; caught in orchestrator
  review, one delivery after the same trap class was first documented for the map-brace flavor.
  The proven spelling class is now mechanically banned: `lint_doc_citations` scans
  `docs/docs/*.mdx` inline spans and fenced examples for a `; @<directive>` comment sharing its
  line with a closing `}`/`]` (self-checked canary; the warning bullet's deliberate
  counterexamples are allowlisted by exact span text). What STAYS declined is the general
  snippet-extraction harness; its reopening signal is now concrete: a docs example invalid for a
  reason the narrow spelling ban cannot see (any non-directive syntax error class recurring in an
  illustration).
  A THIRD instance class on record (2026-08-04), counted against the decline without reopening
  it: a FALSE UNIVERSAL. `current_capacities.mdx`'s tag-rule section claimed the auto-wrap holds
  for \"*every* single-type inner\" while the collection and `T / null` inners registered
  transparent aliases (the T1-13 standalone-tag-drop defect, per the tier-1 close-out's board
  record) — a false claim that SHIPPED past review and stood until the `register_type_alias`
  wire-facts assert's carve-out enumeration falsified it mechanically. It does NOT reopen the
  snippet harness because that harness could not have caught it either: the counterexamples were
  unenumerated instances of a universal quantifier, never spelled as snippets — snippet
  extraction validates the examples a doc gives, not the quantifier over the ones it doesn't.
  The layer that actually catches this class is a behavior-side seam assert whose domain IS the
  quantifier's (the wire-facts assert enumerates every registration, so the doc's \"every\"
  now has a mechanical counterpart), which is where such universals should be anchored: a doc
  universal about generated-code behavior earns a seam assert or a sweep whose axis is the
  quantified domain, and the prose then describes the mechanism rather than substituting for it.
  A FOURTH instance class on record (2026-08-05), counted against the decline without reopening
  it — and the sharpest, because here the doc and the behavior AGREED: `output_format.mdx`'s
  trailing-bytes paragraph stated \"(Nested/embedded decoding — e.g. `bytes .cbor T` — is
  unaffected; only the top-level entry point enforces this.)\" — a TRUE sentence about a DEFECT
  (the `.cbor` embed's missing exhaustion check, the cycle-13 fuzz find, fixed in `cff85166`).
  Any docs-vs-behavior conformance harness passes it by construction, since there is no
  divergence to detect; what was wrong was the boundary itself, stated as if decided when no
  decision record existed. The layer that caught it is a SPEC-side oracle over the behavior —
  the byte-fuzzer's preserve-fidelity leg (`fuzz_bounded_run`), whose contract comes from RFC
  8610 §3.8.4 + the preserve round-trip promise rather than from our own prose — and that layer
  is now standing. The authoring rule this instance adds, same zero-cost class as the
  fixture-header rule above: a docs sentence that states a LIMITATION or asymmetric boundary
  names its provenance — the decided-posture record (a README gotcha, a matrix out-of-scope row)
  or the findings/roadmap entry that owns it — so an anchorless boundary sentence is visible at
  review as an undecision wearing decided clothes. Reopening signal for that rule, measurable by
  whoever fixes the next such defect: a second anchorless boundary sentence found to have
  documented a defect as intended, after this rule was in force.
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx.
"""
authority_reference_id = "ref-testing-docs-vs-behavior-conformance-harness-comment-dsl-mdx-authority"
permanence = "reopenable"
transition_ids = ["testing.trigger.docs-vs-behavior-conformance-harness-comment-dsl-mdx"]

[[record.source_replacement]]
span_id = "span-record-docs-vs-behavior-conformance-harness-comment-dsl-mdx"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.documented-failure-shape-exercises-prose-cross-crate-int"
title = "A documented failure SHAPE that no cell exercises is prose — the cross-crate `Int` channel has three, plus a hand mirror nothing cross-checks."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A documented failure SHAPE that no cell exercises is prose — the cross-crate `Int` channel has three, plus a hand mirror nothing cross-checks.
"""
detail_md = """- **A documented failure SHAPE that no cell exercises is prose — the cross-crate `Int` channel has
  three, plus a hand mirror nothing cross-checks.** The shipped
  `Int`-under-`--common-import-override` coverage proves the positive paths and asserts row-ABSENCE
  on the degraded path, but none of the three failure shapes documented in
  `command_line_flags.mdx`'s `--common-import-override` section is compile-proven by a standing cell:
  (a) the consumer's `_borrowed_key_types_self_check` going RED when the common crate's `Int` lacks
  the demanded flavor (needs a de-flavored fixture variant); (b) the unresolved-import failure when
  the common crate exposes no `Int`/`IntError` (the existence contract's loud half); (c) `E0277` at
  the consumer's own map sites when the override is not a `--workspace-dep` (today only the no-row
  half is asserted, by `int_key_via_common_import_override_sidecar`). Related mirror exposure, to
  close alongside (a): `tests/extern-dep-crate`'s hand-written `Int` mirrors generated `Int`'s
  preserve-encodings wire format and encoding-insensitive key semantics, but `extern_deps` serializes
  AND deserializes through the fixture's own impls, so a generated-`Int` divergence passes silently —
  unlike the `tests/wasm-macro-crate` mirror, which compilation enforces. The cheap drift gate:
  cross-serialize one vector between a generated `Int` (any no-override cell's output) and the
  fixture's, asserting byte equality both ways. Reopening signal on the axis the cost grows along,
  measurable by the party who has the problem: a consumer hitting any of the three failure shapes and
  reporting that the error they got was not the one the docs describe — or a generated-`Int` wire
  change landing while `extern_deps` stays green.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.documented-failure-shape-exercises-prose-cross-crate-int"]
transition_ids = ["testing.trigger.documented-failure-shape-exercises-prose-cross-crate-int"]

[[record.source_replacement]]
span_id = "span-record-documented-failure-shape-exercises-prose-cross-crate-int"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.documented-flag-pairing-broken-fixture-stays-green-because"
title = "A DOCUMENTED flag pairing can be broken while every fixture stays green, because a fixture satisfies the pairing's preconditions by accident."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A DOCUMENTED flag pairing can be broken while every fixture stays green, because a fixture satisfies the pairing's preconditions by accident.
"""
detail_md = """- **A DOCUMENTED flag pairing can be broken while every fixture stays green, because a fixture
  satisfies the pairing's preconditions by accident.** Proven instance (found by reading the fixture,
  not by any gate; consumer-reported first): `command_line_flags.mdx`'s own `--extern-wasm-crate`
  example (`--common-import-override=cml_core --extern-wasm-crate=cml_core=cml_core_wasm`) panicked
  at startup validation for every PURE consumer — one whose override crate is not also a declared
  extern dep — from the `Int`-override feature's ship until the report, because `extern_deps_wasm`'s
  override crate doubles as a declared dep and so satisfied the key check by accident. The exact cell
  the doc example describes was never executed. Standing coverage now: `common_override_wasm_int`
  pins the pure-consumer cell. Working rule meanwhile: a flag pairing added to the user docs names
  the cell that executes it, and a cell whose fixture satisfies a precondition incidentally is not
  that cell. Mechanical layer, on a SECOND documented pairing found broken: a sweep executing each
  `command_line_flags.mdx` example invocation verbatim against a minimal spec that assumes only what
  the example's own prose states, asserting generation exits zero. It is the documented-VALID
  complement of the class-level validation-smoke sweep in § Declined's flag-powerset entry (which
  asserts documented-INVALID values reject), and the two should be built together if either is.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.documented-flag-pairing-broken-fixture-stays-green-because"]
transition_ids = ["testing.trigger.documented-flag-pairing-broken-fixture-stays-green-because"]

[[record.source_replacement]]
span_id = "span-record-documented-flag-pairing-broken-fixture-stays-green-because"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.doubled-doc-comment-markers-emitted-docs-proven-instance"
title = "Doubled doc-comment markers in emitted docs — one proven instance recorded, no machinery yet."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Doubled doc-comment markers in emitted docs — one proven instance recorded, no machinery yet.
"""
detail_md = """- **Doubled doc-comment markers in emitted docs — one proven instance recorded, no machinery
  yet.** The `codegen` builder fork prefixes EVERY newline-separated line of a doc string with
  the marker itself, so an emission-site string embedding a literal marker to \"continue\" a
  multi-line doc ships doubled markers (rustdoc renders the extra slashes as literal text).
  Proven instance: the wasm NonEmpty wrapper doc rewording embedded literal markers after its
  `\\n`s and landed at HEAD inside a commit whose snapshot suite was left stale-red; the next
  bless folded the bug into ~660 golden snapshots and only post-bless diff REVIEW caught it —
  the snapshot layer pins bytes, so it flags a change but blesses a defect just as happily, and
  `generated_code_clippy_clean` cannot see it (no clippy lint fires on a doc comment's text).
  Mechanical layer if the class recurs: a catalog-wide scan asserting no doubled-marker line in
  any `*.snap` (cheap, pure grep). Meanwhile the working rules are: bare `\\n` in `.doc()`
  strings (the builder owns the markers), and the § \"Blessing changes\" discipline — review the
  blessed diff, never accept blind, ESPECIALLY when snapshots were already red before your
  change (the diff then contains someone else's unreviewed delta interleaved with yours).
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.doubled-doc-comment-markers-emitted-docs-proven-instance"]
transition_ids = ["testing.trigger.doubled-doc-comment-markers-emitted-docs-proven-instance"]

[[record.source_replacement]]
span_id = "span-record-doubled-doc-comment-markers-emitted-docs-proven-instance"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.dsl-position-tests-string-level-design-green-emitted"
title = "`dsl_position_tests` is string-level by design, so a cell can be GREEN while the emitted crate is broken."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """`dsl_position_tests` is string-level by design, so a cell can be GREEN while the emitted crate is broken.
"""
detail_md = """- **`dsl_position_tests` is string-level by design, so a cell can be GREEN while the emitted crate is
  broken.** Proven instance (caught by a hand-run end-to-end repro during review, not by any gate):
  the `@custom_json` record-struct cell first landed passing while the generated struct kept its
  serde derives against a now-unskipped `encodings` field (E0277). The landed mitigation is paired
  anchors — the cell asserts the derive line AND the skip attribute, on both the directive and
  control sides — which covers known failure modes only. Working rule meanwhile: a cell whose
  expectation is compile-relevant asserts every attribute the compile outcome depends on, not just
  the one the directive names. Mechanical layer on a SECOND green-but-uncompilable cell: a
  `cargo check` probe over the compile-relevant cells only. Weigh it against the sweep's whole
  premise of cheapness (it runs in ~1 s today; a compile leg is nested-cargo-priced and would need
  the gate cache), which is why the probe is scoped to those cells rather than to the grid.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.dsl-position-tests-string-level-design-green-emitted"]
transition_ids = ["testing.trigger.dsl-position-tests-string-level-design-green-emitted"]

[[record.source_replacement]]
span_id = "span-record-dsl-position-tests-string-level-design-green-emitted"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.duplicates-policy-residuals"
title = "Duplicates-policy residuals."
projection_group = "next-priority"
legacy_aliases = ["Next work 2"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """Duplicates-policy residuals.
"""
detail_md = """2. **Duplicates-policy residuals.** Both `@duplicates` flavors are shipped on every boundary —
   `reject` (set/array uniqueness twins) and `preserve` (table pair-map twins), covering rust,
   preserve-encodings, canonical, JSON/schemars, wasm-bindgen, component/WIT, extern-interface
   projection, and the
   `dsl.duplicates.{reject,preserve}` matrix feature rows. Current state lives in
   `tests/README.md` § \"Per-rule duplicates policy (`@duplicates`) — test map\" (the per-layer
   pin inventory) and the user docs (`docs/docs/output_format.mdx`, `current_capacities.mdx`,
   `wasm_differences.mdx`, `comment_dsl.mdx`). What remains:
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-duplicates-policy-residuals-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.duplicates-policy-residuals's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record.source_replacement]]
span_id = "span-record-duplicates-policy-residuals"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.duplicates.dedicated-generic-collision-message"
title = "Dedicated collision message for the generic-instantiation naming family."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Dedicated collision message for the generic-instantiation naming family.
"""
detail_md = """     - *Dedicated collision message for the generic-instantiation naming family.* An inline
       shape-derived nominal name colliding with a user rule or a structurally-different generic
       instantiation is refused with a set-specific message
       (`inline_258_nominal_name_collision_is_rejected`). The GENERIC-INSTANTIATION family
       (`SetKeyHash` from `set<key_hash>` vs a user `set_key_hash` rule) still falls through to the
       generic duplicate-top-level-ident backstop, whose \"list/map wrapper families\" text is
       misleading for a set nominal — a dedicated per-kind sibling (the decided per-naming-family
       detector structure) is message polish, deferred until a real collision makes the generic
       message actively wrong.
"""
work_state = "armed"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.duplicates-dedicated-generic-collision-message"]
transition_ids = ["testing.trigger.duplicates-dedicated-generic-collision-message"]

[[record.source_replacement]]
span_id = "span-part-part-dedicated-collision-message-generic-instantiation-naming"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.duplicates.member-position-extension"
title = "Member-position `@duplicates` extension."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Member-position `@duplicates` extension.
"""
detail_md = """     - *Member-position `@duplicates` extension.* The inline opt-out is hoist-to-named-rule (no new
       DSL surface); a member-position `; @duplicates` directive on an inline set was DEFERRED until
       a real spec demands it. It looks one-line (the member-position rejection seam in `parsing.rs`
       is ready) but carries hidden machinery: a per-site policy on a named-rule or generic-instance
       reference forks one shared type into two, so it must be restricted to inline shapes, and even
       there the shape-deduped synthesized names would need the policy folded into type identity
       plus policy-variant name disambiguation. Every directive is permanent surface (parser,
       LOCKSTEP `corpus_detect` mirror, docs, wasm matrix); the deferral costs no architectural
       rework (the rejection seam stays), and CML-shaped CDDL never hits it (Cardano writes sets
       through the named generic `set<a0>`, where the rule-level directive already works). Reopening
       signal: a third-party author who won't restructure a spec to hoist.

"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.duplicates-member-position-extension"]

[[record.source_replacement]]
span_id = "span-part-part-member-position-duplicates-extension"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.duplicates.occurrence-aware-generic-identity"
title = "Occurrence-aware generic-instance identity."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Occurrence-aware generic-instance identity.
"""
detail_md = """     - *Occurrence-aware generic-instance identity.* The canonical instance ident still derives
       each argument from bounds-blind `for_variant()`: two uses of the SAME generic definition,
       `set<([* uint])>` and `set<([*5 uint])>`, both become `SetArrU64`, so the later registration
       can replace the former even though their native carriers are `Vec<u64>` and
       `BoundedVec<u64, 0, 5>`. This is a separate parsing-time namespace contract, not part of the
       table-keys boundary normalization. Until the canonical spelling becomes occurrence-aware,
       bind each argument shape to a distinctly named rule before instantiation (or use distinct
       generic definitions). Add a focused rust+wasm compile regression with the fix; a
       generation-only same-definition probe is insufficient because last-registration-wins can
       still exit 0 with the wrong carrier.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.duplicates-occurrence-aware-generic-identity"]

[[record.source_replacement]]
span_id = "span-part-part-occurrence-aware-generic-instance-identity"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.duplicates.same-shape-set-conversion"
title = "`From` between same-shape set instantiations."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """`From` between same-shape set instantiations.
"""
detail_md = """     - *`From` between same-shape set instantiations.* `set<xs_int>` and `set<[* uint]>` (where
       `xs_int = [* uint]`) mint DISTINCT nominals over the same wire shape by the
       spelling-is-identity rule; a member-wise-encoding `From` between two such structurally
       identical nominals would let a consumer convert without a rebuild, but it needs
       structurally-identical-nominal-pair DETECTION that does not exist yet. Niche — CML writes
       sets through one generic `set<a0>`, so it never mints a same-shape pair; build it when a real
       spec mints the pair. The set ergonomics that DID ship (`Deref`/`DerefMut`, both
       `IntoIterator`s, `From`/`TryFrom` Vec, and the std set contract — `insert`/`contains`/
       `Extend`/`FromIterator`/`sort`, the twin refinement doors, `try_opt_from`) are inventoried
       in the user docs (`docs/docs/output_format.mdx` § \"Nominal tag-258 set types\").
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.duplicates-same-shape-set-conversion"]

[[record.source_replacement]]
span_id = "span-part-part-same-shape-set-instantiations"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.e0463-t-find-crate-core-scratch-tree-std"
title = "An `E0463: can't find crate for core` from a SCRATCH-TREE no-std-check run is a toolchain artifact until proven otherwise — and \"fails identically on the baseline binary\" is not that proof, because an environmental failure is binary-independent by construction."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_operational_watch"
summary_md = """An `E0463: can't find crate for core` from a SCRATCH-TREE no-std-check run is a toolchain artifact until proven otherwise — and \"fails identically on the baseline binary\" is not that proof, because an environmental failure is binary-independent by construction.
"""
detail_md = """- **An `E0463: can't find crate for core` from a SCRATCH-TREE no-std-check run is a toolchain
  artifact until proven otherwise — and \"fails identically on the baseline binary\" is not that
  proof, because an environmental failure is binary-independent by construction.** Proven
  2026-08-06 (the tagged-preserve-table delivery): an implementation agent's scratch e2e pass ran a generated
  `no-std-check/` shim outside the repo directory, where `rust-toolchain.toml` does not govern, so
  cargo used the DEFAULT toolchain — which lacks the `thumbv7m-none-eabi` target — and the run
  died E0463 on `core`. The agent controlled for its own change by re-running on the baseline
  binary (also red, necessarily) and reported a \"pre-existing no-std-check defect under
  `--json-serde-derives --json-schema-export`\"; the claim propagated into cycle bookkeeping before
  a disambiguating re-run under the PINNED toolchain (`rustup run <pinned> cargo check --target
  thumbv7m-none-eabi`) compiled the same shim clean and retired it. The per-toolchain-target trap
  was already documented for the GATE (AGENTS.md § fresh-worktree setup); what this watch adds is
  the attribution rule for AD-HOC shim runs: before ledgering any scratch no-std E0463, re-run
  under the pinned toolchain (or from inside the repo directory, where the pin governs) — a
  baseline differential distinguishes regression from pre-existing, never product from
  environment. (The clean pinned-toolchain run also showed 2 `unused_imports` warnings in the
  generated crate under the json flags — the class the \"unused_imports on generated crates\" work
  item above already owns; noted here so the sighting is not re-derived as new.)

"""
watch_state = "attributed"
signature_md = """Scratch no-std `E0463: can't find crate for core` outside the repository pin.
"""
attribution_md = """The proven sighting was default-toolchain target absence, not generated-code behavior.
"""
operating_rule_reference_id = "ref-testing-e0463-t-find-crate-core-scratch-tree-std-operating"
response_md = """Rerun under the repository pin before product attribution.
"""
escalation_transition_id = "testing.trigger.e0463-t-find-crate-core-scratch-tree-std"
retirement_semantics_md = """Retire when every ad-hoc scratch recipe spells the pin and a durable procedure enforces it.
"""

[[record.payload.capture_step]]
step_id = "signature"
capture_md = """Capture cwd, exact cargo/rustc/toolchain/target, target-installed status, baseline differential, and pinned-toolchain rerun.
"""

[[record.payload.capture_step]]
step_id = "scope"
capture_md = """Record the full untruncated log or consumer report, command, full commit, date, environment, relevant versions, and the explicitly unprobed remainder; never cite a disposable draft log as durable evidence.
"""

[[record.source_replacement]]
span_id = "span-record-e0463-t-find-crate-core-scratch-tree-std"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.edn-text-notation-round-trip-edn-edn-draft"
title = "EDN text-notation round-trip (`to_edn` / `from_edn`, draft-ietf-cbor-edn-literals)."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """EDN text-notation round-trip (`to_edn` / `from_edn`, draft-ietf-cbor-edn-literals).
"""
detail_md = """- **EDN text-notation round-trip (`to_edn` / `from_edn`, draft-ietf-cbor-edn-literals).** EDN is
  the text notation that *can* be byte-faithful where JSON structurally cannot (encoding
  indicators, NaN payload bits — JSON's number model collapses both), making it the natural
  third channel: JSON as the value-level lossy view by charter, CBOR as the authoritative
  bytes, EDN as the human-readable byte-faithful text form. The maintainer's `cbor_event_edn`
  library already handles the hard edge cases (NaN payloads included) and is the natural base.
  Deferred because the EDN spec is an evolving IETF draft — a generated-crate surface built
  against it now risks churn with every draft revision. Reopening signal: the draft stabilizes
  (late-stage or RFC) or a real consumer needs byte-faithful text round-trips sooner.
  Mechanical layer: a property over the `AnyCbor` fuzz corpus asserting
  `from_edn(to_edn(x))` byte-identity against the existing span oracle
  (`src/tests/any_cbor_tests.rs`).
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.edn-text-notation-round-trip-edn-edn-draft"]

[[record.source_replacement]]
span_id = "span-record-edn-text-notation-round-trip-edn-edn-draft"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.emission-branch-generated-output-fixture-exercises-invisible-gate"
title = "An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate — it can rot to uncompilable and hide behavioral bugs in both directions."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate — it can rot to uncompilable and hide behavioral bugs in both directions.
"""
detail_md = """- **An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate —
  it can rot to uncompilable and hide behavioral bugs in both directions.** First of a family of
  three, each recorded separately below because each needs a different detector, and all three
  answering \"a fixture exists, so why did it not witness this?\" with a different mode: NEVER
  EXECUTED (here), EXECUTED BUT UNASSERTED (\"Value-anchor rot on field addition\"), and EXECUTED
  AGAINST THE WRONG SHAPE (\"A fixture that models an OBSOLETE user contract stays green while the
  real contract path is broken\"). Proven instance
  (plan-review-caught during the cbor_event 3.2.0 upgrade, not by any gate): the
  nullable-`Special` null-peek (the `Optional`-with-Special arm in `generation/deserialize.rs`)
  had ZERO checked-in output — no snapshot, export, or suite fixture contained its emission — and
  had decayed to emitting a receiver-less `match cbor_type()?` (E0425, uncompilable) around a
  hardcoded 1-byte rewind that both ACCEPTED malformed two-byte simples (`f8 f5` read as
  `Some(true)`) and REJECTED valid 9-byte floats. Compile, round-trip, and conformance gates were
  all blind because no input ever reached the branch. Standing coverage now: the
  `nullable_specials` fixtures (core + preserve suites) execute the emission in both directions.
  Working rule meanwhile: a new emission branch lands WITH a fixture whose generated output
  exercises it (compiling the generator is not evidence the emitted string compiles). The
  mechanical layer on a second instance: a generator branch-coverage sweep — e.g. llvm-cov over
  the corpus + suite generation runs, red on emission arms no fixture executes — the only layer
  that catches the class without knowing each branch by name.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.emission-branch-generated-output-fixture-exercises-invisible-gate"]
transition_ids = ["testing.trigger.emission-branch-generated-output-fixture-exercises-invisible-gate"]

[[record.source_replacement]]
span_id = "span-record-emission-branch-generated-output-fixture-exercises-invisible-gate"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.emit-tests-gate-asserts-round-trip-green-cannot"
title = "An `--emit-tests` gate that asserts only \"the round-trip is green\" cannot tell an INTENDED minted value from a wrong one that happens to land in-window."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """An `--emit-tests` gate that asserts only \"the round-trip is green\" cannot tell an INTENDED minted value from a wrong one that happens to land in-window.
"""
detail_md = """- **An `--emit-tests` gate that asserts only \"the round-trip is green\" cannot tell an INTENDED
  minted value from a wrong one that happens to land in-window.** Proven by the bounded-map-key
  minter: choosing a `nint` key base in VALUE space emitted that base as a u64 MAGNITUDE, so
  `{* nint .le -5 => uint}` minted magnitude 18446744073709551611 — wire value about -1.8e19, which
  SATISFIES `<= -5`, so the emitted round-trip passed. A false green, and the harder half to
  notice: the sign-mirror `{* nint .ge -5 => uint}` failed loudly on the identical wrong base, so a
  fixture carrying only ONE sign of the window would have certified the defect in whichever
  direction it was pointed. The conformance oracle (`--emit-tests-conformance`) cannot see this
  class either — the minted value is genuinely spec-VALID, merely degenerate — which is why it
  needs a layer of its own rather than a wider oracle. The standing rule that covers this today is
  IN FORCE and lives in `tests/README.md` § \"Authoring standard for a bounded-domain emit-tests
  fixture\" (both signs/endpoints of the window; the gate pins the minted SPELLING, compared
  whitespace-stripped, not just the round-trip verdict), with
  `tests/emit-tests-bounded-key/` as that fixture. What remains future here is only the mechanical
  layer: a mint-intent assertion — derive each bounded mint's expected
  literal from the same `MintValue` the renderer consumes and diff the two — so that no bounded
  mint can be wrong-but-passing. Trigger, on the axis the cost grows along: the COUNT of
  minted-value sites whose STORAGE space differs from the CDDL value space, measurable by whoever
  next adds a bounded mint. Today that count is one (the `N64` magnitude, transformed through
  `nint_bounds_to_u64`); a SECOND such site means the working rule is being carried by hand in more
  than one place, and hand-carrying is what produced this instance.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
control_ids = ["testing.guard.emit-tests-gate-asserts-round-trip-green-cannot"]
transition_ids = ["testing.trigger.emit-tests-gate-asserts-round-trip-green-cannot"]

[[record.source_replacement]]
span_id = "span-record-emit-tests-gate-asserts-round-trip-green-cannot"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.emitted-shape-lint-classes-outside-clippy-all-beyond"
title = "Emitted-shape lint classes OUTSIDE `clippy::all` are beyond `generated_code_clippy_clean`'s reach — the wasm-boundary clone-of-owned class stays ledgered, no machinery yet."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Emitted-shape lint classes OUTSIDE `clippy::all` are beyond `generated_code_clippy_clean`'s reach — the wasm-boundary clone-of-owned class stays ledgered, no machinery yet.
"""
detail_md = """- **Emitted-shape lint classes OUTSIDE `clippy::all` are beyond `generated_code_clippy_clean`'s
  reach — the wasm-boundary clone-of-owned class stays ledgered, no machinery yet.** The
  boundary ops (`from_wasm_boundary_clone`) clone every non-Copy expr regardless of the call
  site's ownership, so a wasm ctor over an owned last-use arg emits `Holder::new(val.clone())` — a
  redundant clone `clippy::redundant_clone` would flag but is nursery-tier, so the gate's `-D
  clippy::all` cannot see it; behavior and bytes are unaffected (allocation cost only). One
  neighboring FLAVOR of this class is now retired at the source and needs no ledger: the rust
  `From`/`TryFrom` wrapper impls used to route their owned `inner` through the same boundary ops
  (`new(inner.clone().into())`), but they now construct via `new(inner)` directly — the `inner`
  is by-value and already `new()`'s exact param type, so both the clone and the `.into()` were
  identity (also a `clippy::useless_conversion` the gate DOES see, provoked by `clippy_wrapped_map`
  in `generated_code_clippy_clean`'s input). The wasm-boundary clone-of-owned flavor above stays
  ledgered with unchanged disposition, mirroring the curated rustc style-deny precedent
  (`unused_parens` et al.): evaluate specific beyond-`all` lints one at a time, adding a per-lint
  deny only if it is currently green-able on every one of the gate's cases (nursery lints carry
  known false positives). Act on a second instance or a consumer report, not before.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.emitted-shape-lint-classes-outside-clippy-all-beyond"]
transition_ids = ["testing.trigger.emitted-shape-lint-classes-outside-clippy-all-beyond"]

[[record.source_replacement]]
span_id = "span-record-emitted-shape-lint-classes-outside-clippy-all-beyond"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
title = "An emitter that DROPS an overloadable parameter by building a fresh config is invisible to the lint that catches one which hardcodes the default."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """An emitter that DROPS an overloadable parameter by building a fresh config is invisible to the lint that catches one which hardcodes the default.
"""
detail_md = """- **An emitter that DROPS an overloadable parameter by building a fresh config is invisible to the
  lint that catches one which hardcodes the default.** The hardcoding half is covered:
  `snapshot_tests::emitter_overload_no_bare_default_tokens` (fast tier, current state in
  `tests/README.md` § \"Snapshot tests\") fails any overload-scoped emitter fn whose emitted literal
  spells a bare `raw`/`serializer`, and its arming run cleared both axes — the four historical
  deserialize leaves stay fixed, and the one serializer leaf it found (`end_len`'s `is_end` branch,
  reachable only with the default name today because every overload site sets `is_end(false)`) was
  fixed in the same change. What that lint cannot see is the OTHER shape the same four leaves
  produced: a helper that builds a fresh `DeserializeConfig::new(..)` / `SerializeConfig::new(..)`
  instead of threading the caller's, so every literal below it correctly spells an accessor that
  resolves to the default. The emitted text is indistinguishable from correct; only the config's
  provenance is wrong. A mechanical layer would be a construction lint — a config built inside an
  overload-scoped fn must inherit rather than start fresh — with the legitimate fresh-config
  boundaries (the array element, the map key/value, the canonical map key) as its allowlist.
  Deferred because it would have no work to do on the current tree: all four of today's fresh-config
  constructions inside an overload-scoped fn sit at exactly those boundaries and each re-threads the
  overload explicitly (`elem_config.deserializer_name_overload = config.deserializer_name_overload;`
  and its two map twins; the canonical map key's own `buf`). Reopening signal, on the axis the cost
  grows along: the COUNT of fresh-config constructions inside overload-scoped fns, measurable by
  whoever next adds one — today it is four, all at the named boundaries, so a fifth that is NOT one
  of them is the instance that makes the boundary list worth writing. The lint's OTHER gap — a
  helper receiving the name under a parameter spelling the scoping rule does not know, which would
  put every literal below it out of scope — is closed rather than deferred:
  `snapshot_tests::emitter_overload_lint_scopes_every_name_param` (fast tier) scans the same emitter
  sources for name-typed `serializ` parameters and fails any spelled outside the rule's vocabulary,
  which the scoping rule and the guard now share as one const pair (current state in
  `tests/README.md` § \"Snapshot tests\").

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "wrong_public_api"
family_classification = "none_reviewed"
control_ids = ["testing.guard.emitter-drops-overloadable-parameter-building-fresh-config-invisible"]
transition_ids = ["testing.trigger.emitter-drops-overloadable-parameter-building-fresh-config-invisible"]

[[record.source_replacement]]
span_id = "span-record-emitter-drops-overloadable-parameter-building-fresh-config-invisible"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.exclusion-reasons-informational-interchange"
title = "Exclusion-record reasons are informational, not interchange."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """Exclusion-record reasons are informational, not interchange.
"""
detail_md = """- **Exclusion-record reasons are informational, not interchange.** Consumers never parse the text
  after `; unexported: <ident> — `; wording may change freely. If tooling ever wants to act on
  exclusion REASONS, that's a dialect v2 field, not a regex over prose.
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.exclusion-reasons-informational-interchange.
"""
authority_reference_id = "ref-testing-exclusion-reasons-informational-interchange-authority"
permanence = "permanent"

[[record.source_replacement]]
span_id = "span-record-exclusion-reasons-informational-interchange"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.execution-binary-wrappers-true-last-documented-flag-value"
title = "An execution cell for `--binary-wrappers=true` — the last documented flag value whose only coverage is a compile smoke while its emission risk is byte-level."
projection_group = "next-priority"
legacy_aliases = ["Next work 22"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """An execution cell for `--binary-wrappers=true` — the last documented flag value whose only coverage is a compile smoke while its emission risk is byte-level.
"""
detail_md = """22. **An execution cell for `--binary-wrappers=true` — the last documented flag value whose only
    coverage is a compile smoke while its emission risk is byte-level.** The annotate=false
    delivery established the pattern for flag-conditioned emission branches whose failure is
    byte-level rather than compile-level: a bespoke scratch spec + hand-derived round-trip vectors
"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "coverage_cell"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.execution-binary-wrappers-true-last-documented-flag-value's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""

[[record.source_replacement]]
span_id = "span-record-execution-binary-wrappers-true-last-documented-flag-value"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.extend-recombination-member-kind-table-optional-fixed-shape"
title = "Extend the recombination member-kind table to the OPTIONAL-FIXED shape."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Extend the recombination member-kind table to the OPTIONAL-FIXED shape.
"""
detail_md = """- **Extend the recombination member-kind table to the OPTIONAL-FIXED shape.** `MEMBER_KINDS` now
  carries a per-kind aux-rule slot (`%A%`), so a kind that needs a named rule of its own is a one-row
  edit rather than a mechanism change — the `tagged_optional` row (`%K%: #6.10(%A%)` over
  `uint / null`) is the worked example, and `optional_fixed_member.cddl` /
  `optional_fixed_float.cddl` name the shape the table still cannot compose: `? %K%: 5`, an optional
  member whose type is a fixed value, which mints a `bool` presence field instead of a value and so
  reaches a deserialize seam the `optional` and `fixed_*` kinds each only half touch.
  It is one row, and the reason it is not simply built is that a kind addition is not free: it
  RESHUFFLES every seeded draw downstream of the table, so each one re-samples the triples and
  depth-2 fillers onto different compositions and can surface unrelated pre-existing defects that
  must be fixed or promoted before the row can land. Adding `tagged_optional` cost two emitter fixes
  and one promoted panic class; budget the next row the same way rather than as a table edit.
  Reopening signal, on the axis the deferred cost grows along: a SECOND exit-0
  uncompilable-or-wrong-bytes crate in a supported composition the member-kind table does not span
  (each instance is a loud build or round-trip failure for whoever generates the shape, so the
  reporter exists by construction).

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.extend-recombination-member-kind-table-optional-fixed-shape"]
transition_ids = ["testing.trigger.extend-recombination-member-kind-table-optional-fixed-shape"]

[[record.source_replacement]]
span_id = "span-record-extend-recombination-member-kind-table-optional-fixed-shape"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.extern-deps-wasm-boundary-surface-packaging-json-gen"
title = "Extern-deps wasm-boundary surface: packaging- and json-gen-gaps beyond the behavioral floor."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Extern-deps wasm-boundary surface: packaging- and json-gen-gaps beyond the behavioral floor.
"""
detail_md = """- **Extern-deps wasm-boundary surface: packaging- and json-gen-gaps beyond the behavioral floor.**
  The split-dep cell (`integration_tests::extern_deps_wasm`, `--extern-wasm-crate`) drives the
  generated wasm crate's cross-crate wrappers behaviorally: `tests/extern-deps-wasm/tests_wasm.rs`
  constructs `Everything` and the non-root `nested::NestedHolder` through the wasm API (all eight
  collection wrappers — `ExternCrateFooList`/the table wrapper plus the `nested` module's
  non-root-use `ExternCrateBarList`, `NestedItemList`, and the `NestedItem`/`ExternCrateBar`-keyed
  maps whose keys() re-mint the element list), CBOR round-trips, and value-anchors every getter
  across the boundary (element fields via `.as_ref()` to the dep rust types, map lookups, keys()
  readback), so a semantically wrong boundary conversion (`get`/`add` through the dep's `From`
  impls, an identity `.into()` where a transform was needed) now fails rather than compiling green.
  This mirrors the behavioral floor the sibling index fixture already has
  (`extern_wrapper_index_defers_to_dep` over `tests/extern-deps-wasm-index`, which additionally
  links for the real `wasm32-unknown-unknown` target). Two layers above \"it builds and round-trips\"
  stay unprobed: (a) `wasm-pack`/bindgen-CLI packaging — `cargo build`/`cargo test` cannot see
  duplicate exported JS class names when the dep wasm crate and the consumer both export a
  like-named wrapper (the generate-locally policy makes this reachable); no gate runs bindgen-CLI
  over the extern fixture. (b) json-gen against extern-dep types — `add_schemas` SKIPS
  dep-owned rows (a dep type's schema root is owned by the dep's own json-gen run, and this crate's
  json-gen manifest depends only on the own rust crate), pinned in-memory by
  `snapshot_tests::json_gen_extern_schema_rows`. COMPILING emitted json-gen crates at breadth is
  owned by `cddl-matrix/ROADMAP.md`'s extern-compile-breadth entry; the residual owned HERE is
  execution — no gate RUNS `export_schemas()` from a json-gen crate built from a spec carrying
  extern deps (such a crate now compiles precisely because the dep rows are skipped; the unprobed
  layer is the runtime over the surviving in-crate rows in that configuration).
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.extern-deps-wasm-boundary-surface-packaging-json-gen"]
transition_ids = ["testing.trigger.extern-deps-wasm-boundary-surface-packaging-json-gen"]

[[record.source_replacement]]
span_id = "span-record-extern-deps-wasm-boundary-surface-packaging-json-gen"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.extern-interface-export-dialect-v2-candidates"
title = "Extern-interface export dialect v2 candidates."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Extern-interface export dialect v2 candidates.
"""
detail_md = """- **Extern-interface export dialect v2 candidates.** Each bumps the seam header
  (`_CDDL_CODEGEN_EXTERN_INTERFACE_ v1` — unknown versions hard-error, pinned by
  `extern_import_unknown_version_hard_errors`), so batch them when one gets a real consumer:
  (1) per-variant name pins for c-style enums — the type-level `@rust_name` pin covers the enum
  name but variant names stay consumer-derived, so a variant-naming-rule change reintroduces the
  cross-version skew class on exactly that row (reopening signal: a consumer compile break naming
  a dep enum variant after a codegen naming change); (2) synthesized idents for anonymous generic
  instances — `[* pair<uint,tstr>]` in a dep today closure-excludes the referencing rule
  (reopening signal: a real dep's export carries that `; unexported:` record and a consumer needs
  the rule); (3) transparent spellings for the renderer's known lossy-IR case — a two-sided
  `float32` window has no faithful CDDL form (a literal float range re-parses as `float64`), so
  such rules exclude-with-record (reopening signal: same, for a float32-windowed alias);
  (4) generic PARAMETERS on an exported extern base — the renderer spells every rule body as a bare
  marker, so `ext_set<t0> = _CDDL_CODEGEN_EXTERN_TYPE_` projects param-less and a consumer can
  reference the base but never instantiate it. That is why `@raw_bytes_flavor` does not ride the
  seam: it flavors a generic INSTANCE, so on the param-less form it is unhonorable, and re-parsing
  it is exactly the spelling the non-generic-extern rejection refuses (pinned by
  `extern_import_flavored_generic_base_projects_without_the_tag`). Carrying the params would restore
  both halves at once. Reopening signal: a consumer that needs to instantiate a dependency's generic
  extern over `--extern-import` — its own spec cannot spell the instantiation, so the block is
  visible to the party that has it, not inferred by us.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.extern-interface-export-dialect-v2-candidates"]

[[record.source_replacement]]
span_id = "span-record-extern-interface-export-dialect-v2-candidates"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.extern-interface-export-public-interchange-format"
title = "The extern-interface export is a public interchange format."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """The extern-interface export is a public interchange format.
"""
detail_md = """- **The extern-interface export is a public interchange format.** Once a consumer regenerates
  against a dep's committed `extern-interface/<dep>/**`, its dialect (header line, marker rows,
  `@rust_name` pins, `; unexported:` records) is cross-crate API: any change to what the emitter
  writes must version through the seam header, and a dep-tool-newer-than-consumer combination must
  always hard-error rather than misread (the consumer's strict `@`-token whitelist +
  version-header scan is the enforcement point; `extern_import_unknown_version_hard_errors` and
  `extern_import_unknown_annotation_hard_errors` pin it). Watch: a new comment-DSL directive
  widens the consumer whitelist automatically (it reads `comment_ast`'s tag set), so an OLD
  consumer tool meeting a NEW directive in an export still hard-errors — that asymmetry is the
  designed behavior, not a bug report.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-extern-interface-export-public-interchange-format-file-heading-hcd398e660853cc36a8775a2364f8f7dcdd7f9c1a4fee1a20a98b392f5dcdc1ce"]
claim_md = """The extern-interface versioning and strict-reader contract in the referenced durable documentation remains authoritative.
"""
boundary_md = """This control owns the public interchange-format boundary and does not imply delivery of a new dialect.
"""

[[record.source_replacement]]
span_id = "span-record-extern-interface-export-public-interchange-format"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.extreme-value-boundary-correctness-generation-time-encode-paths"
title = "Extreme-value boundary correctness of generation-time encode paths: a value that FITS the type can still encode wrong at the type's boundary, and no fixture minted from \"supported shapes\" ever lands there."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Extreme-value boundary correctness of generation-time encode paths: a value that FITS the type can still encode wrong at the type's boundary, and no fixture minted from \"supported shapes\" ever lands there.
"""
detail_md = """- **Extreme-value boundary correctness of generation-time encode paths: a value that FITS the
  type can still encode wrong at the type's boundary, and no fixture minted from \"supported
  shapes\" ever lands there.** Proven instance (read-caught during the `FixedValue` i128/u64
  widening, not by any test): `FixedValue::to_bytes` — the generation-time canonical-ordering
  encoder for fixed map keys — called cbor_event's `write_negative_integer(*i as i64)`, which
  produced wrong bytes for `i64::MIN` on the then-current cbor_event 2.4.0 (`-i64::MIN` overflowed
  i64; the 3.x endpoint computes in i128), so a
  canonical spec with a fixed `-9223372036854775808` map key would have gotten wrong key
  ordering silently. `i64::MIN` is representable in the old `isize` field, so this was a
  correctness hole at a representable boundary — a different class from the representability
  widening the nint entry in `cddl-matrix/ROADMAP.md` scopes (that entry even named this call
  site, but as a range cap, and its \"generation already uses `_sz` where the `i64` limit bites\"
  premise was exactly what the bug falsified). Standing coverage now:
  `nint_to_bytes_canonical_across_boundaries` pins hard-coded canonical bytes across the full
  magnitude ladder + `i64::MIN` through `to_bytes`, and
  `i64_min_fixed_value_emits_width_correct_nint` pins the emitted serialize line under default +
  preserve profiles. What no gate yet does: thread each numeric extreme (`i64::MIN`/`MAX`,
  `u64::MAX`, the `±2^63±1` off-by-ones) through every POSITION class systematically — fixed
  value, fixed map key under `--canonical`, range/bounds windows, deserialize mismatch arms — in
  a generated crate whose emitted tests execute. A SECOND read-caught extreme-value bug in any
  encode/compare path is the trigger to build that boundary-vector sweep as a corpus/matrix
  axis rather than hand-pinning per instance.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
control_ids = ["testing.guard.extreme-value-boundary-correctness-generation-time-encode-paths"]
transition_ids = ["testing.trigger.extreme-value-boundary-correctness-generation-time-encode-paths"]

[[record.source_replacement]]
span_id = "span-record-extreme-value-boundary-correctness-generation-time-encode-paths"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.failing-snapshot-test-surfaces-mismatch-per-test-run"
title = "A failing snapshot test surfaces ONE mismatch per test run — the first assertion that panics — so a \"N snapshots changed\" count read off a single failure list understates multi-snapshot churn."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """A failing snapshot test surfaces ONE mismatch per test run — the first assertion that panics — so a \"N snapshots changed\" count read off a single failure list understates multi-snapshot churn.
"""
detail_md = """- **A failing snapshot test surfaces ONE mismatch per test run — the first assertion that panics —
  so a \"N snapshots changed\" count read off a single failure list understates multi-snapshot
  churn.** Proven 2026-08-08 (the component fallibility delivery): a `whole_program` fixture
  snapshots several files per profile, the run stored `.snap.new` only for the first mismatching
  assertion of each failing test, and the full churn (4 files across 2 fixtures — glue AND
  `world.wit` per fixture) was visible only by checking the still-committed twins for the stale
  content the fix must move (the committed `world.wit` snaps still carried infallible
  constructors). A reviewer ruling on churn SCOPE from the stored `.snap.new` set alone would have
  ruled on half of it. Working rule: before ruling on snapshot churn, derive the expected-change
  set from the CHANGE (which emitted surfaces the edit touches, per fixture), check each expected
  twin's committed content, and iterate run-and-bless to exhaustion — the suite's final fully-green
  run is the only complete enumeration; one failure list never is.

"""
control_kind = "operator_procedure"
control_state = "live"
reference_ids = ["ref-testing-failing-snapshot-test-surfaces-mismatch-per-test-run-file-heading-hdfda06d801b21d0d5d207c3f6d65dfb210bc07372c22ac4db084fd65eefa1c3c"]
claim_md = """The operator procedure in testing.failing-snapshot-test-surfaces-mismatch-per-test-run's preserved detail remains authoritative.
"""
boundary_md = """This procedure owns its exact operating surface and does not infer adjacent lifecycle work.
"""

[[record.source_replacement]]
span_id = "span-record-failing-snapshot-test-surfaces-mismatch-per-test-run"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.file-listing-json-schema-root-values-flag-per"
title = "A file listing `--json-schema-root` values, instead of one flag per root."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A file listing `--json-schema-root` values, instead of one flag per root.
"""
detail_md = """- **A file listing `--json-schema-root` values, instead of one flag per root.** The repeatable flag
  is what shipped, on the grounds that it matches every other repeatable flag and that the asking
  consumer's eight entries do not justify a new file format. A file-listing variant stays purely
  additive (same emission, a different way to spell the list), so it is a cost question a real
  consumer settles: build it when someone's root list is large enough that a command line stops being
  the right place for it. Note what such a file must NOT become — the paths are Rust, not CDDL, and
  routing them through the spec is the category error the flag exists to avoid.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.file-listing-json-schema-root-values-flag-per"]

[[record.source_replacement]]
span_id = "span-record-file-listing-json-schema-root-values-flag-per"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.finding-s-defect-side-attribution-recorded-narrative-plausibility"
title = "A finding's DEFECT-SIDE attribution recorded by narrative plausibility, not execution — the RECORDING-side twin of the closure-side reproduce-at-parent rule above."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A finding's DEFECT-SIDE attribution recorded by narrative plausibility, not execution — the RECORDING-side twin of the closure-side reproduce-at-parent rule above.
"""
detail_md = """- **A finding's DEFECT-SIDE attribution recorded by narrative plausibility, not execution — the
  RECORDING-side twin of the closure-side reproduce-at-parent rule above.** First instance
  (caught at item pickup, cycle 10 of the burndown effort): the present-null-optional finding
  (recorded `63feb259`, retired `78aab6d3`) attributed the dropped state to the CBOR re-encode
  and the faithful behaviour to the json round-trip; worktree execution AT THE RECORDING COMMIT
  showed the reverse on both surfaces (CBOR re-encoded `820082190338f6` byte-exact; the json
  detour came back `820081190338`). The inversion was structural, not a typo's risk profile: the
  surfacing gate (`corpus_decode_replay`'s json leg) proves the two decode surfaces DISAGREE — a
  SYMMETRIC fact — and the prose then named a culprit the gate never identified. No existing
  layer could have caught it: the `JSON_SURFACE_SKIP` guard asserts only that the cell STILL
  FAILS, which stayed true (the disagreement was real, whichever side owned it), so an inverted
  attribution stays green indefinitely; and the findings-claims arm (`project_corpus.ts` check
  `I`) checks citation RESOLUTION, not claim semantics — the same boundary the entry above
  records it stopping at. Cost while it stood: two burndown triage passes carried the row with
  its fix shape pointed at the WRONG surface (\"fix serialize for nullable-inner optionals\"), and
  only the pickup re-probe rule (AGENTS.md) prevented a delivery built against it. Working rule,
  now, in place of a mechanical layer: **a finding that attributes a defect to ONE of several
  surfaces or directions quotes each surface's executed probe output — bytes in, bytes out, per
  surface, at the recording sha — in the entry; a disagreement gate licenses recording the
  disagreement, and only an executed per-surface probe licenses naming the culprit.** (Symmetric
  evidence for a symmetric fact; the closure-side rule above already demands the same discipline
  when a finding is retired.) The mechanical layer, if the class recurs: skip-ledger rows carry a
  machine-checkable failure SIGNATURE (which leg diverges, and the divergent bytes) that the
  replay guard executes every run, so a recorded attribution is re-asserted continuously instead
  of trusted from its recording day. Reopening signal: a SECOND finding whose recorded
  surface/direction attribution is falsified at pickup — measured by the pickup party, who is
  already re-probing premises under AGENTS.md's rule, so the observation arrives as a by-product
  of work being done anyway.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.finding-s-defect-side-attribution-recorded-narrative-plausibility"]
transition_ids = ["testing.trigger.finding-s-defect-side-attribution-recorded-narrative-plausibility"]

[[record.source_replacement]]
span_id = "span-record-finding-s-defect-side-attribution-recorded-narrative-plausibility"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.fixture-models-obsolete-user-contract-stays-green-real"
title = "A fixture that models an OBSOLETE user contract stays green while the real contract path is broken — the wrong-shape member of the fixture-blindness family headed by \"An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate\"."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A fixture that models an OBSOLETE user contract stays green while the real contract path is broken — the wrong-shape member of the fixture-blindness family headed by \"An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate\".
"""
detail_md = """- **A fixture that models an OBSOLETE user contract stays green while the real contract path is
  broken — the wrong-shape member of the fixture-blindness family headed by \"An emission branch
  whose generated OUTPUT no fixture exercises is invisible to every gate\".** Proven instance
  (consumer-reported, not by any gate): every raw-bytes fixture appended
  the user-supplied `_CDDL_CODEGEN_RAW_BYTES_TYPE_` definition INTO `rust/src/generated/mod.rs` —
  the pre-thin-root layout no real user can have (that subtree is clobbered every regen) — so bare
  references resolved locally and all raw-bytes gates stayed green while the extern re-export glue
  (`pub use crate::Name;`) covered only `RustStructType::Extern`, not `RawBytesType`; regenerating
  CML's cip36 (`public_key = _CDDL_CODEGEN_RAW_BYTES_TYPE_` referenced only through `pub type`
  aliases, which have no other resolution path) failed E0412 at five sites. Standing coverage now:
  the fix routes raw-bytes defs through the real contract (appended to the user-owned thin
  `lib.rs`, same as extern-type defs), and the glue's own `pub use crate::<Name>;` line makes any
  regression to the masked routing fail loudly (E0255 duplicate definition) rather than silently
  re-masking. The WASM half of the incident was the class's second occurrence, caught by
  interrogating this entry's own first write-up rather than by any gate: the wasm glue covered
  only `Extern` wrappers while the fixture appended the wasm raw-bytes def into
  `wasm/src/generated/mod.rs` (the same no-real-user-contract residence), proven by a
  cip36-shaped scratch repro — rust crate compiled via the landed rust glue, wasm crate failed 3×
  E0425 `PubKey` (the consumer's rust E0412 had masked the wasm failure, since the regen never got
  past the rust crate). Both halves
  are now standing coverage: glue covers `Extern | RawBytesType` in BOTH crates, both def
  routings model the real crate-root contract (thin `lib.rs`), and regression to the masked
  residence collides E0255 with the glue's own re-export in either crate. The formerly-latent alias-walk sibling
  (`scope_references` walked `rust_structs` only, never `type_aliases`, so a CROSS-scope alias to
  a non-extern type under-imported its target — E0412) got its failing case on a production
  project and is fixed: the type-alias walk marks each emitted alias's `base_type` (and, wasm-side,
  the `resolved_wasm_alias_target` wrapper — a helper shared with the wasm alias emitter so the
  emitted target and its import cannot drift), with standing coverage from the placement matrix's
  `aliased` reference mode (every INCLUDED shape × `bal = <ty>` in module `b`; the extern-shape
  exclusion's alias-position residue — the generic-extern-instance `Base<Args>` class, feature
  request 07 — is hand-pinned by `tests/extern-generic-scoped`, see \"Multifile reference-POSITION
  coverage\" below). Working rule meanwhile: a
  fixture that injects hand-written code into
  `generated/**` carries a comment justifying the residence — and the justification must name a
  contract a real user can follow, or explicitly flag itself as a modeled-obsolete shape citing
  this entry (the `use super::*;` serializer-helper appends qualify). Mechanical layer on the
  NEXT instance: an audit sweep enumerating every test append-site that writes into a
  `generated/**` path and failing any not on a justified allowlist — the wasm half already being
  the class's second occurrence is the argument for building it on any further sighting rather
  than deferring again.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.fixture-models-obsolete-user-contract-stays-green-real"]
transition_ids = ["testing.trigger.fixture-models-obsolete-user-contract-stays-green-real"]

[[record.source_replacement]]
span_id = "span-record-fixture-models-obsolete-user-contract-stays-green-real"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.fixture-s-verdict-accident-cargo-feature-unification-harness"
title = "A fixture cell's verdict can be an accident of cargo feature unification: harness-spliced hand code needing a feature a dep reshape disabled stays green whenever a SIBLING dep in the same fixture graph re-enables that feature — so the red arrives on a later graph change, not on the change that broke the code."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A fixture cell's verdict can be an accident of cargo feature unification: harness-spliced hand code needing a feature a dep reshape disabled stays green whenever a SIBLING dep in the same fixture graph re-enables that feature — so the red arrives on a later graph change, not on the change that broke the code.
"""
detail_md = """- **A fixture cell's verdict can be an accident of cargo feature unification: harness-spliced
  hand code needing a feature a dep reshape disabled stays green whenever a SIBLING dep in the
  same fixture graph re-enables that feature — so the red arrives on a later graph change, not on
  the change that broke the code.** Proven instance (D2, the alloc-mode dep reshape): two
  committed hand fragments (`tests/custom_serialization`, `tests/custom_serialization_preserve`,
  spliced into fixture crates by the integration harness) boxed `hex::FromHexError` as
  `Box<dyn Error>` — an impl hex gates on ITS `std` feature, which the reshape's
  `default-features = false` dropped. The non-preserve fragment failed three `local`-tier cells
  immediately (`core_no_wasm`/`core_with_wasm`/`emit_wasm_tests_execute` — the net that works,
  and the third instance of the owning-family lesson: the owning family of a DEP RESHAPE includes
  every fixture whose hand code uses that dep, a set not enumerable from the ops table, so the
  reshape delivery now sweeps the committed hand-file set as part of its battery). The preserve
  twin stayed green PRE-fix for a reason that had nothing to do with the code: its fixture graph
  carries the `cddl` conformance-oracle dep, whose own transitive `hex` uses default features, and
  cargo unifies features per graph — so `hex/std` was back on and the missing impl restored
  (`cargo tree` evidence in the fix-forward log). Both fragments were fixed the consumer way (a
  local newtype with verbatim-delegating `Debug`/`Display` + `core::error::Error` — the in-repo
  migration exemplar cited by the no_std workstream's D5 notes) rather than by re-enabling the
  dep's defaults, precisely so the fixtures model the post-reshape world; but the masked-pass
  CLASS remains: any spliced hand fragment whose compile needs a reshaped-away feature is
  invisible while some oracle-carrying sibling re-unifies it. Coverage boundary, stated so the
  deferral is scoped: the no_std drift gate (workstream D3) structurally covers the TOOL-PRODUCT
  half — fresh standalone crates checked through `default-features = false` shims with no oracle
  in the graph — and never the fixture-hand-code half, because its profiles deliberately exclude
  the harness appendages. Machinery on a trigger, not now (the class has exactly the two members,
  both fixed): a `cargo check --lib`-only cell per spliced-hand-fragment fixture family —
  lib-only checking keeps dev/oracle deps out of the unification graph, so the fragment must
  compile on the fixture's own declared features. *Reopening signal:* a third instance — a
  fixture cell flipped red by a dep-graph or oracle-version change with no edit to the fixture
  itself, or a reshape delivery's hand-file sweep finding a fragment that compiles only under
  unification. Measurable by exactly the party who hits it (the tier runner reading the E0277
  against an unchanged fixture), on the dimension the cost grows (the count of spliced hand
  fragments using reshaped-dep surfaces).
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.fixture-s-verdict-accident-cargo-feature-unification-harness"]
transition_ids = ["testing.trigger.fixture-s-verdict-accident-cargo-feature-unification-harness"]

[[record.source_replacement]]
span_id = "span-record-fixture-s-verdict-accident-cargo-feature-unification-harness"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.fixture-shape-evicted-over-known-defect-stale-guard"
title = "A fixture SHAPE evicted over a known defect has no stale-guard, so the fix never re-adds it."
projection_group = "next-priority"
legacy_aliases = ["Next work 13"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A fixture SHAPE evicted over a known defect has no stale-guard, so the fix never re-adds it.
"""
detail_md = """13. **A fixture SHAPE evicted over a known defect has no stale-guard, so the fix never re-adds
    it.** Skip-listed gate rows are ledgered with citations and stale-guards; an eviction — a
    shape REMOVED from a fixture because it trips a known bug — is recorded only in prose, which
    nothing re-probes. Proven cost: `tests/corpus/tag_set_generic.cddl` dropped its bytes
    instance when the bytes-element list doors failed the wasm compile (E0271), and the corpus
    wasm-face compile floor — the exact gate that owns the class — went blind to it for the
    defect's whole life; the shape came back only because the burndown re-probed the row's
    premises by hand. What to build, when the class recurs: a small eviction ledger (fixture,
    evicted spelling, defect citation) with a guard that generates each evicted spelling and
    fails loudly the day it heals, the same stale-guard contract skip rows already carry.
    Reopening signal: a second eviction — a fixture edit that removes a spelling citing a defect
    rather than restructuring the fixture — measurable by its author at the moment of eviction,
    which is when the ledger row would be written anyway.

"""
work_state = "deferred"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.fixture-shape-evicted-over-known-defect-stale-guard"]

[[record.source_replacement]]
span_id = "span-record-fixture-shape-evicted-over-known-defect-stale-guard"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.flag-cddl-codegen-invocation-wasm-run-wasm-flag"
title = "A no-flag `cddl-codegen` invocation is a WASM run — the `wasm` flag's PARSE default is true — so a \"default profile\" probe that does not spell `--wasm=false` attributes wasm-profile facts to the default profile."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """A no-flag `cddl-codegen` invocation is a WASM run — the `wasm` flag's PARSE default is true — so a \"default profile\" probe that does not spell `--wasm=false` attributes wasm-profile facts to the default profile.
"""
detail_md = """- **A no-flag `cddl-codegen` invocation is a WASM run — the `wasm` flag's PARSE default is true —
  so a \"default profile\" probe that does not spell `--wasm=false` attributes wasm-profile facts to
  the default profile.** Proven 2026-08-07 (the map rest-row refusal delivery): an implementation
  agent probed a panic's site on \"default / preserve / wasm / json\" without `--wasm=false`,
  observed the wasm-only `finalize` wrapper-collision walk's registry assert on every run (that
  walk precedes generation, so with wasm enabled nothing else is reachable), and reported the
  cycle spec's `--wasm=false` panic-site record — measured correctly with the flag spelled — as
  falsified. The overclaim shipped in a commit message before orchestrator review caught it
  against the spec's recorded probe outputs; the in-code comment was corrected in a follow-up
  (`2a5208a6`). Working rule: a profile-coverage claim must list `--wasm=false` as its own probed
  profile, never inferred from a no-flag run — and a reviewer seeing \"premise falsified\" should
  first diff the two probes' FLAG VECTORS, because two contradictory observations are usually two
  scopes, not one error. (`Cli`'s own doc comment documents the parse-default quirk; this watch is
  the probe-discipline consequence.)

"""
control_kind = "operator_procedure"
control_state = "live"
reference_ids = ["ref-testing-flag-cddl-codegen-invocation-wasm-run-wasm-flag-file-heading-h759f2d7eb8a0852ae365a6dccd08e73b4522e560bc3c7e8c7801b2377f6a959c"]
claim_md = """The operator procedure in testing.flag-cddl-codegen-invocation-wasm-run-wasm-flag's preserved detail remains authoritative.
"""
boundary_md = """This procedure owns its exact operating surface and does not infer adjacent lifecycle work.
"""

[[record.source_replacement]]
span_id = "span-record-flag-cddl-codegen-invocation-wasm-run-wasm-flag"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.full-n-flag-powerset-pict-pairwise"
title = "Full `2^N` flag powerset / PICT pairwise"
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """Full `2^N` flag powerset / PICT pairwise
"""
detail_md = """- **Full `2^N` flag powerset / PICT pairwise** — the curated named profiles cover the flag
  *combinations* worth testing, so the full powerset stays out of scope. Escaped interactions earn
  their own standing cells rather than the whole powerset — four so far (the Fourth is recorded
  after the Third below, with a now-armed trigger). First:
  `--common-import-override` × `--preserve-encodings=false` targeting a preserve-flavored common
  crate emitted `CBORReadLen::new(Len)` against a `new(LenSz)` runtime (E0308). Second:
  `--workspace-dep` × `--wasm=false` silently ignored the flag — validation included — fixed to
  mode-independent honoring and pinned by `workspace_dep_unknown_is_rejected_under_wasm_false`
  plus the flavored contract's rust-only byte-identity leg. Its read-caught sibling, the
  `--extern-wrapper-index` validation skip under `--wasm=false`, is likewise fixed to
  mode-independent validation (deferral still wasm-gated) and pinned by
  `extern_wrapper_index_is_validated_under_wasm_false`. Third, an EMISSION-path flavor (not
  validation-inertness): `--preserve-encodings=true` × `--annotate-fields=false` — no profile
  combines them, and `flag_value_smoke`'s annotate=false case runs a spec with no encoding-less
  fixed member, so the combination's fixed-value deserialize paths were unreachable by every gate.
  Two bugs lived there: bool/null fixed members emitted a bare `()` with no statement terminator
  (non-parsing output), and uint/nint/text fixed MAP values bound their inline `{field}_encoding`
  over the arm's outer accumulator (E0308 assign-to-shadow) — the first found by reading during
  the optional-fixed-value delivery, the second at the arming run of the standing cell that now
  owns the combination (`preserve_no_annotate_fixed_members_generate`, generating both fixed-member
  corpus fixtures under the pair). Both fixed. Fourth, the same input-poverty mechanism as the
  Third on a different emit path: `--annotate-fields=false` (non-preserve) × a value-bounded
  `nint` MEMBER — the N64 bounds `.and_then` relied solely on the site's `error_convert`, which
  is empty under annotate=false, so the closure inferred the reader's native `cbor_event::Error`
  and the emitted crate failed E0277. `flag_value_smoke` swept the flag the whole time but its
  input (`tests/canonical/input.cddl`) had no bounded nint, so the path was unreachable by every
  gate until the `generated_code_clippy_clean` provocation shape (`clippy_neg_bounded`) entered
  that shared input and turned the smoke red. Fixed (convert-at-most-once, pinned by
  `deserialize_converts_error_at_most_once`; the shape stays in the smoke's input permanently).
  Cross-check at discovery: no cddl-matrix item covers this axis — the matrix enumerates INPUT
  surface under the named profiles, and the corpus already held the provoking shape
  (`bounds_spellings.cddl`'s `m_nint_range`, verified red-reproducing under annotate=false at the
  pre-fix rev) — the unswept dimension was the FLAG value. That second input-poverty instance armed
  the sub-class trigger, and the named layer is BUILT: the
  `feature_corpus_compiles_no_annotate_shard_NN` leg sweeps the whole feature corpus under two
  gate-local flavor rows (plain `--annotate-fields=false` and
  `--preserve-encodings=true --annotate-fields=false`), rust-crate-only because the emitted `wasm/`
  tree is byte-identical with the flag on or off on every corpus fixture under both rows. The corpus
  is the shape-rich input the canonical smoke is not, and `NO_ANNOTATE_FLOOR_STEMS` pins the three
  provoking stems (`bounds_spellings`, `fixed_bool_member`, `optional_fixed_member`) as present AND
  swept so pruning cannot re-open the hole. `tests/README.md` § the corpus compile gate carries the
  full description; the three emission classes it found on its first run are fixed, and the
  semantic sibling that a compile floor structurally cannot be
  (`no_annotate_reframed_emissions_round_trip_non_canonical_wire_byte_exactly`) rides beside it, so
  the combination is swept for BUILDABILITY and for byte-exact fidelity. The remaining
  recur-first lesson from the first three: a THIRD validating
  flag turning up mode-inert is the trigger
  to build the class-level validation-smoke sweep — each clap flag with documented startup
  validation invoked once with a deliberately invalid value under each `--wasm` mode, asserting
  nonzero exit — rather than accreting per-flag standing cells. The extern-deps surface is now
  probed under both preserve flavors — `integration_tests::extern_deps` (preserve) and
  `integration_tests::extern_deps_non_preserve` (non-preserve, compiled against the preserve-flavored
  `extern-dep-crate` stand-in) — plus the wasm-boundary cell `integration_tests::extern_deps_wasm`
  (`--extern-wasm-crate` against the split `extern-dep-crate`/`extern-dep-crate-wasm` pair; extern
  types as list elements and table keys/values, from root AND non-root use sites — the non-root
  `nested` cells pin the wrapper-element imports registered from the wrapper's emission scope, which
  root use sites would mask) — so those specific cells are pinned without enumerating the rest.
  Adjacent but on an orthogonal axis to the First interaction's runtime-flavor mismatch (that one is
  a `CBORReadLen` construction seam; this one is a derive seam): the cross-crate `Int` **key**-flavor
  channel under `--common-import-override` is pinned by `int_key_via_common_import_override_sidecar`
  (consumer — the `borrowed_key_types.rs` `(<override>, \"int\")` row and its self-check compiling
  against the `extern-dep-crate` fixture's key-capable `Int`) and the `int` leg of
  `workspace_key_requests_derive_effect_and_hard_errors` (dep — an `int` `--key-requests` row emits a
  key-flavored `Int` a spec never references, while a `uint` row still hard-errors), with the
  `extern_deps`/`extern_deps_wasm` cells and the pure-consumer cell `common_override_wasm_int`
  (override crate NOT also a declared extern dep) covering the plain `Int`/`IntError` re-export path.
  Every individual flag *value* now appears in some profile or test: the five that previously
  didn't are covered by `flag_value_smoke` (`--annotate-fields=false`,
  `--to-from-bytes-methods=false`, `--binary-wrappers=true`), `wasm_cbor_json_api_macro_compiles`
  (`--wasm-cbor-json-api-macro`), and — for `--canonical-form=true` without `--preserve-encodings`,
  which emitted a non-compiling crate — a CLI rejection (`api::with_types`, pinned by
  `flag_value_rejects_canonical_without_preserve`).
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.full-n-flag-powerset-pict-pairwise.
"""
authority_reference_id = "ref-testing-full-n-flag-powerset-pict-pairwise-authority"
permanence = "reopenable"
transition_ids = ["testing.trigger.full-n-flag-powerset-pict-pairwise"]

[[record.source_replacement]]
span_id = "span-record-full-n-flag-powerset-pict-pairwise"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.full-suite-flake-attributed-hardened-acquire-scratch-lock"
title = "Full-suite flake, attributed and hardened: `acquire_scratch_lock_serializes` — watch only for a recurrence that outlives the retry deadline."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_operational_watch"
summary_md = """Full-suite flake, attributed and hardened: `acquire_scratch_lock_serializes` — watch only for a recurrence that outlives the retry deadline.
"""
detail_md = """- **Full-suite flake, attributed and hardened: `acquire_scratch_lock_serializes` — watch only for
  a recurrence that outlives the retry deadline.** Five sightings (2026-07-06 through 2026-07-17),
  every one in the `test` gate under parallel nested-cargo load, none reproducible isolated (60
  isolated runs at the second sighting). The FIFTH sighting was the first fully-attributed capture
  (the pipe-to-file discipline finally paying for itself — the third and fourth sightings were
  burned through `tail`/`grep` and carried zero evidence), and it landed on the `match` split's
  **`WouldBlock` arm**, not a syscall errno: the release-assert saw the lock still held
  immediately after dropping the holder. That attributes the class: `flock` locks are per
  open-file-description, and a concurrent `Command` spawn on another test thread forks a child
  that inherits a DUPLICATE of the holder's descriptor until its exec closes the CLOEXEC fds —
  during that fork-to-exec window the duplicate keeps the lock alive, so an INSTANTANEOUS
  post-drop `try_lock` can transiently observe `WouldBlock` exactly and only under the suite's
  constant-subprocess-spawn load profile. The gates' real acquisition path is the BLOCKING
  `lock()`, which waits out that window by construction, so production serialization was never at
  risk — only the test's instantaneous assert raced it. Hardening shipped with the attribution:
  the release assert retries `WouldBlock` on a bounded 5 s deadline (a transient fork-to-exec
  hold clears as soon as the concurrent child execs; a hold that outlives the deadline still
  panics as a genuine release-on-drop semantics break), and the syscall-`Error` arm still reports
  the raw errno for the never-yet-seen ENOLCK-class transient. Standing discipline unchanged and
  proven load-bearing: pipe every `check.ts` run to a FILE from the FIRST invocation — a
  deadline-outliving recurrence is a real kernel/std finding and needs its full log.
"""
watch_state = "attributed"
signature_md = """`acquire_scratch_lock_serializes` still sees `WouldBlock` after the bounded five-second retry deadline, or the raw syscall-error arm fires.
"""
attribution_md = """The five sightings were the fork-to-exec duplicate-descriptor window; production blocking acquisition was safe.
"""
operating_rule_reference_id = "ref-testing-full-suite-flake-attributed-hardened-acquire-scratch-lock-operating"
response_md = """Retain the retry, diagnose descriptor inheritance and lock release, and rerun the complete tier.
"""
escalation_transition_id = "testing.trigger.full-suite-flake-attributed-hardened-acquire-scratch-lock"
retirement_semantics_md = """Retire only when a constructional test proves release independent of fork-to-exec inheritance.
"""

[[record.payload.capture_step]]
step_id = "signature"
capture_md = """Capture full test output, exact arm, elapsed retries, concurrent child-spawn load, errno if any, and isolated reruns.
"""

[[record.payload.capture_step]]
step_id = "scope"
capture_md = """Record the full untruncated log or consumer report, command, full commit, date, environment, relevant versions, and the explicitly unprobed remainder; never cite a disposable draft log as durable evidence.
"""

[[record.source_replacement]]
span_id = "span-record-full-suite-flake-attributed-hardened-acquire-scratch-lock"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.gate-cache-closure-audit-structurally-blind-gate-authored"
title = "The gate-cache closure audit is structurally blind to GATE-AUTHORED inputs parked in scratch: its allowed-read table admits everything under `$TMPDIR` on the justification \"the generated tree is hashed\", which is true only while every file a cached cell reads under scratch actually lives inside a hashed root."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """The gate-cache closure audit is structurally blind to GATE-AUTHORED inputs parked in scratch: its allowed-read table admits everything under `$TMPDIR` on the justification \"the generated tree is hashed\", which is true only while every file a cached cell reads under scratch actually lives inside a hashed root.
"""
detail_md = """- **The gate-cache closure audit is structurally blind to GATE-AUTHORED inputs parked in scratch:
  its allowed-read table admits everything under `$TMPDIR` on the justification \"the generated tree
  is hashed\", which is true only while every file a cached cell reads under scratch actually lives
  inside a hashed root.** A gate that writes its own input files (hand consumer crates, appended
  modules) BESIDE the hashed tree violates the justification without violating the audit: the reads
  classify as `tmp/scratch, allowed`, the key never covers the bytes, and an edit to that input
  serves the stale PASS forever. Near-miss instance (D3, caught by the implementing agent before
  landing — no shipped gate ever had the bug): `no_std_check.ts`'s first draft wrote the host
  std-arm consumer crate outside the profile's hashed output root; the audit, had it traced that
  gate, would have passed it. What shipped instead is the discipline applied (both gate-written
  crates inside the hashed root, relative path deps so hashed bytes stay run-independent) plus the
  rule stated in `tests/README.md` § \"The gate cache\" — review-owned, like the design rules.
  Machinery on a trigger, not now (one cache-participating gate writes gate-authored inputs today,
  and it complies): extend the audit's model from \"scratch is allowed wholesale\" to \"a scratch READ
  must fall under some root the traced gate hashed\" — a per-gate hashed-root manifest the audit
  consumes, turning the discipline into a traced assertion. *Reopening signal:* a second
  cache-participating gate that writes gate-authored input files into its scratch root — measurable
  by that gate's author/reviewer at landing (the red-first mutation check the cache section already
  requires makes the question \"is this file inside the hashed root?\" unavoidable to ask), on the
  dimension the cost grows (the count of such gates, each a fresh chance to repeat the draft bug
  with no audit backstop).
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.gate-cache-closure-audit-structurally-blind-gate-authored"]
transition_ids = ["testing.trigger.gate-cache-closure-audit-structurally-blind-gate-authored"]

[[record.source_replacement]]
span_id = "span-record-gate-cache-closure-audit-structurally-blind-gate-authored"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.gate-cache-costs"
title = "Gate-cache residual costs."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """Gate-cache residual costs.
"""
detail_md = """- **Gate-cache residual costs.** The nested-cargo gates (`feature_corpus_compiles`,
  `wasm_matrix_compiles`, `multifile_matrix_compiles`, the layer-2 recombination sweeps, and
  `verify.ts`) memoize per generated-tree content hash (the gate cache), so re-run wall-clock is a
  first-run price; what remains always-run:
"""
control_kind = "operator_procedure"
control_state = "live"
reference_ids = ["ref-testing-gate-cache-costs-file-heading-hd2498b1dbcaca730bce673b3f6bff5a3a73caa75e63e4868b1df30dd358f795c"]
claim_md = """The operator procedure in testing.gate-cache-costs's preserved detail remains authoritative.
"""
boundary_md = """This procedure owns its exact operating surface and does not infer adjacent lifecycle work.
"""

[[record.source_replacement]]
span_id = "span-record-gate-cache-costs"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.gate-cache.all-hit-cost"
title = "Residual all-hit cost"
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Residual all-hit cost
"""
detail_md = """  - **Residual all-hit cost** — an all-hit `feature_corpus_compiles` still measures ~2.5 min of
    always-run work (177× generation + 177× `cargo generate-lockfile`); dedupe lockfile resolution
    per identical generated `Cargo.toml` within a run if that bites.
"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "infrastructure"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.gate-cache-all-hit-cost"]

[[record.source_replacement]]
span_id = "span-part-part-all-hit-cost"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.gate-cache.closure-audit-traced-set-extension"
title = "Closure-audit traced-set extension"
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Closure-audit traced-set extension
"""
detail_md = """  - **Closure-audit traced-set extension** — the input-closure audit gate
    (`gate_cache_closure_audit`; `tests/README.md` § \"The gate cache\" / \"Soundness gates\") traces
    ONE representative cached gate per run (`multifile_matrix_compiles` by default, the
    highest-risk path-dep read pattern), parameterized by `CLOSURE_AUDIT_GATE`. Widen coverage as
    configuration when a new read pattern warrants it — the cargo-test + json-gen shapes
    (`feature_corpus_compiles`), the roundtrip and recombination sites, and the TS-side `verify.ts`
    cached sites (whose nested cargo runs with cwd = the repo, guarded meanwhile by the audit's
    static `.cargo/config` assert) — each run adding a full trace's wall-clock, so extend
    deliberately rather than tracing every site every run.
"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "infrastructure"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.gate-cache-closure-audit-traced-set-extension"]

[[record.source_replacement]]
span_id = "span-part-part-closure-audit-traced-set-extension"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.gate-cache.coverage-extensions"
title = "Coverage extensions"
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Coverage extensions
"""
detail_md = """  - **Coverage extensions** — the `run_test` fixture suites (uncached: reused export dirs already
    replay warm-incrementally through cargo, and their external path-dep closure is larger) and
    classified deterministic FAILs (expected-red skip-listed cells and unsupported `verify.ts`
    probes re-run every time; caching a *classified* compile-fail is the same soundness argument,
    but transient-env failures must stay uncacheable — needs a careful failure taxonomy first).
"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "infrastructure"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.gate-cache-coverage-extensions"]

[[record.source_replacement]]
span_id = "span-part-part-coverage-extensions"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.gate-demands-premise-probed-against-gate-evidence-about"
title = "A \"no gate demands this\" premise probed against ONE gate is not evidence about a gate in another TIER — and a full-tier gate is where such a premise survives longest, because CI runs `fast` only."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A \"no gate demands this\" premise probed against ONE gate is not evidence about a gate in another TIER — and a full-tier gate is where such a premise survives longest, because CI runs `fast` only.
"""
detail_md = """- **A \"no gate demands this\" premise probed against ONE gate is not evidence about a gate in
  another TIER — and a full-tier gate is where such a premise survives longest, because CI runs
  `fast` only.** Proven instance: the `@no_json_schema_export` delivery deferred its cddl-matrix
  feature row on a premise probed against `project_corpus` (fast tier), where it genuinely holds —
  check D demands a cover only for an id the corpus actually exercises, and an unregistered id has
  no universe entry at all, so it cannot be demanded. The same premise is FALSE for
  `cddl-matrix/verify.ts`'s forward completeness lint, which treats any directive documented in
  `docs/docs/comment_dsl.mdx` as surveyed surface and hard-fails (`missing_cddl_codegen_feature`)
  while it carries no `features/cddl_codegen.toml` row. `verify` is full-tier; no tier run that
  cycle reached it, so the directive shipped over a red gate from the commit that introduced it
  (`9ae0e552` — tag and docs both present, row absent) through four successors, surfaced by
  orchestrator code-read rather than by any run. The standing system that owns this already exists
  and was simply not run (AGENTS.md § \"Build & verify\": `check.ts full` before shipping a feature);
  what is missing is the AUTHORING half. Working rule meanwhile: **a \"no gate covers X\" premise
  names the gates it was probed against AND their tiers**, so the unprobed remainder is visible in
  the claim itself instead of implied — the same scope-the-claim-to-its-evidence discipline the
  point-in-time-inventory-claim entry in this section prescribes. Mechanical layer, on a SECOND
  instance: a `check.ts` listing mode that prints the gates a given tier does NOT run, so a
  deferral rationale can enumerate what it is not evidence about instead of asserting a universal.
  **That trigger has FIRED — second instance, cycle 3 Lane B**, in a new tier pair and a new
  direction: not a deferral rationale but a DELEGATION. A lane restricted to `fast` (heavy tiers
  being serialized centrally) added `tests/emit-tests-bounded-key/`, whose registry is enforced by
  `wasm_api_parity_axes_and_pins_are_live` — a plain `#[test]`, hence `local`. `fast`'s only cargo
  TEST invocation is `cargo test --bin cddl-codegen snapshot_tests`, a SUBSTRING filter, so every
  `#[test]` outside that module is invisible to it. (`fast` does run `cargo fmt`/`cargo clippy`;
  since `clippy --all-targets` type-checks test code, the precise asymmetry is that `fast` catches a
  new `#[test]` that fails to COMPILE and never one that FAILS.) The implementer had no signal and
  shipped the
  fixture without its registry row, costing a fail-fast `local` run that skipped twelve later gates.
  That one fact is now current state where an author will meet it — `tests/README.md` § \"Hand-vector
  suites\" ends with the fixture-dir obligation, the enforcing test, and its tier — which retires the
  instance without retiring the class.
  Two corrections to this entry's own sketch, measured while the trigger fired: (a) the tier pair
  that bites is `fast`/`local`, not only `fast`/`full`, and the `fast`/`local` gap is the one an
  agent-delegation protocol runs into daily; (b) the sketched layer is PARTLY BUILT already — every
  tier run's summary already prints its `not-in-tier` rows — so what is actually missing is
  narrower than \"a listing mode\": a way to answer \"which tier enforces the registry governing THIS
  PATH\" without running anything, which is the question a delegation needs answered before it
  starts. The AUTHORING half is now in force (AGENTS.md § delegation: name the enforcing tier for
  any registry-governed tree a delegation writes into); the mechanical half is a maintainer call,
  deliberately not taken mid-cycle because it edits `check.ts` itself.
  **DEAD END, recorded with its proof, because it is the move anyone reaches for first: do NOT add
  a hand-authored path-glob per registry gate.** The data needed splits in two, and only one half
  exists. `gate -> tier` is already machine-readable in `check.ts`'s registry. `gate -> governed
  paths` exists NOWHERE — `wasm_api_parity_axes_and_pins_are_live` governs `tests/*/input.cddl`, but
  nothing declares that; it is implicit in a `read_dir` inside the test body. Declaring it by hand
  manufactures a SECOND registry describing the first, which can drift from the gate it describes
  while every path in it still resolves — a fresh instance of the precise class this section
  already records four times over (the `KNOWN_PANIC_CLASSES` citation entry and the
  point-in-time-inventory entry). A layer built that way would be a new instance of the failure it
  is meant to prevent, and the drift would be invisible to any resolution check. Deriving the map
  instead of declaring it does not rescue the approach either: the gates that govern a tree
  enumerate it AT RUNTIME, so deriving means running them, which is what the layer exists to avoid.
  UNBUILT SKETCH, not a validated design — offered only so the next attempt starts past the dead
  end: invert the question. The risk materialises only when a NEW FILE appears under a governed
  tree, which is a git-visible event, so a new-file trigger (\"you added `tests/<newdir>/input.cddl`;
  the registries enumerating that tree are X, Y, tier Z\") needs only the LIST of enumerating gates —
  no per-gate glob, hence no second registry to drift — and fires exactly when the delegation risk
  is real rather than requiring someone to think to ask. Nothing about this was prototyped; its own
  premise (that the set of tree-enumerating gates is small and stable enough to list) is unprobed.
"""
work_state = "ready"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.gate-demands-premise-probed-against-gate-evidence-about's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The preserved Standing detail records that its second-instance trigger fired; the due system is now pickup work.
"""
admission_ids = ["testing.admission-path.gate-demands-premise-probed-against-gate-evidence-about"]

[[record.source_replacement]]
span_id = "span-record-gate-demands-premise-probed-against-gate-evidence-about"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.gate-exists-registry-nowhere-tests-readme-md-hand"
title = "A gate exists in the registry but nowhere in `tests/README.md`, and a hand-written count of gates disagrees with the registry — no mechanism ties the two."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A gate exists in the registry but nowhere in `tests/README.md`, and a hand-written count of gates disagrees with the registry — no mechanism ties the two.
"""
detail_md = """- **A gate exists in the registry but nowhere in `tests/README.md`, and a hand-written count of
  gates disagrees with the registry — no mechanism ties the two.** Two instances, so the trigger has
  fired on the count half and is one short on the catalogue half. *Instance 1 (count):* the
  concurrency section read \"the thirteen `#[ignore]`d manual-only heavy gates\" while the runner
  printed `parallel batch: 15 gate(s) in group 'manual_heavy'` — and the SAME file two paragraphs
  earlier carries the correct 15 inside a `<!-- gen:sh:tests-ignored-gates -->` generated block, so
  the doc disagreed with itself across a generated/hand boundary. Fixed by deleting the derived count
  rather than correcting it, the remedy this repo already chose for the identical class in prose
  refusal counts. *Instance 2 (catalogue):* `pin_cold_fetch` shipped as a `full`-tier gate and was
  described in `check.ts` and `static/manifest_changes/README.md` but never in `tests/README.md`,
  where its siblings are catalogued; added by hand on discovery. **Owner meanwhile:** review, plus
  the existing `project_status_headers.ts` generated roll-call, which covers only the `#[ignore]`d
  RUST gates — the script-shaped gates (`verify.ts`, `corpus_detect.ts`, the fuzz compile-rot check,
  `pin_cold_fetch`, the two gate-cache gates) are prose and unguarded, which is exactly where the miss
  landed. **The layer, when built:** a fast-tier lint asserting (a) every registry gate id appears in
  `tests/README.md`, and (b) no prose integer adjacent to a gate-group name disagrees with the
  registry's own membership — or, simpler and preferred on the (b) half, that no such integer is
  written at all. Cheap, no cargo. **Trigger:** a third instance, OR the next gate added to the
  registry by anyone who is not also editing `tests/README.md` in the same commit.

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.gate-exists-registry-nowhere-tests-readme-md-hand"]
transition_ids = ["testing.trigger.gate-exists-registry-nowhere-tests-readme-md-hand"]

[[record.source_replacement]]
span_id = "span-record-gate-exists-registry-nowhere-tests-readme-md-hand"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.gate-s-own-stdout-print-column-result-pass"
title = "A gate's own stdout can print a column-0 `RESULT: PASS (...)` line, so a log poll that greps for `RESULT:` (even line-anchored) reads a sub-check's verdict as the tier's."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """A gate's own stdout can print a column-0 `RESULT: PASS (...)` line, so a log poll that greps for `RESULT:` (even line-anchored) reads a sub-check's verdict as the tier's.
"""
detail_md = """- **A gate's own stdout can print a column-0 `RESULT: PASS (...)` line, so a log poll that greps
  for `RESULT:` (even line-anchored) reads a sub-check's verdict as the tier's.** Proven
  2026-08-07, twice in one cycle: a session polling a live `check-local-*.log` for the tier
  verdict fired early on `RESULT: PASS (editorial mapping holds mechanically)` — an in-gate
  self-check's line — while the tier had ~10 minutes left; the re-armed anchor `^RESULT:` matched
  the same line again. The tier verdict's exact shapes are
  `RESULT: PASS — all in-tier gates green (tier=<t>)` and `RESULT: FAIL — <n> gate(s) failed: …`
  (and a `--only` run's is the distinct `<sel> selected PASS/FAIL … no tier verdict` line), so a
  poll must match those shapes, not the `RESULT:` prefix — and any watcher should also cover
  process death (`pgrep` the run) so silence is distinguishable from still-running. Working rule:
  poll for the tier-shaped line verbatim; treat a bare `RESULT:` match inside a live log as a
  sub-check's output until the process has exited.

"""
control_kind = "operator_procedure"
control_state = "live"
reference_ids = ["ref-testing-gate-s-own-stdout-print-column-result-pass-file-heading-h759f2d7eb8a0852ae365a6dccd08e73b4522e560bc3c7e8c7801b2377f6a959c"]
claim_md = """The operator procedure in testing.gate-s-own-stdout-print-column-result-pass's preserved detail remains authoritative.
"""
boundary_md = """This procedure owns its exact operating surface and does not infer adjacent lifecycle work.
"""

[[record.source_replacement]]
span_id = "span-record-gate-s-own-stdout-print-column-result-pass"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.gate-script-health-cache-staleness-key-fresh-worktree"
title = "Gate-script health is in no cache or staleness key — a fresh worktree/cold run is the only detector, and warm runs launder pre-split rot."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Gate-script health is in no cache or staleness key — a fresh worktree/cold run is the only detector, and warm runs launder pre-split rot.
"""
detail_md = """- **Gate-script health is in no cache or staleness key — a fresh worktree/cold run is the only
  detector, and warm runs launder pre-split rot.** Proven instance: `fuzz/generate.sh` predated the
  thin-root split and still read generated serialization from the pre-split `src/serialization.rs`
  and appended the custom-serialization helpers into the seed-once crate root `lib.rs`; a COLD
  regeneration (fresh worktree, or `--refresh-fuzz`) therefore died at the probe-list step while warm
  machines never re-ran the script (`fuzz_compile_rot` re-runs `generate.sh` only when
  `fuzz/generated` is absent — see `tests/README.md` § the fuzz gate), so pre-split state kept the
  gate green. Fixed by pointing the probe lists at `src/generated/serialization.rs`, appending the
  helpers into the clobbered `generated/mod.rs` (with `rm -f` of the stale root `lib.rs` and
  `--no-preserve-comments`, mirroring `integration_tests::preserve_encodings`' `run_test`), and adding
  a pointed existence check inside `gen_probe_list` so the NEXT layout move fails with a diagnosis
  rather than an opaque grep failure under `set -e`. Working rule meanwhile: after any generated-crate
  LAYOUT change, cold-run the regeneration scripts that consume generated layout (`rm -rf
  fuzz/generated` or `--refresh-fuzz` before the run), since no gate re-runs them on a warm tree.
  Mechanical layer on a SECOND regeneration-script rot instance: a periodic cold-regen leg (or a
  script-consumed-path staleness key that forces `generate.sh` to re-run when the generated layout
  changes) — the only layer that catches script rot without a human remembering to cold-run.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.gate-script-health-cache-staleness-key-fresh-worktree"]
transition_ids = ["testing.trigger.gate-script-health-cache-staleness-key-fresh-worktree"]

[[record.source_replacement]]
span_id = "span-record-gate-script-health-cache-staleness-key-fresh-worktree"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.generated-local-collision-class-refused-mangled-refusal-s"
title = "The generated-local collision class is refused, not mangled — and the refusal's shape scope comes from a bounded probe matrix, so a position that matrix never touched can still ship an uncompilable crate."
projection_group = "next-priority"
legacy_aliases = ["Next work 8"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """The generated-local collision class is refused, not mangled — and the refusal's shape scope comes from a bounded probe matrix, so a position that matrix never touched can still ship an uncompilable crate.
"""
detail_md = """8. **The generated-local collision class is refused, not mangled — and the refusal's shape scope
    comes from a bounded probe matrix, so a position that matrix never touched can still ship an
    uncompilable crate.** A field whose emitted identifier is one of the fixed locals the generated
    serialization bodies bind now rejects at parse time (`parsing::GENERATED_LOCAL_RESERVED`, seven
    names each carrying the shape × profile × error class it was measured to break), with `; @name
    <other>` as the remedy and the CBOR wire key untouched. The emitter-local vocabulary is held
    LOCKSTEP by `identifier_hazard_tests::generated_local_registry_covers_emitter_locals` (a new
    emitter local fails until verdicted into the reserved set or into
    `GENERATED_LOCAL_PROBED_SAFE`), and `generated_local_out_of_scope_crates_compile` (`#[ignore]`,
    full tier) compiles every out-of-scope cell per profile so a too-narrow scope cannot hide.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-generated-local-collision-class-refused-mangled-refusal-s-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.generated-local-collision-class-refused-mangled-refusal-s's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record.source_replacement]]
span_id = "span-record-generated-local-collision-class-refused-mangled-refusal-s"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.generated-local-collision.general-mangling-remedy"
title = "Residual: mangling is still the general fix."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Residual: mangling is still the general fix.
"""
detail_md = """    - **Residual: mangling is still the general fix.** The shipped behavior takes the name away from
      the spec author instead of renaming the generator's own local, so a spec that genuinely wants
      a field called `raw` must carry a `; @name` comment. **Reopening signal, measurable by a party
      who already has the problem:** a consumer whose CDDL is machine-generated or vendored from
      upstream — where a `; @name` comment cannot be added at all — is refused by this check. They
      cannot apply the remedy, which is the only condition under which the cheap fix is not a fix.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.generated-local-collision-general-mangling-remedy"]

[[record.source_replacement]]
span_id = "span-part-part-mangling-still-general-fix"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.generated-local-collision.scope-wide-probe"
title = "Residual: the scope is only as wide as the probe."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Residual: the scope is only as wide as the probe.
"""
detail_md = """    - **Residual: the scope is only as wide as the probe.** Membership and per-name
      `ReservedScope` were measured over five shapes (array-rep / map-rep / tagged record /
      embedded plain group / group-choice arm) × three profiles × two field types, plus a
      `--wasm=true` pass. NOT probed: a field whose type is a named rule or newtype, `.cbor`-payload
      and bounded-type member positions, and the `--json-schema-export` / `--component` faces.
      **Reopening signal:** a generated crate fails `cargo check` with a shadowed-binding error
      (E0599/E0308/E0124) on a field whose name is in `GENERATED_LOCAL_PROBED_SAFE`, or on a
      reserved name used in a shape outside its declared scope — the compile error names the
      binding, so the report arrives pre-diagnosed and says exactly which row to widen.

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.generated-local-collision-scope-wide-probe"]
transition_ids = ["testing.trigger.generated-local-collision-scope-wide-probe"]

[[record.source_replacement]]
span_id = "span-part-part-scope-wide-probe"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.generic-raw-bytes-base-refused-generic-extern-base"
title = "A generic RAW-BYTES base is refused, where a generic EXTERN base is recorded-and-skipped — the asymmetry is the decision, and only one direction of it is swept."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """A generic RAW-BYTES base is refused, where a generic EXTERN base is recorded-and-skipped — the asymmetry is the decision, and only one direction of it is swept.
"""
detail_md = """- **A generic RAW-BYTES base is refused, where a generic EXTERN base is recorded-and-skipped — the
  asymmetry is the decision, and only one direction of it is swept.** A raw-bytes type is exactly
  its own bytes and carries no element type a parameter could name, so
  `foo<T> = _CDDL_CODEGEN_RAW_BYTES_TYPE_` is a parse-time graceful rejection
  (`robustness_tests::generic_raw_bytes_base_rejects_gracefully`, plus the fast-tier
  `snapshot_tests::extern_interface_check_refuses_generic_raw_bytes_base` beside the two skip
  tests); an extern base names an arbitrary hand-written type that MAY legitimately be
  parameterized, so its generic-ness is recorded and the extern-interface self-check and the
  json-gen schema-row emitter skip it. Both directions are now enumerated as MATRIX rows too —
  `ext.extern.generic` / `ext.extern.generic_instance` (the extern base with and without a bound
  instance, whose two faces answer differently) beside `ext.raw_bytes.generic` (the refusal,
  projected into `tests/matrix_reject/`) — so the asymmetry is a committed verdict a parser change
  flips visibly rather than a fact three hand tests happen to agree on. What is NOT swept is the
  rest of the marker×directive
  product at a generic base — `@copy`, `@raw_bytes_flavor` and `@extern_companions` each have their
  own reachability at these two branches, and each is pinned only where someone thought to look.
  Reopening signal: a second marker-branch directive found accepted-and-inert on a generic base
  (the count of hand-pinned marker directives is the magnitude that grows here, not the number of
  consumers).
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.generic-raw-bytes-base-refused-generic-extern-base.
"""
authority_reference_id = "ref-testing-generic-raw-bytes-base-refused-generic-extern-base-authority"
permanence = "permanent"

[[record.source_replacement]]
span_id = "span-record-generic-raw-bytes-base-refused-generic-extern-base"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.grammar-fuzzer-escalations"
title = "Grammar-fuzzer escalations."
projection_group = "next-priority"
legacy_aliases = ["Next work 1"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """Grammar-fuzzer escalations.
"""
detail_md = """1. **Grammar-fuzzer escalations.** The lazy-first shape-recombination fuzzer is shipped
   (`tests/README.md` § \"Shape-recombination fuzzer\": `cddl-matrix/project_recombination.ts` →
   `tests/recomb/ingredients.json` → `recombination_generation_sweep` (default suite) + the
   profile-parameterized layer-2 gates `recombination_crates_execute` /
   `recombination_preserve_crates_execute` / `recombination_json_crates_execute` /
   `recombination_wasm_crates_check` (full tier), with cited known-class ledgers, the
   `ledger_key_shape_floor` key hygiene gate, and a promotion flow into the pinned collections).
   Residuals, in escalation order:
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-grammar-fuzzer-escalations-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.grammar-fuzzer-escalations's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record.source_replacement]]
span_id = "span-record-grammar-fuzzer-escalations"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.grammar-fuzzer.arbitrary-supported-ast"
title = "`arbitrary`-derived \"supported-CDDL\" AST generation"
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """`arbitrary`-derived \"supported-CDDL\" AST generation
"""
detail_md = """   - **`arbitrary`-derived \"supported-CDDL\" AST generation** — only if recombination plateaus (its
     first sweep surfaced six new panic-class families, so the plateau is not near; re-evaluate when
     a sweep over an extended member-kind/template table stops minting findings).
"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "missing_system"
risk = "abort_or_panic"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.grammar-fuzzer-arbitrary-supported-ast"]

[[record.source_replacement]]
span_id = "span-part-part-arbitrary-derived-supported-cddl-ast-generation"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.grammar-fuzzer.batch-masking-detector"
title = "Batch-masking detector for the layer-2 sweeps."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Batch-masking detector for the layer-2 sweeps.
"""
detail_md = """   - **Batch-masking detector for the layer-2 sweeps.** The ~40-rule batching means a green batch
     is not a per-composition guarantee for failure classes whose symptom is a missing CRATE-GLOBAL
     definition: `Int` is a crate-global extern emitted iff any rule registers a reference, so a
     batch-mate that registers it compiles the whole batch green. Proven instance:
     `bytes .cbor { * tstr => int }` fails standalone under default yet rode green default batches
     until the wasm leg's different batch boundaries isolated it — caught by review, not by any
     gate (the filing rule + caveat now live on `LAYER2_RULES_PER_BATCH`). The mechanical layer,
     worth building on the SECOND masked instance: run each layer-2 sweep under a second
     DETERMINISTIC batch permutation (a different fixed grouping — determinism preserved, most
     pair-masks broken, wall-clock ×2 on full-tier gates), or an occasional singleton mode
     (batch size 1 — the exhaustive oracle, but hours of wall-clock). Both costs are FIRST-run
     prices now: layer-2 batches are gate-cached per generated tree, so a permutation's or
     singleton's re-runs skip every batch whose composition didn't change — the detector got
     materially cheaper to adopt. Either also owns the
     misfiled-ledger-contradiction class (an entry claiming profile-specificity while masked in its
     home profile — how this instance actually surfaced). (The observed-baseline comments beside
     these gates' floors have their own rot class — see the standing residual below.)
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.grammar-fuzzer-batch-masking-detector"]
transition_ids = ["testing.trigger.grammar-fuzzer-batch-masking-detector"]

[[record.source_replacement]]
span_id = "span-part-part-batch-masking-detector-layer-sweeps"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.grammar-fuzzer.embed-site-alias-roots"
title = "Embed-site leg for alias-classifying roots — two proven escapes."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Embed-site leg for alias-classifying roots — two proven escapes.
"""
detail_md = """   - **Embed-site leg for alias-classifying roots — two proven escapes.** A composition whose ROOT
     rule classifies as a plain alias (`rcN = bytes .cbor {…}`, `bytes .cbor (int .ne N)`) emits its
     wrapper (de)serialize code only at an embed/USE site — a bare alias rule emits none — so the
     layer-2 sweeps never compile that emission surface for alias roots (embedding exists only when
     such a template lands INNER under another outer, e.g. the
     `arr_mid inner=cbor_payload` case). Both escapes were preserve-only compile bugs found by
     review/fixture-TDD in the session that fixed their ledgered `tag_content` siblings (tag rules
     AUTO-WRAP into a struct, so those failed standalone and were ledgered; alias roots stayed
     green), each verified standalone-green/embedded-red against its pre-fix tree: the
     `.cbor`-payload constrained-int E0308 (dropped `final_exprs`) and the `.cbor` inline-table
     owned-serializer `.end()` E0308 — both since fixed and pinned by the embedding holder members
     of the `tagged_constrained_int` / `cbor_bignint_table` corpus fixtures. Mechanical shape, worth
     building on the THIRD instance: mint each alias-classifying root also as an embedded variant
     (`rcN_embed = [e: rcN]` — the matrix probe's embed-holder pattern), scoped to alias roots to
     bound the layer-2 wall-clock cost. Note the POPULATION shrank on 2026-08-04 without the trigger
     moving: both recorded instances were `.cbor` roots, and a `.cbor` rule body now force-wraps into
     a struct that emits its (de)serialize code standalone, so the sweeps compile that surface
     directly. What still classifies as a plain alias root is the named collection (tagged or not),
     the scalar re-alias and the `T / null` collapse — that is the class a third instance would have
     to come from, and it stands at two.
     The class's SEMANTIC flavor is DELIVERED (2026-08-04) and is not a matrix leg. It was: a bare
     `.cbor` alias root's STANDALONE (de)serialization was the target type's own
     (`y = bytes .cbor int` → `pub type Y = Int`), so the root's `to_cbor_bytes` wrote the bare
     inner form and its `from_cbor_bytes` accepted that form while rejecting the spec's actual
     byte-string one — silently, in a crate that compiled everywhere, while embed sites wrapped
     correctly. A `.cbor` rule body now force-wraps into the wrapper struct the `@newtype` spelling
     produces, so the class is closed at the source rather than detected: the broader invariant it
     was an instance of — no wire-affecting property of a `RustType` may survive on a root that
     emits a transparent alias — is a hard refusal in
     `IntermediateTypes::assert_no_wire_facts_survive_a_transparent_alias`, which fires for any
     FUTURE encoding operation at the one registration seam that could carry one (unconditionally
     over every non-`Fixed` base: a `Fixed` one is the already-refused bare-literal rule, whose
     alias entry never reaches a wire). The emitted-code half — the part no generator assert can speak for — is executed by
     `integration_tests::cbor_rule_body_standalone_codec_agrees_with_its_embed_site` (`local` tier),
     which runs the wrapped-form/bare-form/embed-agreement table on a scalar and a record payload
     under the plain and preserve profiles. The embed-site COMPILE leg above keeps its own
     third-instance trigger unchanged: it is a different instrument on a different failure class,
     and it still stands at two instances.
"""
work_state = "ready"
work_intent = "build_system"
work_kind = "missing_system"
risk = "abort_or_panic"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.grammar-fuzzer.embed-site-alias-roots's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""
admission_ids = ["testing.admission-path.grammar-fuzzer-embed-site-alias-roots"]

[[record.source_replacement]]
span_id = "span-part-part-embed-site-leg-alias-classifying-roots-two-proven"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.grammar-fuzzer.real-world-corpus-differential"
title = "Real-world corpus differential"
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Real-world corpus differential
"""
detail_md = """   - **Real-world corpus differential** (see `draft/testing-recommendations/RECOMMENDATIONS.md`):
     synthetic breadth vs real-world depth — recombination does not replace it.

"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "missing_system"
risk = "abort_or_panic"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.grammar-fuzzer-real-world-corpus-differential"]

[[record.source_replacement]]
span_id = "span-part-part-real-world-corpus-differential"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.guard.add-profile-wrapper-participation-grid-participation-differs-across"
title = "Current control for testing.add-profile-wrapper-participation-grid-participation-differs-across."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.add-profile-wrapper-participation-grid-participation-differs-across.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-add-profile-wrapper-participation-grid-participation-differs-across-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.add-profile-wrapper-participation-grid-participation-differs-across's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.alloc-imports-nested-inline-modules-hand-carried-design"
title = "Current control for testing.alloc-imports-nested-inline-modules-hand-carried-design."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.alloc-imports-nested-inline-modules-hand-carried-design.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-alloc-imports-nested-inline-modules-hand-carried-design-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.alloc-imports-nested-inline-modules-hand-carried-design's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.armed-idle-harness-arms-empty-head-ledgers-zero"
title = "Current control for testing.armed-idle-harness-arms-empty-head-ledgers-zero."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.armed-idle-harness-arms-empty-head-ledgers-zero.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-armed-idle-harness-arms-empty-head-ledgers-zero-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.armed-idle-harness-arms-empty-head-ledgers-zero's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.cargo-feature-generated-crate-s-dependency-change-what"
title = "Current control for testing.cargo-feature-generated-crate-s-dependency-change-what."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.cargo-feature-generated-crate-s-dependency-change-what.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-cargo-feature-generated-crate-s-dependency-change-what-test-symbol-ha4c9663bd6e6a90fce72f489a54d11a70788c1b746766d7e0d016ba6b004788d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.cargo-feature-generated-crate-s-dependency-change-what's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.checking-runtime-flavor-s-safety-condition-rather-stating"
title = "Current control for testing.checking-runtime-flavor-s-safety-condition-rather-stating."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.checking-runtime-flavor-s-safety-condition-rather-stating.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-checking-runtime-flavor-s-safety-condition-rather-stating-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.checking-runtime-flavor-s-safety-condition-rather-stating's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.citation-resolves-naming-wrong-thing-invisible-citation-lint"
title = "Current control for testing.citation-resolves-naming-wrong-thing-invisible-citation-lint."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.citation-resolves-naming-wrong-thing-invisible-citation-lint.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-citation-resolves-naming-wrong-thing-invisible-citation-lint-test-symbol-hffa7bf63970bb6799e6e97c59cb4cd4406606d343959edae1718ba9e2e0b8adf"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.citation-resolves-naming-wrong-thing-invisible-citation-lint's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.comment-residue-false-matches-text-scans-over-emitted"
title = "Current control for testing.comment-residue-false-matches-text-scans-over-emitted."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.comment-residue-false-matches-text-scans-over-emitted.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-comment-residue-false-matches-text-scans-over-emitted-test-symbol-hc73eeb7dc4e0a05361f26c1616137125d0276df00a81387f23f0f1e9d87cdd5c"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.comment-residue-false-matches-text-scans-over-emitted's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.complete-cargo-mutants-sweep-will-never-run"
title = "Current control for testing.complete-cargo-mutants-sweep-will-never-run."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.complete-cargo-mutants-sweep-will-never-run.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-complete-cargo-mutants-sweep-will-never-run-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.complete-cargo-mutants-sweep-will-never-run's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.containment-note-s-stated-reason-go-false-any"
title = "Current control for testing.containment-note-s-stated-reason-go-false-any."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.containment-note-s-stated-reason-go-false-any.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-containment-note-s-stated-reason-go-false-any-gate-h6a27f79888f19ab92549180ecdcdf05a24145020e97ee6233a1528a47d151786"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.containment-note-s-stated-reason-go-false-any's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.control-bytes-committed-source-files-proven-instance-machinery"
title = "Current control for testing.control-bytes-committed-source-files-proven-instance-machinery."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.control-bytes-committed-source-files-proven-instance-machinery.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-control-bytes-committed-source-files-proven-instance-machinery-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797", "ref-testing-control-bytes-committed-source-files-proven-instance-machinery-gate-ha6b9f717f086d7bc829bcf9c7b9f53ffa31fd91a125917ab3510ddada4d8954c"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.control-bytes-committed-source-files-proven-instance-machinery's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.corpus-wide-regen-over-prior-output-sweep-reaches"
title = "Current control for testing.corpus-wide-regen-over-prior-output-sweep-reaches."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.corpus-wide-regen-over-prior-output-sweep-reaches.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-corpus-wide-regen-over-prior-output-sweep-reaches-gate-hf08fa6f837ebd524c01449e86c1865d65b0025a02ada385d347479b8c08b15c8"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.corpus-wide-regen-over-prior-output-sweep-reaches's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.dependency-swap-pass-compile-parity-existing-gate-silently"
title = "Current control for testing.dependency-swap-pass-compile-parity-existing-gate-silently."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.dependency-swap-pass-compile-parity-existing-gate-silently.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-dependency-swap-pass-compile-parity-existing-gate-silently-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.dependency-swap-pass-compile-parity-existing-gate-silently's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.directive-dependent-emitter-edge-case-triggering-shape-hand"
title = "Current control for testing.directive-dependent-emitter-edge-case-triggering-shape-hand."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.directive-dependent-emitter-edge-case-triggering-shape-hand.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-directive-dependent-emitter-edge-case-triggering-shape-hand-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.directive-dependent-emitter-edge-case-triggering-shape-hand's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.directive-s-documented-sentence-stay-true-consumer-visible"
title = "Current control for testing.directive-s-documented-sentence-stay-true-consumer-visible."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.directive-s-documented-sentence-stay-true-consumer-visible.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-directive-s-documented-sentence-stay-true-consumer-visible-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.directive-s-documented-sentence-stay-true-consumer-visible's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
title = "Current control for testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-docs-vs-behavior-conformance-harness-comment-dsl-mdx-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.documented-failure-shape-exercises-prose-cross-crate-int"
title = "Current control for testing.documented-failure-shape-exercises-prose-cross-crate-int."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.documented-failure-shape-exercises-prose-cross-crate-int.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-documented-failure-shape-exercises-prose-cross-crate-int-test-symbol-h4c5e041e97a704e70c4a95276f741dd0e153a6e8cab36dea83486c0d206018f8", "ref-testing-documented-failure-shape-exercises-prose-cross-crate-int-test-symbol-h8e57dd2c7c817395d3ecbe80f5de769676ca735f3c57392108c2b5e3bff80b56"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.documented-failure-shape-exercises-prose-cross-crate-int's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.documented-flag-pairing-broken-fixture-stays-green-because"
title = "Current control for testing.documented-flag-pairing-broken-fixture-stays-green-because."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.documented-flag-pairing-broken-fixture-stays-green-because.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-documented-flag-pairing-broken-fixture-stays-green-because-test-symbol-h567379bcd8f6b431087b7c8a5a70ec1c1dc112624da15e371ac4d0117dcd009f", "ref-testing-documented-flag-pairing-broken-fixture-stays-green-because-test-symbol-hc64f92382996eb99ea23f2c1b7c6a03857d28d5348d758322267f30a93056c52"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.documented-flag-pairing-broken-fixture-stays-green-because's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.doubled-doc-comment-markers-emitted-docs-proven-instance"
title = "Current control for testing.doubled-doc-comment-markers-emitted-docs-proven-instance."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.doubled-doc-comment-markers-emitted-docs-proven-instance.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-doubled-doc-comment-markers-emitted-docs-proven-instance-test-symbol-haf7edde0b79ace73f9b150c48055de3ca3348b998530b48a7b91250a394d3404"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.doubled-doc-comment-markers-emitted-docs-proven-instance's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.dsl-position-tests-string-level-design-green-emitted"
title = "Current control for testing.dsl-position-tests-string-level-design-green-emitted."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.dsl-position-tests-string-level-design-green-emitted.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-dsl-position-tests-string-level-design-green-emitted-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.dsl-position-tests-string-level-design-green-emitted's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.duplicates-dedicated-generic-collision-message"
title = "Current control for testing.duplicates.dedicated-generic-collision-message."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.duplicates.dedicated-generic-collision-message.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-duplicates-dedicated-generic-collision-message-test-symbol-h5fad85f77abba604c3e768d2d77129d2a6a626795833b7048bb3e2e7d906dbc2"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.duplicates.dedicated-generic-collision-message's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.emission-branch-generated-output-fixture-exercises-invisible-gate"
title = "Current control for testing.emission-branch-generated-output-fixture-exercises-invisible-gate."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.emission-branch-generated-output-fixture-exercises-invisible-gate.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-emission-branch-generated-output-fixture-exercises-invisible-gate-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.emission-branch-generated-output-fixture-exercises-invisible-gate's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.emit-tests-gate-asserts-round-trip-green-cannot"
title = "Current control for testing.emit-tests-gate-asserts-round-trip-green-cannot."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.emit-tests-gate-asserts-round-trip-green-cannot.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-emit-tests-gate-asserts-round-trip-green-cannot-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.emit-tests-gate-asserts-round-trip-green-cannot's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.emitted-shape-lint-classes-outside-clippy-all-beyond"
title = "Current control for testing.emitted-shape-lint-classes-outside-clippy-all-beyond."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.emitted-shape-lint-classes-outside-clippy-all-beyond.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-emitted-shape-lint-classes-outside-clippy-all-beyond-test-symbol-haf7edde0b79ace73f9b150c48055de3ca3348b998530b48a7b91250a394d3404"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.emitted-shape-lint-classes-outside-clippy-all-beyond's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
title = "Current control for testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-emitter-drops-overloadable-parameter-building-fresh-config-invisible-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.exclusion-reasons-informational-interchange"
title = "Current control for testing.exclusion-reasons-informational-interchange."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.exclusion-reasons-informational-interchange.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-exclusion-reasons-informational-interchange-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.exclusion-reasons-informational-interchange's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.extend-recombination-member-kind-table-optional-fixed-shape"
title = "Current control for testing.extend-recombination-member-kind-table-optional-fixed-shape."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.extend-recombination-member-kind-table-optional-fixed-shape.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-extend-recombination-member-kind-table-optional-fixed-shape-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.extend-recombination-member-kind-table-optional-fixed-shape's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.extern-deps-wasm-boundary-surface-packaging-json-gen"
title = "Current control for testing.extern-deps-wasm-boundary-surface-packaging-json-gen."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.extern-deps-wasm-boundary-surface-packaging-json-gen.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-extern-deps-wasm-boundary-surface-packaging-json-gen-test-symbol-hb167df3b741e441b329e40b4e266b72eeb0270c3ff37a9648b2578cb2c2fe932"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.extern-deps-wasm-boundary-surface-packaging-json-gen's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.extreme-value-boundary-correctness-generation-time-encode-paths"
title = "Current control for testing.extreme-value-boundary-correctness-generation-time-encode-paths."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.extreme-value-boundary-correctness-generation-time-encode-paths.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-extreme-value-boundary-correctness-generation-time-encode-paths-test-symbol-h03a2d4aca6fd0b7322c8b06e51166f2dd358e81b0f0599b08bc930dafaba33f2"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.extreme-value-boundary-correctness-generation-time-encode-paths's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.finding-s-defect-side-attribution-recorded-narrative-plausibility"
title = "Current control for testing.finding-s-defect-side-attribution-recorded-narrative-plausibility."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.finding-s-defect-side-attribution-recorded-narrative-plausibility.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-finding-s-defect-side-attribution-recorded-narrative-plausibility-gate-hfd5262ebb003d28f8005bf974fa55f6ad2a0d8edd3503b7c105f5136a214c75a"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.finding-s-defect-side-attribution-recorded-narrative-plausibility's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.fixture-models-obsolete-user-contract-stays-green-real"
title = "Current control for testing.fixture-models-obsolete-user-contract-stays-green-real."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.fixture-models-obsolete-user-contract-stays-green-real.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-fixture-models-obsolete-user-contract-stays-green-real-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.fixture-models-obsolete-user-contract-stays-green-real's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.fixture-s-verdict-accident-cargo-feature-unification-harness"
title = "Current control for testing.fixture-s-verdict-accident-cargo-feature-unification-harness."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.fixture-s-verdict-accident-cargo-feature-unification-harness.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-fixture-s-verdict-accident-cargo-feature-unification-harness-test-symbol-h6f14a1dfc8b23912c26f6e2155cb0f7aa7b2090605380a02d393bf82074e2b57", "ref-testing-fixture-s-verdict-accident-cargo-feature-unification-harness-test-symbol-h92e6ce8d56c91219e54fcbeae835cfb71d8d3167d5ec16c6e3ba5ce03e1ea23f", "ref-testing-fixture-s-verdict-accident-cargo-feature-unification-harness-test-symbol-hbdd271db5c0070ba70095dc3db0323b4ebffefd1fb2125040e83e6cfcebfdf4a"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.fixture-s-verdict-accident-cargo-feature-unification-harness's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.full-n-flag-powerset-pict-pairwise"
title = "Current control for testing.full-n-flag-powerset-pict-pairwise."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.full-n-flag-powerset-pict-pairwise.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-full-n-flag-powerset-pict-pairwise-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.full-n-flag-powerset-pict-pairwise's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.gate-cache-closure-audit-structurally-blind-gate-authored"
title = "Current control for testing.gate-cache-closure-audit-structurally-blind-gate-authored."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.gate-cache-closure-audit-structurally-blind-gate-authored.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-gate-cache-closure-audit-structurally-blind-gate-authored-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.gate-cache-closure-audit-structurally-blind-gate-authored's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.gate-exists-registry-nowhere-tests-readme-md-hand"
title = "Current control for testing.gate-exists-registry-nowhere-tests-readme-md-hand."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.gate-exists-registry-nowhere-tests-readme-md-hand.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-gate-exists-registry-nowhere-tests-readme-md-hand-gate-h14a4d326b3ff7b16ea5befdfb6772cbfa23f666cb1d531fc5d54e68ec6398e80"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.gate-exists-registry-nowhere-tests-readme-md-hand's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.gate-script-health-cache-staleness-key-fresh-worktree"
title = "Current control for testing.gate-script-health-cache-staleness-key-fresh-worktree."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.gate-script-health-cache-staleness-key-fresh-worktree.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-gate-script-health-cache-staleness-key-fresh-worktree-gate-he58fbd1649112b6357870774f10623684918928b7d10e5a2207e0389b4c867b7"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.gate-script-health-cache-staleness-key-fresh-worktree's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.generated-local-collision-scope-wide-probe"
title = "Current control for testing.generated-local-collision.scope-wide-probe."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.generated-local-collision.scope-wide-probe.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-generated-local-collision-scope-wide-probe-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.generated-local-collision.scope-wide-probe's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.generic-raw-bytes-base-refused-generic-extern-base"
title = "Current control for testing.generic-raw-bytes-base-refused-generic-extern-base."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.generic-raw-bytes-base-refused-generic-extern-base.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-generic-raw-bytes-base-refused-generic-extern-base-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.generic-raw-bytes-base-refused-generic-extern-base's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.grammar-fuzzer-batch-masking-detector"
title = "Current control for testing.grammar-fuzzer.batch-masking-detector."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.grammar-fuzzer.batch-masking-detector.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-grammar-fuzzer-batch-masking-detector-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.grammar-fuzzer.batch-masking-detector's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
title = "Current control for testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-hand-enumerated-ban-pattern-list-authority-lives-gitignored-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
title = "Current control for testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-hand-maintained-source-dependency-mapping-manifest-changeset-silently-test-symbol-h6c1e0d91bc6033d6e72fba524afbf91d1927393e2d0f847e632d618ea16128bf"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.lint-doc-citations-accepts-docs-heading-durable-citation"
title = "Current control for testing.lint-doc-citations-accepts-docs-heading-durable-citation."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.lint-doc-citations-accepts-docs-heading-durable-citation.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-lint-doc-citations-accepts-docs-heading-durable-citation-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.lint-doc-citations-accepts-docs-heading-durable-citation's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.logic-tool-emits-source-text-outside-layer-reasons"
title = "Current control for testing.logic-tool-emits-source-text-outside-layer-reasons."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.logic-tool-emits-source-text-outside-layer-reasons.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-logic-tool-emits-source-text-outside-layer-reasons-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.logic-tool-emits-source-text-outside-layer-reasons's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.make-fixture-comment-s-claimed-wire-bytes-executable"
title = "Current control for testing.make-fixture-comment-s-claimed-wire-bytes-executable."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.make-fixture-comment-s-claimed-wire-bytes-executable.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-make-fixture-comment-s-claimed-wire-bytes-executable-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.make-fixture-comment-s-claimed-wire-bytes-executable's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.making-type-choice-path-reject-duplicated-explicit-name"
title = "Current control for testing.making-type-choice-path-reject-duplicated-explicit-name."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.making-type-choice-path-reject-duplicated-explicit-name.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-making-type-choice-path-reject-duplicated-explicit-name-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.making-type-choice-path-reject-duplicated-explicit-name's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.matrix-s-note-names-panic-site-aborts-nothing"
title = "Current control for testing.matrix-s-note-names-panic-site-aborts-nothing."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.matrix-s-note-names-panic-site-aborts-nothing.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-matrix-s-note-names-panic-site-aborts-nothing-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.matrix-s-note-names-panic-site-aborts-nothing's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.mechanical-layers-two-review-owned-design-rules-tests"
title = "Current control for testing.mechanical-layers-two-review-owned-design-rules-tests."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.mechanical-layers-two-review-owned-design-rules-tests.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-mechanical-layers-two-review-owned-design-rules-tests-test-symbol-h7252c51ca49273b5edf62a95a88ca8f30b01aa234d9593848a5b35cb2465dd52", "ref-testing-mechanical-layers-two-review-owned-design-rules-tests-test-symbol-hc749902b18315a9d1f12a307c7c9a1196f35ee12722b7211043985cfe3fe37ca"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.mechanical-layers-two-review-owned-design-rules-tests's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.message-s-level-classification-review-owned-two-ways"
title = "Current control for testing.message-s-level-classification-review-owned-two-ways."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.message-s-level-classification-review-owned-two-ways.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-message-s-level-classification-review-owned-two-ways-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.message-s-level-classification-review-owned-two-ways's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.msrv-declaration-os-matrix-generated-code"
title = "Current control for testing.msrv-declaration-os-matrix-generated-code."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.msrv-declaration-os-matrix-generated-code.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-msrv-declaration-os-matrix-generated-code-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.msrv-declaration-os-matrix-generated-code's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.multifile-reference-position-coverage-two-position-keyed-escapes"
title = "Current control for testing.multifile-reference-position-coverage-two-position-keyed-escapes."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.multifile-reference-position-coverage-two-position-keyed-escapes.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-multifile-reference-position-coverage-two-position-keyed-escapes-test-symbol-h1b01a70d4040ac15d5c5b342c3e8dc49b26e0d3d15ab75531aa65fd7ac058804", "ref-testing-multifile-reference-position-coverage-two-position-keyed-escapes-test-symbol-hb058dd1eca57bc8a79f91a36dadd3ca901e2f81888e20775ea28346855b517a6"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.multifile-reference-position-coverage-two-position-keyed-escapes's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.nothing-asserts-how-many-times-rejection-message-emitted"
title = "Current control for testing.nothing-asserts-how-many-times-rejection-message-emitted."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.nothing-asserts-how-many-times-rejection-message-emitted.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-nothing-asserts-how-many-times-rejection-message-emitted-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.nothing-asserts-how-many-times-rejection-message-emitted's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.nothing-asserts-two-ir-sites-cannot-mint-same"
title = "Current control for testing.nothing-asserts-two-ir-sites-cannot-mint-same."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.nothing-asserts-two-ir-sites-cannot-mint-same.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-nothing-asserts-two-ir-sites-cannot-mint-same-test-symbol-h765c81ec81d1528831b49117e4f4c56dbe06beaf882ed56d779ae14c83a5d7f9", "ref-testing-nothing-asserts-two-ir-sites-cannot-mint-same-test-symbol-hc2a3331e11cd1132a1b4075f798bdb40f6559713ca231f7c9b6a28fa598a0e3d", "ref-testing-nothing-asserts-two-ir-sites-cannot-mint-same-test-symbol-hcf12fb74615c93111e6709469e4256b6a5177a805e60d22c6e675fe9a586d6ab"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.nothing-asserts-two-ir-sites-cannot-mint-same's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.observed-baseline-comments-beside-gate-floors-rot-silently"
title = "Current control for testing.observed-baseline-comments-beside-gate-floors-rot-silently."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.observed-baseline-comments-beside-gate-floors-rot-silently.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-observed-baseline-comments-beside-gate-floors-rot-silently-gate-hfd5262ebb003d28f8005bf974fa55f6ad2a0d8edd3503b7c105f5136a214c75a"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.observed-baseline-comments-beside-gate-floors-rot-silently's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.orphan-fixture-directory-meta-test"
title = "Current control for testing.orphan-fixture-directory-meta-test."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.orphan-fixture-directory-meta-test.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-orphan-fixture-directory-meta-test-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.orphan-fixture-directory-meta-test's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.ownership-journal-making-dropped-std-forward-dep-converge"
title = "Current control for testing.ownership-journal-making-dropped-std-forward-dep-converge."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.ownership-journal-making-dropped-std-forward-dep-converge.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-ownership-journal-making-dropped-std-forward-dep-converge-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.ownership-journal-making-dropped-std-forward-dep-converge's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.panic-site-un-shadowing-converting-shallow-panic-graceful"
title = "Current control for testing.panic-site-un-shadowing-converting-shallow-panic-graceful."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.panic-site-un-shadowing-converting-shallow-panic-graceful.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-panic-site-un-shadowing-converting-shallow-panic-graceful-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.panic-site-un-shadowing-converting-shallow-panic-graceful's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.parallel-constructor-fixture-diversity-parser-over-external-input"
title = "Current control for testing.parallel-constructor-fixture-diversity-parser-over-external-input."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.parallel-constructor-fixture-diversity-parser-over-external-input.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-parallel-constructor-fixture-diversity-parser-over-external-input-test-symbol-h0a539c7624cba85a03789079dcbb8396196ccfc603cd7cb8c4fa78a9af03fc2b"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.parallel-constructor-fixture-diversity-parser-over-external-input's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.process-boundary-rustc-after-panic-fix"
title = "Current control for testing.process-boundary.rustc-after-panic-fix."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.process-boundary.rustc-after-panic-fix.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-process-boundary-rustc-after-panic-fix-test-symbol-h1535d59c68ee2febd35ce88ec92365a843838940dda4648495494e28a4a9180d", "ref-testing-process-boundary-rustc-after-panic-fix-test-symbol-h9b42afc58e18471a0a4f1d2ffa1b7c48302bfae88f89740b52be6f1922e8e179"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.process-boundary.rustc-after-panic-fix's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.process-boundary-rustfmt-pre-rustc"
title = "Current control for testing.process-boundary.rustfmt-pre-rustc."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.process-boundary.rustfmt-pre-rustc.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-process-boundary-rustfmt-pre-rustc-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.process-boundary.rustfmt-pre-rustc's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.profile-driven-compile-gate-covers-compositions-profiles-specs"
title = "Current control for testing.profile-driven-compile-gate-covers-compositions-profiles-specs."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.profile-driven-compile-gate-covers-compositions-profiles-specs.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-profile-driven-compile-gate-covers-compositions-profiles-specs-gate-h27130094b630c9c61596568004d5fabd3d8420f7fad801b2c98c52c1c412b72c"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.profile-driven-compile-gate-covers-compositions-profiles-specs's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
title = "Current control for testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-reason-keyed-rejection-evidence-reject-catalogs-proven-near-test-symbol-h2530dd33835f16e026c5d69809e5c1a38b20bee1cd27a5ee3514dce5bcdba173"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.referencing-context-measured-wrapping-deep-honored-base-shape"
title = "Current control for testing.referencing-context-measured-wrapping-deep-honored-base-shape."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.referencing-context-measured-wrapping-deep-honored-base-shape.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-referencing-context-measured-wrapping-deep-honored-base-shape-test-symbol-hdf54feb89788ee827c0fa1a14327f84c1b89c8d44f94ff33911f27e63f2a6253"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.referencing-context-measured-wrapping-deep-honored-base-shape's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.representation-changing-directive-goes-live-new-container-ship"
title = "Current control for testing.representation-changing-directive-goes-live-new-container-ship."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.representation-changing-directive-goes-live-new-container-ship.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-representation-changing-directive-goes-live-new-container-ship-test-symbol-h8fb1437d51762065bee21742c2489192a5e89006867fe660038c9f29c5b1229f"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.representation-changing-directive-goes-live-new-container-ship's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.restructuring-features-std-tool-owned-sub-feature-cddl"
title = "Current control for testing.restructuring-features-std-tool-owned-sub-feature-cddl."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.restructuring-features-std-tool-owned-sub-feature-cddl.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-restructuring-features-std-tool-owned-sub-feature-cddl-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.restructuring-features-std-tool-owned-sub-feature-cddl's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.ruling-premise-gate-x-stays-green-must-probed"
title = "Current control for testing.ruling-premise-gate-x-stays-green-must-probed."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.ruling-premise-gate-x-stays-green-must-probed.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-ruling-premise-gate-x-stays-green-must-probed-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.ruling-premise-gate-x-stays-green-must-probed's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.run-local-values-gate-cache-key-material-proven"
title = "Current control for testing.run-local-values-gate-cache-key-material-proven."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.run-local-values-gate-cache-key-material-proven.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-run-local-values-gate-cache-key-material-proven-gate-h21dded476e33c8fff7c34a5c9b5adedd959977716912b4e5ca82c6189426aa45"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.run-local-values-gate-cache-key-material-proven's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.schema-name-collision-loser-no-row-same-id"
title = "Current control for testing.schema-name.collision-loser-no-row-same-id."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.schema-name.collision-loser-no-row-same-id.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-schema-name-collision-loser-no-row-same-id-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.schema-name.collision-loser-no-row-same-id's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.schema-name-cross-crate-same-id-collision"
title = "Current control for testing.schema-name.cross-crate-same-id-collision."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.schema-name.cross-crate-same-id-collision.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-schema-name-cross-crate-same-id-collision-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.schema-name.cross-crate-same-id-collision's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.schema-name-two-rowless-types-collide"
title = "Current control for testing.schema-name.two-rowless-types-collide."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.schema-name.two-rowless-types-collide.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-schema-name-two-rowless-types-collide-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.schema-name.two-rowless-types-collide's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.stale-known-limitation-prose-surviving-fix-finding-ledgered"
title = "Current control for testing.stale-known-limitation-prose-surviving-fix-finding-ledgered."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.stale-known-limitation-prose-surviving-fix-finding-ledgered.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-stale-known-limitation-prose-surviving-fix-finding-ledgered-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.stale-known-limitation-prose-surviving-fix-finding-ledgered's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.synthesized-name-referenced-never-minted-e0425-flavor"
title = "Current control for testing.synthesized-name-referenced-never-minted-e0425-flavor."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.synthesized-name-referenced-never-minted-e0425-flavor.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-synthesized-name-referenced-never-minted-e0425-flavor-test-symbol-hf32dc18af1b80049eaeceee59c2af22af5f31da3ff3353fe26b6ba5c0de46eb9"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.synthesized-name-referenced-never-minted-e0425-flavor's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.tag-set-non-idiom-generic-boundary"
title = "Current control for testing.tag-set.non-idiom-generic-boundary."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.tag-set.non-idiom-generic-boundary.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-tag-set-non-idiom-generic-boundary-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.tag-set.non-idiom-generic-boundary's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
title = "Current control for testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-tier-s-disk-bandwidth-unmeasured-anything-scratch-floor-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
title = "Current control for testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-tree-wide-sweeps-over-gitignored-regeneration-targets-count-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
title = "Current control for testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-twin-repo-implementations-semantic-drifting-apart-emission-spellings-gate-h6a27f79888f19ab92549180ecdcdf05a24145020e97ee6233a1528a47d151786"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.unused-imports-generated-crates-trait-import-class-name"
title = "Current control for testing.unused-imports-generated-crates-trait-import-class-name."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.unused-imports-generated-crates-trait-import-class-name.
"""
control_kind = "test"
control_state = "live"
reference_ids = ["ref-testing-unused-imports-generated-crates-trait-import-class-name-test-symbol-h4c5e041e97a704e70c4a95276f741dd0e153a6e8cab36dea83486c0d206018f8", "ref-testing-unused-imports-generated-crates-trait-import-class-name-test-symbol-h787d2bf07c1dae413f902528cf38ac20971ed846b263b6b5e3a58b2920d1abbc", "ref-testing-unused-imports-generated-crates-trait-import-class-name-test-symbol-h7e78d3856f7562dfcfd3c5ebf1df8d403b1ef99cbbdc960b64e689499bbbf6b2", "ref-testing-unused-imports-generated-crates-trait-import-class-name-test-symbol-ha47702162c05a22c06c0676347a748cb80857be1127f1cac35ec718cfcd11649", "ref-testing-unused-imports-generated-crates-trait-import-class-name-test-symbol-haf7edde0b79ace73f9b150c48055de3ca3348b998530b48a7b91250a394d3404"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.unused-imports-generated-crates-trait-import-class-name's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.value-anchor-rot-field-addition-delivery-grows-existing"
title = "Current control for testing.value-anchor-rot-field-addition-delivery-grows-existing."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.value-anchor-rot-field-addition-delivery-grows-existing.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-value-anchor-rot-field-addition-delivery-grows-existing-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.value-anchor-rot-field-addition-delivery-grows-existing's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.wac-composed-component-transpiled-jco-loses-cross-instance"
title = "Current control for testing.wac-composed-component-transpiled-jco-loses-cross-instance."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.wac-composed-component-transpiled-jco-loses-cross-instance.
"""
control_kind = "gate"
control_state = "live"
reference_ids = ["ref-testing-wac-composed-component-transpiled-jco-loses-cross-instance-gate-hc8c9ea0a21b96b2f9e6b028ca524ff1535227e2dbb5c9ab52a8e7f9c3e30eec1", "ref-testing-wac-composed-component-transpiled-jco-loses-cross-instance-gate-hf2e5bf53c50997692623794ce66965b548397ac6f3cfba48d34d3000ed57b7b4"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.wac-composed-component-transpiled-jco-loses-cross-instance's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.guard.wrapper-name-user-set-name-synthesized-collection-wrapper"
title = "Current control for testing.wrapper-name-user-set-name-synthesized-collection-wrapper."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "control"
summary_md = """Current control for testing.wrapper-name-user-set-name-synthesized-collection-wrapper.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-wrapper-name-user-set-name-synthesized-collection-wrapper-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.wrapper-name-user-set-name-synthesized-collection-wrapper's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record]]
id = "testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
title = "A hand-enumerated ban-pattern list whose AUTHORITY lives in gitignored plan files lags the id scheme it bans — no in-repo lockstep is possible (the tracked side cannot read the authority in CI), so the list drifts one delivery-phase behind by construction."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A hand-enumerated ban-pattern list whose AUTHORITY lives in gitignored plan files lags the id scheme it bans — no in-repo lockstep is possible (the tracked side cannot read the authority in CI), so the list drifts one delivery-phase behind by construction.
"""
detail_md = """- **A hand-enumerated ban-pattern list whose AUTHORITY lives in gitignored plan files lags the
  id scheme it bans — no in-repo lockstep is possible (the tracked side cannot read the
  authority in CI), so the list drifts one delivery-phase behind by construction.** Proven
  instance (read-caught in orchestrator review; the tier stayed green): `lint_doc_citations`'s
  ephemeral-reference patterns were spelled for one phase's ids (`PROBE-B<n>`, `ruling R<n>`) —
  the next phase's spellings (`PROBE-C<n>`, the `C-R<n>` ruling form) passed the gate, and the
  tree stayed clean only because an implementing agent self-policed its own citations out in a
  follow-up commit. Fixed by construction where possible: the patterns are now phase-GENERIC
  (`PROBE-[A-Z][0-9]`, `[A-Z]-R[0-9]`, `[a-z][0-9]*-spec`), so letter-roll drift is dead.
  Residual class: a genuinely NEW id FORM introduced by a future plan document (a spelling no
  current regex family anticipates). Working rule meanwhile: authoring a new plan/spec document
  that mints an id scheme adds that scheme's spelling to `EPHEMERAL_PATTERNS` (with canary) in
  the same session, and delegation prompts keep repeating the self-contained-comments rule.
  Mechanical layer on the SECOND uncaught-form instance: a local-only canary harvest — when the
  gitignored plan directory EXISTS (developer checkouts; skipped cleanly in CI), scan its
  `*-spec.md` files for id-like tokens (the probe-index/ruling tables are structured enough to
  extract) and fail if any harvested id is matched by NO ephemeral pattern — lockstep with the
  authority exactly where the authority is present.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.hand-enumerated-ban-pattern-list-authority-lives-gitignored"]
transition_ids = ["testing.trigger.hand-enumerated-ban-pattern-list-authority-lives-gitignored"]

[[record.source_replacement]]
span_id = "span-record-hand-enumerated-ban-pattern-list-authority-lives-gitignored"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
title = "A hand-maintained source→dependency mapping in a manifest changeset can silently under-assert: the tool exports source referencing a crate that no changeset op guarantees."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A hand-maintained source→dependency mapping in a manifest changeset can silently under-assert: the tool exports source referencing a crate that no changeset op guarantees.
"""
detail_md = """- **A hand-maintained source→dependency mapping in a manifest changeset can silently under-assert:
  the tool exports source referencing a crate that no changeset op guarantees.** Proven instance
  (consumer-reported, not caught by any gate): the pre-crate-shaped `--export-static-dir` wrote the
  composed runtime source into a consumer's shared crate while owning no manifest at all, so a
  cbor_event major bump updated every generated crate's manifest EXCEPT the shared runtime's, which
  kept the old pin — caught only because the 2.x→3.x API break fails compilation loudly; a
  source-compatible-looking skew would have shipped silently. Fixed structurally by the
  crate-shaped `--export-static-crate` (the export merges the target's `Cargo.toml`;
  regression-pinned by `export_static_crate_writes_composed_runtime_and_manifest`'s hand-manifest
  leg), and the warm-up dep-universe gate sweeps `ops_for_static_runtime` alongside the other three
  op sets. The RESIDUAL: the mapping from composed runtime files to dep ops (`cbor_event`/`hex`
  always; `hashlink`/`serde`/`schemars` per flag) is hand-derived from grepping the static
  files, and nothing re-checks it — a `static/*.rs` runtime file gaining a reference to a new
  external crate (or to an existing one under a flag combination whose op condition doesn't match)
  would under-assert again, and only the consumer's build would notice. Working rule meanwhile: a
  change touching the static runtime files or `composed_runtime_static_files` re-greps the composed
  file set for foreign-crate path roots and reconciles `ops_for_static_runtime` in the same commit.
  Mechanical layer on a SECOND instance: a bidirectional drift gate over the maximal-flavor
  composed file set — every known dep name appearing as a path root in the composed text must be
  asserted by a static-runtime op, and every asserted dep must appear in the text — a natural
  sibling of `warmup_manifest_covers_registry_dep_universe`, accepting the source-scan heuristics
  then, not before.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.hand-maintained-source-dependency-mapping-manifest-changeset-silently"]
transition_ids = ["testing.trigger.hand-maintained-source-dependency-mapping-manifest-changeset-silently"]

[[record.source_replacement]]
span_id = "span-record-hand-maintained-source-dependency-mapping-manifest-changeset-silently"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one"
title = "Independent admitted incident one for testing.build-registration-class-reference-position-sweep-second-escape."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_incident"
summary_md = """Independent admitted incident one for testing.build-registration-class-reference-position-sweep-second-escape.
"""
incident_posture = "attributed"
signature_md = """A twice-aliased set-idiom binding escaped the hand coverage.
"""
evidence_ids = ["testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one-proof"]
attribution_md = """The preserved owner detail identifies this as one of two distinct instances of the admitted class; adjacency alone is not being counted as recurrence.
"""
operating_rule_reference_id = "ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-one-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"

[[record]]
id = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one-proof"
title = "Incident evidence for testing.build-registration-class-reference-position-sweep-second-escape."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.build-registration-class-reference-position-sweep-second-escape.
"""
evidence_kind = "incident"
claim_md = """A twice-aliased set-idiom binding escaped the hand coverage.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-one-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-one-proof-roadmap-h3de9048b09ce092e475d6691816189c52c2beadbd980bb250ed2977a5f213ef6"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]

[[record]]
id = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two"
title = "Independent admitted incident two for testing.build-registration-class-reference-position-sweep-second-escape."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_incident"
summary_md = """Independent admitted incident two for testing.build-registration-class-reference-position-sweep-second-escape.
"""
incident_posture = "attributed"
signature_md = """An alias-first recursive collection escaped the hand coverage.
"""
evidence_ids = ["testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two-proof"]
attribution_md = """The preserved owner detail identifies this as one of two distinct instances of the admitted class; adjacency alone is not being counted as recurrence.
"""
operating_rule_reference_id = "ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-two-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"

[[record]]
id = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two-proof"
title = "Incident evidence for testing.build-registration-class-reference-position-sweep-second-escape."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.build-registration-class-reference-position-sweep-second-escape.
"""
evidence_kind = "incident"
claim_md = """An alias-first recursive collection escaped the hand coverage.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-two-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-two-proof-roadmap-h3de9048b09ce092e475d6691816189c52c2beadbd980bb250ed2977a5f213ef6"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]

[[record]]
id = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one"
title = "Independent admitted incident one for testing.gate-demands-premise-probed-against-gate-evidence-about."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_incident"
summary_md = """Independent admitted incident one for testing.gate-demands-premise-probed-against-gate-evidence-about.
"""
incident_posture = "attributed"
signature_md = """The no-json-schema-export premise was probed against fast project_corpus but falsified by full-tier verify.
"""
evidence_ids = ["testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one-proof"]
attribution_md = """The preserved owner detail identifies this as one of two distinct instances of the admitted class; adjacency alone is not being counted as recurrence.
"""
operating_rule_reference_id = "ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-one-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"

[[record]]
id = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one-proof"
title = "Incident evidence for testing.gate-demands-premise-probed-against-gate-evidence-about."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.gate-demands-premise-probed-against-gate-evidence-about.
"""
evidence_kind = "incident"
claim_md = """The no-json-schema-export premise was probed against fast project_corpus but falsified by full-tier verify.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-one-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-one-proof-roadmap-haebfad78df120de969e13b0169c8754aad4ad4047945530b40fecd9afd40b9b4"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:standing-system"]
tiers = ["fast", "full", "local"]

[[record]]
id = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two"
title = "Independent admitted incident two for testing.gate-demands-premise-probed-against-gate-evidence-about."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_incident"
summary_md = """Independent admitted incident two for testing.gate-demands-premise-probed-against-gate-evidence-about.
"""
incident_posture = "attributed"
signature_md = """A fast-only delegation missed the local-tier fixture-registry test.
"""
evidence_ids = ["testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two-proof"]
attribution_md = """The preserved owner detail identifies this as one of two distinct instances of the admitted class; adjacency alone is not being counted as recurrence.
"""
operating_rule_reference_id = "ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-two-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"

[[record]]
id = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two-proof"
title = "Incident evidence for testing.gate-demands-premise-probed-against-gate-evidence-about."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.gate-demands-premise-probed-against-gate-evidence-about.
"""
evidence_kind = "incident"
claim_md = """A fast-only delegation missed the local-tier fixture-registry test.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-two-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-two-proof-roadmap-haebfad78df120de969e13b0169c8754aad4ad4047945530b40fecd9afd40b9b4"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:standing-system"]
tiers = ["fast", "full", "local"]

[[record]]
id = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one"
title = "Independent admitted incident one for testing.grammar-fuzzer.embed-site-alias-roots."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_incident"
summary_md = """Independent admitted incident one for testing.grammar-fuzzer.embed-site-alias-roots.
"""
incident_posture = "attributed"
signature_md = """The constrained-int cbor-payload alias was standalone-green and embedded-red.
"""
evidence_ids = ["testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one-proof"]
attribution_md = """The preserved owner detail identifies this as one of two distinct instances of the admitted class; adjacency alone is not being counted as recurrence.
"""
operating_rule_reference_id = "ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-one-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"

[[record]]
id = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one-proof"
title = "Incident evidence for testing.grammar-fuzzer.embed-site-alias-roots."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.grammar-fuzzer.embed-site-alias-roots.
"""
evidence_kind = "incident"
claim_md = """The constrained-int cbor-payload alias was standalone-green and embedded-red.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-one-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-one-proof-roadmap-h1838240ada66525dd1b6a047c12fd480acf9eb4d7ffecadafaa3fd7dc251d94a"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]
tiers = ["local"]

[[record]]
id = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two"
title = "Independent admitted incident two for testing.grammar-fuzzer.embed-site-alias-roots."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_incident"
summary_md = """Independent admitted incident two for testing.grammar-fuzzer.embed-site-alias-roots.
"""
incident_posture = "attributed"
signature_md = """The inline-table cbor alias owned serializer was standalone-green and embedded-red.
"""
evidence_ids = ["testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two-proof"]
attribution_md = """The preserved owner detail identifies this as one of two distinct instances of the admitted class; adjacency alone is not being counted as recurrence.
"""
operating_rule_reference_id = "ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-two-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"

[[record]]
id = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two-proof"
title = "Incident evidence for testing.grammar-fuzzer.embed-site-alias-roots."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.grammar-fuzzer.embed-site-alias-roots.
"""
evidence_kind = "incident"
claim_md = """The inline-table cbor alias owned serializer was standalone-green and embedded-red.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-two-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-two-proof-roadmap-h1838240ada66525dd1b6a047c12fd480acf9eb4d7ffecadafaa3fd7dc251d94a"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]
tiers = ["local"]

[[record]]
id = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one"
title = "Independent admitted incident one for testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_incident"
summary_md = """Independent admitted incident one for testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.
"""
incident_posture = "attributed"
signature_md = """Fixed float/nint choice arms and the anonymous nested choice minted unspellable numeric-derived identifiers.
"""
evidence_ids = ["testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one-proof"]
attribution_md = """The preserved owner detail identifies this as one of two distinct instances of the admitted class; adjacency alone is not being counted as recurrence.
"""
operating_rule_reference_id = "ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-one-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"

[[record]]
id = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one-proof"
title = "Incident evidence for testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.
"""
evidence_kind = "incident"
claim_md = """Fixed float/nint choice arms and the anonymous nested choice minted unspellable numeric-derived identifiers.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-one-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-one-proof-roadmap-he7e71a780510c173364f6ac7c53e176b29906556230f49338bec3ae2f32a3ba7"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]

[[record]]
id = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two"
title = "Independent admitted incident two for testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_incident"
summary_md = """Independent admitted incident two for testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.
"""
incident_posture = "attributed"
signature_md = """A fixed-text arm camel-cased self into the Rust keyword Self.
"""
evidence_ids = ["testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two-proof"]
attribution_md = """The preserved owner detail identifies this as one of two distinct instances of the admitted class; adjacency alone is not being counted as recurrence.
"""
operating_rule_reference_id = "ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-two-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"

[[record]]
id = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two-proof"
title = "Incident evidence for testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.
"""
evidence_kind = "incident"
claim_md = """A fixed-text arm camel-cased self into the Rust keyword Self.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-two-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-two-proof-roadmap-he7e71a780510c173364f6ac7c53e176b29906556230f49338bec3ae2f32a3ba7"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]

[[record]]
id = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one"
title = "Independent admitted incident one for testing.rejection-message-s-remedy-string-executable-claim-pin."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_incident"
summary_md = """Independent admitted incident one for testing.rejection-message-s-remedy-string-executable-claim-pin.
"""
incident_posture = "attributed"
signature_md = """The anonymous tagged-group remedy was dead for one triggering construct class.
"""
evidence_ids = ["testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one-proof"]
attribution_md = """The preserved owner detail identifies this as one of two distinct instances of the admitted class; adjacency alone is not being counted as recurrence.
"""
operating_rule_reference_id = "ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-one-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"

[[record]]
id = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one-proof"
title = "Incident evidence for testing.rejection-message-s-remedy-string-executable-claim-pin."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.rejection-message-s-remedy-string-executable-claim-pin.
"""
evidence_kind = "incident"
claim_md = """The anonymous tagged-group remedy was dead for one triggering construct class.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-one-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-one-proof-roadmap-hb77e87c983491a2898906b45e06411cb6c5670c21100469699b1ee7436661fa2"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]

[[record]]
id = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two"
title = "Independent admitted incident two for testing.rejection-message-s-remedy-string-executable-claim-pin."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "testing_incident"
summary_md = """Independent admitted incident two for testing.rejection-message-s-remedy-string-executable-claim-pin.
"""
incident_posture = "attributed"
signature_md = """The bare fixed-table-value refusal retained a stale named-rule remedy.
"""
evidence_ids = ["testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two-proof"]
attribution_md = """The preserved owner detail identifies this as one of two distinct instances of the admitted class; adjacency alone is not being counted as recurrence.
"""
operating_rule_reference_id = "ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-two-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"

[[record]]
id = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two-proof"
title = "Incident evidence for testing.rejection-message-s-remedy-string-executable-claim-pin."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.rejection-message-s-remedy-string-executable-claim-pin.
"""
evidence_kind = "incident"
claim_md = """The bare fixed-table-value refusal retained a stale named-rule remedy.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-two-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-two-proof-roadmap-hb77e87c983491a2898906b45e06411cb6c5670c21100469699b1ee7436661fa2"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]

[[record]]
id = "testing.json2ts-artifact-doc-comments-naming-undefined"
title = "json2ts artifact doc comments naming `undefined`."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """json2ts artifact doc comments naming `undefined`.
"""
detail_md = """- **json2ts artifact doc comments naming `undefined`.** The emitted `.d.ts` carries comments like
  \"This interface was referenced by `undefined`'s JSON-Schema definition via the `patternProperty`
  …\". The name in that comment is json2ts's own standalone name for the schema that OWNS the
  catch-all, and it is `undefined` exactly when that owner is an anonymous INLINE object — a nested
  object property carrying a rest region — because such a schema has no `title`, no `$id` and no
  `$defs` key for json2ts to name it from. Top-level definitions already read correctly
  (`OpenJSON`), and the declaration-name work did not change this: the fix is not managing titles,
  which those inline schemas do not have. What would fix it is giving each such inline object a
  name, and that is not cosmetic — json2ts hoists a named object schema into a top-level
  declaration of its own, so the published surface gains a declaration per nested rest region.
  Reopening signal: a consumer's emitted `json-types.d.ts` carries more of these `undefined`
  comments than named ones — i.e. nested rest regions dominate their surface, which is also the
  condition under which the hoisted-declaration cost is worth paying.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "misleading_docs"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.json2ts-artifact-doc-comments-naming-undefined"]

[[record.source_replacement]]
span_id = "span-record-json2ts-artifact-doc-comments-naming-undefined"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.lint-doc-citations-accepts-docs-heading-durable-citation"
title = "`lint_doc_citations` accepts \"a docs section by heading\" as a durable citation, but a section citation only proves the SECTION still exists — not that the claim cited from it does."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """`lint_doc_citations` accepts \"a docs section by heading\" as a durable citation, but a section citation only proves the SECTION still exists — not that the claim cited from it does.
"""
detail_md = """- **`lint_doc_citations` accepts \"a docs section by heading\" as a durable citation, but a section
  citation only proves the SECTION still exists — not that the claim cited from it does.** One
  instance, self-inflicted and caught in the same session that caused it. `current_capacities.mdx`
  cited a findings SECTION for the claim that `t = true / null / tstr` fails under
  `--preserve-encodings`; the delivery that fixed that defect deleted the ENTRY inside the section
  while the section itself survived, so the citation kept resolving, the lint stayed green, and a
  user-facing doc asserted a defect that no longer existed. The entry-title form the lint prefers
  would have dangled loudly on the same edit. Note this is NOT the positional-citation class the lint
  already bans (`item <N>`, which silently retargets): this one is an ACCEPTED durable form whose
  granularity is coarser than the fact it carries. **Owner meanwhile:** review, and the working rule
  that a citation should name the narrowest stable referent that dies with the claim — an entry
  title, a test/gate name, a pin — never the section enclosing it. **The layer, when built:** extend
  the lint so a citation into a doc that has entry-level headings must name one, rather than the
  enclosing section; a section citation stays legal only where the section IS the referent (a
  whole-mechanism reference). **Trigger:** a second instance, or any delivery that prunes entries
  from a section other docs cite into.

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.lint-doc-citations-accepts-docs-heading-durable-citation"]
transition_ids = ["testing.trigger.lint-doc-citations-accepts-docs-heading-durable-citation"]

[[record.source_replacement]]
span_id = "span-record-lint-doc-citations-accepts-docs-heading-durable-citation"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.lint-provocation-shapes-generated-code-clippy-clean-partially"
title = "Lint-provocation shapes for `generated_code_clippy_clean` (partially systematic at best)."
projection_group = "next-priority"
legacy_aliases = ["Next work 3"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Lint-provocation shapes for `generated_code_clippy_clean` (partially systematic at best).
"""
detail_md = """3. **Lint-provocation shapes for `generated_code_clippy_clean` (partially systematic at best).**
   The gate itself already exists and denies `clippy::all` over the generated rust and wasm crates
   on three cases (`generated_code_clippy_clean`, local tier; documented in `tests/README.md`) —
   yet lint classes still arrive consumer-reported when the gate's rich input is provocation-POOR
   for the shape that mints them. The gate's third case answers that for shapes the rich fixture
   cannot host at all: a minimal scratch-written spec under `--preserve-encodings
   --annotate-fields=false` covering verify-only fixed bool/null/undefined in member position and
   in all three arm positions, including the mixed-special brute-force dispatch. This is where a
   `clippy::no_effect` `();` and a `clippy::bool_comparison` `x != true` both shipped inside the
   deny set while the gate stayed green. The gate's rich input
   (`tests/canonical/input.cddl`) carries the
   identity-op provocations (`clippy_neg_bounded` — a record-field bounded `nint` whose deserialize
   RangeCheck exercises the no-`as i128`-cast path; `clippy_wrapped_map` — a `@newtype` over a map
   whose rust `From` impl must be `new(inner)`; `clippy_enum_record` — a record with a c-style enum
   field whose wasm ctor/getter cross by value), so a regression re-minting any of those
   `clippy::unnecessary_cast` / `clippy::useless_conversion` shapes is hard-red in both profiles.
   One still-open completeness item is an inventory/LOCKSTEP assertion between every unit-like
   `FixedValue` variant and this fixture's fixed-special cross-product. The current bool/null/
   undefined list is broad across positions but hand-maintained: the next fixed major-7 special
   could add a deserialize branch without joining the lint provocation, recreating B3-024B's blind
   spot. The other still-open work item is the THRESHOLD/shape-dependent default-warn lints the
   input still can't provoke: it has no `/` choice asymmetric enough for `large_enum_variant`'s
   200-byte default (a synthetic provocation needs a ~30-uint-field record variant), and the static
   `DeserializeError` sits just under `result_large_err`'s ~128-byte threshold on this input while
   exceeding it in CML's CI. Add a deliberately lint-provoking asymmetric choice so the NEXT
   default-warn lint of that class — e.g. one a toolchain bump introduces — goes red in-repo
   instead of in a consumer's CI; those two lints are already permanently silenced by the generated
   root's allow, so for them the shape merely pins that the allow keeps covering what it was added
   for. Known residual that keeps this \"partial\": threshold-adjacent lints fire spec- and
   layout-dependently, so the gate certifies \"no lint fires on shapes we provoke\", never \"no
   consumer CI will trip\"; the consumer-report channel stays load-bearing for that remainder, which
   is why those two lints are allowed at the generated root rather than chased per-spec.

"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "coverage_cell"
risk = "compile_failure"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.lint-provocation-shapes-generated-code-clippy-clean-partially's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""

[[record.source_replacement]]
span_id = "span-record-lint-provocation-shapes-generated-code-clippy-clean-partially"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.lockstep-drift-gate-rust-ts-kebab-ident-mirror"
title = "A lockstep drift gate for the rust↔TS kebab-ident mirror — today the pin is one-directional and the rust side can drift silently-soft."
projection_group = "next-priority"
legacy_aliases = ["Next work 24"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A lockstep drift gate for the rust↔TS kebab-ident mirror — today the pin is one-directional and the rust side can drift silently-soft.
"""
detail_md = """24. **A lockstep drift gate for the rust↔TS kebab-ident mirror — today the pin is one-directional
    and the rust side can drift silently-soft.** `cddl-matrix/verify.ts::toKebabCase` mirrors
    `src/utils.rs::convert_to_kebab_case` to name the WIT resource the component leg's mint check
    and host look for; the verify startup self-test pins the TS side against a fixture table
    copied from the rust test `convert_to_kebab_case_table`, and LOCKSTEP doc comments on the
    rust side name the TS table. But nothing EXECUTABLE ties the two tables together: a rust-side
    conversion change updates the rust table (its own test forces that) while the TS fixtures
    keep passing against the OLD rules, and the divergence surfaces only as a soft annotations
    diff (\"no minted component surface\" on selected rows) after a full verify run. The
    systematic layer: a drift check that reads BOTH tables and asserts the shared rows agree —
    cheapest as an extension of the existing verify self-test (parse the `convert_to_kebab_case_table`
    assertion rows out of `src/utils.rs` and require every TS fixture row to appear there with
    the same expectation), which puts it on `verify_selftest`'s local-tier path where the
    taxonomy pins already live. The general class is the one the LOCKSTEP convention exists for
    (moved code carries its comments; here the two halves live in different LANGUAGES so no
    comment survives a refactor mechanically), and the corpus-detect mirror's gate-enforced
    precedent is the model.

"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.lockstep-drift-gate-rust-ts-kebab-ident-mirror's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""

[[record.source_replacement]]
span_id = "span-record-lockstep-drift-gate-rust-ts-kebab-ident-mirror"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.logic-tool-emits-source-text-outside-layer-reasons"
title = "Logic the tool EMITS as source text is outside every layer that reasons about the tool's own code, so its only oracle is a hand vector chosen to DISTINGUISH a wrong implementation — and a vector that merely EXERCISES the logic certifies nothing."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Logic the tool EMITS as source text is outside every layer that reasons about the tool's own code, so its only oracle is a hand vector chosen to DISTINGUISH a wrong implementation — and a vector that merely EXERCISES the logic certifies nothing.
"""
detail_md = """- **Logic the tool EMITS as source text is outside every layer that reasons about the tool's own
  code, so its only oracle is a hand vector chosen to DISTINGUISH a wrong implementation — and a
  vector that merely EXERCISES the logic certifies nothing.** Proven instance (orchestrator review,
  not any gate): the emitted schema-document closure check carries a `decode_schema_ref_name` whose
  comment justifies unescaping `~1` before `~0` — the order matters only for a name holding a
  LITERAL `~1`, which encodes to `~01` — while its green-path vector was a schema name
  (`Odd<K>/~name`) that decodes correctly under BOTH orders. So the comment's whole justification
  was unwitnessed: the three escape classes were covered, the ordering they were cited to protect
  was not. Confirmed by simulating `schemars`' `encode_ref_name` against both orders, then closed by
  making the name carry a literal `~1` (`Odd<K>/~1name` → `Odd%3CK%3E~1~01name`, which the wrong
  order decodes to `Odd<K>//name`), which costs no new nested-cargo cell. Why no standing layer sees
  the class: the permanently-declined full `cargo-mutants` sweep (§ Declined) would not cover it
  anyway — that sweep is scoped to the tool's own functions, while logic living inside an emitted
  string constant is not a function it can mutate — so mutation scoring structurally cannot measure
  emitted-runtime behaviour, and the emitted surface is growing (the three name-injectivity panics,
  the closure walk, the decode). Working rule meanwhile: a hand vector for emitted logic states
  which WRONG implementation it distinguishes, and a comment that justifies an ordering or
  precedence choice names the vector that would fail without it. Mechanical layer on a SECOND
  instance: mutation testing for the emitted runtime — perturb each
  emitted-source constant textually (swap adjacent `.replace` calls, invert a comparison, drop a
  branch), regenerate one fixture per mutant, and require some gate to go red; expensive enough
  (a nested cargo run per mutant) that it earns its cost only once the class has recurred.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
control_ids = ["testing.guard.logic-tool-emits-source-text-outside-layer-reasons"]
transition_ids = ["testing.trigger.logic-tool-emits-source-text-outside-layer-reasons"]

[[record.source_replacement]]
span_id = "span-record-logic-tool-emits-source-text-outside-layer-reasons"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.machine-readable-ownership-co-owned-features-std-list"
title = "A machine-readable ownership record for the co-owned `features.std` list."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A machine-readable ownership record for the co-owned `features.std` list.
"""
detail_md = """- **A machine-readable ownership record for the co-owned `features.std` list.** The human half of
  the co-ownership contract is asserted in-band — the `# cddl-codegen:` comment block above the key
  (`STD_OWNERSHIP_COMMENT`, `cargo_manifest.rs`), re-written every run so it cannot rot — but a
  TOOL wanting to partition the list's entries by provenance (tool-computed vs consumer-added) has
  only line-prefix parsing to go on. The structural carrier already exists: a list under the
  manifest's `[package.metadata.cddl-codegen]` table (the `generated-with` stamp's home) would
  state the tool's current entry set with no new public surface and stay freely revocable. Not
  built because it has zero consumers today, and duplicating the list without one buys only bytes.
  Reopening signal (consumer-side, the party who'd measure it): a consumer-side tool or lint that
  needs the tool/hand partition of `std` entries and says so — a feature request naming the tooling
  it would unblock.

"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.machine-readable-ownership-co-owned-features-std-list"]

[[record.source_replacement]]
span_id = "span-record-machine-readable-ownership-co-owned-features-std-list"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.maintainer-ruling-force-convenience-cbor-bytes-door-turns"
title = "A maintainer ruling to force: the convenience `to_cbor_bytes()` door turns `float16`'s loud serialize error into a panic."
projection_group = "next-priority"
legacy_aliases = ["Next work 11"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """A maintainer ruling to force: the convenience `to_cbor_bytes()` door turns `float16`'s loud serialize error into a panic.
"""
detail_md = """11. **A maintainer ruling to force: the convenience `to_cbor_bytes()` door turns `float16`'s loud
    serialize error into a panic.** A `float16` member's carrier is `f32`, and a carrier value that
    is not f16-exact cannot be written at the one head the type declares — so `Serialize` returns
    `Err` (`InvalidLenPassed`, the declared-width refusal working as designed: rounding to fit
    would be a silent value mutation), and `docs/docs/current_capacities.mdx` documents both the
    error and the door. The tension is the door itself: `ToCBORBytes::to_cbor_bytes()` UNWRAPS,
    on the pre-existing design premise that generated serialization is total — a premise
    declared-width floats are the first type class to break. A library panic on a user-supplied
    value is loud, in-policy, and documented; whether it should instead be a `Result`-returning
    door (or a validating construction path for the f16 carrier) is an API-shape decision with
    breaking-change surface across every generated crate, so it is the maintainer's to take, not
    an implementer's — already flagged to the maintainer at delivery (T1-01, cycle 2). The
    fixture pin for the current contract is `float_heads_inexact_float16_fails_serialize_loudly`
    (`tests/core/tests.rs`). Reopening signal: the maintainer takes the ruling, or a consumer
    reports the panic from production data (measurable by the party holding the inexact value).

"""
decision_state = "pending"
question_md = """May the convenience CBOR-bytes door panic, or must it return a fallible result?
"""
transition_ids = ["testing.predicate.convenience-cbor-bytes-ruling"]

[[record.source_replacement]]
span_id = "span-record-maintainer-ruling-force-convenience-cbor-bytes-door-turns"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.make-fixture-comment-s-claimed-wire-bytes-executable"
title = "Make a fixture comment's claimed WIRE BYTES executable, or stop writing them."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Make a fixture comment's claimed WIRE BYTES executable, or stop writing them.
"""
detail_md = """- **Make a fixture comment's claimed WIRE BYTES executable, or stop writing them.** A robustness
  fixture's header comment may state the bytes its rule produces (`tests/robustness/`
  `cbor_payload_same_chain_alias.cddl` claimed `43 41 05` for the value 5; the crate writes
  `42 41 05`, off by one byte-string head length). Nothing could have caught it: the robustness
  catalog asserts an OUTCOME per fixture (`ok` / `error (graceful)`) and never compiles or runs the
  crate, so a byte claim in that tree is prose no gate reads — and prose that looks like evidence is
  worse than none, because the next reader derives from it instead of from the RFC. The error stood
  for as long as the file did and was found only by executing the claim by hand during an unrelated
  delivery. Two shapes would close it: teach the catalog an optional per-fixture `expect_bytes`
  (constructor args + hex), which turns the claim into the thing the gate reads; or forbid byte
  claims in `tests/robustness/*.cddl` outright and require them to cite the executed test that owns
  them (`tests/core/tests.rs`'s hand-derived oracles), which is what the corrected comment now does.
  The second is nearly free and catches the class by construction; the first is worth more only if
  robustness fixtures start carrying wire facts no other tree covers.
  **Reopening signal:** a second robustness fixture comment is found stating bytes that differ from
  what its rule emits — i.e. the citation convention was written down and did not hold.

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.make-fixture-comment-s-claimed-wire-bytes-executable"]
transition_ids = ["testing.trigger.make-fixture-comment-s-claimed-wire-bytes-executable"]

[[record.source_replacement]]
span_id = "span-record-make-fixture-comment-s-claimed-wire-bytes-executable"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.making-type-choice-path-reject-duplicated-explicit-name"
title = "Making the TYPE-CHOICE path reject a duplicated explicit `@name` the way the group-choice arm path does."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """Making the TYPE-CHOICE path reject a duplicated explicit `@name` the way the group-choice arm path does.
"""
detail_md = """- **Making the TYPE-CHOICE path reject a duplicated explicit `@name` the way the group-choice arm
  path does.** The two enum-producing paths now differ in policy: two group-choice arms of one rule
  given the same `; @name` are a graceful rejection (`reject_group_choice_arm_variant_name_collision`
  — an authored name is public API of the generated crate, so renaming one would ship a name nobody
  wrote), while a type choice in the same situation (`foo = 0 ; @name mainnet / 1 ; @name mainnet`)
  silently emits `Mainnet` and `Mainnet2`. The divergence is deliberate and rests on what the two
  produce, not on principle: the arm path was emitting a REPEATED variant — a generated crate that
  does not compile (`E0428`) — so it had to change and the only question was which way, whereas the
  type-choice path emits code that compiles and works today. Tightening it is therefore a pure
  behavior change on working output, and it would break any spec whose author has (knowingly or not)
  built against the `Mainnet2` spelling — a cost paid by existing consumers to buy consistency no
  consumer has asked for. Note the asymmetry does NOT extend to derived names: those carry no
  authorial intent and take a numeric suffix on BOTH paths (`foo = [ x: uint // x: text ]` gives
  `Foo::X` / `Foo::X2`), which is the behavior `append_number_if_duplicate` exists for. Reopening
  signal: a consumer reporting that a variant they EXPLICITLY `@name`d was emitted under a different
  name — the harm this would prevent, observable by the party who has it, and distinct from the
  derived-name suffixing that is working as designed.
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.making-type-choice-path-reject-duplicated-explicit-name.
"""
authority_reference_id = "ref-testing-making-type-choice-path-reject-duplicated-explicit-name-authority"
permanence = "reopenable"
transition_ids = ["testing.trigger.making-type-choice-path-reject-duplicated-explicit-name"]

[[record.source_replacement]]
span_id = "span-record-making-type-choice-path-reject-duplicated-explicit-name"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.matrix-s-note-names-panic-site-aborts-nothing"
title = "A matrix cell's `note` names the panic site it aborts at, and nothing re-derives that name from a run either."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A matrix cell's `note` names the panic site it aborts at, and nothing re-derives that name from a run either.
"""
detail_md = """- **A matrix cell's `note` names the panic site it aborts at, and nothing re-derives that name from
  a run either.** Recorded as a second INSTANCE of the class above, in a different artifact, so a
  cross-artifact evaluation has both in one place. The `note` fields of the `…type2.map` containment
  cells (`cddl-matrix/containment/{array-element,cbor-payload,choice-member,generic-arg,map-value,
  occurrence-target}.toml`, mirrored verbatim into `matrix.json`) attribute the abort to
  `parsing.rs 'Anonymous groups not allowed'`. Every one of them actually reached the
  `\"TODO: non-table types as types\"` site — the same wrong-BRACKET confusion as the ledger instance,
  authored independently in a different file and a different language. The named site is now gone
  entirely (those cells reject gracefully) and the notes were rewritten with that re-grounding;
  what survives the rewrite is the gap, which is that a hand-authored site
  name inside an annotation is checked by nothing. Whether the two instances are ONE detectable class
  is the open question and the reason this is recorded rather than built: the ledger's detector keys
  on a Rust test's `catch_unwind` capture, while a cell note is TOML consumed by TypeScript, so a
  single detector spanning both is a claim to establish, not to assume.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.matrix-s-note-names-panic-site-aborts-nothing"]
transition_ids = ["testing.trigger.matrix-s-note-names-panic-site-aborts-nothing"]

[[record.source_replacement]]
span_id = "span-record-matrix-s-note-names-panic-site-aborts-nothing"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.mechanical-layers-two-review-owned-design-rules-tests"
title = "Mechanical layers for the two review-owned design rules in `tests/README.md` § \"Design rules\" (invariant-softening, vacuity-floor witness) — build only if a class recurs."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Mechanical layers for the two review-owned design rules in `tests/README.md` § \"Design rules\" (invariant-softening, vacuity-floor witness) — build only if a class recurs.
"""
detail_md = """- **Mechanical layers for the two review-owned design rules in `tests/README.md` § \"Design
  rules\" (invariant-softening, vacuity-floor witness) — build only if a class recurs.** The
  vacuity-floor detector is a scoped mutation sweep over the harness's emission helpers — a
  surviving mutant in an emission arm IS the vacuity made visible — but each such mutant needs
  the full-tier gate to kill it, so it stays manual-only and unbuilt until a second instance
  justifies the cost. The invariant-softening class has no mechanical detector (a mutation sweep
  cannot distinguish \"kept loud\" from \"absorbed\"); a recurrence there is the signal to design
  one, not evidence that review can be skipped. The vacuity-floor class also has a first
  on-record instance in a NEEDLE gate (a flavor the harness-scoped mutation sweep would not
  cover): during the collapsible_if burn-down, the retargeted
  `corpus_special_map_key_supported` needle initially counted its indefinite gates by the proxy
  pattern `if matches!(` — any unrelated future matches-gate would pad the count and mask an
  ungated Special peek — and review fixed it to witness the gate's own
  `, cbor_event::Len::Indefinite)` pattern (mutation testing cannot see this flavor either: the
  mask needs an unrelated emission to APPEAR, which no mutant simulates). A second proxy-witness
  needle instance is the signal to design a detector for that flavor too, same policy. A SECOND
  needle-vacuity instance is on record in a distinct STALE-LITERAL flavor:
  `corpus_occurrence_bounds_enforced`'s negative needle pinned the exact emitted spelling of the
  element-value RangeCheck cast, so the i128 widening of `RangeCheck` (which renamed every
  emission from `as isize` to `as i128`) would have left it matching nothing — green forever
  while witnessing nothing — had the widening session's exhaustive grep of the old spelling not
  caught it (the needle was retargeted to `found: x as i128` in the same change). Reading, not a
  gate: a vacuous negative needle passes by construction, and the harness-scoped mutation sweep
  is blind to this flavor too (no mutant renames emitted text). The mechanical layer if THIS
  flavor recurs: pair every negative needle over emitted text with a positive LIVENESS witness
  asserting the same literal still occurs where it is legitimate (e.g. a constrained-int
  serialization snapshot DOES contain `found: x as i128` beside the occurrence cell's does-not),
  so an emission rename flips the liveness half red instead of leaving the negative half
  vacuous. Meanwhile the working rule when renaming emitted text: grep the test tree for needles
  pinning the old spelling in the same change.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.mechanical-layers-two-review-owned-design-rules-tests"]
transition_ids = ["testing.trigger.mechanical-layers-two-review-owned-design-rules-tests"]

[[record.source_replacement]]
span_id = "span-record-mechanical-layers-two-review-owned-design-rules-tests"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.message-s-level-classification-review-owned-two-ways"
title = "A message's LEVEL classification is review-owned, and two ways of getting it wrong are invisible."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A message's LEVEL classification is review-owned, and two ways of getting it wrong are invisible.
"""
detail_md = """- **A message's LEVEL classification is review-owned, and two ways of getting it wrong are
  invisible.** The verbosity delivery classified 63 emission sites across six macros
  (`src/log.rs`), and nothing mechanical checks that any of them landed at the right level. Two
  distinct blind spots, one proven and one structural. **Proven once:** `err!` shipped with zero
  call sites and no tool could say so — `#[macro_export]` exempts a macro from `unused_macros`, so
  a level nobody emits at is indistinguishable from one that is merely rare. Caught by
  orchestrator review of the Phase 1 diff; fixed by converting `main.rs`'s terminal error path,
  which is where it belonged anyway. **Structural:** a warning-class message parked at `info` or
  above is silently invisible at the default level, and the only mechanical watcher covers a
  fraction of the surface — `cddl-matrix/no_silent_directive.ts` needs the `@`-directive notices to
  stay default-visible, but every other diagnostic (the `Recursive type:` notice, `export()`'s four
  warnings, the ~22 `--emit-tests` skip notices) has no such consumer. What keeps this a residual
  rather than a work item is that the one watcher fails SAFE: its FAIL condition is
  byte-identical-AND-unmentioned, so hiding a notice makes it fail loudly rather than pass
  vacuously — a suppressed acknowledgement cannot make that gate go quiet. Working rule meanwhile: a
  new emission site states its level and stream in the same review as its message, and the
  measurement to make is per-level line counts on a fixture that triggers the class (the delivery's
  own before/after used `tests/core/input.cddl`, which emits ZERO diagnostics — so it could not have
  shown a mis-streamed warning, and a second fixture with a recursive type and a `#6.258` set was
  needed to see the stderr half at all). The mechanical layer, when justified: a source-scanning
  test asserting no `info!`/`debug!`/`trace!` format string opens with `warning:`, plus a
  call-site-count floor per macro. Reopening signal, on the dimension the cost actually grows: a
  SECOND consumer depending on a specific message being default-visible (there is one today, and its
  fail-safe direction is what makes the count the right instrument rather than any measure of how
  many sites exist).

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.message-s-level-classification-review-owned-two-ways"]
transition_ids = ["testing.trigger.message-s-level-classification-review-owned-two-ways"]

[[record.source_replacement]]
span_id = "span-record-message-s-level-classification-review-owned-two-ways"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.migration-handoff-s-complete-list-found-survey-negative"
title = "A migration handoff's \"complete list found by survey\" is a NEGATIVE premise, and the first consumer falsified one twice."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_operational_watch"
summary_md = """A migration handoff's \"complete list found by survey\" is a NEGATIVE premise, and the first consumer falsified one twice.
"""
detail_md = """- **A migration handoff's \"complete list found by survey\" is a NEGATIVE premise, and the first
  consumer falsified one twice.** The no_std handoff's hand-action survey ended \"Nothing else,\"
  and CML's executed migration found two more required classes (public struct fields typed
  `LinkedHashMap<…>`; `Entry::or_default()` call sites) plus a three-manifests-wider stale-dep
  sweep. Both misses are instances of standing AGENTS.md rules, recorded here because they
  happened in a SURVEY DOC rather than in code: the field-annotation half was surveyed with a
  grep truncated through `| head -8` and presented as complete (the evidence-lost-to-tail class —
  the pipe ate the `witness_builder.rs` hits), and `or_default` was simply not in the grep
  vocabulary, so absence-of-hits read as absence-of-thing (the vocabulary-bounded-negative
  class — a survey's completeness is established by enumerating the CATEGORY registry, not by
  the searches one thought of). The systematic fix is committed where the next consumer will
  meet it: the four-category migration-search recipe in `docs/docs/output_format.mdx`
  § \"Upgrading a crate generated before the `no_std` output\" (including resolve-`.entry(`-by-type,
  since the inherent method shadows `Deref` and no text search settles the receiver). Watch, not
  work item: the next handoff doc's survey section should cite that recipe as its method — a
  survey that instead presents its own grep list as complete is this entry recurring.

"""
watch_state = "attributed"
signature_md = """A handoff presents a hand-written grep list as a complete negative survey instead of deriving categories through the migration recipe.
"""
attribution_md = """The no_std handoff omitted public LinkedHashMap fields and Entry::or_default() sites.
"""
operating_rule_reference_id = "ref-testing-migration-handoff-s-complete-list-found-survey-negative-operating"
response_md = """Re-run all four recipe categories and correct the handoff.
"""
escalation_transition_id = "testing.trigger.migration-handoff-s-complete-list-found-survey-negative"
retirement_semantics_md = """Retire only when handoffs cannot assert completeness without naming the durable category authority.
"""

[[record.payload.capture_step]]
step_id = "signature"
capture_md = """Capture the handoff claim, full untruncated searches, omitted category and consumer finding, recipe version, and affected files.
"""

[[record.payload.capture_step]]
step_id = "scope"
capture_md = """Record the full untruncated log or consumer report, command, full commit, date, environment, relevant versions, and the explicitly unprobed remainder; never cite a disposable draft log as durable evidence.
"""

[[record.source_replacement]]
span_id = "span-record-migration-handoff-s-complete-list-found-survey-negative"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"
title = "A minted-identifier spellability floor: every identifier the generator emits verbatim must satisfy `is_spellable_variant_name`'s two string properties, asserted at the emission side rather than per-minter."
projection_group = "next-priority"
legacy_aliases = ["Next work 14"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A minted-identifier spellability floor: every identifier the generator emits verbatim must satisfy `is_spellable_variant_name`'s two string properties, asserted at the emission side rather than per-minter.
"""
detail_md = """14. **A minted-identifier spellability floor: every identifier the generator emits verbatim must
    satisfy `is_spellable_variant_name`'s two string properties, asserted at the emission side
    rather than per-minter.** The class this closes: a name-minting path whose output is not a
    spellable Rust identifier ships the bad token to rustfmt, which dies with an error naming its
    own confusion instead of the construct — loud, zero files, and misattributed. Three instances
    on 2026-08-06 alone, each found by hand probing rather than by any gate: fixed float/nint
    choice arms minted `F1.5`/`U-1` (variant idents), the anonymous nested choice minted the RULE
    ident `F1.5OrText` from the same lexemes, and a fixed-text arm camel-cased `\"self\"` into the
    keyword `Self` — identifier-shaped, still unspellable. The delivered repair guards the two
    arm-naming SEAMS (`reject_unnameable_arm_variant_name`, predicate on the minted string), which
    covers every minter those seams consume — but other emit-verbatim name families (rule idents
    from `combined_name`, field names, wasm wrapper names) have no floor, and absence of a fourth
    instance is currently established by nothing. What to build: a check over the finalized IR's
    emitted-name surface (idents, variant names, field names) that fails naming the MINT SITE when
    a name is unspellable, so the next bad minter is a diagnosed refusal rather than a rustfmt
    autopsy. Enumerate the name families from the IR types (`RustIdent`, `VariantIdent`, field
    name storage) rather than grepping for minters — a negative claim (\"no other minter emits bad
    names\") needs the registry, not a keyword search. That enumeration is shared surface: the
    residuals \"Nothing asserts that two IR sites cannot mint the SAME `RustIdent`\" (uniqueness)
    and the synthesized-name referenced-but-never-minted (E0425) flavor want predicates over the
    same emitted-name registry — whichever is built first, the others become added predicates
    rather than fresh enumerations. **Activated by B3-024A (2026-08-09):** its synthesized
    fixed-singleton owner is the third minting site and initially deduplicated `true` with
    `#6.7(true)` by `FixedValue` alone, making parse order choose the wrong wire codec. The local
    owner check now keys the complete `RustType` wire shape and rejects authored collisions in both
    source orders. **The finalized-IR registry proposed above would NOT by itself have caught this
    semantic collision:** the faulty dedup discarded the second claim before finalization, leaving
    one unique, spellable `RustIdent`; every reference compiled, but one use got the wrong wire.
    Add a pre-registration mint-claim predicate to the shared registry: every minter records
    `(RustIdent, canonical structural/wire identity, mint site)` before lookup/dedup/insert;
    byte-identical repeat claims are allowed, while two identities claiming one ident fail naming
    both sites. Its acceptance vectors must reverse claimant order and assert emitted wire bytes,
    not merely a duplicate-name diagnostic. Keep the finalized-IR spellability/uniqueness checks as
    separate predicates: they catch bad surviving state, while the claim ledger catches a lossy
    registration seam that erased the evidence.

"""
work_state = "ready"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""
admission_ids = ["testing.admission-path.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"]

[[record.source_replacement]]
span_id = "span-record-minted-identifier-spellability-floor-identifier-generator-emits-verbatim"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.msrv-declaration-os-matrix-generated-code"
title = "MSRV declaration / OS matrix for GENERATED code."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """MSRV declaration / OS matrix for GENERATED code.
"""
detail_md = """- **MSRV declaration / OS matrix for GENERATED code.** The templates' `edition = \"2024\"` already
  hard-floors the effective MSRV at rustc 1.85 with a self-explanatory compile error, and generated
  output has no platform-conditional code an OS matrix would exercise. Revisit only if a consumer
  reports an MSRV or platform break (dep-driven MSRV creep is the one real vector).
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.msrv-declaration-os-matrix-generated-code.
"""
authority_reference_id = "ref-testing-msrv-declaration-os-matrix-generated-code-authority"
permanence = "reopenable"
transition_ids = ["testing.trigger.msrv-declaration-os-matrix-generated-code"]

[[record.source_replacement]]
span_id = "span-record-msrv-declaration-os-matrix-generated-code"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.multifile-reference-position-coverage-two-position-keyed-escapes"
title = "Multifile reference-POSITION coverage: two position-keyed escapes down, one enumerated position still missing."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Multifile reference-POSITION coverage: two position-keyed escapes down, one enumerated position still missing.
"""
detail_md = """- **Multifile reference-POSITION coverage: two position-keyed escapes down, one enumerated position
  still missing.** Two cross-module import bugs were invisible to `tests/matrix_multifile` because
  its field-embedding modes (`named`/`anon`/`anonb`/`unref`) all reference the shape as a `bholder`
  record FIELD, while each bug lived in a different reference *position* with its own
  import-collection path: the group-ctor class (a GROUP choice over a foreign-scope Record expands
  that record's field types into `new_<variant>` ctor params, unmarked by `scope_references`;
  fixed via `EnumVariant::group_ctor_record_fields`, vector held by the hand fixture
  `tests/multifile` `relay`/`relay_host`, test `cross_module_group_choice_ctor`), and the
  type-alias-target class (a plain alias rule's `pub type` line names its cross-scope target,
  unwalked — E0412, hit in production; fixed by the `scope_references` type-alias walk). The second
  escape triggered this entry's mechanical layer: the position axis now exists as the `aliased`
  mode (`bal = <ty>`, every INCLUDED shape). A third production escape then hit the same `aliased`
  position from the SHAPE side — inside the matrix's extern/rawbytes EXCLUSION, not a missing row:
  a generic-EXTERN instance named by a non-root rule aliases a `Base<Args>` TYPE-EXPRESSION ident
  (`RustIdent::new_generic_with_base`), which the new walk fed to `set_ref` verbatim — a `<…>` in
  the scope's `use` list, rustfmt abort, consumer regen dead (feature request 07, the day after
  the walk landed). Fixed by decomposing in the walk (base imported at the base extern's declaring
  scope via the single-owner `GenericInstance::extern_base_ident`, each argument walked); pinned
  by the hand fixture `tests/extern-generic-scoped` (compile+round-trip proof
  `extern_generic_scoped`; content pin `extern_generic_scoped_alias_imports`, incl. the
  no-`<`-in-`use`-lines invariant). The exclusion itself stays — the matrix's compile floor cannot
  build extern shapes standalone — so extern-shaped placement coverage is hand-fixture-owned.
  Mechanical layer if the excluded region bites again (recur-first): the compile floor forces the
  exclusion, but a GENERATION-ONLY leg (the in-process `api::generated_strings` probe the content
  pin already uses) has no such constraint — an excluded-shapes × reference-modes generation sweep
  needs no hand defs. What remains future-facing is the `gcvariant` mode
  (`bholder = [<ty> // 1, uint]`) over a curated shape subset (the Record-resolving shapes
  `struct`/`mstruct`/`ralias` are the discriminating cells), same participation-pin idiom as
  `anonForm`/`EXPECTED_ANON_SHAPES` — the group-ctor class still rests on the single hand vector
  rather than a matrix row. Its own signal, since the two positions this entry already delivered
  have met the parent's escape-driven one: **the count of Record-resolving shapes reachable through
  a group-choice ctor that the single hand vector does not cover**, which is what the next author
  touching `EnumVariant::group_ctor_record_fields` reads off the shape list — one vector standing in
  for three discriminating cells is tolerable, and stops being so the moment a fourth shape resolves
  through that path.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.multifile-reference-position-coverage-two-position-keyed-escapes"]
transition_ids = ["testing.trigger.multifile-reference-position-coverage-two-position-keyed-escapes"]

[[record.source_replacement]]
span_id = "span-record-multifile-reference-position-coverage-two-position-keyed-escapes"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.nested-cargo-scratch-retention-tmp-cddl-leaks-until"
title = "Nested-cargo scratch retention: `/tmp/cddl*` leaks until the disk fills."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_operational_watch"
summary_md = """Nested-cargo scratch retention: `/tmp/cddl*` leaks until the disk fills.
"""
detail_md = """- **Nested-cargo scratch retention: `/tmp/cddl*` leaks until the disk fills.** The nested-cargo
  gates and the in-process test suites mint per-run scratch dirs under the system temp dir
  (`cddl_codegen_test_*`, `cddl_codegen_corpus_compile_*`, `cddl_verify_*` — the verify wasm
  target dirs are ~2 GB each) and rely on end-of-run cleanup that a killed or crashed run never
  reaches, so debris accumulates silently across sessions. Observed 2026-07-19: 3316 leaked
  `/tmp/cddl*` entries totaling 43 GB filled the root filesystem to 0 bytes free, which then
  cascaded — one session's full-tier run died mid-gate (the closure-audit strace could not write,
  and the death left a half-updated `cddl-matrix/annotations` overlay that failed the NEXT run's
  `build_matrix_check`), and harness task-output plumbing broke for every concurrent session.
  Manual remediation (delete only entries older than a day, sparing paths named by live
  processes' cmdlines — the pattern-kill warning in AGENTS.md applies to deletion too) recovered
  35 GB. Scratch is self-retiring since: every `check.ts` run sweeps registry-named, day-old,
  live-process-unguarded entries before its disk preflight (`tests/README.md` § \"Run-start scratch
  sweep\"), and the sweep's first run on the development box removed 8284 entries / 42 GiB — the same
  saturation, re-formed. What the sweep cannot reach bounds what is left to watch: debris younger
  than the 24 h threshold (a single day of crashed full-tier runs can leak tens of GiB inside the
  window), and scratch minted by a `cargo test` invocation that never goes through `check.ts`. So
  still treat unexplained ENOSPC/mid-gate deaths as possible scratch saturation and check before
  re-attributing — but read the run's own sweep line first, because a run that swept and still
  refused on the disk floor is reporting a different problem.
"""
watch_state = "watching"
signature_md = """Disk-floor refusal or ENOSPC gate death with the same run reporting zero scratch entries removed.
"""
response_md = """Protect live paths, reclaim only explicit old unreferenced scratch, and rerun the whole tier.
"""
escalation_transition_id = "testing.scratch-retention.reopening-signal"
retirement_semantics_md = """Retire only after that root is delivered and durably proven self-retiring; age sweep alone is insufficient.
"""

[[record.payload.capture_step]]
step_id = "signature"
capture_md = """Capture the same-run sweep, preflight, failure, disk usage, and live-path exclusions.
"""

[[record.payload.capture_step]]
step_id = "scope"
capture_md = """Record the full untruncated log or consumer report, command, full commit, date, environment, relevant versions, and the explicitly unprobed remainder; never cite a disposable draft log as durable evidence.
"""

[[record.source_replacement]]
span_id = "span-record-nested-cargo-scratch-retention-tmp-cddl-leaks-until"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.nothing-asserts-how-many-times-rejection-message-emitted"
title = "Nothing asserts how MANY times a rejection message is emitted, and nested composites emit theirs twice."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Nothing asserts how MANY times a rejection message is emitted, and nested composites emit theirs twice.
"""
detail_md = """- **Nothing asserts how MANY times a rejection message is emitted, and nested composites emit
  theirs twice.** `a = [x: [* 5]]` prints its refusal twice because the parse walk visits a nested
  composite twice; `a = [x: { uint => tstr }]` duplicates the same way, so the class predates the
  guards that surfaced it. Behaviour is correct and the exit code is right — only the reading is
  degraded, which is why the robustness/reject catalogs (they record an outcome LABEL, never the
  message) cannot see it. Meanwhile the working rule is that a rejection's text is reviewed by
  reading it once, at authoring time. The mechanical layer is a multiplicity assertion over the
  catalogs' captured stderr, paired with a `contains` de-dup in `record_rejection`. Trigger, on the
  axis the cost grows along — the repetition FACTOR, measurable by whoever next authors a fixture:
  a catalog fixture whose refusal repeats a message THREE or more times (a deeper nest multiplies
  it again), at which point a user cannot tell one refusal from several.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.nothing-asserts-how-many-times-rejection-message-emitted"]
transition_ids = ["testing.trigger.nothing-asserts-how-many-times-rejection-message-emitted"]

[[record.source_replacement]]
span_id = "span-record-nothing-asserts-how-many-times-rejection-message-emitted"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.nothing-asserts-two-ir-sites-cannot-mint-same"
title = "Nothing asserts that two IR sites cannot mint the SAME `RustIdent`: `register_rust_struct` ends in a bare map insert, so a second claimant silently overwrites the first."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Nothing asserts that two IR sites cannot mint the SAME `RustIdent`: `register_rust_struct` ends in a bare map insert, so a second claimant silently overwrites the first.
"""
detail_md = """- **Nothing asserts that two IR sites cannot mint the SAME `RustIdent`: `register_rust_struct` ends
  in a bare map insert, so a second claimant silently overwrites the first.** Proven instance
  (reported by a consumer as a panic, root-caused here): a multi-arm group-choice arm registers its
  record under the arm's own name while parsing, so an arm named for an existing rule took that
  rule's slot — then, if the arm was embeddable, `remove_rust_struct` DELETED the rule outright and
  generation panicked at the first lookup of it. The louder half was not the panic: for a
  non-embeddable arm the overwrite STOOD, so the rule vanished from the emitted crate, references to
  it retargeted onto the arm's record, and two arms sharing a name emitted one struct carrying the
  other's fields AND the other's fixed tag — a wrong-wire-shape crate that compiled clean with no
  diagnostic. Which of the four outcomes fired was decided purely by topological rule order, so an
  unrelated reference edge added elsewhere in the spec flipped a working spec to a broken one.
  Standing coverage now: `arm_ident_collision` (order-independent — rule idents are all scope-marked
  before the parse loop, and two arms reject symmetrically), the structural-equivalence door that
  keeps benign same-shape name reuse generating, and the
  `group_choice_arm_ident_collision_rejects_gracefully` /
  `identical_group_choice_arms_in_different_rules_share_one_struct` /
  `embeddable_group_choice_arm_may_share_a_rule_name` trio. That detector is arm-specific by
  construction: the OTHER synthesizing sites (collection wrappers, generic instances, the
  `prelude_*` rules) mint into the same namespace and are guarded, if at all, by their own bespoke
  per-kind detectors. Working rule meanwhile: a change that mints a `RustIdent` from anything other
  than a rule name states what it collides with and how that is detected. The mechanical layer — a
  debug-time uniqueness assertion inside `register_rust_struct`, overwrite = panic against an
  allowlist of the legitimate re-registration seams (`set_rep_if_plain_group` and the `finalize`
  resolution passes rewrite in place by design) — should be armed as a DETECTOR first and its
  allowlist sized from that arming run, since \"how many overwrites are legitimate today\" is exactly
  the premise nobody has measured. Reopening signal, on the dimension the cost actually grows: a
  THIRD minting site needing its own bespoke collision detector (two are on record — this one and
  the wasm wrapper-name family), or any consumer reporting a declared rule absent from, or
  wrong-shaped in, its generated crate.

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "wrong_public_api"
family_classification = "none_reviewed"
control_ids = ["testing.guard.nothing-asserts-two-ir-sites-cannot-mint-same"]
transition_ids = ["testing.trigger.nothing-asserts-two-ir-sites-cannot-mint-same"]

[[record.source_replacement]]
span_id = "span-record-nothing-asserts-two-ir-sites-cannot-mint-same"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.observed-baseline-comments-beside-gate-floors-rot-silently"
title = "Observed-baseline comments beside gate floors rot silently — TWO instances, two homes, two distinct rot modes; the floors/consts stay the enforced artifact."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Observed-baseline comments beside gate floors rot silently — TWO instances, two homes, two distinct rot modes; the floors/consts stay the enforced artifact.
"""
detail_md = """- **Observed-baseline comments beside gate floors rot silently — TWO instances, two homes, two
  distinct rot modes; the floors/consts stay the enforced artifact.** These comments are
  informational and review-maintained by design (replacing them with exact gate-asserted counts
  buys churn on every ingredient addition), so no gate can see them go stale. First instance: a
  layer-2 recombination gate's observed-baseline comment shipped stale. Second instance, a
  different rot mode (a tally with two incompatible READINGS): `corpus_decode_replay`'s floors
  comment carried a skip-ledger tally (\"6 rows on `JSON_SURFACE_SKIP`\") that was accurate as a
  pinning-time observation but reads as a live count — two later ledger changes each treated it one
  way or the other (an addition left it alone as history; a retirement hand-bumped it as a live
  tally), landing a number that matched neither reading. Both caught by review, not a gate.
  Working rule: floor comments state pinning-time observations in explicitly past-tense framing
  (\"AT THAT confirm run …\"), never present-tense tallies — the consts themselves are the live
  ledger. Mechanical layer, on a stale baseline comment actually misleading triage: replace the
  comments with exact pinned counts asserted by the gate, accepting the churn.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.observed-baseline-comments-beside-gate-floors-rot-silently"]
transition_ids = ["testing.trigger.observed-baseline-comments-beside-gate-floors-rot-silently"]

[[record.source_replacement]]
span_id = "span-record-observed-baseline-comments-beside-gate-floors-rot-silently"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.opt-rfc-faithful-total-cbor-json-rendering-json"
title = "Opt-in RFC 8949 §6.1-faithful total CBOR→JSON rendering (a `to_json_rfc8949`-style lossy flavor)."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Opt-in RFC 8949 §6.1-faithful total CBOR→JSON rendering (a `to_json_rfc8949`-style lossy flavor).
"""
detail_md = """- **Opt-in RFC 8949 §6.1-faithful total CBOR→JSON rendering (a `to_json_rfc8949`-style lossy
  flavor).** §6.1 is self-described \"non-normative advice\", and its non-injective rows are lossy
  by its own admission: byte strings become base64url JSON strings indistinguishable from text,
  tag numbers are ignored, non-finite floats and unknown simples become \"a single substitute
  value, such as a JSON null\", and its integer-key stringification carries an acknowledged
  \"danger of key collision\". The crate's `any` JSON surfaces refuse silent substitution instead —
  the tagged `AnyCbor` codec (serde's externally-tagged enum form,
  `docs/docs/output_format.mdx` § \"JSON representation\") is total but structural, and the
  natural-JSON surface (the open-struct-maps workstream) implements §6.1's injective subset and
  **errors** on the substitute-value rows — because generated JSON feeds a symmetric `from_json`
  and typed consumers, where a silent substitute is write-back corruption (`NaN` → `null` →
  a different value re-encoded; bytes → string → re-read as text), not display fuzz. A
  never-fails *natural* view is still legitimately useful for display/diagnostic consumers; it
  must simply be opt-in by name so totality-by-substitution is a visible choice. Mechanical
  layer when a consumer asks: the same recursive walk with §6.1's leaf policy, a never-fails
  property over the `AnyCbor` fuzz corpus, and vectors pinning each substitution row. Reopening
  signal: a real consumer needs a total natural-JSON view and neither the tagged codec nor the
  fallible natural `to_json` fits.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.opt-rfc-faithful-total-cbor-json-rendering-json"]

[[record.source_replacement]]
span_id = "span-record-opt-rfc-faithful-total-cbor-json-rendering-json"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.orphan-fixture-directory-meta-test"
title = "An orphan-fixture-directory meta-test"
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """An orphan-fixture-directory meta-test
"""
detail_md = """- **An orphan-fixture-directory meta-test** (assert every `tests/<dir>/` is referenced by some gate):
  fixture dirs change rarely and a new gate's author touches the dir listing anyway; the failure
  mode (a committed fixture nothing runs) is caught by review at that rate.
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.orphan-fixture-directory-meta-test.
"""
authority_reference_id = "ref-testing-orphan-fixture-directory-meta-test-authority"
permanence = "permanent"

[[record.source_replacement]]
span_id = "span-record-orphan-fixture-directory-meta-test"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.ownership-journal-making-dropped-std-forward-dep-converge"
title = "An ownership journal making a dropped `--std-forward-dep` converge (safe retraction)."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """An ownership journal making a dropped `--std-forward-dep` converge (safe retraction).
"""
detail_md = """- **An ownership journal making a dropped `--std-forward-dep` converge (safe retraction).**
  Dropping the flag while keeping its `--rust-dep` leaves a stale-but-consistent pair today — the
  dep keeps `default-features = false`, the `<pkg>/std` forward union-survives beside it — safe,
  documented on the flag, fixable by hand. Making the tool retract it requires journaling
  provenance in the manifest (which run wrote the forward AND the `default-features` field, since
  the tool cannot otherwise tell its own defaults-off from the user's), a qualitatively new use of
  the bounded existing-manifest read in which a PRIOR run's flag set changes THIS run's asserted
  content. Declined: a new invariant surface to buy convergence of a documented harmless
  staleness. Reopening signal: a consumer report in which the surviving pair is a defect for them
  — they dropped the flag to STOP forwarding and the persistence harms their build — rather than
  the documented safe leftover.
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.ownership-journal-making-dropped-std-forward-dep-converge.
"""
authority_reference_id = "ref-testing-ownership-journal-making-dropped-std-forward-dep-converge-authority"
permanence = "reopenable"
transition_ids = ["testing.trigger.ownership-journal-making-dropped-std-forward-dep-converge"]

[[record.source_replacement]]
span_id = "span-record-ownership-journal-making-dropped-std-forward-dep-converge"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.panic-site-un-shadowing-converting-shallow-panic-graceful"
title = "Panic-site un-shadowing: converting a shallow panic to a graceful rejection exposes deeper panic sites for inputs that previously died early — and the outcome catalogs, which record CATEGORY only by design, cannot see a PANIC→PANIC site shift."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Panic-site un-shadowing: converting a shallow panic to a graceful rejection exposes deeper panic sites for inputs that previously died early — and the outcome catalogs, which record CATEGORY only by design, cannot see a PANIC→PANIC site shift.
"""
detail_md = """- **Panic-site un-shadowing: converting a shallow panic to a graceful rejection exposes deeper
  panic sites for inputs that previously died early — and the outcome catalogs, which record
  CATEGORY only by design, cannot see a PANIC→PANIC site shift.** Proven instance: converting
  `parse_type`'s fall-through panic made `ctl.bits` (which died there, on its `&(...)` rule)
  travel past the recorded rejection into the control-operator catch-all — still PANIC, so its
  `matrix_panic` catalog row never flipped, and the shift was visible only because the
  conversion's fixture-flip prediction was diffed against a pre-conversion PER-SITE baseline
  probe. Standing coverage: the recombination sweep's ledger keys are message+site-scoped, so a
  SWEPT composition moving to an unledgered deeper class fails loudly as a new finding; and the
  cycle-4 working rule — before converting a panic site, baseline-probe every fixture the site
  owns (panic site + message, not category), and re-probe after — caught this instance. The
  trigger for a mechanical layer (a site-fingerprint sidecar per `matrix_panic` fixture, diffed
  by the catalog test alongside the category) is a SECOND instance where an un-shadowed deeper
  site goes unnoticed past its converting commit; building it now would trade the catalogs'
  deliberate refactor-stability for a fingerprint that churns on every panic-message edit.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "abort_or_panic"
family_classification = "none_reviewed"
control_ids = ["testing.guard.panic-site-un-shadowing-converting-shallow-panic-graceful"]
transition_ids = ["testing.trigger.panic-site-un-shadowing-converting-shallow-panic-graceful"]

[[record.source_replacement]]
span_id = "span-record-panic-site-un-shadowing-converting-shallow-panic-graceful"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.parallel-constructor-fixture-diversity-parser-over-external-input"
title = "Parallel-constructor fixture diversity: a parser over external input must span the ident-class matrix of REAL specs, not the feature spec's mental model."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Parallel-constructor fixture diversity: a parser over external input must span the ident-class matrix of REAL specs, not the feature spec's mental model.
"""
detail_md = """- **Parallel-constructor fixture diversity: a parser over external input must span the ident-class
  matrix of REAL specs, not the feature spec's mental model.** Proven instance: the
  `--wrapper-requests` shape parser hand-built bare `Rust(ident)` element leaves instead of
  resolving through the pipeline's alias rule, and every fixture used plain struct elements — so
  the first REAL spec fed through it (conway, where alias domain naming like
  `policy_id = script_hash` is the pervasive idiom) panicked `is_enum`'s registered-struct
  invariant on 5 of the motivating workspace's 18 committed requests. Two layers closed it: the
  MECHANICAL one is `IntermediateTypes::resolve_alias` — the single owner of the alias-substitution
  rule, called by both `new_type` and the requests parser, so a leaf built outside the pipeline
  cannot drift from pipeline resolution again (plus `workspace_requests_alias_elements_host`
  pinning the named-alias / primitive-alias / extern element classes). The residual this entry
  records is the FIXTURE principle no gate enforces: any NEW parser that reconstructs IR values
  from external content (sidecars, index files, future request formats) must ship fixtures
  spanning every ident class the IR distinguishes — registered struct, rule alias, primitive
  alias, extern, generic instance — because a parallel constructor's coverage is independent of
  the pipeline's, and synthetic fixtures inherit the feature author's blind spots. A second
  instance (a new external-input parser shipping struct-only fixtures) is the trigger to make
  this mechanical, e.g. a checklist gate over `parse_*` functions that take external strings.
  An independent first-principles review subsequently ran the full ident-class × shape matrix over
  the requests path (confirming the fix and surfacing four hardening findings, all closed with
  pinned hard errors: the stub-fidelity diagnosis for directly-exposable shapes, reserved element
  idents, the nesting depth cap, and the element-resolution mismatch appendix). The same review
  cycle also caught the PREDICATE flavor of this class mid-implementation: the reserved-ident
  pre-check first shipped as a hand-mirrored copy of `RustIdent::new`'s two assert predicates, and
  review replaced it with `RustIdent::reserved_reason` — the reservation rule's single owner that
  `new` itself asserts through. Working rule reinforced: when a fix needs the same decision an
  existing function already makes, extract the decision, never mirror it.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "abort_or_panic"
family_classification = "none_reviewed"
control_ids = ["testing.guard.parallel-constructor-fixture-diversity-parser-over-external-input"]
transition_ids = ["testing.trigger.parallel-constructor-fixture-diversity-parser-over-external-input"]

[[record.source_replacement]]
span_id = "span-record-parallel-constructor-fixture-diversity-parser-over-external-input"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.pin-toolchain-verify-ts-s-nested-cargo-scratch"
title = "Pin the toolchain of verify.ts's nested cargo — its scratch-cwd spawns resolve the rustup DEFAULT toolchain, and the cache key claims otherwise."
projection_group = "next-priority"
legacy_aliases = ["Next work 25"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Pin the toolchain of verify.ts's nested cargo — its scratch-cwd spawns resolve the rustup DEFAULT toolchain, and the cache key claims otherwise.
"""
detail_md = """25. **Pin the toolchain of verify.ts's nested cargo — its scratch-cwd spawns resolve the rustup
    DEFAULT toolchain, and the cache key claims otherwise.** Measured 2026-08-08, both halves:
    a `cargo` child of the repo's `cargo test` resolves the PIN even with a scratch cwd (the
    rustup proxy exports `RUSTUP_TOOLCHAIN` into the process tree — probed with a crate whose
    test spawns `cargo -V` from `/tmp`), so every rust-side nested-cargo gate is pinned; but
    verify.ts is reached via bun from a plain shell env, so every `Bun.spawnSync` cargo whose
    cwd is a scratch tree — the main pipeline's `cargo test` legs, the component execution
    leg's builds, the build sweep — resolves the machine's default toolchain (stable 1.97.1
    today, vs the 1.96.1 pin). Two consequences, one latent and one structural: verify's
    verdicts are measured on a toolchain no file in the repo names (link-class verdicts are
    toolchain-knife-edge — the wasip2 LLD crash matrix is the proof), and
    `lib.ts::gateCacheKey` folds `rustc -vV` measured with the CALLING process's cwd (in-repo →
    the pin), so cached cells record a compiler identity their builds did not use — a
    default-toolchain bump changes build behavior without changing the key, which is the
    stale-PASS shape the cache audit exists to prevent. The fix is one funnel wide: parse
    `rust-toolchain.toml`'s channel once, set `RUSTUP_TOOLCHAIN` in the spawn env of verify's
    cargo/rustc invocations (and make the key's `rustc -vV` measurement use the same env), with
    a startup self-test asserting a scratch-cwd spawn resolves the pin — red-first by asserting
    it BEFORE threading the env. Until it lands, a verify/sweep verdict is evidence about the
    machine's default toolchain, and any claim naming the pin must come from the rust-side
    gates or an explicit `rustup run` (the AGENTS unspelled-default rule's toolchain bullet).

"""
work_state = "ready"
work_intent = "repair"
work_kind = "defect"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.pin-toolchain-verify-ts-s-nested-cargo-scratch's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""
regression_evidence_ids = ["testing.proof.pin-toolchain-verify-ts-s-nested-cargo-scratch"]

[[record.source_replacement]]
span_id = "span-record-pin-toolchain-verify-ts-s-nested-cargo-scratch"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"
title = "Positional-diversity fold family for the preserve-fixture corpus — the authoring work that gives the rustfmt-cycle sweep discovery power."
projection_group = "next-priority"
legacy_aliases = ["Next work 5"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Positional-diversity fold family for the preserve-fixture corpus — the authoring work that gives the rustfmt-cycle sweep discovery power.
"""
detail_md = """5. **Positional-diversity fold family for the preserve-fixture corpus — the authoring work that
   gives the rustfmt-cycle sweep discovery power.**
   `preserve_fixtures_rustfmt_cycle_stability` holds the post-rustfmt on-disk fixed point over
   every fixture, but only over fold positions the corpus expresses — its own delivery record is
   explicit that it would not have caught its motivating escape, because no pre-escape fixture
   held a marker at a foldable tail. The gap is NOT in the unfold pre-pass:
   `unfold_trailing_markers` cuts on any non-own-line `cddl-codegen:`-tagged comment regardless
   of the construct it trails, so the pre-pass generalizes over positions by construction. What
   has no standing witness per position is the WHOLE chain — rustfmt folds the marker, the
   pre-pass unfolds it, the merge re-applies the block, and the on-disk form reaches its fixed
   point — where indentation, marker geometry, and block-body reconstruction genuinely differ by
   construct. The family: fixtures placing tagged blocks at each grammar-level trailing position
   rustfmt can fold a comment onto — last match arm is in-corpus (the `*_rustfmt_folded_tail_*`
   family); last statement of a block, if/else chain tails, struct-literal tail, last enum
   variant, and the closing brace of a nested module/impl are not — across the three block
   flavors. Authored as ordinary preserve fixtures, the sweep covers each automatically with zero
   harness change (that compounding is the shipped design). Honest bound to keep with the entry:
   this enumerates trailing-position folds only; rustfmt comment mechanisms that are not
   trail-folds (the proven import-reorder marker-glue class recorded in the standing-system
   residuals below) are a different family, and no finite fixture set anticipates a future
   formatter's novel comment re-owning — the sweep's version-bump tripwire is the instrument for
   those.

"""
work_state = "ready"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""
admission_ids = ["testing.admission-path.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"]

[[record.source_replacement]]
span_id = "span-record-positional-diversity-fold-preserve-fixture-corpus-authoring-gives"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.predicate.convenience-cbor-bytes-ruling"
title = "Transition for testing.maintainer-ruling-force-convenience-cbor-bytes-door-turns."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Transition for testing.maintainer-ruling-force-convenience-cbor-bytes-door-turns.
"""
transition_kind = "unblock_predicate"
owner_reference_id = "ref-testing-predicate-convenience-cbor-bytes-ruling-owner"
event_md = """A maintainer records whether the convenience CBOR-bytes door is panic-permitted or fallible.
"""
check_procedure_md = """Require a durable ruling that names the public API and compatibility consequence; code behavior alone is not authority.
"""
due_action_md = """Create only the implementation and compatibility work selected by the ruling, then convert the pending decision to its durable decided state.
"""
evaluation = "unknown"

[[record]]
id = "testing.predicate.prettyplease-formatting-ruling"
title = "Transition for testing.prettyplease-shelling-rustfmt."
projection_group = "pending-maintainer"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Transition for testing.prettyplease-shelling-rustfmt.
"""
transition_kind = "unblock_predicate"
owner_reference_id = "ref-testing-predicate-prettyplease-formatting-ruling-owner"
event_md = """A maintainer records one of the two formatting rulings and explicitly disposes of rustfmt#5703, both over-width controls, overlay token stability, and snapshot migration.
"""
check_procedure_md = """Read the durable ruling and verify every named constraint has an explicit disposition; an implementation experiment or source read is not a ruling.
"""
due_action_md = """If prettyplease is chosen, create separately accepted migration work; if rustfmt is retained, convert this question to a decided boundary and keep the current controls.
"""
evaluation = "unknown"

[[record]]
id = "testing.predicate.reviewed-fork-publication"
title = "Transition for reviewed RuleTrailing fork publication."
projection_group = "pending-maintainer"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Transition for reviewed RuleTrailing fork publication.
"""
transition_kind = "unblock_predicate"
owner_reference_id = "ref-testing-predicate-reviewed-fork-publication-owner"
event_md = """Commit a7ed0784e89689784ff78ed0e85c7434a3528937 is publicly fetchable, pinned, and adopted.
"""
check_procedure_md = """Verify public fetchability, exact dependency pin adoption, reviewed commit identity, and all current refusal controls.
"""
due_action_md = """Make the external record ready for parent re-evaluation without independently unblocking the parent.
"""
evaluation = "unknown"

[[record]]
id = "testing.predicate.rule-trailing-conjunctive-unblock"
title = "Transition for testing.rule-trailing.directive-classification."
projection_group = "pending-maintainer"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Transition for testing.rule-trailing.directive-classification.
"""
transition_kind = "unblock_predicate"
owner_reference_id = "ref-testing-predicate-rule-trailing-conjunctive-unblock-owner"
event_md = """A maintainer explicitly reverses the 2026-08-04 no-fork-push ruling and commit a7ed0784e89689784ff78ed0e85c7434a3528937 is publicly fetchable, pinned, and adopted.
"""
check_procedure_md = """Verify fresh durable evidence that the maintainer explicitly reversed the 2026-08-04 no-fork-push ruling, the reviewed commit is publicly fetchable and exactly pinned, and all current refusal controls remain live.
"""
due_action_md = """Classify all rule-only directives in one campaign, retain the four positive forms, reject the thirteen inapplicable forms, and remove the no-silent allowlist only in that delivery.
"""
evaluation = "unknown"

[[record]]
id = "testing.predicate.tier-memory-durable-measurements"
title = "Unblock durable tier-memory measurement spending."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Unblock durable tier-memory measurement spending.
"""
transition_kind = "unblock_predicate"
owner_reference_id = "ref-testing-predicate-tier-memory-durable-measurements-owner"
event_md = """A committed, environment-scoped per-slot peak authority exists outside the disposable sampler ledgers and contains enough reviewed real-tier observations to bound a slot.
"""
check_procedure_md = """Verify the authority is committed, records environment and tier scope, excludes draft/memory-peaks.jsonl as evidence, and supports re-deriving both the 4 GiB slot constant and one-permit nested bound.
"""
due_action_md = """Re-derive both constants, record the basis beside them, update the durable authority and regressions, and retain conservative bounds where the evidence is incomplete.
"""
evaluation = "unknown"

[[record]]
id = "testing.prettyplease-shelling-rustfmt"
title = "`prettyplease` instead of shelling to `rustfmt`."
projection_group = "pending-maintainer"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """`prettyplease` instead of shelling to `rustfmt`.
"""
detail_md = """- **`prettyplease` instead of shelling to `rustfmt`.** Removes toolchain-dependent formatting
  churn and the `which` dependency, compiles fast, never bails (it reuses `syn`, already built
  transitively via the proc-macro derives). No longer just a churn mitigation: shelling to rustfmt
  is a proven correctness exposure — rust-lang/rustfmt#5703 (an internal error on over-width
  `pub(crate)` tuple fields, fatal under `export.rs`'s deliberate non-0/3-exit-is-fatal contract)
  aborted consumer regens outright and forced a `#[rustfmt::skip]` + hand-canonical-layout
  workaround into shipped output — now at TWO emission sites (the wasm wrappers and the rust-crate
  default-profile newtype tuple field), sharing one guard owner
  (`push_overwidth_guarded_tuple_field`, `src/generation/mod.rs`; pinned both directions per site by
  `integration_overwidth_wasm_wrapper_field_gets_rustfmt_skip` /
  `integration_overwidth_rust_newtype_field_gets_rustfmt_skip`, and held across the whole hazard
  range by `integration_tuple_field_width_ladder_never_aborts_rustfmt`, whose doc comment records
  the measured 6-char fatal WINDOW — rendered type lengths 86..=91 abort while both 85 and 92
  format clean — that makes a dense ladder, not a huge-name fixture, the only meaningful gate
  shape). A SECOND proven instance of the
  same exposure class: rustfmt's comment-ownership misattribution (the open upstream family of
  rust-lang/rustfmt#6347 / #4654 / #3527) folds an own-line comment after a match's last arm onto
  that arm's `}` as a trailing comment — which rewrote the preserve overlay's own emitted
  `// cddl-codegen:` markers into a form the overlay's next-run scan could not read, breaking
  run-twice=run-once until the overlay grew an unfold pre-pass (pinned by
  `preserve_markers_survive_rustfmt_fold_roundtrip` and the
  `replace_rustfmt_folded_tail_arm_markers` fixture family;
  probed edition-independent and idempotent-on-folded-output, so the folded form is a
  genuine fixed point under current stable). `prettyplease` has no
  internal-error/bail class and does not re-own comments, so adopting it retires both workarounds
  (grep `rustfmt::skip` in
  `src/generation/` — the over-width branch now lives once, in
  `push_overwidth_guarded_tuple_field` — and remove it in the same change; the unfold pre-pass must
  STAY for files already committed in the folded form) — weigh that retirement
  plus the exposure against the emitted-token-stability constraints in AGENTS.md (the snapshot
  corpus and comment-preservation overlay key on the formatter's exact output, so the swap
  re-blesses broadly and must hold the overlay's idempotent-fixed-point property). The swap's
  overlay risk now has a mechanical acceptance suite waiting for it:
  `preserve_fixtures_rustfmt_cycle_stability` sweeps every expected-case preserve fixture through
  `rustfmt_generated_string` and asserts the post-format on-disk fixed point, so pointing that
  seam at prettyplease turns the corpus into the swap's per-structure acceptance run on day one
  (while the width ladder above largely dissolves under the swap — prettyplease has no
  internal-error class, so the exit-contract tripwire it leans on stops being where formatter
  surprises surface).

"""
decision_state = "pending"
question_md = """Should generated Rust formatting adopt prettyplease's stable, non-failing tradeoff, or retain rustfmt's token-stable output contract given rustfmt#5703, both over-width controls, the preservation overlay, and snapshot compatibility?
"""
transition_ids = ["testing.predicate.prettyplease-formatting-ruling"]

[[record.source_replacement]]
span_id = "span-record-prettyplease-shelling-rustfmt"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.probe-component-face-s-js-surface-axes-js"
title = "Probe the component face's JS surface on the axes the JS-host gate left untouched."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Probe the component face's JS surface on the axes the JS-host gate left untouched.
"""
detail_md = """- **Probe the component face's JS surface on the axes the JS-host gate left untouched.** The
  `component_jco` gate drives one jco version (1.26.1) and one node version (22) over the default
  encoding posture, with no JSON doors, in node only — which is what its two reused fixtures carry.
  Four axes are therefore documented as unprobed in `docs/docs/component_differences.mdx`, cited by
  \"Not probed on this face\", each with the probe that would settle it: `--json-serde-derives`'
  `to-json`/`from-json` doors (generate one of the existing fixtures with the flag and drive both
  doors from node); the browser build of `@bytecodealliance/preview2-shim` (the same drivers under a
  headless browser); `--preserve-encodings`' `to-canonical-cbor-bytes` (the surface fixture at that
  posture, asserting byte-exactness across the JS boundary); and other jco/node versions (a second
  pinned lockfile, or a second node in the gate's provisioning preflight). Each is a
  fixture-and-lockfile change rather than new machinery, so what is deferred is coverage breadth and
  the cold-run cost that buys it — none of the four sits on the path the motivating consumer takes.
"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
transition_ids = ["testing.component-js-probe.reopening-signal"]

[[record.source_replacement]]
span_id = "span-record-probe-component-face-s-js-surface-axes-js"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.process-boundary.another-profile-escape"
title = "*another profile, with no flip, no abort and no fix widening a reachable set — the shape always generated."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """*another profile, with no flip, no abort and no fix widening a reachable set — the shape always generated.
"""
detail_md = """  - ***another profile, with no flip, no abort and no fix widening a reachable set — the shape always
    generated.*** A fixture certifies only the PROFILES it is generated under, so a shape added to a
    default-profile fixture is unexamined everywhere else. Proven instance (since fixed and given
    exactly this entry's remedy — promoted into `tests/corpus/cbor_enum_payload.cddl`, all-profile
    by construction, with the preserve round-trip carried by `tests/preserve-encodings/input.cddl`):
    `bytes .cbor <c-style enum>` compiled and round-tripped under the default profile —
    `cbor_payload_leaves` in `tests/core/tests.rs` pins exactly that — while emitting a crate rustc
    rejected under `--preserve-encodings`, because the inlined try-each-variant sequence reused the
    enclosing `final_exprs` as its `Ok(..)` match pattern while the `.cbor` arm had pushed a value
    expression (`StringEncoding::from(<var>_bytes_encoding)`) into them. Established pre-existing by
    regenerating the same probe spec with the production files stashed, so nothing about it was a
    consequence of the change that put a reader in front of it. Two properties made it invisible
    rather than merely unpinned. First, it reached **rustc** (a call in pattern position is
    syntactically a valid tuple-struct pattern, so rustfmt formats the file happily and generation
    exits 0 — the defect was a resolution failure, not a parse failure). Second, `tests/core` is
    default-profile on both of its integration gates and in the snapshot registry, so no committed
    vehicle generated the shape under `--preserve-encodings` at all — which is exactly what
    promoting a shape into `tests/corpus/` fixes, and did. Trigger: **the count of shapes pinned
    green by a default-profile fixture that fail under another profile.** A consumer measures it
    without anything from us — they flip a flag on a spec they already have and their build breaks
    on a shape our docs list as supported. The count returned to zero when the instance above was
    fixed; the next instance means default-profile fixtures are systematically over-claiming, and
    the input extension stops being optional.

"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.process-boundary-another-profile-escape"]

[[record.source_replacement]]
span_id = "span-part-part-another-profile-flip-abort-fix-widening-reachable-set"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.process-boundary.rustc-after-panic-fix"
title = "*rustc, after a PANIC→`ok` flip: a panic fix lands on the FIRST site that dispatches on a shape, not the last — the sites behind it ask the same question and each answers it in its own code."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """*rustc, after a PANIC→`ok` flip: a panic fix lands on the FIRST site that dispatches on a shape, not the last — the sites behind it ask the same question and each answers it in its own code.
"""
detail_md = """  - ***rustc, after a PANIC→`ok` flip: a panic fix lands on the FIRST site that dispatches on a
    shape, not the last — the sites behind it ask the same question and each answers it in its own
    code.*** Proven instance: the `cbor_types` `_ => panic!()` over a nominal reference to a
    collection typedef (`RustStructType::Table`/`Array`, which emit a rust typedef and no impls).
    Fixing the panic arm let the shape GENERATE and produced a crate rustc rejected, because
    `generate_serialize`/`generate_deserialize` emitted `.serialize()`/`::deserialize()` on a
    `BTreeMap`, and — under `--preserve-encodings` ONLY — `encoding_fields_impl` minted no encoding
    sidecar for a member whose serialize read one. Three further sites, one of them profile-gated,
    all reachable from the single input the panic fix unblocked. The standing rule that covers this
    today is IN FORCE and current state in `tests/README.md` (§ \"Running everything\", the
    catalogs-are-generate-only paragraph): `cargo check` the emitted crate under EVERY profile when a
    fix turns an abort into generated code, plus a per-shape integration fixture —
    `tests/recursive-collection-ref/` is that fixture, run both profiles by
    `recursive_collection_ref` / `recursive_collection_ref_preserve`. Trigger for the mechanical
    layer (a promotion path from a flipped PANIC row into `tests/corpus/`, or a compile leg on
    robustness `ok` rows): a SECOND instance where a robustness row flips PANIC→`ok` and the emitted
    crate does not compile. Building it now would put a per-profile nested cargo build behind a
    catalog whose whole value is running in seconds on every `cargo test`.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.process-boundary-rustc-after-panic-fix"]
transition_ids = ["testing.trigger.process-boundary-rustc-after-panic-fix"]

[[record.source_replacement]]
span_id = "span-part-part-rustc-after-panic-ok-flip-panic-fix-lands"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.process-boundary.rustfmt-pre-rustc"
title = "*rustfmt, so the code never reaches rustc at all."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """*rustfmt, so the code never reaches rustc at all.
"""
detail_md = """  - ***rustfmt, so the code never reaches rustc at all.*** Not a duplicate of the class above: there
    the emitted crate reached rustc and was rejected, so any compile leg would catch it; here
    generation itself fails at the rustfmt merge step, so no compile leg can even receive a crate.
    One premise correction this entry now carries, probed rather than reasoned (an earlier version
    asserted the opposite on the strength of a dangling doc reference): the rustfmt pass runs
    IN-PROCESS too — `generated_strings` is post-rustfmt — so any vehicle that generates the shape
    under the failing profile witnesses this class as a generation error; the residual blindness
    is the same two-part bound as the siblings' (a vehicle must exist under the failing profile at
    all, and an `ok`-flip after a fix certifies only generation). Proven instance (since
    fixed on the type-match dispatch sites and given exactly this entry's remedy — promoted into
    `tests/corpus/group_choice_fixed_special.cddl`, whose cells shell the real CLI so the rustfmt
    seam is witnessed for the shape): a bool or null fixed value in a map-representation
    group-choice arm emitted Rust that rustfmt rejects (`expected pattern, found '='`) under
    `--preserve-encodings`. It reproduced against the parent of the fixed-value-arm support commit,
    so it predated that change; making the default profile generate the uint spelling is what put a
    reader in front of it. The class is not empty: the brute-force-dispatch sibling
    (`t = true / null / tstr`) still fails at the same seam and is ledgered in
    `cddl-matrix/ROADMAP.md` § findings — a robustness row for it would record an error today, but
    an `ok`-flip after a fix would again certify only generation. Reopening signal, on the
    magnitude axis the deferred cost grows along: **the count of `ok`-recording robustness rows
    whose emitted crate fails at export** — measurable by a consumer whose generation aborts at
    rustfmt while our catalog records the shape as `ok`, a party that already has the problem and
    needs nothing from us to notice it. That count is zero today, so the signal is unmet rather
    than pre-satisfied.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "abort_or_panic"
family_classification = "none_reviewed"
control_ids = ["testing.guard.process-boundary-rustfmt-pre-rustc"]
transition_ids = ["testing.trigger.process-boundary-rustfmt-pre-rustc"]

[[record.source_replacement]]
span_id = "span-part-part-rustfmt-code-never-reaches-rustc-all"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.profile-driven-compile-gate-covers-compositions-profiles-specs"
title = "A profile-driven compile gate covers the compositions its PROFILES' SPECS reach, and a runtime file emitted on an IR PREDICATE × a FLAG is reached only by their conjunction — the first consumer-reported no_std break lived exactly in that hole."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A profile-driven compile gate covers the compositions its PROFILES' SPECS reach, and a runtime file emitted on an IR PREDICATE × a FLAG is reached only by their conjunction — the first consumer-reported no_std break lived exactly in that hole.
"""
detail_md = """- **A profile-driven compile gate covers the compositions its PROFILES' SPECS reach, and a runtime
  file emitted on an IR PREDICATE × a FLAG is reached only by their conjunction — the first
  consumer-reported no_std break lived exactly in that hole.** Every profile of the no_std drift gate
  is chosen for a runtime surface the snapshot corpus never emits (`tests/README.md` § \"The no_std
  drift gate (`no_std_check`, local tier)\"); none of their CDDL contained `any`. `any_cbor.rs` is
  emitted only when the finalized IR holds `any`, and the whole of `static/any_cbor_json.rs` — eight
  nested inline `natural_any_cbor_*` serde adapters — is appended to it only under
  `--json-serde-derives`. Both flags were already in the `json_schema` profile, so the miss reads
  as flag coverage while being composition coverage: no profile's SPEC put the file in any thumb
  compile, and the consumer met the E0425 first. Owned meanwhile by that profile's extended CDDL —
  the `any_members` rule described in that section, which puts all eight adapters in the compile —
  plus the working rule that a new conditionally-assembled `static/` fragment states which profile's
  spec reaches it. Machinery
  on a trigger, not now (one instance, and the enumeration it would need — every emission predicate
  in `generation/export.rs`'s fragment assembly, paired against every profile's generated file set —
  is a second model of the assembly that can drift from it): a coverage assertion that each
  conditionally-assembled static fragment appears in at least one profile's generated tree.
  *Reopening signal:* a SECOND no_std break in a `static/` fragment that no profile's compile
  reaches — measurable by the party who already has the problem (a consumer's red shim, or a tier
  runner's red cell naming a file no cell had compiled), on the dimension the cost grows (the count
  of conditionally-assembled fragments outside every profile's spec).
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.profile-driven-compile-gate-covers-compositions-profiles-specs"]
transition_ids = ["testing.trigger.profile-driven-compile-gate-covers-compositions-profiles-specs"]

[[record.source_replacement]]
span_id = "span-record-profile-driven-compile-gate-covers-compositions-profiles-specs"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.project-json-gen-documents-run-test-fixture-writes"
title = "Project the json-gen documents that no `run_test` fixture writes."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Project the json-gen documents that no `run_test` fixture writes.
"""
detail_md = """- **Project the json-gen documents that no `run_test` fixture writes.** The TS-projection oracle
  runs from `run_test`'s json-gen block, so it covers exactly the documents a `tests/<dir>/export`
  fixture produces. Six other sites run a generated json-gen crate to a successful document and
  project none of it: `json_schema_dep_threading`, `json_schema_export_without_serde_derives`,
  `custom_schema_impl_writes_a_closing_document` and `json_gen_dep_links_a_threaded_dependency` in
  `integration_tests`, `a_derived_thread_links_and_a_collision_blames_the_consumer` in
  `config_tests`, and
  `package_json_pipeline`'s own json-gen step (whose OUTPUT is projected — the merged `.d.ts` is
  type-checked — but through the shipped script line rather than this leg). Every one of them builds
  its crate from a one- or two-rule scratch spec (`dep_thing = [x: uint]`), so its document is a
  strict simplification of what the fixtures already project; that is the whole reason for the
  deferral, and it is what the build would have to stop being true of. The cheap build is the same
  helper called with the scratch crate's json-gen dir — the shared install and the memo already make
  a call a `node` + `tsc` run of a couple of seconds. Reopening signal: one of those sites grows a
  spec carrying a construct no `tests/*/input.cddl` fixture has (a hand-written `JsonSchema` impl of
  its own, a cross-crate threaded row whose key is not an identifier), which is when its document
  stops being a simplification of a projected one.
"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.project-json-gen-documents-run-test-fixture-writes"]

[[record.source_replacement]]
span_id = "span-record-project-json-gen-documents-run-test-fixture-writes"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.proof-gap.config-run-s-convergence-warning-name-crate-already"
title = "Regression gap for testing.config-run-s-convergence-warning-name-crate-already."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "work"
summary_md = """Regression gap for testing.config-run-s-convergence-warning-name-crate-already.
"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "misleading_docs"
family_classification = "none_reviewed"
acceptance_md = """Add a red-first regression that runs the unchanged config twice and pins whether the convergence warning names the same crate on both runs.
"""
priority_rationale_md = """The parent defect owns repair; this record owns the still-unexecuted regression gap without masquerading as defect evidence.
"""

[[record]]
id = "testing.proof-gap.verify-ts-refuse-unknown-flag-silently-degrading-plain"
title = "Regression gap for testing.verify-ts-refuse-unknown-flag-silently-degrading-plain."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "work"
summary_md = """Regression gap for testing.verify-ts-refuse-unknown-flag-silently-degrading-plain.
"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Add a red-first CLI regression proving an unknown flag is rejected instead of returning success or degrading to plain verify mode.
"""
priority_rationale_md = """The parent defect owns repair; this record owns the still-unexecuted regression gap without masquerading as defect evidence.
"""

[[record]]
id = "testing.proof.directive-effect-round-trip-coherence-sweep-accepted-custom"
title = "Incident evidence for testing.directive-effect-round-trip-coherence-sweep-accepted-custom."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.directive-effect-round-trip-coherence-sweep-accepted-custom.
"""
evidence_kind = "incident"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-proof-directive-effect-round-trip-coherence-sweep-accepted-custom-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-proof-directive-effect-round-trip-coherence-sweep-accepted-custom-roadmap-he5a254293d69634a726bd77c7d1cde49fee2f6fe57c270aa5dbf876d38328506"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]

[[record]]
id = "testing.proof.pin-toolchain-verify-ts-s-nested-cargo-scratch"
title = "Regression proof for testing.pin-toolchain-verify-ts-s-nested-cargo-scratch."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Regression proof for testing.pin-toolchain-verify-ts-s-nested-cargo-scratch.
"""
evidence_kind = "harness_free_repro"
claim_md = """A scratch no-std check fails under the unpinned default toolchain and succeeds under the repository pin.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-proof-pin-toolchain-verify-ts-s-nested-cargo-scratch-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-proof-pin-toolchain-verify-ts-s-nested-cargo-scratch-roadmap-h41c11e8c7262400c23266ecfc4c28e30a1b59b83cb62b8035737fcfac96b55aa"]
command_md = """From a plain shell whose cwd is a scratch tree, run cargo -V and rustc -vV; then run the same probes with RUSTUP_TOOLCHAIN set to the channel parsed from rust-toolchain.toml. Separately spawn cargo -V from a repository cargo-test child after changing its cwd to scratch.
"""
result_md = """The Bun/plain-shell scratch child resolved default stable 1.97.1 while the repository pin was 1.96.1; the cargo-test child inherited RUSTUP_TOOLCHAIN and resolved the pin.
"""
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-08"
environment_md = """rustup proxy; scratch cwd versus repository cargo-test process tree
"""
unprobed_remainder_md = """No other launcher or toolchain coordinate was tested by this proof.
"""

[record.payload.scope]
surfaces = ["verify.ts nested cargo and gate-cache compiler identity"]
toolchains = ["default 1.97.1", "repository pin 1.96.1"]

[[record]]
id = "testing.proof.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"
title = "Incident evidence for testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives.
"""
evidence_kind = "incident"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-proof-positional-diversity-fold-preserve-fixture-corpus-authoring-gives-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-proof-positional-diversity-fold-preserve-fixture-corpus-authoring-gives-roadmap-h8a6dd47dcf615a826f4736b9dee803dae68cf5cddfe34f9ba6d47f3e5d9216c7"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]

[[record]]
id = "testing.proof.same-named-generated-crates-silently-share-cargo-fingerprint"
title = "Incident evidence for testing.same-named-generated-crates-silently-share-cargo-fingerprint."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Incident evidence for testing.same-named-generated-crates-silently-share-cargo-fingerprint.
"""
evidence_kind = "incident"
claim_md = """The preserved owner detail records a shipped or reproducible false-green/silent-corruption class that the proposed system would detect.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-proof-same-named-generated-crates-silently-share-cargo-fingerprint-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-proof-same-named-generated-crates-silently-share-cargo-fingerprint-roadmap-h19b0d970320ceb3f498f1f90cf31f7068da61668d960ddabc101ed40cc62a3cb"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """The evidence proves this admitted class only; it does not establish a closed denominator or the absence of adjacent classes.
"""

[record.payload.scope]
surfaces = ["owner-recorded-failure-class", "testing-roadmap-owner:next-priority"]

[[record]]
id = "testing.proof.warm-up-refresh-dep-universe-lock-trust-fresh"
title = "Regression proof for testing.warm-up-refresh-dep-universe-lock-trust-fresh."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Regression proof for testing.warm-up-refresh-dep-universe-lock-trust-fresh.
"""
evidence_kind = "execution_probe"
claim_md = """Fresh dependency resolution must update the warm-up universe and lock input, and the regression pins that behavior.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-proof-warm-up-refresh-dep-universe-lock-trust-fresh-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d", "ref-testing-proof-warm-up-refresh-dep-universe-lock-trust-fresh-roadmap-hfaf405b8952df1839f312626a996776d1cc6524cf548ffe70bb4d9130e59005e"]
command_md = """Run the offline-forced tier path after the locked warm-up fetch while the oracle-fingerprint scratch manifest resolves fresh against the newly published zerocopy-derive 0.8.56.
"""
result_md = """The fresh scratch resolve requested zerocopy-derive 0.8.56, absent from the lock-trusting warmed cache, and failed offline; the cache contained .52 through .55.
"""
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-06"
environment_md = """cargo registry cache plus offline nested-cargo gate
"""
unprobed_remainder_md = """The separate npm caret-range dependency universe was not executed by this proof.
"""

[record.payload.scope]
surfaces = ["check.ts warm-up and oracle-fingerprint scratch resolve"]

[[record]]
id = "testing.propagating-deliberately-unpublished-type-s-intent-json-ts"
title = "Propagating a deliberately-unpublished type's intent to the JSON → TS scripts."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Propagating a deliberately-unpublished type's intent to the JSON → TS scripts.
"""
detail_md = """- **Propagating a deliberately-unpublished type's intent to the JSON → TS scripts.** A type whose
  CDDL rule carries `@no_json_schema_export` still mints a wasm class with `to_json_value(): any`
  (the directive removes the registration row, not the derives), so when nothing published
  references it either, `json-ts-types.js` fails on it and the consumer restates the class by hand
  in `--allow-untyped`. The tool cannot compute that set at GENERATION time, and the reason is the
  design constraint to build against: whether a rowless type lands in `$defs` is decided by
  `schemars` at json-gen *runtime*, and a hand-written `JsonSchema` impl can introduce references
  the IR cannot see — so any generation-time list is an approximation that goes stale in both
  directions, against a stale-entry check that is a hard error by design. The mechanism worth
  building instead reports from where BOTH facts are known: have the json-gen crate emit, at
  `export_schemas()` time, which deliberately-unrowed types ended up absent from the finished
  document, and have `json-ts-types.js` consume that instead of a hand-maintained flag value. Note
  the shapes it must cover and the ones it must not: a record, a type or group choice, and a
  `@newtype` wrapper mint a class declaring the JSON method; an `_CDDL_CODEGEN_EXTERN_TYPE_`, a
  collection wrapper and a c-style enum do not. Reopening signal: a consumer whose
  deliberately-unpublished set is large enough that restating it on the script command line is a
  maintenance burden.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "wrong_public_api"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.propagating-deliberately-unpublished-type-s-intent-json-ts"]

[[record.source_replacement]]
span_id = "span-record-propagating-deliberately-unpublished-type-s-intent-json-ts"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"
title = "`quickcheck` alongside `proptest`; `goldenfile`/`expect-test` as a second corpus engine; `no-panic` lints; coverage instrumentation of *generated* code; `trybuild` for whole-crate compile-pass"
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """`quickcheck` alongside `proptest`; `goldenfile`/`expect-test` as a second corpus engine; `no-panic` lints; coverage instrumentation of *generated* code; `trybuild` for whole-crate compile-pass
"""
detail_md = """- **`quickcheck` alongside `proptest`; `goldenfile`/`expect-test` as a second corpus engine;
  `no-panic` lints; coverage instrumentation of *generated* code; `trybuild` for whole-crate
  compile-pass** (the corpus `cargo check` is simpler and broader).
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record.source_replacement]]
span_id = "span-record-quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
title = "Reason-keyed rejection evidence for the reject catalogs — one proven near-miss recorded, no machinery yet."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Reason-keyed rejection evidence for the reject catalogs — one proven near-miss recorded, no machinery yet.
"""
detail_md = """- **Reason-keyed rejection evidence for the reject catalogs — one proven near-miss recorded, no
  machinery yet.** The robustness/matrix reject catalogs snapshot the OUTCOME label only
  (`error (graceful)`), so a fixture stays green when a NEW, earlier rejection absorbs the one it
  pins: `finalize` short-circuits on prior rejections, so any parse-time rejection added upstream
  of a finalize-time check silently retargets every fixture whose spec happens to also match the
  new guard — the label is identical, the provenance is wrong, and the originally-pinned boundary
  goes unexercised. Proven near-miss (caught by implementation-time reading, not by any gate):
  an earlier no-occurrence-arrow guard would have absorbed
  `tests/robustness/float_table_key{,_composite}.cddl` (their occur-less spellings matched that
  historical guard before the float-key finalize check ran); the fixtures were respelled `*` and
  their headers document the pattern. The exact-once spelling now generates a `BoundedMap`, so it
  no longer has that rejection interaction. The mechanical
  layer: extend the reject-catalog snapshots from bare labels to a stable rejection-reason
  fingerprint per fixture (e.g. a distinctive message substring, as the decode catalog's
  `expect_err` pins already do for decode errors), so a provenance swap flips the snapshot loudly.
  Deferred because the catalogs' guard against it is a HAND step that has held so far — the working
  rule the float_table_key header states: an author adding a parse-time guard checks which existing
  reject fixtures it can reach, and respells or reason-asserts them. Reopening signal, on the
  dimension where that hand step actually fails: **a reject-catalog fixture found exercising a
  guard other than the one its header documents** — read off by whoever next re-derives a fixture's
  rejection, and evidence that the check was skipped rather than merely tedious.
  The same class in the EXECUTED fixture tests — the hand-derived negative byte vectors in
  `tests/*/tests.rs` — is machinery, not a hand step, since 2026-08-09: `assert_decode_reject_reason`
  (a per-file helper, spelled identically in each file because each is appended standalone into its
  own generated crate) plus `decode_reject_reason_tests`, whose
  `fixture_bare_decode_rejects_hold_at_their_ratchet_baseline` enumerates `tests/*/tests*.rs` by
  `read_dir` and holds each file's undiscriminated site count at an exact per-file baseline — above
  it names the new bare site and points at the helper, below it (or a stale row) says lower the row
  in the same commit. Exact both directions rather than a ceiling with a note beside it, for the
  reason the observed-baseline entry below records. The match rule is statement-scoped, not
  line-scoped: these files are rustfmt-shaped, so a site wraps across lines the moment its byte
  vector grows, and a line-scoped count would drop it for free (the two rules read the same files as
  27+20 and 30+24 respectively). Three residues, stated as plainly as the catalog fingerprint's:
  a message substring discriminates failures whose messages DIFFER, so two defects sharing one
  message stay indistinguishable to it; the ratchet counts SAME-STATEMENT sites only, so a
  decode routed through a local closure — body holds the `from_cbor_bytes`, the `is_err()` is on the
  call — is neither counted nor converted; and the helper checks message-against-substring, never
  substring-against-CLAIM — an author who copies the substring from the observed message rather
  than deriving it from the claimed boundary pins provenance to whatever it already was, so the
  derive-then-confirm order in the conversion guidance above is load-bearing, not stylistic
  (review checks it; no gate can).
  What is left there is volume, not design: **41 undiscriminated sites across 13 fixtures still
  carry baseline rows** (largest `open-struct-map-e2e` at 12), each convertible exactly as
  `tests/core` and `tests/preserve-encodings` were — derive the substring from the boundary the
  vector claims, then confirm it against the message the runtime actually produces, since it is that
  confirmation step, not the helper, that catches a vector rejecting for the wrong reason. Signal to
  convert the rest rather than let the ratchet hold them: **that count rising**, or a wrong-reason
  vector surfacing in any file that still has a row.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.reason-keyed-rejection-evidence-reject-catalogs-proven-near"]
transition_ids = ["testing.trigger.reason-keyed-rejection-evidence-reject-catalogs-proven-near"]

[[record.source_replacement]]
span_id = "span-record-reason-keyed-rejection-evidence-reject-catalogs-proven-near"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.referencing-context-measured-wrapping-deep-honored-base-shape"
title = "The referencing-context axis is measured one wrapping DEEP and at one honored base shape per directive; stacked wrappings and second base shapes are not."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """The referencing-context axis is measured one wrapping DEEP and at one honored base shape per directive; stacked wrappings and second base shapes are not.
"""
detail_md = """- **The referencing-context axis is measured one wrapping DEEP and at one honored base shape per
  directive; stacked wrappings and second base shapes are not.** The sweep that closed the
  directive-through-a-reference class — `src/tests/referencing_context_tests.rs`'s
  `directive_referencing_context_sweep` — crosses every `KNOWN_RULE_METADATA_TAGS` member with ten
  single-level wrapping contexts and found no drop at any of them (current state and design rules
  are in `tests/README.md`, in the third sibling block of the docs-contract sweep's
  description). What it does not reach is a reference TWO
  contexts deep — a tag head over a rule-body `.cbor` alias, a generic argument whose argument is
  itself a re-alias — and a directive's OTHER honored base shapes: the custom pair alone is honored
  on a scalar alias and on a self-carrying extern alias, and the sweep measures one per row. Both are
  crosses of axes that already exist rather than new mechanisms (`CONTEXTS × CONTEXTS` for the first,
  a second `BASES` row per directive for the second), and both are deferred on the same evidence:
  every fixed instance of this class was a REGISTRATION seam that all of a directive's base shapes
  reach and that one level of wrapping already exposes, so the product buys repetition of a seam the
  sweep already covers.
  Reopening signal for building either cross: another referencing-context drop fixed BY HAND — the
  same observable whose firing built the sweep, and one the sweep's own clean result puts at zero
  today, so the entry cannot already meet it.

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
control_ids = ["testing.guard.referencing-context-measured-wrapping-deep-honored-base-shape"]
transition_ids = ["testing.trigger.referencing-context-measured-wrapping-deep-honored-base-shape"]

[[record.source_replacement]]
span_id = "span-record-referencing-context-measured-wrapping-deep-honored-base-shape"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.registry-fetch-transients-nested-cargo-cells"
title = "Registry-fetch transients in nested-cargo cells."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_operational_watch"
summary_md = """Registry-fetch transients in nested-cargo cells.
"""
detail_md = """- **Registry-fetch transients in nested-cargo cells.** Nested-cargo cells used to resolve deps
  from crates.io per temp crate, so a flaky network/proxy (a proxy aborting ~1-in-6 CONNECTs to
  index.crates.io, a class cargo's own transient retry never engages on) killed otherwise-green
  runs at a random cell with `unable to update registry crates-io` / `curl [56] Proxy CONNECT
  aborted` — the shifting-cell + registry-error signature, distinguishable from a real red cell
  at a glance. Hardened by construction in check.ts: local/full runs warm-fetch once (retried)
  and force `CARGO_NET_OFFLINE=true` for every gate, so no cell touches the network
  (`tests/README.md` § \"Offline-after-warmup\"; the warm-up dep-universe manifest is drift-gated by
  `warmup_manifest_covers_registry_dep_universe`). What remains under watch:
"""
watch_state = "watching"
signature_md = """A shifting nested-cargo cell fails with `unable to update registry crates-io` or `curl [56] Proxy CONNECT aborted`, or an offline cell attempts network access after warm-up.
"""
response_md = """Retry the warm-up only; do not relabel a stable cell failure as a network transient.
"""
escalation_transition_id = "testing.trigger.registry-fetch-transients-nested-cargo-cells"
retirement_semantics_md = """Retire only when nested cells are constructionally offline and the warm-up's dependency universe is drift-gated.
"""

[[record.payload.capture_step]]
step_id = "signature"
capture_md = """Capture warm-up result, offline env, failing cell, cargo error, registry/proxy state, and immediate offline retry.
"""

[[record.payload.capture_step]]
step_id = "scope"
capture_md = """Record the full untruncated log or consumer report, command, full commit, date, environment, relevant versions, and the explicitly unprobed remainder; never cite a disposable draft log as durable evidence.
"""

[[record.source_replacement]]
span_id = "span-record-registry-fetch-transients-nested-cargo-cells"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "A rejection message's remedy string is an executable claim — pin each one with a generates-green vector, and execute a findings entry's operative claims at pickup."
projection_group = "next-priority"
legacy_aliases = ["Next work 15"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A rejection message's remedy string is an executable claim — pin each one with a generates-green vector, and execute a findings entry's operative claims at pickup.
"""
detail_md = """15. **A rejection message's remedy string is an executable claim — pin each one with a
    generates-green vector, and execute a findings entry's operative claims at pickup.** The
    `*_rejects_gracefully` family already pins message TEXT; this extends the convention so the
    remedy half of the contract is executed, not just spelled. Sweep the existing family for
    remedies that are only text-pinned and add the generate leg where one is missing; new refusals
    adopt the pattern from the start (the table-domain and tagged-domain tests are the templates,
    and `plain_group_keyed_map_member_rejects_gracefully_at_every_spelling`'s \"the remedy must
    actually work\" legs are the pattern applied from day one). Four working rules bound what the
    sweep has to cover:

    **Status: pending family-wide sweep.** The ten individual pins below prove instances and the
    day-one convention, but they do not complete the sweep of existing refusal remedies.

"""
work_state = "ready"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.rejection-message-s-remedy-string-executable-claim-pin's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""
admission_ids = ["testing.admission-path.rejection-message-s-remedy-string-executable-claim-pin"]

[[record.source_replacement]]
span_id = "span-record-rejection-message-s-remedy-string-executable-claim-pin"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.representation-changing-directive-goes-live-new-container-ship"
title = "A REPRESENTATION-CHANGING directive that goes live on a new container can ship without its extern-interface projection — the cross-crate skew class, invisible to every single-crate gate."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A REPRESENTATION-CHANGING directive that goes live on a new container can ship without its extern-interface projection — the cross-crate skew class, invisible to every single-crate gate.
"""
detail_md = """- **A REPRESENTATION-CHANGING directive that goes live on a new container can ship without its
  extern-interface projection — the cross-crate skew class, invisible to every single-crate
  gate.** Proven instance (orchestrator code-read, not any gate): `@duplicates preserve` on
  tables was live for two commits while the projection helper only travelled `reject`, and the
  helper's own doc-comment actively misled (\"table preserve never reaches here\" — stale from the
  phase-1 refusal era); a dep exporting a preserve table would have had its consumer silently
  rebuild a reject-default `BTreeMap`. Fixed by the shape-aware `duplicates_annotation`, which
  now derives the projection from the SAME `is_reject_ordered_set`/`is_preserve_pair_map`
  predicates that drive representation — the coupling that makes the drift structural rather
  than remembered — plus the `dep-preserve`/`consumer-preserve` two-crate fixture with its
  negative-control skew leg. Standing rule meanwhile: a delivery that makes a directive change
  ANYTHING a dep EMITS — representation, materialized names, or annotation semantics — must land
  the extern-interface projection AND its two-crate fixture in the same change (reviewers: diff
  `extern_interface.rs`'s annotation helpers against the new predicate).
  The wider vocabulary is measured, not cautious: the forward direction's near-miss (delivery 4)
  was `@no_alias` honored on collection rules — a MATERIALIZATION change, no representation
  touched — where the projection had to move in the honoring commit (`f9f69a8d`, with its
  two-crate vector) or every consumer would have imported a `pub type` the dep no longer emits.
  It did not ship, because the delivery's review mandated the projection check ad hoc — but the
  old vocabulary (\"changes a rule's REPRESENTATION\") would not have demanded it, which is exactly
  how the class survives a diligent implementer. Mechanical layer for the forward direction, to
  build if a second emission-changing surface ships without its projection: a marker registry in
  the writer-vocabulary style — enumerate the per-ident records and config predicates in
  `IntermediateTypes` that alter the dep's emitted rust surface, and hold each against a total
  verdict (consulted by `extern_interface.rs`, or classified inapplicable with its reason), the
  way `EXTERN_INTERFACE_WRITER_VOCABULARY` is total over the emitter's `@…` literals.
  The trigger for a mechanical layer — an extern-projection sweep that enumerates
  every representation-changing config predicate and auto-checks a dep/consumer pair per
  directive — is a SECOND representation-changing surface shipping without its projection.
  The class has a CONVERSE direction, proven by a second reading-found instance (2026-08-01,
  delivery 3): the projection EMITTING a directive onto a form the consumer's parse REFUSES.
  `@raw_bytes_flavor` was projected verbatim onto the param-less rendering of a generic extern
  base — a spelling that flavors nothing — and the moment the non-generic-extern refusal landed
  beside it, every consumer of a flavored dep would have hard-failed; no committed fixture was
  such a consumer, so both tiers stayed green while the break shipped in the working tree
  (caught in-delivery by reasoning about the seam, fixed by dropping the tag from the
  projection, pinned by `extern_import_flavored_generic_base_projects_without_the_tag`). What
  makes the converse direction structural rather than remembered is that a REFUSAL delivery has
  no reason to look at the projection: the writer already exists and no representation changed.
  The mechanical layer covering BOTH directions is DELIVERED — the writer-vocabulary registry, its
  per-annotation consumer acceptance vectors, and the LOCKSTEP source scan that holds the two total
  over the emitter's `@…` literals; current state in `tests/README.md` § \"Extern-interface export &
  `--extern-import` (the machine-generated stub channel)\". What remains unmechanized is the DEP-SIDE
  half:
  the registry proves this crate's parse accepts what its writer emits, and says nothing about a
  counterparty crate at a different version doing so. Reopening signal, measurable by whoever
  pays it: a consumer failing to regenerate against a dependency's committed export whose
  annotations this crate's own parse accepts — the version-skew shape the single-repo registry
  cannot see.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "wrong_public_api"
family_classification = "none_reviewed"
control_ids = ["testing.guard.representation-changing-directive-goes-live-new-container-ship"]
transition_ids = ["testing.trigger.representation-changing-directive-goes-live-new-container-ship"]

[[record.source_replacement]]
span_id = "span-record-representation-changing-directive-goes-live-new-container-ship"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.restructuring-features-std-tool-owned-sub-feature-cddl"
title = "Restructuring `features.std` into a tool-owned sub-feature (a `_cddl-codegen-std` key referenced from a consumer-owned `std`)."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """Restructuring `features.std` into a tool-owned sub-feature (a `_cddl-codegen-std` key referenced from a consumer-owned `std`).
"""
detail_md = """- **Restructuring `features.std` into a tool-owned sub-feature (a `_cddl-codegen-std` key
  referenced from a consumer-owned `std`).** Declined because the key layout cannot change the
  SEMANTICS, and its two legibility benefits are each dominated by a cheaper reversible channel.
  The semantics are forced to exactly union-plus-prune by three constraints, whatever the keys:
  the `--rust-dep`/`--std-forward-dep` family is assert-only (a dropped flag carries no name to
  tombstone, and `merge_dep_spec` never removes a field the tool's current spec doesn't set, so
  its `default-features = false` outlives the flag un-retractably — retracting the forward while
  defaults-off persists would hand a `std` consumer the path dep's alloc build silently); a
  forward to an absent dependency is a manifest cargo rejects (the prune); and hand forwards are
  uncomputable even in principle (the dependency's feature NAME is unknowable without a registry
  lookup — itertools spells it `use_std`), so they must union-survive. With semantics fixed, the
  split would deliver only a visible ownership boundary — the human form of which the asserted
  `# cddl-codegen:` comment block delivers (and states the RULES, which a key partition cannot),
  and the machine form of which the metadata-record deferral above delivers strictly better —
  while the split alone is irreversible: a cargo feature name on published generated crates is
  public API, and removing one later is a semver break. Every candidate reopening observable
  routes to a different remedy, which is the decline: consumer confusion recurring AFTER the
  asserted comment ships reopens the comment's TEXT; a machine consumer appearing fires the
  metadata deferral; a hand entry lost under the merge is a merge BUG. Nothing observable routes
  to the split itself.
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.restructuring-features-std-tool-owned-sub-feature-cddl.
"""
authority_reference_id = "ref-testing-restructuring-features-std-tool-owned-sub-feature-cddl-authority"
permanence = "permanent"

[[record.source_replacement]]
span_id = "span-record-restructuring-features-std-tool-owned-sub-feature-cddl"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.rule-trailing.directive-classification"
title = "Adopt the parser's `RuleTrailing` anchor and classify that rule-only slot in one delivery — blocked on publishing the reviewed fork revision."
projection_group = "pending-maintainer"
legacy_aliases = ["B3-002", "B3-005", "T1-09"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Adopt the parser's `RuleTrailing` anchor and classify that rule-only slot in one delivery — blocked on publishing the reviewed fork revision.
"""
detail_md = """### Adopt the parser's `RuleTrailing` anchor and classify that rule-only slot in one delivery — blocked on publishing the reviewed fork revision.

At the pinned cddl revision (`ac1b98ec`) a
  group rule's rule-position directive binds to its LAST GROUP ENTRY's trailing slot, so the
  spelling that puts the closing paren on its own line (`grp = (\\n a: uint\\n) ; @x`) leaves the
  trailing comment past every slot the AST offers — it is merged into the following rule's
  leading-comment slot, or orphaned when the group rule is last. A directive is therefore
  undeliverable in that position at this pin, and generation REFUSES the spelling pre-IR
  (`parsing::multiline_group_trailing_directive_rejection`, named from `api::with_types` right after
  the checked parse) so the formatting choice cannot decide silently what a spec means. Prose
  comments there stay accepted, and both bindable spellings — the whole group on one line, and the
  closing paren on the last entry's line — are unchanged and pinned by
  `group_rule_directive_on_the_last_entry_line_is_honored`. That last-entry slot is
  real field metadata and is correctly covered by `no_silent_directive`; it must not be
  reinterpreted as a rule-only position. The current boundary is documented in `tests/README.md`
  § “The directive×rule-shape sweep”.

  The refusal is the interim posture, not the end state: it makes the spelling loud, where adoption
  would make it WORK. Fork commit `a7ed0784e89689784ff78ed0e85c7434a3528937` (`local-fixes`,
  unpushed) adds the `RuleTrailing` source-position anchor without disturbing the last-entry
  binding, and publishing it is blocked on an explicit maintainer reversal of the 2026-08-04 no-fork-push
  ruling — that reversal is the pending action. When the revision is published and adopted, the
  delivery replaces the refusal seam with
  the prepared `group_rule_pin_metadata` reader (`comments_after_rule` as a third source) and lands
  both spellings' `no_silent_directive` vectors in the same change — the multi-line sweep shape
  `plain_group_spliced_multiline_paren` flips from measuring a rejection to measuring an effect, and
  the `robustness_tests` refusal vectors become honor vectors. At the new rule-only slot, honor
  `@rust_name`, `@no_json_schema_export`, `@custom_json`, and `@used_as_key`; reject `@doc`,
  `@newtype`, `@no_alias`, `@copy`, `@used_as_elem`, `@ignore`, `@duplicates`,
  `@raw_bytes_flavor`, the custom-codec family, and `@extern_companions` with one site-bearing
  diagnostic plus per-directive vectors. The path override was already green with the prepared
  codegen side. Pressure signals for making the call: the fork is bumped for any reason (the
  revision must be re-carried either way), or a consumer reports hitting the refusal on a
  multi-line spelling their formatting tooling forces.

"""
work_state = "blocked"
work_intent = "build_capability"
work_kind = "feature"
risk = "valid_rejection"
family_classification = "none_reviewed"
blocker_md = """The maintainer must reverse the 2026-08-04 no-fork-push ruling and reviewed commit a7ed0784e89689784ff78ed0e85c7434a3528937 must be published, pinned, and adopted.
"""
transition_ids = ["testing.predicate.rule-trailing-conjunctive-unblock"]

[[record.source_replacement]]
span_id = "span-record-rule-trailing-directive-classification"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.rule-trailing.reviewed-fork-publication"
title = "Publish and adopt the reviewed RuleTrailing fork."
projection_group = "pending-maintainer"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "work"
summary_md = """Publish and adopt the reviewed RuleTrailing fork.
"""
work_state = "waiting_external"
work_intent = "build_capability"
work_kind = "feature"
risk = "valid_rejection"
family_classification = "none_reviewed"
transition_ids = ["testing.predicate.reviewed-fork-publication"]
external_owner_reference_id = "ref-testing-rule-trailing-reviewed-fork-publication-commit"

[[record]]
id = "testing.ruling-premise-gate-x-stays-green-must-probed"
title = "A ruling whose premise is \"gate X stays green\" must be probed in the state the ruling CREATES, not the state that precedes it."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A ruling whose premise is \"gate X stays green\" must be probed in the state the ruling CREATES, not the state that precedes it.
"""
detail_md = """- **A ruling whose premise is \"gate X stays green\" must be probed in the state the ruling CREATES,
  not the state that precedes it.** This is the scope-the-claim discipline above applied to the
  ORDER of a probe rather than its breadth, and it is a ruling-protocol rule rather than a quirk of
  any one gate. Proven instance: an orchestrator approved blessing
  `tests/matrix_panic/snapshots/catalog.snap` ahead of a `cddl-matrix/verify.ts` re-grounding, on
  the premise that the snapshot records only the observed generation outcome and encodes no matrix
  class, so `project_robustness.ts --check` compares directory PLACEMENT and would stay green. That
  premise was established by running the gate with the snapshot still UNBLESSED — the one state in
  which the disagreement it checks for cannot exist. The gate ALSO cross-checks the committed
  catalog against the matrix verdict class (it is the drift class the check was built for, and its
  own message states the required order: re-run verify.ts, re-project, re-bless), and it is
  `fast`-tier, so the bless would have turned CI red for fourteen cells. It was found by RUNNING the
  gate in the post-bless state, not by re-reading it — which is the tell: a pre-change green says
  nothing about a post-change gate whose input the change is what alters. Standing consequence, now
  settled: **a catalog bless is the LAST step of a re-grounding, never a step that precedes it.**
  Authoring rule: a ruling resting on a gate verdict names the STATE the verdict was measured in,
  and if that state is not the one the ruling produces, the verdict is not evidence for the ruling.
  Mechanical layer, on a SECOND instance where the pre/post distinction is not visible from the
  gate's own message: have the drift check name both inputs it compares in its GREEN line, so a
  reader of a passing run can tell which comparisons actually ran.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.ruling-premise-gate-x-stays-green-must-probed"]
transition_ids = ["testing.trigger.ruling-premise-gate-x-stays-green-must-probed"]

[[record.source_replacement]]
span_id = "span-record-ruling-premise-gate-x-stays-green-must-probed"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.run-local-values-gate-cache-key-material-proven"
title = "Run-local values in gate-cache key material — one proven instance, no machinery yet."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Run-local values in gate-cache key material — one proven instance, no machinery yet.
"""
detail_md = """- **Run-local values in gate-cache key material — one proven instance, no machinery yet.** The
  TS-side cache keys hashed the literal nested-cargo argv, which embeds the run's `mkdtempSync`
  probe dir — every key was unique to its run, so verify.ts could never hit its cache across runs
  (fixed by path-normalizing the key argv; the normalized form is documented in `tests/README.md`
  § \"The gate cache (memoize-and-skip for nested cargo)\"). Caught by merge-review reading, not by
  any gate — and `verify_cache_transparency`'s ≥1-hit vacuity floor is BLIND to this class by
  construction: identical generated trees hit WITHIN a run (the probe dir is constant inside one
  run), so the hit count stays above zero even when no cross-run hit is possible. Mechanical layer
  if the class recurs (a second run-local value — an env var, a timestamp, a counter — leaking
  into key material): a key path-independence self-test (hash one fixed byte tree under two
  different scratch roots in two separate invocations; assert `gateCacheKey` / the Rust
  `gate_cache_key` produce identical keys), plus a cross-run hit floor that only counts hits whose
  entries PREDATE the running process (within-run hits excluded, so the floor actually measures
  cross-run keyability).
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.run-local-values-gate-cache-key-material-proven"]
transition_ids = ["testing.trigger.run-local-values-gate-cache-key-material-proven"]

[[record.source_replacement]]
span_id = "span-record-run-local-values-gate-cache-key-material-proven"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.rustfmt-seam-error-leg-still-witness-needs-subprocess"
title = "One rustfmt-seam error leg still has no witness, and it is the one that needs a subprocess-scoped test harness."
projection_group = "next-priority"
legacy_aliases = ["Next work 4"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """One rustfmt-seam error leg still has no witness, and it is the one that needs a subprocess-scoped test harness.
"""
detail_md = """4. **One rustfmt-seam error leg still has no witness, and it is the one that needs a
   subprocess-scoped test harness.** The seam's non-0/3-exit-is-fatal contract — which both the
   width ladder (`integration_tuple_field_width_ladder_never_aborts_rustfmt`) and the
   preserve-fixture rustfmt sweep (`preserve_fixtures_rustfmt_cycle_stability`) cite as their
   entire assertion mechanism — now has direct witnesses for its two former holes: non-UTF-8
   formatter output is an `Err(InvalidData)` rather than an `Ok` carrying the UNFORMATTED input
   (`rustfmt_non_utf8_output_is_an_error`, with an echoing-stub control so the failing leg cannot
   pass for being stubbed at all), and an unspawnable formatter binary is a clean `Err` rather than
   an unwrap backtrace (`rustfmt_unspawnable_binary_is_an_error_not_a_panic`). Both drive
   `rustfmt_source_with`, which takes the binary path as an argument precisely so a stub needs no
   `RUSTFMT`/`PATH` mutation — those are process-global and every concurrent formatter call in the
   same test process would pick the stub up. What that mechanism cannot reach is `rustfmt_path()`'s
   OWN `Err`: no `RUSTFMT` set AND `which` failing, which requires a `PATH` with no rustfmt on it —
   process-global again, and racing the tests that spawn `cargo`. The layer that would cover it is a
   subprocess-scoped harness: re-exec one test in a child process with a controlled environment,
   assert its exit and stderr. Reopening signal, on the axis the harness's cost is amortized over:
   the COUNT of error legs that need a controlled process-wide environment to drive, measurable by
   whoever next writes one — today it is one, so a SECOND is what makes the harness worth building
   rather than the leg worth skipping.

"""
work_state = "deferred"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "abort_or_panic"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.rustfmt-seam-error-leg-still-witness-needs-subprocess"]

[[record.source_replacement]]
span_id = "span-record-rustfmt-seam-error-leg-still-witness-needs-subprocess"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.same-named-generated-crates-silently-share-cargo-fingerprint"
title = "Same-named generated crates silently share one cargo fingerprint under a shared `CARGO_TARGET_DIR`, so a concurrent shard can report a non-compiling crate as `Finished`."
projection_group = "next-priority"
legacy_aliases = ["Next work 20"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Same-named generated crates silently share one cargo fingerprint under a shared `CARGO_TARGET_DIR`, so a concurrent shard can report a non-compiling crate as `Finished`.
"""
detail_md = """20. **Same-named generated crates silently share one cargo fingerprint under a shared
    `CARGO_TARGET_DIR`, so a concurrent shard can report a non-compiling crate as `Finished`.**
    Every emitted rust crate is `cddl-lib v0.1.0`, and cargo's unit hash for the ROOT package of a
    build does not include the manifest path — N cells under one target dir share ONE
    `.fingerprint/cddl-lib-<hash>` entry. Sequential generate-then-check is safe (each fresh crate is
    newer than the previous build, so it rebuilds), which is why this went unseen; SHARDED gates are
    not, because a cell generated before a different shard's build completes is judged fresh against
    that build's fingerprint and never compiled. Measured: six pre-generated cells checked in
    sequence into one target dir — the first compiled, the other five reported `Finished` in 0.13 s
    with zero errors, four of them non-compiling; with distinct package names all six compiled and
    the four red ones failed. Observed live as a run-to-run-varying false PASS while building the
    annotate=false floor, which is immunized (`give_cell_its_own_package_identity` renames each
    cell's root package before `cargo check`, keeping the shared target dir and the shared dep
    build). Still exposed, by enumeration of the sharded generate+compile gates:
    `feature_corpus_compiles`, `wasm_matrix_compiles`, `multifile_matrix_compiles`. Exposure is
    WORST exactly when it matters most — the gate cache suppresses most nested cargo runs, so the
    concurrent-cargo window opens widest right after a generator change invalidates the cache. The
    fix is the same three-line rename per gate; what makes it a work item rather than a trivial edit
    is that those gates also build `wasm/` and `wasm/json-gen`, which path-depend on `rust/` by its
    generated name, so each gate needs the rename applied across its whole crate set (or the
    dependency spellings rewritten with it) and its whole gate cache re-earned. The fix should
    land with a DETECTION layer, not just the rename: a per-shard-run red canary — one
    deliberately non-compiling cell asserted to FAIL its `cargo check` — proves the verdict
    channel itself is live, so any FUTURE false-fresh class (this hazard's mechanism or another)
    fails the gate loudly instead of silently greening; the rename alone only removes the one
    known mechanism. (No pre-existing gate could have caught this class:
    `gate_cache_closure_audit` audits OUR cache keys, and here the key was right — the verdict
    cargo handed us was wrong.) NEW MEMBER + a counterexample to the sequential-safety claim
    above (2026-08-08, the matrix component-execution leg's bring-up): even SEQUENTIAL same-name
    cells under one shared target cross-bound — cell 2's native oracle resolved its `cddl-lib`
    path dep against cell 1's already-built same-name rlib and failed loudly on a type the new
    cell defines (`cannot find type ProbeHolder`), a spurious RED rather than this item's silent
    green, but the same mechanism (name+version-keyed fingerprints, manifest path excluded). The
    leg is defended by an mtime `touchTree` per cell before its builds (`verify.ts`,
    `COMPONENT_TARGET`'s comment carries the proof). Standing rule this instance adds: every NEW
    shared-target consumer of same-named generated crates states its defense (package rename or
    `touchTree`) at the site that creates the target dir, so the next leg does not rediscover
    the hazard live.

"""
work_state = "ready"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.same-named-generated-crates-silently-share-cargo-fingerprint's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""
admission_ids = ["testing.admission-path.same-named-generated-crates-silently-share-cargo-fingerprint"]

[[record.source_replacement]]
span_id = "span-record-same-named-generated-crates-silently-share-cargo-fingerprint"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.schema-document-s-name-injectivity-enforced-row-side"
title = "The schema document's name injectivity is enforced ROW-side, so the residue is everything the row set cannot see."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """The schema document's name injectivity is enforced ROW-side, so the residue is everything the row set cannot see.
"""
detail_md = """- **The schema document's name injectivity is enforced ROW-side, so the residue is everything the row
  set cannot see.** The document is written by a program we emit, so its content is a property of the
  RUN rather than of the emitted source, and the row-side guard that closes every collision with a row
  on the losing side — the runtime `add_schema` helper's name ledger, kept-its-own-name check and
  inline-branch conflict check, firing in the consumer's own `cargo run` — is current state in
  `tests/README.md` § \"JSON-schema document — Rust-side coverage (`run_test`'s per-fixture assertions
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-schema-document-s-name-injectivity-enforced-row-side-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.schema-document-s-name-injectivity-enforced-row-side's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record.source_replacement]]
span_id = "span-record-schema-document-s-name-injectivity-enforced-row-side"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.schema-name.collision-loser-no-row-same-id"
title = "A collision whose LOSER has no row and whose `schema_id`s match"
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A collision whose LOSER has no row and whose `schema_id`s match
"""
detail_md = """  - **A collision whose LOSER has no row and whose `schema_id`s match** is a silent merge nothing can
    see: the ledger only holds rows, and the merge makes both returned refs equal the shared name, so
    the kept-its-own-name check reads clean. Reaching it needs a way to enumerate what a row pulls in
    transitively — a post-pass over the finished document comparing `$defs` cardinality against the
    reachable type set, or an upstream `schemars` setting that rejects two `TypeId`s resolving to one
    `schema_id`. The upstream lever is report-and-wait (`schemars` is a crates.io dependency, not a
    fork we pin like the `cddl` parser), so the document post-pass is the schedulable one.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "wrong_public_api"
family_classification = "none_reviewed"
control_ids = ["testing.guard.schema-name-collision-loser-no-row-same-id"]
transition_ids = ["testing.trigger.schema-name-collision-loser-no-row-same-id"]

[[record.source_replacement]]
span_id = "span-part-part-collision-loser-row-schema-id-s-match"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.schema-name.cross-crate-same-id-collision"
title = "A cross-crate collision whose `schema_id`s MATCH"
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A cross-crate collision whose `schema_id`s MATCH
"""
detail_md = """  - **A cross-crate collision whose `schema_id`s MATCH** — two crates' `add_schemas` threaded into
    one `SchemaGenerator` — escapes the ledger, which belongs to the registrar one crate's
    `add_schemas` opens. The
    tool now EMITS that composition (`--json-schema-dep`) and DERIVES it from a config's crate
    graph, so this is no longer a hypothetical layout: what remains uncovered is only the ids-match
    merge, because dep calls are emitted FIRST
    and therefore an ids-DIFFER cross-crate collision hands the consumer's row `<name>2` and trips
    the kept-its-own-name check on the side whose owner can change it. That last step is a
    measurement rather than an inference, pinned by
    `config_tests::a_derived_thread_links_and_a_collision_blames_the_consumer` (what that cell builds
    and asserts: `tests/README.md` § \"Config-file front end (`src/tests/config_tests.rs`)\").
    Widening the ledger to span crates
    still means either a published ledger type in the static runtime (a new cross-crate API surface)
    or making `add_schemas` take the ledger, which would break the composition-point signature a
    consumer was told to call — so the ids-match merge is where a cross-crate report would have to
    land before either is worth doing.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "wrong_public_api"
family_classification = "none_reviewed"
control_ids = ["testing.guard.schema-name-cross-crate-same-id-collision"]
transition_ids = ["testing.trigger.schema-name-cross-crate-same-id-collision"]

[[record.source_replacement]]
span_id = "span-part-part-cross-crate-collision-schema-id-s-match"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.schema-name.percent-encoded-ref"
title = "A `schema_name()` that `schemars` percent-encodes into its `$ref`"
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A `schema_name()` that `schemars` percent-encodes into its `$ref`
"""
detail_md = """  - **A `schema_name()` that `schemars` percent-encodes into its `$ref`** (anything outside
    `[A-Za-z0-9_]`, e.g. the static runtime's `OrderedHashMap<K, V>`) skips the kept-its-own-name
    check entirely: `schemars`' `encode_ref_name` lives in a private module (`mod encoding` in
    `schemars-1.2.1/src/lib.rs`, its items `pub fn` but unreachable), so reconstructing the expected
    ref means duplicating an upstream encoder — a false panic in a consumer's build is worse than a
    missed check. The closing mechanism is now half-built rather than hypothetical: the emitted
    closure check carries a `decode_schema_ref_name` (percent-decode, then the JSON-Pointer escapes),
    so the kept-its-own-name check could compare `decode(assigned)` against the name instead of
    skipping. What it still needs before it can be trusted to panic is a vector per escape class,
    since decoding a name the encoder spelled differently than assumed would fail a build that is
    fine: `tests/json-extern`'s hand-written `schema_name()` covers three of them (`~`, `/`, a
    percent-escaped byte — `tests/README.md` § \"JSON-schema document — Rust-side coverage
    (`run_test`'s per-fixture assertions + the emitted name guard)\"), leaving a multi-byte UTF-8 name.
"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "wrong_public_api"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.schema-name.percent-encoded-ref's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The former independent part identifies the remaining multi-byte UTF-8 vector needed before the half-built decoder can safely become a build-failing guard; no admission trigger is inferred.
"""

[[record.source_replacement]]
span_id = "span-part-part-schema-name-schemars-percent-encodes-ref"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.schema-name.two-rowless-types-collide"
title = "A collision between two types that BOTH lack rows"
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A collision between two types that BOTH lack rows
"""
detail_md = """  - **A collision between two types that BOTH lack rows** — each reached only transitively from some
    row'd type — is seen by neither check: the ledger holds rows, and \"kept its own name\" is asked
    only of a registered type. With distinct `schema_id`s both are emitted and one takes `<name>2`
    by first-encounter order, so an unrelated spec edit (a rule that sorts earlier, a reordered
    struct field) can silently SWAP which published TypeScript name belongs to which type. The
    byte-identity assertion cannot see it — it compares two runs of ONE spec, never two versions of
    a spec. Same closing mechanism as the no-row-loser hole above (a post-pass over the finished
    document); making the ordering irrelevant rather than merely loud would mean a published-name
    scheme that does not depend on traversal order at all — a breaking rename of published names in
    its own right, so it needs a consumer asking for name stability across spec edits. A cheaper
    *tell* is worth recording beside that post-pass, as a candidate rather than a commitment, because
    the post-pass is not implementable from inside the emitted crate at all (nothing there can
    enumerate the reachable type set): schemars assigns a collision suffix by walking `for i in 2..`
    over the names it has already used (`schemars-1.2.1/src/generate.rs`, the `find_ref` name
    assignment — verified at that pinned version, which `static/Cargo_json_gen.toml` is what pins),
    so a document holding both `<base>` and `<base>2` where no registered row publishes `<base>2` is
    the observable signature of the collision. It carries a real false-positive class — a genuine
    Rust type named `Foo2` — so it would need an opt-out, which is most of why it is not obviously
    worth shipping.

    **This hole is MEASURED, not hypothetical — by the consumer, in their corpus, not by us.** CML
    reported that the `AssetBundle`/`AssetBundle2` pair we predicted would trip the name ledger trips
    nothing: neither is a row. They are `$defs` entries reached through fields (`Value.multiasset`,
    `TransactionBody.mint`) from a hand-written generic substituted in by a `replace` block, and
    because that generic `#[derive(JsonSchema)]`s, the ids DIFFER — so both bodies are emitted and
    which instantiation keeps the bare name is traversal order. Milder than the ids-match merge
    recorded above (no wrong body; both types exist) and worse in one specific way: it silently
    renames a *published TypeScript type* on an unrelated spec edit, which for a published package is
    an API break.

    It is recorded rather than built for a reason that is now probed rather than assumed: a precise
    detector needs the schema_id -> assigned-name mapping, and `schemars::SchemaGenerator` exposes no
    such accessor — `definitions()` / `definitions_mut()` / `take_definitions()` all return only
    `Map<String, Value>` (read at `schemars-1.2.1/src/generate.rs`). From the finished document alone
    the `{base}{i}` rename is indistinguishable from a type genuinely named `Foo2`, so the
    `$defs`-shaped heuristic above would fire on legitimate corpora and train people to ignore it.
    The consumer's own committed-`.d.ts` CI diff is the precise detector for this class and is
    already the standing recommendation.

    What a consumer can do TODAY, and why that further lowers the priority: give each instantiation a
    row with `--json-schema-root` (`--json-schema-root=my_crate::AssetBundle<u64>` and `…<i64>` —
    generic arguments are inside the flag's accepted charset). Registering both makes them two rows
    publishing one `schema_name()` with different `std::any::type_name`s, which the ledger reports;
    registering only the instantiation that LOST the bare name makes the kept-its-own-name check
    report the `<name>2` it was assigned. So the row-side guard is opt-in-able for exactly the shape
    it cannot see on its own. (Reasoned from `static/json_schema_gen.rs`'s `add_schema` and `parse_json_schema_root`'s
    charset — not run.) Reopening signal for building anything more: a public schemars accessor for
    the assigned-name map, or a consumer reporting a rename their `.d.ts` diff did not catch.

  The document's reference CLOSURE is checked in two places — our own per-fixture assertion and the
  emitted `export_schemas()` walking the CONSUMER's document before it writes, both current state in
  `tests/README.md` § \"JSON-schema document — Rust-side coverage (`run_test`'s per-fixture assertions
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "wrong_public_api"
family_classification = "none_reviewed"
control_ids = ["testing.guard.schema-name-two-rowless-types-collide"]
transition_ids = ["testing.trigger.schema-name-two-rowless-types-collide"]

[[record.source_replacement]]
span_id = "span-part-part-collision-two-types-both-lack-rows"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.scratch-retention.reopening-signal"
title = "Reopening signal"
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "signal"
summary_md = """Reopening signal
"""
detail_md = """  - **Reopening signal** (for a bounded named scratch root the tiers reuse and truncate, which
    retires debris by construction rather than by age): a disk-floor refusal or an ENOSPC gate death
    on a machine whose SAME run's sweep line reported nothing removed. Both halves are in the one
    log the operator already has, so the report arrives self-diagnosing.
  Two corroborating
  sightings from the concurrent session, same saturation window: a first-ever generation run
  exiting 1 whose only capture went through `tail -3` (evidence burned — the exact failure mode
  the evidence-preservation rule names; green on immediate rerun, attribution now unrecoverable
  but consistent with a full `/tmp`), and a local-tier run failing three nested-cargo gates on
  os error 28 mid-saturation (all green on the post-remediation rerun; its full log was then
  destroyed with its worktree — the per-checkout `draft/logs/` lifetime note in AGENTS.md).
"""
transition_kind = "watch_escalation"
failure_signature_md = """A disk-floor refusal or ENOSPC gate death occurs and the same run's scratch-sweep line reports that it removed nothing.
"""
capture_procedure_md = """Preserve the full untruncated log; record its scratch-sweep line, disk preflight, failing gate and ENOSPC or disk-floor line, command, commit, date, environment, and live-path exclusions. Both conjuncts must come from the same run.
"""
response_md = """Protect live paths, recover capacity using explicit age- and liveness-checked targets, and rerun the complete tier.
"""
escalation_action_md = """Promote the bounded named scratch-root design that tiers reuse and truncate; do not fire from evidence whose two halves are from different runs.
"""
retirement_semantics_md = """Retire only when the bounded root makes killed-run debris self-retiring and a durable gate proves it cannot accumulate across runs; age-based sweep success alone does not retire it.
"""
evaluation = "unknown"

[[record.source_replacement]]
span_id = "span-part-part-reopening-signal-nested"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.shared-cargo-target-dir-across-same-named-scratch"
title = "A shared `CARGO_TARGET_DIR` across same-named scratch crates masks compile failures as cached passes."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_operational_watch"
summary_md = """A shared `CARGO_TARGET_DIR` across same-named scratch crates masks compile failures as cached passes.
"""
detail_md = """- **A shared `CARGO_TARGET_DIR` across same-named scratch crates masks compile failures as
  cached passes.** Proven 2026-08-01 (the same-chain `.cbor` refusal delivery): two scratch
  probes generated crates with the same package name into different directories and
  `cargo check`ed them under one shared target dir; the second check reused the first's
  fingerprint and reported clean for a crate that does not compile — the probe's verdict was
  the CACHE's, not the crate's. The gate-cache machinery is not implicated (it keys on
  generated-crate content hashes); this is raw cargo fingerprint reuse, and it can flip a
  red-first probe green. Working rule: compile probes of scratch crates use a per-crate target
  dir (or distinct package names); a probe that must share a target dir for warm-cache speed
  asserts something the cache cannot fake (`cargo clean -p <name>` first, or check the error
  output of a build it forced). Watch, not work item: the next false-green scratch probe traced
  to fingerprint reuse is the second instance; if one appears inside a REGISTERED gate rather
  than an ad-hoc probe, that gate's cell keying is the defect and it graduates to a work item.

"""
watch_state = "attributed"
signature_md = """A same-named scratch crate reports false-green because another crate's fingerprint was reused in a shared target dir.
"""
attribution_md = """The 2026-08-01 probe reused the first same-named crate's fingerprint.
"""
operating_rule_reference_id = "ref-testing-shared-cargo-target-dir-across-same-named-scratch-operating"
response_md = """Give each crate a target dir or unique package name and rerun.
"""
escalation_transition_id = "testing.trigger.shared-cargo-target-dir-across-same-named-scratch"
retirement_semantics_md = """Retire when scratch probe helpers enforce content-distinct fingerprints.
"""

[[record.payload.capture_step]]
step_id = "signature"
capture_md = """Capture both crate trees/package names, target dir, forced-clean comparison, verbose cargo freshness output, and compile error from an isolated target.
"""

[[record.payload.capture_step]]
step_id = "scope"
capture_md = """Record the full untruncated log or consumer report, command, full commit, date, environment, relevant versions, and the explicitly unprobed remainder; never cite a disposable draft log as durable evidence.
"""

[[record.source_replacement]]
span_id = "span-record-shared-cargo-target-dir-across-same-named-scratch"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.source-observation.add-profile-wrapper-participation-grid-participation-differs-across"
title = "Source-owner review for testing.add-profile-wrapper-participation-grid-participation-differs-across."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.add-profile-wrapper-participation-grid-participation-differs-across.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-add-profile-wrapper-participation-grid-participation-differs-across-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.alloc-imports-nested-inline-modules-hand-carried-design"
title = "Source-owner review for testing.alloc-imports-nested-inline-modules-hand-carried-design."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.alloc-imports-nested-inline-modules-hand-carried-design.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-alloc-imports-nested-inline-modules-hand-carried-design-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.armed-idle-harness-arms-empty-head-ledgers-zero"
title = "Source-owner review for testing.armed-idle-harness-arms-empty-head-ledgers-zero."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.armed-idle-harness-arms-empty-head-ledgers-zero.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-armed-idle-harness-arms-empty-head-ledgers-zero-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.assert-hand-authored-json-schema-against-type-s"
title = "Source-owner review for testing.assert-hand-authored-json-schema-against-type-s."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.assert-hand-authored-json-schema-against-type-s.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-assert-hand-authored-json-schema-against-type-s-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.backgrounded-check-ts-full-launched-sub-agent-s"
title = "Source-owner review for testing.backgrounded-check-ts-full-launched-sub-agent-s."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.backgrounded-check-ts-full-launched-sub-agent-s.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-backgrounded-check-ts-full-launched-sub-agent-s-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:operational-watches", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.cargo-feature-generated-crate-s-dependency-change-what"
title = "Source-owner review for testing.cargo-feature-generated-crate-s-dependency-change-what."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.cargo-feature-generated-crate-s-dependency-change-what.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-cargo-feature-generated-crate-s-dependency-change-what-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.cddl-module-directives-draft-ietf-cbor-cddl-modules"
title = "Source-owner review for testing.cddl-module-directives-draft-ietf-cbor-cddl-modules."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.cddl-module-directives-draft-ietf-cbor-cddl-modules.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-cddl-module-directives-draft-ietf-cbor-cddl-modules-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.citation-resolves-naming-wrong-thing-invisible-citation-lint"
title = "Source-owner review for testing.citation-resolves-naming-wrong-thing-invisible-citation-lint."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.citation-resolves-naming-wrong-thing-invisible-citation-lint.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-citation-resolves-naming-wrong-thing-invisible-citation-lint-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]
tiers = ["local"]

[[record]]
id = "testing.source-observation.comment-residue-false-matches-text-scans-over-emitted"
title = "Source-owner review for testing.comment-residue-false-matches-text-scans-over-emitted."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.comment-residue-false-matches-text-scans-over-emitted.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-comment-residue-false-matches-text-scans-over-emitted-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.component-js-probe-reopening-signal"
title = "Source-owner review for testing.component-js-probe.reopening-signal."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.component-js-probe.reopening-signal.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-component-js-probe-reopening-signal-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.composing-registration-rows-across-generation-passes-share-output"
title = "Source-owner review for testing.composing-registration-rows-across-generation-passes-share-output."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.composing-registration-rows-across-generation-passes-share-output.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-composing-registration-rows-across-generation-passes-share-output-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.config-run-s-convergence-warning-name-crate-already"
title = "Source-owner review for testing.config-run-s-convergence-warning-name-crate-already."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.config-run-s-convergence-warning-name-crate-already.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-config-run-s-convergence-warning-name-crate-already-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:operational-watches", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.container-combinations-reopening-signal"
title = "Source-owner review for testing.container-combinations.reopening-signal."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.container-combinations.reopening-signal.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-container-combinations-reopening-signal-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
title = "Source-owner review for testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-container-construct-conceptual-type-visitor-walks-flat-combinatorial-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.containment-note-s-stated-reason-go-false-any"
title = "Source-owner review for testing.containment-note-s-stated-reason-go-false-any."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.containment-note-s-stated-reason-go-false-any.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-containment-note-s-stated-reason-go-false-any-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.control-bytes-committed-source-files-proven-instance-machinery"
title = "Source-owner review for testing.control-bytes-committed-source-files-proven-instance-machinery."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.control-bytes-committed-source-files-proven-instance-machinery.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-control-bytes-committed-source-files-proven-instance-machinery-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.corpus-wide-regen-over-prior-output-sweep-reaches"
title = "Source-owner review for testing.corpus-wide-regen-over-prior-output-sweep-reaches."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.corpus-wide-regen-over-prior-output-sweep-reaches.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-corpus-wide-regen-over-prior-output-sweep-reaches-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]
profiles = ["component", "json"]
input_modes = ["component", "json"]
tiers = ["local"]

[[record]]
id = "testing.source-observation.crate-generates-json-schema-document-aggregate-package"
title = "Source-owner review for testing.crate-generates-json-schema-document-aggregate-package."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.crate-generates-json-schema-document-aggregate-package.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-crate-generates-json-schema-document-aggregate-package-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.cross-version-preserve-vectors-beyond-std-alloc-rewrite"
title = "Source-owner review for testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-cross-version-preserve-vectors-beyond-std-alloc-rewrite-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.dependency-swap-pass-compile-parity-existing-gate-silently"
title = "Source-owner review for testing.dependency-swap-pass-compile-parity-existing-gate-silently."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.dependency-swap-pass-compile-parity-existing-gate-silently.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-dependency-swap-pass-compile-parity-existing-gate-silently-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.dependency-version-range-resolution-untested-generated-crates-float"
title = "Source-owner review for testing.dependency-version-range-resolution-untested-generated-crates-float."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.dependency-version-range-resolution-untested-generated-crates-float.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-dependency-version-range-resolution-untested-generated-crates-float-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.directive-dependent-emitter-edge-case-triggering-shape-hand"
title = "Source-owner review for testing.directive-dependent-emitter-edge-case-triggering-shape-hand."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.directive-dependent-emitter-edge-case-triggering-shape-hand.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-directive-dependent-emitter-edge-case-triggering-shape-hand-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.directive-s-documented-sentence-stay-true-consumer-visible"
title = "Source-owner review for testing.directive-s-documented-sentence-stay-true-consumer-visible."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.directive-s-documented-sentence-stay-true-consumer-visible.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-directive-s-documented-sentence-stay-true-consumer-visible-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
title = "Source-owner review for testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-docs-vs-behavior-conformance-harness-comment-dsl-mdx-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:declined-boundaries", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.documented-failure-shape-exercises-prose-cross-crate-int"
title = "Source-owner review for testing.documented-failure-shape-exercises-prose-cross-crate-int."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.documented-failure-shape-exercises-prose-cross-crate-int.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-documented-failure-shape-exercises-prose-cross-crate-int-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.documented-flag-pairing-broken-fixture-stays-green-because"
title = "Source-owner review for testing.documented-flag-pairing-broken-fixture-stays-green-because."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.documented-flag-pairing-broken-fixture-stays-green-because.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-documented-flag-pairing-broken-fixture-stays-green-because-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.doubled-doc-comment-markers-emitted-docs-proven-instance"
title = "Source-owner review for testing.doubled-doc-comment-markers-emitted-docs-proven-instance."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.doubled-doc-comment-markers-emitted-docs-proven-instance.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-doubled-doc-comment-markers-emitted-docs-proven-instance-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.dsl-position-tests-string-level-design-green-emitted"
title = "Source-owner review for testing.dsl-position-tests-string-level-design-green-emitted."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.dsl-position-tests-string-level-design-green-emitted.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-dsl-position-tests-string-level-design-green-emitted-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.duplicates-dedicated-generic-collision-message"
title = "Source-owner review for testing.duplicates.dedicated-generic-collision-message."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.duplicates.dedicated-generic-collision-message.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-duplicates-dedicated-generic-collision-message-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.duplicates-member-position-extension"
title = "Source-owner review for testing.duplicates.member-position-extension."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.duplicates.member-position-extension.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-duplicates-member-position-extension-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.duplicates-occurrence-aware-generic-identity"
title = "Source-owner review for testing.duplicates.occurrence-aware-generic-identity."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.duplicates.occurrence-aware-generic-identity.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-duplicates-occurrence-aware-generic-identity-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.duplicates-same-shape-set-conversion"
title = "Source-owner review for testing.duplicates.same-shape-set-conversion."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.duplicates.same-shape-set-conversion.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-duplicates-same-shape-set-conversion-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.edn-text-notation-round-trip-edn-edn-draft"
title = "Source-owner review for testing.edn-text-notation-round-trip-edn-edn-draft."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.edn-text-notation-round-trip-edn-edn-draft.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-edn-text-notation-round-trip-edn-edn-draft-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.emission-branch-generated-output-fixture-exercises-invisible-gate"
title = "Source-owner review for testing.emission-branch-generated-output-fixture-exercises-invisible-gate."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.emission-branch-generated-output-fixture-exercises-invisible-gate.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-emission-branch-generated-output-fixture-exercises-invisible-gate-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.emit-tests-gate-asserts-round-trip-green-cannot"
title = "Source-owner review for testing.emit-tests-gate-asserts-round-trip-green-cannot."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.emit-tests-gate-asserts-round-trip-green-cannot.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-emit-tests-gate-asserts-round-trip-green-cannot-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.emitted-shape-lint-classes-outside-clippy-all-beyond"
title = "Source-owner review for testing.emitted-shape-lint-classes-outside-clippy-all-beyond."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.emitted-shape-lint-classes-outside-clippy-all-beyond.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-emitted-shape-lint-classes-outside-clippy-all-beyond-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
title = "Source-owner review for testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-emitter-drops-overloadable-parameter-building-fresh-config-invisible-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.extend-recombination-member-kind-table-optional-fixed-shape"
title = "Source-owner review for testing.extend-recombination-member-kind-table-optional-fixed-shape."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.extend-recombination-member-kind-table-optional-fixed-shape.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-extend-recombination-member-kind-table-optional-fixed-shape-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.extern-deps-wasm-boundary-surface-packaging-json-gen"
title = "Source-owner review for testing.extern-deps-wasm-boundary-surface-packaging-json-gen."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.extern-deps-wasm-boundary-surface-packaging-json-gen.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-extern-deps-wasm-boundary-surface-packaging-json-gen-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.extern-interface-export-dialect-v2-candidates"
title = "Source-owner review for testing.extern-interface-export-dialect-v2-candidates."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.extern-interface-export-dialect-v2-candidates.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-extern-interface-export-dialect-v2-candidates-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.extreme-value-boundary-correctness-generation-time-encode-paths"
title = "Source-owner review for testing.extreme-value-boundary-correctness-generation-time-encode-paths."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.extreme-value-boundary-correctness-generation-time-encode-paths.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-extreme-value-boundary-correctness-generation-time-encode-paths-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.file-listing-json-schema-root-values-flag-per"
title = "Source-owner review for testing.file-listing-json-schema-root-values-flag-per."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.file-listing-json-schema-root-values-flag-per.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-file-listing-json-schema-root-values-flag-per-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.finding-s-defect-side-attribution-recorded-narrative-plausibility"
title = "Source-owner review for testing.finding-s-defect-side-attribution-recorded-narrative-plausibility."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.finding-s-defect-side-attribution-recorded-narrative-plausibility.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-finding-s-defect-side-attribution-recorded-narrative-plausibility-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.fixture-models-obsolete-user-contract-stays-green-real"
title = "Source-owner review for testing.fixture-models-obsolete-user-contract-stays-green-real."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.fixture-models-obsolete-user-contract-stays-green-real.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-fixture-models-obsolete-user-contract-stays-green-real-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.fixture-s-verdict-accident-cargo-feature-unification-harness"
title = "Source-owner review for testing.fixture-s-verdict-accident-cargo-feature-unification-harness."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.fixture-s-verdict-accident-cargo-feature-unification-harness.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-fixture-s-verdict-accident-cargo-feature-unification-harness-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]
tiers = ["local"]

[[record]]
id = "testing.source-observation.fixture-shape-evicted-over-known-defect-stale-guard"
title = "Source-owner review for testing.fixture-shape-evicted-over-known-defect-stale-guard."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.fixture-shape-evicted-over-known-defect-stale-guard.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-fixture-shape-evicted-over-known-defect-stale-guard-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.full-n-flag-powerset-pict-pairwise"
title = "Source-owner review for testing.full-n-flag-powerset-pict-pairwise."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.full-n-flag-powerset-pict-pairwise.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-full-n-flag-powerset-pict-pairwise-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:declined-boundaries", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.gate-cache-all-hit-cost"
title = "Source-owner review for testing.gate-cache.all-hit-cost."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.gate-cache.all-hit-cost.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-gate-cache-all-hit-cost-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:operational-watches", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.gate-cache-closure-audit-structurally-blind-gate-authored"
title = "Source-owner review for testing.gate-cache-closure-audit-structurally-blind-gate-authored."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.gate-cache-closure-audit-structurally-blind-gate-authored.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-gate-cache-closure-audit-structurally-blind-gate-authored-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.gate-cache-closure-audit-traced-set-extension"
title = "Source-owner review for testing.gate-cache.closure-audit-traced-set-extension."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.gate-cache.closure-audit-traced-set-extension.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-gate-cache-closure-audit-traced-set-extension-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:operational-watches", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.gate-cache-coverage-extensions"
title = "Source-owner review for testing.gate-cache.coverage-extensions."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.gate-cache.coverage-extensions.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-gate-cache-coverage-extensions-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:operational-watches", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.gate-exists-registry-nowhere-tests-readme-md-hand"
title = "Source-owner review for testing.gate-exists-registry-nowhere-tests-readme-md-hand."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.gate-exists-registry-nowhere-tests-readme-md-hand.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-gate-exists-registry-nowhere-tests-readme-md-hand-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]
tiers = ["full"]

[[record]]
id = "testing.source-observation.gate-script-health-cache-staleness-key-fresh-worktree"
title = "Source-owner review for testing.gate-script-health-cache-staleness-key-fresh-worktree."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.gate-script-health-cache-staleness-key-fresh-worktree.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-gate-script-health-cache-staleness-key-fresh-worktree-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.generated-local-collision-general-mangling-remedy"
title = "Source-owner review for testing.generated-local-collision.general-mangling-remedy."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.generated-local-collision.general-mangling-remedy.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-generated-local-collision-general-mangling-remedy-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.generated-local-collision-scope-wide-probe"
title = "Source-owner review for testing.generated-local-collision.scope-wide-probe."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.generated-local-collision.scope-wide-probe.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-generated-local-collision-scope-wide-probe-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.grammar-fuzzer-arbitrary-supported-ast"
title = "Source-owner review for testing.grammar-fuzzer.arbitrary-supported-ast."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.grammar-fuzzer.arbitrary-supported-ast.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-grammar-fuzzer-arbitrary-supported-ast-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.grammar-fuzzer-batch-masking-detector"
title = "Source-owner review for testing.grammar-fuzzer.batch-masking-detector."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.grammar-fuzzer.batch-masking-detector.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-grammar-fuzzer-batch-masking-detector-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.grammar-fuzzer-real-world-corpus-differential"
title = "Source-owner review for testing.grammar-fuzzer.real-world-corpus-differential."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.grammar-fuzzer.real-world-corpus-differential.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-grammar-fuzzer-real-world-corpus-differential-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
title = "Source-owner review for testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-hand-enumerated-ban-pattern-list-authority-lives-gitignored-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
title = "Source-owner review for testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-hand-maintained-source-dependency-mapping-manifest-changeset-silently-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.json2ts-artifact-doc-comments-naming-undefined"
title = "Source-owner review for testing.json2ts-artifact-doc-comments-naming-undefined."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.json2ts-artifact-doc-comments-naming-undefined.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-json2ts-artifact-doc-comments-naming-undefined-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.lint-doc-citations-accepts-docs-heading-durable-citation"
title = "Source-owner review for testing.lint-doc-citations-accepts-docs-heading-durable-citation."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.lint-doc-citations-accepts-docs-heading-durable-citation.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-lint-doc-citations-accepts-docs-heading-durable-citation-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.logic-tool-emits-source-text-outside-layer-reasons"
title = "Source-owner review for testing.logic-tool-emits-source-text-outside-layer-reasons."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.logic-tool-emits-source-text-outside-layer-reasons.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-logic-tool-emits-source-text-outside-layer-reasons-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.machine-readable-ownership-co-owned-features-std-list"
title = "Source-owner review for testing.machine-readable-ownership-co-owned-features-std-list."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.machine-readable-ownership-co-owned-features-std-list.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-machine-readable-ownership-co-owned-features-std-list-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.maintainer-ruling-force-convenience-cbor-bytes-door-turns"
title = "Source-owner review for testing.maintainer-ruling-force-convenience-cbor-bytes-door-turns."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.maintainer-ruling-force-convenience-cbor-bytes-door-turns.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-maintainer-ruling-force-convenience-cbor-bytes-door-turns-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.make-fixture-comment-s-claimed-wire-bytes-executable"
title = "Source-owner review for testing.make-fixture-comment-s-claimed-wire-bytes-executable."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.make-fixture-comment-s-claimed-wire-bytes-executable.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-make-fixture-comment-s-claimed-wire-bytes-executable-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.making-type-choice-path-reject-duplicated-explicit-name"
title = "Source-owner review for testing.making-type-choice-path-reject-duplicated-explicit-name."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.making-type-choice-path-reject-duplicated-explicit-name.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-making-type-choice-path-reject-duplicated-explicit-name-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:declined-boundaries", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.matrix-s-note-names-panic-site-aborts-nothing"
title = "Source-owner review for testing.matrix-s-note-names-panic-site-aborts-nothing."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.matrix-s-note-names-panic-site-aborts-nothing.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-matrix-s-note-names-panic-site-aborts-nothing-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.mechanical-layers-two-review-owned-design-rules-tests"
title = "Source-owner review for testing.mechanical-layers-two-review-owned-design-rules-tests."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.mechanical-layers-two-review-owned-design-rules-tests.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-mechanical-layers-two-review-owned-design-rules-tests-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.message-s-level-classification-review-owned-two-ways"
title = "Source-owner review for testing.message-s-level-classification-review-owned-two-ways."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.message-s-level-classification-review-owned-two-ways.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-message-s-level-classification-review-owned-two-ways-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.msrv-declaration-os-matrix-generated-code"
title = "Source-owner review for testing.msrv-declaration-os-matrix-generated-code."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.msrv-declaration-os-matrix-generated-code.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-msrv-declaration-os-matrix-generated-code-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:declined-boundaries", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.multifile-reference-position-coverage-two-position-keyed-escapes"
title = "Source-owner review for testing.multifile-reference-position-coverage-two-position-keyed-escapes."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.multifile-reference-position-coverage-two-position-keyed-escapes.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-multifile-reference-position-coverage-two-position-keyed-escapes-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.nothing-asserts-how-many-times-rejection-message-emitted"
title = "Source-owner review for testing.nothing-asserts-how-many-times-rejection-message-emitted."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.nothing-asserts-how-many-times-rejection-message-emitted.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-nothing-asserts-how-many-times-rejection-message-emitted-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.nothing-asserts-two-ir-sites-cannot-mint-same"
title = "Source-owner review for testing.nothing-asserts-two-ir-sites-cannot-mint-same."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.nothing-asserts-two-ir-sites-cannot-mint-same.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-nothing-asserts-two-ir-sites-cannot-mint-same-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.observed-baseline-comments-beside-gate-floors-rot-silently"
title = "Source-owner review for testing.observed-baseline-comments-beside-gate-floors-rot-silently."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.observed-baseline-comments-beside-gate-floors-rot-silently.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-observed-baseline-comments-beside-gate-floors-rot-silently-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.opt-rfc-faithful-total-cbor-json-rendering-json"
title = "Source-owner review for testing.opt-rfc-faithful-total-cbor-json-rendering-json."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.opt-rfc-faithful-total-cbor-json-rendering-json.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-opt-rfc-faithful-total-cbor-json-rendering-json-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.ownership-journal-making-dropped-std-forward-dep-converge"
title = "Source-owner review for testing.ownership-journal-making-dropped-std-forward-dep-converge."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.ownership-journal-making-dropped-std-forward-dep-converge.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-ownership-journal-making-dropped-std-forward-dep-converge-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:declined-boundaries", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.panic-site-un-shadowing-converting-shallow-panic-graceful"
title = "Source-owner review for testing.panic-site-un-shadowing-converting-shallow-panic-graceful."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.panic-site-un-shadowing-converting-shallow-panic-graceful.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-panic-site-un-shadowing-converting-shallow-panic-graceful-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.parallel-constructor-fixture-diversity-parser-over-external-input"
title = "Source-owner review for testing.parallel-constructor-fixture-diversity-parser-over-external-input."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.parallel-constructor-fixture-diversity-parser-over-external-input.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-parallel-constructor-fixture-diversity-parser-over-external-input-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.prettyplease-shelling-rustfmt"
title = "Source-owner review for testing.prettyplease-shelling-rustfmt."
projection_group = "pending-maintainer"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.prettyplease-shelling-rustfmt.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-prettyplease-shelling-rustfmt-file-heading-h527e280bc2bd81c1094c49f706d73fc6341eae61070b511ed7e7c7a2114aac2d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:pending-maintainer", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.probe-component-face-s-js-surface-axes-js"
title = "Source-owner review for testing.probe-component-face-s-js-surface-axes-js."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.probe-component-face-s-js-surface-axes-js.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-probe-component-face-s-js-surface-axes-js-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.process-boundary-another-profile-escape"
title = "Source-owner review for testing.process-boundary.another-profile-escape."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.process-boundary.another-profile-escape.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-process-boundary-another-profile-escape-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.process-boundary-rustc-after-panic-fix"
title = "Source-owner review for testing.process-boundary.rustc-after-panic-fix."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.process-boundary.rustc-after-panic-fix.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-process-boundary-rustc-after-panic-fix-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.process-boundary-rustfmt-pre-rustc"
title = "Source-owner review for testing.process-boundary.rustfmt-pre-rustc."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.process-boundary.rustfmt-pre-rustc.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-process-boundary-rustfmt-pre-rustc-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.profile-driven-compile-gate-covers-compositions-profiles-specs"
title = "Source-owner review for testing.profile-driven-compile-gate-covers-compositions-profiles-specs."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.profile-driven-compile-gate-covers-compositions-profiles-specs.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-profile-driven-compile-gate-covers-compositions-profiles-specs-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.project-json-gen-documents-run-test-fixture-writes"
title = "Source-owner review for testing.project-json-gen-documents-run-test-fixture-writes."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.project-json-gen-documents-run-test-fixture-writes.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-project-json-gen-documents-run-test-fixture-writes-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.propagating-deliberately-unpublished-type-s-intent-json-ts"
title = "Source-owner review for testing.propagating-deliberately-unpublished-type-s-intent-json-ts."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.propagating-deliberately-unpublished-type-s-intent-json-ts.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-propagating-deliberately-unpublished-type-s-intent-json-ts-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
title = "Source-owner review for testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-reason-keyed-rejection-evidence-reject-catalogs-proven-near-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.referencing-context-measured-wrapping-deep-honored-base-shape"
title = "Source-owner review for testing.referencing-context-measured-wrapping-deep-honored-base-shape."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.referencing-context-measured-wrapping-deep-honored-base-shape.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-referencing-context-measured-wrapping-deep-honored-base-shape-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.representation-changing-directive-goes-live-new-container-ship"
title = "Source-owner review for testing.representation-changing-directive-goes-live-new-container-ship."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.representation-changing-directive-goes-live-new-container-ship.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-representation-changing-directive-goes-live-new-container-ship-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.rule-trailing-directive-classification"
title = "Source-owner review for testing.rule-trailing.directive-classification."
projection_group = "pending-maintainer"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.rule-trailing.directive-classification.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-rule-trailing-directive-classification-file-heading-h527e280bc2bd81c1094c49f706d73fc6341eae61070b511ed7e7c7a2114aac2d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:pending-maintainer", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.ruling-premise-gate-x-stays-green-must-probed"
title = "Source-owner review for testing.ruling-premise-gate-x-stays-green-must-probed."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.ruling-premise-gate-x-stays-green-must-probed.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-ruling-premise-gate-x-stays-green-must-probed-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]
tiers = ["fast"]

[[record]]
id = "testing.source-observation.run-local-values-gate-cache-key-material-proven"
title = "Source-owner review for testing.run-local-values-gate-cache-key-material-proven."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.run-local-values-gate-cache-key-material-proven.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-run-local-values-gate-cache-key-material-proven-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.rustfmt-seam-error-leg-still-witness-needs-subprocess"
title = "Source-owner review for testing.rustfmt-seam-error-leg-still-witness-needs-subprocess."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.rustfmt-seam-error-leg-still-witness-needs-subprocess.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-rustfmt-seam-error-leg-still-witness-needs-subprocess-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.schema-name-collision-loser-no-row-same-id"
title = "Source-owner review for testing.schema-name.collision-loser-no-row-same-id."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.schema-name.collision-loser-no-row-same-id.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-schema-name-collision-loser-no-row-same-id-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.schema-name-cross-crate-same-id-collision"
title = "Source-owner review for testing.schema-name.cross-crate-same-id-collision."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.schema-name.cross-crate-same-id-collision.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-schema-name-cross-crate-same-id-collision-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.schema-name-two-rowless-types-collide"
title = "Source-owner review for testing.schema-name.two-rowless-types-collide."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.schema-name.two-rowless-types-collide.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-schema-name-two-rowless-types-collide-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.scratch-retention-reopening-signal"
title = "Source-owner review for testing.scratch-retention.reopening-signal."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.scratch-retention.reopening-signal.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-scratch-retention-reopening-signal-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:operational-watches", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.stale-known-limitation-prose-surviving-fix-finding-ledgered"
title = "Source-owner review for testing.stale-known-limitation-prose-surviving-fix-finding-ledgered."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.stale-known-limitation-prose-surviving-fix-finding-ledgered.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-stale-known-limitation-prose-surviving-fix-finding-ledgered-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]
tiers = ["full"]

[[record]]
id = "testing.source-observation.std-generated-crates"
title = "Source-owner review for testing.std-generated-crates."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.std-generated-crates.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-std-generated-crates-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.synthesized-name-referenced-never-minted-e0425-flavor"
title = "Source-owner review for testing.synthesized-name-referenced-never-minted-e0425-flavor."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.synthesized-name-referenced-never-minted-e0425-flavor.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-synthesized-name-referenced-never-minted-e0425-flavor-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.tag-set-inline-anonymous-choice-boundary"
title = "Source-owner review for testing.tag-set.inline-anonymous-choice-boundary."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.tag-set.inline-anonymous-choice-boundary.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-tag-set-inline-anonymous-choice-boundary-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.tier-memory-spend-measurements"
title = "Source-owner review for testing.tier-memory.spend-measurements."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.tier-memory.spend-measurements.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-tier-memory-spend-measurements-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:operational-watches", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
title = "Source-owner review for testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-tier-s-disk-bandwidth-unmeasured-anything-scratch-floor-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:operational-watches", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.tier-s-peak-memory-bounded-arithmetic-over-assumed"
title = "Source-owner review for testing.tier-s-peak-memory-bounded-arithmetic-over-assumed."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.tier-s-peak-memory-bounded-arithmetic-over-assumed.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-tier-s-peak-memory-bounded-arithmetic-over-assumed-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:operational-watches", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
title = "Source-owner review for testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-tree-wide-sweeps-over-gitignored-regeneration-targets-count-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.twin-implementation-comment-ast-change-cadence"
title = "Source-owner review for testing.twin-implementation.comment-ast-change-cadence."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.twin-implementation.comment-ast-change-cadence.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-twin-implementation-comment-ast-change-cadence-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.twin-implementation-read-caught-instance-trigger"
title = "Source-owner review for testing.twin-implementation.read-caught-instance-trigger."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.twin-implementation.read-caught-instance-trigger.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-twin-implementation-read-caught-instance-trigger-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
title = "Source-owner review for testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-twin-repo-implementations-semantic-drifting-apart-emission-spellings-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]
tiers = ["fast", "local"]

[[record]]
id = "testing.source-observation.unused-imports-generated-crates-trait-import-class-name"
title = "Source-owner review for testing.unused-imports-generated-crates-trait-import-class-name."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.unused-imports-generated-crates-trait-import-class-name.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-unused-imports-generated-crates-trait-import-class-name-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.value-anchor-rot-field-addition-delivery-grows-existing"
title = "Source-owner review for testing.value-anchor-rot-field-addition-delivery-grows-existing."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.value-anchor-rot-field-addition-delivery-grows-existing.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-value-anchor-rot-field-addition-delivery-grows-existing-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.verify-ts-warm-up-cargo-test-exit-attributed"
title = "Source-owner review for testing.verify-ts-warm-up-cargo-test-exit-attributed."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.verify-ts-warm-up-cargo-test-exit-attributed.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-verify-ts-warm-up-cargo-test-exit-attributed-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:operational-watches", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.wac-composed-component-transpiled-jco-loses-cross-instance"
title = "Source-owner review for testing.wac-composed-component-transpiled-jco-loses-cross-instance."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.wac-composed-component-transpiled-jco-loses-cross-instance.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-wac-composed-component-transpiled-jco-loses-cross-instance-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:standing-system", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.wasm-door-vocabulary-reopening-signal"
title = "Source-owner review for testing.wasm-door-vocabulary.reopening-signal."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.wasm-door-vocabulary.reopening-signal.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-wasm-door-vocabulary-reopening-signal-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:next-priority", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.wasm32-link-floor-deferred-reject-twin"
title = "Source-owner review for testing.wasm32-link-floor-deferred-reject-twin."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.wasm32-link-floor-deferred-reject-twin.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-wasm32-link-floor-deferred-reject-twin-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.workspace-mode-wasm32-wasip2-build-gate-both-faces"
title = "Source-owner review for testing.workspace-mode-wasm32-wasip2-build-gate-both-faces."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.workspace-mode-wasm32-wasip2-build-gate-both-faces.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-workspace-mode-wasm32-wasip2-build-gate-both-faces-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.workspace-wasip2-reopening-signal"
title = "Source-owner review for testing.workspace-wasip2.reopening-signal."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.workspace-wasip2.reopening-signal.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-workspace-wasip2-reopening-signal-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:deferred-features", "testing-roadmap-source"]

[[record]]
id = "testing.source-observation.wrapper-name-user-set-name-synthesized-collection-wrapper"
title = "Source-owner review for testing.wrapper-name-user-set-name-synthesized-collection-wrapper."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "evidence"
summary_md = """Source-owner review for testing.wrapper-name-user-set-name-synthesized-collection-wrapper.
"""
evidence_kind = "source_read"
claim_md = """The frozen owner detail was reviewed to classify its lifecycle and preserve its authored trigger and exclusions; this review does not establish whether an external or unenumerated event occurred.
"""
evidence_verdict = "confirmed"
freshness = "as_of"
reference_ids = ["ref-testing-source-observation-wrapper-name-user-set-name-synthesized-collection-wrapper-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"]
at_commit = "9246c9a2d65902e0a2f46bae006f19fe57d915ae"
observed_at = "2026-08-12"
unprobed_remainder_md = """No consumer, issue, release, repository, timing, or incident universe was exhaustively queried; transition evaluation remains unknown.
"""

[record.payload.scope]
surfaces = ["testing-roadmap-owner:declined-boundaries", "testing-roadmap-source"]

[[record]]
id = "testing.stale-known-limitation-prose-surviving-fix-finding-ledgered"
title = "Stale \"known limitation\" prose surviving its fix — a finding ledgered in TWO homes where the fixing commit prunes only one."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Stale \"known limitation\" prose surviving its fix — a finding ledgered in TWO homes where the fixing commit prunes only one.
"""
detail_md = """- **Stale \"known limitation\" prose surviving its fix — a finding ledgered in TWO homes where the
  fixing commit prunes only one.** First instance (read-caught during the facade-pin delivery, not
  by any gate): the extern-only-scope undeclared-module finding was ledgered both in
  `cddl-matrix/ROADMAP.md` and as a hand \"Known limitation\" paragraph in
  `docs/docs/comment_dsl.mdx`; the fix pruned the roadmap entry and minted the pin
  (`integration_extern_only_scope_declared_in_root`) but the user-docs paragraph survived — through
  a later docs pass over the same file — until the facade compile pin
  (`facade_composition_compiles`) delivery pruned it. SECOND instance SET, a DIFFERENT home
  (read-caught during the findings-sweep delivery): the corpus findings ledger
  (`cddl-matrix/annotations/corpus/cddl_codegen.toml`) carried four failure-claim `[[finding]]`s /
  sibling `[[note]]`s whose defects had been fixed out from under them (the `bool`-in-type-choice
  E0282, the single-letter-`r` E0574, the inline-group data-loss \"Bug\", and the top-level
  fixed-value \"panics\" prose) — these render into `tests/corpus/COVERAGE.md`, a GENERATED span
  `lint_doc_citations` never scans, so that gate could never have caught them. THIRD instance, a
  distinct rot mode — a NEGATIVE-INVENTORY scan claim (caught at item pickup during the tag-set
  matrix-cell delivery): a `cddl-matrix/ROADMAP.md` bullet asserted \"no current row's example
  matches the recognized shape\" as its coverage-hole premise, and the claim was silently falsified
  the SAME DAY it shipped when the `dsl.duplicates.{reject,preserve}` registration (`5485b2c`)
  landed a matching example (`#6.258([* uint]) / [* uint] ; @duplicates reject`; harmless there —
  that row's verdict was minted with the collapse live, so nothing was actually stale — but the
  prose was wrong from that commit on). The class property: a point-in-time inventory claim
  DANGLES NOTHING — every artifact it names keeps resolving — so no citation lint can ever fire;
  and mechanically re-checking arbitrary prose inventory claims against the live matrix is the
  same claim-semantics boundary check `I` deliberately stops at. No mechanical layer proposed; the
  guard is pickup discipline, which worked here: a roadmap item's scan/inventory premises are
  claims to re-verify against the live tree when the item is picked up (AGENTS.md's
  premise-probing rule — the re-scan is one grep), and the delivery prunes the whole bullet,
  premise included. Authoring rule for future bullets: scope any inventory claim to its evidence
  (\"scanned at <sha>\") or phrase it as the check to re-run, never as a standing fact.
  FOURTH instance, in a home the first three did not reach — a session HANDOFF file, where the
  claim is about VERIFICATION STATE rather than about the tree: a `draft/` worklist's carry-forward
  bullet read \"no cycle-1 commit has been seen by a `full`-tier gate\", true when written and
  falsified as soon as the tier was run. The next cycle's orchestrator propagated the file's line
  into its own report in preference to the fresher fact it had been given directly, which is the
  rot mode worth naming: a handoff doc is READ as current state by construction, so a stale
  standing fact in one is not merely wrong, it OUTRANKS newer evidence in the reader's head. The
  authoring rule above already covers it verbatim (a tier verdict is a point-in-time inventory
  claim, and \"PASSED at `<sha>`\" satisfies it while \"has never run\" does not), so this is the rule
  firing rather than a gap — but note it applies to `draft/` handoff files, which no gate scans and
  which are gitignored, so review is the only reader that can enforce it. The
  SECOND-instance mechanical layer
  is now BUILT for that home: the findings-claims arm (`project_corpus.ts` check `I`, local tier)
  fails a failure-claim finding that names no resolvable tracking pin — current state, including
  its accepted looseness (a resolving citation does not validate claim SEMANTICS, which stay
  review-owned; any backtick token matching `src/tests/` text satisfies resolution — tighten on the
  first vacuous pass observed), is documented in `cddl-matrix/README.md` § \"Gotchas\". Still future:
  the USER-DOCS home has no arm — a parallel limitation-claims arm on `lint_doc_citations` (every
  known-limitation-classed paragraph outside a generated span carries a resolvable tracking
  citation, or folds into a projected span — the matrix north star's \"more docs follow\") remains
  unbuilt, since the two homes have different scanners and neither arm sees the other's prose. That
  half needs its own signal rather than inheriting the parent's second-instance trigger, which four
  recorded instances have already met: on the axis its cost grows along, it is **the count of
  known-limitation paragraphs in `docs/docs/*.mdx` that name no tracking artifact** — measured by the
  next author who greps `docs/` before shipping a fix, which the working rule below already requires
  them to do, so the count arrives as a by-product of work someone is doing anyway. Build the arm
  when that grep returns a set too large to walk by hand; until then the grep IS the arm.
  Working rules meanwhile: a limitation recorded in user docs names its tracking artifact (the
  reject/panic fixture, pin, or ledger entry) in the same paragraph; a fixing session greps `docs/`
  for the limitation's key phrases before shipping; and the prose that CLOSES a stale finding
  attributes the fix by REPRODUCING against the candidate commit's parent (a throwaway worktree +
  the probe is minutes), never by narrative plausibility — the findings sweep's first closure draft
  attributed both compile-bug closures to a plausible-but-wrong commit, falsified only by a
  worktree reproduction at that commit's parent before the wrong story shipped. The
  reproduce-at-parent rule is REVIEW-OWNED by explicit decision, not pending a mechanical layer:
  whether a reproduction happened leaves no machine-checkable artifact, so a detector could only
  check that prose CLAIMS verification — the same claim-semantics boundary check I stops at (same
  disposition class as the declined docs-conformance harness; the reopening signal is a false
  attribution SHIPPING past review, not one being caught by it). Bisect/reproduction mechanics for
  whoever next needs them — chiefly the `git bisect run` exit-convention POLARITY trap (exit 0
  always means the OLD term, so a fix-hunting script that exits 0 on \"works\" is inverted and the
  search returns its own fixed endpoint), plus the specific-error-keyed verdict shape and the
  cheaper reproduce-at-parent/pickaxe-first alternatives — live in
  `draft/bisect-verdict-discipline.md`.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "misleading_docs"
family_classification = "none_reviewed"
control_ids = ["testing.guard.stale-known-limitation-prose-surviving-fix-finding-ledgered"]
transition_ids = ["testing.trigger.stale-known-limitation-prose-surviving-fix-finding-ledgered"]

[[record.source_replacement]]
span_id = "span-record-stale-known-limitation-prose-surviving-fix-finding-ledgered"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.stale-pin-guard-cannot-distinguish-gap-closed-vector"
title = "A stale-pin guard cannot distinguish \"the gap closed\" from \"the vector that proved the gap went blunt\" — and a wholesale re-mint of RANDOMLY GENERATED vectors produces the second while reporting the first."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_operational_watch"
summary_md = """A stale-pin guard cannot distinguish \"the gap closed\" from \"the vector that proved the gap went blunt\" — and a wholesale re-mint of RANDOMLY GENERATED vectors produces the second while reporting the first.
"""
detail_md = """- **A stale-pin guard cannot distinguish \"the gap closed\" from \"the vector that proved the gap went
  blunt\" — and a wholesale re-mint of RANDOMLY GENERATED vectors produces the second while reporting
  the first.** Proven 2026-07-30 (native-float preserve delivery): adding one corpus rule required a
  catalog row, and `verify.ts --mint-decode-corpus` was run in its FULL-refresh form, which re-rolled
  every row's ruby-generated vectors (3817-line diff for one new row). `corpus_decode_replay` then
  failed with `ENCODING_VARIANT_SKIP names (table_preserve.md, reverse_maps) … the gap closed —
  remove the entry (stale pin)`. It had not: that pin exists because reversing a `@duplicates
  preserve` pair-map's entry order changes the VALUE, and the re-rolled vector set had replaced
  HEAD's multi-entry maps (`8200a2…`, `8200a3…`) with the empty map `8200a0`, which reverses to
  itself. Taking the guard at its word would have deleted a live order-collapse tripwire AND left the
  coverage silently gone — the guard's message names exactly one of its two possible causes, and it
  names the wrong one whenever the data moved. The mint's own `--only=<id|fixture>` form is the fix
  and the working rule: **re-mint the row you changed, never the catalog** (108-line diff, other rows'
  vectors preserved verbatim, `table_preserve.md` still carrying multi-entry maps). The mechanical
  layer on a second instance: have the mint refuse a full refresh that REDUCES any row's vector-shape
  diversity (it already classifies vectors by shape — `vectorShapeClass` — so an empty-map-only row
  where the committed one had multi-entry maps is detectable at mint time, before the pin ever goes
  stale). Trigger: a second pin retired or nearly-retired by vector churn rather than by a fix. (The
  order-sensitivity pins that produced the proven instance are gone — those exemptions are now
  derived from each row's `spec` and carry no stale guard, precisely because \"this vector's
  reordering was the identity\" is not evidence about the row. Every remaining stale-guarded pin is
  still exposed to the class, which is why the watch stands.)

"""
watch_state = "watching"
signature_md = """A stale-pin guard claims closure after a mint reduced the pinned row's vector-shape diversity.
"""
response_md = """Restore the proven vector, remint only the changed row, and re-run the owning replay gate.
"""
escalation_transition_id = "testing.trigger.stale-pin-guard-cannot-distinguish-gap-closed-vector"
retirement_semantics_md = """Retire only when that guard covers every randomized catalog mint.
"""

[[record.payload.capture_step]]
step_id = "signature"
capture_md = """Preserve pre/post row vectors, shape classes, mint command, stale-pin output, and single-row rerun.
"""

[[record.payload.capture_step]]
step_id = "scope"
capture_md = """Record the full untruncated log or consumer report, command, full commit, date, environment, relevant versions, and the explicitly unprobed remainder; never cite a disposable draft log as durable evidence.
"""

[[record.source_replacement]]
span_id = "span-record-stale-pin-guard-cannot-distinguish-gap-closed-vector"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.std-generated-crates"
title = "no_std generated crates."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """no_std generated crates.
"""
detail_md = """- **no_std generated crates.** cbor_event supports no_std since 3.0.0, so a CLI flag (`cli.rs` +
  docs) emitting no_std-compatible crates is possible; the static runtime and generated code would
  need their own std-usage sweep (collections, error impls). The mechanical layer when a consumer
  asks: a no_std cross-compile gate over one generated crate.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "compile_failure"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.std-generated-crates"]

[[record.source_replacement]]
span_id = "span-record-std-generated-crates"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.synthesized-name-referenced-never-minted-e0425-flavor"
title = "Synthesized-name residual: the referenced-but-never-minted (E0425) flavor."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Synthesized-name residual: the referenced-but-never-minted (E0425) flavor.
"""
detail_md = """- **Synthesized-name residual: the referenced-but-never-minted (E0425) flavor.** The generator mints
  structural wasm idents (loose `{Elem}List` / `Map{K}To{V}` builders, restricted `NonEmpty*`
  wrappers, table `keys()` list wrappers) whose interactions with USER rule names and with EACH OTHER
  are a NAME-shaped axis the shape catalogs (wasm-ABI matrix, corpus, recombination) never spell. The
  two flavors of the \"generation exits 0 but the wasm crate doesn't compile\" class that live at
  generation time are owned by standing gates: the duplicate-ident (E0428) redefinition flavor by the
  generation-time backstop at the `generated_files` seam, and the dedup/shape-mismatch (E0277) flavor
  by `synthesized_name_interaction_sweep` (families × interactions → graceful rejection with the
  colliding ident named, or a compiling batched wasm crate; see tests/README.md § \"Synthesized-name
  interaction sweep + duplicate-ident backstop\"). The remaining flavor is E0425: an emitter that
  REFERENCES a synthesized wrapper name no mint path emits. It stays owned by the compile gates
  (`wasm_matrix_compiles` + the full-tier recombination wasm leg). If it recurs outside their reach,
  escalate to a scoped generation-time invariant — \"every synthesized wrapper name an emitter
  references must be in the minted set\" — the reference-side complement of the emission-side
  duplicate-ident backstop. (A fourth, CROSS-CRATE flavor — duplicate symbols at link under
  `--extern-wrapper-index`, invisible to any in-crate layer — is owned by the wrapper-participation
  grid's per-mode wasm32 link floors, `tests/README.md`
  § \"The wrapper-participation grid\".)
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.synthesized-name-referenced-never-minted-e0425-flavor"]
transition_ids = ["testing.trigger.synthesized-name-referenced-never-minted-e0425-flavor"]

[[record.source_replacement]]
span_id = "span-record-synthesized-name-referenced-never-minted-e0425-flavor"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.synthetic-protocol-controls-component-probe-host-s-defensive"
title = "Synthetic-protocol controls for the component probe host's defensive verdict arms — an arm no generated component can reach never gets red-proven."
projection_group = "next-priority"
legacy_aliases = ["Next work 23"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Synthetic-protocol controls for the component probe host's defensive verdict arms — an arm no generated component can reach never gets red-proven.
"""
detail_md = """23. **Synthetic-protocol controls for the component probe host's defensive verdict arms — an arm
    no generated component can reach never gets red-proven.** The matrix's generic wasmtime host
    (`cddl-matrix/component-probe-host/`) maps boundary outcomes to verdict tokens
    (`ok`/`err`/`mismatch`/`trap`), and the execution leg's red-first controls proved every arm a
    GENERATED component can drive. The arms it structurally cannot reach — a decode door
    returning `Ok` with no payload, a door whose result is not a `result` at all — went live
    without ever firing, and one of them shipped mapped to the WRONG token (`Ok(None)` emitted
    `ok`, a false-pass channel for the byte-equality assertion; caught by orchestrator design
    review, fixed in the thirteen-row widening commit). The class: a generic driver grows
    defensive arms for protocol shapes its own fixtures cannot produce, so the red-first
    discipline that guards every reachable arm silently exempts exactly the arms most likely to
    be wrong. The systematic layer: unit-style controls over the host's verdict MAPPING on
    synthetic inputs (factor the outcome→token mapping into a function the host's own
    `cargo test` drives with every arm, including the unreachable ones), so a future arm added
    wrong fails the host's build, not a review. Small — the mapping is one match today.

"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.synthetic-protocol-controls-component-probe-host-s-defensive's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""

[[record.source_replacement]]
span_id = "span-record-synthetic-protocol-controls-component-probe-host-s-defensive"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.tag-set.inline-anonymous-choice-boundary"
title = "Inline/anonymous two-arm choices are not recognized."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Inline/anonymous two-arm choices are not recognized.
"""
detail_md = """  - *Inline/anonymous two-arm choices are not recognized.* Recognition lives at the
    `parse_type_choices` named-rule seam, so an inline `[x: #6.258([* uint]) / [* uint]]` stays a
    two-variant enum. Remedy when it bites: run the recognition on anonymous choices too.
"""
work_state = "deferred"
work_intent = "build_capability"
work_kind = "feature"
risk = "valid_rejection"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.tag-set-inline-anonymous-choice-boundary"]

[[record.source_replacement]]
span_id = "span-part-part-inline-anonymous-two-arm-choices-recognized"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.tag-set.non-idiom-generic-boundary"
title = "Non-idiom choice-BODIED generic defs are refused, not supported."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """Non-idiom choice-BODIED generic defs are refused, not supported.
"""
detail_md = """  - *Non-idiom choice-BODIED generic defs are refused, not supported.* The idiom is the ONLY
    choice-bodied generic def the generator can monomorphize — the collapse fires at parse time,
    before the generic machinery, and produces one struct to substitute into. Arms that do not
    satisfy the collapse condition (e.g. mismatched bounds `xs<a0> = #6.258([+ a0]) / [* a0]`) would
    mint a union enum the machinery has no way to thread parameters through, so the definition is
    rejected at parse naming the idiom (`robustness_tests::unsupported_generic_def_bodies_reject_
    gracefully`). Remedy when the refusal bites: teach the generic machinery to substitute into an
    enum's arms, which is the same work a generic GROUP-choice def needs.
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.tag-set.non-idiom-generic-boundary.
"""
authority_reference_id = "ref-testing-tag-set-non-idiom-generic-boundary-authority"
permanence = "permanent"

[[record.source_replacement]]
span_id = "span-part-part-non-idiom-choice-bodied-generic-defs-refused-supported"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.three-defect-classes-process-test-see-sharing-remedy"
title = "Three defect classes that no IN-PROCESS test can see, sharing one remedy: extend the corpus's INPUT."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """Three defect classes that no IN-PROCESS test can see, sharing one remedy: extend the corpus's INPUT.
"""
detail_md = """- **Three defect classes that no IN-PROCESS test can see, sharing one remedy: extend the corpus's
  INPUT.** `api::generated_strings` is the library API every in-process suite drives — the snapshot
  corpus, the robustness/panic/reject catalogs, `wasm_api_parity`, and
  `all_supported_constructs_generate_all_profiles`. It returns emitted source as STRINGS —
  post-rustfmt (its own doc comment; the merge step formats every emitted file, so a
  rustfmt-fatal emission IS an in-process generation error) — but it never runs `export()`'s
  disk-write tail, and it never hands anything to rustc. So a defect landing at either of those
  seams — or landing
  only under a profile no committed vehicle generates the shape under — is invisible to all of them
  at once, and a catalog row recording `ok` discharges exactly one claim: generation exited 0.
  The remedy for all three is an INPUT extension, not a new layer. `tests/corpus/*.cddl` is the input
  of `feature_corpus_compiles`, which shells the real CLI per cell (so `export()` runs, rustfmt
  included — a rustfmt-rejected emission is a non-zero exit and a red cell) and then `cargo check`s
  the rust + wasm + json-gen crates under all three profiles, with
  `feature_corpus_roundtrips_nondefault_profiles` (full tier) executing the emitted round-trips under
  preserve and json. Both gates are described as current state in `tests/README.md` § \"Generated-test
  harness (`--emit-tests`, `src/emit_tests.rs`)\". So the work each class implies is: promote the
  shape into `tests/corpus/` — never build an export-test or per-profile harness beside it.
  **Correction to this entry's own earlier remedy, probed rather than reasoned.** Two of these
  classes previously named `all_supported_constructs_generate_all_profiles` as \"a disk-writing gate
  that already exists, limited only by taking `supported.cddl` as its input\". Both halves are false:
  it reads `tests/matrix_supported/`, and it calls `api::generated_strings` in-process against an
  `--output` path it never writes (its own doc comment says \"generation-only … no compile\"). Routing
  rows through it would have reproduced precisely the blindness these entries exist to record — which
  is why a cited gate's mechanism is probed before a remedy is built on it.
  The three classes stay distinct in WHERE the failure lands, and each keeps its own trigger:
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-three-defect-classes-process-test-see-sharing-remedy-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.three-defect-classes-process-test-see-sharing-remedy's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record.source_replacement]]
span_id = "span-record-three-defect-classes-process-test-see-sharing-remedy"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.tier-memory.spend-measurements"
title = "Spend the measurements."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Spend the measurements.
"""
detail_md = """  - **Spend the measurements.** The 4 GiB slot constant and the one-permit nested bound are
    deliberately pessimistic prices for unmeasured quantities, and the pessimism costs real wall
    time (nested children serialize per gate). Once accumulated `memory-peaks.jsonl` rows from
    real tiers bound a slot's true peak, re-derive both constants from the measurement and record
    the basis where the constants live.
"""
work_state = "blocked"
work_intent = "build_system"
work_kind = "infrastructure"
risk = "resource_exhaustion"
family_classification = "none_reviewed"
blocker_md = """No committed, environment-scoped per-slot peak authority exists outside the disposable sampler ledgers.
"""
transition_ids = ["testing.predicate.tier-memory-durable-measurements"]

[[record.source_replacement]]
span_id = "span-part-part-spend-measurements"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
title = "A tier's DISK BANDWIDTH is unmeasured by anything — the scratch floor is a capacity check, not a rate one, and no incident has ever been attributable either way."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A tier's DISK BANDWIDTH is unmeasured by anything — the scratch floor is a capacity check, not a rate one, and no incident has ever been attributable either way.
"""
detail_md = """- **A tier's DISK BANDWIDTH is unmeasured by anything — the scratch floor is a capacity check,
  not a rate one, and no incident has ever been attributable either way.** The third sibling of
  the two entries above, narrower than both: it names a candidate factor, not a proven one. Up to
  `CHECK_JOBS` gates each drive a nested cargo against its own target dir — separate directories,
  one device — and nothing measures or bounds the write RATE; the device's sustained ceiling is
  an ENVIRONMENT property the tier does not observe, lowest on virtual-disk hosts (WSL2 mounts
  `/` and `/tmp` on one virtual device, far below bare metal). This entry first shipped
  (2026-08-04) reading a whole-machine freeze under ~3 GB/s sustained IO as a bandwidth
  saturation; the same day's root-cause landed the attribution elsewhere — the freezes were
  MEMORY (swap thrashing; three concrete budget holes, closed by `d5d43bba`), with the disk
  traffic as their symptom — so what survives here is exactly the unmeasured-factor claim, plus
  the two facts any future incident inherits: a saturated device is a WHOLE-MACHINE stall, never
  a gate failure, so no PASSIVE gate can observe it (the signal is external — a human at a dead
  terminal; kernel-enforced containment, the operator measure the sibling memory entry records,
  is the one thing that can turn a stall into an in-gate failure), and the post-`d5d43bba` budget
  logs its memory basis per batch — and the sibling entry's sampler now records per-run peaks and
  the MemAvailable floor — so the
  NEXT stall can at least be split into \"memory bound was wrong again\" vs \"memory healthy,
  something else saturated\". Candidate instruments if that second reading ever occurs, recorded
  with costs rather than built (no portable measurement of a device's sustained-write ceiling
  exists, and a fixed cap is what `CHECK_JOBS` already is): an IO axis on the sibling memory
  entry's sampler (which exists and walks `/proc` per tick already) — per-run `/proc/diskstats`
  write-rate deltas on the same ticks, recorded beside the
  durations, reported and never asserted — which is what would turn the next incident's
  attribution from forensics into a lookup; and only downstream of such an attribution, a
  rate-aware degrade (an environment-derived `CHECK_JOBS` default for virtual-disk hosts).
  Build signal: a whole-machine stall with the memory budget demonstrably healthy (its per-batch
  basis lines green in the run's own log) — the first bandwidth-attributed incident there has
  ever been, which buys the sampler's IO axis at minimum.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "resource_exhaustion"
family_classification = "none_reviewed"
control_ids = ["testing.guard.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"]
transition_ids = ["testing.trigger.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"]

[[record.source_replacement]]
span_id = "span-record-tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.tier-s-peak-memory-bounded-arithmetic-over-assumed"
title = "A tier's PEAK MEMORY is bounded by arithmetic over ASSUMED constants — the sampler now measures the real peak, and replacing the assumptions with its measurements is the open work."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_incident"
summary_md = """A tier's PEAK MEMORY is bounded by arithmetic over ASSUMED constants — the sampler now measures the real peak, and replacing the assumptions with its measurements is the open work.
"""
detail_md = """- **A tier's PEAK MEMORY is bounded by arithmetic over ASSUMED constants — the sampler now
  measures the real peak, and replacing the assumptions with its measurements is the open work.**
  The sibling of the disk entry above, and the proven class: a full disk fails a gate, an
  overcommitted memory cap takes the whole machine — three times now (a ~10-minute freeze that
  produced the first bound, then two ~1–1.5 h freezes at 100 % memory and swap, 2026-08-04, that
  went through every check the first fix installed). The second incident's root-cause (`d5d43bba`)
  closed three budget holes the arithmetic had: the basis was MemTotal where the question is
  MemAvailable (now re-measured per batch), the sequential gate path set no bound at all (so
  `CHECK_JOBS=1` — the cautious setting — routed everything through the unbounded path), and bare
  `cargo test`/`build` invocations outside the runner had no bound anywhere (now floored at
  `[build] jobs = 4` by the repo `.cargo/config.toml`). Two further holes closed since, both
  found by asking what still multiplies concurrency after those fixes: the slot COUNT model was
  wrong, not just the slot-size constant — `CARGO_BUILD_JOBS` divides one nested cargo's
  compilers while the number of nested cargos a `cargo test` gate holds open is the libtest
  thread count (`nproc`), a `threads × jobs` product no arithmetic modeled — bounded now by
  `tool_cmd`'s counting semaphore (`CDDL_NESTED_TOOL_PERMITS`, runner-exported per gate, safe
  default for bare runs); and the `.cargo/config.toml` floor never reached a bare invocation's
  NESTED cargos (config discovery walks up from the scratch CWD and finds nothing; cargo does not
  export its jobs to test children), so `tool_cmd` also defaults `CARGO_BUILD_JOBS` to the same
  floor when the environment has neither. The measurement system this entry used to name as
  missing exists now: every run samples its own descendant tree and reports peak Σ RSS
  (test processes included), peak concurrent `rustc`, the largest single process and the
  MemAvailable floor — printed per run and appended to `draft/memory-peaks.jsonl`, reported and
  never asserted (peaks are nondeterministic; a gate that fails on a number would be flaky by
  construction). What remains, in order of leverage:
"""
incident_posture = "historical"
signature_md = """Three machine freezes under memory/swap saturation, including two 2026-08-04 incidents, exposed `MemTotal`, unbounded sequential/bare `cargo`, and nested `threads × jobs` holes.
"""
evidence_ids = ["testing.source-observation.tier-s-peak-memory-bounded-arithmetic-over-assumed"]
attribution_md = """Incomplete concurrency arithmetic; delivered bounds contain the incident, while measurement spending is separate blocked work.
"""
operating_rule_reference_id = "ref-testing-tier-s-peak-memory-bounded-arithmetic-over-assumed-rule"
retirement_reference_id = "ref-testing-tier-s-peak-memory-bounded-arithmetic-over-assumed-retirement"

[[record.source_replacement]]
span_id = "span-record-tier-s-peak-memory-bounded-arithmetic-over-assumed"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.tooling.generated-coverage-instrumentation"
title = "Coverage instrumentation of generated code does not establish semantic correctness and has no reviewed bounded denominator here."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "decision"
summary_md = """Coverage instrumentation of generated code does not establish semantic correctness and has no reviewed bounded denominator here.
"""
decision_state = "decided"
rationale_md = """Coverage instrumentation of generated code does not establish semantic correctness and has no reviewed bounded denominator here.
"""
authority_reference_id = "ref-testing-tooling-generated-coverage-instrumentation-declined-heading"
permanence = "permanent"

[[record]]
id = "testing.tooling.goldenfile-expect-test-corpus"
title = "Goldenfile and expect-test would create a second corpus engine while the incumbent snapshot and hand-vector corpora are simpler and broader."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "decision"
summary_md = """Goldenfile and expect-test would create a second corpus engine while the incumbent snapshot and hand-vector corpora are simpler and broader.
"""
decision_state = "decided"
rationale_md = """Goldenfile and expect-test would create a second corpus engine while the incumbent snapshot and hand-vector corpora are simpler and broader.
"""
authority_reference_id = "ref-testing-tooling-goldenfile-expect-test-corpus-declined-heading"
permanence = "permanent"

[[record]]
id = "testing.tooling.no-panic-lints"
title = "No-panic linting does not replace the existing input-robustness panic/refusal catalog and its exact site-bearing vectors."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "decision"
summary_md = """No-panic linting does not replace the existing input-robustness panic/refusal catalog and its exact site-bearing vectors.
"""
decision_state = "decided"
rationale_md = """No-panic linting does not replace the existing input-robustness panic/refusal catalog and its exact site-bearing vectors.
"""
authority_reference_id = "ref-testing-tooling-no-panic-lints-declined-heading"
permanence = "permanent"

[[record]]
id = "testing.tooling.quickcheck-parallel-corpus"
title = "Quickcheck duplicates the incumbent proptest mechanism without adding a distinct admitted failure class."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "decision"
summary_md = """Quickcheck duplicates the incumbent proptest mechanism without adding a distinct admitted failure class.
"""
decision_state = "decided"
rationale_md = """Quickcheck duplicates the incumbent proptest mechanism without adding a distinct admitted failure class.
"""
authority_reference_id = "ref-testing-tooling-quickcheck-parallel-corpus-declined-heading"
permanence = "permanent"

[[record]]
id = "testing.tooling.trybuild-whole-crate"
title = "Trybuild is narrower than the existing generated-crate cargo check and execution corpus for whole-crate pass behavior."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "decision"
summary_md = """Trybuild is narrower than the existing generated-crate cargo check and execution corpus for whole-crate pass behavior.
"""
decision_state = "decided"
rationale_md = """Trybuild is narrower than the existing generated-crate cargo check and execution corpus for whole-crate pass behavior.
"""
authority_reference_id = "ref-testing-tooling-trybuild-whole-crate-declined-heading"
permanence = "permanent"

[[record]]
id = "testing.transparent-tag-set-idiom-recognized-shape-boundary-request"
title = "Transparent tag-set idiom — recognized-shape boundary (REQUEST-08)."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """Transparent tag-set idiom — recognized-shape boundary (REQUEST-08).
"""
detail_md = """- **Transparent tag-set idiom — recognized-shape boundary (REQUEST-08).** The collapse of a two-arm
  tagged-or-untagged collection choice into one transparent optionally-tagged alias
  (user doc: `docs/docs/current_capacities.mdx` § \"Transparent tag-set idiom\") is narrow by design;
  recognition is pinned by `optional_tag_set_tests`. Its full test map — the tag-set corpus
  fixtures, the `opt_set` golden wire vectors, and the in-process recognition/parity tests — lives
  in `tests/README.md` § \"Transparent tag-set idiom\". The boundary shapes below stay unsupported,
  each with its reopening signal (a real consumer spec hitting it):
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-transparent-tag-set-idiom-recognized-shape-boundary-request-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.transparent-tag-set-idiom-recognized-shape-boundary-request's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record.source_replacement]]
span_id = "span-record-transparent-tag-set-idiom-recognized-shape-boundary-request"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
title = "Tree-wide sweeps over gitignored regeneration targets count DEAD files as product — one proven near-miss, no machinery (and none possible tool-side)."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Tree-wide sweeps over gitignored regeneration targets count DEAD files as product — one proven near-miss, no machinery (and none possible tool-side).
"""
detail_md = """- **Tree-wide sweeps over gitignored regeneration targets count DEAD files as product — one
  proven near-miss, no machinery (and none possible tool-side).** Proven instance (D1's Phase-0
  inventory, caught by classification discipline, not by any gate): a long-lived checkout's
  `tests/*/export*/` trees held 151 stale `.rs` at `<crate>/src/*.rs` from the pre-thin-root
  layout — nothing compiles them (the thin root binds only `src/generated/**`), and a sweep that
  counted them concluded the emitted rust crate has a `std::io` surface, which it does not. The
  near-miss was writing that phantom surface into a delivery inventory as fact and sizing work
  against it. No tool diagnostic can own the class: the crate `src/` level is USER territory by
  the ownership contract (hand modules beside the seed-once `lib.rs` are legitimate, and the tool
  cannot tell a user's file from its own pre-layout-change leftovers), and the trees are
  gitignored, so no repo gate sees them either. Working rule (the fix that worked): classify
  swept files by the CURRENT write-site key set (`src/generation/export.rs`'s write sites are the
  authority), never by presence on disk, and state the unmatched remainder as non-product in the
  sweep's scope note; hygiene half — delete-and-regenerate (or `git clean`) the export trees
  before any tree-wide sweep on a checkout old enough to predate a layout change. Mechanical
  layer on a SECOND distorted-analysis instance: a sweep-preflight that regenerates the swept
  trees first, so the on-disk state IS the current write-site set.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
control_ids = ["testing.guard.tree-wide-sweeps-over-gitignored-regeneration-targets-count"]
transition_ids = ["testing.trigger.tree-wide-sweeps-over-gitignored-regeneration-targets-count"]

[[record.source_replacement]]
span_id = "span-record-tree-wide-sweeps-over-gitignored-regeneration-targets-count"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.trigger.add-profile-wrapper-participation-grid-participation-differs-across"
title = "Lifecycle review for testing.add-profile-wrapper-participation-grid-participation-differs-across."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.add-profile-wrapper-participation-grid-participation-differs-across.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.add-profile-wrapper-participation-grid-participation-differs-across"
dimension = "Add a PROFILE axis to the wrapper-participation grid only when participation differs across profiles."
observable = "- **Add a PROFILE axis to the wrapper-participation grid only when participation differs across profiles.** The current default-profile table and its focused preserve controls are documented in `tests/README.md` § “The wrapper-participation grid”. A full preserve/JSON multiplication is deferred because it multiplies every row to test container/derive changes rather than placement, and no reported row changes its `Defer` / `Borrow` / `Host` / `Local` outcome by profile. Reopening signal: one spec whose `collections.rs` indexes show a different participation outcome under two profiles. At that point add `--preserve-encodings` and `--json-serde-derives` profiles to the data table and its compile floors. The cross-crate duplicate-symbol flavor remains owned here: the duplicate-ident backstop scans one crate's own files, while `synthesized_name_interaction_sweep` has no dependency-index cells."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.add-profile-wrapper-participation-grid-participation-differs-across"]
action_on_fire_md = """Create only the specifically demanded work without widening the settled boundary.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.add-profile-wrapper-participation-grid-participation-differs-across's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.add-profile-wrapper-participation-grid-participation-differs-across"]

[[record]]
id = "testing.trigger.alloc-imports-nested-inline-modules-hand-carried-design"
title = "Lifecycle review for testing.alloc-imports-nested-inline-modules-hand-carried-design."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.alloc-imports-nested-inline-modules-hand-carried-design.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.alloc-imports-nested-inline-modules-hand-carried-design"
dimension = "The alloc imports of nested inline modules are hand-carried by design, and two of the eight `natural_any_cbor_*` adapters were missed — silent under `std`, six E0425s without it."
observable = "- **The alloc imports of nested inline modules are hand-carried by design, and two of the eight `natural_any_cbor_*` adapters were missed — silent under `std`, six E0425s without it.** The alloc-import injector deliberately does not scan nested inline `mod X { … }` bodies: a file-top import added for a nested usage would be unused at file scope and still would not resolve inside the module, so each such module carries its own `use super::alloc::…;`. The four map adapters carried theirs; `natural_any_cbor_seq` and `natural_any_cbor_opt_seq` named `Vec` without one. Fixed minimally — the two hand imports, pointing at the canonical rationale comment on `natural_any_cbor_btreemap` — rather than by teaching the injector to inject into nested bodies, because the nested-module universe in tool output is CLOSED and enumerated (those eight adapters plus `--emit-tests`' `cddl_generated_tests`, which opens `use super::*;` and needs nothing), while a nested-aware injector buys concrete new hazards for it: per-scope dedup against a consumer's `cddl-codegen:insert` block already binding the same name inside the module (E0252 if injected blindly, which is what CML's tree would have met), ownership matching extended past the column-0 exact-match strip rule to indented lines, per-level `super` counting, and insertion-point rules inside nested bodies interacting with the reserved-marker hard stops. What converts \"correctness depends on someone noticing every new nested adapter\" into a red tier is the `json_schema` profile's `shim_thumb` cell above, which now compiles every adapter shape. *Reopening signal* for the nested-aware injector: a SECOND miss of the hand pattern — a red `json_schema`-profile cell, or a consumer filing, on an alloc name inside a nested inline module. Measurable by the party who already has the problem (the tier runner or the consumer reading the E0425), on the dimension the cost grows (recurrence of the class the hand pattern must cover). A new nested adapter added WITH its imports correct does not fire it — the signal is the pattern being missed, not the set growing."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.alloc-imports-nested-inline-modules-hand-carried-design"]
action_on_fire_md = """Set testing.alloc-imports-nested-inline-modules-hand-carried-design to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.alloc-imports-nested-inline-modules-hand-carried-design's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.alloc-imports-nested-inline-modules-hand-carried-design"]

[[record]]
id = "testing.trigger.armed-idle-harness-arms-empty-head-ledgers-zero"
title = "Lifecycle review for testing.armed-idle-harness-arms-empty-head-ledgers-zero."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.armed-idle-harness-arms-empty-head-ledgers-zero.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.armed-idle-harness-arms-empty-head-ledgers-zero"
dimension = "Armed-but-idle harness arms (empty-at-HEAD ledgers, zero-count vector classes, per-row-kind gate branches) have untested first-use paths — seven review-caught instances recorded, no machinery yet."
observable = "- **Armed-but-idle harness arms (empty-at-HEAD ledgers, zero-count vector classes, per-row-kind gate branches) have untested first-use paths — seven review-caught instances recorded, no machinery yet.** The decode-conformance family deliberately keeps machinery armed for residents that don't exist at HEAD (the over-acceptance flow, the exempt ledgers, the stale guards), so a scoping or preservation bug in such an arm is invisible to every gate until the first real resident arrives — and then fires as a false red or a silent data loss. The first three instances, all in the corpus-decode-leg delivery, were caught by in-session diff review and none by a gate, all since fixed: (1) the matrix arm-floor stale-exempt guard iterated a SHARED exempt ledger against only the MATRIX uncovered set, so the first corpus-keyed entry — which the corpus error messages direct users to add — would have falsely failed the matrix gate (fixed: per-catalog ledgers, `CORPUS_DECODE_FLOOR_ARM_EXEMPT`, each stale-guarded against its own set); (2) both mints' zero-candidates pin guard ignored over-acceptance pins (zero committed at HEAD), so the first ruby-generation gap on a row carrying one would have silently discarded a pin that must survive re-mints VERBATIM (fixed in both mints, with the don't-lean-on-`source=\"hand\"` comment); (3) the corpus drift half skipped the example-reconstruction staleness check for PINNED rows, leaving a fixture edit that invalidates a pin's justification green until the next re-mint (fixed: pinned rows check the example half). The shipped exemplar of the fix pattern is the drift gate's § 8 synthetic all-fields sample — but it covers only the writer/reader schema, not ledgers, mint buckets, or per-row-kind gate branches. The general mechanical layer when the build trigger below fires: extend the drift gate's self-check section with synthetic residents and synthetic perturbations — a fake entry per empty-at-HEAD ledger asserting exactly its own guard (and no sibling's) reacts; a pure preservation check of the mint bucket logic over a synthetic prior row carrying every vector class; and a perturbation sweep asserting each committed field's drift flips the gate red for each row KIND (active and pinned). Meanwhile the working rule: an armed-but-idle arm lands with its first-use self-check in the same change, or the review explicitly walks the empty arm. A FOURTH instance is on record from the json/wasm decode-surface-leg delivery, in a distinct CLAIMED-GUARD flavor and a new HOME: `JSON_SURFACE_SKIP` (a replay-gate rust-side skip ledger, not a drift-gate arm) shipped with its code comment and doc claiming the reproduction guard (\"a listed row that now round-trips fails the gate\") while the leg driver consumed entries WITHOUT running the leg — the guard was asserted, never wired; the `WASM_SURFACE_SKIP` sibling in the same change had the real guard. Caught by in-session diff review (the review walked the arm — the working rule holding), fixed in the same series. The first `policy-rejected` residents exposed two further review catches: (5) the matrix report counted policy vectors but the corpus report printed only its total vector count, obscuring its policy evidence; fixed with separate corpus policy/reason counts and the matrix+corpus total; (6) `PolicyFailureKind::DoubledLocation` advertised `DOUBLED_LOCATION_SKIP` but unconditionally failed instead of consuming the stale-guarded ledger, in both matrix and corpus replay loops; fixed by sharing the constraint branch's lookup and still-failing insertion. Two consequences for the mechanical layer: the flavor to detect is \"ledger entry consumed with zero reproduction attempt\", and the layer as sketched above is DRIFT-gate-scoped — the replay gates' rust-side skip ledgers need a sibling arm (a synthetic always-passing resident driven through the leg driver, asserting exactly the stale-pin failure fires and no other). A SEVENTH first-use preservation gap surfaced in the B3-024C corpus mint: the known rust-cddl `undefined`/`f7` oracle defect made the mint drop the spec-valid `fixed_singletons.undefined_value/8200f7` accept vector while still returning PASS; review of the dropped-vector report restored it manually. A future scoped re-mint can repeat that loss because there is no accept-side oracle-gap ledger. Add a per-catalog `(row, hex, expected failing oracle(s), reason)` exemption that retains only a vector the non-exempt oracle still accepts, fails stale when the cited oracle starts accepting or changes its failure signature, and is covered by the synthetic mint-bucket resident above; then migrate `8200f7` onto it. Do not weaken the ordinary two-oracle accept rule or create a row-wide exemption. Count: seven review-caught instances, zero escapes; the build trigger stays the first instance that survives review, with the layer owing BOTH homes when built. Related, smaller catch from the same review with its own trivial layer: a machine-written catalog `pinned_reason` cited a run artifact (\"see mint log\") — catalog prose is outside `lint_doc_citations`' scan surface, so non-durable citations there rot silently; fixed by wording review (the mint now emits self-contained tallies + a durable ledger pointer), and extending that lint's scan to catalog reason strings is the mechanical layer if it recurs."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.armed-idle-harness-arms-empty-head-ledgers-zero"]
action_on_fire_md = """Set testing.armed-idle-harness-arms-empty-head-ledgers-zero to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.armed-idle-harness-arms-empty-head-ledgers-zero's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.armed-idle-harness-arms-empty-head-ledgers-zero"]

[[record]]
id = "testing.trigger.assert-hand-authored-json-schema-against-type-s"
title = "Lifecycle review for testing.assert-hand-authored-json-schema-against-type-s."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.assert-hand-authored-json-schema-against-type-s.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.assert-hand-authored-json-schema-against-type-s"
dimension = "Assert a hand-authored JSON schema against the type's actual serialization."
observable = "- **Assert a hand-authored JSON schema against the type's actual serialization.** Supplying the schema body needs neither new spec syntax nor a hand-written impl: `@custom_json` suppresses the `serde`/`schemars` derives while the type still gets a registration row, and `custom_schema_impl!` writes the `JsonSchema` impl around a JSON file, so an authored body lands in the document as an ordinary local `$defs` entry through the normal route (`docs/docs/comment_dsl.mdx` § Writing the `JsonSchema` impl the directive promises). The missing half is the one with the value: a hand-written schema drifts from the serialization it claims to describe and nothing notices — the reported consumer failure is a hand-maintained schema that had been wrong in two places for years. The assertion SHAPE already exists, hand-authored per fixture: `tests/json/tests.rs`'s `schemas_validate_serialization` validates a concrete value of every exported type against `schema_for!(T)`, with `schemas_reject_wrong_shapes` as the over-permissive counterpart (a degenerate always-true schema passes every positive check). What it needs in order to generalize into a consumer's crate is per-type sample values MINTED rather than hand-listed — the `--emit-tests` IR-derived value minting is the candidate. A spec-level `@json_schema = \"<file>\"` spelling is the cosmetic half and should not ship first: without the assertion it relocates the hazard instead of removing it. Reopening signal: the sample-value minting gets reused for this, or a consumer reports a shipped schema that contradicted its own serialization."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.assert-hand-authored-json-schema-against-type-s"]
action_on_fire_md = """Set testing.assert-hand-authored-json-schema-against-type-s to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.assert-hand-authored-json-schema-against-type-s's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.assert-hand-authored-json-schema-against-type-s"]

[[record]]
id = "testing.trigger.cargo-feature-generated-crate-s-dependency-change-what"
title = "Lifecycle review for testing.cargo-feature-generated-crate-s-dependency-change-what."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.cargo-feature-generated-crate-s-dependency-change-what.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.cargo-feature-generated-crate-s-dependency-change-what"
dimension = "A cargo FEATURE on a generated crate's dependency can change what emitted code MEANS, and no axis varies dependency features — compounded by every generated `Serialize` only ever being exercised through `serde_json`, the one serializer that cannot observe dishonesty in the serde data model."
observable = "- **A cargo FEATURE on a generated crate's dependency can change what emitted code MEANS, and no axis varies dependency features — compounded by every generated `Serialize` only ever being exercised through `serde_json`, the one serializer that cannot observe dishonesty in the serde data model.** Proven instance (consumer-reported, not any gate): under `serde_json/arbitrary_precision`, `serde_json::Number`'s `Serialize` emits a private `$serde_json::private::Number` token struct that only serde_json's own serializer collapses, so every `any`-carrying member's `to_json_value()` shipped a token object to JS at every magnitude — while `to_json()` looked perfect, because `to_json()` IS serde_json. Two independent blind spots produced it. Cargo unifies features across a build graph, so the feature arrives from a crate the generated manifest never names; the manifest merge even asserts it survives regeneration (a correct behaviour — the feature is legitimate), so the tool knowingly preserves a configuration nothing tested. And the assertion \"the emitted `Serialize` is honest in the serde data model\" is unobservable from serde_json: a second serializer is the only oracle that can see it, which is why the fixture takes `ciborium` (pure Rust, no C deps, no special case for the token) rather than adding another serde_json assertion. Owned meanwhile — one fixture, one feature, one alternate serializer, with liveness assertions in BOTH the rust and wasm crates so the feature-unification indirection cannot silently stop working and leave every other assertion in it vacuous — pinned by `json_arbitrary_precision`. Mechanical layer, when the trigger fires: a dependency-feature axis over the existing fixture corpus — for each generated-crate dependency, the set of its features that can change emitted-code semantics, generated once per (fixture × feature) cell and round-tripped through a non-serde_json serializer, which needs a per-dependency feature datum beside the manifest changeset that already knows every dependency the emitter references (`cargo_manifest.rs`'s `ops_for_*`). Building it now would be over-engineering: exactly one such feature is known to change semantics. Trigger: a consumer reporting a SECOND generated-crate behaviour that changes with a cargo feature they did not put in their own manifest — the party who already has the problem measures it as \"my spec and my manifest are unchanged and my generated code behaves differently\", which is what makes it reportable at all, and it lies on the axis the cost grows along, since the deferred layer's value scales with how many such features exist rather than with how many consumers hit the one we know about. A second semantics-changing feature is now known but is TOOL-OWNED and deliberate rather than unification-borne, so it does not fire this trigger: D1 ships `derivative` with `use_core`, which changes every emitted `#[derivative(…)]` expansion from `::std::` to `::core::` paths — landed only after probing the COMPLETE emitted attribute-form set (7 bodies × 3 derive lines) for equivalence on host and a no-std target. Recorded so the count of known semantics-changing dependency features reads two, not one, when this axis is next weighed."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.cargo-feature-generated-crate-s-dependency-change-what"]
action_on_fire_md = """Set testing.cargo-feature-generated-crate-s-dependency-change-what to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.cargo-feature-generated-crate-s-dependency-change-what's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.cargo-feature-generated-crate-s-dependency-change-what"]

[[record]]
id = "testing.trigger.cddl-module-directives-draft-ietf-cbor-cddl-modules"
title = "Lifecycle review for testing.cddl-module-directives-draft-ietf-cbor-cddl-modules."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.cddl-module-directives-draft-ietf-cbor-cddl-modules.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.cddl-module-directives-draft-ietf-cbor-cddl-modules"
dimension = "CDDL module directives (draft-ietf-cbor-cddl-modules) with the draft's real inlining semantics, and `as`-namespacing."
observable = "- **CDDL module directives (draft-ietf-cbor-cddl-modules) with the draft's real inlining semantics, and `as`-namespacing.** Both forms are currently recognized and refused loudly (`module_directive_import_aborts` / `module_directive_include_aborts`; `dotted_rule_name_rejects_gracefully` for cddlc `as`-expansion output) — deliberate: the extern channel shipped on the dep-side export instead (rationale: the dep's extern surface is `f(spec, DSL, flags, tool version)`, which spec-text resolution cannot see; and the draft's `;# import` means inline-and-generate, so extern semantics would contradict it — keep the `;#` namespace clean for a real implementation). Reopening signals: a consumer needs third-party module consumption (RFC extracts) or cross-module name reuse; the latter is the deep one (scope-qualified `RustIdent` through the IR, dep_graph ordering, emitted-test glob imports, JSON schema naming)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.cddl-module-directives-draft-ietf-cbor-cddl-modules"]
action_on_fire_md = """Set testing.cddl-module-directives-draft-ietf-cbor-cddl-modules to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.cddl-module-directives-draft-ietf-cbor-cddl-modules's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.cddl-module-directives-draft-ietf-cbor-cddl-modules"]

[[record]]
id = "testing.trigger.citation-resolves-naming-wrong-thing-invisible-citation-lint"
title = "Lifecycle review for testing.citation-resolves-naming-wrong-thing-invisible-citation-lint."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.citation-resolves-naming-wrong-thing-invisible-citation-lint.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.citation-resolves-naming-wrong-thing-invisible-citation-lint"
dimension = "A citation that RESOLVES while naming the wrong thing is invisible to every citation lint, and the claim it makes is the half a human reads."
observable = "- **A citation that RESOLVES while naming the wrong thing is invisible to every citation lint, and the claim it makes is the half a human reads.** The `KNOWN_PANIC_CLASSES` form of this is closed: each entry pairs a panic-message substring with prose naming the fixture that owns the class, and `known_panic_classes_cite_fixtures_that_produce_them` (always-on, `local` tier) now generates each `tests/**/*.cddl` fixture an entry names and requires at least one to produce the entry's substring, so a mis-citation is a red test rather than a mislead during triage — the instance on record cited a fixture whose actual class was the other anonymous-composite BRACKET, which would have sent a reader to the wrong parse site (current state in `tests/README.md` § \"Shape-recombination fuzzer\"). What remains is the SECOND FORM of the same class, in a population that detector cannot reach — the cited thing is a GATE and the claim is that the gate is EVIDENCE for a property. Instance (read-caught in orchestrator review, never committed): a delivery report cited `check.ts fast` as proof that an `--emit-tests` map-key spelling was byte-identical, but the snapshot corpus holds zero `__i as …` key expressions, so no run of that tier could ever have witnessed the property either way. The class property is identical — the citation RESOLVES (the gate exists, it was green), so nothing that checks resolution can fire, and only the claim is false — but the population is prose in reports and handoff docs, which no gate scans, so the ledger detector above does not extend to it and this is NOT its second-instance trigger firing. What DOES transfer is the cheap instrument, which is the same shape as the detector: before citing a gate as evidence for emitted TEXT, grep that gate's corpus for the text. Which corpus covers what is a `tests/README.md` fact, and the emit-tests key-rendering answer now lives there (§ \"Authoring standard for a bounded-domain emit-tests fixture\"); the general rule is that a tier verdict evidences a property only where that tier's corpus contains an instance of it."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.citation-resolves-naming-wrong-thing-invisible-citation-lint"]
action_on_fire_md = """Set testing.citation-resolves-naming-wrong-thing-invisible-citation-lint to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.citation-resolves-naming-wrong-thing-invisible-citation-lint's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.citation-resolves-naming-wrong-thing-invisible-citation-lint"]

[[record]]
id = "testing.trigger.comment-residue-false-matches-text-scans-over-emitted"
title = "Lifecycle review for testing.comment-residue-false-matches-text-scans-over-emitted."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.comment-residue-false-matches-text-scans-over-emitted.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.comment-residue-false-matches-text-scans-over-emitted"
dimension = "Comment-residue false matches in text scans over emitted/prior `.rs` output — one proven instance, no machinery yet."
observable = "- **Comment-residue false matches in text scans over emitted/prior `.rs` output — one proven instance, no machinery yet.** Any diagnostic or decision that SCANS generated (or user-edited prior) Rust text for a code pattern shares one trap: the comment-preservation overlay and the users of its block grammar leave the scanned pattern behind as COMMENT bytes — a `cddl-codegen:replaces` recorded original is the exact deleted line `//`-commented, and unpreserved-comment blocks / doc comments can quote code verbatim — so a substring `contains` scan reads deleted code as live. Proven instance (development-red-caught, not by any shipped gate): the extern-glue re-export survival scan's first cut used a substring match and read a replace block's recorded `// pub use crate::<Name>;` original as surviving glue — corrected to whole-trimmed-line equality before landing, with the residue case pinned by `extern_reexport_diagnostic_skips_replace_deleted_glue`'s chain assertions (overlay really deleted the live line AND the warning subtracted the name). Working rule meanwhile: a scan over emitted/prior `.rs` text matches live tokens (whole-trimmed-line or token-aware), never substrings, and its test includes a comment-residue adversarial fixture (the pattern present ONLY as a recorded original / comment). One untested premise worth probing when next in the area: `missing_reexports`' whole-ident scan over the USER's seed-once `lib.rs` — whether a commented-out `// pub use generated::<Name>;` line counts as \"provided\" (same residue class, opposite direction: a false NEGATIVE of the warning). Mechanical layer on a SECOND instance: an adversarial-fixture family enumerating every output-text-scanning decision point (the bounded-exception diagnostics list in AGENTS.md is the seed inventory) with one residue fixture each, rather than trusting each new scan to remember the rule."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.comment-residue-false-matches-text-scans-over-emitted"]
action_on_fire_md = """Set testing.comment-residue-false-matches-text-scans-over-emitted to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.comment-residue-false-matches-text-scans-over-emitted's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.comment-residue-false-matches-text-scans-over-emitted"]

[[record]]
id = "testing.trigger.composing-registration-rows-across-generation-passes-share-output"
title = "Lifecycle review for testing.composing-registration-rows-across-generation-passes-share-output."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.composing-registration-rows-across-generation-passes-share-output.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.composing-registration-rows-across-generation-passes-share-output"
dimension = "Composing registration rows across generation passes that share one output crate."
observable = "- **Composing registration rows across generation passes that share one output crate.** Two cddl-codegen passes targeting one output directory each own the json-gen crate's row set outright, so there is no supported way to have one json-gen crate publish both passes' types. Lowest value of the JSON-schema asks by the asking consumer's own ranking: a committed-`.d.ts` CI diff covers the requirement they actually have, and the emitted `add_schemas` is already `pub`, so a hand-written `export_schemas()` in the crate root can call both passes' registrars today. (Scope: this records the ask as understood from the delivery's response notes; the requester's own statement of it is not in-tree.) Reopening signal: a consumer whose layout genuinely forces two passes into one json-gen crate and for whom the hand-written composition is not enough."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.composing-registration-rows-across-generation-passes-share-output"]
action_on_fire_md = """Set testing.composing-registration-rows-across-generation-passes-share-output to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.composing-registration-rows-across-generation-passes-share-output's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.composing-registration-rows-across-generation-passes-share-output"]

[[record]]
id = "testing.trigger.config-run-s-convergence-warning-name-crate-already"
title = "Lifecycle review for testing.config-run-s-convergence-warning-name-crate-already."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.config-run-s-convergence-warning-name-crate-already.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.config-run-s-convergence-warning-name-crate-already"
dimension = "The config run's convergence warning can name a crate that already converged."
observable = "The convergence warning names the same crate on two consecutive runs of an unchanged config."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.config-run-s-convergence-warning-name-crate-already"]
action_on_fire_md = """Set testing.config-run-s-convergence-warning-name-crate-already to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.config-run-s-convergence-warning-name-crate-already's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.config-run-s-convergence-warning-name-crate-already"]

[[record]]
id = "testing.trigger.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
title = "Lifecycle review for testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
dimension = "A container construct the conceptual type visitor walks FLAT has no combinatorial wasm-compile coverage — its placement behaviour rests on one hand cell per construct."
observable = "A third container construct is minted explicitly in generation/mod.rs because the conceptual visitor cannot see it as a composite."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.container-construct-conceptual-type-visitor-walks-flat-combinatorial"]
action_on_fire_md = """Set testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.container-construct-conceptual-type-visitor-walks-flat-combinatorial"]

[[record]]
id = "testing.trigger.containment-note-s-stated-reason-go-false-any"
title = "Lifecycle review for testing.containment-note-s-stated-reason-go-false-any."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.containment-note-s-stated-reason-go-false-any.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.containment-note-s-stated-reason-go-false-any"
dimension = "A containment note's stated REASON can go false without any gate noticing — the note carries a behavioural claim, and only its `spec` and (via the annotation) its support verdict are checked."
observable = "- **A containment note's stated REASON can go false without any gate noticing — the note carries a behavioural claim, and only its `spec` and (via the annotation) its support verdict are checked.** Proven instance, since fixed: six containment notes justified their two-field examples with \"a single-field struct hits an orthogonal cddl-codegen panic\", a panic that was gone — `m = { k: 5 }` and every other shape those notes were attached to generate exit 0, and `cddl-matrix/annotations/corpus/cddl_codegen.toml` separately recorded single-field struct maps as supported, so the note and the annotation had been contradicting each other in the same repo. The notes now state the file's two-field example CONVENTION instead of a causal claim about a defect, and the absence of the panic is pinned by `tests/corpus/single_field_map.cddl` — the shape a note must take when its reason would otherwise be an unowned behavioural claim. Nothing connected them and nothing does now: `project_corpus`'s note↔support check (E) reads the corpus nuance overlay's `[[note]]` entries, not containment `note` prose, and `verify.ts` corroborates only the `example` against `spec`. The class is bounded — a containment note is prose, and most of it (grammar anchors, sibling relationships) is not machine-checkable — so the meanwhile rule is narrower than a checker: a note states the SHAPE and its sibling relationships, and any behavioural claim it makes names the fixture or annotation that owns the claim, so the claim rots where a gate already looks. Trigger for the mechanical layer (a lint that flags behavioural vocabulary — \"panics\", \"unsupported\", \"rejects\" — in a containment note whose id carries no annotation asserting it): a SECOND note found stating a behaviour the annotations contradict, which the next author to reuse a note's reasoning measures at the moment they inherit it."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.containment-note-s-stated-reason-go-false-any"]
action_on_fire_md = """Set testing.containment-note-s-stated-reason-go-false-any to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.containment-note-s-stated-reason-go-false-any's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.containment-note-s-stated-reason-go-false-any"]

[[record]]
id = "testing.trigger.control-bytes-committed-source-files-proven-instance-machinery"
title = "Lifecycle review for testing.control-bytes-committed-source-files-proven-instance-machinery."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.control-bytes-committed-source-files-proven-instance-machinery.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.control-bytes-committed-source-files-proven-instance-machinery"
dimension = "Control bytes in committed source files — one proven instance, no machinery yet."
observable = "- **Control bytes in committed source files — one proven instance, no machinery yet.** A committed TS script embedded a literal 0x00 byte in a template-string separator; grep/diff/review tooling then classifies the whole file as binary — silently exempting it from every text-shaped check and from readable review — while `tsc` parses it happily, so `matrix_typecheck` cannot see the class (fixed by spelling the byte as a backslash-u0000 escape). Working rule meanwhile: escape sequences, never literal control bytes, in source text. Mechanical layer if the class recurs: a tracked-source text-cleanliness lint — every tracked `*.ts`/`*.rs`/`*.toml`/`*.md` must be valid UTF-8 with no control bytes outside tab/LF/CR (a one-command scan; natural sibling of `lint_doc_citations` in the local tier)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.control-bytes-committed-source-files-proven-instance-machinery"]
action_on_fire_md = """Set testing.control-bytes-committed-source-files-proven-instance-machinery to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.control-bytes-committed-source-files-proven-instance-machinery's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.control-bytes-committed-source-files-proven-instance-machinery"]

[[record]]
id = "testing.trigger.corpus-wide-regen-over-prior-output-sweep-reaches"
title = "Lifecycle review for testing.corpus-wide-regen-over-prior-output-sweep-reaches."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.corpus-wide-regen-over-prior-output-sweep-reaches.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.corpus-wide-regen-over-prior-output-sweep-reaches"
dimension = "The corpus-wide regen-over-prior-output sweep reaches two emission profiles of four, and its user-EDIT leg reaches one."
observable = "- **The corpus-wide regen-over-prior-output sweep reaches two emission profiles of four, and its user-EDIT leg reaches one.** `regen_over_prior_output_corpus` sweeps the static floor and the rule-DELETION variant under the default and `--preserve-encodings` profiles (the latter for `cbor_encodings.rs`, a per-rule surface the default never emits); the `json` profile's `json-gen` tree and derive attributes, and the `component` face's WIT-side glue, are unswept, as is the user-EDIT leg outside the default profile. Each added profile costs one more generation per fixture for legs 1-2 (measured ~15 s on the delivering machine) and THREE for the edit leg, plus nested cargo for its compile gate — paid on every `local` tier run. **Owner meanwhile:** the emitter invariant itself (no generated comment on a row a spec change can delete), which is profile-independent in its reasoning even where the sweep is not in its reach, plus the two swept profiles' floors. **Trigger:** a `cddl-codegen:unpreserved-comment` sentinel reported against a regenerated tree — by a consumer or by any gate — in a file neither swept profile emits; the profile that produced it is then the one to add, not all of them."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.corpus-wide-regen-over-prior-output-sweep-reaches"]
action_on_fire_md = """Set testing.corpus-wide-regen-over-prior-output-sweep-reaches to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.corpus-wide-regen-over-prior-output-sweep-reaches's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.corpus-wide-regen-over-prior-output-sweep-reaches"]

[[record]]
id = "testing.trigger.crate-generates-json-schema-document-aggregate-package"
title = "Lifecycle review for testing.crate-generates-json-schema-document-aggregate-package."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.crate-generates-json-schema-document-aggregate-package.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.crate-generates-json-schema-document-aggregate-package"
dimension = "A crate entry that generates only a JSON-schema document (the aggregate package)."
observable = "- **A crate entry that generates only a JSON-schema document (the aggregate package).** An npm package composing several generated crates needs one schema document over their union, but is often not itself a generated crate — so it carries a hand-written json-gen crate transcribing what this tool already emits for a `--json-schema-dep`-only run, registration-order reasoning and all. Measured, the emission side is already there: an input-less run (an empty `--input` directory) emits a `wasm/json-gen/` whose `add_schemas` holds the threaded calls in flag order and nothing else, and a fixture over two generated crates builds it and gets a document carrying a root from each. What blocks it is the SURPLUS such a run also writes — a vestigial rust crate that `static/manifest_changes/json_gen.toml` then makes the json-gen crate depend on (`dependencies.<lib> = { path = \"../../rust\" }`, an unconditional entry in an append-only log). Cargo auto-adopts that path dependency as a workspace member, so its `package.name` collides with the hand-written umbrella crate the aggregate exists to serve; renaming escapes the collision only by changing the document's title and filename, both published surface. What to build, in order: lift the `--json-schema-dep` / `--json-gen-dep` / `--json-schema-root` validations out of `with_types`, which an input-less run never enters; make the `../../rust` manifest op conditional. Only then land the aggregate path on top. The step this list used to open with is already done and is what makes the rest safe: `GenerationScope::export`'s write tail is one parameterised implementation (`generation::write_tail`), so the no-prior-output contract and its diagnostic-only reads have exactly one home and direct coverage (`src/tests/write_tail_tests.rs`). A second write loop beside it is still the shape to avoid — the snapshot corpus reaches `generated_strings`, not `export` — but an aggregate path now has a `WriteTailPlan` to fill in rather than a write loop to clone. Reopening signal (magnitude, consumer-side): the count of hand-written umbrella exporter files a consumer maintains rising above one, which is what a project publishing a second npm package over generated crates produces."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.crate-generates-json-schema-document-aggregate-package"]
action_on_fire_md = """Set testing.crate-generates-json-schema-document-aggregate-package to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.crate-generates-json-schema-document-aggregate-package's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.crate-generates-json-schema-document-aggregate-package"]

[[record]]
id = "testing.trigger.cross-version-preserve-vectors-beyond-std-alloc-rewrite"
title = "Lifecycle review for testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite"
dimension = "Cross-version preserve vectors beyond the std→alloc rewrite."
observable = "An upgrade regeneration emits cddl-codegen:unpreserved-comment or traps an insert/replace/keep block and a second otherwise-unchanged regeneration does not reproduce it."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.cross-version-preserve-vectors-beyond-std-alloc-rewrite"]
action_on_fire_md = """Set testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.cross-version-preserve-vectors-beyond-std-alloc-rewrite"]

[[record]]
id = "testing.trigger.dependency-swap-pass-compile-parity-existing-gate-silently"
title = "Lifecycle review for testing.dependency-swap-pass-compile-parity-existing-gate-silently."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.dependency-swap-pass-compile-parity-existing-gate-silently.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.dependency-swap-pass-compile-parity-existing-gate-silently"
dimension = "A dependency swap can pass compile-parity and every existing gate while silently changing behavior our public API re-exposes — one proven near-miss, pins landed for the instance."
observable = "- **A dependency swap can pass compile-parity and every existing gate while silently changing behavior our public API re-exposes — one proven near-miss, pins landed for the instance.** Proven instance (caught by orchestrator differential probing, not by any test): the linked-hash-map→hashlink swap plan carried an `insert`→`replace` \"compatibility\" forwarding built on a twice-source-read premise that linked-hash-map's `insert` replaces in place. It moves the key to the back — identically to hashlink — so the forwarding would have CHANGED shipped wire-order behavior while presenting as preservation, and the swap's real divergence was elsewhere (`Entry::or_insert` on an occupied entry: hashlink refreshes to back, linked-hash-map doesn't). Every existing layer is structurally blind to the class: round-trip and golden-hex oracles never MUTATE through the dep surface (decode→mutate→re-serialize is where order semantics bite); snapshot pins hold emitted TEXT, which a backing swap legitimately churns; and mutation scoring runs against the CURRENT dep, so it measures coverage of today's baseline, never a baseline change. We therefore pinned the incumbent's semantics at the wire level BEFORE swapping — `ordered_hash_map_{insert_overwrite_moves_to_back, or_insert_keeps_position,from_iter_duplicate_keys,entry_match_shapes}` in `tests/preserve-encodings/tests.rs`, committed green on the old backing in their own commit, so the swap had to reproduce bytes rather than bless new ones — instead of trusting source reads (two independent ones agreed and were both wrong; only an executable differential probe — identical op sequences on both crates, iteration-order traces asserted equal — broke the premise). Residual: the pinned set covers the CONSUMER-PROBED mutation surface (insert, or_insert, from_iter, entry match shapes); other Deref-reachable mutation ops (`remove`/`extend`/`pop_*`) are differential-probed equal but unpinned, and other re-exposed dep surfaces (the `cbor_event` types in every deserialize signature) rely on the round-trip oracles alone, which do cover their read/write behavior but not order-adjacent mutation — no such surface exists there today. Working rule meanwhile: a change to the VERSION FLOOR or IDENTITY of a dependency whose types our public API re-exposes (a Deref target, a re-exported type, a type embedded in public signatures) first lands wire-level pins of the incumbent's consumer-reachable behavior in a separate commit, then swaps under them — and parity premises between two crates are established by executable differential probes, never by source reads. Mechanical layer on a SECOND swap/bump instance of this class (candidates: the cbor_event fork's crates.io upstreaming flip, a hashlink 0.13 floor raise): a curated dep-semantics pin suite per re-exposed surface, enumerated from the public-API type graph (which dep types appear in `pub` signatures/Deref targets), so the pin obligation is discovered by a gate rather than remembered by a rule."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.dependency-swap-pass-compile-parity-existing-gate-silently"]
action_on_fire_md = """Set testing.dependency-swap-pass-compile-parity-existing-gate-silently to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.dependency-swap-pass-compile-parity-existing-gate-silently's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.dependency-swap-pass-compile-parity-existing-gate-silently"]

[[record]]
id = "testing.trigger.dependency-version-range-resolution-untested-generated-crates-float"
title = "Lifecycle review for testing.dependency-version-range-resolution-untested-generated-crates-float."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.dependency-version-range-resolution-untested-generated-crates-float.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.dependency-version-range-resolution-untested-generated-crates-float"
dimension = "Dependency version-RANGE resolution is untested: generated crates float on semver `cbor_event = \"3.3.0\"`, and nothing gates what that range actually resolves to over time."
observable = "- **Dependency version-RANGE resolution is untested: generated crates float on semver `cbor_event = \"3.3.0\"`, and nothing gates what that range actually resolves to over time.** In check.ts runs the nested-cargo cells now resolve OFFLINE from the cargo cache (the registry-transient watch below), so the float enters one step removed — through the warm-up fetch, which pulls the newest semver-compatible releases into the cache — but the exposure is the same: a new upstream release changes what every nested-cargo cell — and every downstream consumer — builds against, with no gate noticing until something breaks. The 3.2.0 upgrade itself arrived as exactly such a version event (the 2.4.0-era prediction this entry re-captures), absorbed deliberately with flip vectors; an UNPLANNED 3.x release would be absorbed silently. The mechanical layer when a release actually bites: a pinned-latest or `--minimal-versions`-style resolve check over one generated crate, red when the resolved `cbor_event` version drifts from the one the vectors were blessed against."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.dependency-version-range-resolution-untested-generated-crates-float"]
action_on_fire_md = """Set testing.dependency-version-range-resolution-untested-generated-crates-float to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.dependency-version-range-resolution-untested-generated-crates-float's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.dependency-version-range-resolution-untested-generated-crates-float"]

[[record]]
id = "testing.trigger.directive-dependent-emitter-edge-case-triggering-shape-hand"
title = "Lifecycle review for testing.directive-dependent-emitter-edge-case-triggering-shape-hand."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.directive-dependent-emitter-edge-case-triggering-shape-hand.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.directive-dependent-emitter-edge-case-triggering-shape-hand"
dimension = "A directive-dependent emitter edge case whose triggering SHAPE is in no hand fixture is invisible to every combinatorial layer — the recombination fuzzer skips ALL `dsl.*` features wholesale (\"DSL comment in root RHS; not a clean reusable expression\"), so no fuzz composition ever carries a directive."
observable = "- **A directive-dependent emitter edge case whose triggering SHAPE is in no hand fixture is invisible to every combinatorial layer — the recombination fuzzer skips ALL `dsl.*` features wholesale (\"DSL comment in root RHS; not a clean reusable expression\"), so no fuzz composition ever carries a directive.** Proven instance (probe-caught in review, then independently confirmed by reading — never a gate failure): the `definite_info` additive-length combine emitted a malformed `\"{n} + \"` expression when the conditional expression is EMPTY, a branch reachable only by an all-mandatory-fields open struct-map whose rest row is `@ignore` (tolerate-and-drop: no optionals, no captured-rest term) — no pre-existing spec could reach it, and only the delivery's own hand-enumerated fixtures (`details_map` in the `open_struct_map_ignore` snapshot, `baz` in the compiled e2e) happened to cover the degenerate combination. The standing discipline that catches the SHIPPED form is exactly that hand-enumeration: a new directive's spec must fixture the degenerate shape combos (all-mandatory, zero-field, empty-collection) alongside the happy paths — reviewers should demand the degenerate rows in the fixture list. Mechanical layer on the SECOND fuzzer-invisible directive-emitter instance: a directive AXIS for the recombination fuzzer — instead of skipping `dsl.*` ingredients, mint directive-BEARING variants of the applicable shape templates (rest-row `@ignore`/`@duplicates`/`@name` on map-record templates, rule-level `@duplicates` on collection templates), letting the existing generation sweep + layer-2 compile gates see directive × shape products no hand fixture enumerates."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.directive-dependent-emitter-edge-case-triggering-shape-hand"]
action_on_fire_md = """Set testing.directive-dependent-emitter-edge-case-triggering-shape-hand to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.directive-dependent-emitter-edge-case-triggering-shape-hand's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.directive-dependent-emitter-edge-case-triggering-shape-hand"]

[[record]]
id = "testing.trigger.directive-s-documented-sentence-stay-true-consumer-visible"
title = "Lifecycle review for testing.directive-s-documented-sentence-stay-true-consumer-visible."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.directive-s-documented-sentence-stay-true-consumer-visible.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.directive-s-documented-sentence-stay-true-consumer-visible"
dimension = "A directive's documented sentence can stay true while its consumer-visible MEANING changes underneath it, because the artifact it controls changed shape."
observable = "- **A directive's documented sentence can stay true while its consumer-visible MEANING changes underneath it, because the artifact it controls changed shape.** `@no_json_schema_export` shipped meaning \"no registration row for this type\", pinned at the row level by `snapshot_tests::json_gen_extern_schema_rows` and the accepted/rejected table in `robustness_tests::no_json_schema_export_misuse_rejects_gracefully`. Both pins still pass verbatim after the JSON surface became one document per crate — and yet the directive now means something materially different to a consumer: it drops the ROOT DECLARATION, not the definition, so a suppressed type that a published type references is now DECLARED in the shipped `.d.ts` where it was previously a dangling `TS2304`. Nothing could have caught that, because every pin was on the emitted intermediate and none was on the surface the directive's own documentation names (\"not part of the published JSON-schema surface\"). The gap generalizes past this directive and past JSON: a pin on generated source answers \"did the emitter branch\", never \"does the shipped artifact say what the directive promises\". Note this is a DIFFERENT class from the directive×rule-shape reachability product entry, whose verdict is {effect visible, loudly rejected} on a byte-compare of generated source — that sweep reads \"effect visible\" for this directive both before and after the change. Owned meanwhile by the end-to-end pin in `integration_tests::package_json_pipeline` (a `@no_json_schema_export` rule that a row'd type embeds, asserted `$defs`-present and `.d.ts`-declared), which exists precisely so the directive's promise is checked where a consumer reads it. Mechanical layer, on the second instance: for each directive that names a SHIPPED artifact in its documentation, require one pin on that artifact rather than on the emitted source — the directive inventory already exists (`KNOWN_RULE_METADATA_TAGS`, mirrored by `corpus_detect.ts`), so the addition is a per-directive datum naming its shipped surface, alongside the witness-profile datum the reachability sweep needs. Trigger: a second directive whose meaning shifts under an artifact-shape change, or a consumer reporting that a directive did not do what its documentation says while every gate stayed green. **Scope note, from a near-miss in a different flavor:** this entry and its mechanical layer are DIRECTIVE-scoped, and the same \"documented promise nobody pins\" class reaches CLI flags and the emitted runtime too, where no equivalent inventory exists to hang a per-item datum on. The near-miss (review-caught before shipping, so no instance is on record): the `--json-schema-export` docs stated that a document failing the closure check \"never reaches `schemas/`\", which is true of the failing document and false about the directory — `export_schemas()` creates `schemas/` before the check and, on failure, writes nothing, so an EARLIER export's document stays on disk and a pipeline ignoring the non-zero exit compiles it. Fixed in the prose rather than by a pin, because a filesystem-effect claim has no artifact inventory to enumerate. If a flag-level instance actually ships, the layer to build is the same shape one rung out: a per-flag datum naming the artifact its documentation makes claims about."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.directive-s-documented-sentence-stay-true-consumer-visible"]
action_on_fire_md = """Set testing.directive-s-documented-sentence-stay-true-consumer-visible to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.directive-s-documented-sentence-stay-true-consumer-visible's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.directive-s-documented-sentence-stay-true-consumer-visible"]

[[record]]
id = "testing.trigger.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
title = "Lifecycle review for testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
dimension = "A docs-vs-behavior conformance harness for `comment_dsl.mdx` / `output_format.mdx`"
observable = "- **A docs-vs-behavior conformance harness for `comment_dsl.mdx` / `output_format.mdx`** (snippet extraction + output spot-checks): emitted output is already pinned by the snapshot corpus and DSL-name drift by the `cddl-matrix/verify.ts` forward lint; a doc-snippet system is heavy machinery for prose drift that review catches at its actual rate. The decline has narrowed since it was made: identifier-existence drift (a cited pin that no longer exists) is now mechanically covered by the `lint_doc_citations` gate, so what stays declined is prose SEMANTICS only — a sentence whose cited pin exists but whose claim about it is wrong. A THIRD prose home joins `docs/` and the generated `COVERAGE.md` span: a FIXTURE HEADER commenting on a SIBLING fixture. `tests/robustness/choice_cbor_ref_arm.cddl` described the rule-body spelling as \"the remaining unsupported spelling … unaffected by rule ordering\" — a claim its sibling `cbor_ref_rule_body.cddl` contradicted in its own header from the commit that fixed both, and which the catalog snapshot (`ok`) had already recorded as false. Fixture prose is outside `lint_doc_citations`' scan surface for the same reason catalog prose is, and the claim named no artifact that could dangle, so this stays in the declined class. It is recorded as evidence the decline is calibrated rather than as a gap: review caught it one cycle after it shipped, which is the \"actual rate\" the decline is priced at, and the reopening signal for this class is a false claim SHIPPING PAST review — not one being caught by it. Authoring rule that would have prevented it at zero cost: a fixture header states what THIS fixture pins, and refers to a sibling only by name and role, never by asserting the sibling's support status — the catalog snapshot is where that status lives, and it is already gate-enforced. NB this decision covers PROSE drift only — the separate class of a directive silently no-oping in an unenumerated attachment position (`@name` was dropped on arrow keys once and bareword keys once, both found by hand) is a real class with its own standing system: the directive × attachment-position sweep `src/tests/dsl_position_tests.rs`, hard-asserted against `comment_dsl.mdx`'s claims. (The `example/`-half of this decision — gating the getting-started command — was accepted and shipped as `integration_tests::getting_started_example`.) A SECOND carve-out (2026-07-25): SYNTACTICALLY INVALID directive illustrations are not prose-semantics drift either, and review's \"actual rate\" proved one delivery late for them — the open-arrays doc pass shipped an inline example in the one-line directive-before-closing-bracket spelling (the CDDL comment swallows the `]`) two bullets BELOW the doc's own warning against exactly that spelling; caught in orchestrator review, one delivery after the same trap class was first documented for the map-brace flavor. The proven spelling class is now mechanically banned: `lint_doc_citations` scans `docs/docs/*.mdx` inline spans and fenced examples for a `; @<directive>` comment sharing its line with a closing `}`/`]` (self-checked canary; the warning bullet's deliberate counterexamples are allowlisted by exact span text). What STAYS declined is the general snippet-extraction harness; its reopening signal is now concrete: a docs example invalid for a reason the narrow spelling ban cannot see (any non-directive syntax error class recurring in an illustration). A THIRD instance class on record (2026-08-04), counted against the decline without reopening it: a FALSE UNIVERSAL. `current_capacities.mdx`'s tag-rule section claimed the auto-wrap holds for \"*every* single-type inner\" while the collection and `T / null` inners registered transparent aliases (the T1-13 standalone-tag-drop defect, per the tier-1 close-out's board record) — a false claim that SHIPPED past review and stood until the `register_type_alias` wire-facts assert's carve-out enumeration falsified it mechanically. It does NOT reopen the snippet harness because that harness could not have caught it either: the counterexamples were unenumerated instances of a universal quantifier, never spelled as snippets — snippet extraction validates the examples a doc gives, not the quantifier over the ones it doesn't. The layer that actually catches this class is a behavior-side seam assert whose domain IS the quantifier's (the wire-facts assert enumerates every registration, so the doc's \"every\" now has a mechanical counterpart), which is where such universals should be anchored: a doc universal about generated-code behavior earns a seam assert or a sweep whose axis is the quantified domain, and the prose then describes the mechanism rather than substituting for it. A FOURTH instance class on record (2026-08-05), counted against the decline without reopening it — and the sharpest, because here the doc and the behavior AGREED: `output_format.mdx`'s trailing-bytes paragraph stated \"(Nested/embedded decoding — e.g. `bytes .cbor T` — is unaffected; only the top-level entry point enforces this.)\" — a TRUE sentence about a DEFECT (the `.cbor` embed's missing exhaustion check, the cycle-13 fuzz find, fixed in `cff85166`). Any docs-vs-behavior conformance harness passes it by construction, since there is no divergence to detect; what was wrong was the boundary itself, stated as if decided when no decision record existed. The layer that caught it is a SPEC-side oracle over the behavior — the byte-fuzzer's preserve-fidelity leg (`fuzz_bounded_run`), whose contract comes from RFC 8610 §3.8.4 + the preserve round-trip promise rather than from our own prose — and that layer is now standing. The authoring rule this instance adds, same zero-cost class as the fixture-header rule above: a docs sentence that states a LIMITATION or asymmetric boundary names its provenance — the decided-posture record (a README gotcha, a matrix out-of-scope row) or the findings/roadmap entry that owns it — so an anchorless boundary sentence is visible at review as an undecision wearing decided clothes. Reopening signal for that rule, measurable by whoever fixes the next such defect: a second anchorless boundary sentence found to have documented a defect as intended, after this rule was in force."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.docs-vs-behavior-conformance-harness-comment-dsl-mdx"]
action_on_fire_md = """Create only the specifically demanded work without widening the settled boundary.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.docs-vs-behavior-conformance-harness-comment-dsl-mdx"]

[[record]]
id = "testing.trigger.documented-failure-shape-exercises-prose-cross-crate-int"
title = "Lifecycle review for testing.documented-failure-shape-exercises-prose-cross-crate-int."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.documented-failure-shape-exercises-prose-cross-crate-int.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.documented-failure-shape-exercises-prose-cross-crate-int"
dimension = "A documented failure SHAPE that no cell exercises is prose — the cross-crate `Int` channel has three, plus a hand mirror nothing cross-checks."
observable = "- **A documented failure SHAPE that no cell exercises is prose — the cross-crate `Int` channel has three, plus a hand mirror nothing cross-checks.** The shipped `Int`-under-`--common-import-override` coverage proves the positive paths and asserts row-ABSENCE on the degraded path, but none of the three failure shapes documented in `command_line_flags.mdx`'s `--common-import-override` section is compile-proven by a standing cell: (a) the consumer's `_borrowed_key_types_self_check` going RED when the common crate's `Int` lacks the demanded flavor (needs a de-flavored fixture variant); (b) the unresolved-import failure when the common crate exposes no `Int`/`IntError` (the existence contract's loud half); (c) `E0277` at the consumer's own map sites when the override is not a `--workspace-dep` (today only the no-row half is asserted, by `int_key_via_common_import_override_sidecar`). Related mirror exposure, to close alongside (a): `tests/extern-dep-crate`'s hand-written `Int` mirrors generated `Int`'s preserve-encodings wire format and encoding-insensitive key semantics, but `extern_deps` serializes AND deserializes through the fixture's own impls, so a generated-`Int` divergence passes silently — unlike the `tests/wasm-macro-crate` mirror, which compilation enforces. The cheap drift gate: cross-serialize one vector between a generated `Int` (any no-override cell's output) and the fixture's, asserting byte equality both ways. Reopening signal on the axis the cost grows along, measurable by the party who has the problem: a consumer hitting any of the three failure shapes and reporting that the error they got was not the one the docs describe — or a generated-`Int` wire change landing while `extern_deps` stays green."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.documented-failure-shape-exercises-prose-cross-crate-int"]
action_on_fire_md = """Set testing.documented-failure-shape-exercises-prose-cross-crate-int to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.documented-failure-shape-exercises-prose-cross-crate-int's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.documented-failure-shape-exercises-prose-cross-crate-int"]

[[record]]
id = "testing.trigger.documented-flag-pairing-broken-fixture-stays-green-because"
title = "Lifecycle review for testing.documented-flag-pairing-broken-fixture-stays-green-because."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.documented-flag-pairing-broken-fixture-stays-green-because.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.documented-flag-pairing-broken-fixture-stays-green-because"
dimension = "A DOCUMENTED flag pairing can be broken while every fixture stays green, because a fixture satisfies the pairing's preconditions by accident."
observable = "- **A DOCUMENTED flag pairing can be broken while every fixture stays green, because a fixture satisfies the pairing's preconditions by accident.** Proven instance (found by reading the fixture, not by any gate; consumer-reported first): `command_line_flags.mdx`'s own `--extern-wasm-crate` example (`--common-import-override=cml_core --extern-wasm-crate=cml_core=cml_core_wasm`) panicked at startup validation for every PURE consumer — one whose override crate is not also a declared extern dep — from the `Int`-override feature's ship until the report, because `extern_deps_wasm`'s override crate doubles as a declared dep and so satisfied the key check by accident. The exact cell the doc example describes was never executed. Standing coverage now: `common_override_wasm_int` pins the pure-consumer cell. Working rule meanwhile: a flag pairing added to the user docs names the cell that executes it, and a cell whose fixture satisfies a precondition incidentally is not that cell. Mechanical layer, on a SECOND documented pairing found broken: a sweep executing each `command_line_flags.mdx` example invocation verbatim against a minimal spec that assumes only what the example's own prose states, asserting generation exits zero. It is the documented-VALID complement of the class-level validation-smoke sweep in § Declined's flag-powerset entry (which asserts documented-INVALID values reject), and the two should be built together if either is."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.documented-flag-pairing-broken-fixture-stays-green-because"]
action_on_fire_md = """Set testing.documented-flag-pairing-broken-fixture-stays-green-because to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.documented-flag-pairing-broken-fixture-stays-green-because's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.documented-flag-pairing-broken-fixture-stays-green-because"]

[[record]]
id = "testing.trigger.doubled-doc-comment-markers-emitted-docs-proven-instance"
title = "Lifecycle review for testing.doubled-doc-comment-markers-emitted-docs-proven-instance."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.doubled-doc-comment-markers-emitted-docs-proven-instance.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.doubled-doc-comment-markers-emitted-docs-proven-instance"
dimension = "Doubled doc-comment markers in emitted docs — one proven instance recorded, no machinery yet."
observable = "- **Doubled doc-comment markers in emitted docs — one proven instance recorded, no machinery yet.** The `codegen` builder fork prefixes EVERY newline-separated line of a doc string with the marker itself, so an emission-site string embedding a literal marker to \"continue\" a multi-line doc ships doubled markers (rustdoc renders the extra slashes as literal text). Proven instance: the wasm NonEmpty wrapper doc rewording embedded literal markers after its `\\n`s and landed at HEAD inside a commit whose snapshot suite was left stale-red; the next bless folded the bug into ~660 golden snapshots and only post-bless diff REVIEW caught it — the snapshot layer pins bytes, so it flags a change but blesses a defect just as happily, and `generated_code_clippy_clean` cannot see it (no clippy lint fires on a doc comment's text). Mechanical layer if the class recurs: a catalog-wide scan asserting no doubled-marker line in any `*.snap` (cheap, pure grep). Meanwhile the working rules are: bare `\\n` in `.doc()` strings (the builder owns the markers), and the § \"Blessing changes\" discipline — review the blessed diff, never accept blind, ESPECIALLY when snapshots were already red before your change (the diff then contains someone else's unreviewed delta interleaved with yours)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.doubled-doc-comment-markers-emitted-docs-proven-instance"]
action_on_fire_md = """Set testing.doubled-doc-comment-markers-emitted-docs-proven-instance to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.doubled-doc-comment-markers-emitted-docs-proven-instance's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.doubled-doc-comment-markers-emitted-docs-proven-instance"]

[[record]]
id = "testing.trigger.dsl-position-tests-string-level-design-green-emitted"
title = "Lifecycle review for testing.dsl-position-tests-string-level-design-green-emitted."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.dsl-position-tests-string-level-design-green-emitted.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.dsl-position-tests-string-level-design-green-emitted"
dimension = "`dsl_position_tests` is string-level by design, so a cell can be GREEN while the emitted crate is broken."
observable = "- **`dsl_position_tests` is string-level by design, so a cell can be GREEN while the emitted crate is broken.** Proven instance (caught by a hand-run end-to-end repro during review, not by any gate): the `@custom_json` record-struct cell first landed passing while the generated struct kept its serde derives against a now-unskipped `encodings` field (E0277). The landed mitigation is paired anchors — the cell asserts the derive line AND the skip attribute, on both the directive and control sides — which covers known failure modes only. Working rule meanwhile: a cell whose expectation is compile-relevant asserts every attribute the compile outcome depends on, not just the one the directive names. Mechanical layer on a SECOND green-but-uncompilable cell: a `cargo check` probe over the compile-relevant cells only. Weigh it against the sweep's whole premise of cheapness (it runs in ~1 s today; a compile leg is nested-cargo-priced and would need the gate cache), which is why the probe is scoped to those cells rather than to the grid."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.dsl-position-tests-string-level-design-green-emitted"]
action_on_fire_md = """Set testing.dsl-position-tests-string-level-design-green-emitted to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.dsl-position-tests-string-level-design-green-emitted's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.dsl-position-tests-string-level-design-green-emitted"]

[[record]]
id = "testing.trigger.duplicates-dedicated-generic-collision-message"
title = "Lifecycle review for testing.duplicates.dedicated-generic-collision-message."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.duplicates.dedicated-generic-collision-message.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.duplicates.dedicated-generic-collision-message"
dimension = "Dedicated collision message for the generic-instantiation naming family."
observable = "A real generic-instantiation nominal collision reaches the misleading generic duplicate-ident message."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.duplicates-dedicated-generic-collision-message"]
action_on_fire_md = """Set testing.duplicates.dedicated-generic-collision-message to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.duplicates.dedicated-generic-collision-message's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.duplicates-dedicated-generic-collision-message"]

[[record]]
id = "testing.trigger.duplicates-member-position-extension"
title = "Lifecycle review for testing.duplicates.member-position-extension."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.duplicates.member-position-extension.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.duplicates.member-position-extension"
dimension = "Member-position `@duplicates` extension."
observable = "A third-party author requires member-position @duplicates on an inline set and will not restructure the spec by hoisting it to a named rule."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.duplicates-member-position-extension"]
action_on_fire_md = """Set testing.duplicates.member-position-extension to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.duplicates.member-position-extension's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.duplicates-member-position-extension"]

[[record]]
id = "testing.trigger.duplicates-occurrence-aware-generic-identity"
title = "Lifecycle review for testing.duplicates.occurrence-aware-generic-identity."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.duplicates.occurrence-aware-generic-identity.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.duplicates.occurrence-aware-generic-identity"
dimension = "Occurrence-aware generic-instance identity."
observable = "A consumer spec must use two occurrences of one generic definition whose bounds-blind argument spellings collide, and binding the argument shapes to distinct named rules or definitions is not acceptable."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.duplicates-occurrence-aware-generic-identity"]
action_on_fire_md = """Set testing.duplicates.occurrence-aware-generic-identity to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.duplicates.occurrence-aware-generic-identity's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.duplicates-occurrence-aware-generic-identity"]

[[record]]
id = "testing.trigger.duplicates-same-shape-set-conversion"
title = "Lifecycle review for testing.duplicates.same-shape-set-conversion."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.duplicates.same-shape-set-conversion.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.duplicates.same-shape-set-conversion"
dimension = "`From` between same-shape set instantiations."
observable = "A real spec mints two distinct nominal tag-258 set types over the same wire shape and its consumer requests member-wise conversion between that exact pair."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.duplicates-same-shape-set-conversion"]
action_on_fire_md = """Set testing.duplicates.same-shape-set-conversion to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.duplicates.same-shape-set-conversion's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.duplicates-same-shape-set-conversion"]

[[record]]
id = "testing.trigger.e0463-t-find-crate-core-scratch-tree-std"
title = "Watch escalation for testing.e0463-t-find-crate-core-scratch-tree-std."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Watch escalation for testing.e0463-t-find-crate-core-scratch-tree-std.
"""
transition_kind = "watch_escalation"
failure_signature_md = """Scratch no-std `E0463: can't find crate for core` outside the repository pin.
"""
capture_procedure_md = """Capture cwd, exact cargo/rustc/toolchain/target, target-installed status, baseline differential, and pinned-toolchain rerun.
"""
response_md = """Rerun under the repository pin before product attribution.
"""
escalation_action_md = """Escalate only if the same shim fails E0463 with the target installed under the exact repository pin.
"""
retirement_semantics_md = """Retire when every ad-hoc scratch recipe spells the pin and a durable procedure enforces it.
"""
evaluation = "unknown"

[[record]]
id = "testing.trigger.edn-text-notation-round-trip-edn-edn-draft"
title = "Lifecycle review for testing.edn-text-notation-round-trip-edn-edn-draft."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.edn-text-notation-round-trip-edn-edn-draft.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.edn-text-notation-round-trip-edn-edn-draft"
dimension = "EDN text-notation round-trip (`to_edn` / `from_edn`, draft-ietf-cbor-edn-literals)."
observable = "- **EDN text-notation round-trip (`to_edn` / `from_edn`, draft-ietf-cbor-edn-literals).** EDN is the text notation that *can* be byte-faithful where JSON structurally cannot (encoding indicators, NaN payload bits — JSON's number model collapses both), making it the natural third channel: JSON as the value-level lossy view by charter, CBOR as the authoritative bytes, EDN as the human-readable byte-faithful text form. The maintainer's `cbor_event_edn` library already handles the hard edge cases (NaN payloads included) and is the natural base. Deferred because the EDN spec is an evolving IETF draft — a generated-crate surface built against it now risks churn with every draft revision. Reopening signal: the draft stabilizes (late-stage or RFC) or a real consumer needs byte-faithful text round-trips sooner. Mechanical layer: a property over the `AnyCbor` fuzz corpus asserting `from_edn(to_edn(x))` byte-identity against the existing span oracle (`src/tests/any_cbor_tests.rs`)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.edn-text-notation-round-trip-edn-edn-draft"]
action_on_fire_md = """Set testing.edn-text-notation-round-trip-edn-edn-draft to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.edn-text-notation-round-trip-edn-edn-draft's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.edn-text-notation-round-trip-edn-edn-draft"]

[[record]]
id = "testing.trigger.emission-branch-generated-output-fixture-exercises-invisible-gate"
title = "Lifecycle review for testing.emission-branch-generated-output-fixture-exercises-invisible-gate."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.emission-branch-generated-output-fixture-exercises-invisible-gate.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.emission-branch-generated-output-fixture-exercises-invisible-gate"
dimension = "An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate — it can rot to uncompilable and hide behavioral bugs in both directions."
observable = "- **An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate — it can rot to uncompilable and hide behavioral bugs in both directions.** First of a family of three, each recorded separately below because each needs a different detector, and all three answering \"a fixture exists, so why did it not witness this?\" with a different mode: NEVER EXECUTED (here), EXECUTED BUT UNASSERTED (\"Value-anchor rot on field addition\"), and EXECUTED AGAINST THE WRONG SHAPE (\"A fixture that models an OBSOLETE user contract stays green while the real contract path is broken\"). Proven instance (plan-review-caught during the cbor_event 3.2.0 upgrade, not by any gate): the nullable-`Special` null-peek (the `Optional`-with-Special arm in `generation/deserialize.rs`) had ZERO checked-in output — no snapshot, export, or suite fixture contained its emission — and had decayed to emitting a receiver-less `match cbor_type()?` (E0425, uncompilable) around a hardcoded 1-byte rewind that both ACCEPTED malformed two-byte simples (`f8 f5` read as `Some(true)`) and REJECTED valid 9-byte floats. Compile, round-trip, and conformance gates were all blind because no input ever reached the branch. Standing coverage now: the `nullable_specials` fixtures (core + preserve suites) execute the emission in both directions. Working rule meanwhile: a new emission branch lands WITH a fixture whose generated output exercises it (compiling the generator is not evidence the emitted string compiles). The mechanical layer on a second instance: a generator branch-coverage sweep — e.g. llvm-cov over the corpus + suite generation runs, red on emission arms no fixture executes — the only layer that catches the class without knowing each branch by name."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.emission-branch-generated-output-fixture-exercises-invisible-gate"]
action_on_fire_md = """Set testing.emission-branch-generated-output-fixture-exercises-invisible-gate to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.emission-branch-generated-output-fixture-exercises-invisible-gate's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.emission-branch-generated-output-fixture-exercises-invisible-gate"]

[[record]]
id = "testing.trigger.emit-tests-gate-asserts-round-trip-green-cannot"
title = "Lifecycle review for testing.emit-tests-gate-asserts-round-trip-green-cannot."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.emit-tests-gate-asserts-round-trip-green-cannot.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.emit-tests-gate-asserts-round-trip-green-cannot"
dimension = "An `--emit-tests` gate that asserts only \"the round-trip is green\" cannot tell an INTENDED minted value from a wrong one that happens to land in-window."
observable = "- **An `--emit-tests` gate that asserts only \"the round-trip is green\" cannot tell an INTENDED minted value from a wrong one that happens to land in-window.** Proven by the bounded-map-key minter: choosing a `nint` key base in VALUE space emitted that base as a u64 MAGNITUDE, so `{* nint .le -5 => uint}` minted magnitude 18446744073709551611 — wire value about -1.8e19, which SATISFIES `<= -5`, so the emitted round-trip passed. A false green, and the harder half to notice: the sign-mirror `{* nint .ge -5 => uint}` failed loudly on the identical wrong base, so a fixture carrying only ONE sign of the window would have certified the defect in whichever direction it was pointed. The conformance oracle (`--emit-tests-conformance`) cannot see this class either — the minted value is genuinely spec-VALID, merely degenerate — which is why it needs a layer of its own rather than a wider oracle. The standing rule that covers this today is IN FORCE and lives in `tests/README.md` § \"Authoring standard for a bounded-domain emit-tests fixture\" (both signs/endpoints of the window; the gate pins the minted SPELLING, compared whitespace-stripped, not just the round-trip verdict), with `tests/emit-tests-bounded-key/` as that fixture. What remains future here is only the mechanical layer: a mint-intent assertion — derive each bounded mint's expected literal from the same `MintValue` the renderer consumes and diff the two — so that no bounded mint can be wrong-but-passing. Trigger, on the axis the cost grows along: the COUNT of minted-value sites whose STORAGE space differs from the CDDL value space, measurable by whoever next adds a bounded mint. Today that count is one (the `N64` magnitude, transformed through `nint_bounds_to_u64`); a SECOND such site means the working rule is being carried by hand in more than one place, and hand-carrying is what produced this instance."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.emit-tests-gate-asserts-round-trip-green-cannot"]
action_on_fire_md = """Set testing.emit-tests-gate-asserts-round-trip-green-cannot to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.emit-tests-gate-asserts-round-trip-green-cannot's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.emit-tests-gate-asserts-round-trip-green-cannot"]

[[record]]
id = "testing.trigger.emitted-shape-lint-classes-outside-clippy-all-beyond"
title = "Lifecycle review for testing.emitted-shape-lint-classes-outside-clippy-all-beyond."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.emitted-shape-lint-classes-outside-clippy-all-beyond.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.emitted-shape-lint-classes-outside-clippy-all-beyond"
dimension = "Emitted-shape lint classes OUTSIDE `clippy::all` are beyond `generated_code_clippy_clean`'s reach — the wasm-boundary clone-of-owned class stays ledgered, no machinery yet."
observable = "- **Emitted-shape lint classes OUTSIDE `clippy::all` are beyond `generated_code_clippy_clean`'s reach — the wasm-boundary clone-of-owned class stays ledgered, no machinery yet.** The boundary ops (`from_wasm_boundary_clone`) clone every non-Copy expr regardless of the call site's ownership, so a wasm ctor over an owned last-use arg emits `Holder::new(val.clone())` — a redundant clone `clippy::redundant_clone` would flag but is nursery-tier, so the gate's `-D clippy::all` cannot see it; behavior and bytes are unaffected (allocation cost only). One neighboring FLAVOR of this class is now retired at the source and needs no ledger: the rust `From`/`TryFrom` wrapper impls used to route their owned `inner` through the same boundary ops (`new(inner.clone().into())`), but they now construct via `new(inner)` directly — the `inner` is by-value and already `new()`'s exact param type, so both the clone and the `.into()` were identity (also a `clippy::useless_conversion` the gate DOES see, provoked by `clippy_wrapped_map` in `generated_code_clippy_clean`'s input). The wasm-boundary clone-of-owned flavor above stays ledgered with unchanged disposition, mirroring the curated rustc style-deny precedent (`unused_parens` et al.): evaluate specific beyond-`all` lints one at a time, adding a per-lint deny only if it is currently green-able on every one of the gate's cases (nursery lints carry known false positives). Act on a second instance or a consumer report, not before."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.emitted-shape-lint-classes-outside-clippy-all-beyond"]
action_on_fire_md = """Set testing.emitted-shape-lint-classes-outside-clippy-all-beyond to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.emitted-shape-lint-classes-outside-clippy-all-beyond's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.emitted-shape-lint-classes-outside-clippy-all-beyond"]

[[record]]
id = "testing.trigger.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
title = "Lifecycle review for testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
dimension = "An emitter that DROPS an overloadable parameter by building a fresh config is invisible to the lint that catches one which hardcodes the default."
observable = "- **An emitter that DROPS an overloadable parameter by building a fresh config is invisible to the lint that catches one which hardcodes the default.** The hardcoding half is covered: `snapshot_tests::emitter_overload_no_bare_default_tokens` (fast tier, current state in `tests/README.md` § \"Snapshot tests\") fails any overload-scoped emitter fn whose emitted literal spells a bare `raw`/`serializer`, and its arming run cleared both axes — the four historical deserialize leaves stay fixed, and the one serializer leaf it found (`end_len`'s `is_end` branch, reachable only with the default name today because every overload site sets `is_end(false)`) was fixed in the same change. What that lint cannot see is the OTHER shape the same four leaves produced: a helper that builds a fresh `DeserializeConfig::new(..)` / `SerializeConfig::new(..)` instead of threading the caller's, so every literal below it correctly spells an accessor that resolves to the default. The emitted text is indistinguishable from correct; only the config's provenance is wrong. A mechanical layer would be a construction lint — a config built inside an overload-scoped fn must inherit rather than start fresh — with the legitimate fresh-config boundaries (the array element, the map key/value, the canonical map key) as its allowlist. Deferred because it would have no work to do on the current tree: all four of today's fresh-config constructions inside an overload-scoped fn sit at exactly those boundaries and each re-threads the overload explicitly (`elem_config.deserializer_name_overload = config.deserializer_name_overload;` and its two map twins; the canonical map key's own `buf`). Reopening signal, on the axis the cost grows along: the COUNT of fresh-config constructions inside overload-scoped fns, measurable by whoever next adds one — today it is four, all at the named boundaries, so a fifth that is NOT one of them is the instance that makes the boundary list worth writing. The lint's OTHER gap — a helper receiving the name under a parameter spelling the scoping rule does not know, which would put every literal below it out of scope — is closed rather than deferred: `snapshot_tests::emitter_overload_lint_scopes_every_name_param` (fast tier) scans the same emitter sources for name-typed `serializ` parameters and fails any spelled outside the rule's vocabulary, which the scoping rule and the guard now share as one const pair (current state in `tests/README.md` § \"Snapshot tests\")."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.emitter-drops-overloadable-parameter-building-fresh-config-invisible"]
action_on_fire_md = """Set testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.emitter-drops-overloadable-parameter-building-fresh-config-invisible"]

[[record]]
id = "testing.trigger.extend-recombination-member-kind-table-optional-fixed-shape"
title = "Lifecycle review for testing.extend-recombination-member-kind-table-optional-fixed-shape."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.extend-recombination-member-kind-table-optional-fixed-shape.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.extend-recombination-member-kind-table-optional-fixed-shape"
dimension = "Extend the recombination member-kind table to the OPTIONAL-FIXED shape."
observable = "- **Extend the recombination member-kind table to the OPTIONAL-FIXED shape.** `MEMBER_KINDS` now carries a per-kind aux-rule slot (`%A%`), so a kind that needs a named rule of its own is a one-row edit rather than a mechanism change — the `tagged_optional` row (`%K%: #6.10(%A%)` over `uint / null`) is the worked example, and `optional_fixed_member.cddl` / `optional_fixed_float.cddl` name the shape the table still cannot compose: `? %K%: 5`, an optional member whose type is a fixed value, which mints a `bool` presence field instead of a value and so reaches a deserialize seam the `optional` and `fixed_*` kinds each only half touch. It is one row, and the reason it is not simply built is that a kind addition is not free: it RESHUFFLES every seeded draw downstream of the table, so each one re-samples the triples and depth-2 fillers onto different compositions and can surface unrelated pre-existing defects that must be fixed or promoted before the row can land. Adding `tagged_optional` cost two emitter fixes and one promoted panic class; budget the next row the same way rather than as a table edit. Reopening signal, on the axis the deferred cost grows along: a SECOND exit-0 uncompilable-or-wrong-bytes crate in a supported composition the member-kind table does not span (each instance is a loud build or round-trip failure for whoever generates the shape, so the reporter exists by construction)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.extend-recombination-member-kind-table-optional-fixed-shape"]
action_on_fire_md = """Set testing.extend-recombination-member-kind-table-optional-fixed-shape to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.extend-recombination-member-kind-table-optional-fixed-shape's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.extend-recombination-member-kind-table-optional-fixed-shape"]

[[record]]
id = "testing.trigger.extern-deps-wasm-boundary-surface-packaging-json-gen"
title = "Lifecycle review for testing.extern-deps-wasm-boundary-surface-packaging-json-gen."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.extern-deps-wasm-boundary-surface-packaging-json-gen.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.extern-deps-wasm-boundary-surface-packaging-json-gen"
dimension = "Extern-deps wasm-boundary surface: packaging- and json-gen-gaps beyond the behavioral floor."
observable = "A consumer or breadth fixture exposes the named wasm-bindgen CLI, extern json-gen, or packaging boundary outside the current compile controls."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.extern-deps-wasm-boundary-surface-packaging-json-gen"]
action_on_fire_md = """Set testing.extern-deps-wasm-boundary-surface-packaging-json-gen to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.extern-deps-wasm-boundary-surface-packaging-json-gen's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.extern-deps-wasm-boundary-surface-packaging-json-gen"]

[[record]]
id = "testing.trigger.extern-interface-export-dialect-v2-candidates"
title = "Lifecycle review for testing.extern-interface-export-dialect-v2-candidates."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.extern-interface-export-dialect-v2-candidates.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.extern-interface-export-dialect-v2-candidates"
dimension = "Extern-interface export dialect v2 candidates."
observable = "- **Extern-interface export dialect v2 candidates.** Each bumps the seam header (`_CDDL_CODEGEN_EXTERN_INTERFACE_ v1` — unknown versions hard-error, pinned by `extern_import_unknown_version_hard_errors`), so batch them when one gets a real consumer: (1) per-variant name pins for c-style enums — the type-level `@rust_name` pin covers the enum name but variant names stay consumer-derived, so a variant-naming-rule change reintroduces the cross-version skew class on exactly that row (reopening signal: a consumer compile break naming a dep enum variant after a codegen naming change); (2) synthesized idents for anonymous generic instances — `[* pair<uint,tstr>]` in a dep today closure-excludes the referencing rule (reopening signal: a real dep's export carries that `; unexported:` record and a consumer needs the rule); (3) transparent spellings for the renderer's known lossy-IR case — a two-sided `float32` window has no faithful CDDL form (a literal float range re-parses as `float64`), so such rules exclude-with-record (reopening signal: same, for a float32-windowed alias); (4) generic PARAMETERS on an exported extern base — the renderer spells every rule body as a bare marker, so `ext_set<t0> = _CDDL_CODEGEN_EXTERN_TYPE_` projects param-less and a consumer can reference the base but never instantiate it. That is why `@raw_bytes_flavor` does not ride the seam: it flavors a generic INSTANCE, so on the param-less form it is unhonorable, and re-parsing it is exactly the spelling the non-generic-extern rejection refuses (pinned by `extern_import_flavored_generic_base_projects_without_the_tag`). Carrying the params would restore both halves at once. Reopening signal: a consumer that needs to instantiate a dependency's generic extern over `--extern-import` — its own spec cannot spell the instantiation, so the block is visible to the party that has it, not inferred by us."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.extern-interface-export-dialect-v2-candidates"]
action_on_fire_md = """Set testing.extern-interface-export-dialect-v2-candidates to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.extern-interface-export-dialect-v2-candidates's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.extern-interface-export-dialect-v2-candidates"]

[[record]]
id = "testing.trigger.extreme-value-boundary-correctness-generation-time-encode-paths"
title = "Lifecycle review for testing.extreme-value-boundary-correctness-generation-time-encode-paths."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.extreme-value-boundary-correctness-generation-time-encode-paths.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.extreme-value-boundary-correctness-generation-time-encode-paths"
dimension = "Extreme-value boundary correctness of generation-time encode paths: a value that FITS the type can still encode wrong at the type's boundary, and no fixture minted from \"supported shapes\" ever lands there."
observable = "- **Extreme-value boundary correctness of generation-time encode paths: a value that FITS the type can still encode wrong at the type's boundary, and no fixture minted from \"supported shapes\" ever lands there.** Proven instance (read-caught during the `FixedValue` i128/u64 widening, not by any test): `FixedValue::to_bytes` — the generation-time canonical-ordering encoder for fixed map keys — called cbor_event's `write_negative_integer(*i as i64)`, which produced wrong bytes for `i64::MIN` on the then-current cbor_event 2.4.0 (`-i64::MIN` overflowed i64; the 3.x endpoint computes in i128), so a canonical spec with a fixed `-9223372036854775808` map key would have gotten wrong key ordering silently. `i64::MIN` is representable in the old `isize` field, so this was a correctness hole at a representable boundary — a different class from the representability widening the nint entry in `cddl-matrix/ROADMAP.md` scopes (that entry even named this call site, but as a range cap, and its \"generation already uses `_sz` where the `i64` limit bites\" premise was exactly what the bug falsified). Standing coverage now: `nint_to_bytes_canonical_across_boundaries` pins hard-coded canonical bytes across the full magnitude ladder + `i64::MIN` through `to_bytes`, and `i64_min_fixed_value_emits_width_correct_nint` pins the emitted serialize line under default + preserve profiles. What no gate yet does: thread each numeric extreme (`i64::MIN`/`MAX`, `u64::MAX`, the `±2^63±1` off-by-ones) through every POSITION class systematically — fixed value, fixed map key under `--canonical`, range/bounds windows, deserialize mismatch arms — in a generated crate whose emitted tests execute. A SECOND read-caught extreme-value bug in any encode/compare path is the trigger to build that boundary-vector sweep as a corpus/matrix axis rather than hand-pinning per instance."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.extreme-value-boundary-correctness-generation-time-encode-paths"]
action_on_fire_md = """Set testing.extreme-value-boundary-correctness-generation-time-encode-paths to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.extreme-value-boundary-correctness-generation-time-encode-paths's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.extreme-value-boundary-correctness-generation-time-encode-paths"]

[[record]]
id = "testing.trigger.file-listing-json-schema-root-values-flag-per"
title = "Lifecycle review for testing.file-listing-json-schema-root-values-flag-per."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.file-listing-json-schema-root-values-flag-per.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.file-listing-json-schema-root-values-flag-per"
dimension = "A file listing `--json-schema-root` values, instead of one flag per root."
observable = "- **A file listing `--json-schema-root` values, instead of one flag per root.** The repeatable flag is what shipped, on the grounds that it matches every other repeatable flag and that the asking consumer's eight entries do not justify a new file format. A file-listing variant stays purely additive (same emission, a different way to spell the list), so it is a cost question a real consumer settles: build it when someone's root list is large enough that a command line stops being the right place for it. Note what such a file must NOT become — the paths are Rust, not CDDL, and routing them through the spec is the category error the flag exists to avoid."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.file-listing-json-schema-root-values-flag-per"]
action_on_fire_md = """Set testing.file-listing-json-schema-root-values-flag-per to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.file-listing-json-schema-root-values-flag-per's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.file-listing-json-schema-root-values-flag-per"]

[[record]]
id = "testing.trigger.finding-s-defect-side-attribution-recorded-narrative-plausibility"
title = "Lifecycle review for testing.finding-s-defect-side-attribution-recorded-narrative-plausibility."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.finding-s-defect-side-attribution-recorded-narrative-plausibility.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.finding-s-defect-side-attribution-recorded-narrative-plausibility"
dimension = "A finding's DEFECT-SIDE attribution recorded by narrative plausibility, not execution — the RECORDING-side twin of the closure-side reproduce-at-parent rule above."
observable = "- **A finding's DEFECT-SIDE attribution recorded by narrative plausibility, not execution — the RECORDING-side twin of the closure-side reproduce-at-parent rule above.** First instance (caught at item pickup, cycle 10 of the burndown effort): the present-null-optional finding (recorded `63feb259`, retired `78aab6d3`) attributed the dropped state to the CBOR re-encode and the faithful behaviour to the json round-trip; worktree execution AT THE RECORDING COMMIT showed the reverse on both surfaces (CBOR re-encoded `820082190338f6` byte-exact; the json detour came back `820081190338`). The inversion was structural, not a typo's risk profile: the surfacing gate (`corpus_decode_replay`'s json leg) proves the two decode surfaces DISAGREE — a SYMMETRIC fact — and the prose then named a culprit the gate never identified. No existing layer could have caught it: the `JSON_SURFACE_SKIP` guard asserts only that the cell STILL FAILS, which stayed true (the disagreement was real, whichever side owned it), so an inverted attribution stays green indefinitely; and the findings-claims arm (`project_corpus.ts` check `I`) checks citation RESOLUTION, not claim semantics — the same boundary the entry above records it stopping at. Cost while it stood: two burndown triage passes carried the row with its fix shape pointed at the WRONG surface (\"fix serialize for nullable-inner optionals\"), and only the pickup re-probe rule (AGENTS.md) prevented a delivery built against it. Working rule, now, in place of a mechanical layer: **a finding that attributes a defect to ONE of several surfaces or directions quotes each surface's executed probe output — bytes in, bytes out, per surface, at the recording sha — in the entry; a disagreement gate licenses recording the disagreement, and only an executed per-surface probe licenses naming the culprit.** (Symmetric evidence for a symmetric fact; the closure-side rule above already demands the same discipline when a finding is retired.) The mechanical layer, if the class recurs: skip-ledger rows carry a machine-checkable failure SIGNATURE (which leg diverges, and the divergent bytes) that the replay guard executes every run, so a recorded attribution is re-asserted continuously instead of trusted from its recording day. Reopening signal: a SECOND finding whose recorded surface/direction attribution is falsified at pickup — measured by the pickup party, who is already re-probing premises under AGENTS.md's rule, so the observation arrives as a by-product of work being done anyway."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.finding-s-defect-side-attribution-recorded-narrative-plausibility"]
action_on_fire_md = """Set testing.finding-s-defect-side-attribution-recorded-narrative-plausibility to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.finding-s-defect-side-attribution-recorded-narrative-plausibility's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.finding-s-defect-side-attribution-recorded-narrative-plausibility"]

[[record]]
id = "testing.trigger.fixture-models-obsolete-user-contract-stays-green-real"
title = "Lifecycle review for testing.fixture-models-obsolete-user-contract-stays-green-real."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.fixture-models-obsolete-user-contract-stays-green-real.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.fixture-models-obsolete-user-contract-stays-green-real"
dimension = "A fixture that models an OBSOLETE user contract stays green while the real contract path is broken — the wrong-shape member of the fixture-blindness family headed by \"An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate\"."
observable = "- **A fixture that models an OBSOLETE user contract stays green while the real contract path is broken — the wrong-shape member of the fixture-blindness family headed by \"An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate\".** Proven instance (consumer-reported, not by any gate): every raw-bytes fixture appended the user-supplied `_CDDL_CODEGEN_RAW_BYTES_TYPE_` definition INTO `rust/src/generated/mod.rs` — the pre-thin-root layout no real user can have (that subtree is clobbered every regen) — so bare references resolved locally and all raw-bytes gates stayed green while the extern re-export glue (`pub use crate::Name;`) covered only `RustStructType::Extern`, not `RawBytesType`; regenerating CML's cip36 (`public_key = _CDDL_CODEGEN_RAW_BYTES_TYPE_` referenced only through `pub type` aliases, which have no other resolution path) failed E0412 at five sites. Standing coverage now: the fix routes raw-bytes defs through the real contract (appended to the user-owned thin `lib.rs`, same as extern-type defs), and the glue's own `pub use crate::<Name>;` line makes any regression to the masked routing fail loudly (E0255 duplicate definition) rather than silently re-masking. The WASM half of the incident was the class's second occurrence, caught by interrogating this entry's own first write-up rather than by any gate: the wasm glue covered only `Extern` wrappers while the fixture appended the wasm raw-bytes def into `wasm/src/generated/mod.rs` (the same no-real-user-contract residence), proven by a cip36-shaped scratch repro — rust crate compiled via the landed rust glue, wasm crate failed 3× E0425 `PubKey` (the consumer's rust E0412 had masked the wasm failure, since the regen never got past the rust crate). Both halves are now standing coverage: glue covers `Extern | RawBytesType` in BOTH crates, both def routings model the real crate-root contract (thin `lib.rs`), and regression to the masked residence collides E0255 with the glue's own re-export in either crate. The formerly-latent alias-walk sibling (`scope_references` walked `rust_structs` only, never `type_aliases`, so a CROSS-scope alias to a non-extern type under-imported its target — E0412) got its failing case on a production project and is fixed: the type-alias walk marks each emitted alias's `base_type` (and, wasm-side, the `resolved_wasm_alias_target` wrapper — a helper shared with the wasm alias emitter so the emitted target and its import cannot drift), with standing coverage from the placement matrix's `aliased` reference mode (every INCLUDED shape × `bal = <ty>` in module `b`; the extern-shape exclusion's alias-position residue — the generic-extern-instance `Base<Args>` class, feature request 07 — is hand-pinned by `tests/extern-generic-scoped`, see \"Multifile reference-POSITION coverage\" below). Working rule meanwhile: a fixture that injects hand-written code into `generated/**` carries a comment justifying the residence — and the justification must name a contract a real user can follow, or explicitly flag itself as a modeled-obsolete shape citing this entry (the `use super::*;` serializer-helper appends qualify). Mechanical layer on the NEXT instance: an audit sweep enumerating every test append-site that writes into a `generated/**` path and failing any not on a justified allowlist — the wasm half already being the class's second occurrence is the argument for building it on any further sighting rather than deferring again."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.fixture-models-obsolete-user-contract-stays-green-real"]
action_on_fire_md = """Set testing.fixture-models-obsolete-user-contract-stays-green-real to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.fixture-models-obsolete-user-contract-stays-green-real's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.fixture-models-obsolete-user-contract-stays-green-real"]

[[record]]
id = "testing.trigger.fixture-s-verdict-accident-cargo-feature-unification-harness"
title = "Lifecycle review for testing.fixture-s-verdict-accident-cargo-feature-unification-harness."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.fixture-s-verdict-accident-cargo-feature-unification-harness.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.fixture-s-verdict-accident-cargo-feature-unification-harness"
dimension = "A fixture cell's verdict can be an accident of cargo feature unification: harness-spliced hand code needing a feature a dep reshape disabled stays green whenever a SIBLING dep in the same fixture graph re-enables that feature — so the red arrives on a later graph change, not on the change that broke the code."
observable = "- **A fixture cell's verdict can be an accident of cargo feature unification: harness-spliced hand code needing a feature a dep reshape disabled stays green whenever a SIBLING dep in the same fixture graph re-enables that feature — so the red arrives on a later graph change, not on the change that broke the code.** Proven instance (D2, the alloc-mode dep reshape): two committed hand fragments (`tests/custom_serialization`, `tests/custom_serialization_preserve`, spliced into fixture crates by the integration harness) boxed `hex::FromHexError` as `Box<dyn Error>` — an impl hex gates on ITS `std` feature, which the reshape's `default-features = false` dropped. The non-preserve fragment failed three `local`-tier cells immediately (`core_no_wasm`/`core_with_wasm`/`emit_wasm_tests_execute` — the net that works, and the third instance of the owning-family lesson: the owning family of a DEP RESHAPE includes every fixture whose hand code uses that dep, a set not enumerable from the ops table, so the reshape delivery now sweeps the committed hand-file set as part of its battery). The preserve twin stayed green PRE-fix for a reason that had nothing to do with the code: its fixture graph carries the `cddl` conformance-oracle dep, whose own transitive `hex` uses default features, and cargo unifies features per graph — so `hex/std` was back on and the missing impl restored (`cargo tree` evidence in the fix-forward log). Both fragments were fixed the consumer way (a local newtype with verbatim-delegating `Debug`/`Display` + `core::error::Error` — the in-repo migration exemplar cited by the no_std workstream's D5 notes) rather than by re-enabling the dep's defaults, precisely so the fixtures model the post-reshape world; but the masked-pass CLASS remains: any spliced hand fragment whose compile needs a reshaped-away feature is invisible while some oracle-carrying sibling re-unifies it. Coverage boundary, stated so the deferral is scoped: the no_std drift gate (workstream D3) structurally covers the TOOL-PRODUCT half — fresh standalone crates checked through `default-features = false` shims with no oracle in the graph — and never the fixture-hand-code half, because its profiles deliberately exclude the harness appendages. Machinery on a trigger, not now (the class has exactly the two members, both fixed): a `cargo check --lib`-only cell per spliced-hand-fragment fixture family — lib-only checking keeps dev/oracle deps out of the unification graph, so the fragment must compile on the fixture's own declared features. *Reopening signal:* a third instance — a fixture cell flipped red by a dep-graph or oracle-version change with no edit to the fixture itself, or a reshape delivery's hand-file sweep finding a fragment that compiles only under unification. Measurable by exactly the party who hits it (the tier runner reading the E0277 against an unchanged fixture), on the dimension the cost grows (the count of spliced hand fragments using reshaped-dep surfaces)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.fixture-s-verdict-accident-cargo-feature-unification-harness"]
action_on_fire_md = """Set testing.fixture-s-verdict-accident-cargo-feature-unification-harness to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.fixture-s-verdict-accident-cargo-feature-unification-harness's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.fixture-s-verdict-accident-cargo-feature-unification-harness"]

[[record]]
id = "testing.trigger.fixture-shape-evicted-over-known-defect-stale-guard"
title = "Lifecycle review for testing.fixture-shape-evicted-over-known-defect-stale-guard."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.fixture-shape-evicted-over-known-defect-stale-guard.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.fixture-shape-evicted-over-known-defect-stale-guard"
dimension = "A fixture SHAPE evicted over a known defect has no stale-guard, so the fix never re-adds it."
observable = "A second fixture edit removes a supported spelling and cites a known defect as the reason."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.fixture-shape-evicted-over-known-defect-stale-guard"]
action_on_fire_md = """Set testing.fixture-shape-evicted-over-known-defect-stale-guard to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.fixture-shape-evicted-over-known-defect-stale-guard's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.fixture-shape-evicted-over-known-defect-stale-guard"]

[[record]]
id = "testing.trigger.full-n-flag-powerset-pict-pairwise"
title = "Lifecycle review for testing.full-n-flag-powerset-pict-pairwise."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.full-n-flag-powerset-pict-pairwise.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.full-n-flag-powerset-pict-pairwise"
dimension = "Full `2^N` flag powerset / PICT pairwise"
observable = "- **Full `2^N` flag powerset / PICT pairwise** — the curated named profiles cover the flag *combinations* worth testing, so the full powerset stays out of scope. Escaped interactions earn their own standing cells rather than the whole powerset — four so far (the Fourth is recorded after the Third below, with a now-armed trigger). First: `--common-import-override` × `--preserve-encodings=false` targeting a preserve-flavored common crate emitted `CBORReadLen::new(Len)` against a `new(LenSz)` runtime (E0308). Second: `--workspace-dep` × `--wasm=false` silently ignored the flag — validation included — fixed to mode-independent honoring and pinned by `workspace_dep_unknown_is_rejected_under_wasm_false` plus the flavored contract's rust-only byte-identity leg. Its read-caught sibling, the `--extern-wrapper-index` validation skip under `--wasm=false`, is likewise fixed to mode-independent validation (deferral still wasm-gated) and pinned by `extern_wrapper_index_is_validated_under_wasm_false`. Third, an EMISSION-path flavor (not validation-inertness): `--preserve-encodings=true` × `--annotate-fields=false` — no profile combines them, and `flag_value_smoke`'s annotate=false case runs a spec with no encoding-less fixed member, so the combination's fixed-value deserialize paths were unreachable by every gate. Two bugs lived there: bool/null fixed members emitted a bare `()` with no statement terminator (non-parsing output), and uint/nint/text fixed MAP values bound their inline `{field}_encoding` over the arm's outer accumulator (E0308 assign-to-shadow) — the first found by reading during the optional-fixed-value delivery, the second at the arming run of the standing cell that now owns the combination (`preserve_no_annotate_fixed_members_generate`, generating both fixed-member corpus fixtures under the pair). Both fixed. Fourth, the same input-poverty mechanism as the Third on a different emit path: `--annotate-fields=false` (non-preserve) × a value-bounded `nint` MEMBER — the N64 bounds `.and_then` relied solely on the site's `error_convert`, which is empty under annotate=false, so the closure inferred the reader's native `cbor_event::Error` and the emitted crate failed E0277. `flag_value_smoke` swept the flag the whole time but its input (`tests/canonical/input.cddl`) had no bounded nint, so the path was unreachable by every gate until the `generated_code_clippy_clean` provocation shape (`clippy_neg_bounded`) entered that shared input and turned the smoke red. Fixed (convert-at-most-once, pinned by `deserialize_converts_error_at_most_once`; the shape stays in the smoke's input permanently). Cross-check at discovery: no cddl-matrix item covers this axis — the matrix enumerates INPUT surface under the named profiles, and the corpus already held the provoking shape (`bounds_spellings.cddl`'s `m_nint_range`, verified red-reproducing under annotate=false at the pre-fix rev) — the unswept dimension was the FLAG value. That second input-poverty instance armed the sub-class trigger, and the named layer is BUILT: the `feature_corpus_compiles_no_annotate_shard_NN` leg sweeps the whole feature corpus under two gate-local flavor rows (plain `--annotate-fields=false` and `--preserve-encodings=true --annotate-fields=false`), rust-crate-only because the emitted `wasm/` tree is byte-identical with the flag on or off on every corpus fixture under both rows. The corpus is the shape-rich input the canonical smoke is not, and `NO_ANNOTATE_FLOOR_STEMS` pins the three provoking stems (`bounds_spellings`, `fixed_bool_member`, `optional_fixed_member`) as present AND swept so pruning cannot re-open the hole. `tests/README.md` § the corpus compile gate carries the full description; the three emission classes it found on its first run are fixed, and the semantic sibling that a compile floor structurally cannot be (`no_annotate_reframed_emissions_round_trip_non_canonical_wire_byte_exactly`) rides beside it, so the combination is swept for BUILDABILITY and for byte-exact fidelity. The remaining recur-first lesson from the first three: a THIRD validating flag turning up mode-inert is the trigger to build the class-level validation-smoke sweep — each clap flag with documented startup validation invoked once with a deliberately invalid value under each `--wasm` mode, asserting nonzero exit — rather than accreting per-flag standing cells. The extern-deps surface is now probed under both preserve flavors — `integration_tests::extern_deps` (preserve) and `integration_tests::extern_deps_non_preserve` (non-preserve, compiled against the preserve-flavored `extern-dep-crate` stand-in) — plus the wasm-boundary cell `integration_tests::extern_deps_wasm` (`--extern-wasm-crate` against the split `extern-dep-crate`/`extern-dep-crate-wasm` pair; extern types as list elements and table keys/values, from root AND non-root use sites — the non-root `nested` cells pin the wrapper-element imports registered from the wrapper's emission scope, which root use sites would mask) — so those specific cells are pinned without enumerating the rest. Adjacent but on an orthogonal axis to the First interaction's runtime-flavor mismatch (that one is a `CBORReadLen` construction seam; this one is a derive seam): the cross-crate `Int` **key**-flavor channel under `--common-import-override` is pinned by `int_key_via_common_import_override_sidecar` (consumer — the `borrowed_key_types.rs` `(<override>, \"int\")` row and its self-check compiling against the `extern-dep-crate` fixture's key-capable `Int`) and the `int` leg of `workspace_key_requests_derive_effect_and_hard_errors` (dep — an `int` `--key-requests` row emits a key-flavored `Int` a spec never references, while a `uint` row still hard-errors), with the `extern_deps`/`extern_deps_wasm` cells and the pure-consumer cell `common_override_wasm_int` (override crate NOT also a declared extern dep) covering the plain `Int`/`IntError` re-export path. Every individual flag *value* now appears in some profile or test: the five that previously didn't are covered by `flag_value_smoke` (`--annotate-fields=false`, `--to-from-bytes-methods=false`, `--binary-wrappers=true`), `wasm_cbor_json_api_macro_compiles` (`--wasm-cbor-json-api-macro`), and — for `--canonical-form=true` without `--preserve-encodings`, which emitted a non-compiling crate — a CLI rejection (`api::with_types`, pinned by `flag_value_rejects_canonical_without_preserve`)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.full-n-flag-powerset-pict-pairwise"]
action_on_fire_md = """Create only the specifically demanded work without widening the settled boundary.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.full-n-flag-powerset-pict-pairwise's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.full-n-flag-powerset-pict-pairwise"]

[[record]]
id = "testing.trigger.full-suite-flake-attributed-hardened-acquire-scratch-lock"
title = "Watch escalation for testing.full-suite-flake-attributed-hardened-acquire-scratch-lock."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Watch escalation for testing.full-suite-flake-attributed-hardened-acquire-scratch-lock.
"""
transition_kind = "watch_escalation"
failure_signature_md = """`acquire_scratch_lock_serializes` still sees `WouldBlock` after the bounded five-second retry deadline, or the raw syscall-error arm fires.
"""
capture_procedure_md = """Capture full test output, exact arm, elapsed retries, concurrent child-spawn load, errno if any, and isolated reruns.
"""
response_md = """Retain the retry, diagnose descriptor inheritance and lock release, and rerun the complete tier.
"""
escalation_action_md = """Escalate a deadline-surviving recurrence to a lock-semantics defect.
"""
retirement_semantics_md = """Retire only when a constructional test proves release independent of fork-to-exec inheritance.
"""
evaluation = "unknown"

[[record]]
id = "testing.trigger.gate-cache-all-hit-cost"
title = "Lifecycle review for testing.gate-cache.all-hit-cost."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.gate-cache.all-hit-cost.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.gate-cache.all-hit-cost"
dimension = "Residual all-hit cost"
observable = "The measured all-hit feature_corpus_compiles generation plus lockfile work becomes a pickup constraint for a maintainer running the tier."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.gate-cache-all-hit-cost"]
action_on_fire_md = """Set testing.gate-cache.all-hit-cost to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.gate-cache.all-hit-cost's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.gate-cache-all-hit-cost"]

[[record]]
id = "testing.trigger.gate-cache-closure-audit-structurally-blind-gate-authored"
title = "Lifecycle review for testing.gate-cache-closure-audit-structurally-blind-gate-authored."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.gate-cache-closure-audit-structurally-blind-gate-authored.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.gate-cache-closure-audit-structurally-blind-gate-authored"
dimension = "The gate-cache closure audit is structurally blind to GATE-AUTHORED inputs parked in scratch: its allowed-read table admits everything under `$TMPDIR` on the justification \"the generated tree is hashed\", which is true only while every file a cached cell reads under scratch actually lives inside a hashed root."
observable = "- **The gate-cache closure audit is structurally blind to GATE-AUTHORED inputs parked in scratch: its allowed-read table admits everything under `$TMPDIR` on the justification \"the generated tree is hashed\", which is true only while every file a cached cell reads under scratch actually lives inside a hashed root.** A gate that writes its own input files (hand consumer crates, appended modules) BESIDE the hashed tree violates the justification without violating the audit: the reads classify as `tmp/scratch, allowed`, the key never covers the bytes, and an edit to that input serves the stale PASS forever. Near-miss instance (D3, caught by the implementing agent before landing — no shipped gate ever had the bug): `no_std_check.ts`'s first draft wrote the host std-arm consumer crate outside the profile's hashed output root; the audit, had it traced that gate, would have passed it. What shipped instead is the discipline applied (both gate-written crates inside the hashed root, relative path deps so hashed bytes stay run-independent) plus the rule stated in `tests/README.md` § \"The gate cache\" — review-owned, like the design rules. Machinery on a trigger, not now (one cache-participating gate writes gate-authored inputs today, and it complies): extend the audit's model from \"scratch is allowed wholesale\" to \"a scratch READ must fall under some root the traced gate hashed\" — a per-gate hashed-root manifest the audit consumes, turning the discipline into a traced assertion. *Reopening signal:* a second cache-participating gate that writes gate-authored input files into its scratch root — measurable by that gate's author/reviewer at landing (the red-first mutation check the cache section already requires makes the question \"is this file inside the hashed root?\" unavoidable to ask), on the dimension the cost grows (the count of such gates, each a fresh chance to repeat the draft bug with no audit backstop)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.gate-cache-closure-audit-structurally-blind-gate-authored"]
action_on_fire_md = """Set testing.gate-cache-closure-audit-structurally-blind-gate-authored to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.gate-cache-closure-audit-structurally-blind-gate-authored's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.gate-cache-closure-audit-structurally-blind-gate-authored"]

[[record]]
id = "testing.trigger.gate-cache-closure-audit-traced-set-extension"
title = "Lifecycle review for testing.gate-cache.closure-audit-traced-set-extension."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.gate-cache.closure-audit-traced-set-extension.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.gate-cache.closure-audit-traced-set-extension"
dimension = "Closure-audit traced-set extension"
observable = "A registered cached gate adds an external-input read pattern not represented by multifile_matrix_compiles or the currently selected CLOSURE_AUDIT_GATE."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.gate-cache-closure-audit-traced-set-extension"]
action_on_fire_md = """Set testing.gate-cache.closure-audit-traced-set-extension to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.gate-cache.closure-audit-traced-set-extension's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.gate-cache-closure-audit-traced-set-extension"]

[[record]]
id = "testing.trigger.gate-cache-coverage-extensions"
title = "Lifecycle review for testing.gate-cache.coverage-extensions."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.gate-cache.coverage-extensions.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.gate-cache.coverage-extensions"
dimension = "Coverage extensions"
observable = "A run_test fixture suite is redesigned so its export directory is no longer reused warm-incrementally, or a reviewed failure taxonomy can distinguish a deterministic classified-red cell from transient environment failures."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.gate-cache-coverage-extensions"]
action_on_fire_md = """Set testing.gate-cache.coverage-extensions to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.gate-cache.coverage-extensions's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.gate-cache-coverage-extensions"]

[[record]]
id = "testing.trigger.gate-exists-registry-nowhere-tests-readme-md-hand"
title = "Lifecycle review for testing.gate-exists-registry-nowhere-tests-readme-md-hand."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.gate-exists-registry-nowhere-tests-readme-md-hand.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.gate-exists-registry-nowhere-tests-readme-md-hand"
dimension = "A gate exists in the registry but nowhere in `tests/README.md`, and a hand-written count of gates disagrees with the registry — no mechanism ties the two."
observable = "- **A gate exists in the registry but nowhere in `tests/README.md`, and a hand-written count of gates disagrees with the registry — no mechanism ties the two.** Two instances, so the trigger has fired on the count half and is one short on the catalogue half. *Instance 1 (count):* the concurrency section read \"the thirteen `#[ignore]`d manual-only heavy gates\" while the runner printed `parallel batch: 15 gate(s) in group 'manual_heavy'` — and the SAME file two paragraphs earlier carries the correct 15 inside a `<!-- gen:sh:tests-ignored-gates -->` generated block, so the doc disagreed with itself across a generated/hand boundary. Fixed by deleting the derived count rather than correcting it, the remedy this repo already chose for the identical class in prose refusal counts. *Instance 2 (catalogue):* `pin_cold_fetch` shipped as a `full`-tier gate and was described in `check.ts` and `static/manifest_changes/README.md` but never in `tests/README.md`, where its siblings are catalogued; added by hand on discovery. **Owner meanwhile:** review, plus the existing `project_status_headers.ts` generated roll-call, which covers only the `#[ignore]`d RUST gates — the script-shaped gates (`verify.ts`, `corpus_detect.ts`, the fuzz compile-rot check, `pin_cold_fetch`, the two gate-cache gates) are prose and unguarded, which is exactly where the miss landed. **The layer, when built:** a fast-tier lint asserting (a) every registry gate id appears in `tests/README.md`, and (b) no prose integer adjacent to a gate-group name disagrees with the registry's own membership — or, simpler and preferred on the (b) half, that no such integer is written at all. Cheap, no cargo. **Trigger:** a third instance, OR the next gate added to the registry by anyone who is not also editing `tests/README.md` in the same commit."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.gate-exists-registry-nowhere-tests-readme-md-hand"]
action_on_fire_md = """Set testing.gate-exists-registry-nowhere-tests-readme-md-hand to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.gate-exists-registry-nowhere-tests-readme-md-hand's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.gate-exists-registry-nowhere-tests-readme-md-hand"]

[[record]]
id = "testing.trigger.gate-script-health-cache-staleness-key-fresh-worktree"
title = "Lifecycle review for testing.gate-script-health-cache-staleness-key-fresh-worktree."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.gate-script-health-cache-staleness-key-fresh-worktree.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.gate-script-health-cache-staleness-key-fresh-worktree"
dimension = "Gate-script health is in no cache or staleness key — a fresh worktree/cold run is the only detector, and warm runs launder pre-split rot."
observable = "- **Gate-script health is in no cache or staleness key — a fresh worktree/cold run is the only detector, and warm runs launder pre-split rot.** Proven instance: `fuzz/generate.sh` predated the thin-root split and still read generated serialization from the pre-split `src/serialization.rs` and appended the custom-serialization helpers into the seed-once crate root `lib.rs`; a COLD regeneration (fresh worktree, or `--refresh-fuzz`) therefore died at the probe-list step while warm machines never re-ran the script (`fuzz_compile_rot` re-runs `generate.sh` only when `fuzz/generated` is absent — see `tests/README.md` § the fuzz gate), so pre-split state kept the gate green. Fixed by pointing the probe lists at `src/generated/serialization.rs`, appending the helpers into the clobbered `generated/mod.rs` (with `rm -f` of the stale root `lib.rs` and `--no-preserve-comments`, mirroring `integration_tests::preserve_encodings`' `run_test`), and adding a pointed existence check inside `gen_probe_list` so the NEXT layout move fails with a diagnosis rather than an opaque grep failure under `set -e`. Working rule meanwhile: after any generated-crate LAYOUT change, cold-run the regeneration scripts that consume generated layout (`rm -rf fuzz/generated` or `--refresh-fuzz` before the run), since no gate re-runs them on a warm tree. Mechanical layer on a SECOND regeneration-script rot instance: a periodic cold-regen leg (or a script-consumed-path staleness key that forces `generate.sh` to re-run when the generated layout changes) — the only layer that catches script rot without a human remembering to cold-run."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.gate-script-health-cache-staleness-key-fresh-worktree"]
action_on_fire_md = """Set testing.gate-script-health-cache-staleness-key-fresh-worktree to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.gate-script-health-cache-staleness-key-fresh-worktree's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.gate-script-health-cache-staleness-key-fresh-worktree"]

[[record]]
id = "testing.trigger.generated-local-collision-general-mangling-remedy"
title = "Lifecycle review for testing.generated-local-collision.general-mangling-remedy."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.generated-local-collision.general-mangling-remedy.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.generated-local-collision.general-mangling-remedy"
dimension = "Residual: mangling is still the general fix."
observable = "A machine-generated or upstream-vendored consumer CDDL is refused for a generated-local name collision and the consumer cannot add the offered ; @name comment."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.generated-local-collision-general-mangling-remedy"]
action_on_fire_md = """Set testing.generated-local-collision.general-mangling-remedy to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.generated-local-collision.general-mangling-remedy's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.generated-local-collision-general-mangling-remedy"]

[[record]]
id = "testing.trigger.generated-local-collision-scope-wide-probe"
title = "Lifecycle review for testing.generated-local-collision.scope-wide-probe."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.generated-local-collision.scope-wide-probe.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.generated-local-collision.scope-wide-probe"
dimension = "Residual: the scope is only as wide as the probe."
observable = "- **Residual: the scope is only as wide as the probe.** Membership and per-name `ReservedScope` were measured over five shapes (array-rep / map-rep / tagged record / embedded plain group / group-choice arm) × three profiles × two field types, plus a `--wasm=true` pass. NOT probed: a field whose type is a named rule or newtype, `.cbor`-payload and bounded-type member positions, and the `--json-schema-export` / `--component` faces. **Reopening signal:** a generated crate fails `cargo check` with a shadowed-binding error (E0599/E0308/E0124) on a field whose name is in `GENERATED_LOCAL_PROBED_SAFE`, or on a reserved name used in a shape outside its declared scope — the compile error names the binding, so the report arrives pre-diagnosed and says exactly which row to widen."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.generated-local-collision-scope-wide-probe"]
action_on_fire_md = """Set testing.generated-local-collision.scope-wide-probe to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.generated-local-collision.scope-wide-probe's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.generated-local-collision-scope-wide-probe"]

[[record]]
id = "testing.trigger.grammar-fuzzer-arbitrary-supported-ast"
title = "Lifecycle review for testing.grammar-fuzzer.arbitrary-supported-ast."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.grammar-fuzzer.arbitrary-supported-ast.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.grammar-fuzzer.arbitrary-supported-ast"
dimension = "`arbitrary`-derived \"supported-CDDL\" AST generation"
observable = "A recombination sweep over an extended member-kind/template table completes without minting a new finding."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.grammar-fuzzer-arbitrary-supported-ast"]
action_on_fire_md = """Set testing.grammar-fuzzer.arbitrary-supported-ast to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.grammar-fuzzer.arbitrary-supported-ast's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.grammar-fuzzer-arbitrary-supported-ast"]

[[record]]
id = "testing.trigger.grammar-fuzzer-batch-masking-detector"
title = "Lifecycle review for testing.grammar-fuzzer.batch-masking-detector."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.grammar-fuzzer.batch-masking-detector.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.grammar-fuzzer.batch-masking-detector"
dimension = "Batch-masking detector for the layer-2 sweeps."
observable = "- **Batch-masking detector for the layer-2 sweeps.** The ~40-rule batching means a green batch is not a per-composition guarantee for failure classes whose symptom is a missing CRATE-GLOBAL definition: `Int` is a crate-global extern emitted iff any rule registers a reference, so a batch-mate that registers it compiles the whole batch green. Proven instance: `bytes .cbor { * tstr => int }` fails standalone under default yet rode green default batches until the wasm leg's different batch boundaries isolated it — caught by review, not by any gate (the filing rule + caveat now live on `LAYER2_RULES_PER_BATCH`). The mechanical layer, worth building on the SECOND masked instance: run each layer-2 sweep under a second DETERMINISTIC batch permutation (a different fixed grouping — determinism preserved, most pair-masks broken, wall-clock ×2 on full-tier gates), or an occasional singleton mode (batch size 1 — the exhaustive oracle, but hours of wall-clock). Both costs are FIRST-run prices now: layer-2 batches are gate-cached per generated tree, so a permutation's or singleton's re-runs skip every batch whose composition didn't change — the detector got materially cheaper to adopt. Either also owns the misfiled-ledger-contradiction class (an entry claiming profile-specificity while masked in its home profile — how this instance actually surfaced). (The observed-baseline comments beside these gates' floors have their own rot class — see the standing residual below.)"
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.grammar-fuzzer-batch-masking-detector"]
action_on_fire_md = """Set testing.grammar-fuzzer.batch-masking-detector to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.grammar-fuzzer.batch-masking-detector's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.grammar-fuzzer-batch-masking-detector"]

[[record]]
id = "testing.trigger.grammar-fuzzer-real-world-corpus-differential"
title = "Lifecycle review for testing.grammar-fuzzer.real-world-corpus-differential."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.grammar-fuzzer.real-world-corpus-differential.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.grammar-fuzzer.real-world-corpus-differential"
dimension = "Real-world corpus differential"
observable = "A committed real-world CDDL specimen contains a supported construct interaction absent from the recombination corpus and exposes a generator defect or a missing admitted classification."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.grammar-fuzzer-real-world-corpus-differential"]
action_on_fire_md = """Set testing.grammar-fuzzer.real-world-corpus-differential to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.grammar-fuzzer.real-world-corpus-differential's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.grammar-fuzzer-real-world-corpus-differential"]

[[record]]
id = "testing.trigger.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
title = "Lifecycle review for testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
dimension = "A hand-enumerated ban-pattern list whose AUTHORITY lives in gitignored plan files lags the id scheme it bans — no in-repo lockstep is possible (the tracked side cannot read the authority in CI), so the list drifts one delivery-phase behind by construction."
observable = "- **A hand-enumerated ban-pattern list whose AUTHORITY lives in gitignored plan files lags the id scheme it bans — no in-repo lockstep is possible (the tracked side cannot read the authority in CI), so the list drifts one delivery-phase behind by construction.** Proven instance (read-caught in orchestrator review; the tier stayed green): `lint_doc_citations`'s ephemeral-reference patterns were spelled for one phase's ids (`PROBE-B<n>`, `ruling R<n>`) — the next phase's spellings (`PROBE-C<n>`, the `C-R<n>` ruling form) passed the gate, and the tree stayed clean only because an implementing agent self-policed its own citations out in a follow-up commit. Fixed by construction where possible: the patterns are now phase-GENERIC (`PROBE-[A-Z][0-9]`, `[A-Z]-R[0-9]`, `[a-z][0-9]*-spec`), so letter-roll drift is dead. Residual class: a genuinely NEW id FORM introduced by a future plan document (a spelling no current regex family anticipates). Working rule meanwhile: authoring a new plan/spec document that mints an id scheme adds that scheme's spelling to `EPHEMERAL_PATTERNS` (with canary) in the same session, and delegation prompts keep repeating the self-contained-comments rule. Mechanical layer on the SECOND uncaught-form instance: a local-only canary harvest — when the gitignored plan directory EXISTS (developer checkouts; skipped cleanly in CI), scan its `*-spec.md` files for id-like tokens (the probe-index/ruling tables are structured enough to extract) and fail if any harvested id is matched by NO ephemeral pattern — lockstep with the authority exactly where the authority is present."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.hand-enumerated-ban-pattern-list-authority-lives-gitignored"]
action_on_fire_md = """Set testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.hand-enumerated-ban-pattern-list-authority-lives-gitignored"]

[[record]]
id = "testing.trigger.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
title = "Lifecycle review for testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
dimension = "A hand-maintained source→dependency mapping in a manifest changeset can silently under-assert: the tool exports source referencing a crate that no changeset op guarantees."
observable = "- **A hand-maintained source→dependency mapping in a manifest changeset can silently under-assert: the tool exports source referencing a crate that no changeset op guarantees.** Proven instance (consumer-reported, not caught by any gate): the pre-crate-shaped `--export-static-dir` wrote the composed runtime source into a consumer's shared crate while owning no manifest at all, so a cbor_event major bump updated every generated crate's manifest EXCEPT the shared runtime's, which kept the old pin — caught only because the 2.x→3.x API break fails compilation loudly; a source-compatible-looking skew would have shipped silently. Fixed structurally by the crate-shaped `--export-static-crate` (the export merges the target's `Cargo.toml`; regression-pinned by `export_static_crate_writes_composed_runtime_and_manifest`'s hand-manifest leg), and the warm-up dep-universe gate sweeps `ops_for_static_runtime` alongside the other three op sets. The RESIDUAL: the mapping from composed runtime files to dep ops (`cbor_event`/`hex` always; `hashlink`/`serde`/`schemars` per flag) is hand-derived from grepping the static files, and nothing re-checks it — a `static/*.rs` runtime file gaining a reference to a new external crate (or to an existing one under a flag combination whose op condition doesn't match) would under-assert again, and only the consumer's build would notice. Working rule meanwhile: a change touching the static runtime files or `composed_runtime_static_files` re-greps the composed file set for foreign-crate path roots and reconciles `ops_for_static_runtime` in the same commit. Mechanical layer on a SECOND instance: a bidirectional drift gate over the maximal-flavor composed file set — every known dep name appearing as a path root in the composed text must be asserted by a static-runtime op, and every asserted dep must appear in the text — a natural sibling of `warmup_manifest_covers_registry_dep_universe`, accepting the source-scan heuristics then, not before."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.hand-maintained-source-dependency-mapping-manifest-changeset-silently"]
action_on_fire_md = """Set testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.hand-maintained-source-dependency-mapping-manifest-changeset-silently"]

[[record]]
id = "testing.trigger.json2ts-artifact-doc-comments-naming-undefined"
title = "Lifecycle review for testing.json2ts-artifact-doc-comments-naming-undefined."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.json2ts-artifact-doc-comments-naming-undefined.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.json2ts-artifact-doc-comments-naming-undefined"
dimension = "json2ts artifact doc comments naming `undefined`."
observable = "- **json2ts artifact doc comments naming `undefined`.** The emitted `.d.ts` carries comments like \"This interface was referenced by `undefined`'s JSON-Schema definition via the `patternProperty` …\". The name in that comment is json2ts's own standalone name for the schema that OWNS the catch-all, and it is `undefined` exactly when that owner is an anonymous INLINE object — a nested object property carrying a rest region — because such a schema has no `title`, no `$id` and no `$defs` key for json2ts to name it from. Top-level definitions already read correctly (`OpenJSON`), and the declaration-name work did not change this: the fix is not managing titles, which those inline schemas do not have. What would fix it is giving each such inline object a name, and that is not cosmetic — json2ts hoists a named object schema into a top-level declaration of its own, so the published surface gains a declaration per nested rest region. Reopening signal: a consumer's emitted `json-types.d.ts` carries more of these `undefined` comments than named ones — i.e. nested rest regions dominate their surface, which is also the condition under which the hoisted-declaration cost is worth paying."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.json2ts-artifact-doc-comments-naming-undefined"]
action_on_fire_md = """Set testing.json2ts-artifact-doc-comments-naming-undefined to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.json2ts-artifact-doc-comments-naming-undefined's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.json2ts-artifact-doc-comments-naming-undefined"]

[[record]]
id = "testing.trigger.lint-doc-citations-accepts-docs-heading-durable-citation"
title = "Lifecycle review for testing.lint-doc-citations-accepts-docs-heading-durable-citation."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.lint-doc-citations-accepts-docs-heading-durable-citation.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.lint-doc-citations-accepts-docs-heading-durable-citation"
dimension = "`lint_doc_citations` accepts \"a docs section by heading\" as a durable citation, but a section citation only proves the SECTION still exists — not that the claim cited from it does."
observable = "- **`lint_doc_citations` accepts \"a docs section by heading\" as a durable citation, but a section citation only proves the SECTION still exists — not that the claim cited from it does.** One instance, self-inflicted and caught in the same session that caused it. `current_capacities.mdx` cited a findings SECTION for the claim that `t = true / null / tstr` fails under `--preserve-encodings`; the delivery that fixed that defect deleted the ENTRY inside the section while the section itself survived, so the citation kept resolving, the lint stayed green, and a user-facing doc asserted a defect that no longer existed. The entry-title form the lint prefers would have dangled loudly on the same edit. Note this is NOT the positional-citation class the lint already bans (`item <N>`, which silently retargets): this one is an ACCEPTED durable form whose granularity is coarser than the fact it carries. **Owner meanwhile:** review, and the working rule that a citation should name the narrowest stable referent that dies with the claim — an entry title, a test/gate name, a pin — never the section enclosing it. **The layer, when built:** extend the lint so a citation into a doc that has entry-level headings must name one, rather than the enclosing section; a section citation stays legal only where the section IS the referent (a whole-mechanism reference). **Trigger:** a second instance, or any delivery that prunes entries from a section other docs cite into."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.lint-doc-citations-accepts-docs-heading-durable-citation"]
action_on_fire_md = """Set testing.lint-doc-citations-accepts-docs-heading-durable-citation to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.lint-doc-citations-accepts-docs-heading-durable-citation's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.lint-doc-citations-accepts-docs-heading-durable-citation"]

[[record]]
id = "testing.trigger.logic-tool-emits-source-text-outside-layer-reasons"
title = "Lifecycle review for testing.logic-tool-emits-source-text-outside-layer-reasons."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.logic-tool-emits-source-text-outside-layer-reasons.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.logic-tool-emits-source-text-outside-layer-reasons"
dimension = "Logic the tool EMITS as source text is outside every layer that reasons about the tool's own code, so its only oracle is a hand vector chosen to DISTINGUISH a wrong implementation — and a vector that merely EXERCISES the logic certifies nothing."
observable = "- **Logic the tool EMITS as source text is outside every layer that reasons about the tool's own code, so its only oracle is a hand vector chosen to DISTINGUISH a wrong implementation — and a vector that merely EXERCISES the logic certifies nothing.** Proven instance (orchestrator review, not any gate): the emitted schema-document closure check carries a `decode_schema_ref_name` whose comment justifies unescaping `~1` before `~0` — the order matters only for a name holding a LITERAL `~1`, which encodes to `~01` — while its green-path vector was a schema name (`Odd<K>/~name`) that decodes correctly under BOTH orders. So the comment's whole justification was unwitnessed: the three escape classes were covered, the ordering they were cited to protect was not. Confirmed by simulating `schemars`' `encode_ref_name` against both orders, then closed by making the name carry a literal `~1` (`Odd<K>/~1name` → `Odd%3CK%3E~1~01name`, which the wrong order decodes to `Odd<K>//name`), which costs no new nested-cargo cell. Why no standing layer sees the class: the permanently-declined full `cargo-mutants` sweep (§ Declined) would not cover it anyway — that sweep is scoped to the tool's own functions, while logic living inside an emitted string constant is not a function it can mutate — so mutation scoring structurally cannot measure emitted-runtime behaviour, and the emitted surface is growing (the three name-injectivity panics, the closure walk, the decode). Working rule meanwhile: a hand vector for emitted logic states which WRONG implementation it distinguishes, and a comment that justifies an ordering or precedence choice names the vector that would fail without it. Mechanical layer on a SECOND instance: mutation testing for the emitted runtime — perturb each emitted-source constant textually (swap adjacent `.replace` calls, invert a comparison, drop a branch), regenerate one fixture per mutant, and require some gate to go red; expensive enough (a nested cargo run per mutant) that it earns its cost only once the class has recurred."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.logic-tool-emits-source-text-outside-layer-reasons"]
action_on_fire_md = """Set testing.logic-tool-emits-source-text-outside-layer-reasons to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.logic-tool-emits-source-text-outside-layer-reasons's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.logic-tool-emits-source-text-outside-layer-reasons"]

[[record]]
id = "testing.trigger.machine-readable-ownership-co-owned-features-std-list"
title = "Lifecycle review for testing.machine-readable-ownership-co-owned-features-std-list."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.machine-readable-ownership-co-owned-features-std-list.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.machine-readable-ownership-co-owned-features-std-list"
dimension = "A machine-readable ownership record for the co-owned `features.std` list."
observable = "- **A machine-readable ownership record for the co-owned `features.std` list.** The human half of the co-ownership contract is asserted in-band — the `# cddl-codegen:` comment block above the key (`STD_OWNERSHIP_COMMENT`, `cargo_manifest.rs`), re-written every run so it cannot rot — but a TOOL wanting to partition the list's entries by provenance (tool-computed vs consumer-added) has only line-prefix parsing to go on. The structural carrier already exists: a list under the manifest's `[package.metadata.cddl-codegen]` table (the `generated-with` stamp's home) would state the tool's current entry set with no new public surface and stay freely revocable. Not built because it has zero consumers today, and duplicating the list without one buys only bytes. Reopening signal (consumer-side, the party who'd measure it): a consumer-side tool or lint that needs the tool/hand partition of `std` entries and says so — a feature request naming the tooling it would unblock."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.machine-readable-ownership-co-owned-features-std-list"]
action_on_fire_md = """Set testing.machine-readable-ownership-co-owned-features-std-list to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.machine-readable-ownership-co-owned-features-std-list's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.machine-readable-ownership-co-owned-features-std-list"]

[[record]]
id = "testing.trigger.make-fixture-comment-s-claimed-wire-bytes-executable"
title = "Lifecycle review for testing.make-fixture-comment-s-claimed-wire-bytes-executable."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.make-fixture-comment-s-claimed-wire-bytes-executable.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.make-fixture-comment-s-claimed-wire-bytes-executable"
dimension = "Make a fixture comment's claimed WIRE BYTES executable, or stop writing them."
observable = "- **Make a fixture comment's claimed WIRE BYTES executable, or stop writing them.** A robustness fixture's header comment may state the bytes its rule produces (`tests/robustness/` `cbor_payload_same_chain_alias.cddl` claimed `43 41 05` for the value 5; the crate writes `42 41 05`, off by one byte-string head length). Nothing could have caught it: the robustness catalog asserts an OUTCOME per fixture (`ok` / `error (graceful)`) and never compiles or runs the crate, so a byte claim in that tree is prose no gate reads — and prose that looks like evidence is worse than none, because the next reader derives from it instead of from the RFC. The error stood for as long as the file did and was found only by executing the claim by hand during an unrelated delivery. Two shapes would close it: teach the catalog an optional per-fixture `expect_bytes` (constructor args + hex), which turns the claim into the thing the gate reads; or forbid byte claims in `tests/robustness/*.cddl` outright and require them to cite the executed test that owns them (`tests/core/tests.rs`'s hand-derived oracles), which is what the corrected comment now does. The second is nearly free and catches the class by construction; the first is worth more only if robustness fixtures start carrying wire facts no other tree covers. **Reopening signal:** a second robustness fixture comment is found stating bytes that differ from what its rule emits — i.e. the citation convention was written down and did not hold."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.make-fixture-comment-s-claimed-wire-bytes-executable"]
action_on_fire_md = """Set testing.make-fixture-comment-s-claimed-wire-bytes-executable to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.make-fixture-comment-s-claimed-wire-bytes-executable's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.make-fixture-comment-s-claimed-wire-bytes-executable"]

[[record]]
id = "testing.trigger.making-type-choice-path-reject-duplicated-explicit-name"
title = "Lifecycle review for testing.making-type-choice-path-reject-duplicated-explicit-name."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.making-type-choice-path-reject-duplicated-explicit-name.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.making-type-choice-path-reject-duplicated-explicit-name"
dimension = "Making the TYPE-CHOICE path reject a duplicated explicit `@name` the way the group-choice arm path does."
observable = "A consumer reports that a type-choice variant they explicitly named with @name was emitted under a different suffixed name."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.making-type-choice-path-reject-duplicated-explicit-name"]
action_on_fire_md = """Create only the specifically demanded work without widening the settled boundary.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.making-type-choice-path-reject-duplicated-explicit-name's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.making-type-choice-path-reject-duplicated-explicit-name"]

[[record]]
id = "testing.trigger.matrix-s-note-names-panic-site-aborts-nothing"
title = "Lifecycle review for testing.matrix-s-note-names-panic-site-aborts-nothing."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.matrix-s-note-names-panic-site-aborts-nothing.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.matrix-s-note-names-panic-site-aborts-nothing"
dimension = "A matrix cell's `note` names the panic site it aborts at, and nothing re-derives that name from a run either."
observable = "A second matrix note names a panic/abort site whose executable carrier does not exercise that site."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.matrix-s-note-names-panic-site-aborts-nothing"]
action_on_fire_md = """Set testing.matrix-s-note-names-panic-site-aborts-nothing to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.matrix-s-note-names-panic-site-aborts-nothing's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.matrix-s-note-names-panic-site-aborts-nothing"]

[[record]]
id = "testing.trigger.mechanical-layers-two-review-owned-design-rules-tests"
title = "Lifecycle review for testing.mechanical-layers-two-review-owned-design-rules-tests."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.mechanical-layers-two-review-owned-design-rules-tests.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.mechanical-layers-two-review-owned-design-rules-tests"
dimension = "Mechanical layers for the two review-owned design rules in `tests/README.md` § \"Design rules\" (invariant-softening, vacuity-floor witness) — build only if a class recurs."
observable = "- **Mechanical layers for the two review-owned design rules in `tests/README.md` § \"Design rules\" (invariant-softening, vacuity-floor witness) — build only if a class recurs.** The vacuity-floor detector is a scoped mutation sweep over the harness's emission helpers — a surviving mutant in an emission arm IS the vacuity made visible — but each such mutant needs the full-tier gate to kill it, so it stays manual-only and unbuilt until a second instance justifies the cost. The invariant-softening class has no mechanical detector (a mutation sweep cannot distinguish \"kept loud\" from \"absorbed\"); a recurrence there is the signal to design one, not evidence that review can be skipped. The vacuity-floor class also has a first on-record instance in a NEEDLE gate (a flavor the harness-scoped mutation sweep would not cover): during the collapsible_if burn-down, the retargeted `corpus_special_map_key_supported` needle initially counted its indefinite gates by the proxy pattern `if matches!(` — any unrelated future matches-gate would pad the count and mask an ungated Special peek — and review fixed it to witness the gate's own `, cbor_event::Len::Indefinite)` pattern (mutation testing cannot see this flavor either: the mask needs an unrelated emission to APPEAR, which no mutant simulates). A second proxy-witness needle instance is the signal to design a detector for that flavor too, same policy. A SECOND needle-vacuity instance is on record in a distinct STALE-LITERAL flavor: `corpus_occurrence_bounds_enforced`'s negative needle pinned the exact emitted spelling of the element-value RangeCheck cast, so the i128 widening of `RangeCheck` (which renamed every emission from `as isize` to `as i128`) would have left it matching nothing — green forever while witnessing nothing — had the widening session's exhaustive grep of the old spelling not caught it (the needle was retargeted to `found: x as i128` in the same change). Reading, not a gate: a vacuous negative needle passes by construction, and the harness-scoped mutation sweep is blind to this flavor too (no mutant renames emitted text). The mechanical layer if THIS flavor recurs: pair every negative needle over emitted text with a positive LIVENESS witness asserting the same literal still occurs where it is legitimate (e.g. a constrained-int serialization snapshot DOES contain `found: x as i128` beside the occurrence cell's does-not), so an emission rename flips the liveness half red instead of leaving the negative half vacuous. Meanwhile the working rule when renaming emitted text: grep the test tree for needles pinning the old spelling in the same change."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.mechanical-layers-two-review-owned-design-rules-tests"]
action_on_fire_md = """Set testing.mechanical-layers-two-review-owned-design-rules-tests to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.mechanical-layers-two-review-owned-design-rules-tests's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.mechanical-layers-two-review-owned-design-rules-tests"]

[[record]]
id = "testing.trigger.message-s-level-classification-review-owned-two-ways"
title = "Lifecycle review for testing.message-s-level-classification-review-owned-two-ways."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.message-s-level-classification-review-owned-two-ways.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.message-s-level-classification-review-owned-two-ways"
dimension = "A message's LEVEL classification is review-owned, and two ways of getting it wrong are invisible."
observable = "- **A message's LEVEL classification is review-owned, and two ways of getting it wrong are invisible.** The verbosity delivery classified 63 emission sites across six macros (`src/log.rs`), and nothing mechanical checks that any of them landed at the right level. Two distinct blind spots, one proven and one structural. **Proven once:** `err!` shipped with zero call sites and no tool could say so — `#[macro_export]` exempts a macro from `unused_macros`, so a level nobody emits at is indistinguishable from one that is merely rare. Caught by orchestrator review of the Phase 1 diff; fixed by converting `main.rs`'s terminal error path, which is where it belonged anyway. **Structural:** a warning-class message parked at `info` or above is silently invisible at the default level, and the only mechanical watcher covers a fraction of the surface — `cddl-matrix/no_silent_directive.ts` needs the `@`-directive notices to stay default-visible, but every other diagnostic (the `Recursive type:` notice, `export()`'s four warnings, the ~22 `--emit-tests` skip notices) has no such consumer. What keeps this a residual rather than a work item is that the one watcher fails SAFE: its FAIL condition is byte-identical-AND-unmentioned, so hiding a notice makes it fail loudly rather than pass vacuously — a suppressed acknowledgement cannot make that gate go quiet. Working rule meanwhile: a new emission site states its level and stream in the same review as its message, and the measurement to make is per-level line counts on a fixture that triggers the class (the delivery's own before/after used `tests/core/input.cddl`, which emits ZERO diagnostics — so it could not have shown a mis-streamed warning, and a second fixture with a recursive type and a `#6.258` set was needed to see the stderr half at all). The mechanical layer, when justified: a source-scanning test asserting no `info!`/`debug!`/`trace!` format string opens with `warning:`, plus a call-site-count floor per macro. Reopening signal, on the dimension the cost actually grows: a SECOND consumer depending on a specific message being default-visible (there is one today, and its fail-safe direction is what makes the count the right instrument rather than any measure of how many sites exist)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.message-s-level-classification-review-owned-two-ways"]
action_on_fire_md = """Set testing.message-s-level-classification-review-owned-two-ways to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.message-s-level-classification-review-owned-two-ways's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.message-s-level-classification-review-owned-two-ways"]

[[record]]
id = "testing.trigger.migration-handoff-s-complete-list-found-survey-negative"
title = "Watch escalation for testing.migration-handoff-s-complete-list-found-survey-negative."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Watch escalation for testing.migration-handoff-s-complete-list-found-survey-negative.
"""
transition_kind = "watch_escalation"
failure_signature_md = """A handoff presents a hand-written grep list as a complete negative survey instead of deriving categories through the migration recipe.
"""
capture_procedure_md = """Capture the handoff claim, full untruncated searches, omitted category and consumer finding, recipe version, and affected files.
"""
response_md = """Re-run all four recipe categories and correct the handoff.
"""
escalation_action_md = """Escalate the next recurrence to a mechanically enforced handoff-template/checklist owner.
"""
retirement_semantics_md = """Retire only when handoffs cannot assert completeness without naming the durable category authority.
"""
evaluation = "unknown"

[[record]]
id = "testing.trigger.msrv-declaration-os-matrix-generated-code"
title = "Lifecycle review for testing.msrv-declaration-os-matrix-generated-code."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.msrv-declaration-os-matrix-generated-code.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.msrv-declaration-os-matrix-generated-code"
dimension = "MSRV declaration / OS matrix for GENERATED code."
observable = "A consumer reports a generated-code MSRV or platform break, including dependency-driven MSRV creep."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.msrv-declaration-os-matrix-generated-code"]
action_on_fire_md = """Create only the specifically demanded work without widening the settled boundary.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.msrv-declaration-os-matrix-generated-code's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.msrv-declaration-os-matrix-generated-code"]

[[record]]
id = "testing.trigger.multifile-reference-position-coverage-two-position-keyed-escapes"
title = "Lifecycle review for testing.multifile-reference-position-coverage-two-position-keyed-escapes."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.multifile-reference-position-coverage-two-position-keyed-escapes.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.multifile-reference-position-coverage-two-position-keyed-escapes"
dimension = "Multifile reference-POSITION coverage: two position-keyed escapes down, one enumerated position still missing."
observable = "- **Multifile reference-POSITION coverage: two position-keyed escapes down, one enumerated position still missing.** Two cross-module import bugs were invisible to `tests/matrix_multifile` because its field-embedding modes (`named`/`anon`/`anonb`/`unref`) all reference the shape as a `bholder` record FIELD, while each bug lived in a different reference *position* with its own import-collection path: the group-ctor class (a GROUP choice over a foreign-scope Record expands that record's field types into `new_<variant>` ctor params, unmarked by `scope_references`; fixed via `EnumVariant::group_ctor_record_fields`, vector held by the hand fixture `tests/multifile` `relay`/`relay_host`, test `cross_module_group_choice_ctor`), and the type-alias-target class (a plain alias rule's `pub type` line names its cross-scope target, unwalked — E0412, hit in production; fixed by the `scope_references` type-alias walk). The second escape triggered this entry's mechanical layer: the position axis now exists as the `aliased` mode (`bal = <ty>`, every INCLUDED shape). A third production escape then hit the same `aliased` position from the SHAPE side — inside the matrix's extern/rawbytes EXCLUSION, not a missing row: a generic-EXTERN instance named by a non-root rule aliases a `Base<Args>` TYPE-EXPRESSION ident (`RustIdent::new_generic_with_base`), which the new walk fed to `set_ref` verbatim — a `<…>` in the scope's `use` list, rustfmt abort, consumer regen dead (feature request 07, the day after the walk landed). Fixed by decomposing in the walk (base imported at the base extern's declaring scope via the single-owner `GenericInstance::extern_base_ident`, each argument walked); pinned by the hand fixture `tests/extern-generic-scoped` (compile+round-trip proof `extern_generic_scoped`; content pin `extern_generic_scoped_alias_imports`, incl. the no-`<`-in-`use`-lines invariant). The exclusion itself stays — the matrix's compile floor cannot build extern shapes standalone — so extern-shaped placement coverage is hand-fixture-owned. Mechanical layer if the excluded region bites again (recur-first): the compile floor forces the exclusion, but a GENERATION-ONLY leg (the in-process `api::generated_strings` probe the content pin already uses) has no such constraint — an excluded-shapes × reference-modes generation sweep needs no hand defs. What remains future-facing is the `gcvariant` mode (`bholder = [<ty> // 1, uint]`) over a curated shape subset (the Record-resolving shapes `struct`/`mstruct`/`ralias` are the discriminating cells), same participation-pin idiom as `anonForm`/`EXPECTED_ANON_SHAPES` — the group-ctor class still rests on the single hand vector rather than a matrix row. Its own signal, since the two positions this entry already delivered have met the parent's escape-driven one: **the count of Record-resolving shapes reachable through a group-choice ctor that the single hand vector does not cover**, which is what the next author touching `EnumVariant::group_ctor_record_fields` reads off the shape list — one vector standing in for three discriminating cells is tolerable, and stops being so the moment a fourth shape resolves through that path."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.multifile-reference-position-coverage-two-position-keyed-escapes"]
action_on_fire_md = """Set testing.multifile-reference-position-coverage-two-position-keyed-escapes to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.multifile-reference-position-coverage-two-position-keyed-escapes's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.multifile-reference-position-coverage-two-position-keyed-escapes"]

[[record]]
id = "testing.trigger.nothing-asserts-how-many-times-rejection-message-emitted"
title = "Lifecycle review for testing.nothing-asserts-how-many-times-rejection-message-emitted."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.nothing-asserts-how-many-times-rejection-message-emitted.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.nothing-asserts-how-many-times-rejection-message-emitted"
dimension = "Nothing asserts how MANY times a rejection message is emitted, and nested composites emit theirs twice."
observable = "- **Nothing asserts how MANY times a rejection message is emitted, and nested composites emit theirs twice.** `a = [x: [* 5]]` prints its refusal twice because the parse walk visits a nested composite twice; `a = [x: { uint => tstr }]` duplicates the same way, so the class predates the guards that surfaced it. Behaviour is correct and the exit code is right — only the reading is degraded, which is why the robustness/reject catalogs (they record an outcome LABEL, never the message) cannot see it. Meanwhile the working rule is that a rejection's text is reviewed by reading it once, at authoring time. The mechanical layer is a multiplicity assertion over the catalogs' captured stderr, paired with a `contains` de-dup in `record_rejection`. Trigger, on the axis the cost grows along — the repetition FACTOR, measurable by whoever next authors a fixture: a catalog fixture whose refusal repeats a message THREE or more times (a deeper nest multiplies it again), at which point a user cannot tell one refusal from several."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.nothing-asserts-how-many-times-rejection-message-emitted"]
action_on_fire_md = """Set testing.nothing-asserts-how-many-times-rejection-message-emitted to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.nothing-asserts-how-many-times-rejection-message-emitted's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.nothing-asserts-how-many-times-rejection-message-emitted"]

[[record]]
id = "testing.trigger.nothing-asserts-two-ir-sites-cannot-mint-same"
title = "Lifecycle review for testing.nothing-asserts-two-ir-sites-cannot-mint-same."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.nothing-asserts-two-ir-sites-cannot-mint-same.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.nothing-asserts-two-ir-sites-cannot-mint-same"
dimension = "Nothing asserts that two IR sites cannot mint the SAME `RustIdent`: `register_rust_struct` ends in a bare map insert, so a second claimant silently overwrites the first."
observable = "- **Nothing asserts that two IR sites cannot mint the SAME `RustIdent`: `register_rust_struct` ends in a bare map insert, so a second claimant silently overwrites the first.** Proven instance (reported by a consumer as a panic, root-caused here): a multi-arm group-choice arm registers its record under the arm's own name while parsing, so an arm named for an existing rule took that rule's slot — then, if the arm was embeddable, `remove_rust_struct` DELETED the rule outright and generation panicked at the first lookup of it. The louder half was not the panic: for a non-embeddable arm the overwrite STOOD, so the rule vanished from the emitted crate, references to it retargeted onto the arm's record, and two arms sharing a name emitted one struct carrying the other's fields AND the other's fixed tag — a wrong-wire-shape crate that compiled clean with no diagnostic. Which of the four outcomes fired was decided purely by topological rule order, so an unrelated reference edge added elsewhere in the spec flipped a working spec to a broken one. Standing coverage now: `arm_ident_collision` (order-independent — rule idents are all scope-marked before the parse loop, and two arms reject symmetrically), the structural-equivalence door that keeps benign same-shape name reuse generating, and the `group_choice_arm_ident_collision_rejects_gracefully` / `identical_group_choice_arms_in_different_rules_share_one_struct` / `embeddable_group_choice_arm_may_share_a_rule_name` trio. That detector is arm-specific by construction: the OTHER synthesizing sites (collection wrappers, generic instances, the `prelude_*` rules) mint into the same namespace and are guarded, if at all, by their own bespoke per-kind detectors. Working rule meanwhile: a change that mints a `RustIdent` from anything other than a rule name states what it collides with and how that is detected. The mechanical layer — a debug-time uniqueness assertion inside `register_rust_struct`, overwrite = panic against an allowlist of the legitimate re-registration seams (`set_rep_if_plain_group` and the `finalize` resolution passes rewrite in place by design) — should be armed as a DETECTOR first and its allowlist sized from that arming run, since \"how many overwrites are legitimate today\" is exactly the premise nobody has measured. Reopening signal, on the dimension the cost actually grows: a THIRD minting site needing its own bespoke collision detector (two are on record — this one and the wasm wrapper-name family), or any consumer reporting a declared rule absent from, or wrong-shaped in, its generated crate."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.nothing-asserts-two-ir-sites-cannot-mint-same"]
action_on_fire_md = """Set testing.nothing-asserts-two-ir-sites-cannot-mint-same to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.nothing-asserts-two-ir-sites-cannot-mint-same's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.nothing-asserts-two-ir-sites-cannot-mint-same"]

[[record]]
id = "testing.trigger.observed-baseline-comments-beside-gate-floors-rot-silently"
title = "Lifecycle review for testing.observed-baseline-comments-beside-gate-floors-rot-silently."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.observed-baseline-comments-beside-gate-floors-rot-silently.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.observed-baseline-comments-beside-gate-floors-rot-silently"
dimension = "Observed-baseline comments beside gate floors rot silently — TWO instances, two homes, two distinct rot modes; the floors/consts stay the enforced artifact."
observable = "- **Observed-baseline comments beside gate floors rot silently — TWO instances, two homes, two distinct rot modes; the floors/consts stay the enforced artifact.** These comments are informational and review-maintained by design (replacing them with exact gate-asserted counts buys churn on every ingredient addition), so no gate can see them go stale. First instance: a layer-2 recombination gate's observed-baseline comment shipped stale. Second instance, a different rot mode (a tally with two incompatible READINGS): `corpus_decode_replay`'s floors comment carried a skip-ledger tally (\"6 rows on `JSON_SURFACE_SKIP`\") that was accurate as a pinning-time observation but reads as a live count — two later ledger changes each treated it one way or the other (an addition left it alone as history; a retirement hand-bumped it as a live tally), landing a number that matched neither reading. Both caught by review, not a gate. Working rule: floor comments state pinning-time observations in explicitly past-tense framing (\"AT THAT confirm run …\"), never present-tense tallies — the consts themselves are the live ledger. Mechanical layer, on a stale baseline comment actually misleading triage: replace the comments with exact pinned counts asserted by the gate, accepting the churn."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.observed-baseline-comments-beside-gate-floors-rot-silently"]
action_on_fire_md = """Set testing.observed-baseline-comments-beside-gate-floors-rot-silently to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.observed-baseline-comments-beside-gate-floors-rot-silently's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.observed-baseline-comments-beside-gate-floors-rot-silently"]

[[record]]
id = "testing.trigger.opt-rfc-faithful-total-cbor-json-rendering-json"
title = "Lifecycle review for testing.opt-rfc-faithful-total-cbor-json-rendering-json."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.opt-rfc-faithful-total-cbor-json-rendering-json.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.opt-rfc-faithful-total-cbor-json-rendering-json"
dimension = "Opt-in RFC 8949 §6.1-faithful total CBOR→JSON rendering (a `to_json_rfc8949`-style lossy flavor)."
observable = "- **Opt-in RFC 8949 §6.1-faithful total CBOR→JSON rendering (a `to_json_rfc8949`-style lossy flavor).** §6.1 is self-described \"non-normative advice\", and its non-injective rows are lossy by its own admission: byte strings become base64url JSON strings indistinguishable from text, tag numbers are ignored, non-finite floats and unknown simples become \"a single substitute value, such as a JSON null\", and its integer-key stringification carries an acknowledged \"danger of key collision\". The crate's `any` JSON surfaces refuse silent substitution instead — the tagged `AnyCbor` codec (serde's externally-tagged enum form, `docs/docs/output_format.mdx` § \"JSON representation\") is total but structural, and the natural-JSON surface (the open-struct-maps workstream) implements §6.1's injective subset and **errors** on the substitute-value rows — because generated JSON feeds a symmetric `from_json` and typed consumers, where a silent substitute is write-back corruption (`NaN` → `null` → a different value re-encoded; bytes → string → re-read as text), not display fuzz. A never-fails *natural* view is still legitimately useful for display/diagnostic consumers; it must simply be opt-in by name so totality-by-substitution is a visible choice. Mechanical layer when a consumer asks: the same recursive walk with §6.1's leaf policy, a never-fails property over the `AnyCbor` fuzz corpus, and vectors pinning each substitution row. Reopening signal: a real consumer needs a total natural-JSON view and neither the tagged codec nor the fallible natural `to_json` fits."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.opt-rfc-faithful-total-cbor-json-rendering-json"]
action_on_fire_md = """Set testing.opt-rfc-faithful-total-cbor-json-rendering-json to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.opt-rfc-faithful-total-cbor-json-rendering-json's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.opt-rfc-faithful-total-cbor-json-rendering-json"]

[[record]]
id = "testing.trigger.ownership-journal-making-dropped-std-forward-dep-converge"
title = "Lifecycle review for testing.ownership-journal-making-dropped-std-forward-dep-converge."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.ownership-journal-making-dropped-std-forward-dep-converge.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.ownership-journal-making-dropped-std-forward-dep-converge"
dimension = "An ownership journal making a dropped `--std-forward-dep` converge (safe retraction)."
observable = "- **An ownership journal making a dropped `--std-forward-dep` converge (safe retraction).** Dropping the flag while keeping its `--rust-dep` leaves a stale-but-consistent pair today — the dep keeps `default-features = false`, the `<pkg>/std` forward union-survives beside it — safe, documented on the flag, fixable by hand. Making the tool retract it requires journaling provenance in the manifest (which run wrote the forward AND the `default-features` field, since the tool cannot otherwise tell its own defaults-off from the user's), a qualitatively new use of the bounded existing-manifest read in which a PRIOR run's flag set changes THIS run's asserted content. Declined: a new invariant surface to buy convergence of a documented harmless staleness. Reopening signal: a consumer report in which the surviving pair is a defect for them — they dropped the flag to STOP forwarding and the persistence harms their build — rather than the documented safe leftover."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.ownership-journal-making-dropped-std-forward-dep-converge"]
action_on_fire_md = """Create only the specifically demanded work without widening the settled boundary.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.ownership-journal-making-dropped-std-forward-dep-converge's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.ownership-journal-making-dropped-std-forward-dep-converge"]

[[record]]
id = "testing.trigger.panic-site-un-shadowing-converting-shallow-panic-graceful"
title = "Lifecycle review for testing.panic-site-un-shadowing-converting-shallow-panic-graceful."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.panic-site-un-shadowing-converting-shallow-panic-graceful.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.panic-site-un-shadowing-converting-shallow-panic-graceful"
dimension = "Panic-site un-shadowing: converting a shallow panic to a graceful rejection exposes deeper panic sites for inputs that previously died early — and the outcome catalogs, which record CATEGORY only by design, cannot see a PANIC→PANIC site shift."
observable = "- **Panic-site un-shadowing: converting a shallow panic to a graceful rejection exposes deeper panic sites for inputs that previously died early — and the outcome catalogs, which record CATEGORY only by design, cannot see a PANIC→PANIC site shift.** Proven instance: converting `parse_type`'s fall-through panic made `ctl.bits` (which died there, on its `&(...)` rule) travel past the recorded rejection into the control-operator catch-all — still PANIC, so its `matrix_panic` catalog row never flipped, and the shift was visible only because the conversion's fixture-flip prediction was diffed against a pre-conversion PER-SITE baseline probe. Standing coverage: the recombination sweep's ledger keys are message+site-scoped, so a SWEPT composition moving to an unledgered deeper class fails loudly as a new finding; and the cycle-4 working rule — before converting a panic site, baseline-probe every fixture the site owns (panic site + message, not category), and re-probe after — caught this instance. The trigger for a mechanical layer (a site-fingerprint sidecar per `matrix_panic` fixture, diffed by the catalog test alongside the category) is a SECOND instance where an un-shadowed deeper site goes unnoticed past its converting commit; building it now would trade the catalogs' deliberate refactor-stability for a fingerprint that churns on every panic-message edit."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.panic-site-un-shadowing-converting-shallow-panic-graceful"]
action_on_fire_md = """Set testing.panic-site-un-shadowing-converting-shallow-panic-graceful to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.panic-site-un-shadowing-converting-shallow-panic-graceful's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.panic-site-un-shadowing-converting-shallow-panic-graceful"]

[[record]]
id = "testing.trigger.parallel-constructor-fixture-diversity-parser-over-external-input"
title = "Lifecycle review for testing.parallel-constructor-fixture-diversity-parser-over-external-input."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.parallel-constructor-fixture-diversity-parser-over-external-input.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.parallel-constructor-fixture-diversity-parser-over-external-input"
dimension = "Parallel-constructor fixture diversity: a parser over external input must span the ident-class matrix of REAL specs, not the feature spec's mental model."
observable = "- **Parallel-constructor fixture diversity: a parser over external input must span the ident-class matrix of REAL specs, not the feature spec's mental model.** Proven instance: the `--wrapper-requests` shape parser hand-built bare `Rust(ident)` element leaves instead of resolving through the pipeline's alias rule, and every fixture used plain struct elements — so the first REAL spec fed through it (conway, where alias domain naming like `policy_id = script_hash` is the pervasive idiom) panicked `is_enum`'s registered-struct invariant on 5 of the motivating workspace's 18 committed requests. Two layers closed it: the MECHANICAL one is `IntermediateTypes::resolve_alias` — the single owner of the alias-substitution rule, called by both `new_type` and the requests parser, so a leaf built outside the pipeline cannot drift from pipeline resolution again (plus `workspace_requests_alias_elements_host` pinning the named-alias / primitive-alias / extern element classes). The residual this entry records is the FIXTURE principle no gate enforces: any NEW parser that reconstructs IR values from external content (sidecars, index files, future request formats) must ship fixtures spanning every ident class the IR distinguishes — registered struct, rule alias, primitive alias, extern, generic instance — because a parallel constructor's coverage is independent of the pipeline's, and synthetic fixtures inherit the feature author's blind spots. A second instance (a new external-input parser shipping struct-only fixtures) is the trigger to make this mechanical, e.g. a checklist gate over `parse_*` functions that take external strings. An independent first-principles review subsequently ran the full ident-class × shape matrix over the requests path (confirming the fix and surfacing four hardening findings, all closed with pinned hard errors: the stub-fidelity diagnosis for directly-exposable shapes, reserved element idents, the nesting depth cap, and the element-resolution mismatch appendix). The same review cycle also caught the PREDICATE flavor of this class mid-implementation: the reserved-ident pre-check first shipped as a hand-mirrored copy of `RustIdent::new`'s two assert predicates, and review replaced it with `RustIdent::reserved_reason` — the reservation rule's single owner that `new` itself asserts through. Working rule reinforced: when a fix needs the same decision an existing function already makes, extract the decision, never mirror it."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.parallel-constructor-fixture-diversity-parser-over-external-input"]
action_on_fire_md = """Set testing.parallel-constructor-fixture-diversity-parser-over-external-input to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.parallel-constructor-fixture-diversity-parser-over-external-input's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.parallel-constructor-fixture-diversity-parser-over-external-input"]

[[record]]
id = "testing.trigger.probe-component-face-s-js-surface-axes-js"
title = "Lifecycle review for testing.probe-component-face-s-js-surface-axes-js."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.probe-component-face-s-js-surface-axes-js.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.probe-component-face-s-js-surface-axes-js"
dimension = "Probe the component face's JS surface on the axes the JS-host gate left untouched."
observable = "- **Probe the component face's JS surface on the axes the JS-host gate left untouched.** The `component_jco` gate drives one jco version (1.26.1) and one node version (22) over the default encoding posture, with no JSON doors, in node only — which is what its two reused fixtures carry. Four axes are therefore documented as unprobed in `docs/docs/component_differences.mdx`, cited by \"Not probed on this face\", each with the probe that would settle it: `--json-serde-derives`' `to-json`/`from-json` doors (generate one of the existing fixtures with the flag and drive both doors from node); the browser build of `@bytecodealliance/preview2-shim` (the same drivers under a headless browser); `--preserve-encodings`' `to-canonical-cbor-bytes` (the surface fixture at that posture, asserting byte-exactness across the JS boundary); and other jco/node versions (a second pinned lockfile, or a second node in the gate's provisioning preflight). Each is a fixture-and-lockfile change rather than new machinery, so what is deferred is coverage breadth and the cold-run cost that buys it — none of the four sits on the path the motivating consumer takes."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.probe-component-face-s-js-surface-axes-js"]
action_on_fire_md = """Set testing.probe-component-face-s-js-surface-axes-js to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.probe-component-face-s-js-surface-axes-js's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.probe-component-face-s-js-surface-axes-js"]

[[record]]
id = "testing.trigger.process-boundary-another-profile-escape"
title = "Lifecycle review for testing.process-boundary.another-profile-escape."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.process-boundary.another-profile-escape.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.process-boundary.another-profile-escape"
dimension = "*another profile, with no flip, no abort and no fix widening a reachable set — the shape always generated."
observable = "A shape pinned green by a default-profile fixture fails under another named ALL_PROFILES profile."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.process-boundary-another-profile-escape"]
action_on_fire_md = """Set testing.process-boundary.another-profile-escape to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.process-boundary.another-profile-escape's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.process-boundary-another-profile-escape"]

[[record]]
id = "testing.trigger.process-boundary-rustc-after-panic-fix"
title = "Lifecycle review for testing.process-boundary.rustc-after-panic-fix."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.process-boundary.rustc-after-panic-fix.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.process-boundary.rustc-after-panic-fix"
dimension = "*rustc, after a PANIC→`ok` flip: a panic fix lands on the FIRST site that dispatches on a shape, not the last — the sites behind it ask the same question and each answers it in its own code."
observable = "- ***rustc, after a PANIC→`ok` flip: a panic fix lands on the FIRST site that dispatches on a shape, not the last — the sites behind it ask the same question and each answers it in its own code.*** Proven instance: the `cbor_types` `_ => panic!()` over a nominal reference to a collection typedef (`RustStructType::Table`/`Array`, which emit a rust typedef and no impls). Fixing the panic arm let the shape GENERATE and produced a crate rustc rejected, because `generate_serialize`/`generate_deserialize` emitted `.serialize()`/`::deserialize()` on a `BTreeMap`, and — under `--preserve-encodings` ONLY — `encoding_fields_impl` minted no encoding sidecar for a member whose serialize read one. Three further sites, one of them profile-gated, all reachable from the single input the panic fix unblocked. The standing rule that covers this today is IN FORCE and current state in `tests/README.md` (§ \"Running everything\", the catalogs-are-generate-only paragraph): `cargo check` the emitted crate under EVERY profile when a fix turns an abort into generated code, plus a per-shape integration fixture — `tests/recursive-collection-ref/` is that fixture, run both profiles by `recursive_collection_ref` / `recursive_collection_ref_preserve`. Trigger for the mechanical layer (a promotion path from a flipped PANIC row into `tests/corpus/`, or a compile leg on robustness `ok` rows): a SECOND instance where a robustness row flips PANIC→`ok` and the emitted crate does not compile. Building it now would put a per-profile nested cargo build behind a catalog whose whole value is running in seconds on every `cargo test`."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.process-boundary-rustc-after-panic-fix"]
action_on_fire_md = """Set testing.process-boundary.rustc-after-panic-fix to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.process-boundary.rustc-after-panic-fix's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.process-boundary-rustc-after-panic-fix"]

[[record]]
id = "testing.trigger.process-boundary-rustfmt-pre-rustc"
title = "Lifecycle review for testing.process-boundary.rustfmt-pre-rustc."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.process-boundary.rustfmt-pre-rustc.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.process-boundary.rustfmt-pre-rustc"
dimension = "*rustfmt, so the code never reaches rustc at all."
observable = "- ***rustfmt, so the code never reaches rustc at all.*** Not a duplicate of the class above: there the emitted crate reached rustc and was rejected, so any compile leg would catch it; here generation itself fails at the rustfmt merge step, so no compile leg can even receive a crate. One premise correction this entry now carries, probed rather than reasoned (an earlier version asserted the opposite on the strength of a dangling doc reference): the rustfmt pass runs IN-PROCESS too — `generated_strings` is post-rustfmt — so any vehicle that generates the shape under the failing profile witnesses this class as a generation error; the residual blindness is the same two-part bound as the siblings' (a vehicle must exist under the failing profile at all, and an `ok`-flip after a fix certifies only generation). Proven instance (since fixed on the type-match dispatch sites and given exactly this entry's remedy — promoted into `tests/corpus/group_choice_fixed_special.cddl`, whose cells shell the real CLI so the rustfmt seam is witnessed for the shape): a bool or null fixed value in a map-representation group-choice arm emitted Rust that rustfmt rejects (`expected pattern, found '='`) under `--preserve-encodings`. It reproduced against the parent of the fixed-value-arm support commit, so it predated that change; making the default profile generate the uint spelling is what put a reader in front of it. The class is not empty: the brute-force-dispatch sibling (`t = true / null / tstr`) still fails at the same seam and is ledgered in `cddl-matrix/ROADMAP.md` § findings — a robustness row for it would record an error today, but an `ok`-flip after a fix would again certify only generation. Reopening signal, on the magnitude axis the deferred cost grows along: **the count of `ok`-recording robustness rows whose emitted crate fails at export** — measurable by a consumer whose generation aborts at rustfmt while our catalog records the shape as `ok`, a party that already has the problem and needs nothing from us to notice it. That count is zero today, so the signal is unmet rather than pre-satisfied."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.process-boundary-rustfmt-pre-rustc"]
action_on_fire_md = """Set testing.process-boundary.rustfmt-pre-rustc to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.process-boundary.rustfmt-pre-rustc's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.process-boundary-rustfmt-pre-rustc"]

[[record]]
id = "testing.trigger.profile-driven-compile-gate-covers-compositions-profiles-specs"
title = "Lifecycle review for testing.profile-driven-compile-gate-covers-compositions-profiles-specs."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.profile-driven-compile-gate-covers-compositions-profiles-specs.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.profile-driven-compile-gate-covers-compositions-profiles-specs"
dimension = "A profile-driven compile gate covers the compositions its PROFILES' SPECS reach, and a runtime file emitted on an IR PREDICATE × a FLAG is reached only by their conjunction — the first consumer-reported no_std break lived exactly in that hole."
observable = "- **A profile-driven compile gate covers the compositions its PROFILES' SPECS reach, and a runtime file emitted on an IR PREDICATE × a FLAG is reached only by their conjunction — the first consumer-reported no_std break lived exactly in that hole.** Every profile of the no_std drift gate is chosen for a runtime surface the snapshot corpus never emits (`tests/README.md` § \"The no_std drift gate (`no_std_check`, local tier)\"); none of their CDDL contained `any`. `any_cbor.rs` is emitted only when the finalized IR holds `any`, and the whole of `static/any_cbor_json.rs` — eight nested inline `natural_any_cbor_*` serde adapters — is appended to it only under `--json-serde-derives`. Both flags were already in the `json_schema` profile, so the miss reads as flag coverage while being composition coverage: no profile's SPEC put the file in any thumb compile, and the consumer met the E0425 first. Owned meanwhile by that profile's extended CDDL — the `any_members` rule described in that section, which puts all eight adapters in the compile — plus the working rule that a new conditionally-assembled `static/` fragment states which profile's spec reaches it. Machinery on a trigger, not now (one instance, and the enumeration it would need — every emission predicate in `generation/export.rs`'s fragment assembly, paired against every profile's generated file set — is a second model of the assembly that can drift from it): a coverage assertion that each conditionally-assembled static fragment appears in at least one profile's generated tree. *Reopening signal:* a SECOND no_std break in a `static/` fragment that no profile's compile reaches — measurable by the party who already has the problem (a consumer's red shim, or a tier runner's red cell naming a file no cell had compiled), on the dimension the cost grows (the count of conditionally-assembled fragments outside every profile's spec)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.profile-driven-compile-gate-covers-compositions-profiles-specs"]
action_on_fire_md = """Set testing.profile-driven-compile-gate-covers-compositions-profiles-specs to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.profile-driven-compile-gate-covers-compositions-profiles-specs's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.profile-driven-compile-gate-covers-compositions-profiles-specs"]

[[record]]
id = "testing.trigger.project-json-gen-documents-run-test-fixture-writes"
title = "Lifecycle review for testing.project-json-gen-documents-run-test-fixture-writes."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.project-json-gen-documents-run-test-fixture-writes.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.project-json-gen-documents-run-test-fixture-writes"
dimension = "Project the json-gen documents that no `run_test` fixture writes."
observable = "- **Project the json-gen documents that no `run_test` fixture writes.** The TS-projection oracle runs from `run_test`'s json-gen block, so it covers exactly the documents a `tests/<dir>/export` fixture produces. Six other sites run a generated json-gen crate to a successful document and project none of it: `json_schema_dep_threading`, `json_schema_export_without_serde_derives`, `custom_schema_impl_writes_a_closing_document` and `json_gen_dep_links_a_threaded_dependency` in `integration_tests`, `a_derived_thread_links_and_a_collision_blames_the_consumer` in `config_tests`, and `package_json_pipeline`'s own json-gen step (whose OUTPUT is projected — the merged `.d.ts` is type-checked — but through the shipped script line rather than this leg). Every one of them builds its crate from a one- or two-rule scratch spec (`dep_thing = [x: uint]`), so its document is a strict simplification of what the fixtures already project; that is the whole reason for the deferral, and it is what the build would have to stop being true of. The cheap build is the same helper called with the scratch crate's json-gen dir — the shared install and the memo already make a call a `node` + `tsc` run of a couple of seconds. Reopening signal: one of those sites grows a spec carrying a construct no `tests/*/input.cddl` fixture has (a hand-written `JsonSchema` impl of its own, a cross-crate threaded row whose key is not an identifier), which is when its document stops being a simplification of a projected one."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.project-json-gen-documents-run-test-fixture-writes"]
action_on_fire_md = """Set testing.project-json-gen-documents-run-test-fixture-writes to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.project-json-gen-documents-run-test-fixture-writes's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.project-json-gen-documents-run-test-fixture-writes"]

[[record]]
id = "testing.trigger.propagating-deliberately-unpublished-type-s-intent-json-ts"
title = "Lifecycle review for testing.propagating-deliberately-unpublished-type-s-intent-json-ts."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.propagating-deliberately-unpublished-type-s-intent-json-ts.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.propagating-deliberately-unpublished-type-s-intent-json-ts"
dimension = "Propagating a deliberately-unpublished type's intent to the JSON → TS scripts."
observable = "- **Propagating a deliberately-unpublished type's intent to the JSON → TS scripts.** A type whose CDDL rule carries `@no_json_schema_export` still mints a wasm class with `to_json_value(): any` (the directive removes the registration row, not the derives), so when nothing published references it either, `json-ts-types.js` fails on it and the consumer restates the class by hand in `--allow-untyped`. The tool cannot compute that set at GENERATION time, and the reason is the design constraint to build against: whether a rowless type lands in `$defs` is decided by `schemars` at json-gen *runtime*, and a hand-written `JsonSchema` impl can introduce references the IR cannot see — so any generation-time list is an approximation that goes stale in both directions, against a stale-entry check that is a hard error by design. The mechanism worth building instead reports from where BOTH facts are known: have the json-gen crate emit, at `export_schemas()` time, which deliberately-unrowed types ended up absent from the finished document, and have `json-ts-types.js` consume that instead of a hand-maintained flag value. Note the shapes it must cover and the ones it must not: a record, a type or group choice, and a `@newtype` wrapper mint a class declaring the JSON method; an `_CDDL_CODEGEN_EXTERN_TYPE_`, a collection wrapper and a c-style enum do not. Reopening signal: a consumer whose deliberately-unpublished set is large enough that restating it on the script command line is a maintenance burden."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.propagating-deliberately-unpublished-type-s-intent-json-ts"]
action_on_fire_md = """Set testing.propagating-deliberately-unpublished-type-s-intent-json-ts to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.propagating-deliberately-unpublished-type-s-intent-json-ts's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.propagating-deliberately-unpublished-type-s-intent-json-ts"]

[[record]]
id = "testing.trigger.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
title = "Lifecycle review for testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
dimension = "Reason-keyed rejection evidence for the reject catalogs — one proven near-miss recorded, no machinery yet."
observable = "- **Reason-keyed rejection evidence for the reject catalogs — one proven near-miss recorded, no machinery yet.** The robustness/matrix reject catalogs snapshot the OUTCOME label only (`error (graceful)`), so a fixture stays green when a NEW, earlier rejection absorbs the one it pins: `finalize` short-circuits on prior rejections, so any parse-time rejection added upstream of a finalize-time check silently retargets every fixture whose spec happens to also match the new guard — the label is identical, the provenance is wrong, and the originally-pinned boundary goes unexercised. Proven near-miss (caught by implementation-time reading, not by any gate): an earlier no-occurrence-arrow guard would have absorbed `tests/robustness/float_table_key{,_composite}.cddl` (their occur-less spellings matched that historical guard before the float-key finalize check ran); the fixtures were respelled `*` and their headers document the pattern. The exact-once spelling now generates a `BoundedMap`, so it no longer has that rejection interaction. The mechanical layer: extend the reject-catalog snapshots from bare labels to a stable rejection-reason fingerprint per fixture (e.g. a distinctive message substring, as the decode catalog's `expect_err` pins already do for decode errors), so a provenance swap flips the snapshot loudly. Deferred because the catalogs' guard against it is a HAND step that has held so far — the working rule the float_table_key header states: an author adding a parse-time guard checks which existing reject fixtures it can reach, and respells or reason-asserts them. Reopening signal, on the dimension where that hand step actually fails: **a reject-catalog fixture found exercising a guard other than the one its header documents** — read off by whoever next re-derives a fixture's rejection, and evidence that the check was skipped rather than merely tedious. The same class in the EXECUTED fixture tests — the hand-derived negative byte vectors in `tests/*/tests.rs` — is machinery, not a hand step, since 2026-08-09: `assert_decode_reject_reason` (a per-file helper, spelled identically in each file because each is appended standalone into its own generated crate) plus `decode_reject_reason_tests`, whose `fixture_bare_decode_rejects_hold_at_their_ratchet_baseline` enumerates `tests/*/tests*.rs` by `read_dir` and holds each file's undiscriminated site count at an exact per-file baseline — above it names the new bare site and points at the helper, below it (or a stale row) says lower the row in the same commit. Exact both directions rather than a ceiling with a note beside it, for the reason the observed-baseline entry below records. The match rule is statement-scoped, not line-scoped: these files are rustfmt-shaped, so a site wraps across lines the moment its byte vector grows, and a line-scoped count would drop it for free (the two rules read the same files as 27+20 and 30+24 respectively). Three residues, stated as plainly as the catalog fingerprint's: a message substring discriminates failures whose messages DIFFER, so two defects sharing one message stay indistinguishable to it; the ratchet counts SAME-STATEMENT sites only, so a decode routed through a local closure — body holds the `from_cbor_bytes`, the `is_err()` is on the call — is neither counted nor converted; and the helper checks message-against-substring, never substring-against-CLAIM — an author who copies the substring from the observed message rather than deriving it from the claimed boundary pins provenance to whatever it already was, so the derive-then-confirm order in the conversion guidance above is load-bearing, not stylistic (review checks it; no gate can). What is left there is volume, not design: **41 undiscriminated sites across 13 fixtures still carry baseline rows** (largest `open-struct-map-e2e` at 12), each convertible exactly as `tests/core` and `tests/preserve-encodings` were — derive the substring from the boundary the vector claims, then confirm it against the message the runtime actually produces, since it is that confirmation step, not the helper, that catches a vector rejecting for the wrong reason. Signal to convert the rest rather than let the ratchet hold them: **that count rising**, or a wrong-reason vector surfacing in any file that still has a row."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.reason-keyed-rejection-evidence-reject-catalogs-proven-near"]
action_on_fire_md = """Set testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.reason-keyed-rejection-evidence-reject-catalogs-proven-near"]

[[record]]
id = "testing.trigger.referencing-context-measured-wrapping-deep-honored-base-shape"
title = "Lifecycle review for testing.referencing-context-measured-wrapping-deep-honored-base-shape."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.referencing-context-measured-wrapping-deep-honored-base-shape.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.referencing-context-measured-wrapping-deep-honored-base-shape"
dimension = "The referencing-context axis is measured one wrapping DEEP and at one honored base shape per directive; stacked wrappings and second base shapes are not."
observable = "- **The referencing-context axis is measured one wrapping DEEP and at one honored base shape per directive; stacked wrappings and second base shapes are not.** The sweep that closed the directive-through-a-reference class — `src/tests/referencing_context_tests.rs`'s `directive_referencing_context_sweep` — crosses every `KNOWN_RULE_METADATA_TAGS` member with ten single-level wrapping contexts and found no drop at any of them (current state and design rules are in `tests/README.md`, in the third sibling block of the docs-contract sweep's description). What it does not reach is a reference TWO contexts deep — a tag head over a rule-body `.cbor` alias, a generic argument whose argument is itself a re-alias — and a directive's OTHER honored base shapes: the custom pair alone is honored on a scalar alias and on a self-carrying extern alias, and the sweep measures one per row. Both are crosses of axes that already exist rather than new mechanisms (`CONTEXTS × CONTEXTS` for the first, a second `BASES` row per directive for the second), and both are deferred on the same evidence: every fixed instance of this class was a REGISTRATION seam that all of a directive's base shapes reach and that one level of wrapping already exposes, so the product buys repetition of a seam the sweep already covers. Reopening signal for building either cross: another referencing-context drop fixed BY HAND — the same observable whose firing built the sweep, and one the sweep's own clean result puts at zero today, so the entry cannot already meet it."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.referencing-context-measured-wrapping-deep-honored-base-shape"]
action_on_fire_md = """Set testing.referencing-context-measured-wrapping-deep-honored-base-shape to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.referencing-context-measured-wrapping-deep-honored-base-shape's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.referencing-context-measured-wrapping-deep-honored-base-shape"]

[[record]]
id = "testing.trigger.registry-fetch-transients-nested-cargo-cells"
title = "Watch escalation for testing.registry-fetch-transients-nested-cargo-cells."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Watch escalation for testing.registry-fetch-transients-nested-cargo-cells.
"""
transition_kind = "watch_escalation"
failure_signature_md = """A shifting nested-cargo cell fails with `unable to update registry crates-io` or `curl [56] Proxy CONNECT aborted`, or an offline cell attempts network access after warm-up.
"""
capture_procedure_md = """Capture warm-up result, offline env, failing cell, cargo error, registry/proxy state, and immediate offline retry.
"""
response_md = """Retry the warm-up only; do not relabel a stable cell failure as a network transient.
"""
escalation_action_md = """Escalate on a post-warm-up offline network attempt or a second transient class not contained by the warm-up.
"""
retirement_semantics_md = """Retire only when nested cells are constructionally offline and the warm-up's dependency universe is drift-gated.
"""
evaluation = "unknown"

[[record]]
id = "testing.trigger.representation-changing-directive-goes-live-new-container-ship"
title = "Lifecycle review for testing.representation-changing-directive-goes-live-new-container-ship."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.representation-changing-directive-goes-live-new-container-ship.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.representation-changing-directive-goes-live-new-container-ship"
dimension = "A REPRESENTATION-CHANGING directive that goes live on a new container can ship without its extern-interface projection — the cross-crate skew class, invisible to every single-crate gate."
observable = "- **A REPRESENTATION-CHANGING directive that goes live on a new container can ship without its extern-interface projection — the cross-crate skew class, invisible to every single-crate gate.** Proven instance (orchestrator code-read, not any gate): `@duplicates preserve` on tables was live for two commits while the projection helper only travelled `reject`, and the helper's own doc-comment actively misled (\"table preserve never reaches here\" — stale from the phase-1 refusal era); a dep exporting a preserve table would have had its consumer silently rebuild a reject-default `BTreeMap`. Fixed by the shape-aware `duplicates_annotation`, which now derives the projection from the SAME `is_reject_ordered_set`/`is_preserve_pair_map` predicates that drive representation — the coupling that makes the drift structural rather than remembered — plus the `dep-preserve`/`consumer-preserve` two-crate fixture with its negative-control skew leg. Standing rule meanwhile: a delivery that makes a directive change ANYTHING a dep EMITS — representation, materialized names, or annotation semantics — must land the extern-interface projection AND its two-crate fixture in the same change (reviewers: diff `extern_interface.rs`'s annotation helpers against the new predicate). The wider vocabulary is measured, not cautious: the forward direction's near-miss (delivery 4) was `@no_alias` honored on collection rules — a MATERIALIZATION change, no representation touched — where the projection had to move in the honoring commit (`f9f69a8d`, with its two-crate vector) or every consumer would have imported a `pub type` the dep no longer emits. It did not ship, because the delivery's review mandated the projection check ad hoc — but the old vocabulary (\"changes a rule's REPRESENTATION\") would not have demanded it, which is exactly how the class survives a diligent implementer. Mechanical layer for the forward direction, to build if a second emission-changing surface ships without its projection: a marker registry in the writer-vocabulary style — enumerate the per-ident records and config predicates in `IntermediateTypes` that alter the dep's emitted rust surface, and hold each against a total verdict (consulted by `extern_interface.rs`, or classified inapplicable with its reason), the way `EXTERN_INTERFACE_WRITER_VOCABULARY` is total over the emitter's `@…` literals. The trigger for a mechanical layer — an extern-projection sweep that enumerates every representation-changing config predicate and auto-checks a dep/consumer pair per directive — is a SECOND representation-changing surface shipping without its projection. The class has a CONVERSE direction, proven by a second reading-found instance (2026-08-01, delivery 3): the projection EMITTING a directive onto a form the consumer's parse REFUSES. `@raw_bytes_flavor` was projected verbatim onto the param-less rendering of a generic extern base — a spelling that flavors nothing — and the moment the non-generic-extern refusal landed beside it, every consumer of a flavored dep would have hard-failed; no committed fixture was such a consumer, so both tiers stayed green while the break shipped in the working tree (caught in-delivery by reasoning about the seam, fixed by dropping the tag from the projection, pinned by `extern_import_flavored_generic_base_projects_without_the_tag`). What makes the converse direction structural rather than remembered is that a REFUSAL delivery has no reason to look at the projection: the writer already exists and no representation changed. The mechanical layer covering BOTH directions is DELIVERED — the writer-vocabulary registry, its per-annotation consumer acceptance vectors, and the LOCKSTEP source scan that holds the two total over the emitter's `@…` literals; current state in `tests/README.md` § \"Extern-interface export & `--extern-import` (the machine-generated stub channel)\". What remains unmechanized is the DEP-SIDE half: the registry proves this crate's parse accepts what its writer emits, and says nothing about a counterparty crate at a different version doing so. Reopening signal, measurable by whoever pays it: a consumer failing to regenerate against a dependency's committed export whose annotations this crate's own parse accepts — the version-skew shape the single-repo registry cannot see."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.representation-changing-directive-goes-live-new-container-ship"]
action_on_fire_md = """Set testing.representation-changing-directive-goes-live-new-container-ship to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.representation-changing-directive-goes-live-new-container-ship's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.representation-changing-directive-goes-live-new-container-ship"]

[[record]]
id = "testing.trigger.ruling-premise-gate-x-stays-green-must-probed"
title = "Lifecycle review for testing.ruling-premise-gate-x-stays-green-must-probed."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.ruling-premise-gate-x-stays-green-must-probed.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.ruling-premise-gate-x-stays-green-must-probed"
dimension = "A ruling whose premise is \"gate X stays green\" must be probed in the state the ruling CREATES, not the state that precedes it."
observable = "- **A ruling whose premise is \"gate X stays green\" must be probed in the state the ruling CREATES, not the state that precedes it.** This is the scope-the-claim discipline above applied to the ORDER of a probe rather than its breadth, and it is a ruling-protocol rule rather than a quirk of any one gate. Proven instance: an orchestrator approved blessing `tests/matrix_panic/snapshots/catalog.snap` ahead of a `cddl-matrix/verify.ts` re-grounding, on the premise that the snapshot records only the observed generation outcome and encodes no matrix class, so `project_robustness.ts --check` compares directory PLACEMENT and would stay green. That premise was established by running the gate with the snapshot still UNBLESSED — the one state in which the disagreement it checks for cannot exist. The gate ALSO cross-checks the committed catalog against the matrix verdict class (it is the drift class the check was built for, and its own message states the required order: re-run verify.ts, re-project, re-bless), and it is `fast`-tier, so the bless would have turned CI red for fourteen cells. It was found by RUNNING the gate in the post-bless state, not by re-reading it — which is the tell: a pre-change green says nothing about a post-change gate whose input the change is what alters. Standing consequence, now settled: **a catalog bless is the LAST step of a re-grounding, never a step that precedes it.** Authoring rule: a ruling resting on a gate verdict names the STATE the verdict was measured in, and if that state is not the one the ruling produces, the verdict is not evidence for the ruling. Mechanical layer, on a SECOND instance where the pre/post distinction is not visible from the gate's own message: have the drift check name both inputs it compares in its GREEN line, so a reader of a passing run can tell which comparisons actually ran."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.ruling-premise-gate-x-stays-green-must-probed"]
action_on_fire_md = """Set testing.ruling-premise-gate-x-stays-green-must-probed to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.ruling-premise-gate-x-stays-green-must-probed's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.ruling-premise-gate-x-stays-green-must-probed"]

[[record]]
id = "testing.trigger.run-local-values-gate-cache-key-material-proven"
title = "Lifecycle review for testing.run-local-values-gate-cache-key-material-proven."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.run-local-values-gate-cache-key-material-proven.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.run-local-values-gate-cache-key-material-proven"
dimension = "Run-local values in gate-cache key material — one proven instance, no machinery yet."
observable = "- **Run-local values in gate-cache key material — one proven instance, no machinery yet.** The TS-side cache keys hashed the literal nested-cargo argv, which embeds the run's `mkdtempSync` probe dir — every key was unique to its run, so verify.ts could never hit its cache across runs (fixed by path-normalizing the key argv; the normalized form is documented in `tests/README.md` § \"The gate cache (memoize-and-skip for nested cargo)\"). Caught by merge-review reading, not by any gate — and `verify_cache_transparency`'s ≥1-hit vacuity floor is BLIND to this class by construction: identical generated trees hit WITHIN a run (the probe dir is constant inside one run), so the hit count stays above zero even when no cross-run hit is possible. Mechanical layer if the class recurs (a second run-local value — an env var, a timestamp, a counter — leaking into key material): a key path-independence self-test (hash one fixed byte tree under two different scratch roots in two separate invocations; assert `gateCacheKey` / the Rust `gate_cache_key` produce identical keys), plus a cross-run hit floor that only counts hits whose entries PREDATE the running process (within-run hits excluded, so the floor actually measures cross-run keyability)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.run-local-values-gate-cache-key-material-proven"]
action_on_fire_md = """Set testing.run-local-values-gate-cache-key-material-proven to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.run-local-values-gate-cache-key-material-proven's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.run-local-values-gate-cache-key-material-proven"]

[[record]]
id = "testing.trigger.rustfmt-seam-error-leg-still-witness-needs-subprocess"
title = "Lifecycle review for testing.rustfmt-seam-error-leg-still-witness-needs-subprocess."
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.rustfmt-seam-error-leg-still-witness-needs-subprocess.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.rustfmt-seam-error-leg-still-witness-needs-subprocess"
dimension = "One rustfmt-seam error leg still has no witness, and it is the one that needs a subprocess-scoped test harness."
observable = "A second rustfmt error leg requires a controlled process-wide environment to drive."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.rustfmt-seam-error-leg-still-witness-needs-subprocess"]
action_on_fire_md = """Set testing.rustfmt-seam-error-leg-still-witness-needs-subprocess to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.rustfmt-seam-error-leg-still-witness-needs-subprocess's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.rustfmt-seam-error-leg-still-witness-needs-subprocess"]

[[record]]
id = "testing.trigger.schema-name-collision-loser-no-row-same-id"
title = "Lifecycle review for testing.schema-name.collision-loser-no-row-same-id."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.schema-name.collision-loser-no-row-same-id.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.schema-name.collision-loser-no-row-same-id"
dimension = "A collision whose LOSER has no row and whose `schema_id`s match"
observable = "A no-row losing type shares schema_id with the winner and silently merges in the finished document."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.schema-name-collision-loser-no-row-same-id"]
action_on_fire_md = """Set testing.schema-name.collision-loser-no-row-same-id to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.schema-name.collision-loser-no-row-same-id's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.schema-name-collision-loser-no-row-same-id"]

[[record]]
id = "testing.trigger.schema-name-cross-crate-same-id-collision"
title = "Lifecycle review for testing.schema-name.cross-crate-same-id-collision."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.schema-name.cross-crate-same-id-collision.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.schema-name.cross-crate-same-id-collision"
dimension = "A cross-crate collision whose `schema_id`s MATCH"
observable = "A real cross-crate report demonstrates two types with matching schema_id merging through one SchemaGenerator."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.schema-name-cross-crate-same-id-collision"]
action_on_fire_md = """Set testing.schema-name.cross-crate-same-id-collision to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.schema-name.cross-crate-same-id-collision's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.schema-name-cross-crate-same-id-collision"]

[[record]]
id = "testing.trigger.schema-name-two-rowless-types-collide"
title = "Lifecycle review for testing.schema-name.two-rowless-types-collide."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.schema-name.two-rowless-types-collide.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.schema-name.two-rowless-types-collide"
dimension = "A collision between two types that BOTH lack rows"
observable = "What a consumer can do TODAY, and why that further lowers the priority: give each instantiation a row with `--json-schema-root` (`--json-schema-root=my_crate::AssetBundle<u64>` and `…<i64>` — generic arguments are inside the flag's accepted charset). Registering both makes them two rows publishing one `schema_name()` with different `std::any::type_name`s, which the ledger reports; registering only the instantiation that LOST the bare name makes the kept-its-own-name check report the `<name>2` it was assigned. So the row-side guard is opt-in-able for exactly the shape it cannot see on its own. (Reasoned from `static/json_schema_gen.rs`'s `add_schema` and `parse_json_schema_root`'s charset — not run.) Reopening signal for building anything more: a public schemars accessor for the assigned-name map, or a consumer reporting a rename their `.d.ts` diff did not catch."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.schema-name-two-rowless-types-collide"]
action_on_fire_md = """Set testing.schema-name.two-rowless-types-collide to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.schema-name.two-rowless-types-collide's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.schema-name-two-rowless-types-collide"]

[[record]]
id = "testing.trigger.shared-cargo-target-dir-across-same-named-scratch"
title = "Watch escalation for testing.shared-cargo-target-dir-across-same-named-scratch."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Watch escalation for testing.shared-cargo-target-dir-across-same-named-scratch.
"""
transition_kind = "watch_escalation"
failure_signature_md = """A same-named scratch crate reports false-green because another crate's fingerprint was reused in a shared target dir.
"""
capture_procedure_md = """Capture both crate trees/package names, target dir, forced-clean comparison, verbose cargo freshness output, and compile error from an isolated target.
"""
response_md = """Give each crate a target dir or unique package name and rerun.
"""
escalation_action_md = """Escalate the second ad-hoc instance to probe tooling; escalate any registered-gate instance immediately as that gate's defect.
"""
retirement_semantics_md = """Retire when scratch probe helpers enforce content-distinct fingerprints.
"""
evaluation = "unknown"

[[record]]
id = "testing.trigger.stale-known-limitation-prose-surviving-fix-finding-ledgered"
title = "Lifecycle review for testing.stale-known-limitation-prose-surviving-fix-finding-ledgered."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.stale-known-limitation-prose-surviving-fix-finding-ledgered.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.stale-known-limitation-prose-surviving-fix-finding-ledgered"
dimension = "Stale \"known limitation\" prose surviving its fix — a finding ledgered in TWO homes where the fixing commit prunes only one."
observable = "- **Stale \"known limitation\" prose surviving its fix — a finding ledgered in TWO homes where the fixing commit prunes only one.** First instance (read-caught during the facade-pin delivery, not by any gate): the extern-only-scope undeclared-module finding was ledgered both in `cddl-matrix/ROADMAP.md` and as a hand \"Known limitation\" paragraph in `docs/docs/comment_dsl.mdx`; the fix pruned the roadmap entry and minted the pin (`integration_extern_only_scope_declared_in_root`) but the user-docs paragraph survived — through a later docs pass over the same file — until the facade compile pin (`facade_composition_compiles`) delivery pruned it. SECOND instance SET, a DIFFERENT home (read-caught during the findings-sweep delivery): the corpus findings ledger (`cddl-matrix/annotations/corpus/cddl_codegen.toml`) carried four failure-claim `[[finding]]`s / sibling `[[note]]`s whose defects had been fixed out from under them (the `bool`-in-type-choice E0282, the single-letter-`r` E0574, the inline-group data-loss \"Bug\", and the top-level fixed-value \"panics\" prose) — these render into `tests/corpus/COVERAGE.md`, a GENERATED span `lint_doc_citations` never scans, so that gate could never have caught them. THIRD instance, a distinct rot mode — a NEGATIVE-INVENTORY scan claim (caught at item pickup during the tag-set matrix-cell delivery): a `cddl-matrix/ROADMAP.md` bullet asserted \"no current row's example matches the recognized shape\" as its coverage-hole premise, and the claim was silently falsified the SAME DAY it shipped when the `dsl.duplicates.{reject,preserve}` registration (`5485b2c`) landed a matching example (`#6.258([* uint]) / [* uint] ; @duplicates reject`; harmless there — that row's verdict was minted with the collapse live, so nothing was actually stale — but the prose was wrong from that commit on). The class property: a point-in-time inventory claim DANGLES NOTHING — every artifact it names keeps resolving — so no citation lint can ever fire; and mechanically re-checking arbitrary prose inventory claims against the live matrix is the same claim-semantics boundary check `I` deliberately stops at. No mechanical layer proposed; the guard is pickup discipline, which worked here: a roadmap item's scan/inventory premises are claims to re-verify against the live tree when the item is picked up (AGENTS.md's premise-probing rule — the re-scan is one grep), and the delivery prunes the whole bullet, premise included. Authoring rule for future bullets: scope any inventory claim to its evidence (\"scanned at <sha>\") or phrase it as the check to re-run, never as a standing fact. FOURTH instance, in a home the first three did not reach — a session HANDOFF file, where the claim is about VERIFICATION STATE rather than about the tree: a `draft/` worklist's carry-forward bullet read \"no cycle-1 commit has been seen by a `full`-tier gate\", true when written and falsified as soon as the tier was run. The next cycle's orchestrator propagated the file's line into its own report in preference to the fresher fact it had been given directly, which is the rot mode worth naming: a handoff doc is READ as current state by construction, so a stale standing fact in one is not merely wrong, it OUTRANKS newer evidence in the reader's head. The authoring rule above already covers it verbatim (a tier verdict is a point-in-time inventory claim, and \"PASSED at `<sha>`\" satisfies it while \"has never run\" does not), so this is the rule firing rather than a gap — but note it applies to `draft/` handoff files, which no gate scans and which are gitignored, so review is the only reader that can enforce it. The SECOND-instance mechanical layer is now BUILT for that home: the findings-claims arm (`project_corpus.ts` check `I`, local tier) fails a failure-claim finding that names no resolvable tracking pin — current state, including its accepted looseness (a resolving citation does not validate claim SEMANTICS, which stay review-owned; any backtick token matching `src/tests/` text satisfies resolution — tighten on the first vacuous pass observed), is documented in `cddl-matrix/README.md` § \"Gotchas\". Still future: the USER-DOCS home has no arm — a parallel limitation-claims arm on `lint_doc_citations` (every known-limitation-classed paragraph outside a generated span carries a resolvable tracking citation, or folds into a projected span — the matrix north star's \"more docs follow\") remains unbuilt, since the two homes have different scanners and neither arm sees the other's prose. That half needs its own signal rather than inheriting the parent's second-instance trigger, which four recorded instances have already met: on the axis its cost grows along, it is **the count of known-limitation paragraphs in `docs/docs/*.mdx` that name no tracking artifact** — measured by the next author who greps `docs/` before shipping a fix, which the working rule below already requires them to do, so the count arrives as a by-product of work someone is doing anyway. Build the arm when that grep returns a set too large to walk by hand; until then the grep IS the arm. Working rules meanwhile: a limitation recorded in user docs names its tracking artifact (the reject/panic fixture, pin, or ledger entry) in the same paragraph; a fixing session greps `docs/` for the limitation's key phrases before shipping; and the prose that CLOSES a stale finding attributes the fix by REPRODUCING against the candidate commit's parent (a throwaway worktree + the probe is minutes), never by narrative plausibility — the findings sweep's first closure draft attributed both compile-bug closures to a plausible-but-wrong commit, falsified only by a worktree reproduction at that commit's parent before the wrong story shipped. The reproduce-at-parent rule is REVIEW-OWNED by explicit decision, not pending a mechanical layer: whether a reproduction happened leaves no machine-checkable artifact, so a detector could only check that prose CLAIMS verification — the same claim-semantics boundary check I stops at (same disposition class as the declined docs-conformance harness; the reopening signal is a false attribution SHIPPING past review, not one being caught by it). Bisect/reproduction mechanics for whoever next needs them — chiefly the `git bisect run` exit-convention POLARITY trap (exit 0 always means the OLD term, so a fix-hunting script that exits 0 on \"works\" is inverted and the search returns its own fixed endpoint), plus the specific-error-keyed verdict shape and the cheaper reproduce-at-parent/pickaxe-first alternatives — live in `draft/bisect-verdict-discipline.md`."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.stale-known-limitation-prose-surviving-fix-finding-ledgered"]
action_on_fire_md = """Set testing.stale-known-limitation-prose-surviving-fix-finding-ledgered to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.stale-known-limitation-prose-surviving-fix-finding-ledgered's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.stale-known-limitation-prose-surviving-fix-finding-ledgered"]

[[record]]
id = "testing.trigger.stale-pin-guard-cannot-distinguish-gap-closed-vector"
title = "Watch escalation for testing.stale-pin-guard-cannot-distinguish-gap-closed-vector."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Watch escalation for testing.stale-pin-guard-cannot-distinguish-gap-closed-vector.
"""
transition_kind = "watch_escalation"
failure_signature_md = """A stale-pin guard claims closure after a mint reduced the pinned row's vector-shape diversity.
"""
capture_procedure_md = """Preserve pre/post row vectors, shape classes, mint command, stale-pin output, and single-row rerun.
"""
response_md = """Restore the proven vector, remint only the changed row, and re-run the owning replay gate.
"""
escalation_action_md = """On a second vector-churn retirement or near-retirement, add the mint-time no-diversity-reduction guard.
"""
retirement_semantics_md = """Retire only when that guard covers every randomized catalog mint.
"""
evaluation = "unknown"

[[record]]
id = "testing.trigger.std-generated-crates"
title = "Lifecycle review for testing.std-generated-crates."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.std-generated-crates.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.std-generated-crates"
dimension = "no_std generated crates."
observable = "- **no_std generated crates.** cbor_event supports no_std since 3.0.0, so a CLI flag (`cli.rs` + docs) emitting no_std-compatible crates is possible; the static runtime and generated code would need their own std-usage sweep (collections, error impls). The mechanical layer when a consumer asks: a no_std cross-compile gate over one generated crate."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.std-generated-crates"]
action_on_fire_md = """Set testing.std-generated-crates to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.std-generated-crates's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.std-generated-crates"]

[[record]]
id = "testing.trigger.synthesized-name-referenced-never-minted-e0425-flavor"
title = "Lifecycle review for testing.synthesized-name-referenced-never-minted-e0425-flavor."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.synthesized-name-referenced-never-minted-e0425-flavor.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.synthesized-name-referenced-never-minted-e0425-flavor"
dimension = "Synthesized-name residual: the referenced-but-never-minted (E0425) flavor."
observable = "- **Synthesized-name residual: the referenced-but-never-minted (E0425) flavor.** The generator mints structural wasm idents (loose `{Elem}List` / `Map{K}To{V}` builders, restricted `NonEmpty*` wrappers, table `keys()` list wrappers) whose interactions with USER rule names and with EACH OTHER are a NAME-shaped axis the shape catalogs (wasm-ABI matrix, corpus, recombination) never spell. The two flavors of the \"generation exits 0 but the wasm crate doesn't compile\" class that live at generation time are owned by standing gates: the duplicate-ident (E0428) redefinition flavor by the generation-time backstop at the `generated_files` seam, and the dedup/shape-mismatch (E0277) flavor by `synthesized_name_interaction_sweep` (families × interactions → graceful rejection with the colliding ident named, or a compiling batched wasm crate; see tests/README.md § \"Synthesized-name interaction sweep + duplicate-ident backstop\"). The remaining flavor is E0425: an emitter that REFERENCES a synthesized wrapper name no mint path emits. It stays owned by the compile gates (`wasm_matrix_compiles` + the full-tier recombination wasm leg). If it recurs outside their reach, escalate to a scoped generation-time invariant — \"every synthesized wrapper name an emitter references must be in the minted set\" — the reference-side complement of the emission-side duplicate-ident backstop. (A fourth, CROSS-CRATE flavor — duplicate symbols at link under `--extern-wrapper-index`, invisible to any in-crate layer — is owned by the wrapper-participation grid's per-mode wasm32 link floors, `tests/README.md` § \"The wrapper-participation grid\".)"
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.synthesized-name-referenced-never-minted-e0425-flavor"]
action_on_fire_md = """Set testing.synthesized-name-referenced-never-minted-e0425-flavor to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.synthesized-name-referenced-never-minted-e0425-flavor's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.synthesized-name-referenced-never-minted-e0425-flavor"]

[[record]]
id = "testing.trigger.tag-set-inline-anonymous-choice-boundary"
title = "Lifecycle review for testing.tag-set.inline-anonymous-choice-boundary."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.tag-set.inline-anonymous-choice-boundary.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.tag-set.inline-anonymous-choice-boundary"
dimension = "Inline/anonymous two-arm choices are not recognized."
observable = "A consumer spec requires the inline or anonymous two-arm tagged-or-untagged set choice and cannot move it to the named-rule recognition seam."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.tag-set-inline-anonymous-choice-boundary"]
action_on_fire_md = """Set testing.tag-set.inline-anonymous-choice-boundary to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.tag-set.inline-anonymous-choice-boundary's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.tag-set-inline-anonymous-choice-boundary"]

[[record]]
id = "testing.trigger.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
title = "Lifecycle review for testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
dimension = "A tier's DISK BANDWIDTH is unmeasured by anything — the scratch floor is a capacity check, not a rate one, and no incident has ever been attributable either way."
observable = "A whole-machine stall occurs while the same run's per-batch memory-basis lines demonstrate that the memory budget remained healthy."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"]
action_on_fire_md = """Set testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"]

[[record]]
id = "testing.trigger.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
title = "Lifecycle review for testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
dimension = "Tree-wide sweeps over gitignored regeneration targets count DEAD files as product — one proven near-miss, no machinery (and none possible tool-side)."
observable = "- **Tree-wide sweeps over gitignored regeneration targets count DEAD files as product — one proven near-miss, no machinery (and none possible tool-side).** Proven instance (D1's Phase-0 inventory, caught by classification discipline, not by any gate): a long-lived checkout's `tests/*/export*/` trees held 151 stale `.rs` at `<crate>/src/*.rs` from the pre-thin-root layout — nothing compiles them (the thin root binds only `src/generated/**`), and a sweep that counted them concluded the emitted rust crate has a `std::io` surface, which it does not. The near-miss was writing that phantom surface into a delivery inventory as fact and sizing work against it. No tool diagnostic can own the class: the crate `src/` level is USER territory by the ownership contract (hand modules beside the seed-once `lib.rs` are legitimate, and the tool cannot tell a user's file from its own pre-layout-change leftovers), and the trees are gitignored, so no repo gate sees them either. Working rule (the fix that worked): classify swept files by the CURRENT write-site key set (`src/generation/export.rs`'s write sites are the authority), never by presence on disk, and state the unmatched remainder as non-product in the sweep's scope note; hygiene half — delete-and-regenerate (or `git clean`) the export trees before any tree-wide sweep on a checkout old enough to predate a layout change. Mechanical layer on a SECOND distorted-analysis instance: a sweep-preflight that regenerates the swept trees first, so the on-disk state IS the current write-site set."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.tree-wide-sweeps-over-gitignored-regeneration-targets-count"]
action_on_fire_md = """Set testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.tree-wide-sweeps-over-gitignored-regeneration-targets-count"]

[[record]]
id = "testing.trigger.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
title = "Lifecycle review for testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
dimension = "Twin in-repo implementations of one semantic decision drifting apart (emission spellings, detection walkers, cross-language scanner mirrors) — single-owner extraction is the fix pattern; only the directive-SET flavor has a firing detector so far."
observable = "- **Twin in-repo implementations of one semantic decision drifting apart (emission spellings, detection walkers, cross-language scanner mirrors) — single-owner extraction is the fix pattern; only the directive-SET flavor has a firing detector so far.** The in-repo sibling of the parallel-constructor residual's predicate lesson above (\"when a fix needs the same decision an existing function already makes, extract the decision, never mirror it\"): two implementations of one decision, both living in this repo, drift apart while every behavioral oracle stays green — semantically-equivalent drift has no oracle but reading (decode conformance sees equal accept/reject sets, the mutation sweep sees no behavioral delta, the compile gates stay green; pending-system coverage check done at recording time: the grammar-fuzzer escalations own only the ACTIVE missing-import flavor of the walker instance — a recombination shape table extended to nested container positions would compile-fail it, E0433 — and NO pending item sees the semantically-equivalent flavor). Three read-caught instances, each fixed by single-owner extraction and pinned: (1) `generate_wrapper_struct` hand-built its range-check `cond`/`failure_expr` beside the shared helpers and drifted three spellings apart (`!= N` collapse, zero-min lower-leg elision, an inline float-payload duplicate) — `bounds_check_if_block` + `range_check_err` (`non_negative`/`location` params) are now the single owner, pinned by the `bounds_spellings` corpus fixture; (2) `uses_non_empty_{vec,map}` hand-walked the `RustStructType` variants, mirroring `visit_all_rust_types`' position enumeration, and drifted two legs short (named-array `element_type`, table `domain`) — masked by the transparent alias every top-level array/table rule co-registers; now folded onto the canonical visitor, pinned by `nonempty_nested_positions`; (3) cross-language: `corpus_detect.ts`'s `DSL_TAGS` scanner is a documented hand mirror of `comment_ast.rs`'s directive grammar and drifted twice (no `@used_as_elem`; pre-flavor `@used_as_key` grammar), with its hand-picked selfCheck vectors drifting WITH it — the layers that fired (`verify.ts`'s docs-lint, `project_corpus`'s content-drift check A) trigger only via adjacent artifacts (a docs mention, a `[[cover]]` entry), not the mirror itself. A fourth, BYTE-COUPLED flavor was read-caught before it fired (D1's core/alloc rewrite, 2026-07-29): `composed_runtime_static_files`' preserve flavoring `.replace()`s a byte-exact import line that lives in `static/non_empty_map.rs`, so rewriting that line's path would have silently no-op'd the substitution — distinct from the instances above in failure mode: not silent (the mangled flavor is an E0432 in every preserve corpus-compile cell, `local` tier) but loud-LATE and far from the causing edit, and invisible to any grep of either file alone. Fixed as a synchronized edit with a move-together comment on BOTH sides; the instance-level tripwire, due on the next touch of that composition code, is a must-hit replace (assert the pattern occurred before substituting — the selfCheck-lockstep analog for byte mirrors, turning the no-op into a generation-time panic that every snapshot regeneration sees at `fast` tier). Directive-SET drift now has a firing detector: `corpus_detect.ts`'s selfCheck lockstep tripwire demands set equality between the mirror's `MIRRORED_DIRECTIVES` and the authority's `tag(\"@…\")` literals at import time, so `project_corpus` (fast tier, CI) fires the moment a directive is added or removed on either side. Remaining work, each piece behind its trigger:"
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.twin-repo-implementations-semantic-drifting-apart-emission-spellings"]
action_on_fire_md = """Set testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.twin-repo-implementations-semantic-drifting-apart-emission-spellings"]

[[record]]
id = "testing.trigger.unused-imports-generated-crates-trait-import-class-name"
title = "Lifecycle review for testing.unused-imports-generated-crates-trait-import-class-name."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.unused-imports-generated-crates-trait-import-class-name.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.unused-imports-generated-crates-trait-import-class-name"
dimension = "`unused_imports` on generated crates — residual trait-import class the name-scan model cannot reach."
observable = "A THIRD known DETECTOR blind spot is still open, recorded as a posture when the extern-breadth seeding surfaced the wasm re-export contract wart: the matrix compile probes (`cddl-matrix/verify.ts`, the `DEF_SPLICE`-seeded user-code rows included) judge cargo EXIT CODES only and run no warning scan — the wart was caught because the same shape sits in the corpus, whose `feature_corpus_compiles` scan is the warning detector. So a warning-class generator imprecision reachable ONLY through a matrix-only shape (a cell with no corpus sibling) ships unseen today. Deliberate for now: the matrix's verdict model is support-classification, not lint, and the corpus mirrors most compiled shapes. Trigger to build the layer (port `unused_generated_import_lines` over the matrix probes' captured stderr): a SECOND warning-class finding whose only compiled home is a matrix cell."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.unused-imports-generated-crates-trait-import-class-name"]
action_on_fire_md = """Set testing.unused-imports-generated-crates-trait-import-class-name to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.unused-imports-generated-crates-trait-import-class-name's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.unused-imports-generated-crates-trait-import-class-name"]

[[record]]
id = "testing.trigger.value-anchor-rot-field-addition-delivery-grows-existing"
title = "Lifecycle review for testing.value-anchor-rot-field-addition-delivery-grows-existing."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.value-anchor-rot-field-addition-delivery-grows-existing.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.value-anchor-rot-field-addition-delivery-grows-existing"
dimension = "Value-anchor rot on field addition: a delivery that grows an existing type's fields leaves the type's existing hand-vector sweeps decoding the new data but asserting nothing about it — the EXECUTED-but-unasserted member of the fixture-blindness family, whose never-executed first member is \"An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate\"."
observable = "- **Value-anchor rot on field addition: a delivery that grows an existing type's fields leaves the type's existing hand-vector sweeps decoding the new data but asserting nothing about it — the EXECUTED-but-unasserted member of the fixture-blindness family, whose never-executed first member is \"An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate\".** Proven instance (review-caught, not by any gate): the float presence-field delivery gave `array_opt_fields` (`tests/core`) real `x`/`z` presence fields, and the fixture's existing decode sweep — which already fed x/z-present bytes through `from_cbor_bytes`, and whose own comment states the discipline (\"decode-accepts alone proved nothing — pin every field\") — kept passing without asserting the new fields or exercising their serialize side in that long-optional-chain shape; closed by extending the sweep's anchors + round-trip in the review commit. The permanently-declined full `cargo-mutants` sweep (§ Declined) would have covered this class only PARTIALLY: a generator mutant breaking the new behavior dies to any OTHER fixture asserting the same arm (here the corpus fixture's emitted tests), so mutation scoring cannot see that one hand suite's anchors went vacuous for its own distinct shape. The working rule is current state in `tests/README.md` § \"Hand-vector suites\", as the fourth of that section's assertion shapes. Mechanical layer on a SECOND instance: a per-suite anchor-completeness check (each round-tripped type's public fields ⊆ the fields its suite's asserts mention — buildable as a grep-level floor over `tests/*/tests.rs`), accepting its enumeration cost then, not before."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.value-anchor-rot-field-addition-delivery-grows-existing"]
action_on_fire_md = """Set testing.value-anchor-rot-field-addition-delivery-grows-existing to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.value-anchor-rot-field-addition-delivery-grows-existing's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.value-anchor-rot-field-addition-delivery-grows-existing"]

[[record]]
id = "testing.trigger.verify-cache-transparency-b-split-emission-profile-embed"
title = "Watch escalation for testing.verify-cache-transparency-b-split-emission-profile-embed."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Watch escalation for testing.verify-cache-transparency-b-split-emission-profile-embed.
"""
transition_kind = "watch_escalation"
failure_signature_md = """One emission-embed evidence row flips its embedded/minted bit across the A/B run, while offline immediate retry is stable.
"""
capture_procedure_md = """Capture both evidence rows, holder generated source, cache hit provenance, versions, and solo/offline retry.
"""
response_md = """Rerun once offline and do not hand-revert unless the deterministic retry reproduces.
"""
escalation_action_md = """When recurrence rate justifies work, make emission-embed generation use a fresh output directory per cell.
"""
retirement_semantics_md = """Retire after the A/B gate repeatedly proves deterministic fresh-dir behavior.
"""
evaluation = "unknown"

[[record]]
id = "testing.trigger.wac-composed-component-transpiled-jco-loses-cross-instance"
title = "Lifecycle review for testing.wac-composed-component-transpiled-jco-loses-cross-instance."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.wac-composed-component-transpiled-jco-loses-cross-instance.
"""
transition_kind = "promotion_trigger"
observer = "fresh-evidence submitter for testing.wac-composed-component-transpiled-jco-loses-cross-instance"
dimension = "A `wac`-composed component transpiled by jco loses cross-instance resource identity, and the worse half of that is SILENT."
observable = "The known-broken composed-artifact leg fails under a supported wac version, proving the cross-instance transpilation defect changed; a green or loud-skip does not fire."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.wac-composed-component-transpiled-jco-loses-cross-instance"]
action_on_fire_md = """Set testing.wac-composed-component-transpiled-jco-loses-cross-instance to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.wac-composed-component-transpiled-jco-loses-cross-instance's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.wac-composed-component-transpiled-jco-loses-cross-instance"]

[[record]]
id = "testing.trigger.wasm32-link-floor-deferred-reject-twin"
title = "Lifecycle review for testing.wasm32-link-floor-deferred-reject-twin."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.wasm32-link-floor-deferred-reject-twin.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.wasm32-link-floor-deferred-reject-twin"
dimension = "A wasm32 LINK floor for the deferred reject twin."
observable = "- **A wasm32 LINK floor for the deferred reject twin.** `workspace_dep_defers_reject_ordered_set_twins` compiles both generated crates on the HOST target, which is blind to the failure the deferral exists to remove: two `#[wasm_bindgen]` classes of one name are a `rust-lld: duplicate symbol` and nothing else. The reject family therefore inherits its link evidence from the loose-list cell (`extern_wrapper_index_defers_to_dep`'s RED leg) rather than owning it. Adding the floor means teaching the committed hand dep pair (`tests/index-dep-crate{,-wasm}`) an `IdxFooOrderedSet` class — a shared fixture several index-mode cells read, so the change has to be made with their expectations in hand rather than as a drive-by. Reopening signal (magnitude, consumer-side): a workspace whose consumers borrow reject sets from more than one dependency, where a placement mistake stops being reproducible from the single-dep cells the host-target check covers."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.wasm32-link-floor-deferred-reject-twin"]
action_on_fire_md = """Set testing.wasm32-link-floor-deferred-reject-twin to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.wasm32-link-floor-deferred-reject-twin's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.wasm32-link-floor-deferred-reject-twin"]

[[record]]
id = "testing.trigger.working-tree-gate-run-concurrent-live-implementation-agent"
title = "Watch escalation for testing.working-tree-gate-run-concurrent-live-implementation-agent."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Watch escalation for testing.working-tree-gate-run-concurrent-live-implementation-agent.
"""
transition_kind = "watch_escalation"
failure_signature_md = """A publishing gate reads half-edited `src/`/`static/` files and rewrites committed outputs from a dirty hybrid tree.
"""
capture_procedure_md = """Capture dirty-status before/after, editing process/session, gate banner, rewritten diff, commit comparison, and quiet-tree rerun.
"""
response_md = """Discard only the gate-owned hybrid rewrite after attribution, serialize the editor and gate, and rerun quiet.
"""
escalation_action_md = """On a second instance, make publishing verify refuse rewrites when relevant source is dirty.
"""
retirement_semantics_md = """Retire when publication is constructionally tied to one commit or explicit reviewed override.
"""
evaluation = "unknown"

[[record]]
id = "testing.trigger.workspace-mode-wasm32-wasip2-build-gate-both-faces"
title = "Lifecycle review for testing.workspace-mode-wasm32-wasip2-build-gate-both-faces."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.workspace-mode-wasm32-wasip2-build-gate-both-faces.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.workspace-mode-wasm32-wasip2-build-gate-both-faces"
dimension = "A workspace-mode `wasm32-wasip2` build gate for a BOTH-FACES tree."
observable = "- **A workspace-mode `wasm32-wasip2` build gate for a BOTH-FACES tree.** `component_wasip2_build` builds component-only workspaces from the root over manifests exactly as emitted, which reaches the rust crate's own lib targets; a component-only tree is emitted `crate-type = [\"rlib\"]`, so no cdylib is linked for wasip2 there. A tree generated with `--wasm=true` as well keeps the cdylib that face needs, and that shape is the one whose wasip2 workspace build can SIGSEGV the sysroot's `wasm-component-ld`/LLD. No gate builds it: the one both-faces sweep (`component_corpus_compiles`) `cargo check`s the component package alone, over a rust crate it narrows by hand first — exactly what the `--component` flag doc prescribes for a both-faces consumer. Such a gate would assert a claim about the LINKER rather than about anything this tool emits, and that claim is red against a LIVE upstream LLD defect (measured 2026-08-08, each cell deterministic across repeated links: the current emission's cdylib crashes rustc 1.96.1's LLD (LLVM 22.1.2) and links clean on 1.97.1 (LLVM 22.1.6); the 2026-08-01-era emission flips exactly the other way — so no pin choice gives the gate a green side to be red-FIRST against, and a permanently-red gate asserts nothing a doc does not). The unfiled upstream report with both reproducer recipes and the full matrix is the gitignored `draft/` LLD-crash note (conclusion and numbers recorded here because that note is checkout-local)."
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.workspace-mode-wasm32-wasip2-build-gate-both-faces"]
action_on_fire_md = """Set testing.workspace-mode-wasm32-wasip2-build-gate-both-faces to ready and execute only its preserved acceptance/mechanical-layer scope.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.workspace-mode-wasm32-wasip2-build-gate-both-faces's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.workspace-mode-wasm32-wasip2-build-gate-both-faces"]

[[record]]
id = "testing.trigger.wrapper-name-user-set-name-synthesized-collection-wrapper"
title = "Lifecycle review for testing.wrapper-name-user-set-name-synthesized-collection-wrapper."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "semantic_only"

[record.payload]
kind = "signal"
summary_md = """Lifecycle review for testing.wrapper-name-user-set-name-synthesized-collection-wrapper.
"""
transition_kind = "reopening_signal"
observer = "fresh-evidence submitter for testing.wrapper-name-user-set-name-synthesized-collection-wrapper"
dimension = "`@wrapper_name` — a user-set name for a synthesized collection wrapper."
observable = "- **`@wrapper_name` — a user-set name for a synthesized collection wrapper.** Declined in favor of container-encoded structural names, which is what the wrapper-name collision class needed: a name carrying its container (`PairMapKToV` vs `MapKToV`) makes a same-shape/different-flavor claim unrepresentable rather than merely diagnosable. The SAME-flavor sibling-crate collision — two crates minting one structural name — splits in two, and only one half is still open. When both sides name **one concept** (a crate re-exporting a sibling's type and minting that type's companion classes over it), renaming is the wrong remedy in principle: it mints a SECOND JS class for one concept, with no type identity against the sibling's own API, so a value obtained from the sibling cannot be handed back to it. That half is **served** by the `@extern_companions` directive (docs: Comment DSL § `@extern_companions`; its link-level acceptance is pinned by `extern_companions_defers_to_sibling_wasm_crate`), which references the sibling's class instead of minting one — the collision becomes unrepresentable rather than renamed-around. What is left for `@wrapper_name` is the genuinely-**two-concepts** collision: two crates whose same-spelled local types are different domain concepts that happen to derive one structural name, where re-spelling a key or value alias in one spec is the current answer. The design surface a future build must settle before it is cheap: two same-shape rows carrying different names, against a structural name that is currently the identity the deferral index, the request sidecars and the wrapper dedup all key on; user-authored names entering the rule-ident collision detectors, which reason about generated spellings only; and name-keyed deferral matching seeing a name no other crate can derive from the shape. Reopening signal: a consumer reports a same-flavor sibling collision between types that are **distinct domain concepts on the two sides** — so neither crate can reference the other's class, and the alias-respelling workaround would misname one of them rather than merely restate it. (The one-concept flavor no longer fires this: it has a remedy that keeps the shared name.)"
predicate_kind = "manual"
current_evidence_ids = ["testing.source-observation.wrapper-name-user-set-name-synthesized-collection-wrapper"]
action_on_fire_md = """Create only the specifically demanded work without widening the settled boundary.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "manual"
review_procedure_md = """Compare fresh, durable evidence with the exact trigger and explicit non-trigger exclusions in testing.wrapper-name-user-set-name-synthesized-collection-wrapper's payload.detail_md. Record the observer, scope, commit or consumer, date, and unprobed remainder. A source read alone cannot decide the event.
"""
evidence_ids = ["testing.source-observation.wrapper-name-user-set-name-synthesized-collection-wrapper"]

[[record]]
id = "testing.twin-implementation.comment-ast-change-cadence"
title = "On the next `comment_ast` grammar change of ANY form (not just a new directive):"
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "signal"
summary_md = """On the next `comment_ast` grammar change of ANY form (not just a new directive):
"""
detail_md = """  - *On the next `comment_ast` grammar change of ANY form (not just a new directive):* do the AST
    floor instead of hand-extending `DSL_TAGS` again — parse the comments with the real
    `comment_ast` via a small `examples/` binary, as the role floor already does with
    `ast_roles.rs`. The floor is the only fix for the tripwire's blind spot, ARG-GRAMMAR drift
    within an unchanged directive set (the proven `@used_as_key` flavor shape: no set delta,
    changed consumption, a new panic path), because hand-picked selfCheck vectors drift WITH the
    mirror by construction. The floor's scope also includes the mirror's flavor-set→sibling-id
    crediting (`dsl.used_as_key.{hash,ord,hash_ord}`, mirroring `DemandSet`'s bare/flavored
    exclusivity) — hand-mirrored consumption accreted without any `comment_ast` grammar change,
    which is why this trigger has correctly not fired yet.
"""
transition_kind = "cadence"
owner_reference_id = "ref-testing-twin-implementation-comment-ast-change-cadence-parent"
event_source = "comment_ast grammar authority"
period_or_event_md = """Run on the next comment_ast grammar change of any form, including argument grammar with no directive-set delta.
"""
checklist_md = """Replace the hand-extended DSL_TAGS mirror with a small examples binary parsing through the real comment_ast; cover flavor-set to sibling-id crediting and unchanged-directive-set argument grammar.
"""
missed_action_md = """Block the grammar change from landing with another hand-mirrored extension; the AST floor is due in the same delivery.
"""
evaluation = "unknown"

[[record.source_replacement]]
span_id = "span-part-part-comment-ast-grammar-change-any-form-just-new"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.twin-implementation.own-reviewed-change-rule"
title = "Its own reviewed change, never a drive-by:"
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "control"
summary_md = """Its own reviewed change, never a drive-by:
"""
detail_md = """  - *Its own reviewed change, never a drive-by:* unify the zero-min RangeCheck payload — the same
    authored zero-min bound reports `min: Some(0)` in wrapper payloads vs `min: None` at member
    sites because the drop-the-redundant-zero decision itself has multiple owners
    (`RustType::with_bounds` normalizes the member channel at IR build; the wrapper's `min_max`
    channel bypasses it; parsing's occurrence filter and `classify_sign_arm`'s uint-arm filter
    spell the same decision in their own domains). A user-visible error-message change.
"""
control_kind = "review_rule"
control_state = "live"
reference_ids = ["ref-testing-twin-implementation-own-reviewed-change-rule-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"]
claim_md = """The current controls, proven instance, and standing rule are the exact claims in testing.twin-implementation.own-reviewed-change-rule's preserved detail.
"""
boundary_md = """This control owns current containment only. It does not claim the deferred mechanical layer exists, that the authored trigger is unmet, or that an unprobed consumer and repository universe is empty.
"""

[[record.source_replacement]]
span_id = "span-part-part-own-reviewed-change-never-drive"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.twin-implementation.read-caught-instance-trigger"
title = "On the next read-caught instance of the class:"
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "signal"
summary_md = """On the next read-caught instance of the class:
"""
detail_md = """  - *On the next read-caught instance of the class:* build the mechanical layer — a
    same-construct differential sweep (emit one bounded/flagged construct per site class — member
    ctor/setter, wrapper `new()`/deserialize, primitive-deserialize `.and_then`, collection len
    check — and diff the check conditions + failure payloads, ledgering deliberate site-specific
    differences), plus the cheap grep half: a snapshot-wide wart scan (same-N `< N || > N`, dead
    `< 0` over unsigned/`len()` exprs) in the spirit of the doubled-doc-marker scan above.
"""
transition_kind = "promotion_trigger"
observer = "reviewer finding semantic drift between twin implementations"
dimension = "read-caught twin-implementation drift instances"
observable = "a fresh read-caught instance of the class recorded by the parent"
predicate_kind = "event"
current_evidence_ids = ["testing.source-observation.twin-implementation-read-caught-instance-trigger"]
action_on_fire_md = """Build the same-construct differential sweep and snapshot-wide wart scan exactly scoped in the preserved detail.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "event"
event_md = """a fresh read-caught instance of the class recorded by the parent
"""
evidence_ids = ["testing.source-observation.twin-implementation-read-caught-instance-trigger"]

[[record.source_replacement]]
span_id = "span-part-part-read-caught-instance-class"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
title = "Twin in-repo implementations of one semantic decision drifting apart (emission spellings, detection walkers, cross-language scanner mirrors) — single-owner extraction is the fix pattern; only the directive-SET flavor has a firing detector so far."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Twin in-repo implementations of one semantic decision drifting apart (emission spellings, detection walkers, cross-language scanner mirrors) — single-owner extraction is the fix pattern; only the directive-SET flavor has a firing detector so far.
"""
detail_md = """- **Twin in-repo implementations of one semantic decision drifting apart (emission spellings,
  detection walkers, cross-language scanner mirrors) — single-owner extraction is the fix
  pattern; only the directive-SET flavor has a firing detector so far.** The in-repo sibling of
  the parallel-constructor residual's predicate lesson above (\"when a fix needs the same decision
  an existing function already makes, extract the decision, never mirror it\"): two implementations
  of one decision, both living in this repo, drift apart while every behavioral oracle stays
  green — semantically-equivalent drift has no oracle but reading (decode conformance sees equal
  accept/reject sets, the mutation sweep sees no behavioral delta, the compile gates stay green;
  pending-system coverage check done at recording time: the grammar-fuzzer escalations own only
  the ACTIVE missing-import flavor of the walker instance — a recombination shape table extended
  to nested container positions would compile-fail it, E0433 — and NO pending item sees the
  semantically-equivalent flavor). Three read-caught instances, each fixed by single-owner
  extraction and pinned: (1) `generate_wrapper_struct` hand-built its range-check
  `cond`/`failure_expr` beside the shared helpers and drifted three spellings apart (`!= N`
  collapse, zero-min lower-leg elision, an inline float-payload duplicate) —
  `bounds_check_if_block` + `range_check_err` (`non_negative`/`location` params) are now the
  single owner, pinned by the `bounds_spellings` corpus fixture; (2) `uses_non_empty_{vec,map}`
  hand-walked the `RustStructType` variants, mirroring `visit_all_rust_types`' position
  enumeration, and drifted two legs short (named-array `element_type`, table `domain`) — masked
  by the transparent alias every top-level array/table rule co-registers; now folded onto the
  canonical visitor, pinned by `nonempty_nested_positions`; (3) cross-language:
  `corpus_detect.ts`'s `DSL_TAGS` scanner is a documented hand mirror of `comment_ast.rs`'s
  directive grammar and drifted twice (no `@used_as_elem`; pre-flavor `@used_as_key` grammar),
  with its hand-picked selfCheck vectors drifting WITH it — the layers that fired
  (`verify.ts`'s docs-lint, `project_corpus`'s content-drift check A) trigger only via adjacent
  artifacts (a docs mention, a `[[cover]]` entry), not the mirror itself. A fourth, BYTE-COUPLED
  flavor was read-caught before it fired (D1's core/alloc rewrite, 2026-07-29):
  `composed_runtime_static_files`' preserve flavoring `.replace()`s a byte-exact import line that
  lives in `static/non_empty_map.rs`, so rewriting that line's path would have silently no-op'd
  the substitution — distinct from the instances above in failure mode: not silent (the mangled
  flavor is an E0432 in every preserve corpus-compile cell, `local` tier) but loud-LATE and far
  from the causing edit, and invisible to any grep of either file alone. Fixed as a synchronized
  edit with a move-together comment on BOTH sides; the instance-level tripwire, due on the next
  touch of that composition code, is a must-hit replace (assert the pattern occurred before
  substituting — the selfCheck-lockstep analog for byte mirrors, turning the no-op into a
  generation-time panic that every snapshot regeneration sees at `fast` tier). Directive-SET
  drift now has a firing detector: `corpus_detect.ts`'s selfCheck lockstep tripwire demands set equality
  between the mirror's `MIRRORED_DIRECTIVES` and the authority's `tag(\"@…\")` literals at import
  time, so `project_corpus` (fast tier, CI) fires the moment a directive is added or removed on
  either side. Remaining work, each piece behind its trigger:
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
control_ids = ["testing.guard.twin-repo-implementations-semantic-drifting-apart-emission-spellings"]
transition_ids = ["testing.trigger.twin-repo-implementations-semantic-drifting-apart-emission-spellings"]

[[record.source_replacement]]
span_id = "span-record-twin-repo-implementations-semantic-drifting-apart-emission-spellings"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.unused-imports-generated-crates-trait-import-class-name"
title = "`unused_imports` on generated crates — residual trait-import class the name-scan model cannot reach."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """`unused_imports` on generated crates — residual trait-import class the name-scan model cannot reach.
"""
detail_md = """- **`unused_imports` on generated crates — residual trait-import class the name-scan model cannot
  reach.** The rustc-warning DETECTOR is live and BROAD — the generated-code unused-import scan
  (`unused_generated_import_lines`) inside `feature_corpus_compiles` fails on ANY `unused import`
  warning in the purely-generated crates, and (v3) its sibling `unused_generated_variable_lines`
  fails on ANY `unused variable` warning too, cache-key-versioned by the `lint=unused-imports-v3`
  marker (current state in `tests/README.md`'s description of that gate). The variables half was
  added on a consumer-reported instance (the constant count-arm `Some(x) => 1` binding): the corpus
  held the provoking shape all along (33 committed-snapshot instances), so the escape mechanism was
  a missing ASSERTED CLASS, not input poverty — `generated_code_clippy_clean` had kept
  `unused_variables` at warn, lumping it with `unused_imports`, whose stay-warn rationale (the
  legitimate trait residue below) has no variables analogue. That gate now denies
  `-D unused_variables` across all four of its cases and all three generated crates, so the class
  has two owners; `unused_imports` stays at warn there, because the trait residue is real.

  The detector's reach is three capture-site families. First, `feature_corpus_compiles`' own cells,
  where both scans run directly over crates that are 100% generated. Second, the nested cargo stderr
  the workspace-requests gates already capture: `assert_no_unused_generated_warnings` runs both
  scans over `workspace_requests_hosts_cross_scope_elements` (facet-1 wasm check),
  `workspace_requests_cohosted_keys_list_no_self_import` (wasm check) and
  `workspace_requests_hosts_borrowed_wrappers` (both wasm32 builds) — at no added cargo cost. That
  family was itself a known blind spot until it was wired, proven by the path-tail instance this
  entry records: the scan lived only in `feature_corpus_compiles` cells, which never generate under
  the cross-crate workspace flags, so prune imprecision visible only in
  `--wrapper-requests`/`--workspace-dep` output (the requested-collections sidecar) reached
  consumers before any gate — a detector-coverage cousin of the input-poverty sub-class ledgered in
  the flag-powerset entry (there the swept input lacks the shape; here the scanning gate lacks the
  flag).

  Third, every `run_test` fixture, the `extern_deps` family included, scanned by
  the location-aware `assert_no_generator_owned_unused_warnings` on all six
  cargo-driving stages `run_test` runs (rust `cargo test`; wasm `cargo test` / `cargo build`;
  `wasm-pack build`; both json-gen `cargo run`s). A `run_test` crate is not 100% generated, so the
  restriction is POSITIONAL rather than per-file: the harness appends its `tests.rs`/`deser_test`
  modules INSIDE the generated `generated/mod.rs`, so a file-level filter could not separate the two.
  `GeneratedOwnership` records the export's generated `src/` roots plus each appended file's
  pre-append line count, and rustc's separate `--> path:line:col` line attributes each warning
  against them; a warning in an appended region, in a hand-written path dep
  (`tests/extern-dep-crate/src/*`, which rustc renders ABSOLUTE where the crate's own files render
  relative to the cargo cwd), or with no location to pair at all, is exempt — as is the
  `Serialize` trait residue this entry's remaining half records, by class.

  Retracted with this wiring: an earlier version of this
  entry recorded an EMISSION defect — a generated root `src/generated/mod.rs` emitting an unused
  `use serialization::*;`, claimed to reach every consumer of a multifile-shaped spec — which is
  FALSE. The glob is `run_test`'s own unconditional append into the just-generated
  `generated/mod.rs` (\"some pasted-in tests need this\"), so it never leaves the throwaway fixture
  export trees and no consumer ever sees it; the append now carries `#[allow(unused_imports)]`,
  the honest annotation for harness-owned convenience glue that some pasted tests need and some do
  not. Probed 2026-08-07 by generating `tests/multifile/inputs`, `tests/core/input.cddl` (default
  profile, i.e. wasm ON) and `tests/preserve-encodings/input.cddl`
  (`--preserve-encodings=true --json-serde-derives=true --json-schema-export=true`), plus a
  `--wasm=false` core cell, to scratch dirs: the string appears NOWHERE under any emitted crate.
  NOT probed against every fixture's flag set.

  Wiring the scan over the 44 `run_test` fixtures
  surfaced exactly one generator-owned warning, and it was FIXED rather than ledgered: the
  positional `@duplicates preserve` pair-map serialize loop emitted its `.enumerate()` index
  unconditionally, while the emitted body reads it only through the key/value encoding-sidecar
  lookups, so a pair-map whose key and value both carry empty sidecars warned `unused variable: i`
  in every consumer build. The escape hatch for a future hit that CANNOT be fixed in place —
  `KNOWN_GENERATOR_OWNED_WARNINGS`, a pin by file and exact warning text, asserted LIVE at the end
  of the run that owns its export so a fixed emission fails its pin as stale — ships EMPTY with its
  enforcement live, the same shape as `KNOWN_POSITION_DROPS`.

  Proven instance of exactly the blind spot the scanning covers
  (2026-07-22, consumer-reported, fixed class-level): the used-ident scan counted `::`-path-tail
  segments (`cml_chain::assets::Coin` counting `assets`), which collide with the parent's `pub mod`
  defs, so `super_glob_needed` conservatively kept the sidecar's dead `use super::*;`; the
  path-tail exclusion in `collect_idents_in_tokens` removed the false-positive class (unit + map-
  level twin tests pin both directions). The entry's earlier escalation trigger
  (a 26-warning consumer report + chain/cip36 regen) fired and is delivered — what the prune now
  removes (the `super::*` and `<common>::error::*` globs against a fully-enumerable universe, the
  wasm prelude names, the `--wasm-*-macro` leaf names, the cross-scope type idents
  `scope_references` over-imports) is the shipped contract in `docs/docs/output_format.mdx`, and the
  protector/disqualifier model it removes them by, with its pins, is current state in
  `tests/README.md` § \"Extern-interface export & `--extern-import` (the machine-generated stub
  channel)\".

  Still future-facing: the `cbor_event::se::Serialize` TRAIT
  import the non-canonical serialization prelude emits — a trait is exercised by a method call
  whose ident never appears, so name-scan cannot prove it unused; the detector's
  `UNUSED_IMPORT_TRAIT_RESIDUE` skips it. What a consumer sees is one rustc `unused import` warning
  on every build of a crate whose flavor does not need it: `push_base_serialize_imports` pushes the
  import whenever `!(preserve_encodings && canonical_form)`, while specs whose emitted impls name
  the trait only by full path (`impl cbor_event::se::Serialize for …`) never exercise it — reported
  from a downstream migration against a cip25-shaped non-preserve crate. The design that would
  retire it, recorded so it is not re-derived: a trait's only ident-invisible use is METHOD-CALL
  position, so `cbor_event::se::Serialize` is justified in a module family iff the family contains a
  bare `Serialize` ident (UFCS, a bound, a trait object — all of which the scan already sees, since
  a path TAIL like `cbor_event::se::Serialize` is correctly skipped) OR an ident `serialize`
  IMMEDIATELY PRECEDED BY A LONE `.`. The `.`-precedence is the whole load: every generated
  `impl … Serialize for X` block DEFINES `fn serialize`, so counting the bare ident would justify
  the import in every file and the naive version is useless;
  `.serialize_as_embedded_group(..)` is a different ident, so token-level matching needs no special
  case, and `collect_idents_in_tokens` already tracks `prev1`/`prev2` for the path-tail skip, which
  makes the detection itself a small local addition. The COST is not there: `is_candidate` keys on
  the import's leaf ident against `ALLOWLIST ∪ PruneConfig::extra_candidates`, and justification is
  a single `used` ident set per file, unioned across a module family through the super-glob
  protector logic — a method-call-justified candidate needs a SECOND ident set threaded through
  `used_by_path` and through every protector-union and disqualifier path beside the existing one.
  That is a structural change to a pass whose entire design is conservative-keep, and its failure
  direction is the bad one: over-prune is a consumer COMPILE ERROR where under-prune is only a
  warning, and the gate that would catch an over-prune is `feature_corpus_compiles`, so the change
  is only worth making in a cycle that can run it. Retiring the detector's
  `UNUSED_IMPORT_TRAIT_RESIDUE` exemption (and its self-test) is the other half of the same change —
  the residue and the exemption go together or not at all. Reopening signal: a second unremovable
  trait joins the residue, or a consumer for whom the warning is more than cosmetic (it breaks a
  `-D warnings` build, or it masks a real unused import in the same crate).

  Also watched, warning-severity only (never a compile error):
  the one deliberately-conservative keep the disqualifiers do not cover — an intermediate module
  between the ancestor and a deeper protector that consumes the ancestor's copy for everything
  below it; replace the per-descendant approximation with exact resolution modelling only on a
  real warning report from the live arm.

  A THIRD known DETECTOR blind spot is still open, recorded as a posture
  when the extern-breadth seeding surfaced the wasm re-export contract wart: the matrix compile
  probes (`cddl-matrix/verify.ts`, the `DEF_SPLICE`-seeded user-code rows included) judge cargo
  EXIT CODES only and run no warning scan — the wart was caught because the same shape sits in
  the corpus, whose `feature_corpus_compiles` scan is the warning detector. So a warning-class
  generator imprecision reachable ONLY through a matrix-only shape (a cell with no corpus
  sibling) ships unseen today. Deliberate for now: the matrix's verdict model is
  support-classification, not lint, and the corpus mirrors most compiled shapes. Trigger to build
  the layer (port `unused_generated_import_lines` over the matrix probes' captured stderr): a
  SECOND warning-class finding whose only compiled home is a matrix cell.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
control_ids = ["testing.guard.unused-imports-generated-crates-trait-import-class-name"]
transition_ids = ["testing.trigger.unused-imports-generated-crates-trait-import-class-name"]

[[record.source_replacement]]
span_id = "span-record-unused-imports-generated-crates-trait-import-class-name"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.value-anchor-rot-field-addition-delivery-grows-existing"
title = "Value-anchor rot on field addition: a delivery that grows an existing type's fields leaves the type's existing hand-vector sweeps decoding the new data but asserting nothing about it — the EXECUTED-but-unasserted member of the fixture-blindness family, whose never-executed first member is \"An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate\"."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """Value-anchor rot on field addition: a delivery that grows an existing type's fields leaves the type's existing hand-vector sweeps decoding the new data but asserting nothing about it — the EXECUTED-but-unasserted member of the fixture-blindness family, whose never-executed first member is \"An emission branch whose generated OUTPUT no fixture exercises is invisible to every gate\".
"""
detail_md = """- **Value-anchor rot on field addition: a delivery that grows an existing type's fields leaves the
  type's existing hand-vector sweeps decoding the new data but asserting nothing about it — the
  EXECUTED-but-unasserted member of the fixture-blindness family, whose never-executed first
  member is \"An emission branch whose generated OUTPUT no fixture exercises is invisible to every
  gate\".** Proven instance (review-caught, not by any gate): the float presence-field delivery gave
  `array_opt_fields` (`tests/core`) real `x`/`z` presence fields, and the fixture's existing decode
  sweep — which already fed x/z-present bytes through `from_cbor_bytes`, and whose own comment
  states the discipline (\"decode-accepts alone proved nothing — pin every field\") — kept passing
  without asserting the new fields or exercising their serialize side in that long-optional-chain
  shape; closed by extending the sweep's anchors + round-trip in the review commit. The
  permanently-declined full `cargo-mutants` sweep (§ Declined) would have covered this class only
  PARTIALLY: a generator mutant breaking the new behavior dies to any OTHER fixture asserting the
  same arm
  (here the corpus fixture's emitted tests), so mutation scoring cannot see that one hand suite's
  anchors went vacuous for its own distinct shape. The working rule is current state in
  `tests/README.md` § \"Hand-vector suites\", as the fourth of that section's assertion shapes.
  Mechanical layer on a SECOND instance: a per-suite anchor-completeness check (each
  round-tripped type's public fields ⊆ the fields its suite's asserts mention — buildable as a
  grep-level floor over `tests/*/tests.rs`), accepting its enumeration cost then, not before.
"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "silent_wrong_bytes"
family_classification = "none_reviewed"
control_ids = ["testing.guard.value-anchor-rot-field-addition-delivery-grows-existing"]
transition_ids = ["testing.trigger.value-anchor-rot-field-addition-delivery-grows-existing"]

[[record.source_replacement]]
span_id = "span-record-value-anchor-rot-field-addition-delivery-grows-existing"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.verify-cache-transparency-b-split-emission-profile-embed"
title = "`verify_cache_transparency` A/B split on an emission-profile EMBED cell — intermittent, distinct from the ruby and registry classes."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_operational_watch"
summary_md = """`verify_cache_transparency` A/B split on an emission-profile EMBED cell — intermittent, distinct from the ruby and registry classes.
"""
detail_md = """- **`verify_cache_transparency` A/B split on an emission-profile EMBED cell — intermittent, distinct
  from the ruby and registry classes.** Observed 2026-07-13: one `cache_transparency.ts` leg diverged
  on `prelude.tstr`'s `emission.preserve.evidence` — the synthetic-holder embed round-trip
  (`embedFallback`) landed `embedded=true` (\"round-trips when embedded\") in one run and `embedded=false`
  (\"no minted round-trip surface\") in another, same input. A back-to-back offline re-run
  (`CARGO_NET_OFFLINE=true`) was byte-identical (714 cache hits), so it is INTERMITTENT, not a
  deterministic cache-reconstruction bug: the `minted` bit of the embed holder's generated
  `generated/mod.rs` varies run-to-run (a transparent-alias-under-preserve cell whose holder sometimes
  mints a standalone test surface, sometimes not — the residual tail of the fresh-output-dir /
  warm-up-clobber class the `72cf4ce`/`a796b7c`/`dbcead7` fixes addressed for the base probe, not yet
  the emission-embed path). The tell that separates it from a ruby flake (now deterministic — README
  § \"Gotchas\") and the registry-transient class (shifting cell + `unable to update registry`): a SINGLE
  emission-embed evidence line flipping on the `embedded`/`minted` bit, reproducing on neither an
  offline re-run nor a solo probe. Re-run once (offline) to confirm transience before hand-reverting.
  The mechanical fix, when the rate warrants: make the emission-embed generation use a fresh output dir
  per cell (mirror the base-probe fix) so a prior cell's minted `mod.rs` can't be read for the current
  holder.
"""
watch_state = "watching"
signature_md = """One emission-embed evidence row flips its embedded/minted bit across the A/B run, while offline immediate retry is stable.
"""
response_md = """Rerun once offline and do not hand-revert unless the deterministic retry reproduces.
"""
escalation_transition_id = "testing.trigger.verify-cache-transparency-b-split-emission-profile-embed"
retirement_semantics_md = """Retire after the A/B gate repeatedly proves deterministic fresh-dir behavior.
"""

[[record.payload.capture_step]]
step_id = "signature"
capture_md = """Capture both evidence rows, holder generated source, cache hit provenance, versions, and solo/offline retry.
"""

[[record.payload.capture_step]]
step_id = "scope"
capture_md = """Record the full untruncated log or consumer report, command, full commit, date, environment, relevant versions, and the explicitly unprobed remainder; never cite a disposable draft log as durable evidence.
"""

[[record.source_replacement]]
span_id = "span-record-verify-cache-transparency-b-split-emission-profile-embed"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.verify-ts-refuse-unknown-flag-silently-degrading-plain"
title = "verify.ts should refuse an unknown flag instead of silently degrading to a plain run."
projection_group = "next-priority"
legacy_aliases = ["Next work 26"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """verify.ts should refuse an unknown flag instead of silently degrading to a plain run.
"""
detail_md = """26. **verify.ts should refuse an unknown flag instead of silently degrading to a plain run.**
    Every mode flag is tested with a bare `process.argv.includes`, so a mistyped flag matches
    nothing and the run proceeds as an ordinary full verify — same exit 0, same `RESULT: PASS`,
    nothing distinguishing \"did what you asked\" from \"ignored what you asked\" except the absence
    of the mode's own output lines. Proven expensive 2026-08-08 (B3-021 close): two ~14-minute
    full sweeps ran under `--mint-decode` (the real flag is `--mint-decode-foreign`) before the
    7.8-second mint itself; the log even *looks* plausible for a mint run until the missing
    `wrote …catalog.toml` line is noticed. The fix is one seam: collect the known flag/`--k=v`
    prefixes (they are all declared in one block at the top), and exit 2 at startup naming any
    `--`-prefixed argv token that matches none — the same hard-fail posture `--only` already
    takes for an unknown row id. Red-first: assert `verify.ts --mint-decode` exits 2 before
    building the list.

"""
work_state = "ready"
work_intent = "repair"
work_kind = "defect"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.verify-ts-refuse-unknown-flag-silently-degrading-plain's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""
regression_gap_ids = ["testing.proof-gap.verify-ts-refuse-unknown-flag-silently-degrading-plain"]

[[record.source_replacement]]
span_id = "span-record-verify-ts-refuse-unknown-flag-silently-degrading-plain"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.verify-ts-warm-up-cargo-test-exit-attributed"
title = "`verify.ts` warm-up `cargo test exit -15` (2026-07-19): ATTRIBUTED on the first sighting — not a flake, do not watch for it."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_incident"
summary_md = """`verify.ts` warm-up `cargo test exit -15` (2026-07-19): ATTRIBUTED on the first sighting — not a flake, do not watch for it.
"""
detail_md = """- **`verify.ts` warm-up `cargo test exit -15` (2026-07-19): ATTRIBUTED on the first sighting — not
  a flake, do not watch for it.** One session's `verify.ts` run (json warm-up; logged signature
  \"generate exit 0, cargo test exit -15, minted=true\") died because a CONCURRENT session ran
  `pkill -f cddl_verify` to clean up what it believed were its own killed run's orphans — but its
  killed `check.ts full` had died in `corpus_decode_replay`, BEFORE its verify gate ever ran, so
  the `/tmp/cddl_verify_*` nested-cargo processes it pattern-matched belonged to the other
  session's LIVE run (exit -15 = pkill's default SIGTERM; window and scratch naming match). A
  recurrence of this signature under multi-session load should FIRST be checked against
  process-kill activity in the other session before any harness attribution. The standing
  discipline it feeds is in `AGENTS.md` (never pattern-kill by tool-generic substring on a shared
  machine).
"""
incident_posture = "historical"
signature_md = """The 2026-07-19 verify warm-up exited -15 after another session pattern-killed `cddl_verify` processes.
"""
evidence_ids = ["testing.source-observation.verify-ts-warm-up-cargo-test-exit-attributed"]
attribution_md = """Cross-session SIGTERM, not a verify flake.
"""
operating_rule_reference_id = "ref-testing-verify-ts-warm-up-cargo-test-exit-attributed-rule"
retirement_reference_id = "ref-testing-verify-ts-warm-up-cargo-test-exit-attributed-rule"

[[record.source_replacement]]
span_id = "span-record-verify-ts-warm-up-cargo-test-exit-attributed"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.wac-composed-component-transpiled-jco-loses-cross-instance"
title = "A `wac`-composed component transpiled by jco loses cross-instance resource identity, and the worse half of that is SILENT."
projection_group = "standing-system"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A `wac`-composed component transpiled by jco loses cross-instance resource identity, and the worse half of that is SILENT.
"""
detail_md = """- **A `wac`-composed component transpiled by jco loses cross-instance resource identity, and the
  worse half of that is SILENT.** jco 1.26.1 allocates a separate handle table per component
  *instance* for the same resource type and emits no transfer between them for a JS-held handle, so
  on the dual-export world `component_compose` drives correctly through wasmtime: a dependency-minted
  handle passed into a consumer function throws `Resource error: Not a valid \"…\" resource` (scalar
  borrow and accumulator alike), and — the half that costs — a consumer getter returning a
  dependency-typed `own` handle resolves the index in the wrong table and returns a **different
  object**, with no error anywhere. A second, cheaper symptom sits on the same artifact: both
  packages' single interface is named `types`, so the root typings emit `export * as types` twice and
  fail `tsc --strict` with `TS2300`. It is a defect in the transpiler, not in what this tool emits —
  the identical artifact is correct through wasmtime, and a single generated crate with *two*
  interfaces transpiles and drives correctly, so the boundary is component instances rather than
  interfaces. Owned meanwhile by two things: the known-broken pin in
  `tests/component-jco/js/composed.test.mjs` (gate `component_jco`), whose failure messages each name
  the work a fix creates, and the consumer instruction in `docs/docs/component_differences.mdx`,
  cited by \"Do not transpile a composed artifact\", which prescribes per-component transpile plus
  `--map` instead. **Retirement observable: that leg FAILING** — a green composed leg means the
  defect is still there, and a red one is the good news. When it fires the work is one commit:
  replace the three known-broken assertions with positive ones, drop the hazard section, and
  re-point the packaging recipe if composing becomes the better shape. Read the gate's own log
  before concluding either way — the leg **loud-skips alone** when the ambient `wac` is absent or
  below 0.9, so a green run whose log does not name a `wac` version is silence, not evidence.

"""
work_state = "armed"
work_intent = "build_system"
work_kind = "missing_system"
risk = "wrong_public_api"
family_classification = "none_reviewed"
control_ids = ["testing.guard.wac-composed-component-transpiled-jco-loses-cross-instance"]
transition_ids = ["testing.trigger.wac-composed-component-transpiled-jco-loses-cross-instance"]

[[record.source_replacement]]
span_id = "span-record-wac-composed-component-transpiled-jco-loses-cross-instance"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.warm-up-refresh-dep-universe-lock-trust-fresh"
title = "The warm-up should REFRESH the dep-universe lock, not trust it: a fresh-resolving scratch crate outruns `tests/warmup/Cargo.lock` at every upstream release."
projection_group = "next-priority"
legacy_aliases = ["Next work 17"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """The warm-up should REFRESH the dep-universe lock, not trust it: a fresh-resolving scratch crate outruns `tests/warmup/Cargo.lock` at every upstream release.
"""
detail_md = """17. **The warm-up should REFRESH the dep-universe lock, not trust it: a fresh-resolving scratch
    crate outruns `tests/warmup/Cargo.lock` at every upstream release.** Proven 2026-08-06 (the
    stale-lock offline tail in § Operational watches, \"Registry-fetch transients\"): the
    oracle-fingerprint probe resolves a temp crate FRESH against crates.io, so the day
    `zerocopy-derive 0.8.56` published, the offline-forced gate failed sub-second wanting a
    version the locked warm-up fetch never cached — and the cargo cache holding .52 through .55
    shows the class fires at release cadence, with 13 further warmup deps sitting behind latest.
    Mechanical shape: in `check.ts`'s `warmupThenOffline`, run
    `cargo update --manifest-path tests/warmup/Cargo.toml` (and the fuzz manifest if its scratch
    consumers resolve fresh) before the retried fetch — the warm-up is already the tier's one
    online step, and the lock is gitignored per-checkout state, so refreshing it there is free of
    determinism cost; keep the workspace fetch `--locked` (the workspace lock is committed and
    must NOT drift). Alternative if refresh-always proves noisy: pin the fingerprint probe crate's
    manifest to exact versions so it stops resolving fresh — either ends the class; the manual
    `cargo update -p <dep>` tail (§ Operational watches, \"Registry-fetch transients\") stays only
    until one of them lands. A SECOND unlocked dep universe sits beside this one with the inverse
    failure mode: the shared TS-toolchain install (`/.ts-toolchain/<manifest-hash>/`) runs
    `npm install` over caret ranges (`json-schema-to-typescript: ^15.0.4`, `TS_COMPILER_PIN`
    `^5.9.0`) with no lockfile, and its `.installed` stamp means it never re-resolves — so two
    checkouts under one manifest-hash key can hold different resolved trees, while the `<key>JSON`
    declaration-name contract was established empirically against json2ts 15.0.4 only. Whichever
    refresh policy this item lands should state that universe's posture too (lock it, or
    re-resolve deliberately).

"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "false_pass_or_red"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.warm-up-refresh-dep-universe-lock-trust-fresh's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""
regression_evidence_ids = ["testing.proof.warm-up-refresh-dep-universe-lock-trust-fresh"]

[[record.source_replacement]]
span_id = "span-record-warm-up-refresh-dep-universe-lock-trust-fresh"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.wasm-door-vocabulary.reopening-signal"
title = "Reopening signal:"
projection_group = "next-priority"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "signal"
summary_md = """Reopening signal:
"""
detail_md = """    - **Reopening signal:** a consumer reporting a wasm member absent that the rust crate has. That
      reaches us pre-diagnosed (the observable is an `undefined` method on a JS class whose rust
      counterpart they can point at), and the count of silently-missing members is the dimension
      along which a hand-listed vocabulary stops being maintainable.

"""
transition_kind = "reopening_signal"
observer = "consumer comparing a generated JS class with its rust counterpart"
dimension = "rust door members silently absent from wasm"
observable = "a consumer reports an undefined wasm member and identifies the rust member that exists"
predicate_kind = "event"
current_evidence_ids = ["testing.source-observation.wasm-door-vocabulary-reopening-signal"]
action_on_fire_md = """Reopen the delivered door-vocabulary boundary and derive the reported member class without widening unrelated trait methods.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "event"
event_md = """a consumer reports an undefined wasm member and identifies the rust member that exists
"""
evidence_ids = ["testing.source-observation.wasm-door-vocabulary-reopening-signal"]

[[record.source_replacement]]
span_id = "span-part-part-reopening-signal-wasm"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.wasm-face-s-door-vocabulary-hand-listed-mechanism"
title = "The wasm face's door vocabulary is hand-listed, and no mechanism derives it from the rust surface it mirrors."
projection_group = "next-priority"
legacy_aliases = ["Next work 10"]
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """The wasm face's door vocabulary is hand-listed, and no mechanism derives it from the rust surface it mirrors.
"""
detail_md = """10. **The wasm face's door vocabulary is hand-listed, and no mechanism derives it from the rust
    surface it mirrors.** `wasm_door_vocabulary_matches_the_posture_that_owes_it`
    (`src/tests/wasm_parity_tests.rs`) pins the six flag-conditional door members —
    `to_cbor_bytes`, `from_cbor_bytes`, `to_canonical_cbor_bytes`, `to_json`, `to_json_value`,
    `from_json` — per posture and in both directions, against a table it carries in source. A
    SEVENTH member, added to the emitter and not to that table, is caught by nothing: the table is
    the vocabulary, and it is written by hand.
"""
work_state = "ready"
work_intent = "add_regression"
work_kind = "regression_gap"
risk = "wrong_public_api"
family_classification = "none_reviewed"
acceptance_md = """Complete exactly the operative work, validation, and bounded exclusions stated in testing.wasm-face-s-door-vocabulary-hand-listed-mechanism's payload.detail_md; do not widen adjacent future work.
"""
priority_rationale_md = """The source owner places this item in Next priority order; WP5T preserves that authored pickup posture without deriving priority from its numeric alias.
"""

[[record.source_replacement]]
span_id = "span-record-wasm-face-s-door-vocabulary-hand-listed-mechanism"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.wasm32-link-floor-deferred-reject-twin"
title = "A wasm32 LINK floor for the deferred reject twin."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A wasm32 LINK floor for the deferred reject twin.
"""
detail_md = """- **A wasm32 LINK floor for the deferred reject twin.** `workspace_dep_defers_reject_ordered_set_twins`
  compiles both generated crates on the HOST target, which is blind to the failure the deferral
  exists to remove: two `#[wasm_bindgen]` classes of one name are a `rust-lld: duplicate symbol`
  and nothing else. The reject family therefore inherits its link evidence from the loose-list
  cell (`extern_wrapper_index_defers_to_dep`'s RED leg) rather than owning it. Adding the floor
  means teaching the committed hand dep pair (`tests/index-dep-crate{,-wasm}`) an
  `IdxFooOrderedSet` class — a shared fixture several index-mode cells read, so the change has to
  be made with their expectations in hand rather than as a drive-by. Reopening signal
  (magnitude, consumer-side): a workspace whose consumers borrow reject sets from more than one
  dependency, where a placement mistake stops being reproducible from the single-dep cells the
  host-target check covers.
"""
work_state = "deferred"
work_intent = "add_regression"
work_kind = "coverage_cell"
risk = "compile_failure"
family_classification = "none_reviewed"
transition_ids = ["testing.trigger.wasm32-link-floor-deferred-reject-twin"]

[[record.source_replacement]]
span_id = "span-record-wasm32-link-floor-deferred-reject-twin"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.working-tree-gate-run-concurrent-live-implementation-agent"
title = "A working-tree gate run concurrent with a live implementation agent measures a hybrid tree that exists in no commit — and a gate that REWRITES committed files on pass launders that hybrid into plausible-looking state."
projection_group = "operational-watches"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "testing_operational_watch"
summary_md = """A working-tree gate run concurrent with a live implementation agent measures a hybrid tree that exists in no commit — and a gate that REWRITES committed files on pass launders that hybrid into plausible-looking state.
"""
detail_md = """- **A working-tree gate run concurrent with a live implementation agent measures a hybrid tree that
  exists in no commit — and a gate that REWRITES committed files on pass launders that hybrid into
  plausible-looking state.** Proven 2026-08-06: an orchestrator launched `check.ts --only verify`
  while an implementation sub-agent was mid-edit in `src/generation/deserialize.rs`; verify built
  the generator from the working tree, probed dozens of fixed-value cells through the half-edited
  binary (every one exit-101), PASSED its own gate contract, and rewrote
  `cddl-matrix/annotations/cddl_codegen.toml` with the garbage verdicts. Nothing failed loudly: the
  banner's `-dirty` marker was the only trace, and the damage was caught by reading the diff, not
  by any guard. Working rule: gates that read `src/` are serialized against anything editing
  `src/` — an orchestrator running implementation agents runs NO gates until the agent commits and
  reports (the same session later ran the identical gate on the quiet tree: PASS with a
  byte-identical annotation rewrite, which is what validation looks like). Mechanical layer on a
  second instance: `verify.ts` refusing to REWRITE annotations when the tree is dirty under
  `src/` or `static/` (probing dirty is fine; publishing verdicts from a tree no commit names is
  the hazard — the `-dirty` marker it already prints is the predicate).

"""
watch_state = "attributed"
signature_md = """A publishing gate reads half-edited `src/`/`static/` files and rewrites committed outputs from a dirty hybrid tree.
"""
attribution_md = """The 2026-08-06 verify run published exit-101 hybrid annotations and a quiet rerun repaired them.
"""
operating_rule_reference_id = "ref-testing-working-tree-gate-run-concurrent-live-implementation-agent-operating"
response_md = """Discard only the gate-owned hybrid rewrite after attribution, serialize the editor and gate, and rerun quiet.
"""
escalation_transition_id = "testing.trigger.working-tree-gate-run-concurrent-live-implementation-agent"
retirement_semantics_md = """Retire when publication is constructionally tied to one commit or explicit reviewed override.
"""

[[record.payload.capture_step]]
step_id = "signature"
capture_md = """Capture dirty-status before/after, editing process/session, gate banner, rewritten diff, commit comparison, and quiet-tree rerun.
"""

[[record.payload.capture_step]]
step_id = "scope"
capture_md = """Record the full untruncated log or consumer report, command, full commit, date, environment, relevant versions, and the explicitly unprobed remainder; never cite a disposable draft log as durable evidence.
"""

[[record.source_replacement]]
span_id = "span-record-working-tree-gate-run-concurrent-live-implementation-agent"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.workspace-mode-wasm32-wasip2-build-gate-both-faces"
title = "A workspace-mode `wasm32-wasip2` build gate for a BOTH-FACES tree."
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "work"
summary_md = """A workspace-mode `wasm32-wasip2` build gate for a BOTH-FACES tree.
"""
detail_md = """- **A workspace-mode `wasm32-wasip2` build gate for a BOTH-FACES tree.** `component_wasip2_build`
  builds component-only workspaces from the root over manifests exactly as emitted, which reaches
  the rust crate's own lib targets; a component-only tree is emitted `crate-type = [\"rlib\"]`, so no
  cdylib is linked for wasip2 there. A tree generated with `--wasm=true` as well keeps the cdylib
  that face needs, and that shape is the one whose wasip2 workspace build can SIGSEGV the sysroot's
  `wasm-component-ld`/LLD. No gate builds it: the one both-faces sweep
  (`component_corpus_compiles`) `cargo check`s the component package alone, over a rust crate it
  narrows by hand first — exactly what the `--component` flag doc prescribes for a both-faces
  consumer. Such a gate would assert a claim about the LINKER rather than about anything this tool
  emits, and that claim is red against a LIVE upstream LLD defect (measured 2026-08-08, each cell
  deterministic across repeated links: the current emission's cdylib crashes rustc 1.96.1's LLD
  (LLVM 22.1.2) and links clean on 1.97.1 (LLVM 22.1.6); the 2026-08-01-era emission flips exactly
  the other way — so no pin choice gives the gate a green side to be red-FIRST against, and a
  permanently-red gate asserts nothing a doc does not). The unfiled upstream report with both
  reproducer recipes and the full matrix is the gitignored `draft/` LLD-crash note (conclusion and
  numbers recorded here because that note is checkout-local).
"""
work_state = "deferred"
work_intent = "build_system"
work_kind = "missing_system"
risk = "compile_failure"
family_classification = "none_reviewed"
transition_ids = ["testing.workspace-wasip2.reopening-signal"]

[[record.source_replacement]]
span_id = "span-record-workspace-mode-wasm32-wasip2-build-gate-both-faces"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.workspace-wasip2.reopening-signal"
title = "Reopening signal:"
projection_group = "deferred-features"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "signal"
summary_md = """Reopening signal:
"""
detail_md = """  - **Reopening signal:** an upstream LLD fix reaching the pinned toolchain (re-measure BOTH
    matrix cells before believing it — a single green cell is what the 2026-08-08 correction was
    shipped on), or a consumer reporting the linker SIGSEGV from their own both-faces workspace's
    wasip2 build — the flag doc names the exact failure signature, so the report arrives
    pre-diagnosed with the input that reproduces in their environment.

"""
transition_kind = "reopening_signal"
observer = "toolchain updater or both-faces workspace consumer"
dimension = "wasm32-wasip2 both-faces workspace linker viability"
observable = "an upstream LLD fix reaches the pinned toolchain and both matrix cells remeasure green, or a consumer reports the documented linker SIGSEGV from a both-faces workspace wasip2 build"
predicate_kind = "event"
current_evidence_ids = ["testing.source-observation.workspace-wasip2-reopening-signal"]
action_on_fire_md = """For the upstream branch, remeasure both matrix cells; for the consumer branch, promote the reproducing both-faces workspace gate.
"""
evaluation = "unknown"

[record.payload.predicate]
predicate_kind = "event"
event_md = """an upstream LLD fix reaches the pinned toolchain and both matrix cells remeasure green, or a consumer reports the documented linker SIGSEGV from a both-faces workspace wasip2 build
"""
evidence_ids = ["testing.source-observation.workspace-wasip2-reopening-signal"]

[[record.source_replacement]]
span_id = "span-part-part-reopening-signal-workspace"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[record]]
id = "testing.wrapper-name-user-set-name-synthesized-collection-wrapper"
title = "`@wrapper_name` — a user-set name for a synthesized collection wrapper."
projection_group = "declined-boundaries"
render_authority = "semantic"
projection_visibility = "document"

[record.payload]
kind = "decision"
summary_md = """`@wrapper_name` — a user-set name for a synthesized collection wrapper.
"""
detail_md = """- **`@wrapper_name` — a user-set name for a synthesized collection wrapper.** Declined in favor of
  container-encoded structural names, which is what the wrapper-name collision class needed: a name
  carrying its container (`PairMapKToV` vs `MapKToV`) makes a same-shape/different-flavor claim
  unrepresentable rather than merely diagnosable. The SAME-flavor sibling-crate collision — two
  crates minting one structural name — splits in two, and only one half is still open.
  When both sides name **one concept** (a crate re-exporting a sibling's type and minting that
  type's companion classes over it), renaming is the wrong remedy in principle: it mints a SECOND JS
  class for one concept, with no type identity against the sibling's own API, so a value obtained
  from the sibling cannot be handed back to it. That half is **served** by the
  `@extern_companions` directive (docs: Comment DSL § `@extern_companions`; its link-level
  acceptance is pinned by `extern_companions_defers_to_sibling_wasm_crate`), which references the
  sibling's class instead of minting one — the collision becomes unrepresentable rather than
  renamed-around. What is left for `@wrapper_name` is the genuinely-**two-concepts** collision: two
  crates whose same-spelled local types are different domain concepts that happen to derive one
  structural name, where re-spelling a key or value alias in one spec is the current answer. The
  design surface a future build must settle before it is cheap: two same-shape rows carrying
  different names, against a structural name that is currently the identity the deferral index, the
  request sidecars and the wrapper dedup all key on; user-authored names entering the rule-ident
  collision detectors, which reason about generated spellings only; and name-keyed deferral matching
  seeing a name no other crate can derive from the shape. Reopening signal: a consumer reports a
  same-flavor sibling collision between types that are **distinct domain concepts on the two sides**
  — so neither crate can reference the other's class, and the alias-respelling workaround would
  misname one of them rather than merely restate it. (The one-concept flavor no longer fires this:
  it has a remedy that keeps the shared name.)
"""
decision_state = "decided"
rationale_md = """The decided boundary and its reasons are the exact preserved detail of testing.wrapper-name-user-set-name-synthesized-collection-wrapper.
"""
authority_reference_id = "ref-testing-wrapper-name-user-set-name-synthesized-collection-wrapper-authority"
permanence = "reopenable"
transition_ids = ["testing.trigger.wrapper-name-user-set-name-synthesized-collection-wrapper"]

[[record.source_replacement]]
span_id = "span-record-wrapper-name-user-set-name-synthesized-collection-wrapper"
replacement_field = "payload.detail_md"
review_note_md = """WP5T source-owner review: the whole legacy block remains byte-exact semantic detail; typed fields own lifecycle and joins.
"""

[[part]]
part_id = "part-advice-shaped-spelling-message-prints-remedy-string-strings"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "EVERY advice-shaped spelling a message prints is a remedy string, not only the strings"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - EVERY advice-shaped spelling a message prints is a remedy string, not only the strings
      introduced by \"instead\" or \"remedy\": an advertised VALID PLACEMENT is advice a reader copies
      verbatim.
"""

[[part.source_replacement]]
span_id = "span-part-part-advice-shaped-spelling-message-prints-remedy-string-strings"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-anonymous-group-rejection-s-give-name-using-name"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "The anonymous-group rejection's \"give it a name using the `@name` notation\" was true for the"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - The anonymous-group rejection's \"give it a name using the `@name` notation\" was true for the
      bare inline array and dead advice for the tag-wrapped trigger (`f: #6.42([x: uint]) ; @name Foo`),
      because the name reader's parent-chain walk stopped at the interposed `TaggedData` node — one
      message, two trigger classes, and the hand-chosen untagged vector stayed green throughout. The
      reader now walks tag layers, pinned by
      `tagged_anon_array_member_name_emits_the_named_rule_remedy`, so what that instance leaves is
      the sweep's shape rather than the shape itself.
"""

[[part.source_replacement]]
span_id = "span-part-part-anonymous-group-rejection-s-give-name-using-name"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-bare-fixed-table-value-refusal-continued-say-named"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "The bare fixed-table-VALUE refusal continued to say a named fixed rule was rejected after"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - The bare fixed-table-VALUE refusal continued to say a named fixed rule was rejected after
      B3-024A made it a nominal singleton. Reading found the stale sentence; its corrected
      `five = 5` / `{ * uint => five }` advice is now executed under default and preserve by
      `fixed_table_value_rejection_advertises_executable_nominal_remedy`.

"""

[[part.source_replacement]]
span_id = "span-part-part-bare-fixed-table-value-refusal-continued-say-named"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-both-ignore-rejections-printed-k-v-ignore-valid"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "Both `@ignore` rejections printed \"`{ 1: a, * k => v } ; @ignore`\" as the one valid placement,"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - Both `@ignore` rejections printed \"`{ 1: a, * k => v } ; @ignore`\" as the one valid placement,
      and that exact spelling itself refuses (a type rule's after-brace comment binds to the RULE
      slot, where `@ignore` is rejected) — the working entry-line spelling was documented correctly
      in `comment_dsl.mdx` all along, so only the messages lied.
"""

[[part.source_replacement]]
span_id = "span-part-part-both-ignore-rejections-printed-k-v-ignore-valid"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-cheap-system-matrix-shape-pairs-compiled-wasm-side"
parent_record_id = "testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
title = "The cheap system is a matrix of shape PAIRS compiled wasm-side"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - **The cheap system is a matrix of shape PAIRS compiled wasm-side** — generated once per pair,
      `cargo check`ed, no vectors. `tests/matrix_multifile` (shape × reference-mode, wasm-checked per
      cell) is the nearest existing machinery, but its SHAPES axis enumerates self-contained RULE
      shapes only; a construct that exists only INSIDE a record has no row it can occupy, which is
      why the two above are hand cells. Extending that axis to record-embedded shapes is the
      concrete build.
"""

[[part.source_replacement]]
span_id = "span-part-part-cheap-system-matrix-shape-pairs-compiled-wasm-side"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-default-head-refusal-s-remedy-list-offered-bare"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "The `.default` head-refusal's remedy list offered bare `int` — a head that very check"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - The `.default` head-refusal's remedy list offered bare `int` — a head that very check
      refuses (`int` resolves to the bignum `Int` extern struct, not a rust primitive), so the
      advice was dead for the exact value kind (signed) most likely to send a reader to it. Caught
      by executing the advertised remedy at pickup, not by any gate; the corrected message (now
      naming `nint` and the signed-RANGE spelling) adopted the pattern day-one via
      `default_head_remedy_does_not_name_the_head_it_refuses`, whose generate leg emits the
      printed range remedy so the message cannot go stale against it.
"""

[[part.source_replacement]]
span_id = "span-part-part-default-head-refusal-s-remedy-list-offered-bare"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-emitted-name-guard-mint-records-unminted-extra"
parent_record_id = "testing.schema-document-s-name-injectivity-enforced-row-side"
title = "the emitted name guard)\". Mint the one cell that section records as unminted — an extra"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """  + the emitted name guard)\". Mint the one cell that section records as unminted — an extra
  `--json-schema-root` root on the LOSING side of a collision — if a consumer reports a collision they
  introduced through that flag. What remains — four holes, each needing its own mechanism:
"""

[[part.source_replacement]]
span_id = "span-part-part-emitted-name-guard-mint-records-unminted-extra"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-emitted-name-guard-neither-closes-holes-above-two"
parent_record_id = "testing.schema-document-s-name-injectivity-enforced-row-side"
title = "the emitted name guard)\" — and neither closes the holes above. Two bounds worth keeping in view."
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """  + the emitted name guard)\" — and neither closes the holes above. Two bounds worth keeping in view.
  The emitted check resolves the definitions map through the generator's
  `definitions_path` setting and SKIPS if that resolves to nothing, so a schemars default whose
  reference namespace stops matching the emitted document shape turns the check off rather than
  reddening every consumer build. It is silent IN A CONSUMER'S run, but not undetectable here: the
  emitter writes the `$defs` key literally while `run_test`'s own closure assertion matches
  `#/$defs/` literally too, so the same schemars change that trips the skip reddens our suite's
  assertion first, at local tier. What no layer covers is the window between an upstream release and
  our next local run — a vector pinning the emitted document against a bumped schemars is what would
  close that, and it is the same missing system as the dependency version-RANGE entry below. And
  closure is not injectivity: a document can resolve every reference and still publish one type's
  shape under another's name.

"""

[[part.source_replacement]]
span_id = "span-part-part-emitted-name-guard-neither-closes-holes-above-two"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-findings-couples-carriers-least-carrier-executes-s-headline"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "When a findings entry couples to carriers, at least one carrier executes the entry's HEADLINE"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - When a findings entry couples to carriers, at least one carrier executes the entry's HEADLINE
      claim — for a claims-the-API-does-X sentence that means calling X (a compile-fail probe is an
      execution too) — so the entry cannot drift from the code while its carriers stay green.

    Pickup re-probe remains the enforcement of last resort, not the plan. Ten proven instances,
    each caught only by executing the claim rather than reading it:
"""

[[part.source_replacement]]
span_id = "span-part-part-findings-couples-carriers-least-carrier-executes-s-headline"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-findings-s-description-claimed-shape-s-standalone-codec"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "A findings entry's DESCRIPTION claimed the shape's standalone codec \"writes/accepts a bare"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - A findings entry's DESCRIPTION claimed the shape's standalone codec \"writes/accepts a bare
      map\" when no standalone codec existed at all (`to_cbor_bytes` failed E0599 for the `PairMap`
      instantiation; `from_cbor_bytes` was absent), while both of the entry's coupled test carriers
      pinned an adjacent structural fact rather than executing the operative sentence.
"""

[[part.source_replacement]]
span_id = "span-part-part-findings-s-description-claimed-shape-s-standalone-codec"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-findings-s-recommended-remedy-c-kv-keyed-plain"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "A findings entry's recommended remedy (`{ c: [kv] }` for the keyed plain-group map member) was"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - A findings entry's recommended remedy (`{ c: [kv] }` for the keyed plain-group map member) was
      itself refused — as a conflicting representation, a different wall — caught by the pickup
      re-probe before the refusal message shipped naming it.
"""

[[part.source_replacement]]
span_id = "span-part-part-findings-s-recommended-remedy-c-kv-keyed-plain"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-freezes-livelock-repo-arithmetic-make-them-impossible-containment"
parent_record_id = "testing.tier-s-peak-memory-bounded-arithmetic-over-assumed"
title = "Freezes are a LIVELOCK, and no in-repo arithmetic can make them impossible — containment is an operator measure."
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """  - **Freezes are a LIVELOCK, and no in-repo arithmetic can make them impossible — containment is
    an operator measure.** The incident signature (hours at 100 % memory and swap, and the kernel
    log carrying NO oom-kill for the window) is the near-OOM state where reclaim keeps barely
    succeeding on file-backed pages, so the OOM killer never fires and the machine thrashes
    indefinitely. This narrows an earlier claim of this entry's family: a saturated machine is
    unobservable *by passive gates*, but kernel-enforced containment — an early-OOM daemon, or a
    cgroup memory cap with `memory.oom.group` around the tier — converts the freeze into a killed
    process inside a gate: attributable, loud, and over in seconds. It also covers the class the
    arithmetic structurally cannot: MemAvailable per batch is a snapshot, not a reservation, so
    concurrent sessions (and co-tenants like editors and language servers) can jointly overcommit
    a box whose every individual budget was correct. Machine-level, so it is an operator action
    recorded here rather than a gate to build.
"""

[[part.source_replacement]]
span_id = "span-part-part-freezes-livelock-repo-arithmetic-make-them-impossible-containment"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-generics-repeat-refusal-s-first-drafted-remedy-advertised"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "The generics-repeat refusal's first drafted remedy advertised `a<t> = <arm1> / <arm2>` — a"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - The generics-repeat refusal's first drafted remedy advertised `a<t> = <arm1> / <arm2>` — a
      choice-bodied generic definition, which is itself refused — and was caught BEFORE shipping
      by the day-one adoption of this item's pattern: the new refusal's test asserted every
      advertised remedy generates, and the assert went red on the drafted message (B3-021 cycle,
      2026-08-08). The instance is recorded because it is the pattern working as designed
      pre-ship — the first refusal family authored since this item whose remedy defect never
      reached a commit — which is the evidence the day-one half of the convention pays for
      itself, not only the sweep half.
"""

[[part.source_replacement]]
span_id = "span-part-part-generics-repeat-refusal-s-first-drafted-remedy-advertised"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-inline-group-choice-rejection-s-remedy-g-b"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "The inline group-choice rejection's remedy (\"`g = (a // b)`, then reference `g`\") named a"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - The inline group-choice rejection's remedy (\"`g = (a // b)`, then reference `g`\") named a
      spelling that PANICKED the pre-registration assert (now a refusal). That instance and the
      `@ignore` pair were fixed with the messages' pins in the same commits.
"""

[[part.source_replacement]]
span_id = "span-part-part-inline-group-choice-rejection-s-remedy-g-b"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-md-mdmap"
parent_record_id = "testing.duplicates-policy-residuals"
title = "md] / …`, `mdmap = {"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """   - (The per-role wasm-ABI/multifile grid rows for both flavors are delivered — the
     `rset`/`nerset`/`rseta`/`nerseta` and `pmap`/`nepmap`/`pmapa`/`nepmapa` `SHAPES` entries;
     inventory in `tests/README.md` § \"Per-rule duplicates policy (`@duplicates`) — test map\".
     The recursive-union-VALUED table this fixture exercises (`md = mdmap / [* md] / …`,
     `mdmap = { * text => md } ; @duplicates preserve`) carries no residual: re-probed 2026-08-01 at
     `0a0f969e`, its standalone holder generates at exit 0 and the emitted rust and wasm crates both
     `cargo check` clean under BOTH the default and `--preserve-encodings` profiles, and
     `table_preserve.mdmap` sits in `tests/decode_conformance/corpus_catalog.toml` with accept
     vectors and no `pinned_reason`. The recursion-in-the-DOMAIN sibling carries none either: the
     union-rooted ordering that used to abort now defers its keys-list mint to finalize, and both
     rootings hold a compile-and-wire floor in the `key_`/`ukey_`/`upres_` blocks of
     `tests/recursive-collection-ref/input.cddl`.)

"""

[[part.source_replacement]]
span_id = "span-part-part-md-mdmap"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-negative-premise-stands"
parent_record_id = "testing.tier-s-peak-memory-bounded-arithmetic-over-assumed"
title = "The negative premise stands watch."
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """  - **The negative premise stands watch.** A future spawn path outside `tool_cmd`, the runner's
    env exports and cargo config discovery would regress the bounds silently; the sampler's
    per-run peaks are what would show it (a peak far above the budget's arithmetic is the tell),
    which is another reason the reports must keep being produced even while nobody is debugging.
"""

[[part.source_replacement]]
span_id = "span-part-part-negative-premise-stands"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-nested-cargo-test"
parent_record_id = "testing.execution-binary-wrappers-true-last-documented-flag-value"
title = "nested `cargo test`"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    + nested `cargo test`
    (`no_annotate_reframed_emissions_round_trip_non_canonical_wire_byte_exactly`), added because a
    compile floor structurally cannot tell \"compiles\" from \"compiles and re-encodes the same
    bytes\". Walking the other documented flag values against that standard:
    `--annotate-fields=false` now has the semantic floor; `--canonical-form` (with preserve) has
    real execution (`tests/canonical` via `run_test`, the `golden_hex_canonical` KATs,
    `open_array_preserve_e2e`); `--to-from-bytes-methods=false` removes API surface, so its
    failure mode is compile-level and `flag_value_smoke`'s compile cell is the right depth. That
    leaves `--binary-wrappers=true`: it mints new rust types that OWN byte-string
    serialize/deserialize, its wrongness would be byte-level (a wrapper that re-encodes with a
    different width, drops a `bytes .size` bound check, or double-wraps), and its only gate is
    `flag_value_smoke`'s `cargo check`. One semantic cell in the delivered pattern — generate a
    byte-string-rich spec under the flag (default AND preserve rows; non-canonical byte-string
    heads under preserve) and round-trip hand-derived vectors byte-exactly — closes the gap.
    Scoped probe record for the claim above: \"compile-only\" was established by enumerating the
    `--binary-wrappers` passers in `src/tests/` (`flag_value_smoke`, `config_tests`) on the tree
    that delivered the annotate=false floors, not by grep vocabulary alone.

"""

[[part.source_replacement]]
span_id = "span-part-part-nested-cargo-test"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-non-recorded-isn-t-re-litigated-coverage-gap"
parent_record_id = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
title = "Non-item, recorded so it isn't re-litigated as a coverage gap:"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """  - *Non-item, recorded so it isn't re-litigated as a coverage gap:*
    `bounds_check_expr_non_negative`'s Array/Map leg and the member-side zero-min elision are
    defensive-only — every zero lower bound is dropped upstream by the owners above before
    emission, so the corner is unreachable end-to-end and a fixture cannot pin it.
"""

[[part.source_replacement]]
span_id = "span-part-part-non-recorded-isn-t-re-litigated-coverage-gap"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-pre-cache-remedies-stay-valid-if-uncached-path"
parent_record_id = "testing.gate-cache-costs"
title = "The pre-cache remedies stay valid if the UNCACHED path ever bites (a touch-everything change"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """  - The pre-cache remedies stay valid if the UNCACHED path ever bites (a touch-everything change
    pays full price): batch cells into fewer crates, or adopt `cargo-nextest` as the suite runner
    (`multifile_matrix_compiles` measured ~35 s cold / ~30 s warm at 43 cells; 144 at HEAD).
"""

[[part.source_replacement]]
span_id = "span-part-part-pre-cache-remedies-stay-valid-if-uncached-path"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-refusal-s-first-predicate-refused-exact-remedy-own"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "A refusal's first predicate refused the exact remedy its own message named"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - A refusal's first predicate refused the exact remedy its own message named
      (`{ * uint => [coords] }` — the registry keeps materialized plain groups, so the lookup
      over-matched).
"""

[[part.source_replacement]]
span_id = "span-part-part-refusal-s-first-predicate-refused-exact-remedy-own"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-remedy-pin-per-message-per-triggering-construct-class"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "A remedy pin is per message PER TRIGGERING CONSTRUCT CLASS, or the message must name the"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - A remedy pin is per message PER TRIGGERING CONSTRUCT CLASS, or the message must name the
      boundary itself. This is the half a generates-green vector alone cannot deliver: one message
      reachable from two construct classes can be sound for the class whose vector someone
      hand-picked and dead advice for the other.
"""

[[part.source_replacement]]
span_id = "span-part-part-remedy-pin-per-message-per-triggering-construct-class"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-remedy-string-executable-wherever-lives-emitted-message-gets"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "A remedy string is executable wherever it lives — in an emitted message it gets a pinned"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - A remedy string is executable wherever it lives — in an emitted message it gets a pinned
      generate leg, in a findings entry it gets executed at pickup.
"""

[[part.source_replacement]]
span_id = "span-part-part-remedy-string-executable-wherever-lives-emitted-message-gets"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-set-nominalization-residuals-delivery-set-architecture-rethink-d177516"
parent_record_id = "testing.duplicates-policy-residuals"
title = "Set-nominalization residuals (Delivery 2 of the set-architecture rethink — `d177516`/`816969f`/`13b63a2`/`7daca67`)."
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """   - **Set-nominalization residuals (Delivery 2 of the set-architecture rethink —
     `d177516`/`816969f`/`13b63a2`/`7daca67`).** Named, generic-instance, and inline `#6.258` sets
     are now nominal wrapper types owning their `{tag, len, elem}` encodings. Three follow-ups
     outlive the delivery:
"""

[[part.source_replacement]]
span_id = "span-part-part-set-nominalization-residuals-delivery-set-architecture-rethink-d177516"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-sighting-offline-gate-run-new-class"
parent_record_id = "testing.registry-fetch-transients-nested-cargo-cells"
title = "A sighting under an offline gate run is a NEW class"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """  - **A sighting under an offline gate run is a NEW class**, not this one — an offline cell
    cannot hit the registry; investigate it, don't re-ledger it here. Two manual tails are now
    known, distinguished by the error text: (1) `no matching package named <dep>` — a tests/
    fixture crate grew a dep outside the warm-up manifest; add it to `tests/warmup/Cargo.toml`.
    (2) `failed to download <dep> vX.Y.Z … --offline was specified` — a FRESH-resolving scratch
    crate (the oracle-fingerprint probe is one) wants a version newer than the gitignored
    `tests/warmup/Cargo.lock`, i.e. an upstream release landed since the lock was minted; the
    signature is a SUB-SECOND `HARNESS FAILURE` from the gate (the probe dies before building
    anything), and the remedy is a targeted `cargo update -p <dep> --manifest-path
    tests/warmup/Cargo.toml` (an `=`-pinned twin like `zerocopy-derive` moves via its parent),
    never `CHECK_ONLINE=1` as a habit. Observed 2026-08-06 (`zerocopy-derive 0.8.56`, first seen
    amid 4-way gate concurrency and initially misread as lock contention — the solo re-run is
    what separated the classes; per-checkout, so every checkout re-fixes on first contact). The
    systematic retirement of tail (2) is the work item \"The warm-up should REFRESH the
    dep-universe lock, not trust it\".
  Fail-fast discipline is unchanged and general: a failed run plus a one-gate retry is NOT a tier
  pass — fail-fast SKIPS every downstream gate, so a tier-level claim needs the tier re-run (the
  gate cache keeps already-passed cells cheap).
"""

[[part.source_replacement]]
span_id = "span-part-part-sighting-offline-gate-run-new-class"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-surfaces-still-online"
parent_record_id = "testing.registry-fetch-transients-nested-cargo-cells"
title = "Surfaces still online"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """  - **Surfaces still online**: standalone script/gate runs outside check.ts (a bare
    `bun run verify.ts`, an isolated `cargo test --bin cddl-codegen <gate>` confirm) — prefix
    `CARGO_NET_OFFLINE=true` there after any warm run; the warm-up fetch itself (retried,
    hard-stop before any gate if it can't fetch, `CHECK_ONLINE=1` to opt back into online runs);
    and the shared TS-toolchain install's FIRST run per manifest hash (`npm`, not cargo — an
    `npm install` under `/.ts-toolchain/` that needs the network once, then serves every
    TypeScript-side leg offline; unprobed against a fully-offline tier run).
"""

[[part.source_replacement]]
span_id = "span-part-part-surfaces-still-online"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-tagged-remedy-spelling-was-imprecise-coords-faithful-coords"
parent_record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
title = "A tagged remedy spelling was imprecise (`[#6.5(coords)]` for the faithful `#6.5([coords])`)"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - A tagged remedy spelling was imprecise (`[#6.5(coords)]` for the faithful `#6.5([coords])`)
      until a pin asserted the printed remedy generates.
"""

[[part.source_replacement]]
span_id = "span-part-part-tagged-remedy-spelling-was-imprecise-coords-faithful-coords"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-verify-ts-evidence-absorber-stays"
parent_record_id = "testing.registry-fetch-transients-nested-cargo-cells"
title = "verify.ts evidence absorber stays"
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """  - **verify.ts evidence absorber stays**: a transient null replay would flip a row's
    decode-foreign clause to \"FAILED\", which `verify_cache_transparency` reads as an A/B
    divergence — absorbed by decodeForeignProbe's regenerate-retry-once (the same retry the mint
    paths carry); the transparency gate is the standing detector if a null-replay class outruns
    the retry.
"""

[[part.source_replacement]]
span_id = "span-part-part-verify-ts-evidence-absorber-stays"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-what-hand-per-construct-buy-established-escape-produced"
parent_record_id = "testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
title = "What one hand cell per construct does not buy, established by the escape that produced this entry."
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - **What one hand cell per construct does not buy, established by the escape that produced this
      entry.** A rest row whose inner types lived in another file scope emitted two unresolved-name
      classes at once: the module's rest accessor returned a wrapper class minted at the crate ROOT
      and imported nowhere, and that root wrapper's own `insert`/`get`/`keys` named the inner types
      bare with no import there either. Every ingredient had a green fixture of its own — rest rows,
      cross-scope references, nested maps — and no cell crossed them, so the wasm crate did not
      compile while every gate stayed green.
"""

[[part.source_replacement]]
span_id = "span-part-part-what-hand-per-construct-buy-established-escape-produced"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[part]]
part_id = "part-why-parity-gate-cannot-host-generalization"
parent_record_id = "testing.wasm-face-s-door-vocabulary-hand-listed-mechanism"
title = "Why the parity gate cannot host the generalization."
render_authority = "semantic"
lifecycle_disposition = "parent_supporting_prose"
body_md = """    - **Why the parity gate cannot host the generalization.** `wasm_api_parity` exists to report
      \"a rust member with no wasm counterpart\", but its rust-side walk reads INHERENT impls only
      (`syn::Item::Impl(im) if im.trait_.is_none()`) and documents the exclusion of trait impls as a
      deliberate structural exemption. Every door member's rust-side home is outside that walk —
      three are trait methods on the generated runtime's `ToCBORBytes`/`Serialize`/`Deserialize`,
      three are backed by serde derives — so each contributes no rust-side row and none can be
      reported missing there. The exemption is right for `From`/`AsRef` and wrong for exactly this
      family, whose members exist ONLY as trait methods on the rust side and ONLY as inherent fns on
      the wasm side. Widening the walk to trait impls would drown the differential in the
      `From`/`AsRef` noise the exemption exists to remove; the generalization wants its own
      mechanism, keyed on the trait set the wasm face promises to mirror.
"""

[[part.source_replacement]]
span_id = "span-part-part-why-parity-gate-cannot-host-generalization"
replacement_field = "body_md"
review_note_md = """WP5T source-owner review: exact supporting-part bytes remain parent prose.
"""

[[manifest.entry]]
kind = "section"
section_id = "preamble"

[[manifest.entry]]
kind = "section"
section_id = "north-star"

[[manifest.entry]]
kind = "section"
section_id = "pending-maintainer"

[[manifest.entry]]
kind = "record"
record_id = "testing.prettyplease-shelling-rustfmt"

[[manifest.entry]]
kind = "record"
record_id = "testing.rule-trailing.directive-classification"

[[manifest.entry]]
kind = "section"
section_id = "next-priority"

[[manifest.entry]]
kind = "record"
record_id = "testing.grammar-fuzzer-escalations"

[[manifest.entry]]
kind = "record"
record_id = "testing.grammar-fuzzer.embed-site-alias-roots"

[[manifest.entry]]
kind = "record"
record_id = "testing.grammar-fuzzer.arbitrary-supported-ast"

[[manifest.entry]]
kind = "record"
record_id = "testing.grammar-fuzzer.batch-masking-detector"

[[manifest.entry]]
kind = "record"
record_id = "testing.grammar-fuzzer.real-world-corpus-differential"

[[manifest.entry]]
kind = "record"
record_id = "testing.duplicates-policy-residuals"

[[manifest.entry]]
kind = "part"
part_id = "part-set-nominalization-residuals-delivery-set-architecture-rethink-d177516"

[[manifest.entry]]
kind = "record"
record_id = "testing.duplicates.same-shape-set-conversion"

[[manifest.entry]]
kind = "record"
record_id = "testing.duplicates.occurrence-aware-generic-identity"

[[manifest.entry]]
kind = "record"
record_id = "testing.duplicates.dedicated-generic-collision-message"

[[manifest.entry]]
kind = "record"
record_id = "testing.duplicates.member-position-extension"

[[manifest.entry]]
kind = "part"
part_id = "part-md-mdmap"

[[manifest.entry]]
kind = "record"
record_id = "testing.lint-provocation-shapes-generated-code-clippy-clean-partially"

[[manifest.entry]]
kind = "record"
record_id = "testing.rustfmt-seam-error-leg-still-witness-needs-subprocess"

[[manifest.entry]]
kind = "record"
record_id = "testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"

[[manifest.entry]]
kind = "record"
record_id = "testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite"

[[manifest.entry]]
kind = "record"
record_id = "testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial"

[[manifest.entry]]
kind = "part"
part_id = "part-what-hand-per-construct-buy-established-escape-produced"

[[manifest.entry]]
kind = "part"
part_id = "part-cheap-system-matrix-shape-pairs-compiled-wasm-side"

[[manifest.entry]]
kind = "record"
record_id = "testing.container-combinations.reopening-signal"

[[manifest.entry]]
kind = "record"
record_id = "testing.generated-local-collision-class-refused-mangled-refusal-s"

[[manifest.entry]]
kind = "record"
record_id = "testing.generated-local-collision.general-mangling-remedy"

[[manifest.entry]]
kind = "record"
record_id = "testing.generated-local-collision.scope-wide-probe"

[[manifest.entry]]
kind = "record"
record_id = "testing.wasm-face-s-door-vocabulary-hand-listed-mechanism"

[[manifest.entry]]
kind = "part"
part_id = "part-why-parity-gate-cannot-host-generalization"

[[manifest.entry]]
kind = "record"
record_id = "testing.wasm-door-vocabulary.reopening-signal"

[[manifest.entry]]
kind = "record"
record_id = "testing.maintainer-ruling-force-convenience-cbor-bytes-door-turns"

[[manifest.entry]]
kind = "record"
record_id = "testing.build-registration-class-reference-position-sweep-second-escape"

[[manifest.entry]]
kind = "record"
record_id = "testing.fixture-shape-evicted-over-known-defect-stale-guard"

[[manifest.entry]]
kind = "record"
record_id = "testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"

[[manifest.entry]]
kind = "record"
record_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"

[[manifest.entry]]
kind = "part"
part_id = "part-remedy-string-executable-wherever-lives-emitted-message-gets"

[[manifest.entry]]
kind = "part"
part_id = "part-advice-shaped-spelling-message-prints-remedy-string-strings"

[[manifest.entry]]
kind = "part"
part_id = "part-remedy-pin-per-message-per-triggering-construct-class"

[[manifest.entry]]
kind = "part"
part_id = "part-findings-couples-carriers-least-carrier-executes-s-headline"

[[manifest.entry]]
kind = "part"
part_id = "part-refusal-s-first-predicate-refused-exact-remedy-own"

[[manifest.entry]]
kind = "part"
part_id = "part-tagged-remedy-spelling-was-imprecise-coords-faithful-coords"

[[manifest.entry]]
kind = "part"
part_id = "part-findings-s-recommended-remedy-c-kv-keyed-plain"

[[manifest.entry]]
kind = "part"
part_id = "part-findings-s-description-claimed-shape-s-standalone-codec"

[[manifest.entry]]
kind = "part"
part_id = "part-both-ignore-rejections-printed-k-v-ignore-valid"

[[manifest.entry]]
kind = "part"
part_id = "part-inline-group-choice-rejection-s-remedy-g-b"

[[manifest.entry]]
kind = "part"
part_id = "part-anonymous-group-rejection-s-give-name-using-name"

[[manifest.entry]]
kind = "part"
part_id = "part-default-head-refusal-s-remedy-list-offered-bare"

[[manifest.entry]]
kind = "part"
part_id = "part-generics-repeat-refusal-s-first-drafted-remedy-advertised"

[[manifest.entry]]
kind = "part"
part_id = "part-bare-fixed-table-value-refusal-continued-say-named"

[[manifest.entry]]
kind = "record"
record_id = "testing.directive-effect-round-trip-coherence-sweep-accepted-custom"

[[manifest.entry]]
kind = "record"
record_id = "testing.warm-up-refresh-dep-universe-lock-trust-fresh"

[[manifest.entry]]
kind = "record"
record_id = "testing.check-merged-d-ts-name-collisions-two-halves"

[[manifest.entry]]
kind = "record"
record_id = "testing.corpus-preserve-pair-map-key-value-self-encoding"

[[manifest.entry]]
kind = "record"
record_id = "testing.same-named-generated-crates-silently-share-cargo-fingerprint"

[[manifest.entry]]
kind = "record"
record_id = "testing.corpus-c-style-enum-data-type-choice-arm"

[[manifest.entry]]
kind = "record"
record_id = "testing.execution-binary-wrappers-true-last-documented-flag-value"

[[manifest.entry]]
kind = "part"
part_id = "part-nested-cargo-test"

[[manifest.entry]]
kind = "record"
record_id = "testing.synthetic-protocol-controls-component-probe-host-s-defensive"

[[manifest.entry]]
kind = "record"
record_id = "testing.lockstep-drift-gate-rust-ts-kebab-ident-mirror"

[[manifest.entry]]
kind = "record"
record_id = "testing.pin-toolchain-verify-ts-s-nested-cargo-scratch"

[[manifest.entry]]
kind = "record"
record_id = "testing.verify-ts-refuse-unknown-flag-silently-degrading-plain"

[[manifest.entry]]
kind = "section"
section_id = "standing-system"

[[manifest.entry]]
kind = "record"
record_id = "testing.gate-exists-registry-nowhere-tests-readme-md-hand"

[[manifest.entry]]
kind = "record"
record_id = "testing.lint-doc-citations-accepts-docs-heading-durable-citation"

[[manifest.entry]]
kind = "record"
record_id = "testing.representation-changing-directive-goes-live-new-container-ship"

[[manifest.entry]]
kind = "record"
record_id = "testing.emission-branch-generated-output-fixture-exercises-invisible-gate"

[[manifest.entry]]
kind = "record"
record_id = "testing.logic-tool-emits-source-text-outside-layer-reasons"

[[manifest.entry]]
kind = "record"
record_id = "testing.value-anchor-rot-field-addition-delivery-grows-existing"

[[manifest.entry]]
kind = "record"
record_id = "testing.corpus-wide-regen-over-prior-output-sweep-reaches"

[[manifest.entry]]
kind = "record"
record_id = "testing.stale-known-limitation-prose-surviving-fix-finding-ledgered"

[[manifest.entry]]
kind = "record"
record_id = "testing.finding-s-defect-side-attribution-recorded-narrative-plausibility"

[[manifest.entry]]
kind = "record"
record_id = "testing.gate-demands-premise-probed-against-gate-evidence-about"

[[manifest.entry]]
kind = "record"
record_id = "testing.ruling-premise-gate-x-stays-green-must-probed"

[[manifest.entry]]
kind = "record"
record_id = "testing.observed-baseline-comments-beside-gate-floors-rot-silently"

[[manifest.entry]]
kind = "record"
record_id = "testing.extreme-value-boundary-correctness-generation-time-encode-paths"

[[manifest.entry]]
kind = "record"
record_id = "testing.fixture-models-obsolete-user-contract-stays-green-real"

[[manifest.entry]]
kind = "record"
record_id = "testing.panic-site-un-shadowing-converting-shallow-panic-graceful"

[[manifest.entry]]
kind = "record"
record_id = "testing.three-defect-classes-process-test-see-sharing-remedy"

[[manifest.entry]]
kind = "record"
record_id = "testing.process-boundary.rustc-after-panic-fix"

[[manifest.entry]]
kind = "record"
record_id = "testing.process-boundary.rustfmt-pre-rustc"

[[manifest.entry]]
kind = "record"
record_id = "testing.process-boundary.another-profile-escape"

[[manifest.entry]]
kind = "record"
record_id = "testing.parallel-constructor-fixture-diversity-parser-over-external-input"

[[manifest.entry]]
kind = "record"
record_id = "testing.synthesized-name-referenced-never-minted-e0425-flavor"

[[manifest.entry]]
kind = "record"
record_id = "testing.multifile-reference-position-coverage-two-position-keyed-escapes"

[[manifest.entry]]
kind = "record"
record_id = "testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near"

[[manifest.entry]]
kind = "record"
record_id = "testing.armed-idle-harness-arms-empty-head-ledgers-zero"

[[manifest.entry]]
kind = "record"
record_id = "testing.doubled-doc-comment-markers-emitted-docs-proven-instance"

[[manifest.entry]]
kind = "record"
record_id = "testing.emitted-shape-lint-classes-outside-clippy-all-beyond"

[[manifest.entry]]
kind = "record"
record_id = "testing.unused-imports-generated-crates-trait-import-class-name"

[[manifest.entry]]
kind = "record"
record_id = "testing.mechanical-layers-two-review-owned-design-rules-tests"

[[manifest.entry]]
kind = "record"
record_id = "testing.add-profile-wrapper-participation-grid-participation-differs-across"

[[manifest.entry]]
kind = "record"
record_id = "testing.extern-deps-wasm-boundary-surface-packaging-json-gen"

[[manifest.entry]]
kind = "record"
record_id = "testing.generic-raw-bytes-base-refused-generic-extern-base"

[[manifest.entry]]
kind = "record"
record_id = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"

[[manifest.entry]]
kind = "record"
record_id = "testing.twin-implementation.read-caught-instance-trigger"

[[manifest.entry]]
kind = "record"
record_id = "testing.twin-implementation.comment-ast-change-cadence"

[[manifest.entry]]
kind = "record"
record_id = "testing.twin-implementation.own-reviewed-change-rule"

[[manifest.entry]]
kind = "part"
part_id = "part-non-recorded-isn-t-re-litigated-coverage-gap"

[[manifest.entry]]
kind = "record"
record_id = "testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count"

[[manifest.entry]]
kind = "record"
record_id = "testing.run-local-values-gate-cache-key-material-proven"

[[manifest.entry]]
kind = "record"
record_id = "testing.control-bytes-committed-source-files-proven-instance-machinery"

[[manifest.entry]]
kind = "record"
record_id = "testing.gate-script-health-cache-staleness-key-fresh-worktree"

[[manifest.entry]]
kind = "record"
record_id = "testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently"

[[manifest.entry]]
kind = "record"
record_id = "testing.dependency-swap-pass-compile-parity-existing-gate-silently"

[[manifest.entry]]
kind = "record"
record_id = "testing.fixture-s-verdict-accident-cargo-feature-unification-harness"

[[manifest.entry]]
kind = "record"
record_id = "testing.profile-driven-compile-gate-covers-compositions-profiles-specs"

[[manifest.entry]]
kind = "record"
record_id = "testing.alloc-imports-nested-inline-modules-hand-carried-design"

[[manifest.entry]]
kind = "record"
record_id = "testing.gate-cache-closure-audit-structurally-blind-gate-authored"

[[manifest.entry]]
kind = "record"
record_id = "testing.comment-residue-false-matches-text-scans-over-emitted"

[[manifest.entry]]
kind = "record"
record_id = "testing.directive-dependent-emitter-edge-case-triggering-shape-hand"

[[manifest.entry]]
kind = "record"
record_id = "testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored"

[[manifest.entry]]
kind = "record"
record_id = "testing.extend-recombination-member-kind-table-optional-fixed-shape"

[[manifest.entry]]
kind = "record"
record_id = "testing.referencing-context-measured-wrapping-deep-honored-base-shape"

[[manifest.entry]]
kind = "record"
record_id = "testing.arm-position-s-classifier-compile-time-forcing-function"

[[manifest.entry]]
kind = "record"
record_id = "testing.schema-document-s-name-injectivity-enforced-row-side"

[[manifest.entry]]
kind = "part"
part_id = "part-emitted-name-guard-mint-records-unminted-extra"

[[manifest.entry]]
kind = "record"
record_id = "testing.schema-name.collision-loser-no-row-same-id"

[[manifest.entry]]
kind = "record"
record_id = "testing.schema-name.cross-crate-same-id-collision"

[[manifest.entry]]
kind = "record"
record_id = "testing.schema-name.percent-encoded-ref"

[[manifest.entry]]
kind = "record"
record_id = "testing.schema-name.two-rowless-types-collide"

[[manifest.entry]]
kind = "part"
part_id = "part-emitted-name-guard-neither-closes-holes-above-two"

[[manifest.entry]]
kind = "record"
record_id = "testing.directive-s-documented-sentence-stay-true-consumer-visible"

[[manifest.entry]]
kind = "record"
record_id = "testing.citation-resolves-naming-wrong-thing-invisible-citation-lint"

[[manifest.entry]]
kind = "record"
record_id = "testing.matrix-s-note-names-panic-site-aborts-nothing"

[[manifest.entry]]
kind = "record"
record_id = "testing.nothing-asserts-how-many-times-rejection-message-emitted"

[[manifest.entry]]
kind = "record"
record_id = "testing.containment-note-s-stated-reason-go-false-any"

[[manifest.entry]]
kind = "record"
record_id = "testing.emit-tests-gate-asserts-round-trip-green-cannot"

[[manifest.entry]]
kind = "record"
record_id = "testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible"

[[manifest.entry]]
kind = "record"
record_id = "testing.cargo-feature-generated-crate-s-dependency-change-what"

[[manifest.entry]]
kind = "record"
record_id = "testing.documented-flag-pairing-broken-fixture-stays-green-because"

[[manifest.entry]]
kind = "record"
record_id = "testing.documented-failure-shape-exercises-prose-cross-crate-int"

[[manifest.entry]]
kind = "record"
record_id = "testing.dsl-position-tests-string-level-design-green-emitted"

[[manifest.entry]]
kind = "record"
record_id = "testing.nothing-asserts-two-ir-sites-cannot-mint-same"

[[manifest.entry]]
kind = "record"
record_id = "testing.message-s-level-classification-review-owned-two-ways"

[[manifest.entry]]
kind = "record"
record_id = "testing.wac-composed-component-transpiled-jco-loses-cross-instance"

[[manifest.entry]]
kind = "record"
record_id = "testing.make-fixture-comment-s-claimed-wire-bytes-executable"

[[manifest.entry]]
kind = "section"
section_id = "deferred-features"

[[manifest.entry]]
kind = "record"
record_id = "testing.workspace-mode-wasm32-wasip2-build-gate-both-faces"

[[manifest.entry]]
kind = "record"
record_id = "testing.workspace-wasip2.reopening-signal"

[[manifest.entry]]
kind = "record"
record_id = "testing.probe-component-face-s-js-surface-axes-js"

[[manifest.entry]]
kind = "record"
record_id = "testing.component-js-probe.reopening-signal"

[[manifest.entry]]
kind = "record"
record_id = "testing.crate-generates-json-schema-document-aggregate-package"

[[manifest.entry]]
kind = "record"
record_id = "testing.wasm32-link-floor-deferred-reject-twin"

[[manifest.entry]]
kind = "record"
record_id = "testing.extern-interface-export-dialect-v2-candidates"

[[manifest.entry]]
kind = "record"
record_id = "testing.cddl-module-directives-draft-ietf-cbor-cddl-modules"

[[manifest.entry]]
kind = "record"
record_id = "testing.std-generated-crates"

[[manifest.entry]]
kind = "record"
record_id = "testing.dependency-version-range-resolution-untested-generated-crates-float"

[[manifest.entry]]
kind = "record"
record_id = "testing.transparent-tag-set-idiom-recognized-shape-boundary-request"

[[manifest.entry]]
kind = "record"
record_id = "testing.tag-set.non-idiom-generic-boundary"

[[manifest.entry]]
kind = "record"
record_id = "testing.tag-set.inline-anonymous-choice-boundary"

[[manifest.entry]]
kind = "record"
record_id = "testing.opt-rfc-faithful-total-cbor-json-rendering-json"

[[manifest.entry]]
kind = "record"
record_id = "testing.edn-text-notation-round-trip-edn-edn-draft"

[[manifest.entry]]
kind = "record"
record_id = "testing.project-json-gen-documents-run-test-fixture-writes"

[[manifest.entry]]
kind = "record"
record_id = "testing.propagating-deliberately-unpublished-type-s-intent-json-ts"

[[manifest.entry]]
kind = "record"
record_id = "testing.json2ts-artifact-doc-comments-naming-undefined"

[[manifest.entry]]
kind = "record"
record_id = "testing.file-listing-json-schema-root-values-flag-per"

[[manifest.entry]]
kind = "record"
record_id = "testing.assert-hand-authored-json-schema-against-type-s"

[[manifest.entry]]
kind = "record"
record_id = "testing.composing-registration-rows-across-generation-passes-share-output"

[[manifest.entry]]
kind = "record"
record_id = "testing.machine-readable-ownership-co-owned-features-std-list"

[[manifest.entry]]
kind = "section"
section_id = "operational-watches"

[[manifest.entry]]
kind = "record"
record_id = "testing.gate-s-own-stdout-print-column-result-pass"

[[manifest.entry]]
kind = "record"
record_id = "testing.flag-cddl-codegen-invocation-wasm-run-wasm-flag"

[[manifest.entry]]
kind = "record"
record_id = "testing.failing-snapshot-test-surfaces-mismatch-per-test-run"

[[manifest.entry]]
kind = "record"
record_id = "testing.e0463-t-find-crate-core-scratch-tree-std"

[[manifest.entry]]
kind = "record"
record_id = "testing.working-tree-gate-run-concurrent-live-implementation-agent"

[[manifest.entry]]
kind = "record"
record_id = "testing.stale-pin-guard-cannot-distinguish-gap-closed-vector"

[[manifest.entry]]
kind = "record"
record_id = "testing.migration-handoff-s-complete-list-found-survey-negative"

[[manifest.entry]]
kind = "record"
record_id = "testing.shared-cargo-target-dir-across-same-named-scratch"

[[manifest.entry]]
kind = "record"
record_id = "testing.config-run-s-convergence-warning-name-crate-already"

[[manifest.entry]]
kind = "record"
record_id = "testing.extern-interface-export-public-interchange-format"

[[manifest.entry]]
kind = "record"
record_id = "testing.exclusion-reasons-informational-interchange"

[[manifest.entry]]
kind = "record"
record_id = "testing.nested-cargo-scratch-retention-tmp-cddl-leaks-until"

[[manifest.entry]]
kind = "record"
record_id = "testing.scratch-retention.reopening-signal"

[[manifest.entry]]
kind = "record"
record_id = "testing.tier-s-peak-memory-bounded-arithmetic-over-assumed"

[[manifest.entry]]
kind = "record"
record_id = "testing.tier-memory.spend-measurements"

[[manifest.entry]]
kind = "part"
part_id = "part-freezes-livelock-repo-arithmetic-make-them-impossible-containment"

[[manifest.entry]]
kind = "part"
part_id = "part-negative-premise-stands"

[[manifest.entry]]
kind = "record"
record_id = "testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"

[[manifest.entry]]
kind = "record"
record_id = "testing.gate-cache-costs"

[[manifest.entry]]
kind = "record"
record_id = "testing.gate-cache.all-hit-cost"

[[manifest.entry]]
kind = "record"
record_id = "testing.gate-cache.coverage-extensions"

[[manifest.entry]]
kind = "record"
record_id = "testing.gate-cache.closure-audit-traced-set-extension"

[[manifest.entry]]
kind = "part"
part_id = "part-pre-cache-remedies-stay-valid-if-uncached-path"

[[manifest.entry]]
kind = "record"
record_id = "testing.registry-fetch-transients-nested-cargo-cells"

[[manifest.entry]]
kind = "part"
part_id = "part-surfaces-still-online"

[[manifest.entry]]
kind = "part"
part_id = "part-verify-ts-evidence-absorber-stays"

[[manifest.entry]]
kind = "part"
part_id = "part-sighting-offline-gate-run-new-class"

[[manifest.entry]]
kind = "record"
record_id = "testing.verify-cache-transparency-b-split-emission-profile-embed"

[[manifest.entry]]
kind = "record"
record_id = "testing.full-suite-flake-attributed-hardened-acquire-scratch-lock"

[[manifest.entry]]
kind = "record"
record_id = "testing.verify-ts-warm-up-cargo-test-exit-attributed"

[[manifest.entry]]
kind = "record"
record_id = "testing.backgrounded-check-ts-full-launched-sub-agent-s"

[[manifest.entry]]
kind = "section"
section_id = "declined-boundaries"

[[manifest.entry]]
kind = "record"
record_id = "testing.complete-cargo-mutants-sweep-will-never-run"

[[manifest.entry]]
kind = "record"
record_id = "testing.making-type-choice-path-reject-duplicated-explicit-name"

[[manifest.entry]]
kind = "record"
record_id = "testing.msrv-declaration-os-matrix-generated-code"

[[manifest.entry]]
kind = "record"
record_id = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"

[[manifest.entry]]
kind = "record"
record_id = "testing.full-n-flag-powerset-pict-pairwise"

[[manifest.entry]]
kind = "record"
record_id = "testing.quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"

[[manifest.entry]]
kind = "record"
record_id = "testing.orphan-fixture-directory-meta-test"

[[manifest.entry]]
kind = "record"
record_id = "testing.restructuring-features-std-tool-owned-sub-feature-cddl"

[[manifest.entry]]
kind = "record"
record_id = "testing.ownership-journal-making-dropped-std-forward-dep-converge"

[[manifest.entry]]
kind = "record"
record_id = "testing.wrapper-name-user-set-name-synthesized-collection-wrapper"

[[manifest.entry]]
kind = "record"
record_id = "testing.checking-runtime-flavor-s-safety-condition-rather-stating"

[[manifest.entry]]
kind = "section"
section_id = "sources"

[[manifest.entry]]
kind = "fragment"
fragment_id = "sources-exhaustive-menu"

[[manifest.entry]]
kind = "fragment"
fragment_id = "sources-expert-writeups"

[[source_span]]
id = "span-section-preamble"
start_byte = 0
end_byte = 1437
sha256 = "6bc0e74325f09b97cadf2600a9bb51638f4f204ef176db73b8ba11aa1f355d04"
source_kind = "section"
owner_id = "preamble"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-section-north-star"
start_byte = 1437
end_byte = 5432
sha256 = "02bab865d87ff0b330c6008ef45033389eb40146e7545eeb436879276f5b10e7"
source_kind = "section"
owner_id = "north-star"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-section-pending-maintainer"
start_byte = 5432
end_byte = 5462
sha256 = "bc57a21c68138cd2c7c868d5720fc66d93a287f08e89e44485084ab06116e2c1"
source_kind = "section"
owner_id = "pending-maintainer"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-prettyplease-shelling-rustfmt"
start_byte = 5462
end_byte = 8737
sha256 = "8b27d25cc4fed32e03619bec55782b262e44c9b1feeb5c095c556cca864b8346"
source_kind = "record"
owner_id = "testing.prettyplease-shelling-rustfmt"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-rule-trailing-directive-classification"
start_byte = 8737
end_byte = 11601
sha256 = "c5a5b506dba80f59781f9024767bd7b6bd14d191981f1923553d12ad65b8d338"
source_kind = "record"
owner_id = "testing.rule-trailing.directive-classification"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-section-next-priority"
start_byte = 11601
end_byte = 11640
sha256 = "bfc0333406c6bea0f6c671cc35320581b8e8a924de72b6d0b857c67a8bf680e9"
source_kind = "section"
owner_id = "next-priority"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-grammar-fuzzer-escalations"
start_byte = 11640
end_byte = 12293
sha256 = "288cc737cf71f535702a8a2e956520cf4b8f599d5e5c9314eb902d985316102c"
source_kind = "record"
owner_id = "testing.grammar-fuzzer-escalations"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-embed-site-leg-alias-classifying-roots-two-proven"
start_byte = 12293
end_byte = 15849
sha256 = "923e7273f55227211dd2cfb5039076588599ff1e2e6707b8d30c55c61ad5619d"
source_kind = "record"
owner_id = "testing.grammar-fuzzer.embed-site-alias-roots"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-arbitrary-derived-supported-cddl-ast-generation"
start_byte = 15849
end_byte = 16133
sha256 = "d92a00bf426ea4519651147408b04be090766376dc81cf76869e99fbf1c07a1e"
source_kind = "record"
owner_id = "testing.grammar-fuzzer.arbitrary-supported-ast"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-batch-masking-detector-layer-sweeps"
start_byte = 16133
end_byte = 17708
sha256 = "7e8b960ff9e2d9fb0ac2c9fa8ae81115b2db601332851b7dcb426e94910d82df"
source_kind = "record"
owner_id = "testing.grammar-fuzzer.batch-masking-detector"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-real-world-corpus-differential"
start_byte = 17708
end_byte = 17889
sha256 = "e441a49c2014db21e0f80c866e5f2aa7da5854bd4863f07763abc61914424765"
source_kind = "record"
owner_id = "testing.grammar-fuzzer.real-world-corpus-differential"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-duplicates-policy-residuals"
start_byte = 17889
end_byte = 18533
sha256 = "d7d28f11acdab64faf6d3056569e6a718a0152d4af9a2bd0517013e15bce3515"
source_kind = "record"
owner_id = "testing.duplicates-policy-residuals"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-set-nominalization-residuals-delivery-set-architecture-rethink-d177516"
start_byte = 18533
end_byte = 18838
sha256 = "3bb55b7ca34fb7cca1ddba51df75c78ee5be4d4d4cf23cf93106c21c83559aeb"
source_kind = "part"
owner_id = "part-set-nominalization-residuals-delivery-set-architecture-rethink-d177516"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-same-shape-set-instantiations"
start_byte = 18838
end_byte = 19767
sha256 = "540748bab9ec59a3e6698a9a707df0760aa631a5b0c516a9d11d72d2ea5505c1"
source_kind = "record"
owner_id = "testing.duplicates.same-shape-set-conversion"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-occurrence-aware-generic-instance-identity"
start_byte = 19767
end_byte = 20665
sha256 = "e89c124f0598abfe8b6333d200de3e56f2bc99caed815e5b5b43ef0e8f3c5dfb"
source_kind = "record"
owner_id = "testing.duplicates.occurrence-aware-generic-identity"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-dedicated-collision-message-generic-instantiation-naming"
start_byte = 20665
end_byte = 21425
sha256 = "1b7dfb391f60ecdca17d732e49c2ea4910b0990b9a481f0345a0e5e157f36913"
source_kind = "record"
owner_id = "testing.duplicates.dedicated-generic-collision-message"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-member-position-duplicates-extension"
start_byte = 21425
end_byte = 22490
sha256 = "efee82a3d83a4da7ff9a17d515ee47e3f508d6b6e799d76b218d61173adc2e5a"
source_kind = "record"
owner_id = "testing.duplicates.member-position-extension"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-md-mdmap"
start_byte = 22490
end_byte = 23580
sha256 = "700838f12c5630132393e7eb2e590f782edddf5795df8c56675e381ab54eb33a"
source_kind = "part"
owner_id = "part-md-mdmap"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-lint-provocation-shapes-generated-code-clippy-clean-partially"
start_byte = 23580
end_byte = 26583
sha256 = "fd823b14610bdffd121aa00048236c1e98b70d8a458871af4023943f38ac8655"
source_kind = "record"
owner_id = "testing.lint-provocation-shapes-generated-code-clippy-clean-partially"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-rustfmt-seam-error-leg-still-witness-needs-subprocess"
start_byte = 26583
end_byte = 28363
sha256 = "b0339fdea4c7be113faad1714cca07a9913e831b9632fa2393daa38980e5cda2"
source_kind = "record"
owner_id = "testing.rustfmt-seam-error-leg-still-witness-needs-subprocess"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-positional-diversity-fold-preserve-fixture-corpus-authoring-gives"
start_byte = 28363
end_byte = 30313
sha256 = "a15ea13a7ae2576ec3f2de054a0bc45dc8a79a5bd876b2185ab166f0c0dafc43"
source_kind = "record"
owner_id = "testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-cross-version-preserve-vectors-beyond-std-alloc-rewrite"
start_byte = 30313
end_byte = 31951
sha256 = "5331d49593a4f55b3b4632e807893d08df97437ad87476e67a456b8d1637d306"
source_kind = "record"
owner_id = "testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-container-construct-conceptual-type-visitor-walks-flat-combinatorial"
start_byte = 31951
end_byte = 32923
sha256 = "087255c9d5207a6142c02f17b87913ecd5cf882e2e9032806a55b220f19fbdb8"
source_kind = "record"
owner_id = "testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-what-hand-per-construct-buy-established-escape-produced"
start_byte = 32923
end_byte = 33569
sha256 = "90b003dd251df37aa95fcd57704c5da83bb74877a8da86a74e3f793790956616"
source_kind = "part"
owner_id = "part-what-hand-per-construct-buy-established-escape-produced"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-cheap-system-matrix-shape-pairs-compiled-wasm-side"
start_byte = 33569
end_byte = 34086
sha256 = "a1cf3adc6947ad78b9e678072ad1b6a449bf72c438d000b5e465a9180357ad71"
source_kind = "part"
owner_id = "part-cheap-system-matrix-shape-pairs-compiled-wasm-side"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-reopening-signal"
start_byte = 34086
end_byte = 34578
sha256 = "3ffbfc6d4f3d77fcd2653c2921ccd442bc91352f62986cebe9a70b5a4bc5c02e"
source_kind = "record"
owner_id = "testing.container-combinations.reopening-signal"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-generated-local-collision-class-refused-mangled-refusal-s"
start_byte = 34578
end_byte = 35534
sha256 = "d9f4f4602e52cf3df5a1d2420f16d94f42149663792dc063a183559e0508b34d"
source_kind = "record"
owner_id = "testing.generated-local-collision-class-refused-mangled-refusal-s"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-mangling-still-general-fix"
start_byte = 35534
end_byte = 36135
sha256 = "41afd130fc0a9464cc056defa3df104a93db7529f3b4e9fd2a8fc1744659ba80"
source_kind = "record"
owner_id = "testing.generated-local-collision.general-mangling-remedy"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-scope-wide-probe"
start_byte = 36135
end_byte = 36972
sha256 = "5a720ac40b096296e34298ef4a3bdd3eb3df6870fe31fa295183c23958850137"
source_kind = "record"
owner_id = "testing.generated-local-collision.scope-wide-probe"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-wasm-face-s-door-vocabulary-hand-listed-mechanism"
start_byte = 36972
end_byte = 37573
sha256 = "702b3cd7440bb71fc40566e391f31f356d6ab8e264613da864e8e38d658d7ab8"
source_kind = "record"
owner_id = "testing.wasm-face-s-door-vocabulary-hand-listed-mechanism"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-why-parity-gate-cannot-host-generalization"
start_byte = 37573
end_byte = 38618
sha256 = "7f6861fb53d65155fe2bd71399a80fe56f801ef1730aec04d4506a5e67cf55d7"
source_kind = "part"
owner_id = "part-why-parity-gate-cannot-host-generalization"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-reopening-signal-wasm"
start_byte = 38618
end_byte = 38982
sha256 = "cc8de3d6eccba9b86a19ad2aeca8f7aab2c661d3bbdf964bb1cd38d42e9be6ea"
source_kind = "record"
owner_id = "testing.wasm-door-vocabulary.reopening-signal"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-maintainer-ruling-force-convenience-cbor-bytes-door-turns"
start_byte = 38982
end_byte = 40424
sha256 = "baf556983d62bd1e58eb7eef4fd739f31029e2a44f55cc0f91b24edabbf8dc85"
source_kind = "record"
owner_id = "testing.maintainer-ruling-force-convenience-cbor-bytes-door-turns"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-build-registration-class-reference-position-sweep-second-escape"
start_byte = 40424
end_byte = 41586
sha256 = "b05baa432f9b0b5c05c9c146e4cd1b6a15e71b49583b336b1d93df79c6e03500"
source_kind = "record"
owner_id = "testing.build-registration-class-reference-position-sweep-second-escape"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-fixture-shape-evicted-over-known-defect-stale-guard"
start_byte = 41586
end_byte = 42781
sha256 = "4e6af38c609683d73aa052091271cda45b09368732c3a5dcf274eff19065eee2"
source_kind = "record"
owner_id = "testing.fixture-shape-evicted-over-known-defect-stale-guard"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-minted-identifier-spellability-floor-identifier-generator-emits-verbatim"
start_byte = 42781
end_byte = 46119
sha256 = "1321fde85d70c141bc9bab3ce8fc547d6a48e2ecbd3dc8c0d3fafc45b6d2b165"
source_kind = "record"
owner_id = "testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-rejection-message-s-remedy-string-executable-claim-pin"
start_byte = 46119
end_byte = 47091
sha256 = "046ecbb7dc5fa6c1cbffd41225e65e37a8052bf380804123431d5f223ae0c6f7"
source_kind = "record"
owner_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-remedy-string-executable-wherever-lives-emitted-message-gets"
start_byte = 47091
end_byte = 47256
sha256 = "b4ed1a695e1272ffca395f705c45f590abb7df4660ea85ce9a37db0da701f2c1"
source_kind = "part"
owner_id = "part-remedy-string-executable-wherever-lives-emitted-message-gets"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-advice-shaped-spelling-message-prints-remedy-string-strings"
start_byte = 47256
end_byte = 47464
sha256 = "173d553f198ba9900269cb9ecbb9b6665500c74d8c1eca40e84c69cb8ea7b557"
source_kind = "part"
owner_id = "part-advice-shaped-spelling-message-prints-remedy-string-strings"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-remedy-pin-per-message-per-triggering-construct-class"
start_byte = 47464
end_byte = 47798
sha256 = "7568c321fb9433b5d68912b4c300f64f0a45bb37ac7fa26519b4d3c8989015f0"
source_kind = "part"
owner_id = "part-remedy-pin-per-message-per-triggering-construct-class"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-findings-couples-carriers-least-carrier-executes-s-headline"
start_byte = 47798
end_byte = 48261
sha256 = "5dea5062b1c41848b872d1acd5530242fcefdcfa3757dd1b5114840db0dbb8f2"
source_kind = "part"
owner_id = "part-findings-couples-carriers-least-carrier-executes-s-headline"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-refusal-s-first-predicate-refused-exact-remedy-own"
start_byte = 48261
end_byte = 48459
sha256 = "e72b4085a5f7b666755401f92558abc32acdedaf30b4ee67051186ac9766968e"
source_kind = "part"
owner_id = "part-refusal-s-first-predicate-refused-exact-remedy-own"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-tagged-remedy-spelling-was-imprecise-coords-faithful-coords"
start_byte = 48459
end_byte = 48614
sha256 = "63dd558da0030f42e3bb10c67a859ae16f7e36c07cd86db83a82a7480b072c74"
source_kind = "part"
owner_id = "part-tagged-remedy-spelling-was-imprecise-coords-faithful-coords"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-findings-s-recommended-remedy-c-kv-keyed-plain"
start_byte = 48614
end_byte = 48876
sha256 = "7e86d70b42adad4d4aeeccd68dc3e47214ab9a1c56cdf1cd2a4cceb076dbc4cb"
source_kind = "part"
owner_id = "part-findings-s-recommended-remedy-c-kv-keyed-plain"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-findings-s-description-claimed-shape-s-standalone-codec"
start_byte = 48876
end_byte = 49259
sha256 = "0e14bc3f72907988038a4a4112547589b9157827752908f89e0ec250cb08dfc2"
source_kind = "part"
owner_id = "part-findings-s-description-claimed-shape-s-standalone-codec"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-both-ignore-rejections-printed-k-v-ignore-valid"
start_byte = 49259
end_byte = 49625
sha256 = "3a9b05c8674ccc84fde48a2c47987bc398f1450fa5250a8207fe3eb996b748c8"
source_kind = "part"
owner_id = "part-both-ignore-rejections-printed-k-v-ignore-valid"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-inline-group-choice-rejection-s-remedy-g-b"
start_byte = 49625
end_byte = 49894
sha256 = "c026b70a25bc8474c26bb9cd0dd00f72c04a7eeecb88692488755d59512caa3d"
source_kind = "part"
owner_id = "part-inline-group-choice-rejection-s-remedy-g-b"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-anonymous-group-rejection-s-give-name-using-name"
start_byte = 49894
end_byte = 50499
sha256 = "7ab7259676efad49a11d80255a69f28784142c3cab32fada474dc7166c778493"
source_kind = "part"
owner_id = "part-anonymous-group-rejection-s-give-name-using-name"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-default-head-refusal-s-remedy-list-offered-bare"
start_byte = 50499
end_byte = 51128
sha256 = "f8f5ac258c19f202ba265f544f98dd62877abf115a1e94211d19eb5432395a56"
source_kind = "part"
owner_id = "part-default-head-refusal-s-remedy-list-offered-bare"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-generics-repeat-refusal-s-first-drafted-remedy-advertised"
start_byte = 51128
end_byte = 51832
sha256 = "daf85d493ded8151472515c090e6a42a2c27e3cba0e53f7314f3ff1f29460f75"
source_kind = "part"
owner_id = "part-generics-repeat-refusal-s-first-drafted-remedy-advertised"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-bare-fixed-table-value-refusal-continued-say-named"
start_byte = 51832
end_byte = 52187
sha256 = "e676c2988f2eadcde8c1b9c100814b4755d82dbac2cc8d5a9ced7352956c080a"
source_kind = "part"
owner_id = "part-bare-fixed-table-value-refusal-continued-say-named"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-directive-effect-round-trip-coherence-sweep-accepted-custom"
start_byte = 52187
end_byte = 55491
sha256 = "20707462852d8689ad8b15d279ce71d1e953b7af787e7aca388b0c4d4f53e20b"
source_kind = "record"
owner_id = "testing.directive-effect-round-trip-coherence-sweep-accepted-custom"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-warm-up-refresh-dep-universe-lock-trust-fresh"
start_byte = 55491
end_byte = 57584
sha256 = "bd8d02a256a194e5590479f7dbd67407600613fd348ccf5bd901d146bcbab001"
source_kind = "record"
owner_id = "testing.warm-up-refresh-dep-universe-lock-trust-fresh"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-check-merged-d-ts-name-collisions-two-halves"
start_byte = 57584
end_byte = 59062
sha256 = "aa2ad404d39b10c04c06f2471c9f15ec51d969c9fbb18de118b4e017e2d5d87e"
source_kind = "record"
owner_id = "testing.check-merged-d-ts-name-collisions-two-halves"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-corpus-preserve-pair-map-key-value-self-encoding"
start_byte = 59062
end_byte = 60630
sha256 = "f10349d97be02e467183f4d75e99051f28520d3d56467926501289002e406f69"
source_kind = "record"
owner_id = "testing.corpus-preserve-pair-map-key-value-self-encoding"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-same-named-generated-crates-silently-share-cargo-fingerprint"
start_byte = 60630
end_byte = 64166
sha256 = "93af4b06f52c5e7bd688a7c26380131ee114067322851fe339b56c285e04857a"
source_kind = "record"
owner_id = "testing.same-named-generated-crates-silently-share-cargo-fingerprint"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-corpus-c-style-enum-data-type-choice-arm"
start_byte = 64166
end_byte = 66402
sha256 = "2adc0455d2335ca4a8a8782fda756d6384d44e28c2d66d0a861fb11de9cd7e4c"
source_kind = "record"
owner_id = "testing.corpus-c-style-enum-data-type-choice-arm"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-execution-binary-wrappers-true-last-documented-flag-value"
start_byte = 66402
end_byte = 66785
sha256 = "be7763ede1423e3db4595fda52542c42d53e90b0745947bd7c4697bf89c1d6ec"
source_kind = "record"
owner_id = "testing.execution-binary-wrappers-true-last-documented-flag-value"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-nested-cargo-test"
start_byte = 66785
end_byte = 68281
sha256 = "f8aaf1484f09f26b5695be5ffee8a098d7944add293db66f121bcf1c9541d025"
source_kind = "part"
owner_id = "part-nested-cargo-test"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-synthetic-protocol-controls-component-probe-host-s-defensive"
start_byte = 68281
end_byte = 69673
sha256 = "50cd23689d69d56c55d2a8688fae9f3b7167535a9a2f2608228304c07d70a539"
source_kind = "record"
owner_id = "testing.synthetic-protocol-controls-component-probe-host-s-defensive"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-lockstep-drift-gate-rust-ts-kebab-ident-mirror"
start_byte = 69673
end_byte = 71222
sha256 = "8a55d50f0a5461dbac89c6cdadac6f655b93f1915261ce078283a2a7200025dd"
source_kind = "record"
owner_id = "testing.lockstep-drift-gate-rust-ts-kebab-ident-mirror"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-pin-toolchain-verify-ts-s-nested-cargo-scratch"
start_byte = 71222
end_byte = 73168
sha256 = "1fce38b9d3676babd531da86918c6d77af88820fcf68af344fbdabf014fbac3a"
source_kind = "record"
owner_id = "testing.pin-toolchain-verify-ts-s-nested-cargo-scratch"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-verify-ts-refuse-unknown-flag-silently-degrading-plain"
start_byte = 73168
end_byte = 74237
sha256 = "550a5095030e15ea742382b6952114d0b07259cdf6bda021550d6b30117a39fa"
source_kind = "record"
owner_id = "testing.verify-ts-refuse-unknown-flag-silently-degrading-plain"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-section-standing-system"
start_byte = 74237
end_byte = 76888
sha256 = "4f99e0bd59a03c65ece1f4d084aee4fbee16f711020a71f73621abbc744fba5b"
source_kind = "section"
owner_id = "standing-system"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-gate-exists-registry-nowhere-tests-readme-md-hand"
start_byte = 76888
end_byte = 78751
sha256 = "6a62b78b937c4876032565e2f7550dadd98f55b6348a5e656cb51bce9e67f6a3"
source_kind = "record"
owner_id = "testing.gate-exists-registry-nowhere-tests-readme-md-hand"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-lint-doc-citations-accepts-docs-heading-durable-citation"
start_byte = 78751
end_byte = 80254
sha256 = "98f322ff8524153c8bd21d03b7fccf53ec6d4958f79a1b643a3ebb8f426e76b1"
source_kind = "record"
owner_id = "testing.lint-doc-citations-accepts-docs-heading-durable-citation"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-representation-changing-directive-goes-live-new-container-ship"
start_byte = 80254
end_byte = 84708
sha256 = "8112333e2500cecbb93daca03ebda3ac48f90f5dd27bb1dca7168eed617ef484"
source_kind = "record"
owner_id = "testing.representation-changing-directive-goes-live-new-container-ship"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-emission-branch-generated-output-fixture-exercises-invisible-gate"
start_byte = 84708
end_byte = 86513
sha256 = "95a18b721368de231c5d10dbda48e620bcdbd8a209850278509f7db436cd5b29"
source_kind = "record"
owner_id = "testing.emission-branch-generated-output-fixture-exercises-invisible-gate"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-logic-tool-emits-source-text-outside-layer-reasons"
start_byte = 86513
end_byte = 88626
sha256 = "6a822c15a014f20ee0945d1dff1465950a7683b4763fcd70d600a8f7509a1fc5"
source_kind = "record"
owner_id = "testing.logic-tool-emits-source-text-outside-layer-reasons"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-value-anchor-rot-field-addition-delivery-grows-existing"
start_byte = 88626
end_byte = 90365
sha256 = "9c5a9a0a10265ce7fc97199618affca108cde23252306a98ae44bdcb797f8dde"
source_kind = "record"
owner_id = "testing.value-anchor-rot-field-addition-delivery-grows-existing"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-corpus-wide-regen-over-prior-output-sweep-reaches"
start_byte = 90365
end_byte = 91596
sha256 = "aebd70ceee31b0c1ffe3b03a99843238832f35155e3fabf829333b8c9b96061f"
source_kind = "record"
owner_id = "testing.corpus-wide-regen-over-prior-output-sweep-reaches"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-stale-known-limitation-prose-surviving-fix-finding-ledgered"
start_byte = 91596
end_byte = 98391
sha256 = "e70ffc32ef354c170e24174e73c0efc1fa12e2c5a74098389da184dabed42ae4"
source_kind = "record"
owner_id = "testing.stale-known-limitation-prose-surviving-fix-finding-ledgered"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-finding-s-defect-side-attribution-recorded-narrative-plausibility"
start_byte = 98391
end_byte = 100991
sha256 = "edcdce21c5e032b8afbc4244a5c73f2f75291a3c52b571254850f8baf228679d"
source_kind = "record"
owner_id = "testing.finding-s-defect-side-attribution-recorded-narrative-plausibility"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-gate-demands-premise-probed-against-gate-evidence-about"
start_byte = 100991
end_byte = 106684
sha256 = "d50bcd7cac64b63ad8f68b0d9100abf07e4229e98b393575ee928328a97ef61d"
source_kind = "record"
owner_id = "testing.gate-demands-premise-probed-against-gate-evidence-about"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-ruling-premise-gate-x-stays-green-must-probed"
start_byte = 106684
end_byte = 108585
sha256 = "bb2fb9376fd64782b076fdf8531c3caf19a66502d997d18f70180015f7e3e01e"
source_kind = "record"
owner_id = "testing.ruling-premise-gate-x-stays-green-must-probed"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-observed-baseline-comments-beside-gate-floors-rot-silently"
start_byte = 108585
end_byte = 109903
sha256 = "bb278c371c60b4ebeb3d8b65260dea97e20b291e76c065e496713f7fec87cafc"
source_kind = "record"
owner_id = "testing.observed-baseline-comments-beside-gate-floors-rot-silently"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-extreme-value-boundary-correctness-generation-time-encode-paths"
start_byte = 109903
end_byte = 111820
sha256 = "4438ccd1b5d6dbd08dc5fd19fb5b19a4d7ef8bb8c6519f59e60e0036ce215586"
source_kind = "record"
owner_id = "testing.extreme-value-boundary-correctness-generation-time-encode-paths"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-fixture-models-obsolete-user-contract-stays-green-real"
start_byte = 111820
end_byte = 115406
sha256 = "7ff6ab6706acfee47a10ebf476340df61c66d73043680774c65b533e49d91e56"
source_kind = "record"
owner_id = "testing.fixture-models-obsolete-user-contract-stays-green-real"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-panic-site-un-shadowing-converting-shallow-panic-graceful"
start_byte = 115406
end_byte = 116819
sha256 = "9251ab70159fc2fa57f69cc329c530a056f37331ccfaaf3f5181df5fa12d3234"
source_kind = "record"
owner_id = "testing.panic-site-un-shadowing-converting-shallow-panic-graceful"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-three-defect-classes-process-test-see-sharing-remedy"
start_byte = 116819
end_byte = 119213
sha256 = "22f3191ac9cdb15ee19ead6b0f8c237bafc6f8e0df3e523e8d8518e0465f8034"
source_kind = "record"
owner_id = "testing.three-defect-classes-process-test-see-sharing-remedy"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-rustc-after-panic-ok-flip-panic-fix-lands"
start_byte = 119213
end_byte = 120913
sha256 = "dc5a03a0a183e4eeb6934290aa4c37e0bb727a425039898fd4d7ac61acc8df1f"
source_kind = "record"
owner_id = "testing.process-boundary.rustc-after-panic-fix"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-rustfmt-code-never-reaches-rustc-all"
start_byte = 120913
end_byte = 123135
sha256 = "17a2b3e92eec5d0d1b521ac5cf852fb98a432268019a344f9f7473af307fa792"
source_kind = "record"
owner_id = "testing.process-boundary.rustfmt-pre-rustc"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-another-profile-flip-abort-fix-widening-reachable-set"
start_byte = 123135
end_byte = 125348
sha256 = "dcfdc3674658b809616687aed1d354daed8957dd2582b01a82dc576d23941bab"
source_kind = "record"
owner_id = "testing.process-boundary.another-profile-escape"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-parallel-constructor-fixture-diversity-parser-over-external-input"
start_byte = 125348
end_byte = 127883
sha256 = "41651aff847599b3a2463bb94d002a5e9cf2a359fe34d2e63d74112e4d48a01a"
source_kind = "record"
owner_id = "testing.parallel-constructor-fixture-diversity-parser-over-external-input"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-synthesized-name-referenced-never-minted-e0425-flavor"
start_byte = 127883
end_byte = 129560
sha256 = "60643bd0b2b0f98efdddd3e2d1a4dec451371b2edf0ce256a919f201afd6fb53"
source_kind = "record"
owner_id = "testing.synthesized-name-referenced-never-minted-e0425-flavor"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-multifile-reference-position-coverage-two-position-keyed-escapes"
start_byte = 129560
end_byte = 132834
sha256 = "3f468c1ee5e94d499f4014b542b8c9347b5c495f2a13ba7a50313038843fd78c"
source_kind = "record"
owner_id = "testing.multifile-reference-position-coverage-two-position-keyed-escapes"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-reason-keyed-rejection-evidence-reject-catalogs-proven-near"
start_byte = 132834
end_byte = 137263
sha256 = "379df39691a934fe4459213dc6ac01f0fe38c35e54d50d9884b7749c25d70580"
source_kind = "record"
owner_id = "testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-armed-idle-harness-arms-empty-head-ledgers-zero"
start_byte = 137263
end_byte = 142744
sha256 = "584596b6b19e7b20fdef39f3f730b263252aaebba538df35435d7f3d31485be3"
source_kind = "record"
owner_id = "testing.armed-idle-harness-arms-empty-head-ledgers-zero"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-doubled-doc-comment-markers-emitted-docs-proven-instance"
start_byte = 142744
end_byte = 144055
sha256 = "91a71a674e5cca65e145b303afebfe5d8e169fe028537a93db9f945877b05f47"
source_kind = "record"
owner_id = "testing.doubled-doc-comment-markers-emitted-docs-proven-instance"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-emitted-shape-lint-classes-outside-clippy-all-beyond"
start_byte = 144055
end_byte = 145570
sha256 = "1a719f5f8d1a254cda6f5a45c5df4aeafef53e8e32096837326c21bcd98dd32f"
source_kind = "record"
owner_id = "testing.emitted-shape-lint-classes-outside-clippy-all-beyond"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-unused-imports-generated-crates-trait-import-class-name"
start_byte = 145570
end_byte = 156048
sha256 = "50eed48bac79853d3111beb25ac010de3355b580c152d919877c2a05a2873e14"
source_kind = "record"
owner_id = "testing.unused-imports-generated-crates-trait-import-class-name"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-mechanical-layers-two-review-owned-design-rules-tests"
start_byte = 156048
end_byte = 158761
sha256 = "c2606db415df306435315c21451c6aa03607e6ace369a91c0e5677ab698c1f79"
source_kind = "record"
owner_id = "testing.mechanical-layers-two-review-owned-design-rules-tests"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-add-profile-wrapper-participation-grid-participation-differs-across"
start_byte = 158761
end_byte = 159686
sha256 = "372b057cd5b9755a725f66c7fe3bcde2d8a55abd1c3ef3156a5847ce93715b1f"
source_kind = "record"
owner_id = "testing.add-profile-wrapper-participation-grid-participation-differs-across"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-extern-deps-wasm-boundary-surface-packaging-json-gen"
start_byte = 159686
end_byte = 161945
sha256 = "cbbdf84df482bb6c1a53432ebaf74255d5ac9590a7df8ce3e1807c44f1ba3606"
source_kind = "record"
owner_id = "testing.extern-deps-wasm-boundary-surface-packaging-json-gen"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-generic-raw-bytes-base-refused-generic-extern-base"
start_byte = 161945
end_byte = 163576
sha256 = "62cac3d3fd0f8679e90ef7b22d4ee7a5c1b147a38a744fb2815047d4f75cf131"
source_kind = "record"
owner_id = "testing.generic-raw-bytes-base-refused-generic-extern-base"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-twin-repo-implementations-semantic-drifting-apart-emission-spellings"
start_byte = 163576
end_byte = 167289
sha256 = "636571bccbf774336a631f7cb6adc50a6590a0f75418dcefd7202b48ff6fec99"
source_kind = "record"
owner_id = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-read-caught-instance-class"
start_byte = 167289
end_byte = 167858
sha256 = "508fc71a1f358fb1d91dad08955f5d7290fb44d41d3904e06233b286b7b2ed0a"
source_kind = "record"
owner_id = "testing.twin-implementation.read-caught-instance-trigger"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-comment-ast-grammar-change-any-form-just-new"
start_byte = 167858
end_byte = 168763
sha256 = "2bb5878470297973ea887421dee115a49d5cf8436fdbeb228a9036d9e9f73dc8"
source_kind = "record"
owner_id = "testing.twin-implementation.comment-ast-change-cadence"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-own-reviewed-change-never-drive"
start_byte = 168763
end_byte = 169320
sha256 = "a7de9229a9312c226d451ea98db3f9b50c38769007c9f0cdb07ff1ff6b3b6f45"
source_kind = "record"
owner_id = "testing.twin-implementation.own-reviewed-change-rule"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-non-recorded-isn-t-re-litigated-coverage-gap"
start_byte = 169320
end_byte = 169659
sha256 = "9fa4b54aa72252fb7899d89365646f5a349957103654dd68be7c09d8d78537f4"
source_kind = "part"
owner_id = "part-non-recorded-isn-t-re-litigated-coverage-gap"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-tree-wide-sweeps-over-gitignored-regeneration-targets-count"
start_byte = 169659
end_byte = 171244
sha256 = "79114f1a350586755df85fe5c7865268a5f4316fc6a5da538ea4084a6ee33219"
source_kind = "record"
owner_id = "testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-run-local-values-gate-cache-key-material-proven"
start_byte = 171244
end_byte = 172527
sha256 = "595c20172c0fe1c34e2f9ac884dfc4ba382d877bf8835fb93802a739b081c61f"
source_kind = "record"
owner_id = "testing.run-local-values-gate-cache-key-material-proven"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-control-bytes-committed-source-files-proven-instance-machinery"
start_byte = 172527
end_byte = 173350
sha256 = "f9842a5d4686cbe4d5f16e5b82d65c2acd5f60f0b9dc022b1ee947986241e785"
source_kind = "record"
owner_id = "testing.control-bytes-committed-source-files-proven-instance-machinery"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-gate-script-health-cache-staleness-key-fresh-worktree"
start_byte = 173350
end_byte = 175008
sha256 = "0461e84254db73a854739806d7c789ca92dd59be6c41ad8f65682fabe0afd296"
source_kind = "record"
owner_id = "testing.gate-script-health-cache-staleness-key-fresh-worktree"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-hand-maintained-source-dependency-mapping-manifest-changeset-silently"
start_byte = 175008
end_byte = 177052
sha256 = "aae6e1440011859eebf38756419f09c195c9c977c3ecd14d1bbabfa14119fbdc"
source_kind = "record"
owner_id = "testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-dependency-swap-pass-compile-parity-existing-gate-silently"
start_byte = 177052
end_byte = 180151
sha256 = "66299b54b0e166e5e769f6df58cf1da77ec10b60aa6ec4e33c589a482ce3d657"
source_kind = "record"
owner_id = "testing.dependency-swap-pass-compile-parity-existing-gate-silently"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-fixture-s-verdict-accident-cargo-feature-unification-harness"
start_byte = 180151
end_byte = 183225
sha256 = "700a78cdc8da54b10601a92b933b175f76e046d2a98a50043c941d695c632b92"
source_kind = "record"
owner_id = "testing.fixture-s-verdict-accident-cargo-feature-unification-harness"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-profile-driven-compile-gate-covers-compositions-profiles-specs"
start_byte = 183225
end_byte = 185187
sha256 = "040ab06be86d9537b59b6b1f8a5645bd533880757a049915573e9bf6b94d8698"
source_kind = "record"
owner_id = "testing.profile-driven-compile-gate-covers-compositions-profiles-specs"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-alloc-imports-nested-inline-modules-hand-carried-design"
start_byte = 185187
end_byte = 187354
sha256 = "56d5342f0ccd9d007f45a5aece33826ea7523855c36b7bccca45d3229eb20a32"
source_kind = "record"
owner_id = "testing.alloc-imports-nested-inline-modules-hand-carried-design"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-gate-cache-closure-audit-structurally-blind-gate-authored"
start_byte = 187354
end_byte = 189334
sha256 = "6b3375e2d5cdfc6d9567e7ad9a43bd9a3d04c252ca7802acd7000efc281d5b90"
source_kind = "record"
owner_id = "testing.gate-cache-closure-audit-structurally-blind-gate-authored"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-comment-residue-false-matches-text-scans-over-emitted"
start_byte = 189334
end_byte = 191270
sha256 = "265a766d45ffd42975fbae65e89cf944df015c8e6688a2d34b5c9bd96c6a14e0"
source_kind = "record"
owner_id = "testing.comment-residue-false-matches-text-scans-over-emitted"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-directive-dependent-emitter-edge-case-triggering-shape-hand"
start_byte = 191270
end_byte = 192994
sha256 = "71f8edc065af0bbe49dd4c2942f6d8ab68a91017b37462dcd58b384eaaa01de1"
source_kind = "record"
owner_id = "testing.directive-dependent-emitter-edge-case-triggering-shape-hand"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-hand-enumerated-ban-pattern-list-authority-lives-gitignored"
start_byte = 192994
end_byte = 194645
sha256 = "f57cda44b9f3118b6525df99f8da96e87e207f8b559973750efcae9f9896ae96"
source_kind = "record"
owner_id = "testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-extend-recombination-member-kind-table-optional-fixed-shape"
start_byte = 194645
end_byte = 196091
sha256 = "4a71868d7cd6b0d3193bfcaec6ae5ef2b57c828ceb1457d74ba576c35938f940"
source_kind = "record"
owner_id = "testing.extend-recombination-member-kind-table-optional-fixed-shape"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-referencing-context-measured-wrapping-deep-honored-base-shape"
start_byte = 196091
end_byte = 197680
sha256 = "187a1958dc57c700d4bc8b9fe2c9ed2196e553b144115bf1d0b8f99c05feabee"
source_kind = "record"
owner_id = "testing.referencing-context-measured-wrapping-deep-honored-base-shape"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-arm-position-s-classifier-compile-time-forcing-function"
start_byte = 197680
end_byte = 199360
sha256 = "ebe8ffc9ff73daedb2cb491c1a3f7c04634fce283da04d9e9ade466176abd777"
source_kind = "record"
owner_id = "testing.arm-position-s-classifier-compile-time-forcing-function"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-schema-document-s-name-injectivity-enforced-row-side"
start_byte = 199360
end_byte = 199969
sha256 = "5a6764c2f59db73158a46ed723e4ad5412ece478b771ea425a7a0b4a0e2a5f72"
source_kind = "record"
owner_id = "testing.schema-document-s-name-injectivity-enforced-row-side"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-emitted-name-guard-mint-records-unminted-extra"
start_byte = 199969
end_byte = 200261
sha256 = "8deb71df910f631772136647ab084d8411e5a2e7b9e6691382deb235f8d573ff"
source_kind = "part"
owner_id = "part-emitted-name-guard-mint-records-unminted-extra"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-collision-loser-row-schema-id-s-match"
start_byte = 200261
end_byte = 200962
sha256 = "dbf77990d13e0a34b637861328ad8066142c6a62eddba1c56afdf20d4b2356c3"
source_kind = "record"
owner_id = "testing.schema-name.collision-loser-no-row-same-id"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-cross-crate-collision-schema-id-s-match"
start_byte = 200962
end_byte = 202242
sha256 = "147eb980bcd390f719f1f27869b597147d0ccc03d35158bb83a56881d8d83311"
source_kind = "record"
owner_id = "testing.schema-name.cross-crate-same-id-collision"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-schema-name-schemars-percent-encodes-ref"
start_byte = 202242
end_byte = 203513
sha256 = "a74e165599a8263cfab534e90d89a09213c6f5edec2cc5895a37e7429c9e8f2a"
source_kind = "record"
owner_id = "testing.schema-name.percent-encoded-ref"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-collision-two-types-both-lack-rows"
start_byte = 203513
end_byte = 208090
sha256 = "5a2293a0c7cb67b10b37e490fd19f7e362a0c42da3e0b5c7394022da4d185993"
source_kind = "record"
owner_id = "testing.schema-name.two-rowless-types-collide"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-emitted-name-guard-neither-closes-holes-above-two"
start_byte = 208090
end_byte = 209175
sha256 = "f5528a9ca03489535cb33b967356362ce4d77bd3c5879a13aab611850901afc0"
source_kind = "part"
owner_id = "part-emitted-name-guard-neither-closes-holes-above-two"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-directive-s-documented-sentence-stay-true-consumer-visible"
start_byte = 209175
end_byte = 212567
sha256 = "eafa910d6fb87d6abc3e1cc50447e74ea54ba5536a36126e72d3f748e2dae79c"
source_kind = "record"
owner_id = "testing.directive-s-documented-sentence-stay-true-consumer-visible"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-citation-resolves-naming-wrong-thing-invisible-citation-lint"
start_byte = 212567
end_byte = 214715
sha256 = "f7be6359156888ab0130104d961c1aeb9d5f827a71e7cff57fff42c92a74dde5"
source_kind = "record"
owner_id = "testing.citation-resolves-naming-wrong-thing-invisible-citation-lint"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-matrix-s-note-names-panic-site-aborts-nothing"
start_byte = 214715
end_byte = 216035
sha256 = "fa9db69e8bd98f7779f5d0c1dd493e978426ba833fb9cf8761f8bd240e57d3f1"
source_kind = "record"
owner_id = "testing.matrix-s-note-names-panic-site-aborts-nothing"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-nothing-asserts-how-many-times-rejection-message-emitted"
start_byte = 216035
end_byte = 217085
sha256 = "099c5c72fff6b31e9d3a5e598a3b48835f8c2f25d812233abdb272e750136baf"
source_kind = "record"
owner_id = "testing.nothing-asserts-how-many-times-rejection-message-emitted"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-containment-note-s-stated-reason-go-false-any"
start_byte = 217085
end_byte = 219020
sha256 = "fe539e6dfa55c4983bbffcc328470eb8a1f83352ed5533d005cffb6b2424cf9c"
source_kind = "record"
owner_id = "testing.containment-note-s-stated-reason-go-false-any"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-emit-tests-gate-asserts-round-trip-green-cannot"
start_byte = 219020
end_byte = 220955
sha256 = "29939e99940deb9b4f4df1986cdcab5c1fc42ba48d1ed0c0135a300d687d009a"
source_kind = "record"
owner_id = "testing.emit-tests-gate-asserts-round-trip-green-cannot"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-emitter-drops-overloadable-parameter-building-fresh-config-invisible"
start_byte = 220955
end_byte = 223506
sha256 = "12cdf4da975489b643bd78750b6605934c7606db77f7ec3084dc10eec178be5c"
source_kind = "record"
owner_id = "testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-cargo-feature-generated-crate-s-dependency-change-what"
start_byte = 223506
end_byte = 226834
sha256 = "94fdc516b957748ec678717f05e51aedd76ea25390f56b633df1dc5d552edd8b"
source_kind = "record"
owner_id = "testing.cargo-feature-generated-crate-s-dependency-change-what"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-documented-flag-pairing-broken-fixture-stays-green-because"
start_byte = 226834
end_byte = 228319
sha256 = "861ae783e050e6a676eca60b7115c5833e36a2fa4cf568f9bbffff438a36124e"
source_kind = "record"
owner_id = "testing.documented-flag-pairing-broken-fixture-stays-green-because"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-documented-failure-shape-exercises-prose-cross-crate-int"
start_byte = 228319
end_byte = 230101
sha256 = "082f4d599a40c492cc52e78872dbc3ab3a94adfe2712f59e2b6a42729f7c1634"
source_kind = "record"
owner_id = "testing.documented-failure-shape-exercises-prose-cross-crate-int"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-dsl-position-tests-string-level-design-green-emitted"
start_byte = 230101
end_byte = 231168
sha256 = "994a599cf9c834387718e9b588b017d140504e573c10302d58e8d98707a6ab84"
source_kind = "record"
owner_id = "testing.dsl-position-tests-string-level-design-green-emitted"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-nothing-asserts-two-ir-sites-cannot-mint-same"
start_byte = 231168
end_byte = 233829
sha256 = "06143d65c96981db81936d8bc52f12deff10687dd58df3cd3325b76f58e11eaf"
source_kind = "record"
owner_id = "testing.nothing-asserts-two-ir-sites-cannot-mint-same"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-message-s-level-classification-review-owned-two-ways"
start_byte = 233829
end_byte = 236147
sha256 = "fd2fc1bd18efc07d474b8efc2893e327caa51291f6217e006ae8f863b752e39b"
source_kind = "record"
owner_id = "testing.message-s-level-classification-review-owned-two-ways"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-wac-composed-component-transpiled-jco-loses-cross-instance"
start_byte = 236147
end_byte = 238262
sha256 = "6b107f59767f9663245b2b7356c7b93d5e23776b64826de816d2e24e879bf461"
source_kind = "record"
owner_id = "testing.wac-composed-component-transpiled-jco-loses-cross-instance"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-make-fixture-comment-s-claimed-wire-bytes-executable"
start_byte = 238262
end_byte = 239789
sha256 = "d8a0c47931251776e9db0aac0107e03ab2af6f169f78096fad4593d0eac6190e"
source_kind = "record"
owner_id = "testing.make-fixture-comment-s-claimed-wire-bytes-executable"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-section-deferred-features"
start_byte = 239789
end_byte = 239851
sha256 = "2dd5da89bae092a95d9b7e3189ca234b8df25156788aa690bdf721a0d600035b"
source_kind = "section"
owner_id = "deferred-features"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-workspace-mode-wasm32-wasip2-build-gate-both-faces"
start_byte = 239851
end_byte = 241352
sha256 = "7e37ff740fefee532206ea2a6291ce7fca230cf4325cb3004afcfad6e0caacdb"
source_kind = "record"
owner_id = "testing.workspace-mode-wasm32-wasip2-build-gate-both-faces"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-reopening-signal-workspace"
start_byte = 241352
end_byte = 241805
sha256 = "93c3fcec93d547a73093f3340485fbdb0c1c13b9453b22e04a68bbe8186a95a6"
source_kind = "record"
owner_id = "testing.workspace-wasip2.reopening-signal"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-probe-component-face-s-js-surface-axes-js"
start_byte = 241805
end_byte = 242979
sha256 = "de875f14e7710b532dee14d66f04d64c555d0da7ce3f7e428d541b5d77d1b453"
source_kind = "record"
owner_id = "testing.probe-component-face-s-js-surface-axes-js"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-reopening-signal-probe"
start_byte = 242979
end_byte = 243484
sha256 = "321ead11fff1d5c8d6e08f132a52da701e9976d4265a05821dfc54a53147c342"
source_kind = "record"
owner_id = "testing.component-js-probe.reopening-signal"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-crate-generates-json-schema-document-aggregate-package"
start_byte = 243484
end_byte = 245770
sha256 = "dc089b4e14ef0dbfc3d0935ffba2bb92c284229015f65d0970bf9442dfe3580e"
source_kind = "record"
owner_id = "testing.crate-generates-json-schema-document-aggregate-package"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-wasm32-link-floor-deferred-reject-twin"
start_byte = 245770
end_byte = 246745
sha256 = "03a7a46c89c798ae97b8a353555da4007cba7258aa3810cab23b0327cb395018"
source_kind = "record"
owner_id = "testing.wasm32-link-floor-deferred-reject-twin"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-extern-interface-export-dialect-v2-candidates"
start_byte = 246745
end_byte = 248689
sha256 = "8d0055e772f759c4d73ffc77954ce3ecf2f4fe160022ea7f29728db0ea648449"
source_kind = "record"
owner_id = "testing.extern-interface-export-dialect-v2-candidates"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-cddl-module-directives-draft-ietf-cbor-cddl-modules"
start_byte = 248689
end_byte = 249627
sha256 = "e17084b604e9b5189574353fefe2e84ed1b0cd5f111ec91edd70d2de885557c8"
source_kind = "record"
owner_id = "testing.cddl-module-directives-draft-ietf-cbor-cddl-modules"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-std-generated-crates"
start_byte = 249627
end_byte = 249983
sha256 = "bd1b9bd0fd6d31f77ba22a1c2ef90f3de4cd08d35601429d0e677d07b5bc8b29"
source_kind = "record"
owner_id = "testing.std-generated-crates"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-dependency-version-range-resolution-untested-generated-crates-float"
start_byte = 249983
end_byte = 251074
sha256 = "616524de40965232635f410ae52162f0bcf68eb1c42f41d66ca35ccef2baf67d"
source_kind = "record"
owner_id = "testing.dependency-version-range-resolution-untested-generated-crates-float"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-transparent-tag-set-idiom-recognized-shape-boundary-request"
start_byte = 251074
end_byte = 251723
sha256 = "3cf07aaaae812a492b40658f98add60104dd5f12712a81423ea723d60e7c82c6"
source_kind = "record"
owner_id = "testing.transparent-tag-set-idiom-recognized-shape-boundary-request"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-non-idiom-choice-bodied-generic-defs-refused-supported"
start_byte = 251723
end_byte = 252480
sha256 = "f90d5dbe0c6166a23001505eb367c163a31d39106de689301c5ab0c105921a22"
source_kind = "record"
owner_id = "testing.tag-set.non-idiom-generic-boundary"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-inline-anonymous-two-arm-choices-recognized"
start_byte = 252480
end_byte = 252752
sha256 = "32b12882c58d0d91bbcb930e2e2cfe208a83dab88f053cea911058b8b7c362c6"
source_kind = "record"
owner_id = "testing.tag-set.inline-anonymous-choice-boundary"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-opt-rfc-faithful-total-cbor-json-rendering-json"
start_byte = 252752
end_byte = 254374
sha256 = "80a1207ae71069e46ea9d1008be71380286f3ff5c671d02592eec7f204f660f8"
source_kind = "record"
owner_id = "testing.opt-rfc-faithful-total-cbor-json-rendering-json"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-edn-text-notation-round-trip-edn-edn-draft"
start_byte = 254374
end_byte = 255386
sha256 = "681d821e94895b0ad1a045e0b38b46be54c0bae17e9f3ca1fb27e504f258fc96"
source_kind = "record"
owner_id = "testing.edn-text-notation-round-trip-edn-edn-draft"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-project-json-gen-documents-run-test-fixture-writes"
start_byte = 255386
end_byte = 256926
sha256 = "c2fe1c977338a7c808d9dc8f67f6211782977b7bbd7f25573576320e2321e267"
source_kind = "record"
owner_id = "testing.project-json-gen-documents-run-test-fixture-writes"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-propagating-deliberately-unpublished-type-s-intent-json-ts"
start_byte = 256926
end_byte = 258470
sha256 = "62253c79762c4c0d609d259e36112965f6e8893d405d1f53d27d51d554b90126"
source_kind = "record"
owner_id = "testing.propagating-deliberately-unpublished-type-s-intent-json-ts"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-json2ts-artifact-doc-comments-naming-undefined"
start_byte = 258470
end_byte = 259693
sha256 = "ccf9722e449c303380ca3e0e8fa20f536fb4bed8d6bbc19e60a9f9f9c75738f1"
source_kind = "record"
owner_id = "testing.json2ts-artifact-doc-comments-naming-undefined"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-file-listing-json-schema-root-values-flag-per"
start_byte = 259693
end_byte = 260367
sha256 = "62a48da4f600a1b5445e10607420f0c1f235252da023b776d6a8ff08cc7b37fb"
source_kind = "record"
owner_id = "testing.file-listing-json-schema-root-values-flag-per"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-assert-hand-authored-json-schema-against-type-s"
start_byte = 260367
end_byte = 261998
sha256 = "c00607bad3e17c520c0c09b92a27b67dc3904ba50f47241e1885179be3a40472"
source_kind = "record"
owner_id = "testing.assert-hand-authored-json-schema-against-type-s"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-composing-registration-rows-across-generation-passes-share-output"
start_byte = 261998
end_byte = 262861
sha256 = "77c0da5d8c24dd9bb568c371cca2ee6052bb15fc1e2970c61325ef63288aabd9"
source_kind = "record"
owner_id = "testing.composing-registration-rows-across-generation-passes-share-output"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-machine-readable-ownership-co-owned-features-std-list"
start_byte = 262861
end_byte = 263861
sha256 = "d6371c4e925609609c30e6fc7a445a2292d133beaabf95c9560d76c966641aa6"
source_kind = "record"
owner_id = "testing.machine-readable-ownership-co-owned-features-std-list"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-section-operational-watches"
start_byte = 263861
end_byte = 263885
sha256 = "d0806d2a287653911b25b1507812bb5306b0912bc58032113e48c8be29fda70c"
source_kind = "section"
owner_id = "operational-watches"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-gate-s-own-stdout-print-column-result-pass"
start_byte = 263885
end_byte = 264957
sha256 = "eaf222cd544071584303141324572db8bce4ea044cb76f7d9838ee22bb9992aa"
source_kind = "record"
owner_id = "testing.gate-s-own-stdout-print-column-result-pass"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-flag-cddl-codegen-invocation-wasm-run-wasm-flag"
start_byte = 264957
end_byte = 266255
sha256 = "c96e83e4ff9c4f19ccf5e168698f6b9784508f836b02f70108a848aa42906432"
source_kind = "record"
owner_id = "testing.flag-cddl-codegen-invocation-wasm-run-wasm-flag"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-failing-snapshot-test-surfaces-mismatch-per-test-run"
start_byte = 266255
end_byte = 267380
sha256 = "4060228b2079cf244515920c796240417bc94bab2b77b9fadf9cbef0c97e9197"
source_kind = "record"
owner_id = "testing.failing-snapshot-test-surfaces-mismatch-per-test-run"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-e0463-t-find-crate-core-scratch-tree-std"
start_byte = 267380
end_byte = 269094
sha256 = "d76e925c70bb9a64f9da3850a25134b07c6624d23d391ce12ea40fafdc25e6e9"
source_kind = "record"
owner_id = "testing.e0463-t-find-crate-core-scratch-tree-std"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-working-tree-gate-run-concurrent-live-implementation-agent"
start_byte = 269094
end_byte = 270503
sha256 = "c015839fd3bd959c6f763a64b2451628e468d84c4081aadf935a1d4df98d4a78"
source_kind = "record"
owner_id = "testing.working-tree-gate-run-concurrent-live-implementation-agent"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-stale-pin-guard-cannot-distinguish-gap-closed-vector"
start_byte = 270503
end_byte = 272662
sha256 = "1256c341156b9a18dc03c7377dec56f5e826c7ef8ebfee4d97e5c4195522a5c8"
source_kind = "record"
owner_id = "testing.stale-pin-guard-cannot-distinguish-gap-closed-vector"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-migration-handoff-s-complete-list-found-survey-negative"
start_byte = 272662
end_byte = 274162
sha256 = "7321cb4ac4dca070563c669fb8b59a5176daaaa9432f7b22c0f0bfeb13c74be3"
source_kind = "record"
owner_id = "testing.migration-handoff-s-complete-list-found-survey-negative"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-shared-cargo-target-dir-across-same-named-scratch"
start_byte = 274162
end_byte = 275356
sha256 = "6422032547f1b6e5de7c4bb6e6c3e842eb3845c9dd6f48ece9801a4d86cd45ee"
source_kind = "record"
owner_id = "testing.shared-cargo-target-dir-across-same-named-scratch"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-config-run-s-convergence-warning-name-crate-already"
start_byte = 275356
end_byte = 277045
sha256 = "b7770b7e134e736f463b6f5d5c33daa893d84a832f0334049d1811e42bd6b134"
source_kind = "record"
owner_id = "testing.config-run-s-convergence-warning-name-crate-already"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-extern-interface-export-public-interchange-format"
start_byte = 277045
end_byte = 277933
sha256 = "64ba0b190be86ed6f6885283437c0e0a9f0830cbe7300cf7389d329137988fbc"
source_kind = "record"
owner_id = "testing.extern-interface-export-public-interchange-format"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-exclusion-reasons-informational-interchange"
start_byte = 277933
end_byte = 278200
sha256 = "6fcca8b0b8dfb7d44a18c085baa2a32962b61071ee17b9e472867123591282f0"
source_kind = "record"
owner_id = "testing.exclusion-reasons-informational-interchange"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-nested-cargo-scratch-retention-tmp-cddl-leaks-until"
start_byte = 278200
end_byte = 280088
sha256 = "148a295fd27fa0f524f12c3e460d11f8202e6dd4fd13d54a7798d1f02f9a4db2"
source_kind = "record"
owner_id = "testing.nested-cargo-scratch-retention-tmp-cddl-leaks-until"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-reopening-signal-nested"
start_byte = 280088
end_byte = 281044
sha256 = "e8f67bfa77c0336bd2262cb921dce4dc82f0dd577457caf5d79822f1d11b21c7"
source_kind = "record"
owner_id = "testing.scratch-retention.reopening-signal"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-tier-s-peak-memory-bounded-arithmetic-over-assumed"
start_byte = 281044
end_byte = 283364
sha256 = "c467667ad16b1fd50b66f5b292e0fd84fad71f380d93d881fa792d07fbf24601"
source_kind = "record"
owner_id = "testing.tier-s-peak-memory-bounded-arithmetic-over-assumed"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-spend-measurements"
start_byte = 283364
end_byte = 283786
sha256 = "055d1a32d9b90c601cee1d0fdbc511faef7b48baab3ea1a91d04ab6aae4770ce"
source_kind = "record"
owner_id = "testing.tier-memory.spend-measurements"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-freezes-livelock-repo-arithmetic-make-them-impossible-containment"
start_byte = 283786
end_byte = 284898
sha256 = "df32a50052e3c87fa439f177cb70e0538730049a9c9293583960b10db3cbc11b"
source_kind = "part"
owner_id = "part-freezes-livelock-repo-arithmetic-make-them-impossible-containment"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-negative-premise-stands"
start_byte = 284898
end_byte = 285280
sha256 = "78a9f27a5c8507147a78992f40d15eb1216c2b4e9039bee9a35fdd74dc312f50"
source_kind = "part"
owner_id = "part-negative-premise-stands"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
start_byte = 285280
end_byte = 287902
sha256 = "b0c8982b04c7e58ea7fbef66e90175ed16c795b3972d795069e3f7a6813bca01"
source_kind = "record"
owner_id = "testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-gate-cache-costs"
start_byte = 287902
end_byte = 288222
sha256 = "edb49a168fccc0ea4496eb895e9bc47157bbdf3bcca8635f1a842d2f061ad3c2"
source_kind = "record"
owner_id = "testing.gate-cache-costs"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-all-hit-cost"
start_byte = 288222
end_byte = 288490
sha256 = "ef80f589dc4a9f6a34ce0ed1506d49326fce9a94d1307647fc8171bacdcf8dc5"
source_kind = "record"
owner_id = "testing.gate-cache.all-hit-cost"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-coverage-extensions"
start_byte = 288490
end_byte = 288977
sha256 = "8da4cf5118393f8b173a62281daeda22ddb984f2cf884f500ce65539a296fde9"
source_kind = "record"
owner_id = "testing.gate-cache.coverage-extensions"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-closure-audit-traced-set-extension"
start_byte = 288977
end_byte = 289776
sha256 = "394f9f77b82d72d0561c32777e2b9f3203580ecfaaecabd926bfb099f31f507c"
source_kind = "record"
owner_id = "testing.gate-cache.closure-audit-traced-set-extension"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-pre-cache-remedies-stay-valid-if-uncached-path"
start_byte = 289776
end_byte = 290064
sha256 = "f58d071bf22c0b429eca987933640aaeba9281eb4de5fd6ea14ca6c49d093f2a"
source_kind = "part"
owner_id = "part-pre-cache-remedies-stay-valid-if-uncached-path"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-registry-fetch-transients-nested-cargo-cells"
start_byte = 290064
end_byte = 290899
sha256 = "dcf0a2c0481eb9c465d01e5b5c9f65e2a531e5716028397d2cab961ea92f10f9"
source_kind = "record"
owner_id = "testing.registry-fetch-transients-nested-cargo-cells"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-surfaces-still-online"
start_byte = 290899
end_byte = 291527
sha256 = "33fd5da4d3ad04e26200d1faf784452e0f7cf2589d09c772f41457354d7216cd"
source_kind = "part"
owner_id = "part-surfaces-still-online"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-verify-ts-evidence-absorber-stays"
start_byte = 291527
end_byte = 291912
sha256 = "98b43e87c8dd33f221f3a6b3f936e755c9fce06242e2b2f0cef58a2fa795528c"
source_kind = "part"
owner_id = "part-verify-ts-evidence-absorber-stays"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-part-part-sighting-offline-gate-run-new-class"
start_byte = 291912
end_byte = 293507
sha256 = "c38bac582889425ca4ecb4680d532ffc35cf74ff4502f19dcbee51668ea3adc6"
source_kind = "part"
owner_id = "part-sighting-offline-gate-run-new-class"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-verify-cache-transparency-b-split-emission-profile-embed"
start_byte = 293507
end_byte = 295112
sha256 = "ba5faa632850c5b01c71b269849edcd0281192c07599aa19500a2fa9302a21e4"
source_kind = "record"
owner_id = "testing.verify-cache-transparency-b-split-emission-profile-embed"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-full-suite-flake-attributed-hardened-acquire-scratch-lock"
start_byte = 295112
end_byte = 297099
sha256 = "833a87a8793ef4e5522355bb39394b0a1a5aa6686da58d540fe186c254099979"
source_kind = "record"
owner_id = "testing.full-suite-flake-attributed-hardened-acquire-scratch-lock"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-verify-ts-warm-up-cargo-test-exit-attributed"
start_byte = 297099
end_byte = 298059
sha256 = "8b591aea248f86bced1bc35a005612f7632833f46a95867d65f6c5629ab5b4eb"
source_kind = "record"
owner_id = "testing.verify-ts-warm-up-cargo-test-exit-attributed"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-backgrounded-check-ts-full-launched-sub-agent-s"
start_byte = 298059
end_byte = 299757
sha256 = "4026d74baeb03b1ddda46fdba00e0da63b6d13fe44fe55054141dc429ec25f5d"
source_kind = "record"
owner_id = "testing.backgrounded-check-ts-full-launched-sub-agent-s"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-section-declined-boundaries"
start_byte = 299757
end_byte = 299833
sha256 = "9164294b3ed22e517c59641572603f0d7897c0409b0c7c39b15db441e6108dae"
source_kind = "section"
owner_id = "declined-boundaries"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-complete-cargo-mutants-sweep-will-never-run"
start_byte = 299833
end_byte = 300426
sha256 = "5de23b32af4cb10fd73a101a3afd413583661510196907b6d932b748fe875d30"
source_kind = "record"
owner_id = "testing.complete-cargo-mutants-sweep-will-never-run"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-making-type-choice-path-reject-duplicated-explicit-name"
start_byte = 300426
end_byte = 302067
sha256 = "8b20e13bf366198497f4c771c7470be2f652dec8a2dbf6ed70d1f45399d17e78"
source_kind = "record"
owner_id = "testing.making-type-choice-path-reject-duplicated-explicit-name"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-msrv-declaration-os-matrix-generated-code"
start_byte = 302067
end_byte = 302446
sha256 = "dfbf886683fd7c8af280b0fdba5bdf9b0be32478f8fb23ff4782b6568f8f9ea4"
source_kind = "record"
owner_id = "testing.msrv-declaration-os-matrix-generated-code"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-docs-vs-behavior-conformance-harness-comment-dsl-mdx"
start_byte = 302446
end_byte = 308868
sha256 = "d039650d6ea7d055c8b470c75a179c1b06e980ffed33637f8da297ec1b4164d3"
source_kind = "record"
owner_id = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-full-n-flag-powerset-pict-pairwise"
start_byte = 308868
end_byte = 315478
sha256 = "13d4335dbf54e3d4636c6726735872367b0067b14fded5521bd2523056d0320c"
source_kind = "record"
owner_id = "testing.full-n-flag-powerset-pict-pairwise"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"
start_byte = 315478
end_byte = 315732
sha256 = "fdc962bae38c253e1c1439920e8044945959ac2d430b460f9df9113636401d36"
source_kind = "record"
owner_id = "testing.quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-orphan-fixture-directory-meta-test"
start_byte = 315732
end_byte = 316007
sha256 = "6ca577e74cc4fd8a7a4335cafc7f01596660568069f64869a854a6c915ec7213"
source_kind = "record"
owner_id = "testing.orphan-fixture-directory-meta-test"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-restructuring-features-std-tool-owned-sub-feature-cddl"
start_byte = 316007
end_byte = 317844
sha256 = "6968f8c72e45427bf8c9988468880153e17f0a5ca7a1e566105c4e1d6b6abb3d"
source_kind = "record"
owner_id = "testing.restructuring-features-std-tool-owned-sub-feature-cddl"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-ownership-journal-making-dropped-std-forward-dep-converge"
start_byte = 317844
end_byte = 318839
sha256 = "90dd0e035f234c70e9409d022099b82680b3391ef326ab5f98165885e2a00b2f"
source_kind = "record"
owner_id = "testing.ownership-journal-making-dropped-std-forward-dep-converge"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-wrapper-name-user-set-name-synthesized-collection-wrapper"
start_byte = 318839
end_byte = 321107
sha256 = "83d653ec6446e1b66babf54d2c03123bf07a64c5eb15400211128c44190edddb"
source_kind = "record"
owner_id = "testing.wrapper-name-user-set-name-synthesized-collection-wrapper"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-record-checking-runtime-flavor-s-safety-condition-rather-stating"
start_byte = 321107
end_byte = 323209
sha256 = "42b78061de298d44cf32cbb78c34d6d308f453fe6d2f18a5b6023633aa25ecb9"
source_kind = "record"
owner_id = "testing.checking-runtime-flavor-s-safety-condition-rather-stating"
owner_field = "payload.detail_md"
migration_status = "replaced"

[[source_span]]
id = "span-section-sources"
start_byte = 323209
end_byte = 323220
sha256 = "8a23b2f7befd3e34e37db86c490ee38b226bd1b0a95bf35097ad88cde8dcfec7"
source_kind = "section"
owner_id = "sources"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-fragment-sources-exhaustive-menu"
start_byte = 323220
end_byte = 323327
sha256 = "1a4976cd268645a9c093de41b4ae6b2c59d73b3a45c5ab1fb71e7732362184ee"
source_kind = "fragment"
owner_id = "sources-exhaustive-menu"
owner_field = "body_md"
migration_status = "replaced"

[[source_span]]
id = "span-fragment-sources-expert-writeups"
start_byte = 323327
end_byte = 323398
sha256 = "6a6538e08cd6f2c2d26df1d60be9d49295bf3947003b96bddeb845fd0f638ce9"
source_kind = "fragment"
owner_id = "sources-expert-writeups"
owner_field = "body_md"
migration_status = "replaced"

[[relation]]
source = "testing.add-profile-wrapper-participation-grid-participation-differs-across"
kind = "parent_of"
target = "testing.guard.add-profile-wrapper-participation-grid-participation-differs-across"

[[relation]]
source = "testing.add-profile-wrapper-participation-grid-participation-differs-across"
kind = "parent_of"
target = "testing.source-observation.add-profile-wrapper-participation-grid-participation-differs-across"

[[relation]]
source = "testing.add-profile-wrapper-participation-grid-participation-differs-across"
kind = "parent_of"
target = "testing.trigger.add-profile-wrapper-participation-grid-participation-differs-across"

[[relation]]
source = "testing.alloc-imports-nested-inline-modules-hand-carried-design"
kind = "parent_of"
target = "testing.guard.alloc-imports-nested-inline-modules-hand-carried-design"

[[relation]]
source = "testing.alloc-imports-nested-inline-modules-hand-carried-design"
kind = "parent_of"
target = "testing.source-observation.alloc-imports-nested-inline-modules-hand-carried-design"

[[relation]]
source = "testing.alloc-imports-nested-inline-modules-hand-carried-design"
kind = "parent_of"
target = "testing.trigger.alloc-imports-nested-inline-modules-hand-carried-design"

[[relation]]
source = "testing.armed-idle-harness-arms-empty-head-ledgers-zero"
kind = "parent_of"
target = "testing.guard.armed-idle-harness-arms-empty-head-ledgers-zero"

[[relation]]
source = "testing.armed-idle-harness-arms-empty-head-ledgers-zero"
kind = "parent_of"
target = "testing.source-observation.armed-idle-harness-arms-empty-head-ledgers-zero"

[[relation]]
source = "testing.armed-idle-harness-arms-empty-head-ledgers-zero"
kind = "parent_of"
target = "testing.trigger.armed-idle-harness-arms-empty-head-ledgers-zero"

[[relation]]
source = "testing.assert-hand-authored-json-schema-against-type-s"
kind = "parent_of"
target = "testing.source-observation.assert-hand-authored-json-schema-against-type-s"

[[relation]]
source = "testing.assert-hand-authored-json-schema-against-type-s"
kind = "parent_of"
target = "testing.trigger.assert-hand-authored-json-schema-against-type-s"

[[relation]]
source = "testing.backgrounded-check-ts-full-launched-sub-agent-s"
kind = "parent_of"
target = "testing.source-observation.backgrounded-check-ts-full-launched-sub-agent-s"

[[relation]]
source = "testing.build-registration-class-reference-position-sweep-second-escape"
kind = "parent_of"
target = "testing.admission-path.build-registration-class-reference-position-sweep-second-escape"

[[relation]]
source = "testing.build-registration-class-reference-position-sweep-second-escape"
kind = "parent_of"
target = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one"

[[relation]]
source = "testing.build-registration-class-reference-position-sweep-second-escape"
kind = "parent_of"
target = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one-proof"

[[relation]]
source = "testing.build-registration-class-reference-position-sweep-second-escape"
kind = "parent_of"
target = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two"

[[relation]]
source = "testing.build-registration-class-reference-position-sweep-second-escape"
kind = "parent_of"
target = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two-proof"

[[relation]]
source = "testing.cargo-feature-generated-crate-s-dependency-change-what"
kind = "parent_of"
target = "testing.guard.cargo-feature-generated-crate-s-dependency-change-what"

[[relation]]
source = "testing.cargo-feature-generated-crate-s-dependency-change-what"
kind = "parent_of"
target = "testing.source-observation.cargo-feature-generated-crate-s-dependency-change-what"

[[relation]]
source = "testing.cargo-feature-generated-crate-s-dependency-change-what"
kind = "parent_of"
target = "testing.trigger.cargo-feature-generated-crate-s-dependency-change-what"

[[relation]]
source = "testing.cddl-module-directives-draft-ietf-cbor-cddl-modules"
kind = "parent_of"
target = "testing.source-observation.cddl-module-directives-draft-ietf-cbor-cddl-modules"

[[relation]]
source = "testing.cddl-module-directives-draft-ietf-cbor-cddl-modules"
kind = "parent_of"
target = "testing.trigger.cddl-module-directives-draft-ietf-cbor-cddl-modules"

[[relation]]
source = "testing.checking-runtime-flavor-s-safety-condition-rather-stating"
kind = "parent_of"
target = "testing.guard.checking-runtime-flavor-s-safety-condition-rather-stating"

[[relation]]
source = "testing.citation-resolves-naming-wrong-thing-invisible-citation-lint"
kind = "parent_of"
target = "testing.guard.citation-resolves-naming-wrong-thing-invisible-citation-lint"

[[relation]]
source = "testing.citation-resolves-naming-wrong-thing-invisible-citation-lint"
kind = "parent_of"
target = "testing.source-observation.citation-resolves-naming-wrong-thing-invisible-citation-lint"

[[relation]]
source = "testing.citation-resolves-naming-wrong-thing-invisible-citation-lint"
kind = "parent_of"
target = "testing.trigger.citation-resolves-naming-wrong-thing-invisible-citation-lint"

[[relation]]
source = "testing.comment-residue-false-matches-text-scans-over-emitted"
kind = "parent_of"
target = "testing.guard.comment-residue-false-matches-text-scans-over-emitted"

[[relation]]
source = "testing.comment-residue-false-matches-text-scans-over-emitted"
kind = "parent_of"
target = "testing.source-observation.comment-residue-false-matches-text-scans-over-emitted"

[[relation]]
source = "testing.comment-residue-false-matches-text-scans-over-emitted"
kind = "parent_of"
target = "testing.trigger.comment-residue-false-matches-text-scans-over-emitted"

[[relation]]
source = "testing.complete-cargo-mutants-sweep-will-never-run"
kind = "parent_of"
target = "testing.guard.complete-cargo-mutants-sweep-will-never-run"

[[relation]]
source = "testing.component-js-probe.reopening-signal"
kind = "parent_of"
target = "testing.source-observation.component-js-probe-reopening-signal"

[[relation]]
source = "testing.composing-registration-rows-across-generation-passes-share-output"
kind = "parent_of"
target = "testing.source-observation.composing-registration-rows-across-generation-passes-share-output"

[[relation]]
source = "testing.composing-registration-rows-across-generation-passes-share-output"
kind = "parent_of"
target = "testing.trigger.composing-registration-rows-across-generation-passes-share-output"

[[relation]]
source = "testing.config-run-s-convergence-warning-name-crate-already"
kind = "parent_of"
target = "testing.proof-gap.config-run-s-convergence-warning-name-crate-already"

[[relation]]
source = "testing.config-run-s-convergence-warning-name-crate-already"
kind = "parent_of"
target = "testing.source-observation.config-run-s-convergence-warning-name-crate-already"

[[relation]]
source = "testing.config-run-s-convergence-warning-name-crate-already"
kind = "parent_of"
target = "testing.trigger.config-run-s-convergence-warning-name-crate-already"

[[relation]]
source = "testing.container-combinations.reopening-signal"
kind = "parent_of"
target = "testing.source-observation.container-combinations-reopening-signal"

[[relation]]
source = "testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
kind = "parent_of"
target = "testing.container-combinations.reopening-signal"

[[relation]]
source = "testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
kind = "parent_of"
target = "testing.source-observation.container-construct-conceptual-type-visitor-walks-flat-combinatorial"

[[relation]]
source = "testing.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
kind = "parent_of"
target = "testing.trigger.container-construct-conceptual-type-visitor-walks-flat-combinatorial"

[[relation]]
source = "testing.containment-note-s-stated-reason-go-false-any"
kind = "parent_of"
target = "testing.guard.containment-note-s-stated-reason-go-false-any"

[[relation]]
source = "testing.containment-note-s-stated-reason-go-false-any"
kind = "parent_of"
target = "testing.source-observation.containment-note-s-stated-reason-go-false-any"

[[relation]]
source = "testing.containment-note-s-stated-reason-go-false-any"
kind = "parent_of"
target = "testing.trigger.containment-note-s-stated-reason-go-false-any"

[[relation]]
source = "testing.control-bytes-committed-source-files-proven-instance-machinery"
kind = "parent_of"
target = "testing.guard.control-bytes-committed-source-files-proven-instance-machinery"

[[relation]]
source = "testing.control-bytes-committed-source-files-proven-instance-machinery"
kind = "parent_of"
target = "testing.source-observation.control-bytes-committed-source-files-proven-instance-machinery"

[[relation]]
source = "testing.control-bytes-committed-source-files-proven-instance-machinery"
kind = "parent_of"
target = "testing.trigger.control-bytes-committed-source-files-proven-instance-machinery"

[[relation]]
source = "testing.corpus-wide-regen-over-prior-output-sweep-reaches"
kind = "parent_of"
target = "testing.guard.corpus-wide-regen-over-prior-output-sweep-reaches"

[[relation]]
source = "testing.corpus-wide-regen-over-prior-output-sweep-reaches"
kind = "parent_of"
target = "testing.source-observation.corpus-wide-regen-over-prior-output-sweep-reaches"

[[relation]]
source = "testing.corpus-wide-regen-over-prior-output-sweep-reaches"
kind = "parent_of"
target = "testing.trigger.corpus-wide-regen-over-prior-output-sweep-reaches"

[[relation]]
source = "testing.crate-generates-json-schema-document-aggregate-package"
kind = "parent_of"
target = "testing.source-observation.crate-generates-json-schema-document-aggregate-package"

[[relation]]
source = "testing.crate-generates-json-schema-document-aggregate-package"
kind = "parent_of"
target = "testing.trigger.crate-generates-json-schema-document-aggregate-package"

[[relation]]
source = "testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite"
kind = "parent_of"
target = "testing.source-observation.cross-version-preserve-vectors-beyond-std-alloc-rewrite"

[[relation]]
source = "testing.cross-version-preserve-vectors-beyond-std-alloc-rewrite"
kind = "parent_of"
target = "testing.trigger.cross-version-preserve-vectors-beyond-std-alloc-rewrite"

[[relation]]
source = "testing.dependency-swap-pass-compile-parity-existing-gate-silently"
kind = "parent_of"
target = "testing.guard.dependency-swap-pass-compile-parity-existing-gate-silently"

[[relation]]
source = "testing.dependency-swap-pass-compile-parity-existing-gate-silently"
kind = "parent_of"
target = "testing.source-observation.dependency-swap-pass-compile-parity-existing-gate-silently"

[[relation]]
source = "testing.dependency-swap-pass-compile-parity-existing-gate-silently"
kind = "parent_of"
target = "testing.trigger.dependency-swap-pass-compile-parity-existing-gate-silently"

[[relation]]
source = "testing.dependency-version-range-resolution-untested-generated-crates-float"
kind = "parent_of"
target = "testing.source-observation.dependency-version-range-resolution-untested-generated-crates-float"

[[relation]]
source = "testing.dependency-version-range-resolution-untested-generated-crates-float"
kind = "parent_of"
target = "testing.trigger.dependency-version-range-resolution-untested-generated-crates-float"

[[relation]]
source = "testing.directive-dependent-emitter-edge-case-triggering-shape-hand"
kind = "parent_of"
target = "testing.guard.directive-dependent-emitter-edge-case-triggering-shape-hand"

[[relation]]
source = "testing.directive-dependent-emitter-edge-case-triggering-shape-hand"
kind = "parent_of"
target = "testing.source-observation.directive-dependent-emitter-edge-case-triggering-shape-hand"

[[relation]]
source = "testing.directive-dependent-emitter-edge-case-triggering-shape-hand"
kind = "parent_of"
target = "testing.trigger.directive-dependent-emitter-edge-case-triggering-shape-hand"

[[relation]]
source = "testing.directive-effect-round-trip-coherence-sweep-accepted-custom"
kind = "parent_of"
target = "testing.admission-path.directive-effect-round-trip-coherence-sweep-accepted-custom"

[[relation]]
source = "testing.directive-effect-round-trip-coherence-sweep-accepted-custom"
kind = "parent_of"
target = "testing.proof.directive-effect-round-trip-coherence-sweep-accepted-custom"

[[relation]]
source = "testing.directive-s-documented-sentence-stay-true-consumer-visible"
kind = "parent_of"
target = "testing.guard.directive-s-documented-sentence-stay-true-consumer-visible"

[[relation]]
source = "testing.directive-s-documented-sentence-stay-true-consumer-visible"
kind = "parent_of"
target = "testing.source-observation.directive-s-documented-sentence-stay-true-consumer-visible"

[[relation]]
source = "testing.directive-s-documented-sentence-stay-true-consumer-visible"
kind = "parent_of"
target = "testing.trigger.directive-s-documented-sentence-stay-true-consumer-visible"

[[relation]]
source = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
kind = "parent_of"
target = "testing.guard.docs-vs-behavior-conformance-harness-comment-dsl-mdx"

[[relation]]
source = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
kind = "parent_of"
target = "testing.source-observation.docs-vs-behavior-conformance-harness-comment-dsl-mdx"

[[relation]]
source = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
kind = "parent_of"
target = "testing.trigger.docs-vs-behavior-conformance-harness-comment-dsl-mdx"

[[relation]]
source = "testing.documented-failure-shape-exercises-prose-cross-crate-int"
kind = "parent_of"
target = "testing.guard.documented-failure-shape-exercises-prose-cross-crate-int"

[[relation]]
source = "testing.documented-failure-shape-exercises-prose-cross-crate-int"
kind = "parent_of"
target = "testing.source-observation.documented-failure-shape-exercises-prose-cross-crate-int"

[[relation]]
source = "testing.documented-failure-shape-exercises-prose-cross-crate-int"
kind = "parent_of"
target = "testing.trigger.documented-failure-shape-exercises-prose-cross-crate-int"

[[relation]]
source = "testing.documented-flag-pairing-broken-fixture-stays-green-because"
kind = "parent_of"
target = "testing.guard.documented-flag-pairing-broken-fixture-stays-green-because"

[[relation]]
source = "testing.documented-flag-pairing-broken-fixture-stays-green-because"
kind = "parent_of"
target = "testing.source-observation.documented-flag-pairing-broken-fixture-stays-green-because"

[[relation]]
source = "testing.documented-flag-pairing-broken-fixture-stays-green-because"
kind = "parent_of"
target = "testing.trigger.documented-flag-pairing-broken-fixture-stays-green-because"

[[relation]]
source = "testing.doubled-doc-comment-markers-emitted-docs-proven-instance"
kind = "parent_of"
target = "testing.guard.doubled-doc-comment-markers-emitted-docs-proven-instance"

[[relation]]
source = "testing.doubled-doc-comment-markers-emitted-docs-proven-instance"
kind = "parent_of"
target = "testing.source-observation.doubled-doc-comment-markers-emitted-docs-proven-instance"

[[relation]]
source = "testing.doubled-doc-comment-markers-emitted-docs-proven-instance"
kind = "parent_of"
target = "testing.trigger.doubled-doc-comment-markers-emitted-docs-proven-instance"

[[relation]]
source = "testing.dsl-position-tests-string-level-design-green-emitted"
kind = "parent_of"
target = "testing.guard.dsl-position-tests-string-level-design-green-emitted"

[[relation]]
source = "testing.dsl-position-tests-string-level-design-green-emitted"
kind = "parent_of"
target = "testing.source-observation.dsl-position-tests-string-level-design-green-emitted"

[[relation]]
source = "testing.dsl-position-tests-string-level-design-green-emitted"
kind = "parent_of"
target = "testing.trigger.dsl-position-tests-string-level-design-green-emitted"

[[relation]]
source = "testing.duplicates-policy-residuals"
kind = "parent_of"
target = "testing.duplicates.dedicated-generic-collision-message"

[[relation]]
source = "testing.duplicates-policy-residuals"
kind = "parent_of"
target = "testing.duplicates.member-position-extension"

[[relation]]
source = "testing.duplicates-policy-residuals"
kind = "parent_of"
target = "testing.duplicates.occurrence-aware-generic-identity"

[[relation]]
source = "testing.duplicates-policy-residuals"
kind = "parent_of"
target = "testing.duplicates.same-shape-set-conversion"

[[relation]]
source = "testing.duplicates.dedicated-generic-collision-message"
kind = "parent_of"
target = "testing.guard.duplicates-dedicated-generic-collision-message"

[[relation]]
source = "testing.duplicates.dedicated-generic-collision-message"
kind = "parent_of"
target = "testing.source-observation.duplicates-dedicated-generic-collision-message"

[[relation]]
source = "testing.duplicates.dedicated-generic-collision-message"
kind = "parent_of"
target = "testing.trigger.duplicates-dedicated-generic-collision-message"

[[relation]]
source = "testing.duplicates.member-position-extension"
kind = "parent_of"
target = "testing.source-observation.duplicates-member-position-extension"

[[relation]]
source = "testing.duplicates.member-position-extension"
kind = "parent_of"
target = "testing.trigger.duplicates-member-position-extension"

[[relation]]
source = "testing.duplicates.occurrence-aware-generic-identity"
kind = "parent_of"
target = "testing.source-observation.duplicates-occurrence-aware-generic-identity"

[[relation]]
source = "testing.duplicates.occurrence-aware-generic-identity"
kind = "parent_of"
target = "testing.trigger.duplicates-occurrence-aware-generic-identity"

[[relation]]
source = "testing.duplicates.same-shape-set-conversion"
kind = "parent_of"
target = "testing.source-observation.duplicates-same-shape-set-conversion"

[[relation]]
source = "testing.duplicates.same-shape-set-conversion"
kind = "parent_of"
target = "testing.trigger.duplicates-same-shape-set-conversion"

[[relation]]
source = "testing.e0463-t-find-crate-core-scratch-tree-std"
kind = "parent_of"
target = "testing.trigger.e0463-t-find-crate-core-scratch-tree-std"

[[relation]]
source = "testing.edn-text-notation-round-trip-edn-edn-draft"
kind = "parent_of"
target = "testing.source-observation.edn-text-notation-round-trip-edn-edn-draft"

[[relation]]
source = "testing.edn-text-notation-round-trip-edn-edn-draft"
kind = "parent_of"
target = "testing.trigger.edn-text-notation-round-trip-edn-edn-draft"

[[relation]]
source = "testing.emission-branch-generated-output-fixture-exercises-invisible-gate"
kind = "parent_of"
target = "testing.guard.emission-branch-generated-output-fixture-exercises-invisible-gate"

[[relation]]
source = "testing.emission-branch-generated-output-fixture-exercises-invisible-gate"
kind = "parent_of"
target = "testing.source-observation.emission-branch-generated-output-fixture-exercises-invisible-gate"

[[relation]]
source = "testing.emission-branch-generated-output-fixture-exercises-invisible-gate"
kind = "parent_of"
target = "testing.trigger.emission-branch-generated-output-fixture-exercises-invisible-gate"

[[relation]]
source = "testing.emit-tests-gate-asserts-round-trip-green-cannot"
kind = "parent_of"
target = "testing.guard.emit-tests-gate-asserts-round-trip-green-cannot"

[[relation]]
source = "testing.emit-tests-gate-asserts-round-trip-green-cannot"
kind = "parent_of"
target = "testing.source-observation.emit-tests-gate-asserts-round-trip-green-cannot"

[[relation]]
source = "testing.emit-tests-gate-asserts-round-trip-green-cannot"
kind = "parent_of"
target = "testing.trigger.emit-tests-gate-asserts-round-trip-green-cannot"

[[relation]]
source = "testing.emitted-shape-lint-classes-outside-clippy-all-beyond"
kind = "parent_of"
target = "testing.guard.emitted-shape-lint-classes-outside-clippy-all-beyond"

[[relation]]
source = "testing.emitted-shape-lint-classes-outside-clippy-all-beyond"
kind = "parent_of"
target = "testing.source-observation.emitted-shape-lint-classes-outside-clippy-all-beyond"

[[relation]]
source = "testing.emitted-shape-lint-classes-outside-clippy-all-beyond"
kind = "parent_of"
target = "testing.trigger.emitted-shape-lint-classes-outside-clippy-all-beyond"

[[relation]]
source = "testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
kind = "parent_of"
target = "testing.guard.emitter-drops-overloadable-parameter-building-fresh-config-invisible"

[[relation]]
source = "testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
kind = "parent_of"
target = "testing.source-observation.emitter-drops-overloadable-parameter-building-fresh-config-invisible"

[[relation]]
source = "testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
kind = "parent_of"
target = "testing.trigger.emitter-drops-overloadable-parameter-building-fresh-config-invisible"

[[relation]]
source = "testing.exclusion-reasons-informational-interchange"
kind = "parent_of"
target = "testing.guard.exclusion-reasons-informational-interchange"

[[relation]]
source = "testing.extend-recombination-member-kind-table-optional-fixed-shape"
kind = "parent_of"
target = "testing.guard.extend-recombination-member-kind-table-optional-fixed-shape"

[[relation]]
source = "testing.extend-recombination-member-kind-table-optional-fixed-shape"
kind = "parent_of"
target = "testing.source-observation.extend-recombination-member-kind-table-optional-fixed-shape"

[[relation]]
source = "testing.extend-recombination-member-kind-table-optional-fixed-shape"
kind = "parent_of"
target = "testing.trigger.extend-recombination-member-kind-table-optional-fixed-shape"

[[relation]]
source = "testing.extern-deps-wasm-boundary-surface-packaging-json-gen"
kind = "parent_of"
target = "testing.guard.extern-deps-wasm-boundary-surface-packaging-json-gen"

[[relation]]
source = "testing.extern-deps-wasm-boundary-surface-packaging-json-gen"
kind = "parent_of"
target = "testing.source-observation.extern-deps-wasm-boundary-surface-packaging-json-gen"

[[relation]]
source = "testing.extern-deps-wasm-boundary-surface-packaging-json-gen"
kind = "parent_of"
target = "testing.trigger.extern-deps-wasm-boundary-surface-packaging-json-gen"

[[relation]]
source = "testing.extern-interface-export-dialect-v2-candidates"
kind = "parent_of"
target = "testing.source-observation.extern-interface-export-dialect-v2-candidates"

[[relation]]
source = "testing.extern-interface-export-dialect-v2-candidates"
kind = "parent_of"
target = "testing.trigger.extern-interface-export-dialect-v2-candidates"

[[relation]]
source = "testing.extreme-value-boundary-correctness-generation-time-encode-paths"
kind = "parent_of"
target = "testing.guard.extreme-value-boundary-correctness-generation-time-encode-paths"

[[relation]]
source = "testing.extreme-value-boundary-correctness-generation-time-encode-paths"
kind = "parent_of"
target = "testing.source-observation.extreme-value-boundary-correctness-generation-time-encode-paths"

[[relation]]
source = "testing.extreme-value-boundary-correctness-generation-time-encode-paths"
kind = "parent_of"
target = "testing.trigger.extreme-value-boundary-correctness-generation-time-encode-paths"

[[relation]]
source = "testing.file-listing-json-schema-root-values-flag-per"
kind = "parent_of"
target = "testing.source-observation.file-listing-json-schema-root-values-flag-per"

[[relation]]
source = "testing.file-listing-json-schema-root-values-flag-per"
kind = "parent_of"
target = "testing.trigger.file-listing-json-schema-root-values-flag-per"

[[relation]]
source = "testing.finding-s-defect-side-attribution-recorded-narrative-plausibility"
kind = "parent_of"
target = "testing.guard.finding-s-defect-side-attribution-recorded-narrative-plausibility"

[[relation]]
source = "testing.finding-s-defect-side-attribution-recorded-narrative-plausibility"
kind = "parent_of"
target = "testing.source-observation.finding-s-defect-side-attribution-recorded-narrative-plausibility"

[[relation]]
source = "testing.finding-s-defect-side-attribution-recorded-narrative-plausibility"
kind = "parent_of"
target = "testing.trigger.finding-s-defect-side-attribution-recorded-narrative-plausibility"

[[relation]]
source = "testing.fixture-models-obsolete-user-contract-stays-green-real"
kind = "parent_of"
target = "testing.guard.fixture-models-obsolete-user-contract-stays-green-real"

[[relation]]
source = "testing.fixture-models-obsolete-user-contract-stays-green-real"
kind = "parent_of"
target = "testing.source-observation.fixture-models-obsolete-user-contract-stays-green-real"

[[relation]]
source = "testing.fixture-models-obsolete-user-contract-stays-green-real"
kind = "parent_of"
target = "testing.trigger.fixture-models-obsolete-user-contract-stays-green-real"

[[relation]]
source = "testing.fixture-s-verdict-accident-cargo-feature-unification-harness"
kind = "parent_of"
target = "testing.guard.fixture-s-verdict-accident-cargo-feature-unification-harness"

[[relation]]
source = "testing.fixture-s-verdict-accident-cargo-feature-unification-harness"
kind = "parent_of"
target = "testing.source-observation.fixture-s-verdict-accident-cargo-feature-unification-harness"

[[relation]]
source = "testing.fixture-s-verdict-accident-cargo-feature-unification-harness"
kind = "parent_of"
target = "testing.trigger.fixture-s-verdict-accident-cargo-feature-unification-harness"

[[relation]]
source = "testing.fixture-shape-evicted-over-known-defect-stale-guard"
kind = "parent_of"
target = "testing.source-observation.fixture-shape-evicted-over-known-defect-stale-guard"

[[relation]]
source = "testing.fixture-shape-evicted-over-known-defect-stale-guard"
kind = "parent_of"
target = "testing.trigger.fixture-shape-evicted-over-known-defect-stale-guard"

[[relation]]
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "parent_of"
target = "testing.guard.full-n-flag-powerset-pict-pairwise"

[[relation]]
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "parent_of"
target = "testing.source-observation.full-n-flag-powerset-pict-pairwise"

[[relation]]
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "parent_of"
target = "testing.trigger.full-n-flag-powerset-pict-pairwise"

[[relation]]
source = "testing.full-suite-flake-attributed-hardened-acquire-scratch-lock"
kind = "parent_of"
target = "testing.trigger.full-suite-flake-attributed-hardened-acquire-scratch-lock"

[[relation]]
source = "testing.gate-cache-closure-audit-structurally-blind-gate-authored"
kind = "parent_of"
target = "testing.guard.gate-cache-closure-audit-structurally-blind-gate-authored"

[[relation]]
source = "testing.gate-cache-closure-audit-structurally-blind-gate-authored"
kind = "parent_of"
target = "testing.source-observation.gate-cache-closure-audit-structurally-blind-gate-authored"

[[relation]]
source = "testing.gate-cache-closure-audit-structurally-blind-gate-authored"
kind = "parent_of"
target = "testing.trigger.gate-cache-closure-audit-structurally-blind-gate-authored"

[[relation]]
source = "testing.gate-cache-costs"
kind = "parent_of"
target = "testing.gate-cache.all-hit-cost"

[[relation]]
source = "testing.gate-cache-costs"
kind = "parent_of"
target = "testing.gate-cache.closure-audit-traced-set-extension"

[[relation]]
source = "testing.gate-cache-costs"
kind = "parent_of"
target = "testing.gate-cache.coverage-extensions"

[[relation]]
source = "testing.gate-cache.all-hit-cost"
kind = "parent_of"
target = "testing.source-observation.gate-cache-all-hit-cost"

[[relation]]
source = "testing.gate-cache.all-hit-cost"
kind = "parent_of"
target = "testing.trigger.gate-cache-all-hit-cost"

[[relation]]
source = "testing.gate-cache.closure-audit-traced-set-extension"
kind = "parent_of"
target = "testing.source-observation.gate-cache-closure-audit-traced-set-extension"

[[relation]]
source = "testing.gate-cache.closure-audit-traced-set-extension"
kind = "parent_of"
target = "testing.trigger.gate-cache-closure-audit-traced-set-extension"

[[relation]]
source = "testing.gate-cache.coverage-extensions"
kind = "parent_of"
target = "testing.source-observation.gate-cache-coverage-extensions"

[[relation]]
source = "testing.gate-cache.coverage-extensions"
kind = "parent_of"
target = "testing.trigger.gate-cache-coverage-extensions"

[[relation]]
source = "testing.gate-demands-premise-probed-against-gate-evidence-about"
kind = "parent_of"
target = "testing.admission-path.gate-demands-premise-probed-against-gate-evidence-about"

[[relation]]
source = "testing.gate-demands-premise-probed-against-gate-evidence-about"
kind = "parent_of"
target = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one"

[[relation]]
source = "testing.gate-demands-premise-probed-against-gate-evidence-about"
kind = "parent_of"
target = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one-proof"

[[relation]]
source = "testing.gate-demands-premise-probed-against-gate-evidence-about"
kind = "parent_of"
target = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two"

[[relation]]
source = "testing.gate-demands-premise-probed-against-gate-evidence-about"
kind = "parent_of"
target = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two-proof"

[[relation]]
source = "testing.gate-exists-registry-nowhere-tests-readme-md-hand"
kind = "parent_of"
target = "testing.guard.gate-exists-registry-nowhere-tests-readme-md-hand"

[[relation]]
source = "testing.gate-exists-registry-nowhere-tests-readme-md-hand"
kind = "parent_of"
target = "testing.source-observation.gate-exists-registry-nowhere-tests-readme-md-hand"

[[relation]]
source = "testing.gate-exists-registry-nowhere-tests-readme-md-hand"
kind = "parent_of"
target = "testing.trigger.gate-exists-registry-nowhere-tests-readme-md-hand"

[[relation]]
source = "testing.gate-script-health-cache-staleness-key-fresh-worktree"
kind = "parent_of"
target = "testing.guard.gate-script-health-cache-staleness-key-fresh-worktree"

[[relation]]
source = "testing.gate-script-health-cache-staleness-key-fresh-worktree"
kind = "parent_of"
target = "testing.source-observation.gate-script-health-cache-staleness-key-fresh-worktree"

[[relation]]
source = "testing.gate-script-health-cache-staleness-key-fresh-worktree"
kind = "parent_of"
target = "testing.trigger.gate-script-health-cache-staleness-key-fresh-worktree"

[[relation]]
source = "testing.generated-local-collision-class-refused-mangled-refusal-s"
kind = "parent_of"
target = "testing.generated-local-collision.general-mangling-remedy"

[[relation]]
source = "testing.generated-local-collision-class-refused-mangled-refusal-s"
kind = "parent_of"
target = "testing.generated-local-collision.scope-wide-probe"

[[relation]]
source = "testing.generated-local-collision.general-mangling-remedy"
kind = "parent_of"
target = "testing.source-observation.generated-local-collision-general-mangling-remedy"

[[relation]]
source = "testing.generated-local-collision.general-mangling-remedy"
kind = "parent_of"
target = "testing.trigger.generated-local-collision-general-mangling-remedy"

[[relation]]
source = "testing.generated-local-collision.scope-wide-probe"
kind = "parent_of"
target = "testing.guard.generated-local-collision-scope-wide-probe"

[[relation]]
source = "testing.generated-local-collision.scope-wide-probe"
kind = "parent_of"
target = "testing.source-observation.generated-local-collision-scope-wide-probe"

[[relation]]
source = "testing.generated-local-collision.scope-wide-probe"
kind = "parent_of"
target = "testing.trigger.generated-local-collision-scope-wide-probe"

[[relation]]
source = "testing.generic-raw-bytes-base-refused-generic-extern-base"
kind = "parent_of"
target = "testing.guard.generic-raw-bytes-base-refused-generic-extern-base"

[[relation]]
source = "testing.grammar-fuzzer-escalations"
kind = "parent_of"
target = "testing.grammar-fuzzer.arbitrary-supported-ast"

[[relation]]
source = "testing.grammar-fuzzer-escalations"
kind = "parent_of"
target = "testing.grammar-fuzzer.batch-masking-detector"

[[relation]]
source = "testing.grammar-fuzzer-escalations"
kind = "parent_of"
target = "testing.grammar-fuzzer.embed-site-alias-roots"

[[relation]]
source = "testing.grammar-fuzzer-escalations"
kind = "parent_of"
target = "testing.grammar-fuzzer.real-world-corpus-differential"

[[relation]]
source = "testing.grammar-fuzzer.arbitrary-supported-ast"
kind = "parent_of"
target = "testing.source-observation.grammar-fuzzer-arbitrary-supported-ast"

[[relation]]
source = "testing.grammar-fuzzer.arbitrary-supported-ast"
kind = "parent_of"
target = "testing.trigger.grammar-fuzzer-arbitrary-supported-ast"

[[relation]]
source = "testing.grammar-fuzzer.batch-masking-detector"
kind = "parent_of"
target = "testing.guard.grammar-fuzzer-batch-masking-detector"

[[relation]]
source = "testing.grammar-fuzzer.batch-masking-detector"
kind = "parent_of"
target = "testing.source-observation.grammar-fuzzer-batch-masking-detector"

[[relation]]
source = "testing.grammar-fuzzer.batch-masking-detector"
kind = "parent_of"
target = "testing.trigger.grammar-fuzzer-batch-masking-detector"

[[relation]]
source = "testing.grammar-fuzzer.embed-site-alias-roots"
kind = "parent_of"
target = "testing.admission-path.grammar-fuzzer-embed-site-alias-roots"

[[relation]]
source = "testing.grammar-fuzzer.embed-site-alias-roots"
kind = "parent_of"
target = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one"

[[relation]]
source = "testing.grammar-fuzzer.embed-site-alias-roots"
kind = "parent_of"
target = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one-proof"

[[relation]]
source = "testing.grammar-fuzzer.embed-site-alias-roots"
kind = "parent_of"
target = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two"

[[relation]]
source = "testing.grammar-fuzzer.embed-site-alias-roots"
kind = "parent_of"
target = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two-proof"

[[relation]]
source = "testing.grammar-fuzzer.real-world-corpus-differential"
kind = "parent_of"
target = "testing.source-observation.grammar-fuzzer-real-world-corpus-differential"

[[relation]]
source = "testing.grammar-fuzzer.real-world-corpus-differential"
kind = "parent_of"
target = "testing.trigger.grammar-fuzzer-real-world-corpus-differential"

[[relation]]
source = "testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
kind = "parent_of"
target = "testing.guard.hand-enumerated-ban-pattern-list-authority-lives-gitignored"

[[relation]]
source = "testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
kind = "parent_of"
target = "testing.source-observation.hand-enumerated-ban-pattern-list-authority-lives-gitignored"

[[relation]]
source = "testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
kind = "parent_of"
target = "testing.trigger.hand-enumerated-ban-pattern-list-authority-lives-gitignored"

[[relation]]
source = "testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
kind = "parent_of"
target = "testing.guard.hand-maintained-source-dependency-mapping-manifest-changeset-silently"

[[relation]]
source = "testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
kind = "parent_of"
target = "testing.source-observation.hand-maintained-source-dependency-mapping-manifest-changeset-silently"

[[relation]]
source = "testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
kind = "parent_of"
target = "testing.trigger.hand-maintained-source-dependency-mapping-manifest-changeset-silently"

[[relation]]
source = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one"
kind = "parent_of"
target = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one-proof"

[[relation]]
source = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two"
kind = "parent_of"
target = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two-proof"

[[relation]]
source = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one"
kind = "parent_of"
target = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one-proof"

[[relation]]
source = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two"
kind = "parent_of"
target = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two-proof"

[[relation]]
source = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one"
kind = "parent_of"
target = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one-proof"

[[relation]]
source = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two"
kind = "parent_of"
target = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two-proof"

[[relation]]
source = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one"
kind = "parent_of"
target = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one-proof"

[[relation]]
source = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two"
kind = "parent_of"
target = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two-proof"

[[relation]]
source = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one"
kind = "parent_of"
target = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one-proof"

[[relation]]
source = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two"
kind = "parent_of"
target = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two-proof"

[[relation]]
source = "testing.json2ts-artifact-doc-comments-naming-undefined"
kind = "parent_of"
target = "testing.source-observation.json2ts-artifact-doc-comments-naming-undefined"

[[relation]]
source = "testing.json2ts-artifact-doc-comments-naming-undefined"
kind = "parent_of"
target = "testing.trigger.json2ts-artifact-doc-comments-naming-undefined"

[[relation]]
source = "testing.lint-doc-citations-accepts-docs-heading-durable-citation"
kind = "parent_of"
target = "testing.guard.lint-doc-citations-accepts-docs-heading-durable-citation"

[[relation]]
source = "testing.lint-doc-citations-accepts-docs-heading-durable-citation"
kind = "parent_of"
target = "testing.source-observation.lint-doc-citations-accepts-docs-heading-durable-citation"

[[relation]]
source = "testing.lint-doc-citations-accepts-docs-heading-durable-citation"
kind = "parent_of"
target = "testing.trigger.lint-doc-citations-accepts-docs-heading-durable-citation"

[[relation]]
source = "testing.logic-tool-emits-source-text-outside-layer-reasons"
kind = "parent_of"
target = "testing.guard.logic-tool-emits-source-text-outside-layer-reasons"

[[relation]]
source = "testing.logic-tool-emits-source-text-outside-layer-reasons"
kind = "parent_of"
target = "testing.source-observation.logic-tool-emits-source-text-outside-layer-reasons"

[[relation]]
source = "testing.logic-tool-emits-source-text-outside-layer-reasons"
kind = "parent_of"
target = "testing.trigger.logic-tool-emits-source-text-outside-layer-reasons"

[[relation]]
source = "testing.machine-readable-ownership-co-owned-features-std-list"
kind = "parent_of"
target = "testing.source-observation.machine-readable-ownership-co-owned-features-std-list"

[[relation]]
source = "testing.machine-readable-ownership-co-owned-features-std-list"
kind = "parent_of"
target = "testing.trigger.machine-readable-ownership-co-owned-features-std-list"

[[relation]]
source = "testing.maintainer-ruling-force-convenience-cbor-bytes-door-turns"
kind = "parent_of"
target = "testing.predicate.convenience-cbor-bytes-ruling"

[[relation]]
source = "testing.maintainer-ruling-force-convenience-cbor-bytes-door-turns"
kind = "parent_of"
target = "testing.source-observation.maintainer-ruling-force-convenience-cbor-bytes-door-turns"

[[relation]]
source = "testing.make-fixture-comment-s-claimed-wire-bytes-executable"
kind = "parent_of"
target = "testing.guard.make-fixture-comment-s-claimed-wire-bytes-executable"

[[relation]]
source = "testing.make-fixture-comment-s-claimed-wire-bytes-executable"
kind = "parent_of"
target = "testing.source-observation.make-fixture-comment-s-claimed-wire-bytes-executable"

[[relation]]
source = "testing.make-fixture-comment-s-claimed-wire-bytes-executable"
kind = "parent_of"
target = "testing.trigger.make-fixture-comment-s-claimed-wire-bytes-executable"

[[relation]]
source = "testing.making-type-choice-path-reject-duplicated-explicit-name"
kind = "parent_of"
target = "testing.guard.making-type-choice-path-reject-duplicated-explicit-name"

[[relation]]
source = "testing.making-type-choice-path-reject-duplicated-explicit-name"
kind = "parent_of"
target = "testing.source-observation.making-type-choice-path-reject-duplicated-explicit-name"

[[relation]]
source = "testing.making-type-choice-path-reject-duplicated-explicit-name"
kind = "parent_of"
target = "testing.trigger.making-type-choice-path-reject-duplicated-explicit-name"

[[relation]]
source = "testing.matrix-s-note-names-panic-site-aborts-nothing"
kind = "parent_of"
target = "testing.guard.matrix-s-note-names-panic-site-aborts-nothing"

[[relation]]
source = "testing.matrix-s-note-names-panic-site-aborts-nothing"
kind = "parent_of"
target = "testing.source-observation.matrix-s-note-names-panic-site-aborts-nothing"

[[relation]]
source = "testing.matrix-s-note-names-panic-site-aborts-nothing"
kind = "parent_of"
target = "testing.trigger.matrix-s-note-names-panic-site-aborts-nothing"

[[relation]]
source = "testing.mechanical-layers-two-review-owned-design-rules-tests"
kind = "parent_of"
target = "testing.guard.mechanical-layers-two-review-owned-design-rules-tests"

[[relation]]
source = "testing.mechanical-layers-two-review-owned-design-rules-tests"
kind = "parent_of"
target = "testing.source-observation.mechanical-layers-two-review-owned-design-rules-tests"

[[relation]]
source = "testing.mechanical-layers-two-review-owned-design-rules-tests"
kind = "parent_of"
target = "testing.trigger.mechanical-layers-two-review-owned-design-rules-tests"

[[relation]]
source = "testing.message-s-level-classification-review-owned-two-ways"
kind = "parent_of"
target = "testing.guard.message-s-level-classification-review-owned-two-ways"

[[relation]]
source = "testing.message-s-level-classification-review-owned-two-ways"
kind = "parent_of"
target = "testing.source-observation.message-s-level-classification-review-owned-two-ways"

[[relation]]
source = "testing.message-s-level-classification-review-owned-two-ways"
kind = "parent_of"
target = "testing.trigger.message-s-level-classification-review-owned-two-ways"

[[relation]]
source = "testing.migration-handoff-s-complete-list-found-survey-negative"
kind = "parent_of"
target = "testing.trigger.migration-handoff-s-complete-list-found-survey-negative"

[[relation]]
source = "testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"
kind = "parent_of"
target = "testing.admission-path.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"

[[relation]]
source = "testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"
kind = "parent_of"
target = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one"

[[relation]]
source = "testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"
kind = "parent_of"
target = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one-proof"

[[relation]]
source = "testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"
kind = "parent_of"
target = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two"

[[relation]]
source = "testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"
kind = "parent_of"
target = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two-proof"

[[relation]]
source = "testing.msrv-declaration-os-matrix-generated-code"
kind = "parent_of"
target = "testing.guard.msrv-declaration-os-matrix-generated-code"

[[relation]]
source = "testing.msrv-declaration-os-matrix-generated-code"
kind = "parent_of"
target = "testing.source-observation.msrv-declaration-os-matrix-generated-code"

[[relation]]
source = "testing.msrv-declaration-os-matrix-generated-code"
kind = "parent_of"
target = "testing.trigger.msrv-declaration-os-matrix-generated-code"

[[relation]]
source = "testing.multifile-reference-position-coverage-two-position-keyed-escapes"
kind = "parent_of"
target = "testing.guard.multifile-reference-position-coverage-two-position-keyed-escapes"

[[relation]]
source = "testing.multifile-reference-position-coverage-two-position-keyed-escapes"
kind = "parent_of"
target = "testing.source-observation.multifile-reference-position-coverage-two-position-keyed-escapes"

[[relation]]
source = "testing.multifile-reference-position-coverage-two-position-keyed-escapes"
kind = "parent_of"
target = "testing.trigger.multifile-reference-position-coverage-two-position-keyed-escapes"

[[relation]]
source = "testing.nested-cargo-scratch-retention-tmp-cddl-leaks-until"
kind = "parent_of"
target = "testing.scratch-retention.reopening-signal"

[[relation]]
source = "testing.nothing-asserts-how-many-times-rejection-message-emitted"
kind = "parent_of"
target = "testing.guard.nothing-asserts-how-many-times-rejection-message-emitted"

[[relation]]
source = "testing.nothing-asserts-how-many-times-rejection-message-emitted"
kind = "parent_of"
target = "testing.source-observation.nothing-asserts-how-many-times-rejection-message-emitted"

[[relation]]
source = "testing.nothing-asserts-how-many-times-rejection-message-emitted"
kind = "parent_of"
target = "testing.trigger.nothing-asserts-how-many-times-rejection-message-emitted"

[[relation]]
source = "testing.nothing-asserts-two-ir-sites-cannot-mint-same"
kind = "parent_of"
target = "testing.guard.nothing-asserts-two-ir-sites-cannot-mint-same"

[[relation]]
source = "testing.nothing-asserts-two-ir-sites-cannot-mint-same"
kind = "parent_of"
target = "testing.source-observation.nothing-asserts-two-ir-sites-cannot-mint-same"

[[relation]]
source = "testing.nothing-asserts-two-ir-sites-cannot-mint-same"
kind = "parent_of"
target = "testing.trigger.nothing-asserts-two-ir-sites-cannot-mint-same"

[[relation]]
source = "testing.observed-baseline-comments-beside-gate-floors-rot-silently"
kind = "parent_of"
target = "testing.guard.observed-baseline-comments-beside-gate-floors-rot-silently"

[[relation]]
source = "testing.observed-baseline-comments-beside-gate-floors-rot-silently"
kind = "parent_of"
target = "testing.source-observation.observed-baseline-comments-beside-gate-floors-rot-silently"

[[relation]]
source = "testing.observed-baseline-comments-beside-gate-floors-rot-silently"
kind = "parent_of"
target = "testing.trigger.observed-baseline-comments-beside-gate-floors-rot-silently"

[[relation]]
source = "testing.opt-rfc-faithful-total-cbor-json-rendering-json"
kind = "parent_of"
target = "testing.source-observation.opt-rfc-faithful-total-cbor-json-rendering-json"

[[relation]]
source = "testing.opt-rfc-faithful-total-cbor-json-rendering-json"
kind = "parent_of"
target = "testing.trigger.opt-rfc-faithful-total-cbor-json-rendering-json"

[[relation]]
source = "testing.orphan-fixture-directory-meta-test"
kind = "parent_of"
target = "testing.guard.orphan-fixture-directory-meta-test"

[[relation]]
source = "testing.ownership-journal-making-dropped-std-forward-dep-converge"
kind = "parent_of"
target = "testing.guard.ownership-journal-making-dropped-std-forward-dep-converge"

[[relation]]
source = "testing.ownership-journal-making-dropped-std-forward-dep-converge"
kind = "parent_of"
target = "testing.source-observation.ownership-journal-making-dropped-std-forward-dep-converge"

[[relation]]
source = "testing.ownership-journal-making-dropped-std-forward-dep-converge"
kind = "parent_of"
target = "testing.trigger.ownership-journal-making-dropped-std-forward-dep-converge"

[[relation]]
source = "testing.panic-site-un-shadowing-converting-shallow-panic-graceful"
kind = "parent_of"
target = "testing.guard.panic-site-un-shadowing-converting-shallow-panic-graceful"

[[relation]]
source = "testing.panic-site-un-shadowing-converting-shallow-panic-graceful"
kind = "parent_of"
target = "testing.source-observation.panic-site-un-shadowing-converting-shallow-panic-graceful"

[[relation]]
source = "testing.panic-site-un-shadowing-converting-shallow-panic-graceful"
kind = "parent_of"
target = "testing.trigger.panic-site-un-shadowing-converting-shallow-panic-graceful"

[[relation]]
source = "testing.parallel-constructor-fixture-diversity-parser-over-external-input"
kind = "parent_of"
target = "testing.guard.parallel-constructor-fixture-diversity-parser-over-external-input"

[[relation]]
source = "testing.parallel-constructor-fixture-diversity-parser-over-external-input"
kind = "parent_of"
target = "testing.source-observation.parallel-constructor-fixture-diversity-parser-over-external-input"

[[relation]]
source = "testing.parallel-constructor-fixture-diversity-parser-over-external-input"
kind = "parent_of"
target = "testing.trigger.parallel-constructor-fixture-diversity-parser-over-external-input"

[[relation]]
source = "testing.pin-toolchain-verify-ts-s-nested-cargo-scratch"
kind = "parent_of"
target = "testing.proof.pin-toolchain-verify-ts-s-nested-cargo-scratch"

[[relation]]
source = "testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"
kind = "parent_of"
target = "testing.admission-path.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"

[[relation]]
source = "testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"
kind = "parent_of"
target = "testing.proof.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"

[[relation]]
source = "testing.prettyplease-shelling-rustfmt"
kind = "parent_of"
target = "testing.predicate.prettyplease-formatting-ruling"

[[relation]]
source = "testing.prettyplease-shelling-rustfmt"
kind = "parent_of"
target = "testing.source-observation.prettyplease-shelling-rustfmt"

[[relation]]
source = "testing.probe-component-face-s-js-surface-axes-js"
kind = "parent_of"
target = "testing.component-js-probe.reopening-signal"

[[relation]]
source = "testing.probe-component-face-s-js-surface-axes-js"
kind = "parent_of"
target = "testing.source-observation.probe-component-face-s-js-surface-axes-js"

[[relation]]
source = "testing.probe-component-face-s-js-surface-axes-js"
kind = "parent_of"
target = "testing.trigger.probe-component-face-s-js-surface-axes-js"

[[relation]]
source = "testing.process-boundary.another-profile-escape"
kind = "parent_of"
target = "testing.source-observation.process-boundary-another-profile-escape"

[[relation]]
source = "testing.process-boundary.another-profile-escape"
kind = "parent_of"
target = "testing.trigger.process-boundary-another-profile-escape"

[[relation]]
source = "testing.process-boundary.rustc-after-panic-fix"
kind = "parent_of"
target = "testing.guard.process-boundary-rustc-after-panic-fix"

[[relation]]
source = "testing.process-boundary.rustc-after-panic-fix"
kind = "parent_of"
target = "testing.source-observation.process-boundary-rustc-after-panic-fix"

[[relation]]
source = "testing.process-boundary.rustc-after-panic-fix"
kind = "parent_of"
target = "testing.trigger.process-boundary-rustc-after-panic-fix"

[[relation]]
source = "testing.process-boundary.rustfmt-pre-rustc"
kind = "parent_of"
target = "testing.guard.process-boundary-rustfmt-pre-rustc"

[[relation]]
source = "testing.process-boundary.rustfmt-pre-rustc"
kind = "parent_of"
target = "testing.source-observation.process-boundary-rustfmt-pre-rustc"

[[relation]]
source = "testing.process-boundary.rustfmt-pre-rustc"
kind = "parent_of"
target = "testing.trigger.process-boundary-rustfmt-pre-rustc"

[[relation]]
source = "testing.profile-driven-compile-gate-covers-compositions-profiles-specs"
kind = "parent_of"
target = "testing.guard.profile-driven-compile-gate-covers-compositions-profiles-specs"

[[relation]]
source = "testing.profile-driven-compile-gate-covers-compositions-profiles-specs"
kind = "parent_of"
target = "testing.source-observation.profile-driven-compile-gate-covers-compositions-profiles-specs"

[[relation]]
source = "testing.profile-driven-compile-gate-covers-compositions-profiles-specs"
kind = "parent_of"
target = "testing.trigger.profile-driven-compile-gate-covers-compositions-profiles-specs"

[[relation]]
source = "testing.project-json-gen-documents-run-test-fixture-writes"
kind = "parent_of"
target = "testing.source-observation.project-json-gen-documents-run-test-fixture-writes"

[[relation]]
source = "testing.project-json-gen-documents-run-test-fixture-writes"
kind = "parent_of"
target = "testing.trigger.project-json-gen-documents-run-test-fixture-writes"

[[relation]]
source = "testing.propagating-deliberately-unpublished-type-s-intent-json-ts"
kind = "parent_of"
target = "testing.source-observation.propagating-deliberately-unpublished-type-s-intent-json-ts"

[[relation]]
source = "testing.propagating-deliberately-unpublished-type-s-intent-json-ts"
kind = "parent_of"
target = "testing.trigger.propagating-deliberately-unpublished-type-s-intent-json-ts"

[[relation]]
source = "testing.quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"
kind = "parent_of"
target = "testing.tooling.generated-coverage-instrumentation"

[[relation]]
source = "testing.quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"
kind = "parent_of"
target = "testing.tooling.goldenfile-expect-test-corpus"

[[relation]]
source = "testing.quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"
kind = "parent_of"
target = "testing.tooling.no-panic-lints"

[[relation]]
source = "testing.quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"
kind = "parent_of"
target = "testing.tooling.quickcheck-parallel-corpus"

[[relation]]
source = "testing.quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"
kind = "parent_of"
target = "testing.tooling.trybuild-whole-crate"

[[relation]]
source = "testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
kind = "parent_of"
target = "testing.guard.reason-keyed-rejection-evidence-reject-catalogs-proven-near"

[[relation]]
source = "testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
kind = "parent_of"
target = "testing.source-observation.reason-keyed-rejection-evidence-reject-catalogs-proven-near"

[[relation]]
source = "testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
kind = "parent_of"
target = "testing.trigger.reason-keyed-rejection-evidence-reject-catalogs-proven-near"

[[relation]]
source = "testing.referencing-context-measured-wrapping-deep-honored-base-shape"
kind = "parent_of"
target = "testing.guard.referencing-context-measured-wrapping-deep-honored-base-shape"

[[relation]]
source = "testing.referencing-context-measured-wrapping-deep-honored-base-shape"
kind = "parent_of"
target = "testing.source-observation.referencing-context-measured-wrapping-deep-honored-base-shape"

[[relation]]
source = "testing.referencing-context-measured-wrapping-deep-honored-base-shape"
kind = "parent_of"
target = "testing.trigger.referencing-context-measured-wrapping-deep-honored-base-shape"

[[relation]]
source = "testing.registry-fetch-transients-nested-cargo-cells"
kind = "parent_of"
target = "testing.trigger.registry-fetch-transients-nested-cargo-cells"

[[relation]]
source = "testing.rejection-message-s-remedy-string-executable-claim-pin"
kind = "parent_of"
target = "testing.admission-path.rejection-message-s-remedy-string-executable-claim-pin"

[[relation]]
source = "testing.rejection-message-s-remedy-string-executable-claim-pin"
kind = "parent_of"
target = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one"

[[relation]]
source = "testing.rejection-message-s-remedy-string-executable-claim-pin"
kind = "parent_of"
target = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one-proof"

[[relation]]
source = "testing.rejection-message-s-remedy-string-executable-claim-pin"
kind = "parent_of"
target = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two"

[[relation]]
source = "testing.rejection-message-s-remedy-string-executable-claim-pin"
kind = "parent_of"
target = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two-proof"

[[relation]]
source = "testing.representation-changing-directive-goes-live-new-container-ship"
kind = "parent_of"
target = "testing.guard.representation-changing-directive-goes-live-new-container-ship"

[[relation]]
source = "testing.representation-changing-directive-goes-live-new-container-ship"
kind = "parent_of"
target = "testing.source-observation.representation-changing-directive-goes-live-new-container-ship"

[[relation]]
source = "testing.representation-changing-directive-goes-live-new-container-ship"
kind = "parent_of"
target = "testing.trigger.representation-changing-directive-goes-live-new-container-ship"

[[relation]]
source = "testing.restructuring-features-std-tool-owned-sub-feature-cddl"
kind = "parent_of"
target = "testing.guard.restructuring-features-std-tool-owned-sub-feature-cddl"

[[relation]]
source = "testing.rule-trailing.directive-classification"
kind = "blocked_by"
target = "testing.rule-trailing.reviewed-fork-publication"

[[relation]]
source = "testing.rule-trailing.directive-classification"
kind = "parent_of"
target = "testing.predicate.rule-trailing-conjunctive-unblock"

[[relation]]
source = "testing.rule-trailing.directive-classification"
kind = "parent_of"
target = "testing.rule-trailing.reviewed-fork-publication"

[[relation]]
source = "testing.rule-trailing.directive-classification"
kind = "parent_of"
target = "testing.source-observation.rule-trailing-directive-classification"

[[relation]]
source = "testing.rule-trailing.reviewed-fork-publication"
kind = "parent_of"
target = "testing.predicate.reviewed-fork-publication"

[[relation]]
source = "testing.ruling-premise-gate-x-stays-green-must-probed"
kind = "parent_of"
target = "testing.guard.ruling-premise-gate-x-stays-green-must-probed"

[[relation]]
source = "testing.ruling-premise-gate-x-stays-green-must-probed"
kind = "parent_of"
target = "testing.source-observation.ruling-premise-gate-x-stays-green-must-probed"

[[relation]]
source = "testing.ruling-premise-gate-x-stays-green-must-probed"
kind = "parent_of"
target = "testing.trigger.ruling-premise-gate-x-stays-green-must-probed"

[[relation]]
source = "testing.run-local-values-gate-cache-key-material-proven"
kind = "parent_of"
target = "testing.guard.run-local-values-gate-cache-key-material-proven"

[[relation]]
source = "testing.run-local-values-gate-cache-key-material-proven"
kind = "parent_of"
target = "testing.source-observation.run-local-values-gate-cache-key-material-proven"

[[relation]]
source = "testing.run-local-values-gate-cache-key-material-proven"
kind = "parent_of"
target = "testing.trigger.run-local-values-gate-cache-key-material-proven"

[[relation]]
source = "testing.rustfmt-seam-error-leg-still-witness-needs-subprocess"
kind = "parent_of"
target = "testing.source-observation.rustfmt-seam-error-leg-still-witness-needs-subprocess"

[[relation]]
source = "testing.rustfmt-seam-error-leg-still-witness-needs-subprocess"
kind = "parent_of"
target = "testing.trigger.rustfmt-seam-error-leg-still-witness-needs-subprocess"

[[relation]]
source = "testing.same-named-generated-crates-silently-share-cargo-fingerprint"
kind = "parent_of"
target = "testing.admission-path.same-named-generated-crates-silently-share-cargo-fingerprint"

[[relation]]
source = "testing.same-named-generated-crates-silently-share-cargo-fingerprint"
kind = "parent_of"
target = "testing.proof.same-named-generated-crates-silently-share-cargo-fingerprint"

[[relation]]
source = "testing.schema-document-s-name-injectivity-enforced-row-side"
kind = "parent_of"
target = "testing.schema-name.collision-loser-no-row-same-id"

[[relation]]
source = "testing.schema-document-s-name-injectivity-enforced-row-side"
kind = "parent_of"
target = "testing.schema-name.cross-crate-same-id-collision"

[[relation]]
source = "testing.schema-document-s-name-injectivity-enforced-row-side"
kind = "parent_of"
target = "testing.schema-name.percent-encoded-ref"

[[relation]]
source = "testing.schema-document-s-name-injectivity-enforced-row-side"
kind = "parent_of"
target = "testing.schema-name.two-rowless-types-collide"

[[relation]]
source = "testing.schema-name.collision-loser-no-row-same-id"
kind = "parent_of"
target = "testing.guard.schema-name-collision-loser-no-row-same-id"

[[relation]]
source = "testing.schema-name.collision-loser-no-row-same-id"
kind = "parent_of"
target = "testing.source-observation.schema-name-collision-loser-no-row-same-id"

[[relation]]
source = "testing.schema-name.collision-loser-no-row-same-id"
kind = "parent_of"
target = "testing.trigger.schema-name-collision-loser-no-row-same-id"

[[relation]]
source = "testing.schema-name.cross-crate-same-id-collision"
kind = "parent_of"
target = "testing.guard.schema-name-cross-crate-same-id-collision"

[[relation]]
source = "testing.schema-name.cross-crate-same-id-collision"
kind = "parent_of"
target = "testing.source-observation.schema-name-cross-crate-same-id-collision"

[[relation]]
source = "testing.schema-name.cross-crate-same-id-collision"
kind = "parent_of"
target = "testing.trigger.schema-name-cross-crate-same-id-collision"

[[relation]]
source = "testing.schema-name.two-rowless-types-collide"
kind = "parent_of"
target = "testing.guard.schema-name-two-rowless-types-collide"

[[relation]]
source = "testing.schema-name.two-rowless-types-collide"
kind = "parent_of"
target = "testing.source-observation.schema-name-two-rowless-types-collide"

[[relation]]
source = "testing.schema-name.two-rowless-types-collide"
kind = "parent_of"
target = "testing.trigger.schema-name-two-rowless-types-collide"

[[relation]]
source = "testing.scratch-retention.reopening-signal"
kind = "parent_of"
target = "testing.source-observation.scratch-retention-reopening-signal"

[[relation]]
source = "testing.shared-cargo-target-dir-across-same-named-scratch"
kind = "parent_of"
target = "testing.trigger.shared-cargo-target-dir-across-same-named-scratch"

[[relation]]
source = "testing.stale-known-limitation-prose-surviving-fix-finding-ledgered"
kind = "parent_of"
target = "testing.guard.stale-known-limitation-prose-surviving-fix-finding-ledgered"

[[relation]]
source = "testing.stale-known-limitation-prose-surviving-fix-finding-ledgered"
kind = "parent_of"
target = "testing.source-observation.stale-known-limitation-prose-surviving-fix-finding-ledgered"

[[relation]]
source = "testing.stale-known-limitation-prose-surviving-fix-finding-ledgered"
kind = "parent_of"
target = "testing.trigger.stale-known-limitation-prose-surviving-fix-finding-ledgered"

[[relation]]
source = "testing.stale-pin-guard-cannot-distinguish-gap-closed-vector"
kind = "parent_of"
target = "testing.trigger.stale-pin-guard-cannot-distinguish-gap-closed-vector"

[[relation]]
source = "testing.std-generated-crates"
kind = "parent_of"
target = "testing.source-observation.std-generated-crates"

[[relation]]
source = "testing.std-generated-crates"
kind = "parent_of"
target = "testing.trigger.std-generated-crates"

[[relation]]
source = "testing.synthesized-name-referenced-never-minted-e0425-flavor"
kind = "parent_of"
target = "testing.guard.synthesized-name-referenced-never-minted-e0425-flavor"

[[relation]]
source = "testing.synthesized-name-referenced-never-minted-e0425-flavor"
kind = "parent_of"
target = "testing.source-observation.synthesized-name-referenced-never-minted-e0425-flavor"

[[relation]]
source = "testing.synthesized-name-referenced-never-minted-e0425-flavor"
kind = "parent_of"
target = "testing.trigger.synthesized-name-referenced-never-minted-e0425-flavor"

[[relation]]
source = "testing.tag-set.inline-anonymous-choice-boundary"
kind = "parent_of"
target = "testing.source-observation.tag-set-inline-anonymous-choice-boundary"

[[relation]]
source = "testing.tag-set.inline-anonymous-choice-boundary"
kind = "parent_of"
target = "testing.trigger.tag-set-inline-anonymous-choice-boundary"

[[relation]]
source = "testing.tag-set.non-idiom-generic-boundary"
kind = "parent_of"
target = "testing.guard.tag-set-non-idiom-generic-boundary"

[[relation]]
source = "testing.three-defect-classes-process-test-see-sharing-remedy"
kind = "parent_of"
target = "testing.process-boundary.another-profile-escape"

[[relation]]
source = "testing.three-defect-classes-process-test-see-sharing-remedy"
kind = "parent_of"
target = "testing.process-boundary.rustc-after-panic-fix"

[[relation]]
source = "testing.three-defect-classes-process-test-see-sharing-remedy"
kind = "parent_of"
target = "testing.process-boundary.rustfmt-pre-rustc"

[[relation]]
source = "testing.tier-memory.spend-measurements"
kind = "parent_of"
target = "testing.predicate.tier-memory-durable-measurements"

[[relation]]
source = "testing.tier-memory.spend-measurements"
kind = "parent_of"
target = "testing.source-observation.tier-memory-spend-measurements"

[[relation]]
source = "testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
kind = "parent_of"
target = "testing.guard.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"

[[relation]]
source = "testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
kind = "parent_of"
target = "testing.source-observation.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"

[[relation]]
source = "testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
kind = "parent_of"
target = "testing.trigger.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"

[[relation]]
source = "testing.tier-s-peak-memory-bounded-arithmetic-over-assumed"
kind = "parent_of"
target = "testing.source-observation.tier-s-peak-memory-bounded-arithmetic-over-assumed"

[[relation]]
source = "testing.tier-s-peak-memory-bounded-arithmetic-over-assumed"
kind = "parent_of"
target = "testing.tier-memory.spend-measurements"

[[relation]]
source = "testing.transparent-tag-set-idiom-recognized-shape-boundary-request"
kind = "parent_of"
target = "testing.tag-set.inline-anonymous-choice-boundary"

[[relation]]
source = "testing.transparent-tag-set-idiom-recognized-shape-boundary-request"
kind = "parent_of"
target = "testing.tag-set.non-idiom-generic-boundary"

[[relation]]
source = "testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
kind = "parent_of"
target = "testing.guard.tree-wide-sweeps-over-gitignored-regeneration-targets-count"

[[relation]]
source = "testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
kind = "parent_of"
target = "testing.source-observation.tree-wide-sweeps-over-gitignored-regeneration-targets-count"

[[relation]]
source = "testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
kind = "parent_of"
target = "testing.trigger.tree-wide-sweeps-over-gitignored-regeneration-targets-count"

[[relation]]
source = "testing.trigger.add-profile-wrapper-participation-grid-participation-differs-across"
kind = "reopens"
target = "testing.add-profile-wrapper-participation-grid-participation-differs-across"

[[relation]]
source = "testing.trigger.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
kind = "reopens"
target = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"

[[relation]]
source = "testing.trigger.full-n-flag-powerset-pict-pairwise"
kind = "reopens"
target = "testing.full-n-flag-powerset-pict-pairwise"

[[relation]]
source = "testing.trigger.making-type-choice-path-reject-duplicated-explicit-name"
kind = "reopens"
target = "testing.making-type-choice-path-reject-duplicated-explicit-name"

[[relation]]
source = "testing.trigger.msrv-declaration-os-matrix-generated-code"
kind = "reopens"
target = "testing.msrv-declaration-os-matrix-generated-code"

[[relation]]
source = "testing.trigger.ownership-journal-making-dropped-std-forward-dep-converge"
kind = "reopens"
target = "testing.ownership-journal-making-dropped-std-forward-dep-converge"

[[relation]]
source = "testing.trigger.wrapper-name-user-set-name-synthesized-collection-wrapper"
kind = "reopens"
target = "testing.wrapper-name-user-set-name-synthesized-collection-wrapper"

[[relation]]
source = "testing.twin-implementation.comment-ast-change-cadence"
kind = "parent_of"
target = "testing.source-observation.twin-implementation-comment-ast-change-cadence"

[[relation]]
source = "testing.twin-implementation.read-caught-instance-trigger"
kind = "parent_of"
target = "testing.source-observation.twin-implementation-read-caught-instance-trigger"

[[relation]]
source = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
kind = "parent_of"
target = "testing.guard.twin-repo-implementations-semantic-drifting-apart-emission-spellings"

[[relation]]
source = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
kind = "parent_of"
target = "testing.source-observation.twin-repo-implementations-semantic-drifting-apart-emission-spellings"

[[relation]]
source = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
kind = "parent_of"
target = "testing.trigger.twin-repo-implementations-semantic-drifting-apart-emission-spellings"

[[relation]]
source = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
kind = "parent_of"
target = "testing.twin-implementation.comment-ast-change-cadence"

[[relation]]
source = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
kind = "parent_of"
target = "testing.twin-implementation.own-reviewed-change-rule"

[[relation]]
source = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
kind = "parent_of"
target = "testing.twin-implementation.read-caught-instance-trigger"

[[relation]]
source = "testing.unused-imports-generated-crates-trait-import-class-name"
kind = "parent_of"
target = "testing.guard.unused-imports-generated-crates-trait-import-class-name"

[[relation]]
source = "testing.unused-imports-generated-crates-trait-import-class-name"
kind = "parent_of"
target = "testing.source-observation.unused-imports-generated-crates-trait-import-class-name"

[[relation]]
source = "testing.unused-imports-generated-crates-trait-import-class-name"
kind = "parent_of"
target = "testing.trigger.unused-imports-generated-crates-trait-import-class-name"

[[relation]]
source = "testing.value-anchor-rot-field-addition-delivery-grows-existing"
kind = "parent_of"
target = "testing.guard.value-anchor-rot-field-addition-delivery-grows-existing"

[[relation]]
source = "testing.value-anchor-rot-field-addition-delivery-grows-existing"
kind = "parent_of"
target = "testing.source-observation.value-anchor-rot-field-addition-delivery-grows-existing"

[[relation]]
source = "testing.value-anchor-rot-field-addition-delivery-grows-existing"
kind = "parent_of"
target = "testing.trigger.value-anchor-rot-field-addition-delivery-grows-existing"

[[relation]]
source = "testing.verify-cache-transparency-b-split-emission-profile-embed"
kind = "parent_of"
target = "testing.trigger.verify-cache-transparency-b-split-emission-profile-embed"

[[relation]]
source = "testing.verify-ts-refuse-unknown-flag-silently-degrading-plain"
kind = "parent_of"
target = "testing.proof-gap.verify-ts-refuse-unknown-flag-silently-degrading-plain"

[[relation]]
source = "testing.verify-ts-warm-up-cargo-test-exit-attributed"
kind = "parent_of"
target = "testing.source-observation.verify-ts-warm-up-cargo-test-exit-attributed"

[[relation]]
source = "testing.wac-composed-component-transpiled-jco-loses-cross-instance"
kind = "parent_of"
target = "testing.guard.wac-composed-component-transpiled-jco-loses-cross-instance"

[[relation]]
source = "testing.wac-composed-component-transpiled-jco-loses-cross-instance"
kind = "parent_of"
target = "testing.source-observation.wac-composed-component-transpiled-jco-loses-cross-instance"

[[relation]]
source = "testing.wac-composed-component-transpiled-jco-loses-cross-instance"
kind = "parent_of"
target = "testing.trigger.wac-composed-component-transpiled-jco-loses-cross-instance"

[[relation]]
source = "testing.warm-up-refresh-dep-universe-lock-trust-fresh"
kind = "parent_of"
target = "testing.proof.warm-up-refresh-dep-universe-lock-trust-fresh"

[[relation]]
source = "testing.wasm-door-vocabulary.reopening-signal"
kind = "parent_of"
target = "testing.source-observation.wasm-door-vocabulary-reopening-signal"

[[relation]]
source = "testing.wasm-door-vocabulary.reopening-signal"
kind = "reopens"
target = "testing.wasm-face-s-door-vocabulary-hand-listed-mechanism"

[[relation]]
source = "testing.wasm-face-s-door-vocabulary-hand-listed-mechanism"
kind = "parent_of"
target = "testing.wasm-door-vocabulary.reopening-signal"

[[relation]]
source = "testing.wasm32-link-floor-deferred-reject-twin"
kind = "parent_of"
target = "testing.source-observation.wasm32-link-floor-deferred-reject-twin"

[[relation]]
source = "testing.wasm32-link-floor-deferred-reject-twin"
kind = "parent_of"
target = "testing.trigger.wasm32-link-floor-deferred-reject-twin"

[[relation]]
source = "testing.working-tree-gate-run-concurrent-live-implementation-agent"
kind = "parent_of"
target = "testing.trigger.working-tree-gate-run-concurrent-live-implementation-agent"

[[relation]]
source = "testing.workspace-mode-wasm32-wasip2-build-gate-both-faces"
kind = "parent_of"
target = "testing.source-observation.workspace-mode-wasm32-wasip2-build-gate-both-faces"

[[relation]]
source = "testing.workspace-mode-wasm32-wasip2-build-gate-both-faces"
kind = "parent_of"
target = "testing.trigger.workspace-mode-wasm32-wasip2-build-gate-both-faces"

[[relation]]
source = "testing.workspace-mode-wasm32-wasip2-build-gate-both-faces"
kind = "parent_of"
target = "testing.workspace-wasip2.reopening-signal"

[[relation]]
source = "testing.workspace-wasip2.reopening-signal"
kind = "parent_of"
target = "testing.source-observation.workspace-wasip2-reopening-signal"

[[relation]]
source = "testing.wrapper-name-user-set-name-synthesized-collection-wrapper"
kind = "parent_of"
target = "testing.guard.wrapper-name-user-set-name-synthesized-collection-wrapper"

[[relation]]
source = "testing.wrapper-name-user-set-name-synthesized-collection-wrapper"
kind = "parent_of"
target = "testing.source-observation.wrapper-name-user-set-name-synthesized-collection-wrapper"

[[relation]]
source = "testing.wrapper-name-user-set-name-synthesized-collection-wrapper"
kind = "parent_of"
target = "testing.trigger.wrapper-name-user-set-name-synthesized-collection-wrapper"

[[reference]]
id = "ref-testing-add-profile-wrapper-participation-grid-participation-differs-across-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.add-profile-wrapper-participation-grid-participation-differs-across"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-add-profile-wrapper-participation-grid-participation-differs-across-authority"
source = "testing.add-profile-wrapper-participation-grid-participation-differs-across"
kind = "roadmap"
target_id = "testing.guard.add-profile-wrapper-participation-grid-participation-differs-across"

[[reference]]
id = "ref-testing-add-profile-wrapper-participation-grid-participation-differs-across-test-symbol-hf32dc18af1b80049eaeceee59c2af22af5f31da3ff3353fe26b6ba5c0de46eb9"
source = "testing.add-profile-wrapper-participation-grid-participation-differs-across"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::synthesized_name_interaction_sweep"
symbol = "tests::integration_tests::synthesized_name_interaction_sweep"

[[reference]]
id = "ref-testing-alloc-imports-nested-inline-modules-hand-carried-design-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.alloc-imports-nested-inline-modules-hand-carried-design"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-arm-position-s-classifier-compile-time-forcing-function-gate-he00a841db53ba7a7564c502924f7598afeaf955962ec6bc5d5081a220a841f21"
source = "testing.arm-position-s-classifier-compile-time-forcing-function"
kind = "gate"
gate_id = "no_silent_directive"

[[reference]]
id = "ref-testing-armed-idle-harness-arms-empty-head-ledgers-zero-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797"
source = "testing.armed-idle-harness-arms-empty-head-ledgers-zero"
kind = "gate"
gate_id = "lint_doc_citations"

[[reference]]
id = "ref-testing-backgrounded-check-ts-full-launched-sub-agent-s-rule"
source = "testing.backgrounded-check-ts-full-launched-sub-agent-s"
kind = "file_heading"
path = "AGENTS.md"
heading = "Build & verify"

[[reference]]
id = "ref-testing-cargo-feature-generated-crate-s-dependency-change-what-test-symbol-ha4c9663bd6e6a90fce72f489a54d11a70788c1b746766d7e0d016ba6b004788d"
source = "testing.cargo-feature-generated-crate-s-dependency-change-what"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::json_arbitrary_precision"
symbol = "tests::integration_tests::json_arbitrary_precision"

[[reference]]
id = "ref-testing-checking-runtime-flavor-s-safety-condition-rather-stating-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.checking-runtime-flavor-s-safety-condition-rather-stating"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-checking-runtime-flavor-s-safety-condition-rather-stating-authority"
source = "testing.checking-runtime-flavor-s-safety-condition-rather-stating"
kind = "roadmap"
target_id = "testing.guard.checking-runtime-flavor-s-safety-condition-rather-stating"

[[reference]]
id = "ref-testing-checking-runtime-flavor-s-safety-condition-rather-stating-test-symbol-h05d8997762838fa1d4b8618122858a8c3744bce5677cb56be2137a7679d59db9"
source = "testing.checking-runtime-flavor-s-safety-condition-rather-stating"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::config_tests::a_runtime_table_exports_a_runtime_the_other_flavor_compiles_against"
symbol = "tests::config_tests::a_runtime_table_exports_a_runtime_the_other_flavor_compiles_against"

[[reference]]
id = "ref-testing-citation-resolves-naming-wrong-thing-invisible-citation-lint-test-symbol-hffa7bf63970bb6799e6e97c59cb4cd4406606d343959edae1718ba9e2e0b8adf"
source = "testing.citation-resolves-naming-wrong-thing-invisible-citation-lint"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::recombination_tests::known_panic_classes_cite_fixtures_that_produce_them"
symbol = "tests::recombination_tests::known_panic_classes_cite_fixtures_that_produce_them"

[[reference]]
id = "ref-testing-comment-residue-false-matches-text-scans-over-emitted-test-symbol-hc73eeb7dc4e0a05361f26c1616137125d0276df00a81387f23f0f1e9d87cdd5c"
source = "testing.comment-residue-false-matches-text-scans-over-emitted"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::extern_reexport_diagnostic_skips_replace_deleted_glue"
symbol = "tests::integration_tests::extern_reexport_diagnostic_skips_replace_deleted_glue"

[[reference]]
id = "ref-testing-complete-cargo-mutants-sweep-will-never-run-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.complete-cargo-mutants-sweep-will-never-run"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-complete-cargo-mutants-sweep-will-never-run-authority"
source = "testing.complete-cargo-mutants-sweep-will-never-run"
kind = "roadmap"
target_id = "testing.guard.complete-cargo-mutants-sweep-will-never-run"

[[reference]]
id = "ref-testing-containment-note-s-stated-reason-go-false-any-gate-h6a27f79888f19ab92549180ecdcdf05a24145020e97ee6233a1528a47d151786"
source = "testing.containment-note-s-stated-reason-go-false-any"
kind = "gate"
gate_id = "project_corpus"

[[reference]]
id = "ref-testing-control-bytes-committed-source-files-proven-instance-machinery-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797"
source = "testing.control-bytes-committed-source-files-proven-instance-machinery"
kind = "gate"
gate_id = "lint_doc_citations"

[[reference]]
id = "ref-testing-control-bytes-committed-source-files-proven-instance-machinery-gate-ha6b9f717f086d7bc829bcf9c7b9f53ffa31fd91a125917ab3510ddada4d8954c"
source = "testing.control-bytes-committed-source-files-proven-instance-machinery"
kind = "gate"
gate_id = "matrix_typecheck"

[[reference]]
id = "ref-testing-corpus-wide-regen-over-prior-output-sweep-reaches-gate-hf08fa6f837ebd524c01449e86c1865d65b0025a02ada385d347479b8c08b15c8"
source = "testing.corpus-wide-regen-over-prior-output-sweep-reaches"
kind = "gate"
gate_id = "regen_over_prior_output_corpus"

[[reference]]
id = "ref-testing-corpus-wide-regen-over-prior-output-sweep-reaches-test-symbol-hd90de09305bdc040a05e6672173d293c15f94e889c250c4942893b9d82e946a5"
source = "testing.corpus-wide-regen-over-prior-output-sweep-reaches"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::json"
symbol = "tests::integration_tests::json"

[[reference]]
id = "ref-testing-corpus-wide-regen-over-prior-output-sweep-reaches-test-symbol-h39e7182f9385a5e34742d617292e95331a15f02bba7282a59e86fc253a10fd50"
source = "testing.corpus-wide-regen-over-prior-output-sweep-reaches"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::regen_over_prior_tests::regen_over_prior_output_corpus"
symbol = "tests::regen_over_prior_tests::regen_over_prior_output_corpus"

[[reference]]
id = "ref-testing-dependency-swap-pass-compile-parity-existing-gate-silently-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.dependency-swap-pass-compile-parity-existing-gate-silently"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-directive-dependent-emitter-edge-case-triggering-shape-hand-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.directive-dependent-emitter-edge-case-triggering-shape-hand"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-directive-s-documented-sentence-stay-true-consumer-visible-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.directive-s-documented-sentence-stay-true-consumer-visible"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-docs-vs-behavior-conformance-harness-comment-dsl-mdx-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-docs-vs-behavior-conformance-harness-comment-dsl-mdx-gate-h92f92e21b9beaff9ef57cc420e20e24f55a08e91f74047acb1b5d950b15b3231"
source = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
kind = "gate"
gate_id = "fuzz_bounded_run"

[[reference]]
id = "ref-testing-docs-vs-behavior-conformance-harness-comment-dsl-mdx-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797"
source = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
kind = "gate"
gate_id = "lint_doc_citations"

[[reference]]
id = "ref-testing-docs-vs-behavior-conformance-harness-comment-dsl-mdx-authority"
source = "testing.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
kind = "roadmap"
target_id = "testing.guard.docs-vs-behavior-conformance-harness-comment-dsl-mdx"

[[reference]]
id = "ref-testing-documented-failure-shape-exercises-prose-cross-crate-int-test-symbol-h4c5e041e97a704e70c4a95276f741dd0e153a6e8cab36dea83486c0d206018f8"
source = "testing.documented-failure-shape-exercises-prose-cross-crate-int"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::extern_deps"
symbol = "tests::integration_tests::extern_deps"

[[reference]]
id = "ref-testing-documented-failure-shape-exercises-prose-cross-crate-int-test-symbol-h8e57dd2c7c817395d3ecbe80f5de769676ca735f3c57392108c2b5e3bff80b56"
source = "testing.documented-failure-shape-exercises-prose-cross-crate-int"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::int_key_via_common_import_override_sidecar"
symbol = "tests::integration_tests::int_key_via_common_import_override_sidecar"

[[reference]]
id = "ref-testing-documented-flag-pairing-broken-fixture-stays-green-because-test-symbol-hc64f92382996eb99ea23f2c1b7c6a03857d28d5348d758322267f30a93056c52"
source = "testing.documented-flag-pairing-broken-fixture-stays-green-because"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::common_override_wasm_int"
symbol = "tests::integration_tests::common_override_wasm_int"

[[reference]]
id = "ref-testing-documented-flag-pairing-broken-fixture-stays-green-because-test-symbol-h567379bcd8f6b431087b7c8a5a70ec1c1dc112624da15e371ac4d0117dcd009f"
source = "testing.documented-flag-pairing-broken-fixture-stays-green-because"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::extern_deps_wasm"
symbol = "tests::integration_tests::extern_deps_wasm"

[[reference]]
id = "ref-testing-doubled-doc-comment-markers-emitted-docs-proven-instance-test-symbol-haf7edde0b79ace73f9b150c48055de3ca3348b998530b48a7b91250a394d3404"
source = "testing.doubled-doc-comment-markers-emitted-docs-proven-instance"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::generated_code_clippy_clean"
symbol = "tests::integration_tests::generated_code_clippy_clean"

[[reference]]
id = "ref-testing-dsl-position-tests-string-level-design-green-emitted-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.dsl-position-tests-string-level-design-green-emitted"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-duplicates-policy-residuals-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.duplicates-policy-residuals"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-duplicates-dedicated-generic-collision-message-test-symbol-h5fad85f77abba604c3e768d2d77129d2a6a626795833b7048bb3e2e7d906dbc2"
source = "testing.duplicates.dedicated-generic-collision-message"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::robustness_tests::inline_258_nominal_name_collision_is_rejected"
symbol = "tests::robustness_tests::inline_258_nominal_name_collision_is_rejected"

[[reference]]
id = "ref-testing-e0463-t-find-crate-core-scratch-tree-std-operating"
source = "testing.e0463-t-find-crate-core-scratch-tree-std"
kind = "file_heading"
path = "AGENTS.md"
heading = "Build & verify"

[[reference]]
id = "ref-testing-emission-branch-generated-output-fixture-exercises-invisible-gate-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.emission-branch-generated-output-fixture-exercises-invisible-gate"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-emit-tests-gate-asserts-round-trip-green-cannot-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.emit-tests-gate-asserts-round-trip-green-cannot"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-emitted-shape-lint-classes-outside-clippy-all-beyond-test-symbol-haf7edde0b79ace73f9b150c48055de3ca3348b998530b48a7b91250a394d3404"
source = "testing.emitted-shape-lint-classes-outside-clippy-all-beyond"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::generated_code_clippy_clean"
symbol = "tests::integration_tests::generated_code_clippy_clean"

[[reference]]
id = "ref-testing-emitter-drops-overloadable-parameter-building-fresh-config-invisible-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-exclusion-reasons-informational-interchange-authority"
source = "testing.exclusion-reasons-informational-interchange"
kind = "file_heading"
path = "tests/README.md"
heading = "Extern-interface export & `--extern-import` (the machine-generated stub channel)"

[[reference]]
id = "ref-testing-exclusion-reasons-informational-interchange-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.exclusion-reasons-informational-interchange"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-extend-recombination-member-kind-table-optional-fixed-shape-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.extend-recombination-member-kind-table-optional-fixed-shape"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-extern-deps-wasm-boundary-surface-packaging-json-gen-test-symbol-hb167df3b741e441b329e40b4e266b72eeb0270c3ff37a9648b2578cb2c2fe932"
source = "testing.extern-deps-wasm-boundary-surface-packaging-json-gen"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::extern_wrapper_index_defers_to_dep"
symbol = "tests::integration_tests::extern_wrapper_index_defers_to_dep"

[[reference]]
id = "ref-testing-extern-interface-export-public-interchange-format-file-heading-hcd398e660853cc36a8775a2364f8f7dcdd7f9c1a4fee1a20a98b392f5dcdc1ce"
source = "testing.extern-interface-export-public-interchange-format"
kind = "file_heading"
path = "tests/README.md"
heading = "Extern-interface export & `--extern-import` (the machine-generated stub channel)"

[[reference]]
id = "ref-testing-extreme-value-boundary-correctness-generation-time-encode-paths-test-symbol-h03a2d4aca6fd0b7322c8b06e51166f2dd358e81b0f0599b08bc930dafaba33f2"
source = "testing.extreme-value-boundary-correctness-generation-time-encode-paths"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::robustness_tests::i64_min_fixed_value_emits_width_correct_nint"
symbol = "tests::robustness_tests::i64_min_fixed_value_emits_width_correct_nint"

[[reference]]
id = "ref-testing-failing-snapshot-test-surfaces-mismatch-per-test-run-file-heading-hdfda06d801b21d0d5d207c3f6d65dfb210bc07372c22ac4db084fd65eefa1c3c"
source = "testing.failing-snapshot-test-surfaces-mismatch-per-test-run"
kind = "file_heading"
path = "tests/README.md"
heading = "Blessing changes"

[[reference]]
id = "ref-testing-finding-s-defect-side-attribution-recorded-narrative-plausibility-gate-hfd5262ebb003d28f8005bf974fa55f6ad2a0d8edd3503b7c105f5136a214c75a"
source = "testing.finding-s-defect-side-attribution-recorded-narrative-plausibility"
kind = "gate"
gate_id = "corpus_decode_replay"

[[reference]]
id = "ref-testing-finding-s-defect-side-attribution-recorded-narrative-plausibility-test-symbol-h91ab61cdff6159c5294eec8058119963c8dae390181e7e6add05fc2fca12d304"
source = "testing.finding-s-defect-side-attribution-recorded-narrative-plausibility"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::corpus_decode_replay"
symbol = "tests::integration_tests::corpus_decode_replay"

[[reference]]
id = "ref-testing-fixture-models-obsolete-user-contract-stays-green-real-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.fixture-models-obsolete-user-contract-stays-green-real"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-fixture-s-verdict-accident-cargo-feature-unification-harness-test-symbol-h92e6ce8d56c91219e54fcbeae835cfb71d8d3167d5ec16c6e3ba5ce03e1ea23f"
source = "testing.fixture-s-verdict-accident-cargo-feature-unification-harness"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::core_no_wasm"
symbol = "tests::integration_tests::core_no_wasm"

[[reference]]
id = "ref-testing-fixture-s-verdict-accident-cargo-feature-unification-harness-test-symbol-h6f14a1dfc8b23912c26f6e2155cb0f7aa7b2090605380a02d393bf82074e2b57"
source = "testing.fixture-s-verdict-accident-cargo-feature-unification-harness"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::core_with_wasm"
symbol = "tests::integration_tests::core_with_wasm"

[[reference]]
id = "ref-testing-fixture-s-verdict-accident-cargo-feature-unification-harness-test-symbol-hbdd271db5c0070ba70095dc3db0323b4ebffefd1fb2125040e83e6cfcebfdf4a"
source = "testing.fixture-s-verdict-accident-cargo-feature-unification-harness"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::emit_wasm_tests_execute"
symbol = "tests::integration_tests::emit_wasm_tests_execute"

[[reference]]
id = "ref-testing-flag-cddl-codegen-invocation-wasm-run-wasm-flag-file-heading-h759f2d7eb8a0852ae365a6dccd08e73b4522e560bc3c7e8c7801b2377f6a959c"
source = "testing.flag-cddl-codegen-invocation-wasm-run-wasm-flag"
kind = "file_heading"
path = "AGENTS.md"
heading = "Build & verify"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-authority"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "roadmap"
target_id = "testing.guard.full-n-flag-powerset-pict-pairwise"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-hc64f92382996eb99ea23f2c1b7c6a03857d28d5348d758322267f30a93056c52"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::common_override_wasm_int"
symbol = "tests::integration_tests::common_override_wasm_int"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-h4c5e041e97a704e70c4a95276f741dd0e153a6e8cab36dea83486c0d206018f8"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::extern_deps"
symbol = "tests::integration_tests::extern_deps"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-h567379bcd8f6b431087b7c8a5a70ec1c1dc112624da15e371ac4d0117dcd009f"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::extern_deps_wasm"
symbol = "tests::integration_tests::extern_deps_wasm"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-hb2fb1160880f2ab60ccb9ef98d1df3d215214446bc8672faeb24ca12e2f4112e"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::extern_wrapper_index_is_validated_under_wasm_false"
symbol = "tests::integration_tests::extern_wrapper_index_is_validated_under_wasm_false"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-hce0a1eaefb48776b0356a7bad6b95514b5e4424505e6a89b2cd030b70ad97d49"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::flag_value_rejects_canonical_without_preserve"
symbol = "tests::integration_tests::flag_value_rejects_canonical_without_preserve"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-h27b1fb699c3672796b1c337160835effb6cfdeccb9c9c4afb25e50c0179d562a"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::flag_value_smoke"
symbol = "tests::integration_tests::flag_value_smoke"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-haf7edde0b79ace73f9b150c48055de3ca3348b998530b48a7b91250a394d3404"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::generated_code_clippy_clean"
symbol = "tests::integration_tests::generated_code_clippy_clean"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-h8e57dd2c7c817395d3ecbe80f5de769676ca735f3c57392108c2b5e3bff80b56"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::int_key_via_common_import_override_sidecar"
symbol = "tests::integration_tests::int_key_via_common_import_override_sidecar"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-ha6c46315f3b84ad9e802fa7c2cbd19bacf51aa069d05c1f2e1275c978dded1d9"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::no_annotate_reframed_emissions_round_trip_non_canonical_wire_byte_exactly"
symbol = "tests::integration_tests::no_annotate_reframed_emissions_round_trip_non_canonical_wire_byte_exactly"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-h3af84f0e3bdd04e013152b176a35d32bac3eb03b14c54d0c3ad13cb2abad1a0a"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::preserve_no_annotate_fixed_members_generate"
symbol = "tests::integration_tests::preserve_no_annotate_fixed_members_generate"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-hf518dee8ab7eed2edd275a09f1d6e85c5fd039a93616ac2a03d0e1d1ad14694f"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::wasm_cbor_json_api_macro_compiles"
symbol = "tests::integration_tests::wasm_cbor_json_api_macro_compiles"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-h8c6f99ea75155b7d703b49c3624efd8392dcf2cd33477d09fd91fa661714261a"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::workspace_dep_unknown_is_rejected_under_wasm_false"
symbol = "tests::integration_tests::workspace_dep_unknown_is_rejected_under_wasm_false"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-h837ddd6c8ecedbc6520aa975cd91f47ecfe56e314d8959bc1bb633b0596ce5b0"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::workspace_key_requests_derive_effect_and_hard_errors"
symbol = "tests::integration_tests::workspace_key_requests_derive_effect_and_hard_errors"

[[reference]]
id = "ref-testing-full-n-flag-powerset-pict-pairwise-test-symbol-h7da4c620f0a0af074e621837c5735a552e525cf626e7722cc560fbcb2fe21580"
source = "testing.full-n-flag-powerset-pict-pairwise"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::snapshot_tests::deserialize_converts_error_at_most_once"
symbol = "tests::snapshot_tests::deserialize_converts_error_at_most_once"

[[reference]]
id = "ref-testing-full-suite-flake-attributed-hardened-acquire-scratch-lock-operating"
source = "testing.full-suite-flake-attributed-hardened-acquire-scratch-lock"
kind = "file_heading"
path = "tests/README.md"
heading = "Running everything"

[[reference]]
id = "ref-testing-gate-cache-closure-audit-structurally-blind-gate-authored-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.gate-cache-closure-audit-structurally-blind-gate-authored"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-gate-cache-costs-file-heading-hd2498b1dbcaca730bce673b3f6bff5a3a73caa75e63e4868b1df30dd358f795c"
source = "testing.gate-cache-costs"
kind = "file_heading"
path = "tests/README.md"
heading = "The gate cache (memoize-and-skip for nested cargo)"

[[reference]]
id = "ref-testing-gate-exists-registry-nowhere-tests-readme-md-hand-gate-h14a4d326b3ff7b16ea5befdfb6772cbfa23f666cb1d531fc5d54e68ec6398e80"
source = "testing.gate-exists-registry-nowhere-tests-readme-md-hand"
kind = "gate"
gate_id = "pin_cold_fetch"

[[reference]]
id = "ref-testing-gate-s-own-stdout-print-column-result-pass-file-heading-h759f2d7eb8a0852ae365a6dccd08e73b4522e560bc3c7e8c7801b2377f6a959c"
source = "testing.gate-s-own-stdout-print-column-result-pass"
kind = "file_heading"
path = "AGENTS.md"
heading = "Build & verify"

[[reference]]
id = "ref-testing-gate-script-health-cache-staleness-key-fresh-worktree-gate-he58fbd1649112b6357870774f10623684918928b7d10e5a2207e0389b4c867b7"
source = "testing.gate-script-health-cache-staleness-key-fresh-worktree"
kind = "gate"
gate_id = "fuzz_compile_rot"

[[reference]]
id = "ref-testing-generated-local-collision-class-refused-mangled-refusal-s-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.generated-local-collision-class-refused-mangled-refusal-s"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-generated-local-collision-class-refused-mangled-refusal-s-gate-h2b903aaf6a1fad0f077b632e4203c5e0b49c8bd1ccb14d4444c25f8ec125a1c7"
source = "testing.generated-local-collision-class-refused-mangled-refusal-s"
kind = "gate"
gate_id = "generated_local_out_of_scope_crates_compile"

[[reference]]
id = "ref-testing-generated-local-collision-class-refused-mangled-refusal-s-test-symbol-h10c0edddf5fff5d5d4d6d4d97122c0b49b0c3a2be9cb1baae7222283ad6a74eb"
source = "testing.generated-local-collision-class-refused-mangled-refusal-s"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::identifier_hazard_tests::generated_local_out_of_scope_crates_compile"
symbol = "tests::identifier_hazard_tests::generated_local_out_of_scope_crates_compile"

[[reference]]
id = "ref-testing-generated-local-collision-scope-wide-probe-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.generated-local-collision.scope-wide-probe"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-generic-raw-bytes-base-refused-generic-extern-base-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.generic-raw-bytes-base-refused-generic-extern-base"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-generic-raw-bytes-base-refused-generic-extern-base-matrix-feature-h46bcbd72e0a2997098ddc4ce0a183fdac58742246edacf5f544944555e975bed"
source = "testing.generic-raw-bytes-base-refused-generic-extern-base"
kind = "matrix_feature"
feature_id = "ext.extern.generic"

[[reference]]
id = "ref-testing-generic-raw-bytes-base-refused-generic-extern-base-matrix-feature-h037c3812dd78e531990735304c89c4f28b18d3b4605b60c99b815678c2ac7cc8"
source = "testing.generic-raw-bytes-base-refused-generic-extern-base"
kind = "matrix_feature"
feature_id = "ext.extern.generic_instance"

[[reference]]
id = "ref-testing-generic-raw-bytes-base-refused-generic-extern-base-matrix-feature-h8e8a4ec65b1327790726a0fa31ad3af8341e461ef600e8d656b079796217709d"
source = "testing.generic-raw-bytes-base-refused-generic-extern-base"
kind = "matrix_feature"
feature_id = "ext.raw_bytes.generic"

[[reference]]
id = "ref-testing-generic-raw-bytes-base-refused-generic-extern-base-authority"
source = "testing.generic-raw-bytes-base-refused-generic-extern-base"
kind = "roadmap"
target_id = "testing.guard.generic-raw-bytes-base-refused-generic-extern-base"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.grammar-fuzzer-escalations"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-gate-hbcc978d64228d9aa31f694c0db8dd8725546f574db1fe2d6bf146fd4552212bd"
source = "testing.grammar-fuzzer-escalations"
kind = "gate"
gate_id = "recombination_crates_execute"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-gate-hbd8567161363be69fcdbb3bd57d1b002a372f2394f258f624de8314de5131b54"
source = "testing.grammar-fuzzer-escalations"
kind = "gate"
gate_id = "recombination_json_crates_execute"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-gate-h8a50e2502eeca655ec7ce4c9628abf496ee41f1554caca0ae11b18ddf7cc840e"
source = "testing.grammar-fuzzer-escalations"
kind = "gate"
gate_id = "recombination_preserve_crates_execute"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-gate-h89d35505cd400602aee174c279a4d15bca40d4ed17317da9265106c0f86580c0"
source = "testing.grammar-fuzzer-escalations"
kind = "gate"
gate_id = "recombination_wasm_crates_check"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-test-symbol-h9014886f8bce9de42e2cdd3ccee1b2fe66cd150e840f85a654b5292208c9dddb"
source = "testing.grammar-fuzzer-escalations"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::recombination_tests::ledger_key_shape_floor"
symbol = "tests::recombination_tests::ledger_key_shape_floor"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-test-symbol-h852d294085f3656d35b4acff7e3f89b48dd40529f2a072621d41d591a3aae658"
source = "testing.grammar-fuzzer-escalations"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::recombination_tests::recombination_crates_execute"
symbol = "tests::recombination_tests::recombination_crates_execute"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-test-symbol-h225a7426b5c7fe2ea1afe6b04f3fb52beb5e4507ee8b5d0bd469817fd9f75afc"
source = "testing.grammar-fuzzer-escalations"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::recombination_tests::recombination_generation_sweep"
symbol = "tests::recombination_tests::recombination_generation_sweep"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-test-symbol-hfa78fa7a01247373d294c50af853916bafb19e37307642fb56e99ad1e6fdb916"
source = "testing.grammar-fuzzer-escalations"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::recombination_tests::recombination_json_crates_execute"
symbol = "tests::recombination_tests::recombination_json_crates_execute"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-test-symbol-hc9703fd922465db49fb1fcaf4ff6eedf18caa39d97ab7a67a7c291df695cf2d5"
source = "testing.grammar-fuzzer-escalations"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::recombination_tests::recombination_preserve_crates_execute"
symbol = "tests::recombination_tests::recombination_preserve_crates_execute"

[[reference]]
id = "ref-testing-grammar-fuzzer-escalations-test-symbol-hce171f02ce1af922e066e0fb8ac15c93bbef451bdc878ae4c5389958f4b33991"
source = "testing.grammar-fuzzer-escalations"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::recombination_tests::recombination_wasm_crates_check"
symbol = "tests::recombination_tests::recombination_wasm_crates_check"

[[reference]]
id = "ref-testing-grammar-fuzzer-batch-masking-detector-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.grammar-fuzzer.batch-masking-detector"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-hand-enumerated-ban-pattern-list-authority-lives-gitignored-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797"
source = "testing.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
kind = "gate"
gate_id = "lint_doc_citations"

[[reference]]
id = "ref-testing-hand-maintained-source-dependency-mapping-manifest-changeset-silently-test-symbol-h6c1e0d91bc6033d6e72fba524afbf91d1927393e2d0f847e632d618ea16128bf"
source = "testing.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::export_static_crate_writes_composed_runtime_and_manifest"
symbol = "tests::integration_tests::export_static_crate_writes_composed_runtime_and_manifest"

[[reference]]
id = "ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-one-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-one-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one-proof"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-one-proof-roadmap-h3de9048b09ce092e475d6691816189c52c2beadbd980bb250ed2977a5f213ef6"
source = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.one-proof"
kind = "roadmap"
target_id = "testing.build-registration-class-reference-position-sweep-second-escape"

[[reference]]
id = "ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-two-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-two-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two-proof"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-build-registration-class-reference-position-sweep-second-escape-two-proof-roadmap-h3de9048b09ce092e475d6691816189c52c2beadbd980bb250ed2977a5f213ef6"
source = "testing.incident-observation.build-registration-class-reference-position-sweep-second-escape.two-proof"
kind = "roadmap"
target_id = "testing.build-registration-class-reference-position-sweep-second-escape"

[[reference]]
id = "ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-one-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-one-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one-proof"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-one-proof-roadmap-haebfad78df120de969e13b0169c8754aad4ad4047945530b40fecd9afd40b9b4"
source = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.one-proof"
kind = "roadmap"
target_id = "testing.gate-demands-premise-probed-against-gate-evidence-about"

[[reference]]
id = "ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-two-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-two-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two-proof"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-gate-demands-premise-probed-against-gate-evidence-about-two-proof-roadmap-haebfad78df120de969e13b0169c8754aad4ad4047945530b40fecd9afd40b9b4"
source = "testing.incident-observation.gate-demands-premise-probed-against-gate-evidence-about.two-proof"
kind = "roadmap"
target_id = "testing.gate-demands-premise-probed-against-gate-evidence-about"

[[reference]]
id = "ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-one-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-one-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one-proof"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-one-proof-roadmap-h1838240ada66525dd1b6a047c12fd480acf9eb4d7ffecadafaa3fd7dc251d94a"
source = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.one-proof"
kind = "roadmap"
target_id = "testing.grammar-fuzzer.embed-site-alias-roots"

[[reference]]
id = "ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-two-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-two-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two-proof"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-grammar-fuzzer-embed-site-alias-roots-two-proof-roadmap-h1838240ada66525dd1b6a047c12fd480acf9eb4d7ffecadafaa3fd7dc251d94a"
source = "testing.incident-observation.grammar-fuzzer-embed-site-alias-roots.two-proof"
kind = "roadmap"
target_id = "testing.grammar-fuzzer.embed-site-alias-roots"

[[reference]]
id = "ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-one-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-one-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one-proof"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-one-proof-roadmap-he7e71a780510c173364f6ac7c53e176b29906556230f49338bec3ae2f32a3ba7"
source = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.one-proof"
kind = "roadmap"
target_id = "testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"

[[reference]]
id = "ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-two-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-two-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two-proof"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-minted-identifier-spellability-floor-identifier-generator-emits-verbatim-two-proof-roadmap-he7e71a780510c173364f6ac7c53e176b29906556230f49338bec3ae2f32a3ba7"
source = "testing.incident-observation.minted-identifier-spellability-floor-identifier-generator-emits-verbatim.two-proof"
kind = "roadmap"
target_id = "testing.minted-identifier-spellability-floor-identifier-generator-emits-verbatim"

[[reference]]
id = "ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-one-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-one-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one-proof"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-one-proof-roadmap-hb77e87c983491a2898906b45e06411cb6c5670c21100469699b1ee7436661fa2"
source = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.one-proof"
kind = "roadmap"
target_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"

[[reference]]
id = "ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-two-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-two-proof-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two-proof"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-incident-observation-rejection-message-s-remedy-string-executable-claim-pin-two-proof-roadmap-hb77e87c983491a2898906b45e06411cb6c5670c21100469699b1ee7436661fa2"
source = "testing.incident-observation.rejection-message-s-remedy-string-executable-claim-pin.two-proof"
kind = "roadmap"
target_id = "testing.rejection-message-s-remedy-string-executable-claim-pin"

[[reference]]
id = "ref-testing-lint-doc-citations-accepts-docs-heading-durable-citation-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797"
source = "testing.lint-doc-citations-accepts-docs-heading-durable-citation"
kind = "gate"
gate_id = "lint_doc_citations"

[[reference]]
id = "ref-testing-logic-tool-emits-source-text-outside-layer-reasons-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.logic-tool-emits-source-text-outside-layer-reasons"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-make-fixture-comment-s-claimed-wire-bytes-executable-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.make-fixture-comment-s-claimed-wire-bytes-executable"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-making-type-choice-path-reject-duplicated-explicit-name-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.making-type-choice-path-reject-duplicated-explicit-name"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-making-type-choice-path-reject-duplicated-explicit-name-authority"
source = "testing.making-type-choice-path-reject-duplicated-explicit-name"
kind = "roadmap"
target_id = "testing.guard.making-type-choice-path-reject-duplicated-explicit-name"

[[reference]]
id = "ref-testing-matrix-s-note-names-panic-site-aborts-nothing-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.matrix-s-note-names-panic-site-aborts-nothing"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-mechanical-layers-two-review-owned-design-rules-tests-test-symbol-h7252c51ca49273b5edf62a95a88ca8f30b01aa234d9593848a5b35cb2465dd52"
source = "testing.mechanical-layers-two-review-owned-design-rules-tests"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::corpus_occurrence_bounds_enforced"
symbol = "tests::integration_tests::corpus_occurrence_bounds_enforced"

[[reference]]
id = "ref-testing-mechanical-layers-two-review-owned-design-rules-tests-test-symbol-hc749902b18315a9d1f12a307c7c9a1196f35ee12722b7211043985cfe3fe37ca"
source = "testing.mechanical-layers-two-review-owned-design-rules-tests"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::corpus_special_map_key_supported"
symbol = "tests::integration_tests::corpus_special_map_key_supported"

[[reference]]
id = "ref-testing-message-s-level-classification-review-owned-two-ways-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.message-s-level-classification-review-owned-two-ways"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-migration-handoff-s-complete-list-found-survey-negative-operating"
source = "testing.migration-handoff-s-complete-list-found-survey-negative"
kind = "file_heading"
path = "docs/docs/output_format.mdx"
heading = "Upgrading a crate generated before the `no_std` output"

[[reference]]
id = "ref-testing-msrv-declaration-os-matrix-generated-code-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.msrv-declaration-os-matrix-generated-code"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-msrv-declaration-os-matrix-generated-code-authority"
source = "testing.msrv-declaration-os-matrix-generated-code"
kind = "roadmap"
target_id = "testing.guard.msrv-declaration-os-matrix-generated-code"

[[reference]]
id = "ref-testing-multifile-reference-position-coverage-two-position-keyed-escapes-test-symbol-h1b01a70d4040ac15d5c5b342c3e8dc49b26e0d3d15ab75531aa65fd7ac058804"
source = "testing.multifile-reference-position-coverage-two-position-keyed-escapes"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::extern_generic_scoped"
symbol = "tests::integration_tests::extern_generic_scoped"

[[reference]]
id = "ref-testing-multifile-reference-position-coverage-two-position-keyed-escapes-test-symbol-hb058dd1eca57bc8a79f91a36dadd3ca901e2f81888e20775ea28346855b517a6"
source = "testing.multifile-reference-position-coverage-two-position-keyed-escapes"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::snapshot_tests::extern_generic_scoped_alias_imports"
symbol = "tests::snapshot_tests::extern_generic_scoped_alias_imports"

[[reference]]
id = "ref-testing-nothing-asserts-how-many-times-rejection-message-emitted-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.nothing-asserts-how-many-times-rejection-message-emitted"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-nothing-asserts-two-ir-sites-cannot-mint-same-test-symbol-hcf12fb74615c93111e6709469e4256b6a5177a805e60d22c6e675fe9a586d6ab"
source = "testing.nothing-asserts-two-ir-sites-cannot-mint-same"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::robustness_tests::embeddable_group_choice_arm_may_share_a_rule_name"
symbol = "tests::robustness_tests::embeddable_group_choice_arm_may_share_a_rule_name"

[[reference]]
id = "ref-testing-nothing-asserts-two-ir-sites-cannot-mint-same-test-symbol-hc2a3331e11cd1132a1b4075f798bdb40f6559713ca231f7c9b6a28fa598a0e3d"
source = "testing.nothing-asserts-two-ir-sites-cannot-mint-same"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::robustness_tests::group_choice_arm_ident_collision_rejects_gracefully"
symbol = "tests::robustness_tests::group_choice_arm_ident_collision_rejects_gracefully"

[[reference]]
id = "ref-testing-nothing-asserts-two-ir-sites-cannot-mint-same-test-symbol-h765c81ec81d1528831b49117e4f4c56dbe06beaf882ed56d779ae14c83a5d7f9"
source = "testing.nothing-asserts-two-ir-sites-cannot-mint-same"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::robustness_tests::identical_group_choice_arms_in_different_rules_share_one_struct"
symbol = "tests::robustness_tests::identical_group_choice_arms_in_different_rules_share_one_struct"

[[reference]]
id = "ref-testing-observed-baseline-comments-beside-gate-floors-rot-silently-gate-hfd5262ebb003d28f8005bf974fa55f6ad2a0d8edd3503b7c105f5136a214c75a"
source = "testing.observed-baseline-comments-beside-gate-floors-rot-silently"
kind = "gate"
gate_id = "corpus_decode_replay"

[[reference]]
id = "ref-testing-observed-baseline-comments-beside-gate-floors-rot-silently-test-symbol-h91ab61cdff6159c5294eec8058119963c8dae390181e7e6add05fc2fca12d304"
source = "testing.observed-baseline-comments-beside-gate-floors-rot-silently"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::corpus_decode_replay"
symbol = "tests::integration_tests::corpus_decode_replay"

[[reference]]
id = "ref-testing-orphan-fixture-directory-meta-test-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.orphan-fixture-directory-meta-test"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-orphan-fixture-directory-meta-test-authority"
source = "testing.orphan-fixture-directory-meta-test"
kind = "roadmap"
target_id = "testing.guard.orphan-fixture-directory-meta-test"

[[reference]]
id = "ref-testing-ownership-journal-making-dropped-std-forward-dep-converge-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.ownership-journal-making-dropped-std-forward-dep-converge"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-ownership-journal-making-dropped-std-forward-dep-converge-authority"
source = "testing.ownership-journal-making-dropped-std-forward-dep-converge"
kind = "roadmap"
target_id = "testing.guard.ownership-journal-making-dropped-std-forward-dep-converge"

[[reference]]
id = "ref-testing-panic-site-un-shadowing-converting-shallow-panic-graceful-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.panic-site-un-shadowing-converting-shallow-panic-graceful"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-parallel-constructor-fixture-diversity-parser-over-external-input-test-symbol-h0a539c7624cba85a03789079dcbb8396196ccfc603cd7cb8c4fa78a9af03fc2b"
source = "testing.parallel-constructor-fixture-diversity-parser-over-external-input"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::workspace_requests_alias_elements_host"
symbol = "tests::integration_tests::workspace_requests_alias_elements_host"

[[reference]]
id = "ref-testing-predicate-convenience-cbor-bytes-ruling-owner"
source = "testing.predicate.convenience-cbor-bytes-ruling"
kind = "roadmap"
target_id = "testing.maintainer-ruling-force-convenience-cbor-bytes-door-turns"

[[reference]]
id = "ref-testing-predicate-prettyplease-formatting-ruling-owner"
source = "testing.predicate.prettyplease-formatting-ruling"
kind = "roadmap"
target_id = "testing.prettyplease-shelling-rustfmt"

[[reference]]
id = "ref-testing-predicate-reviewed-fork-publication-owner"
source = "testing.predicate.reviewed-fork-publication"
kind = "roadmap"
target_id = "testing.rule-trailing.reviewed-fork-publication"

[[reference]]
id = "ref-testing-predicate-rule-trailing-conjunctive-unblock-owner"
source = "testing.predicate.rule-trailing-conjunctive-unblock"
kind = "roadmap"
target_id = "testing.rule-trailing.directive-classification"

[[reference]]
id = "ref-testing-predicate-tier-memory-durable-measurements-owner"
source = "testing.predicate.tier-memory-durable-measurements"
kind = "roadmap"
target_id = "testing.tier-memory.spend-measurements"

[[reference]]
id = "ref-testing-process-boundary-rustc-after-panic-fix-test-symbol-h1535d59c68ee2febd35ce88ec92365a843838940dda4648495494e28a4a9180d"
source = "testing.process-boundary.rustc-after-panic-fix"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::recursive_collection_ref"
symbol = "tests::integration_tests::recursive_collection_ref"

[[reference]]
id = "ref-testing-process-boundary-rustc-after-panic-fix-test-symbol-h9b42afc58e18471a0a4f1d2ffa1b7c48302bfae88f89740b52be6f1922e8e179"
source = "testing.process-boundary.rustc-after-panic-fix"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::recursive_collection_ref_preserve"
symbol = "tests::integration_tests::recursive_collection_ref_preserve"

[[reference]]
id = "ref-testing-process-boundary-rustfmt-pre-rustc-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.process-boundary.rustfmt-pre-rustc"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-profile-driven-compile-gate-covers-compositions-profiles-specs-gate-h27130094b630c9c61596568004d5fabd3d8420f7fad801b2c98c52c1c412b72c"
source = "testing.profile-driven-compile-gate-covers-compositions-profiles-specs"
kind = "gate"
gate_id = "no_std_check"

[[reference]]
id = "ref-testing-proof-directive-effect-round-trip-coherence-sweep-accepted-custom-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.proof.directive-effect-round-trip-coherence-sweep-accepted-custom"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-proof-directive-effect-round-trip-coherence-sweep-accepted-custom-roadmap-he5a254293d69634a726bd77c7d1cde49fee2f6fe57c270aa5dbf876d38328506"
source = "testing.proof.directive-effect-round-trip-coherence-sweep-accepted-custom"
kind = "roadmap"
target_id = "testing.directive-effect-round-trip-coherence-sweep-accepted-custom"

[[reference]]
id = "ref-testing-proof-pin-toolchain-verify-ts-s-nested-cargo-scratch-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.proof.pin-toolchain-verify-ts-s-nested-cargo-scratch"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-proof-pin-toolchain-verify-ts-s-nested-cargo-scratch-roadmap-h41c11e8c7262400c23266ecfc4c28e30a1b59b83cb62b8035737fcfac96b55aa"
source = "testing.proof.pin-toolchain-verify-ts-s-nested-cargo-scratch"
kind = "roadmap"
target_id = "testing.pin-toolchain-verify-ts-s-nested-cargo-scratch"

[[reference]]
id = "ref-testing-proof-positional-diversity-fold-preserve-fixture-corpus-authoring-gives-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.proof.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-proof-positional-diversity-fold-preserve-fixture-corpus-authoring-gives-roadmap-h8a6dd47dcf615a826f4736b9dee803dae68cf5cddfe34f9ba6d47f3e5d9216c7"
source = "testing.proof.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"
kind = "roadmap"
target_id = "testing.positional-diversity-fold-preserve-fixture-corpus-authoring-gives"

[[reference]]
id = "ref-testing-proof-same-named-generated-crates-silently-share-cargo-fingerprint-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.proof.same-named-generated-crates-silently-share-cargo-fingerprint"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-proof-same-named-generated-crates-silently-share-cargo-fingerprint-roadmap-h19b0d970320ceb3f498f1f90cf31f7068da61668d960ddabc101ed40cc62a3cb"
source = "testing.proof.same-named-generated-crates-silently-share-cargo-fingerprint"
kind = "roadmap"
target_id = "testing.same-named-generated-crates-silently-share-cargo-fingerprint"

[[reference]]
id = "ref-testing-proof-warm-up-refresh-dep-universe-lock-trust-fresh-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.proof.warm-up-refresh-dep-universe-lock-trust-fresh"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-proof-warm-up-refresh-dep-universe-lock-trust-fresh-roadmap-hfaf405b8952df1839f312626a996776d1cc6524cf548ffe70bb4d9130e59005e"
source = "testing.proof.warm-up-refresh-dep-universe-lock-trust-fresh"
kind = "roadmap"
target_id = "testing.warm-up-refresh-dep-universe-lock-trust-fresh"

[[reference]]
id = "ref-testing-quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.quickcheck-alongside-proptest-goldenfile-expect-test-second-corpus"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-reason-keyed-rejection-evidence-reject-catalogs-proven-near-test-symbol-h2530dd33835f16e026c5d69809e5c1a38b20bee1cd27a5ee3514dce5bcdba173"
source = "testing.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::decode_reject_reason_tests::fixture_bare_decode_rejects_hold_at_their_ratchet_baseline"
symbol = "tests::decode_reject_reason_tests::fixture_bare_decode_rejects_hold_at_their_ratchet_baseline"

[[reference]]
id = "ref-testing-referencing-context-measured-wrapping-deep-honored-base-shape-test-symbol-hdf54feb89788ee827c0fa1a14327f84c1b89c8d44f94ff33911f27e63f2a6253"
source = "testing.referencing-context-measured-wrapping-deep-honored-base-shape"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::referencing_context_tests::directive_referencing_context_sweep"
symbol = "tests::referencing_context_tests::directive_referencing_context_sweep"

[[reference]]
id = "ref-testing-representation-changing-directive-goes-live-new-container-ship-test-symbol-h8fb1437d51762065bee21742c2489192a5e89006867fe660038c9f29c5b1229f"
source = "testing.representation-changing-directive-goes-live-new-container-ship"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::extern_import_tests::extern_import_flavored_generic_base_projects_without_the_tag"
symbol = "tests::extern_import_tests::extern_import_flavored_generic_base_projects_without_the_tag"

[[reference]]
id = "ref-testing-restructuring-features-std-tool-owned-sub-feature-cddl-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.restructuring-features-std-tool-owned-sub-feature-cddl"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-restructuring-features-std-tool-owned-sub-feature-cddl-authority"
source = "testing.restructuring-features-std-tool-owned-sub-feature-cddl"
kind = "roadmap"
target_id = "testing.guard.restructuring-features-std-tool-owned-sub-feature-cddl"

[[reference]]
id = "ref-testing-rule-trailing-reviewed-fork-publication-commit"
source = "testing.rule-trailing.reviewed-fork-publication"
kind = "external_commit"
repository = "dcSpark/cddl"
commit = "a7ed0784e89689784ff78ed0e85c7434a3528937"

[[reference]]
id = "ref-testing-ruling-premise-gate-x-stays-green-must-probed-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.ruling-premise-gate-x-stays-green-must-probed"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-run-local-values-gate-cache-key-material-proven-gate-h21dded476e33c8fff7c34a5c9b5adedd959977716912b4e5ca82c6189426aa45"
source = "testing.run-local-values-gate-cache-key-material-proven"
kind = "gate"
gate_id = "verify_cache_transparency"

[[reference]]
id = "ref-testing-schema-document-s-name-injectivity-enforced-row-side-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.schema-document-s-name-injectivity-enforced-row-side"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-schema-name-collision-loser-no-row-same-id-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.schema-name.collision-loser-no-row-same-id"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-schema-name-cross-crate-same-id-collision-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.schema-name.cross-crate-same-id-collision"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-schema-name-two-rowless-types-collide-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.schema-name.two-rowless-types-collide"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-shared-cargo-target-dir-across-same-named-scratch-operating"
source = "testing.shared-cargo-target-dir-across-same-named-scratch"
kind = "file_heading"
path = "AGENTS.md"
heading = "Build & verify"

[[reference]]
id = "ref-testing-source-observation-add-profile-wrapper-participation-grid-participation-differs-across-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.add-profile-wrapper-participation-grid-participation-differs-across"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-alloc-imports-nested-inline-modules-hand-carried-design-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.alloc-imports-nested-inline-modules-hand-carried-design"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-armed-idle-harness-arms-empty-head-ledgers-zero-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.armed-idle-harness-arms-empty-head-ledgers-zero"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-assert-hand-authored-json-schema-against-type-s-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.assert-hand-authored-json-schema-against-type-s"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-backgrounded-check-ts-full-launched-sub-agent-s-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.source-observation.backgrounded-check-ts-full-launched-sub-agent-s"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-source-observation-cargo-feature-generated-crate-s-dependency-change-what-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.cargo-feature-generated-crate-s-dependency-change-what"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-cddl-module-directives-draft-ietf-cbor-cddl-modules-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.cddl-module-directives-draft-ietf-cbor-cddl-modules"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-citation-resolves-naming-wrong-thing-invisible-citation-lint-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.citation-resolves-naming-wrong-thing-invisible-citation-lint"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-comment-residue-false-matches-text-scans-over-emitted-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.comment-residue-false-matches-text-scans-over-emitted"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-component-js-probe-reopening-signal-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.component-js-probe-reopening-signal"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-composing-registration-rows-across-generation-passes-share-output-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.composing-registration-rows-across-generation-passes-share-output"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-config-run-s-convergence-warning-name-crate-already-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.source-observation.config-run-s-convergence-warning-name-crate-already"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-source-observation-container-combinations-reopening-signal-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.container-combinations-reopening-signal"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-container-construct-conceptual-type-visitor-walks-flat-combinatorial-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.container-construct-conceptual-type-visitor-walks-flat-combinatorial"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-containment-note-s-stated-reason-go-false-any-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.containment-note-s-stated-reason-go-false-any"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-control-bytes-committed-source-files-proven-instance-machinery-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.control-bytes-committed-source-files-proven-instance-machinery"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-corpus-wide-regen-over-prior-output-sweep-reaches-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.corpus-wide-regen-over-prior-output-sweep-reaches"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-crate-generates-json-schema-document-aggregate-package-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.crate-generates-json-schema-document-aggregate-package"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-cross-version-preserve-vectors-beyond-std-alloc-rewrite-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.cross-version-preserve-vectors-beyond-std-alloc-rewrite"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-dependency-swap-pass-compile-parity-existing-gate-silently-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.dependency-swap-pass-compile-parity-existing-gate-silently"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-dependency-version-range-resolution-untested-generated-crates-float-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.dependency-version-range-resolution-untested-generated-crates-float"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-directive-dependent-emitter-edge-case-triggering-shape-hand-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.directive-dependent-emitter-edge-case-triggering-shape-hand"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-directive-s-documented-sentence-stay-true-consumer-visible-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.directive-s-documented-sentence-stay-true-consumer-visible"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-docs-vs-behavior-conformance-harness-comment-dsl-mdx-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.source-observation.docs-vs-behavior-conformance-harness-comment-dsl-mdx"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-source-observation-documented-failure-shape-exercises-prose-cross-crate-int-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.documented-failure-shape-exercises-prose-cross-crate-int"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-documented-flag-pairing-broken-fixture-stays-green-because-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.documented-flag-pairing-broken-fixture-stays-green-because"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-doubled-doc-comment-markers-emitted-docs-proven-instance-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.doubled-doc-comment-markers-emitted-docs-proven-instance"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-dsl-position-tests-string-level-design-green-emitted-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.dsl-position-tests-string-level-design-green-emitted"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-duplicates-dedicated-generic-collision-message-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.duplicates-dedicated-generic-collision-message"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-duplicates-member-position-extension-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.duplicates-member-position-extension"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-duplicates-occurrence-aware-generic-identity-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.duplicates-occurrence-aware-generic-identity"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-duplicates-same-shape-set-conversion-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.duplicates-same-shape-set-conversion"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-edn-text-notation-round-trip-edn-edn-draft-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.edn-text-notation-round-trip-edn-edn-draft"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-emission-branch-generated-output-fixture-exercises-invisible-gate-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.emission-branch-generated-output-fixture-exercises-invisible-gate"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-emit-tests-gate-asserts-round-trip-green-cannot-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.emit-tests-gate-asserts-round-trip-green-cannot"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-emitted-shape-lint-classes-outside-clippy-all-beyond-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.emitted-shape-lint-classes-outside-clippy-all-beyond"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-emitter-drops-overloadable-parameter-building-fresh-config-invisible-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.emitter-drops-overloadable-parameter-building-fresh-config-invisible"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-extend-recombination-member-kind-table-optional-fixed-shape-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.extend-recombination-member-kind-table-optional-fixed-shape"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-extern-deps-wasm-boundary-surface-packaging-json-gen-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.extern-deps-wasm-boundary-surface-packaging-json-gen"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-extern-interface-export-dialect-v2-candidates-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.extern-interface-export-dialect-v2-candidates"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-extreme-value-boundary-correctness-generation-time-encode-paths-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.extreme-value-boundary-correctness-generation-time-encode-paths"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-file-listing-json-schema-root-values-flag-per-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.file-listing-json-schema-root-values-flag-per"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-finding-s-defect-side-attribution-recorded-narrative-plausibility-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.finding-s-defect-side-attribution-recorded-narrative-plausibility"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-fixture-models-obsolete-user-contract-stays-green-real-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.fixture-models-obsolete-user-contract-stays-green-real"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-fixture-s-verdict-accident-cargo-feature-unification-harness-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.fixture-s-verdict-accident-cargo-feature-unification-harness"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-fixture-shape-evicted-over-known-defect-stale-guard-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.fixture-shape-evicted-over-known-defect-stale-guard"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-full-n-flag-powerset-pict-pairwise-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.source-observation.full-n-flag-powerset-pict-pairwise"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-source-observation-gate-cache-all-hit-cost-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.source-observation.gate-cache-all-hit-cost"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-source-observation-gate-cache-closure-audit-structurally-blind-gate-authored-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.gate-cache-closure-audit-structurally-blind-gate-authored"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-gate-cache-closure-audit-traced-set-extension-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.source-observation.gate-cache-closure-audit-traced-set-extension"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-source-observation-gate-cache-coverage-extensions-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.source-observation.gate-cache-coverage-extensions"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-source-observation-gate-exists-registry-nowhere-tests-readme-md-hand-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.gate-exists-registry-nowhere-tests-readme-md-hand"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-gate-script-health-cache-staleness-key-fresh-worktree-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.gate-script-health-cache-staleness-key-fresh-worktree"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-generated-local-collision-general-mangling-remedy-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.generated-local-collision-general-mangling-remedy"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-generated-local-collision-scope-wide-probe-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.generated-local-collision-scope-wide-probe"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-grammar-fuzzer-arbitrary-supported-ast-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.grammar-fuzzer-arbitrary-supported-ast"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-grammar-fuzzer-batch-masking-detector-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.grammar-fuzzer-batch-masking-detector"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-grammar-fuzzer-real-world-corpus-differential-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.grammar-fuzzer-real-world-corpus-differential"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-hand-enumerated-ban-pattern-list-authority-lives-gitignored-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.hand-enumerated-ban-pattern-list-authority-lives-gitignored"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-hand-maintained-source-dependency-mapping-manifest-changeset-silently-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.hand-maintained-source-dependency-mapping-manifest-changeset-silently"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-json2ts-artifact-doc-comments-naming-undefined-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.json2ts-artifact-doc-comments-naming-undefined"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-lint-doc-citations-accepts-docs-heading-durable-citation-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.lint-doc-citations-accepts-docs-heading-durable-citation"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-logic-tool-emits-source-text-outside-layer-reasons-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.logic-tool-emits-source-text-outside-layer-reasons"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-machine-readable-ownership-co-owned-features-std-list-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.machine-readable-ownership-co-owned-features-std-list"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-maintainer-ruling-force-convenience-cbor-bytes-door-turns-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.maintainer-ruling-force-convenience-cbor-bytes-door-turns"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-make-fixture-comment-s-claimed-wire-bytes-executable-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.make-fixture-comment-s-claimed-wire-bytes-executable"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-making-type-choice-path-reject-duplicated-explicit-name-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.source-observation.making-type-choice-path-reject-duplicated-explicit-name"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-source-observation-matrix-s-note-names-panic-site-aborts-nothing-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.matrix-s-note-names-panic-site-aborts-nothing"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-mechanical-layers-two-review-owned-design-rules-tests-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.mechanical-layers-two-review-owned-design-rules-tests"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-message-s-level-classification-review-owned-two-ways-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.message-s-level-classification-review-owned-two-ways"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-msrv-declaration-os-matrix-generated-code-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.source-observation.msrv-declaration-os-matrix-generated-code"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-source-observation-multifile-reference-position-coverage-two-position-keyed-escapes-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.multifile-reference-position-coverage-two-position-keyed-escapes"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-nothing-asserts-how-many-times-rejection-message-emitted-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.nothing-asserts-how-many-times-rejection-message-emitted"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-nothing-asserts-two-ir-sites-cannot-mint-same-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.nothing-asserts-two-ir-sites-cannot-mint-same"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-observed-baseline-comments-beside-gate-floors-rot-silently-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.observed-baseline-comments-beside-gate-floors-rot-silently"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-opt-rfc-faithful-total-cbor-json-rendering-json-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.opt-rfc-faithful-total-cbor-json-rendering-json"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-ownership-journal-making-dropped-std-forward-dep-converge-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.source-observation.ownership-journal-making-dropped-std-forward-dep-converge"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-source-observation-panic-site-un-shadowing-converting-shallow-panic-graceful-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.panic-site-un-shadowing-converting-shallow-panic-graceful"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-parallel-constructor-fixture-diversity-parser-over-external-input-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.parallel-constructor-fixture-diversity-parser-over-external-input"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-prettyplease-shelling-rustfmt-file-heading-h527e280bc2bd81c1094c49f706d73fc6341eae61070b511ed7e7c7a2114aac2d"
source = "testing.source-observation.prettyplease-shelling-rustfmt"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Pending maintainer action"

[[reference]]
id = "ref-testing-source-observation-probe-component-face-s-js-surface-axes-js-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.probe-component-face-s-js-surface-axes-js"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-process-boundary-another-profile-escape-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.process-boundary-another-profile-escape"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-process-boundary-rustc-after-panic-fix-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.process-boundary-rustc-after-panic-fix"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-process-boundary-rustfmt-pre-rustc-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.process-boundary-rustfmt-pre-rustc"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-profile-driven-compile-gate-covers-compositions-profiles-specs-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.profile-driven-compile-gate-covers-compositions-profiles-specs"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-project-json-gen-documents-run-test-fixture-writes-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.project-json-gen-documents-run-test-fixture-writes"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-propagating-deliberately-unpublished-type-s-intent-json-ts-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.propagating-deliberately-unpublished-type-s-intent-json-ts"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-reason-keyed-rejection-evidence-reject-catalogs-proven-near-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.reason-keyed-rejection-evidence-reject-catalogs-proven-near"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-referencing-context-measured-wrapping-deep-honored-base-shape-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.referencing-context-measured-wrapping-deep-honored-base-shape"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-representation-changing-directive-goes-live-new-container-ship-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.representation-changing-directive-goes-live-new-container-ship"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-rule-trailing-directive-classification-file-heading-h527e280bc2bd81c1094c49f706d73fc6341eae61070b511ed7e7c7a2114aac2d"
source = "testing.source-observation.rule-trailing-directive-classification"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Pending maintainer action"

[[reference]]
id = "ref-testing-source-observation-ruling-premise-gate-x-stays-green-must-probed-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.ruling-premise-gate-x-stays-green-must-probed"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-run-local-values-gate-cache-key-material-proven-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.run-local-values-gate-cache-key-material-proven"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-rustfmt-seam-error-leg-still-witness-needs-subprocess-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.rustfmt-seam-error-leg-still-witness-needs-subprocess"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-schema-name-collision-loser-no-row-same-id-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.schema-name-collision-loser-no-row-same-id"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-schema-name-cross-crate-same-id-collision-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.schema-name-cross-crate-same-id-collision"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-schema-name-two-rowless-types-collide-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.schema-name-two-rowless-types-collide"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-scratch-retention-reopening-signal-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.source-observation.scratch-retention-reopening-signal"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-source-observation-stale-known-limitation-prose-surviving-fix-finding-ledgered-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.stale-known-limitation-prose-surviving-fix-finding-ledgered"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-std-generated-crates-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.std-generated-crates"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-synthesized-name-referenced-never-minted-e0425-flavor-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.synthesized-name-referenced-never-minted-e0425-flavor"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-tag-set-inline-anonymous-choice-boundary-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.tag-set-inline-anonymous-choice-boundary"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-tier-memory-spend-measurements-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.source-observation.tier-memory-spend-measurements"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-source-observation-tier-s-disk-bandwidth-unmeasured-anything-scratch-floor-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.source-observation.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-source-observation-tier-s-peak-memory-bounded-arithmetic-over-assumed-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.source-observation.tier-s-peak-memory-bounded-arithmetic-over-assumed"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-source-observation-tree-wide-sweeps-over-gitignored-regeneration-targets-count-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-twin-implementation-comment-ast-change-cadence-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.twin-implementation-comment-ast-change-cadence"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-twin-implementation-read-caught-instance-trigger-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.twin-implementation-read-caught-instance-trigger"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-twin-repo-implementations-semantic-drifting-apart-emission-spellings-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-unused-imports-generated-crates-trait-import-class-name-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.unused-imports-generated-crates-trait-import-class-name"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-value-anchor-rot-field-addition-delivery-grows-existing-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.value-anchor-rot-field-addition-delivery-grows-existing"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-verify-ts-warm-up-cargo-test-exit-attributed-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.source-observation.verify-ts-warm-up-cargo-test-exit-attributed"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-source-observation-wac-composed-component-transpiled-jco-loses-cross-instance-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.wac-composed-component-transpiled-jco-loses-cross-instance"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-wasm-door-vocabulary-reopening-signal-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.source-observation.wasm-door-vocabulary-reopening-signal"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-source-observation-wasm32-link-floor-deferred-reject-twin-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.wasm32-link-floor-deferred-reject-twin"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-workspace-mode-wasm32-wasip2-build-gate-both-faces-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.workspace-mode-wasm32-wasip2-build-gate-both-faces"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-workspace-wasip2-reopening-signal-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.source-observation.workspace-wasip2-reopening-signal"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-source-observation-wrapper-name-user-set-name-synthesized-collection-wrapper-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.source-observation.wrapper-name-user-set-name-synthesized-collection-wrapper"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-stale-known-limitation-prose-surviving-fix-finding-ledgered-gate-h3e599ca304737456b28414057bd77659676f5f807544e7c8d75f8d3d62d80797"
source = "testing.stale-known-limitation-prose-surviving-fix-finding-ledgered"
kind = "gate"
gate_id = "lint_doc_citations"

[[reference]]
id = "ref-testing-stale-known-limitation-prose-surviving-fix-finding-ledgered-test-symbol-hf56c41fd40492d55461f131282a45332d6dbcf25870414ace33425828a1011dd"
source = "testing.stale-known-limitation-prose-surviving-fix-finding-ledgered"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::facade_composition_compiles"
symbol = "tests::integration_tests::facade_composition_compiles"

[[reference]]
id = "ref-testing-stale-known-limitation-prose-surviving-fix-finding-ledgered-test-symbol-h003eb69773f7ec4bdad0526ab52208064f423d1ae952f6e6facb0f984e0a1fcf"
source = "testing.stale-known-limitation-prose-surviving-fix-finding-ledgered"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::integration_extern_only_scope_declared_in_root"
symbol = "tests::integration_tests::integration_extern_only_scope_declared_in_root"

[[reference]]
id = "ref-testing-synthesized-name-referenced-never-minted-e0425-flavor-test-symbol-hf32dc18af1b80049eaeceee59c2af22af5f31da3ff3353fe26b6ba5c0de46eb9"
source = "testing.synthesized-name-referenced-never-minted-e0425-flavor"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::synthesized_name_interaction_sweep"
symbol = "tests::integration_tests::synthesized_name_interaction_sweep"

[[reference]]
id = "ref-testing-tag-set-non-idiom-generic-boundary-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.tag-set.non-idiom-generic-boundary"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-tag-set-non-idiom-generic-boundary-authority"
source = "testing.tag-set.non-idiom-generic-boundary"
kind = "roadmap"
target_id = "testing.guard.tag-set-non-idiom-generic-boundary"

[[reference]]
id = "ref-testing-three-defect-classes-process-test-see-sharing-remedy-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.three-defect-classes-process-test-see-sharing-remedy"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-three-defect-classes-process-test-see-sharing-remedy-gate-h2aa524e34e8f48375ee9e9e45104902167fda5d035c9fedb1f623ce234697733"
source = "testing.three-defect-classes-process-test-see-sharing-remedy"
kind = "gate"
gate_id = "all_supported_constructs_generate_all_profiles"

[[reference]]
id = "ref-testing-three-defect-classes-process-test-see-sharing-remedy-gate-h20a465abab9bd99b2b0f658fa9420b1e76d6c46d673b0313e7b503ed2ed0da56"
source = "testing.three-defect-classes-process-test-see-sharing-remedy"
kind = "gate"
gate_id = "feature_corpus_roundtrips_nondefault_profiles"

[[reference]]
id = "ref-testing-three-defect-classes-process-test-see-sharing-remedy-test-symbol-h5d60b6dffc0a736899b02c6bd28d78ab5d21cef804a8f5da7975afb9383ebe34"
source = "testing.three-defect-classes-process-test-see-sharing-remedy"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::all_supported_constructs_generate_all_profiles"
symbol = "tests::integration_tests::all_supported_constructs_generate_all_profiles"

[[reference]]
id = "ref-testing-three-defect-classes-process-test-see-sharing-remedy-test-symbol-h894f1ec250eb93e74a6dc867e73684072291d7bdb8d2264e3990ee2ea2f181f6"
source = "testing.three-defect-classes-process-test-see-sharing-remedy"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::feature_corpus_roundtrips_nondefault_profiles"
symbol = "tests::integration_tests::feature_corpus_roundtrips_nondefault_profiles"

[[reference]]
id = "ref-testing-tier-s-disk-bandwidth-unmeasured-anything-scratch-floor-file-heading-he05a163471131cbfda71d664fd575398611216b2c65b7e7910f984498d0a7e02"
source = "testing.tier-s-disk-bandwidth-unmeasured-anything-scratch-floor"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Operational watches"

[[reference]]
id = "ref-testing-tier-s-peak-memory-bounded-arithmetic-over-assumed-rule"
source = "testing.tier-s-peak-memory-bounded-arithmetic-over-assumed"
kind = "file_heading"
path = "AGENTS.md"
heading = "Build & verify"

[[reference]]
id = "ref-testing-tier-s-peak-memory-bounded-arithmetic-over-assumed-retirement"
source = "testing.tier-s-peak-memory-bounded-arithmetic-over-assumed"
kind = "file_heading"
path = "tests/README.md"
heading = "Gate-level concurrency (registry-declared, opt-in)"

[[reference]]
id = "ref-testing-tooling-generated-coverage-instrumentation-declined-heading"
source = "testing.tooling.generated-coverage-instrumentation"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-tooling-goldenfile-expect-test-corpus-declined-heading"
source = "testing.tooling.goldenfile-expect-test-corpus"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-tooling-no-panic-lints-declined-heading"
source = "testing.tooling.no-panic-lints"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-tooling-quickcheck-parallel-corpus-declined-heading"
source = "testing.tooling.quickcheck-parallel-corpus"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-tooling-trybuild-whole-crate-declined-heading"
source = "testing.tooling.trybuild-whole-crate"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-transparent-tag-set-idiom-recognized-shape-boundary-request-file-heading-h893d22e3d517c841e39837c99dc4b74cfa7efc5d5ec40fa4c0eb70ccac88ea39"
source = "testing.transparent-tag-set-idiom-recognized-shape-boundary-request"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Deferred features (build when a real consumer needs them)"

[[reference]]
id = "ref-testing-tree-wide-sweeps-over-gitignored-regeneration-targets-count-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.tree-wide-sweeps-over-gitignored-regeneration-targets-count"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-twin-implementation-comment-ast-change-cadence-parent"
source = "testing.twin-implementation.comment-ast-change-cadence"
kind = "roadmap"
target_id = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"

[[reference]]
id = "ref-testing-twin-implementation-own-reviewed-change-rule-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.twin-implementation.own-reviewed-change-rule"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-twin-repo-implementations-semantic-drifting-apart-emission-spellings-gate-h6a27f79888f19ab92549180ecdcdf05a24145020e97ee6233a1528a47d151786"
source = "testing.twin-repo-implementations-semantic-drifting-apart-emission-spellings"
kind = "gate"
gate_id = "project_corpus"

[[reference]]
id = "ref-testing-unused-imports-generated-crates-trait-import-class-name-test-symbol-h4c5e041e97a704e70c4a95276f741dd0e153a6e8cab36dea83486c0d206018f8"
source = "testing.unused-imports-generated-crates-trait-import-class-name"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::extern_deps"
symbol = "tests::integration_tests::extern_deps"

[[reference]]
id = "ref-testing-unused-imports-generated-crates-trait-import-class-name-test-symbol-haf7edde0b79ace73f9b150c48055de3ca3348b998530b48a7b91250a394d3404"
source = "testing.unused-imports-generated-crates-trait-import-class-name"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::generated_code_clippy_clean"
symbol = "tests::integration_tests::generated_code_clippy_clean"

[[reference]]
id = "ref-testing-unused-imports-generated-crates-trait-import-class-name-test-symbol-ha47702162c05a22c06c0676347a748cb80857be1127f1cac35ec718cfcd11649"
source = "testing.unused-imports-generated-crates-trait-import-class-name"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::workspace_requests_cohosted_keys_list_no_self_import"
symbol = "tests::integration_tests::workspace_requests_cohosted_keys_list_no_self_import"

[[reference]]
id = "ref-testing-unused-imports-generated-crates-trait-import-class-name-test-symbol-h7e78d3856f7562dfcfd3c5ebf1df8d403b1ef99cbbdc960b64e689499bbbf6b2"
source = "testing.unused-imports-generated-crates-trait-import-class-name"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::workspace_requests_hosts_borrowed_wrappers"
symbol = "tests::integration_tests::workspace_requests_hosts_borrowed_wrappers"

[[reference]]
id = "ref-testing-unused-imports-generated-crates-trait-import-class-name-test-symbol-h787d2bf07c1dae413f902528cf38ac20971ed846b263b6b5e3a58b2920d1abbc"
source = "testing.unused-imports-generated-crates-trait-import-class-name"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::workspace_requests_hosts_cross_scope_elements"
symbol = "tests::integration_tests::workspace_requests_hosts_cross_scope_elements"

[[reference]]
id = "ref-testing-value-anchor-rot-field-addition-delivery-grows-existing-file-heading-h1870867f5d3f2e8d012a5442c1984fec97b86166b6180574b291bea45497561d"
source = "testing.value-anchor-rot-field-addition-delivery-grows-existing"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Next work items, in priority order"

[[reference]]
id = "ref-testing-verify-ts-warm-up-cargo-test-exit-attributed-rule"
source = "testing.verify-ts-warm-up-cargo-test-exit-attributed"
kind = "file_heading"
path = "AGENTS.md"
heading = "Build & verify"

[[reference]]
id = "ref-testing-wac-composed-component-transpiled-jco-loses-cross-instance-gate-hf2e5bf53c50997692623794ce66965b548397ac6f3cfba48d34d3000ed57b7b4"
source = "testing.wac-composed-component-transpiled-jco-loses-cross-instance"
kind = "gate"
gate_id = "component_compose"

[[reference]]
id = "ref-testing-wac-composed-component-transpiled-jco-loses-cross-instance-gate-hc8c9ea0a21b96b2f9e6b028ca524ff1535227e2dbb5c9ab52a8e7f9c3e30eec1"
source = "testing.wac-composed-component-transpiled-jco-loses-cross-instance"
kind = "gate"
gate_id = "component_jco"

[[reference]]
id = "ref-testing-working-tree-gate-run-concurrent-live-implementation-agent-operating"
source = "testing.working-tree-gate-run-concurrent-live-implementation-agent"
kind = "file_heading"
path = "AGENTS.md"
heading = "Build & verify"

[[reference]]
id = "ref-testing-wrapper-name-user-set-name-synthesized-collection-wrapper-file-heading-h1566c3deb4d7b978ec42e365111a76a1e4e863f7c993f9d2c6e69c565d0a2e9e"
source = "testing.wrapper-name-user-set-name-synthesized-collection-wrapper"
kind = "file_heading"
path = "tests/TESTING_ROADMAP.md"
heading = "Declined (decided, with a reopening signal unless explicitly permanent)"

[[reference]]
id = "ref-testing-wrapper-name-user-set-name-synthesized-collection-wrapper-authority"
source = "testing.wrapper-name-user-set-name-synthesized-collection-wrapper"
kind = "roadmap"
target_id = "testing.guard.wrapper-name-user-set-name-synthesized-collection-wrapper"

[[reference]]
id = "ref-testing-wrapper-name-user-set-name-synthesized-collection-wrapper-test-symbol-ha14405d607785cd4d3d73c9308ab683fefb24decf97f9ef1d259f601e0355dd2"
source = "testing.wrapper-name-user-set-name-synthesized-collection-wrapper"
kind = "test_symbol"
test_id = "rust-test:cddl-codegen#tests::integration_tests::extern_companions_defers_to_sibling_wasm_crate"
symbol = "tests::integration_tests::extern_companions_defers_to_sibling_wasm_crate"
