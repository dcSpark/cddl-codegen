# Append-only change log for the generated `rust/Cargo.toml`.
# APPEND-ONLY: never edit, delete, or renumber an existing entry — change = append (last write
# wins), removal = append `remove = true`. Format and rules: README.md in this folder.

[[change]]
id = 1
path = "package.name"
set = '"cddl-lib"'

[[change]]
id = 2
path = "package.version"
seed = '"0.1.0"'

[[change]]
id = 3
path = "package.edition"
set = '"2024"'

[[change]]
id = 4
path = "lib.crate-type"
set = '["cdylib", "rlib"]'

[[change]]
id = 5
path = "dependencies.cbor_event"
set = '"2.4.0"'

[[change]]
id = 6
path = "dependencies.cbor_event"
set = '"3.2.0"'

# dcSpark fork pinned to the `float-round-trip` branch head ("Fix handling of float heads", same
# rev the MAIN crate's Cargo.toml pins): adds the width-preserving `float_sz`/`write_float_sz` API,
# rejects reserved simple values `0xfc-0xfe` at read, and carries the float surface the runtime
# reads against — `se::smallest_float_sz`, `se::f64_to_f32_exact`, `se::f32_to_f64_exact`.
# Generated crates need it once they carry the `AnyCbor` runtime type (loose CBOR parsing), whose
# float slot is `Float(f64, Option<Sz>)`. Interim measure — move this back to a crates.io version
# `set` once the fork upstreams to primetype/cbor_event + releases.
# NOTE for that flip: merge_dep_spec treats a version-only `set` as a floor that PRESERVES an
# existing git source on already-written manifests — the flip must also clear `git`/`rev` (see
# the source-assertion contract in src/cargo_manifest.rs).
# (Corrected in place 2026-08-03: the rev this entry originally set was never reachable from any
# remote — a fresh checkout could not resolve it. See README "Correcting a never-valid value";
# the `pin_cold_fetch` gate now proves every mentioned rev against its remote.)
[[change]]
id = 7
path = "dependencies.cbor_event"
set = '{ git = "https://github.com/dcSpark/cbor_event", rev = "28514916e97240e1d2b01ba8f804d45c221fbdcd" }'

# The generated crate's `std` feature: default-ON and emitted UNCONDITIONALLY, so every crate this
# tool writes has the same manifest shape and a plain `cargo build` is unchanged. It gates the few
# genuinely-std pieces of the runtime from inside the crate; a no_std consumer depends on the crate
# with `default-features = false`.
#
# The `[]` below is only the BASE. `ops_for_rust` overrides `features.std` every run with a COMPUTED
# list: `<pkg>/std` for each `--std-forward-dep` path dependency, then for each third-party dep this
# flag set ships in alloc mode and which has a `std` feature to name (`serde`, `serde_json`,
# `schemars`, `hex`). A per-run computed op can do what a static log entry could not — the run that
# emits a manifest knows every dep it wrote, so it never names an absent one. `hashlink` and the
# pinned `cbor_event` fork declare no `std` feature at all, so nothing forwards to them.
#
# `features.default` and `features.std` are the two NON-dependency paths `apply` merges instead of
# replacing (union, the user's entries first), so a hand-customized list keeps its entries and gains
# ours. `features.std` additionally PRUNES any `<pkg>/<feat>` entry whose `<pkg>` is not in
# `[dependencies]` when the op applies — the tool's std list varies with the flags, so a flag flip
# would otherwise strand yesterday's forward beside a tombstoned dep, which cargo rejects.
#
# The `default` union is monotone — `"std"` cannot be durably removed there, which is exactly why the
# supported no_std path is `default-features = false` at the DEPENDANT rather than an edit to it.
[[change]]
id = 8
path = "features.default"
set = '["std"]'

[[change]]
id = 9
path = "features.std"
set = '[]'

# Back to a crates.io version, which is what entry 7 called an interim measure: `cbor_event 3.3.0`
# (released 2026-08-03) publishes the fork's `float-round-trip` head — its `src/` is byte-identical
# to rev `28514916…` — so the float surface the runtime reads against (`se::smallest_float_sz`,
# `se::f64_to_f32_exact`, `se::f32_to_f64_exact`, `write_float_sz`) and the `0xfc-0xfe` rejection
# now come from the registry. `rust-version` is `1.96.1` on both sides, and 3.3.0 declares no
# features at all, so the computed `features.std` list still has nothing to forward here.
# `assert_source` is load-bearing, not decoration: a version-only `set` is a FLOOR, so without it
# `merge_dep_spec` would preserve the `{ git, rev }` entry already written into every consumer
# manifest and the flip would land only on fresh output. See the source-assertion contract in
# src/cargo_manifest.rs.
[[change]]
id = 10
path = "dependencies.cbor_event"
set = '"3.3.0"'
assert_source = true
